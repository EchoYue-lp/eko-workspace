# A-INP-01 / V01-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: not-applicable
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only inspection)

## Claim

`PreparedUserTurn`, `UserTurnInput`, `InputResourceRef`, and the
attachment primitives (`AttachmentRef`, `AttachmentData`,
`AttachmentSource`, `build_message_from_refs`) are each defined exactly
once across the `echo-agent` and `echo-agent-cli` repositories, with no
parallel/duplicate implementation hiding elsewhere.

## Method

Static grep across both repositories. Working directory:
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
1. Definition search:
   grep -rn "pub struct PreparedUserTurn\|pub struct UserTurnInput\|pub struct InputResourceRef\|pub struct AttachmentRef\|pub struct AttachmentData\|enum AttachmentSource\|fn build_message_from_refs\|fn should_spill\|fn spill_to_artifact\|fn cleanup_user_input_scope\|fn resolve_user_input_spill_dir" \
       echo-agent echo-agent-cli --include="*.rs"

2. Type-name token search (to catch shadowing or wrappers):
   grep -rn "PreparedUserTurn\|UserTurnInput\|InputResourceRef" \
       echo-agent echo-agent-cli --include="*.rs" | grep -v test
```

## Expected Result

- Each type / function has exactly one definition site.
- No second `PreparedUserTurn`-like abstraction exists in the framework.
- `AttachmentRef` and `build_message_from_refs` are unique to the
  application layer.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary: every target symbol has exactly one definition.
  - `pub struct PreparedUserTurn` — `echo-agent-cli/echo-agent-app-core/src/prepared_turn.rs:221` (single).
  - `pub struct UserTurnInput` — `prepared_turn.rs:232` (single).
  - `pub struct InputResourceRef` — `prepared_turn.rs:191` (single).
  - `pub struct AttachmentRef` — `echo-agent-cli/echo-agent-app-core/src/attachments.rs:204` (single).
  - `pub struct AttachmentData` — `echo-agent-cli/echo-agent-app-core/src/types/request.rs:163` (single; `types::AttachmentData` re-export).
  - `enum AttachmentSource` — `types/request.rs:155` (single).
  - `pub fn build_message_from_refs` — `attachments.rs:235` (single).
  - `fn should_spill` — `prepared_turn.rs:389` (single).
  - `fn spill_to_artifact` — `prepared_turn.rs:517` (single).
  - `pub fn cleanup_user_input_scope` — `prepared_turn.rs:150` (single).
  - `pub fn resolve_user_input_spill_dir` — `prepared_turn.rs:100` (single).

  Framework-side nearest neighbours (deliberately generic primitives,
  not duplicates):
  - `echo_agent::memory::project_message` / `restore_message` — used by
    `prepared_turn.rs:868-876` test only.
  - `echo_agent::tools::artifact::cleanup_tool_output_scope` — mirrored
    by `cleanup_user_input_scope`; the docstring at
    `prepared_turn.rs:148-149` explicitly references it.
  - `echo_agent::tools::artifact::ToolOutputArtifactConfig` — provides
    `root_dir` reused as parent of `user-input/` (see V03-02).

## Deviations

None.

## Conclusion

Supports the Layering Decision section of A-INP-01: there is exactly one
application-layer normalization type (`PreparedUserTurn`) and one
attachment identity type (`AttachmentRef`). No duplicate authority was
found. Supports the negative half of A-INP-01-P2-01 (the divergence is
not from a duplicate type — it is from one entry point skipping the
single type).

## Follow-Up

None. V02-01 traces the live callers of these symbols.
