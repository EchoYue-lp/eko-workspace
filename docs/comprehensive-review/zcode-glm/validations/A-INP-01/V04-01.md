# A-INP-01 / V04-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: not-applicable
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review; tests executed against the
> committed b3b2e81 build)

## Claim

The unit tests covering `prepared_turn` and `attachments` pass cleanly
on the reviewed commit. Together they cover: short-text passthrough,
mode-hint folding, byte and token spill thresholds, CJK /
emoji-safe preview, attachment image inlining, paste-as-instruction
spill, uploaded-text-as-data spill, framework projection round-trip,
path-component sanitization, scope/TTL cleanup, attachment name
sanitization, and image/text-file multimodal round-trip.

## Method

Two cargo test invocations targeting the modules under review. Working
directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli`.

Feature flags: default workspace features (no `--all-features`,
no `--no-default-features`). Rust toolchain: stable, as resolved by the
workspace `rust-toolchain` if present.

```text
cargo test --package echo-agent-app-core --lib prepared_turn::
cargo test --package echo-agent-app-core --lib attachments::
```

## Expected Result

- All targeted tests pass with exit code 0.
- No panics, no `unwrap` failures in production paths exercised by the
  spill / preview / cleanup tests.
- The UTF-8 / CJK / emoji fixtures do not panic during preview
  truncation (load-bearing for the AGENTS.md hard rule).

## Result

- Exit code: 0 for both commands.
- Duration:
  - `prepared_turn::` — finished in 0.03s after a 1.60s incremental
    build.
  - `attachments::` — finished in 0.02s after a 1.33s incremental
    build.
- Summary:
  - `prepared_turn::` — 14 passed, 0 failed, 0 ignored, 635 filtered.
    Tests executed:
    - `mode_hint_is_folded_for_short_text`
    - `path_component_sanitizes_separators`
    - `cleanup_removes_expired_artifacts_and_conversation_scope`
    - `short_text_is_not_spilled`
    - `attachment_is_inlined_as_resource`
    - `should_spill_byte_threshold`
    - `should_spill_token_threshold_for_ascii`
    - `emoji_preview_is_utf8_safe`
    - `reference_instruction_is_utf8_safe`
    - `pasted_text_is_always_a_tool_reference_and_may_contain_instructions`
    - `uploaded_long_text_remains_data_separate_from_instruction`
    - `long_cjk_text_spills_without_byte_slicing`
    - `long_original_message_is_spilled_without_losing_instruction_semantics`
    - `artifact_reference_survives_framework_projection_round_trip`
  - `attachments::` — 4 passed, 0 failed, 0 ignored, 645 filtered.
    Tests executed:
    - `build_message_no_attachments_is_text`
    - `sanitize_rejects_traversal`
    - `save_and_build_image_roundtrip`
    - `save_and_build_text_file`
- Log/artifact: not captured; cargo wrote the summary to stdout.

## Deviations

None. Both commands returned 0; the existing test set exercises every
invariant claimed by V03-01 except the multi-workspace cleanup
divergence (V03-4), which is by construction not covered by an
existing test — it is the subject of A-INP-01-P2-02.

## Conclusion

Supports the behavioral claims in A-INP-01: UTF-8 safety holds (CJK +
emoji fixtures pass), spill thresholds gate correctly on both byte and
token counts, paste-vs-upload provenance is preserved into the
model-facing instruction, framework `project_message` /
`restore_message` round-trip preserves the artifact reference text
part, and cleanup primitives behave for both scope and TTL sweeps.

Does not prove: (a) that all production callers route through
`PreparedUserTurn` (that is V02-01, which fails on the TUI
`steer_active_turn` bypass), or (b) that multi-workspace cleanup
matches (V03-4, opened as P2-02).

## Follow-Up

When A-INP-01-P2-01 is fixed (routing `steer_active_turn` through
`PreparedUserTurn::build`), a new integration test should drive
`handle_enter` with a long `/steer ` paste and assert the spill
reference reaches `agent.steer_input`. That test would be validated in
a new V04-02 attempt report, not by editing this one.
