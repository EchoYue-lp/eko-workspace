# Q-TST-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 3aa7929 (baseline 9b0e0fa + 1 post-baseline M1 commit)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean

## Claim

The `#[ignore]`, flaky-marker, and platform-gated test inventory across both
repositories is fully enumerated, and every gated test is either a documented
opt-in live test or a deliberately-pinned-red regression placeholder — i.e. no
test is silently skipped, and no flaky test is hidden.

## Method

Grep both repositories for skip/gate markers and classify each hit:

```text
cd echo-agent
grep -rn '#\[ignore' --include='*.rs' src/ echo-*/ tests/
grep -rn 'cfg(target_os\|cfg(unix\|cfg(windows\|cfg(not(unix' --include='*.rs' \
  src/ echo-*/
cd ../echo-agent-cli
grep -rn '#\[ignore' --include='*.rs' src/ echo-agent-app-core/src/ src-tauri/src/
grep -rn 'cfg(target_os\|cfg(unix\|cfg(windows' --include='*.rs' \
  src/ echo-agent-app-core/src/
cd web-frontend
grep -rn '\.skip\|describe\.skip\|it\.skip\|test\.skip\|xit(\|xdescribe(' src/
```

## Expected Result

A complete list of every `#[ignore]` test with its reason string; confirmation
that platform `#[cfg(...)]` matches in test context are legitimate per-platform
branches (not silent skips); zero frontend `*.skip`; zero unexplained flaky
markers.

## Result

- Exit code: not-applicable (read-only grep inventory)
- Summary: 6 `#[ignore]` tests total (4 framework, 2 application), all
  documented; 0 frontend skips; platform gates are production branches.

### `#[ignore]` inventory (6)

| File:line | Reason string | Classification |
|---|---|---|
| `echo-agent/src/agent/react/run/stream_channel.rs:2318` | `"M3: truncated stream terminal check (Q-FLT-01-P1-01); red until the fix lands"` | deliberately pinned-red regression placeholder (test asserts the fix; `#[ignore]` until M3 lands) |
| `echo-agent/echo-integration/src/lsp/manager.rs:236` | `"opt-in live LSP smoke test; set EKO_LSP_SMOKE=1"` | documented opt-in live test |
| `echo-agent/echo-tools/src/research/clients.rs:979` | `"opt-in live provider ingestion smoke test; set EKO_PROVIDER_SMOKE=1"` | documented opt-in live test |
| `echo-agent/echo-tools/src/research/clients.rs:1005` | `"requires ZOTERO_API_KEY and ZOTERO_LIBRARY_ID"` | documented credential-gated test |
| `echo-agent-cli/echo-agent-app-core/src/research_connectors.rs:616` | `"opt-in live provider ingestion smoke test; set EKO_PROVIDER_SMOKE=1"` | documented opt-in live test |
| `echo-agent-cli/echo-agent-app-core/src/research_connectors.rs:649` | `"requires ZOTERO_API_KEY and ZOTERO_LIBRARY_ID"` | documented credential-gated test |

All six carry an explicit reason string; none is a bare `#[ignore]`.

### Platform gates in test context

`cfg(target_os = ...)` / `cfg(unix)` / `cfg(not(unix))` matches in test modules
occur in: `echo-execution/src/sandbox/local.rs` (Linux-specific syscall
interception branches), `echo-core/src/tools/artifact.rs`, `echo-core/src/plugin/registry.rs`,
`echo-core/src/project_rules.rs`, and CLI `src/tui/{events,clipboard}.rs`,
`src/tauri/{ipc,desktop}.rs`, `src/tauri/commands/browser.rs`. These are
per-platform production branches exercised inside tests (e.g. asserting the
macOS keybinding, the Linux clipboard path), not silent test skips. No test is
gated off entirely by a platform `cfg`.

### Frontend

Zero `*.skip` / `describe.skip` / `it.skip` / `test.skip` / `xit(` / `xdescribe(`
in `web-frontend/src/`. The single `skipTask` hit (`TaskRuntimePanel.tsx:779`)
is a UI action (user skipping a todo), not a test skip.

### Flaky markers

No `#[ignore = "flaky"]`, no `flaky!` macro, no retry-loop wrappers found. The
one test with deliberate timing control
(`pipeline.rs:1641`, 10ms vs 60ms controlled delays with biased `select!`) is
not marked flaky and is deterministic by construction (the delays only order
the two single-tool stages; the assertion does not race on them).

## Deviations

None. The inventory is exhaustive for the audited trees. The `react_smoke.rs`
integration-test file (`tests/`) is not `#[ignore]`-gated; its limited scope is
documented in a module header comment (stale "deferred" claim — see
Q-TST-01-P1-01), not a skip.

## Conclusion

The ignored/flaky/platform-gated surface is clean: every `#[ignore]` is a
documented opt-in live test or a deliberately-pinned-red placeholder, the
frontend has no skips, and platform gates are legitimate production branches.
No silently-skipped or hidden-flaky test inflates the pass rate. This is a
positive confirmation (no finding) — the credibility risk for this task lies
in the coverage gaps (V01/V04) and the print-only fixture (V02 #6), not in
hidden skips.

## Follow-Up

None.
