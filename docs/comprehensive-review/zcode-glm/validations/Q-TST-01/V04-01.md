# Q-TST-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 3aa7929 (baseline 9b0e0fa + 1 post-baseline M1 commit)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean

## Claim

The mock-driven test suite's pass rate is a credible signal for the production
contracts that matter. Specifically: (a) the F-TST-01 mock-invisibility seams
(single-chunk streaming, usage-on-content-chunk, completion-order batch) are
either resolved or still open as documented; (b) for each still-open seam,
a static negative-control identifies the bug class that ships invisible.

## Method

Trace each F-TST-01 finding against the current mock source, then a static
negative-control (read-only "would the suite catch this regression?"
thought experiment anchored to the tested code). No mutation was executed
(read-only review; AGENTS.md disk/build conservation). Working directory
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
cd echo-agent
git log --oneline 9b0e0fa..HEAD                       # the M1 commit
git show 3aa7929 --stat                                # what M1 touched
grep -n 'StreamChunk\|with_stream_script\|with_delay' src/testing/mock_llm.rs
Read src/testing/mock_llm.rs:400-610                   # new chat_stream two-chunk shape
grep -n 'pub fn with_\|ToolFailure' src/testing/mock_tool.rs
git log --oneline 9b0e0fa..HEAD -- src/testing/mock_agent.rs   # unchanged?
grep -n 'stream::once\|FinalAnswer' src/testing/mock_agent.rs
grep -n 'InitializationFailed' src/testing/mock_agent.rs
grep -rn 'with_stream_script' --include='*.rs' src/    # who uses the new API
```

## Expected Result

A per-seam verdict: RESOLVED (mock now models the contract), PARTIALLY
RESOLVED, or STILL OPEN, each with the negative-control bug class.

## Result

- Exit code: not-applicable (read-only static analysis)
- Summary: of the F-TST-01 invisibility seams, 3 are RESOLVED by the post-
  baseline M1 commit and 5 remain OPEN. See tables.

### RESOLVED by the M1 commit (`3aa7929` "mock 隐身衣 removal")

| F-TST-01 / zcode-ds seam | Old mock behavior | Current behavior | Evidence |
|---|---|---|---|
| Single-chunk streaming (F-TST-01-P2-01) | `chat_stream` emitted `stream::once(Ok(ChatChunk{content+finish_reason+usage}))` | Plain builders emit `[Delta(content), Terminal(finish_reason+usage)]`; `with_stream_script(Vec<StreamChunk>)` scripts arbitrary chunk sequences | mock_llm.rs:506-602, 578-601; fixtures at stream_channel.rs:2172, 2217 |
| Usage-on-content-chunk invisibility cloak (zcode-ds P1-01 usage half) | `think.rs:147 usage_reported = last_usage.is_some()` was always `true` because the single chunk carried usage | Mock separates usage onto the terminal chunk; two fixtures assert `usage_reported` true/false from the wire shape | stream_channel.rs:2172-2246; think.rs:107-147 |
| Completion-order batch / pipeline-restated-wrong-contract (F-RCT-04-P1-01, zcode-ds P1-02) | `phases/tools.rs` pushed results in `FuturesUnordered` completion order; the only ordering test pinned `["call-b","call-a"]` | Production reordered to call order; `concurrent_batch_results_follow_call_order` asserts call order; pipeline.rs:1635-1640 doc restricts the completion-order assertion to a per-stream fact | stream_channel.rs:2250-2311; pipeline.rs:1635-1726 |
| Mid-stream ERROR (error half of F-TST-01-P2-02) | Errors only modelled before the stream started | `StreamChunk::Err(ReactError)` ends a scripted stream mid-iteration | mock_llm.rs:67, 460, 599; pinned-red fixture stream_channel.rs:2317-2345 |

### STILL OPEN — mock seams that still hide integration failures

| Open seam | Mock limitation | Negative-control bug class that ships invisible |
|---|---|---|
| `MockTool` text-only (F-TST-01-P2-03) | `with_failure`/`with_response` produce only `ToolResult{kind:Text/Error, failure:None, bytes:None, data:None, truncated:false}`. M1 added only `with_delay`. No `ToolFailure`/`bytes`/`data`/`truncated`/pagination builder. | A regression in structured-failure routing (a tool reporting `ToolFailureCategory::PartialSideEffect` not routed to verify-then-retry) or in bounded-output artifact spill (`truncated=true`) is invisible to every mock-driven tool test. |
| `MockAgent` single-`FinalAnswer` stream (F-TST-01-P3-02) | `mock_agent.rs` UNCHANGED by M1 (git-confirmed); every streaming override emits `stream::once(Ok(AgentEvent::FinalAnswer))`. ~15 `AgentEvent` variants never produced. | Orchestration tests cannot assert on subagent intermediate-event ordering (parent seeing a child's `ToolCall`/`Token` before `FinalAnswer`). Any future feature that routes subagent intermediate events has no scaffold. |
| `FailingMockAgent` one error variant (F-TST-01-P3-01) | Always returns `AgentError::InitializationFailed` (mock_agent.rs:394-395, 406). | Recovery routing for `NoResponse`/`MaxIterationsExceeded`/`TokenLimitExceeded`/`TransportClosed`/`Interrupted` is untested via this mock (low impact today — routing is uniform — but preventive). |
| Mid-stream CANCEL (cancel half of F-TST-01-P2-02) | `with_delay` cancels only before the stream starts (`select!` at mock_llm.rs:496-504). No `with_cancel_after_chunks(n)` builder. | Cancellation arriving after the LLM has emitted tokens but before completion is indistinguishable from pre-stream cancel. The real transport polls `is_cancelled()` between chunks (client.rs:252-254 per F-LLM-01). |
| `MockTool` ignores `ToolContext` (F-TST-01 handoff #4) | Inherits the trait default `execute_with_context` that delegates to `execute` and drops `ctx` (echo-core/tools/mod.rs:777-783). | A test asserting working-dir-aware or cancel-via-context tool output silently gets the default context. No audited test does this, but the scaffold does not prevent it. |
| Multi-fragment tool-call assembly end-to-end (DeepSeek-repair context) | `parse_tool_args` repair IS unit-tested (processor.rs:192-237, hand-crafted JSON); but the tool-call branch of `chat_stream` still builds the complete `DeltaToolCall` in one delta (mock_llm.rs:535-551). No mock-driven test drives fragmented `{index:0,id,name}`→`{index:0,args:"…"}` deltas through `build_tool_calls_from_map`. | The repair *function* is covered; the *integration* (multi-fragment assembly in the streaming consumer) is not mock-reachable without hand-building `DeltaMessage`s via `with_stream_script`. |

### Who uses the new `with_stream_script` API

Three call sites, all in `stream_channel.rs` (:2180, :2222, :2320) — the two
usage fixtures and the pinned-red truncated-stream fixture. No CLI test site
uses it yet (the CLI inherits the benefit indirectly because the framework
streaming-channel suite now certifies the real wire shape, but CLI-local
fixtures were not added in M1).

## Deviations

- **Commit divergence (restated for this validation).** The M1 commit
  `3aa7929` is the direct cause of the four RESOLVED verdicts. At the task's
  pinned baseline `9b0e0fa`, all four would have been STILL OPEN (matching
  F-TST-01 and zcode-ds-Q-TST-01). Recording the actual reviewed commit is
  required by REPORTING.md and is what makes the RESOLVED verdicts honest.
- Negative controls are static thought experiments, not executed mutations.
  Suite greenness at `3aa7929` is carried from F-TST-01 V04 (mock-driven
  subset green at the baseline) plus the M1 commit's own pre-commit gate
  (a commit titled "test-credibility re-basing" by definition passed the
  gate before landing). No `cargo test` was re-run in this task (disk/build
  conservation; the gate tasks Q-FW-01/Q-CLI-01 own execution).

## Conclusion

The mock invisibility cloak has been **substantially lifted** for the
streaming-shape, usage-accounting, and batch-ordering contracts (the three
highest-impact seams from F-TST-01) — the M1 commit landed exactly the
builders F-TST-01-P2-01/P2-02 recommended plus the F-RCT-04-P1-01 regression
fixture. Five lower-impact seams remain open (text-only `MockTool`,
single-`FinalAnswer` `MockAgent`, single-variant `FailingMockAgent`,
mid-stream cancel, `ToolContext`-ignoring tool, multi-fragment assembly).
These support Q-TST-01-P2-03, P3-01, P3-02, and P3-03. The non-mock coverage
gaps (react_loop zero tests, provider streaming parse untested,
revisioned_adapter zero tests) are independent of mock fidelity and are
recorded in V01/P1-01/P1-02/P2-01.

## Follow-Up

None. The findings and their dependency links are finalized in the task
report. A mock-fidelity-enhancement follow-up should land the remaining
builders (F-TST-01-P2-03/P3-01/P3-02 directions still open).
