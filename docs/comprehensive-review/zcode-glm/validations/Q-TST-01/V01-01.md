# Q-TST-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 3aa7929 (baseline 9b0e0fa + 1 post-baseline commit; see Deviations)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

For each major production module (agent/react, tools, memory, tasks, mcp,
providers) the test count is known and the coverage is classifiable as
"meaningful invariant coverage", "wiring/builder only", or "compile-tested
only". No duplicate test infrastructure exists.

## Method

Static inventory across both repositories. Working directory
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`. Test counts are
`#[test]`/`#[tokio::test]` attribute occurrences (one per test fn):

```text
cd echo-agent
# per-crate totals
grep -rn '#\[test\]\|#\[tokio::test\]' --include='*.rs' src/ echo-*/ tests/
# per-module breakdowns, fn-name listings, mock-driver classification
grep -n 'MockLlmClient\|MockAgent' src/agent/react/tests.rs
grep -rn 'then_tool_calls' --include='*.rs' .
grep -n 'fn run_react_loop' src/agent/react/run/react_loop.rs
# CLI task_runtime + provider + mcp + memory per-file counts
cd ../echo-agent-cli
grep -c '#\[test\]\|#\[tokio::test\]' echo-agent-app-core/src/tasks/task_runtime/*.rs
```

Duplicate-search terms (both repos): `MockLlmClient`, `MockAgent`,
`then_tool_calls`, `with_stream_script`, `#[cfg(test)]` test modules, frontend
`*.test.ts(x)` files, per-store test mapping.

## Expected Result

A complete module-to-test map distinguishing (a) tests that drive production
invariants through the real loop/contract, (b) tests that exercise only
builders/registries/mocks, and (c) modules with zero tests.

## Result

- Exit code: not-applicable (read-only static inventory)
- Summary: framework = 1942 test attributes; application Rust ~ (task_runtime
  alone has 230 across its files). The map below classifies coverage.

### Framework (`echo-agent`) — 1942 test attributes across 8 crates

| Module | Tests | Coverage class | Anchor |
|---|---:|---|---|
| `src/agent/react/run/stream_channel.rs` | 27 | **meaningful** (real loop via MockLlmClient; post-M1 has usage + call-order fixtures) | stream_channel.rs:2172-2311 |
| `src/agent/react/tests.rs` | 81 | **wiring/builder only** (23 use MockAgent, 0 use MockLlmClient/then_text/then_tool_call — never drives the real loop) | tests.rs:15,24,514 |
| `src/agent/react/run/react_loop.rs` (non-streaming `run_react_loop`) | **0** | **compile-tested only** | react_loop.rs:598 |
| `src/agent/react/run/pipeline.rs` | 15 | meaningful (multiplexed identity + terminal order, now scope-clarified) | pipeline.rs:1641 |
| `src/agent/react/run/phases/tools.rs` (batch) | 0 inline; 1 via stream_channel `concurrent_batch_results_follow_call_order` | **meaningful** (call-order fixture added by M1) | stream_channel.rs:2250 |
| `src/agent/react/run/processor.rs` | 7 | meaningful (parse_tool_args DeepSeek-repair unit tests at :192-237; NOT mock-driven) | processor.rs:104,192 |
| `src/agent/subagent/` | 127 | meaningful (terminal-monotonicity/recovery tests) | executor.rs:2373,2433,2477 |
| `src/evolution/` | 144 | meaningful | — |
| `echo-tools/src/security.rs` | 37; `shell.rs` 22; `code.rs` 11; `text.rs` 11; files/* 32 | **meaningful** (builtin tools well-covered) | — |
| `echo-state/src/compression/invariants.rs` | 13 | **meaningful** (orphaned-tool, preserve-system-prompt, idempotent) | invariants.rs:114 |
| `echo-state/src/compression/mod.rs` | 28 | mixed: 1 print-only (`test_sliding_window_compressor` :2252 zero asserts) + meaningful ones | mod.rs:2252 |
| `echo-state/src/memory/*` | 63 (sqlite 15, store 10, snapshot 11, typed 8, file_conv 7) | meaningful | — |
| `echo-orchestration/src/tasks/executor.rs` | 23; runtime 10; scheduler 8; replanner 9 | meaningful | executor.rs:1994 |
| `echo-orchestration/src/tasks/{dag,events,manager,store,time}.rs` | **0 each** | compile-tested only (behavior covered, if at all, by sibling-module tests) | dag.rs, manager.rs, store.rs |
| `echo-integration/src/providers/anthropic.rs` | 7 | **request-side only**; streaming parse path (`convert_response`:293, `chat_stream`:421) untested | anthropic.rs:1100 |
| `echo-integration/src/providers/openai.rs` | 4 | **request-side only**; `stream_chat`:175 untested | openai.rs:516 |
| `echo-integration/src/providers/client.rs` | 3 | SSE line-parser helpers only; `stream_post`:182 loop + cancel/error untested | client.rs:356 |
| `echo-integration/src/providers/thinking_translate.rs` | 18 | meaningful | — |
| `echo-integration/src/mcp/server.rs` | 11; `tool_adapter.rs` 3 | partial (server config tested; client.rs/config_loader.rs/server_config.rs = 0) | — |
| `src/a2a/`, `src/intent/`, `src/security.rs` | 38 / 18 / 11 | meaningful | — |

### Application (`echo-agent-cli`)

| Module | Tests | Coverage class | Anchor |
|---|---:|---|---|
| `tasks/task_runtime/executor.rs` | 46 | **meaningful** (drives real loop via MockLlmClient) | executor.rs |
| `tasks/task_runtime/store.rs` | 34 | **meaningful** (claims, terminal monotonicity, atomic rollback, stale revisions) | store.rs:2300,2394 |
| `tasks/task_runtime/worktree.rs` | 25; task_tools 14; types 13; planner 11 | meaningful | — |
| `tasks/task_runtime/revisioned_adapter.rs` | **0** | **compile-tested only** (388-line EKO→framework adapter; no round-trip test) | revisioned_adapter.rs |
| `chat_driver.rs`, `agent_pool.rs`, `unified_memory.rs`, `plugin_runtime.rs` | non-zero (reuse framework MockLlmClient) | meaningful (inherit framework mock fidelity) | chat_driver.rs:764 |

### Duplicate search

No duplicate test infrastructure. The CLI reuses the framework mocks via
`echo_agent::testing::MockLlmClient` (chat_driver.rs:764, agent_pool.rs:1250,
unified_memory.rs:228, plugin_runtime.rs:1364, tasks/service.rs:872). The four
framework mock types are defined exactly once in `src/testing/`. The CLI adds
no sibling framework-level mocks; its application-layer test doubles
(`tests/task_runtime` fakes) are scoped to EKO TaskRuntime internals.

## Deviations

- **Reviewed commit diverges from the task header.** The task header pins
  `echo-agent: 9b0e0fa`, but the working tree is at `3aa7929` — exactly one
  post-baseline commit titled "fix(tests): M1 test-credibility re-basing (mock
  隐身衣 removal)". This commit is directly on-topic (it adds
  `with_stream_script`/`StreamChunk`, the real two-chunk wire shape, and the
  `concurrent_batch_results_follow_call_order` fixture). Per REPORTING.md
  ("Each report records the actual reviewed commits"), this report records
  `3aa7929` and treats the baseline `9b0e0fa` map as partially superseded:
  `react/tests.rs`/`react_loop.rs`/provider-streaming/`revisioned_adapter`
  findings are unchanged, but the M1 streaming-shape and call-order findings
  from F-TST-01 / zcode-ds-Q-TST-01 are now resolved (see V04-01).
- `echo-agent-cli` is at the baseline `b3b2e81` (no divergence).

## Conclusion

The map supports the task findings: the **EKO store/executor layer,
subagent fault-tolerance, compression invariants, and builtin tools** carry
meaningful invariant tests; the **framework streaming-channel suite is now
(post-M1) provider-contract-faithful**; four production seams are
**compile-tested only** — the non-streaming `react_loop.rs`, the Anthropic AND
OpenAI streaming response parse paths, `revisioned_adapter.rs`, and several
`echo-orchestration` task files. `react/tests.rs` (81 tests) is wiring/builder
coverage only. Supports Q-TST-01-P1-01, P1-02, P2-01, P2-02 (findings).

## Follow-Up

None — V02 samples assertion quality of selected entries; V03 inventories
ignored/platform-gated tests; V04 traces mock-invisibility seams (including
the M1 resolutions).
