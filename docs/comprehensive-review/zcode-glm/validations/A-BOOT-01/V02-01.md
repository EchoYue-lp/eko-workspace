# A-BOOT-01 / V02-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: not-applicable
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only inspection)

## Claim

TUI, CLI, channels, and GUI entries construct the same core services
(TaskRuntimeStore, AgentPool, SchedulerRunner, BackgroundTaskService,
conversation store, MCP health check, dreaming) — except where the task report
documents a parity gap. This validation cross-checks each entry's construction
set.

## Method

Static inspection of the four entry branches. Working directory:
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
1. Read echo-agent-cli/src/main.rs:60-451 (main + run_tui_or_cli_entry +
   build_task_runtime_store_for_headless).
2. Read echo-agent-cli/src/tauri/desktop.rs:124-271 (run_desktop).
3. Read echo-agent-cli/src/cli/modes.rs:32-110 (start_headless_services +
   run_cli_mode).
4. For each of the 7 services, record which entry constructs it and via which
   helper. Cross-check against the claim.
```

## Expected Result

A per-entry, per-service matrix showing: bootstrap-equivalent on all entries;
TaskRuntimeStore divergence (P2-01); channels-only missing scheduler/bg-task
(P2-02); MCP-health/dreaming GUI-only (P2-03).

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary: the matrix below matches the claim.

  Per-entry construction matrix (Y = constructed, N = not constructed, "x2" =
  constructed twice):

  | Service | TUI | CLI | Channels-only | GUI |
  |---|---|---|---|---|
  | `AgentRuntime::bootstrap` (agent stack) | Y (`main.rs:168`) | Y (`main.rs:168`) | Y (`main.rs:168`) | Y (`desktop.rs:160`) |
  | `TaskRuntimeStore` (open + `recover_incomplete`) | x2 (`main.rs:175` + `state.rs:547` via `modes.rs:57`) | x2 (same path via `run_cli_mode` -> `start_headless_services`) | Y once (`main.rs:175`; `start_headless_services` not called) | Y once (`desktop.rs:187` -> `state.rs:547`) |
  | `AgentPool` | Y inline (`main.rs:183-201`) | Y inline (same) | Y inline (same) | Y via `init_pool` (`desktop.rs:210`) |
  | `SchedulerRunner` | Y (`start_headless_services` `modes.rs:62`) | Y (same, via `run_cli_mode`) | **N** (branch skips `start_headless_services`) | Y (`desktop.rs:232`) |
  | `BackgroundTaskService` | Y (`start_headless_services` `modes.rs:61`) | Y (same) | **N** (branch skips `start_headless_services`) | Y (`desktop.rs:231`) |
  | Conversation store | Y (`main.rs:127`) | Y (same) | Y (same) | Y (`desktop.rs:184`) |
  | MCP health check | **N** | **N** | **N** | Y (`desktop.rs:243`) |
  | Dreaming task | **N** | **N** | **N** | Y (`desktop.rs:247`) |
  | Config watcher | Y (`main.rs:232`) | Y (same) | Y (same) | Y (`desktop.rs:166`) |

  Supporting anchors:
  - Channels-only branch: `main.rs:357-403` enters `tokio::spawn(run_channels_mode(...))`
    and the exit cleanup (`main.rs:394-400`) runs without ever calling
    `start_headless_services`. Confirms P2-02.
  - MCP-health / dreaming grep: zero hits in `main.rs`, `modes.rs`, or
    `channels.rs` for `spawn_mcp_health_check` / `spawn_dreaming_task`. Confirms
    P2-03.
  - `start_headless_services` (`modes.rs:56-60`) builds a throwaway `AppState`
    via `from_shared` (which builds a `TaskRuntimeStore`), then overwrites
    `state.tasks.runtime = task_runtime_store` with the headless-built store.
    Confirms the double construction in P2-01.
  - GUI builds the pool via `runtime.init_pool(...)` (`desktop.rs:210`);
    headless builds it inline (`main.rs:183-201`) and does not call `init_pool`.
    Confirms P3-01.

- Log/artifact: none

## Deviations

None. All four entries were statically reachable via the feature-gated branches
in `main.rs:60-92` and `desktop.rs:67`.

## Conclusion

Supports A-BOOT-01-P2-01 (TaskRuntimeStore double construction / parallel
paths), A-BOOT-01-P2-02 (channels-only missing scheduler + bg-task), and
A-BOOT-01-P2-03 (MCP-health + dreaming GUI-only). Also supports A-BOOT-01-P3-01
(pool construction duplication). The matrix is the primary evidence table for
those findings.

## Follow-Up

None.
