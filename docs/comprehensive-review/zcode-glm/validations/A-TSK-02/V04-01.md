# A-TSK-02 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

Per AGENTS.md rule 6, the application layer contains none of the
forbidden parallel task CRUD tools (`todo_write`, `plan_create`,
`plan_patch`, `plan_execute`) and no hidden global todo state (no
`static TODO`, no `Lazy<Mutex<…>>` for todos, no `TodoStore`). The
legacy global `todo_write` is gone from production; the only
repository-wide reference is the framework's negative test assertion
that confirms its removal.

## Method

Repository-wide symbol search across the whole `echo-agent-cli` tree
(excluding `target/`) for the parallel-CRUD names banned by AGENTS.md
rule 6, plus structural inventory of any global/static state that could
serve as a hidden todo authority.

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
grep -rn 'todo_write\|"todo"\|TodoStore\|fn create_todo\|fn update_todo\|global_todo\|fn write_todo\|todo_state\|fn list_todo\|TODO_REGISTRY\|todo_registry' echo-agent-cli --include='*.rs'
grep -rn 'plan_create\|plan_patch\|plan_execute\b' echo-agent-cli --include='*.rs'
grep -rn 'fn name(&self) -> &str' echo-agent-cli/echo-agent-app-core/src/tasks --include='*.rs'
grep -rn 'static.*TODO\|static.*TODO\|Lazy::new.*[Tt]odo\|once_cell.*[Tt]odo' echo-agent-cli --include='*.rs'
grep -rn 'todo_write' echo-agent echo-agent-cli --include='*.rs'
```

## Expected Result

- Zero matches for `todo_write` in `echo-agent-cli` (production code).
- Zero matches for `plan_create` / `plan_patch` / `plan_execute` as tool
  names in `echo-agent-cli`.
- Zero definitions of `TodoStore`, `create_todo`, `update_todo`,
  `global_todo`, `TODO_REGISTRY`, or any `static`/`Lazy` todo state.
- The only repository-wide `todo_write` hit is the framework's negative
  test assertion at `echo-agent/src/agent/react/builder.rs:1181`.
- `list_todos`, the only `*todo*` read helper, is a derived projection
  with no independent store.

## Result

- Exit code: 0
- Duration: <3s
- Summary:
  - **`todo_write` in `echo-agent-cli`: 0 matches.** The application
    layer contains no definition, registration, or call site of the
    legacy global todo tool.
  - **`plan_create` / `plan_patch` / `plan_execute` in `echo-agent-cli`:
    0 matches as tool names.** The only `plan_patch` substring hits are:
    - `to_task_plan_patch` (`types.rs:1284`, `store.rs:913`,
      `revisioned_adapter.rs:328`) — the legitimate adapter converter
      that turns an EKO `TaskUpdateRequest` into the framework
      `TaskPlanPatch`. This is a method name on the existing DTO, not a
      tool.
    - `attach_plan_creates_tasks_and_todos` (`store.rs:2327`) — a test
      name asserting that `attach_plan` populates tasks/todos. Not a
      tool.
    Neither is a parallel CRUD tool.
  - **No global todo state.** Searches for `static.*TODO`,
    `Lazy::new.*[Tt]odo`, `once_cell.*[Tt]odo`, `TODO_REGISTRY`,
    `todo_registry`, `global_todo`, `fn create_todo`, `fn update_todo`,
    `fn write_todo`, `TodoStore`, `todo_state` returned 0 matches in
    `echo-agent-cli`. There is no global todo authority outside
    `TaskRuntimeStore`.
  - **`list_todos` is a derived projection only.** Two definitions, both
    read-only:
    - `TaskRuntimeStore::list_todos`
      (`echo-agent-app-core/src/tasks/task_runtime/store.rs:1784-…`)
      delegates to `FileTaskStore::list_todos`.
    - `FileTaskStore::list_todos`
      (`echo-agent-app-core/src/tasks/task_runtime/file_store.rs:164-210`)
      derives every `TodoItem` field from `plan.json` (spec) +
      `run-state.json` (execution) + folded `Task*` event payloads. No
      dedicated store; no create/update/delete mutator. (Carry-over
    from A-TSK-01 V03-01.)
  - **Repository-wide `todo_write`: 1 hit.**
    `echo-agent/src/agent/react/builder.rs:1181`:
    `assert!(!names.iter().any(|name| name == "todo_write"));`
    — a negative test assertion inside `default_agent_uses_one_task_relation_api`
    confirming the legacy tool is NOT registered by default. The
    framework side guarantees the absence; the application inherits it.
    (Matches F-TSK-01 V01-01 finding.)
  - **Tool-name inventory.** Every `fn name(&self) -> &str` defined in
    `echo-agent-cli/echo-agent-app-core/src/tasks/`:
    - `task_execute` (`task_execute_tool.rs:203-205`)
    - `create_complex_task` (`task_tools.rs:801-804`)
    - `check_run_status` (`task_tools.rs:1041-1044`)
    - `cancel_run` (`task_tools.rs:1102-1105`)
    The framework task tools (`task_create`/`task_update`/`task_list`)
    are defined in `echo-agent/echo-orchestration/src/tasks/task_tools.rs`
    and registered onto the EKO agent via the framework swap
    (V01-01). No task-CRUD-shaped tool name (`todo_*`, `plan_*`) is
    defined in the application.
- Log/artifact: none

## Deviations

None. The application layer is clean: zero banned parallel CRUD tools,
zero global todo state, the only `todo_write` reference is the
framework's negative test assertion.

## Conclusion

The application layer satisfies AGENTS.md rule 6 at the tool surface:
no `todo_write` / `plan_create` / `plan_patch` / `plan_execute` is
defined or registered, no hidden global todo state exists, and
`list_todos` is a derived projection over the single `TaskRuntimeStore`
authority. Combined with F-TSK-01 V01-01 (framework side) and A-TSK-01
V03-01 (persistence side), this closes the "single authority" chain
across all three layers. Supports the task report's primary conclusion.
No finding raised from this validation.

## Follow-Up

None.
