# Q-DEP-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

All Rust crates in both repositories declare the `MIT` license. No
non-MIT licenses (GPL, AGPL, MPL, Apache-2.0-only with patent
concerns, etc.) are present in any crate manifest.

## Method

```text
# Read the license field of every Cargo.toml in both repos.
grep -rn '^license' echo-agent echo-agent-cli \
    --include='Cargo.toml' \
    | grep -v target | grep -v worktrees
```

Crates inspected (10 total):

- `echo-agent` root package (`echo_agent`) + 7 sub-crates:
  `echo_core`, `echo_macros`, `echo_execution`, `echo_integration`,
  `echo_tools`, `echo_state`, `echo_orchestration`.
- `echo-agent-cli`: `echo-agent-app-core`, the CLI binary crate,
  and the Tauri crate.

## Expected Result

Per general license hygiene: all crates in a permissively-licensed
project should declare a consistent, OSI-approved license (MIT or
Apache-2.0). Mixed or copyleft licenses (GPL, AGPL) would create
compliance and distribution concerns.

## Result

- Exit code: not-applicable (static manifest inspection)
- Duration: n/a
- Summary:

### License manifest

| Crate | License |
|---|---|
| `echo_agent` (root) | MIT |
| `echo_core` | MIT |
| `echo_macros` | MIT |
| `echo_execution` | MIT |
| `echo_integration` | MIT |
| `echo_tools` | MIT |
| `echo_state` | MIT |
| `echo_orchestration` | MIT |
| `echo-agent-app-core` | MIT |
| `echo-agent-cli` (CLI + Tauri crates) | MIT |

All 10 Rust crates declare `MIT`. No non-MIT licenses found.

- Log/artifact: none.

## Deviations

Only the direct crate manifests' `license` fields were read. The
full transitive-dependency license tree (the `cargo-license` /
`cargo about` output for all 564+787 crates) was not enumerated in
this pass. A full license tree is recommended if license compliance
for binary distribution is required, but is not necessary for this
audit's scope (which covers the project's own code licensing only).

## Conclusion

Supports the positive confirmation in the task report: licenses are
clean. All 10 Rust crates declare MIT; no license-compliance blocker
exists for the framework or application. No downstream task needs to
handle a non-MIT license at the project-code level.

## Follow-Up

A full `cargo-license` run is recommended as a follow-up outside
this catalog if transitive-dependency license compliance for
distribution is required. No issue to address at the project-code
level.
