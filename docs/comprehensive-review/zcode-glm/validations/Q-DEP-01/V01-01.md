# Q-DEP-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

Both `Cargo.lock` files contain meaningful duplicate crate versions
(compiled on the macOS target, not platform-gated), with `hashbrown`
(5 versions) and `quick-xml` (4 framework / 5 CLI) being the highest-
impact offenders. Platform-gated `windows-*` duplicates are expected
and can be ignored on macOS.

## Method

```text
# Parse each Cargo.lock, group by package name, report names with >1 version.
python3 -c "<parse Cargo.lock name+version pairs, report names with >1 version>"

# Classify each duplicate as platform-gated (windows-*) or compiled.
# Attribute the meaningful duplicates to pulling direct dependencies
# via the lockfile dependency graph.
```

Lockfiles inspected:

- `echo-agent/Cargo.lock` (564 crates)
- `echo-agent-cli/Cargo.lock` (787 crates)

## Expected Result

Per AGENTS.md: no dedup target is codified, but duplicate majors of
foundational crates (HashMap impl, XML parser, RNG) inflate compile
time and binary size. Platform-specific `windows-*` crates are
expected to appear duplicated because they are separate target packages
per architecture and are not compiled on macOS.

## Result

- Exit code: not-applicable (static lockfile parse)
- Duration: n/a
- Summary:

### Meaningful duplicate versions (compiled on macOS target)

| Package | echo-agent copies | echo-agent-cli copies | Concern |
|---|---:|---:|---|
| `hashbrown` | 5 | 5 | HashMap impl; multiple majors pulled by different deps (older indexmap/ahash vs newer stacks) |
| `quick-xml` | 4 | 5 | XML parsing; research/RAG feature pins a different major than transitive crates |
| `rand` | 3 | - | Random number generation |
| `rand_core` | 3 | - | rand internal |
| `getrandom` | 3 | - | OS randomness |
| `toml` | - | 3 | TOML parsing (cargo + config + another consumer) |
| `toml_edit` | - | 3 | TOML editing |
| `toml_datetime` | - | 3 | TOML datetime (toml family) |
| `winnow` | - | 3 | Parser combinator (toml dep) |
| `zune-jpeg` | 2 | - | JPEG decoding |

### Platform-gated duplicates (expected, not compiled on macOS)

`windows-sys` (5-6 copies) and the `windows_*` per-target packages
(3-4 copies each) are platform-specific target packages for different
Windows architectures. They are parsed by cargo but not compiled on
the macOS target, so they impose no compile-time or binary-size cost
on this target. Their duplication is expected and not actionable.

- Log/artifact: none.

## Deviations

Attribution of each duplicate major to its exact pulling direct
dependency is "best effort" from manifest + lockfile-graph analysis.
A per-package `cargo tree -d -i <pkg>` run would give authoritative
attribution but was not reproduced here for every duplicate; the
high-value targets (hashbrown, quick-xml) are documented at the
finding level in the task report.

## Conclusion

Supports findings:

- **Q-DEP-01-P2-01**: `hashbrown` resolves to 5 major versions across
  the workspace — the highest-value convergence target (foundational
  data-structure crate, compiled early, widely depended on).
- **Q-DEP-01-P3-01**: `quick-xml` resolves to 4 (framework) / 5 (CLI)
  versions — the research/RAG feature likely pins a different major
  than transitive crates.

Confirms the Q-STA-01-P2-03 handoff: the meaningful duplicates are
`hashbrown`, `quick-xml`, the `rand`/`rand_core`/`getrandom` family,
and the CLI-side `toml`/`toml_edit`/`winnow` family. Platform-gated
`windows-*` duplicates are not a concern on macOS.

## Follow-Up

A future dependency-convergence task (outside this catalog) should
run `cargo tree -d -i hashbrown` and `cargo tree -d -i quick-xml` to
get authoritative attribution, then update the pulling direct
dependencies to converge on 1-2 majors each.
