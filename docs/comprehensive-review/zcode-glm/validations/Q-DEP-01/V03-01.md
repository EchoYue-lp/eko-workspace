# Q-DEP-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

The build-script surface is minimal: only `echo-agent-cli/build.rs`
exists (the Tauri build script, gated on `CARGO_FEATURE_GUI`). No
`Cargo.toml` in either repo declares `pkg-config`, `cmake`, or any
native system-dependency crate. The project builds from the Rust
toolchain alone (plus the platform WebView for the GUI feature).

## Method

```text
# Enumerate all build.rs files in both repos.
find echo-agent echo-agent-cli -name 'build.rs' \
    -not -path '*/target/*' -not -path '*/.worktrees/*'

# Search every Cargo.toml for native-dependency declarations.
grep -rn 'pkg-config\|cmake\|system-dep\|pkg_config\|cc =\|\"cc\"' \
    echo-agent echo-agent-cli --include='Cargo.toml' \
    | grep -v target | grep -v worktrees
```

## Expected Result

Per AGENTS.md and general build hygiene: build scripts should be
minimal and gated to the feature that needs them. Native system
dependencies (pkg-config, cmake, system libraries) impose an
environment-setup burden on contributors and CI, and their absence
is a positive signal for build portability.

## Result

- Exit code: not-applicable (static file enumeration)
- Duration: n/a
- Summary:

### Build scripts

| File | Purpose | Gating |
|---|---|---|
| `echo-agent-cli/build.rs` | Tauri build script (generates GUI resources, sets up WebView) | Gated on `CARGO_FEATURE_GUI` — only runs when the `gui` feature is enabled |

No other `build.rs` files exist in either repository.

### Native system dependencies

None. No `Cargo.toml` in either repo declares:

- `pkg-config` / `pkg_config` build dependency
- `cmake` build dependency
- `system-dep` crate
- `cc` / `cc-sys` build dependency
- Any other crate that would require a system library at build time

The only non-Rust-toolchain requirement is the platform WebView,
needed solely for the Tauri GUI feature (standard for Tauri apps).

- Log/artifact: none.

## Deviations

None. The build surface is exactly as claimed.

## Conclusion

Supports the positive confirmation in the task report: build scripts
are minimal (one Tauri `build.rs`, GUI-gated) and no native system
dependencies are declared. `Q-FW-01`/`Q-CLI-01` can assume the build
runs without system prerequisites beyond the Rust toolchain (and the
platform WebView for the GUI feature).

## Follow-Up

None. No build-script or native-dependency issue to address.
