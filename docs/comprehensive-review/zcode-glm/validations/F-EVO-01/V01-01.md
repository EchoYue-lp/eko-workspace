# F-EVO-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only inspection + targeted `cargo check`)

## Claim

`eval`, `improve`, and `evolution` are isolated as optional framework
capabilities with a traceable feature/reachability footprint: `eval` and
`improve` are `cfg`-gated marker features that compile standalone and
compose correctly; `evolution`'s compilation boundary is explicitly
characterized. No capability is both defined and untraceable.

## Method

Read-only feature census plus three targeted standalone compile checks.
Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`.

```text
1. grep -rn 'cfg(feature = "eval")' --include='*.rs' . | grep -v target
   grep -rn 'cfg(feature = "improve")' --include='*.rs' . | grep -v target
   grep -rn 'cfg(feature = "evolution")' --include='*.rs' . | grep -v target
2. Read lib.rs module declarations and Cargo.toml [features] for the three
   capabilities.
3. cargo check -p echo_agent --no-default-features --features eval --locked
4. cargo check -p echo_agent --no-default-features --features improve --locked
5. cargo check -p echo_agent --no-default-features --features eval,improve --locked
6. Confirm CLI feature selection excludes eval/improve and that evolution is
   unconditional.
```

Disk note: `/` was at ~46 GiB free (below the AGENTS.md 50 GiB threshold),
so only the three directly-relevant `cargo check`s were run (incremental on
the existing 12 GiB `target/`). No `cargo clean` was performed.

## Expected Result

`eval` and `improve` each have a non-zero, accounted `cfg` footprint, gate
real modules in `lib.rs`, compile standalone, and compose when both are
enabled (the improve↔eval cross-gate documented in `improve/mod.rs`).
`evolution` is characterized honestly (feature-gated or not). CLI selection
matches F-FEAT-01.

## Result

- Exit code: 0 (all three `cargo check`s finished; 0.49s / 0.49s / 0.18s
  incremental).
- Summary: `eval` has 15 `cfg` matches (1 module gate in `lib.rs`, 13 in
  `improve/mod.rs` gating the eval-driven analyzer/loop/generator, 1
  `doc(cfg)`). `improve` has 2 `cfg` matches (its module gate + `doc(cfg)`).
  `evolution` has **0** `cfg` matches — it is NOT feature-gated
  (`lib.rs:40` is unconditional). All three feature combinations compile.

### cfg(feature census

| Feature | cfg matches | Gate location | Standalone compile |
|---|---:|---|---|
| `eval` | 15 | `lib.rs:36-38` (module) + `improve/mod.rs:42-62` (cross-gate) | passes (`--features eval`) |
| `improve` | 2 | `lib.rs:43-45` (module) | passes (`--features improve`) |
| `evolution` | 0 | none — `lib.rs:40` unconditional | n/a (always compiled) |

### Cargo.toml feature definitions (`echo-agent/Cargo.toml`)

- `eval = []` (line 95), `improve = []` (line 96): empty marker features.
- No `evolution` feature is declared.
- `examples/demo50_eval` `required-features = ["eval"]` (line 304-305).
- `examples/demo51_self_improvement` `required-features = ["eval",
  "improve"]` (line 308-309). These are the only in-tree eval/improve
  consumers.

### improve ↔ eval composition (verified by compile)

`improve`'s base `trajectory` submodule is ungated (available with
`improve` alone). Its eval-driven half (`analyzer`, `eval_improvement`,
`generator`, `loop` and the `CritiqueIssue`/`ImprovementSuggestion`/
`RunCritique` types) is `#[cfg(feature = "eval")]`. So `--features improve`
alone compiles the base; `--features eval,improve` activates the
eval-driven parts. Both compile cleanly. This matches the `improve/mod.rs`
doc ("Eval-driven analysis is available only when the `eval` feature is
enabled as well").

### evolution module shape (always compiled)

- 17 files, 11096 lines. `pub mod evolution;` at `lib.rs:40`, no `#[cfg]`.
- Internal dependencies (`use crate::...` in `src/evolution/`): `error`
  (10), `evolution` (self, 5), `memory` (3), `trace` (2), `skills` (1),
  `llm` (1). All are always-compiled core modules — evolution pulls NO
  optional crate dependencies, so it adds no dep weight, only compile
  time. This is why the missing feature gate has low impact (see
  F-EVO-01-P3-01).

### CLI selection (cross-repo)

- `echo-agent-cli/echo-agent-app-core/Cargo.toml:10-15`: `echo-agent =
  { default-features = false, features = ["mcp","lsp","human-loop",
  "subagent","git","tasks","shell","files","web","data","statistics",
  "chart","research","media","rag"] }`. Neither `eval` nor `improve`.
- `echo-agent-cli/Cargo.toml:50`: root binary enables `["mcp","lsp",
  "human-loop","subagent","tasks"]`. Neither `eval` nor `improve`.
- `evolution` is pulled in unconditionally (it is not optional).

## Deviations

None for eval/improve. One structural observation (not a command
deviation): `evolution` is not feature-gated, unlike eval/improve. This is
recorded as finding F-EVO-01-P3-01 in the task report, not as a
validation failure — the claim being validated is "characterized
honestly", and it now is.

## Conclusion

Supports the V01 claim: `eval` and `improve` are properly isolated,
standalone-compilable, and correctly compose. `evolution` is honestly
characterized as unconditional (0 cfg gates, always compiled, no optional
dep weight). Establishes the baseline used by V02 (genericity) and V03
(mutation boundary). Underpins findings F-EVO-01-P3-01 (evolution gating
inconsistency) and confirms F-FEAT-01's "eval/improve are live marker
features excluded by the CLI".

## Follow-Up

`None.` for eval/improve isolation. The `evolution` feature-gate question
is tracked as F-EVO-01-P3-01 in the task report.
