# F-TSK-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable (framework-only scope)
> Worktree state: clean

## Claim

There is exactly one canonical task/plan/todo model in `echo-orchestration`
backed by `TaskRevisionService` + `RevisionedTaskStore`; no parallel
`todo_write` / `plan_create` / `plan_patch` / `plan_execute` task CRUD exists
anywhere in the `echo-agent` repository, and the legacy `todo_write` tool is
confirmed dead (test-only).

## Method

Repository-wide symbol search across `echo-agent` (all crates) for the
parallel-task-CRUD names banned by AGENTS.md rule 6, plus structural inventory
of the canonical task model.

```text
grep -rn "todo_write" --include="*.rs" echo-agent/
grep -rn "plan_create\|plan_patch\|plan_execute" --include="*.rs" echo-agent/
Read echo-agent/echo-orchestration/src/tasks/task.rs
Read echo-agent/echo-orchestration/src/tasks/revisioned.rs
Read echo-agent/echo-orchestration/src/tasks/task_tools.rs
Read echo-agent/src/tasks.rs
```

## Expected Result

- Zero production matches for `plan_create`, `plan_patch`, `plan_execute`.
- `todo_write` only in a test assertion (dead in production).
- A single canonical task model in `echo-orchestration::tasks` with one
  revision service and one store trait.

## Result

- Exit code: not-applicable (static inspection)
- Duration: n/a
- Summary:
  - `todo_write`: exactly **1** match at
    `echo-agent/src/agent/react/builder.rs:1181` — an `assert!` inside a
    `#[cfg(test)]` block confirming the legacy tool is NOT registered. No
    production definition, registration, or call site.
  - `plan_create` / `plan_patch` / `plan_execute`: **0** matches anywhere in
    the `echo-agent` repository.
  - Canonical task model is the single authority. Inventory of
    `echo-orchestration/src/tasks/task.rs`:
    - `TaskType` enum (14 variants incl. `background`).
    - `TaskInput` (30) / `InputType` (40) / `TaskOutput` (49) / `OutputType`
      (59) / `ContextScope` (70) / `RiskLevel` (86).
    - `VerificationSpec` (98) / `VerificationType` (134) /
      `FallbackStrategy` (149) / `CheckpointPolicy` (161).
    - `TaskAttempt` (171) / `AttemptStatus` (184) / `Evidence` (194) /
      `EvidenceType` (204) / `FileChange` (214) / `ChangeType` (224) /
      `Artifact` (233) / `ArtifactType` (244) / `CommandRecord` (255) /
      `VerificationResult` (265).
    - `TaskState` (275) / `ManagedTask` (292) builder with `new()`,
      `with_dependencies()`, `with_priority()`, `with_timeout()`,
      `with_max_retries()`, `with_assigned_agent()`, `with_tags()`,
      `with_execute_fn()`, `with_metadata()`, `task_spec()`,
      `task_execution()`, `to_task()`.
  - Revisioned layer (`revisioned.rs`): `RevisionedTaskGraph` (42, versioned
    graph), `TaskDraft` (49), `TaskCreateInput` (67), `TaskSpecPatch` (78),
    `TaskPlanPatchOp` (94), `TaskPlanPatch` (117), `TaskUpdateInput` (148),
    `TaskPatchEffects` (156), `TaskGraphCommit` (167),
    `RevisionedTaskStore` trait (247), `TaskToolPolicy` trait (270),
    `DefaultTaskToolPolicy` (318), `TaskRevisionService` (674).
  - Root `echo-agent/src/tasks.rs` is a thin re-export
    (`pub use echo_orchestration::tasks::*;`) plus `register_task_tools()`.
- Log/artifact: none

## Deviations

None. The banned parallel CRUD names are absent; the legacy `todo_write` is
test-only and the test itself asserts its removal, matching AGENTS.md rule 6
("旧的进程全局 `todo_write` 已由框架直接删除").

## Conclusion

The framework exposes exactly one canonical, revisioned task model. There is
no parallel plan/todo/task CRUD authority. This satisfies the
"single authority" requirement of AGENTS.md rule 6 and supports the task's
positive finding that the layering is clean. Supports the task report's
primary conclusion and finding F-TSK-01-P3-01 (the only adjacent concern is
the parallel input-op enum, audited in V02/V03, not a duplicate authority).

## Follow-Up

None.
