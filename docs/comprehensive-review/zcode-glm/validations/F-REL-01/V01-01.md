# F-REL-01 / V01-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: not-applicable
> Worktree state: clean

## Claim

Each reliability primitive (`RetryPolicy`, `TokenBudget`, `CircuitBreaker`,
`hash`, `time`, `json_parse` utils) has exactly one implementation in
`echo-core`, with no duplicate implementations elsewhere in the repository.

## Method

Repository-wide definition and duplicate search.

```text
1. grep -rn "struct RetryPolicy" echo-agent echo-agent-cli
2. grep -rn "struct TokenBudget" echo-agent echo-agent-cli
3. grep -rn "struct CircuitBreaker" echo-agent echo-agent-cli
4. grep -rn "fn fnv1a_64" echo-agent echo-agent-cli
5. grep -rn "fn extract_json_from_markdown\|fn clean_json" echo-agent echo-agent-cli
6. Document the definition site and any caller sites for each symbol.
```

## Expected Result

Exactly one definition per symbol, located in `echo-core/src/`, with callers
in LLM call paths, tool execution, and MCP integration. No parallel
application-layer implementations.

## Result

- Exit code: 0
- Duration: not-applicable
- Summary: Single definitions confirmed.
  - `RetryPolicy`: `echo-core/src/retry.rs:35` (root `retry.rs` is a thin
    re-export with `with_retry` / `with_retry_if`).
  - `TokenBudget`: `echo-core/src/budget.rs:17`.
  - `CircuitBreaker`: `echo-core/src/circuit_breaker.rs:59`.
  - `fnv1a_64`: `echo-core/src/hash.rs`.
  - `now_secs` / `now_millis`: `echo-core/src/time.rs`.
  - `extract_json_from_markdown` / `clean_json`: `echo-core/src/json_parse.rs`.
  - Callers: LLM calls, tool execution, MCP. No duplicate definitions found.
- Log/artifact: none

## Deviations

None.

## Conclusion

Supports the task report's positive observation that each reliability primitive
is implemented exactly once in the framework with no duplication, and that
layering is clean (generic mechanism in framework, no EKO product policy).
Used by finding F-REL-01-P3-01 and F-REL-01-P3-02 context.

## Follow-Up

None.
