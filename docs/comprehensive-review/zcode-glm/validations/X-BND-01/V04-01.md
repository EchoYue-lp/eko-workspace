# X-BND-01 / V04 / Attempt 01

> Status: passed_with_notes
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: echo-agent has unrelated dirty paths (`src/agent/react/run/*`, `src/testing/*`); echo-agent-cli clean.

## Claim

The EKO adapters are thin (type conversion + metadata + product policy
only): no adapter owns a second ready-frontier, retry loop, DAG validator,
or state machine. The deletion-target matrix is closed: each old
implementation that should be removed has a named successor.

## Method

Spot-check the live CLI adapters against the AGENTS.md "adapter must be
thin, lossless, and free of scheduling/state authority" rule, and
consolidate the deletion targets surfaced by V01-V03.

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# 1. forbidden scheduling/DAG/retry authority in app-core?
grep -rn "fn.*cycle|fn.*topological|fn.*topological_order|dependency_cycles|ready_frontier|fn ready_tasks|compute_frontier" \
  echo-agent-cli/echo-agent-app-core/src --include="*.rs"
grep -rn "retry_count|max_retries|backoff|RetryPolicy" \
  echo-agent-cli/echo-agent-app-core/src --include="*.rs" | grep -vE "test|#\[cfg"
grep -n "RuntimeDagExecutor|select_ready_wave|dispatch_ready" \
  echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs

# 2. confirm CLI delegates execution to framework RuntimeDagExecutor
grep -rn "RuntimeDagExecutor::new|RuntimeDagExecutorConfig" \
  echo-agent-cli/echo-agent-app-core/src --include="*.rs"

# 3. read select_ready_wave + the framework's contract for it
sed -n '100,135p' echo-agent/echo-orchestration/src/tasks/runtime_executor.rs  # framework trait
sed -n '1255,1290p' echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs  # CLI impl

# 4. review.rs retry — product policy or generic retry?
sed -n '140,170p' echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/review.rs
```

## Expected Result

- Zero frontier/cycle/topological computation in app-core (framework owns it).
- CLI constructs `RuntimeDagExecutor` (delegates scheduling).
- `select_ready_wave` only filters the framework-provided frontier for
  product policy (file ownership), per the framework's documented hook.
- `review.rs` retry is reviewer-strategy policy exercised through
  `resolve_dispatch`, not a parallel generic retry.
- Each deletion target has a named successor.

## Result

- Exit code: 0
- Duration: <15s

### Adapter authority spot-checks (POSITIVE — adapters are thin)

| Adapter | Authority it DOES NOT own (correctly) | Evidence |
|---|---|---|
| Frontier computation | Framework owns it. CLI `select_ready_wave` only filters. | Framework `RuntimeDagController::select_ready_wave` doc (`runtime_executor.rs:100-108`): "Select a conflict-free subset of the ready frontier. The default dispatches the whole frontier. **Applications may defer tasks for product-specific resource or file-ownership policy**." CLI impl (`executor.rs:1265-1281`) takes `ready_task_ids` (already the frontier) and filters through `select_ownership_safe_wave`. No dependency analysis. |
| DAG scheduling | Framework owns it. | CLI constructs `echo_agent::tasks::RuntimeDagExecutor::new(...)` (`executor.rs:1645`). The executor drives waves; CLI is the controller. |
| Cycle/DFS/topological | Framework owns it (A-TSK-01 V03-01 confirmed zero app validators). | Zero `fn dependency_cycles`/`fn topological`/`compute_frontier` defs in app-core; only test-name hits (`store.rs:3398`, `executor.rs:5785`). |
| Generic retry/backoff | Framework owns transient-failure retry (`RuntimeDagExecutor`). | CLI `review.rs` retry_count/max_retries is the **reviewer-strategy** circuit breaker ("if review fails N times, Suspend or create a fix task") exercised through `resolve_dispatch`, not a parallel transient-retry loop. Comment: "Circuit breaker (plan §810-840): if the same task hits `max_retries`..." (`review.rs:16`). |
| Durable-result reuse | Product policy through the controller hook. | `dispatch_task` checks `store.recoverable_subagent_result(...)` and reuses a completed result after restart (`executor.rs:1271-1290`) — recovery policy, not a second executor. |

- The CLI's `TaskDispatcher` trait (`executor.rs:793`) is an EKO-INTERNAL
  dispatch abstraction (not a framework trait). It does NOT implement the
  framework's `TaskSubagent` (which is dead — V02/V03). It implements the
  framework's `RuntimeDagController`. The layering is correct.

### Deletion-target matrix (closed — each has a named successor)

| Target | Layer | Successor / reason | Finding |
|---|---|---|---|
| `TaskSubagent` trait + `TaskExecutionSummary`/`SuggestedTask` (FW `tasks/runtime.rs:296-345`) | framework | `RuntimeDagController` (`runtime_executor.rs:86`) fully covers it; zero implementors; CLI converter test-only | X-BND-01-P2-02 |
| `GLOBAL_EVENT_BUS` / `EventBus` (`event_bus.rs:11-45`) + `pub mod event_bus` (`lib.rs:39`) | framework | Direct stream composition (`envelope_event_stream`) covers it; zero callers | X-BND-01-P2-03 (reaffirms F-CORE-01-P2-01) |
| 5 of 6 `atomic_write` copies (after consolidation) | framework + app | One canonical `echo_core::utils::fs::atomic_write(path, bytes)` with parent-dir fsync; all sites call it | X-BND-01-P2-01 |
| CLI `WorktreeError` (`task_runtime/worktree.rs:117`) | adapter | Reuse framework `echo_agent::agent::subagent::worktree::WorktreeError` (add `From<io::Error>` there) OR document why the task-runtime subsystem needs its own | X-BND-01-P3-01 |
| (Optional) CLI `run_git` worktree primitive (`task_runtime/worktree.rs:149,412`) | adapter | Optionally reuse `echo_tools::git_worktree::create_worktree`; may be justified by extra prune/merge-base semantics — document if kept | X-BND-01-P3-02 |

- Log/artifact: none.

## Deviations

None for the adapter-authority checks. The `review.rs` retry-count fields
look superficially like a retry loop, but reading the body confirms they
encode reviewer-strategy policy (Suspend / CreateFix) dispatched through
`resolve_dispatch`, not a generic transient-retry loop. Flagged here so a
future reviewer does not re-classify it as a forbidden retry authority.

## Conclusion

The EKO adapters are thin and correct. The CLI implements the framework's
`RuntimeDagController` and uses the designed product-policy hooks
(`select_ready_wave` for file-ownership waves, `resolve_dispatch` for
reviewer strategy, durable-result reuse for recovery) without owning a
second frontier, scheduler, DAG validator, or generic retry loop. The
deletion-target matrix is closed: five targets, each with a named
successor and an owning finding. Supports the task report's positive
adapter conclusion and the five findings (P2-01, P2-02, P2-03, P3-01,
P3-02).

## Follow-Up

None at the validation level. The five deletion/consolidation targets are
handed off as findings to the synthesis/roadmap phase; they are not
implemented in this review.
