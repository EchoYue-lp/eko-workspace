# X-BND-01 / V03 / Attempt 01

> Status: passed_with_notes
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: echo-agent has unrelated dirty paths (`src/agent/react/run/*`, `src/testing/*`); echo-agent-cli clean.

## Claim

Framework public APIs that the CLI does not consume are legitimate
"framework option menu" items (multiple Store/Compressor/provider impls,
optional capabilities) retained per AGENTS.md, with the exception of a
small number of truly dead/superseded surfaces that are deletion
candidates. Specifically: the Store/Compressor menu is intact;
`SqliteStore`/`HybridCompressor` are correctly retained; `TaskSubagent`,
`GLOBAL_EVENT_BUS`, and `EventBus` are dead (not retained options).

## Method

Inventory framework pub backends, the CLI's picks, and framework pub
traits with zero implementors. Cross-reference the AGENTS.md rule
("framework API is retained unless framework-wide evidence shows it is
obsolete or fully replaced").

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# framework backend menu
grep -rn "^pub struct .*Store\b\|^pub struct .*Compressor\b\|^pub struct .*Conversation" \
  echo-agent --include="*.rs" | grep -v /target/

# CLI picks (what EKO actually constructs)
grep -nE "Store::new|::new\(|FileRuntimeStateStore|FileStore|InMemoryStore|SqliteStore|SlidingWindow|Summary|Hybrid" \
  echo-agent-cli/echo-agent-app-core/src/infra.rs

# framework traits with zero implementors (deletion candidates)
for t in TaskSubagent HumanLoopProvider Handoff Topology IntentClassifier; do
  grep -rn "impl.*${t}\b|impl ${t} for|: ${t}\b|dyn ${t}\b" \
    echo-agent echo-agent-cli --include="*.rs" | grep -v /target/
done

# dead infra re-confirm
grep -rn "GLOBAL_EVENT_BUS" echo-agent echo-agent-cli --include="*.rs" | grep -v /target/
```

## Expected Result

- Multiple framework Store/Compressor impls exist; CLI picks File backends;
  unused ones (`SqliteStore`, `HybridCompressor`, etc.) are retained
  (correct per AGENTS.md).
- `TaskSubagent`, `GLOBAL_EVENT_BUS`, `EventBus` have no producer/consumer
  in either repo (dead).
- Live traits (`HumanLoopProvider`, `IntentClassifier`) have implementors.

## Result

- Exit code: 0
- Duration: <15s
- Summary:

**Framework "menu" — legitimate retained options (CLI picks File backends,
unused ones retained per AGENTS.md):**

| Family | Framework impls | CLI pick |
|---|---|---|
| Memory `Store` | `FileStore` (`echo-state/src/memory/store.rs:220`), `InMemoryStore` (`store.rs:20`), `SqliteStore` (`sqlite_store.rs:65`) | `FileStore` (`infra.rs:1332`) |
| Conversation store | `FileConversationStore` (`file_conversation.rs:68`), `SqliteConversationStore` (`sqlite_conversation.rs:20`) | `FileConversationStore` (`infra.rs:1218`) |
| Runtime state | `FileRuntimeStateStore` (root `src/state/file.rs`), `SqliteRuntimeStateStore` (root `src/state/sqlite.rs`) | `FileRuntimeStateStore` (`infra.rs:1254`) |
| Compressor | `SlidingWindowCompressor`, `SummaryCompressor`, `HybridCompressor`+`Builder`, `VisibilityHorizonCompressor`, `AdaptiveCompressor` | (builder default; CLI does not directly name a compressor) |
| Other stores | `EmbeddingStore`, `TypedMemoryStore`, `ProfileStore`, `SkillTelemetryStore` | (selectively enabled) |
| Run store | — | CLI-local `JsonlRunStore` (`infra.rs:377`; app-owned, noted unbounded by F-OPS-01) |

- The CLI constructs ONLY File backends and does NOT enable the `sqlite`
  feature on `echo-state` (X-INV-01 V02-01 re-confirmed). Per AGENTS.md,
  `SqliteStore`/`SqliteConversationStore`/`HybridCompressor`/`InMemoryStore`
  are **legitimate retained framework options** — they are the menu for
  other consumers. The CLI's choice is product policy, NOT a deletion
  criterion. (Positive — AGENTS.md rule holds.)

**Framework pub traits — implementor count:**

| Trait | Implementors across both repos | Status |
|---|---|---|
| `TaskSubagent` (`runtime.rs:331`) | **0** | DEAD. Superseded by `RuntimeDagController`. Deletion candidate (X-BND-01-P2-02). |
| `GLOBAL_EVENT_BUS` / `EventBus` (`event_bus.rs:44/11`) | **0** (no `send`/`subscribe` callers) | DEAD. Reaffirms F-CORE-01-P2-01. Deletion candidate. |
| `HumanLoopProvider` | 42 hits | LIVE (multiple impls in framework + CLI). |
| `IntentClassifier` | 10 hits | LIVE. |
| `Topology` | 1 impl | Borderline (F-MAG-01 owns deep review). Not classified here. |
| `RuntimeDagController` (`runtime_executor.rs:86`) | CLI `EkoRuntimeDagController` (`executor.rs:1222`) + framework test impls | LIVE — the actual task-execution contract. |

- The two dead surfaces (`TaskSubagent`, `GLOBAL_EVENT_BUS`) meet the
  AGENTS.md deletion criterion (2): "fully replaced by a new implementation
  that covers the old capability" — `RuntimeDagController` replaces
  `TaskSubagent`; direct stream composition replaces the bus. They are NOT
  "trait with multiple impls" or "reasonable menu option" cases, so the
  "retain pub API" default does not rescue them.
- Log/artifact: none.

## Deviations

`Topology` (1 implementor) was not deeply classified — its ownership
review belongs to F-MAG-01. Recorded as residual uncertainty.

## Conclusion

The framework's backend "menu" (multiple Store/Compressor impls) is
correctly retained even though the CLI picks File backends — this is the
AGENTS.md framework-API rule working as intended. Two dead framework
surfaces (`TaskSubagent` trait, `GLOBAL_EVENT_BUS`/`EventBus`) are NOT
retained options; they are fully-replaced superseded code and qualify as
deletion targets. The live task-execution contract is `RuntimeDagController`
(implemented by the CLI). Supports X-BND-01-P2-02 and reaffirms
F-CORE-01-P2-01. The V03 "public framework option check" passes for
everything except the two named dead surfaces, which become findings.

## Follow-Up

V04-01 consolidates the deletion-target matrix (the two dead surfaces plus
the `atomic_write` consolidation plus the `WorktreeError` near-duplicate)
and spot-checks the live adapters (`RuntimeDagController`, `select_ready_wave`,
`review.rs`) for forbidden scheduling/state authority.
