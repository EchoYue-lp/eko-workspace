# F-FEAT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only inspection)

## Claim

Every `[features]` entry defined in `echo-agent` can be enumerated, and each
feature's `cfg(...)` footprint across the workspace is accounted for. No
feature is both defined and untraceable.

## Method

Read-only `cfg(feature = ...)` census across all 8 crates of the
`echo-agent` workspace. Working directory:
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`.

```text
1. grep -rn 'cfg(feature = ' --include='*.rs' . | sort | uniq -c
2. Aggregate per crate (root echo_agent + 7 sub-crates).
3. Cross-reference each feature name against the [features] table in
   echo-agent/Cargo.toml.
4. Confirm root [features] definitions for every name appearing in cfg gates.
```

## Expected Result

A finite, per-crate count of `cfg(feature = "...")` occurrences that matches
the declared feature set in `echo-agent/Cargo.toml`. Sub-crates that do not
declare features should contribute only via their own `[features]` tables
(forwarded through the root manifest).

## Result

- Exit code: 0
- Summary: 302 `cfg(feature = ...)` occurrences across the workspace. The
  root crate dominates (226), followed by `echo-tools` (48). `echo-macros`
  contributes 0 (it is a proc-macro crate with no feature gates). Every
  feature name observed in `cfg` gates is declared in the root manifest.

### cfg(feature count per crate

| Crate | cfg(feature count |
|---|---:|
| root (`echo_agent`) | 226 |
| echo-tools | 48 |
| echo-core | 13 |
| echo-state | 6 |
| echo-execution | 3 |
| echo-integration | 3 |
| echo-orchestration | 3 |
| echo-macros | 0 |
| **Total** | **302** |

### Feature definitions inventory (root `echo-agent/Cargo.toml`)

The root manifest declares 35 features (including `default = []` and the
`full` aggregator). Key groups observed:

- **Tool / capability features**: `tasks`, `eval`, `handoff`, `topology`,
  `project-rules`, `testing`, `improve`, `workflow` — each gates a module
  under `src/` via `#[cfg(feature = "...")]`.
- **Dependency-enabling features**: `a2a`, `mcp`, `lsp`, `sqlite`,
  `telemetry`, `human-loop`, `web`, `media`, `data`, `statistics`,
  `channels`, `git`, `database`, `rag`, `chart`, `shell`, `files`,
  `research`, `content-guard` — each activates one or more optional
  dependencies via `dep:` prefix.
- **Composed features**: `subagent = ["tasks"]` (correctly forwards to the
  tasks module that subagent code imports), `full` (aggregates most
  features).
- **Marker / suspect features**: `sandbox`, `semantic-memory`, `macros`,
  `provider-factory`, `multimodal` — declared as `= []` with 0 cfg matches
  (see V02).

### Duplicate search

No two crates declare a feature with the same name and conflicting
definitions. Sub-crate features referenced from the root (e.g.
`echo_integration/mcp`, `echo_core/lsp`, `echo_state/sqlite`,
`echo_orchestration/websocket`) are forwarded via the standard
`<crate>/<feature>` syntax and exist in the corresponding sub-crate's
`[features]` table.

## Deviations

None.

## Conclusion

Supports F-FEAT-01 finding inventory: all features are enumerated and their
`cfg` footprints are accounted for. Establishes the baseline used by V02 to
classify features as either live (gates real code) or dead (0 cfg matches).
No duplicate feature definitions exist across the workspace.

## Follow-Up

V02 classifies the 5 zero-cfg features as dead and identifies the 8 marker
features that gate modules. `None.` for this report.
