# F-HITL-01 / V01-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable (framework permission model only)
> Worktree state: clean

## Claim

The permission model composes three typed primitives — `PermissionMode`
(launch policy), `ToolPermission[]` (per-tool capability set), and
`PermissionDecision` (per-call result) — through a single authoritative
pipeline in `PermissionService::check_with_permissions_in_mode`, with
exactly one live per-call check path (`AgentRunSnapshot::check_tool_approval`
invoked by `PermissionStage`) and no live duplicate.

## Method

Static inspection at `echo-agent/` commit `9b0e0fa`. Working directory
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`.

```text
1. Confirm the three typed primitives and their variant counts:
   grep -n "pub enum ToolPermission\|pub enum PermissionMode\|pub enum PermissionDecision" \
     echo-core/src/tools/permission.rs

2. Confirm the single authoritative check pipeline by reading the
   decision-returning arms of:
   echo-orchestration/src/human_loop/service.rs   (check_with_permissions_in_mode, lines 484-681)

3. Confirm the single live per-call caller of the service from the tool
   pipeline, and that it targets the SNAPSHOT method (3-arg) not the
   ReactAgent method (2-arg):
   grep -rn "\.check_tool_approval(" --include="*.rs" src/ echo-orchestration/

4. Repository-wide duplicate search for parallel permission services /
   check entry points:
   grep -rn "pub.*fn check_with_permissions\|struct PermissionService\b" \
     --include="*.rs" . | grep -v "/target/"
```

## Expected Result

1. Exactly one definition each for `ToolPermission` (5 variants),
   `PermissionMode` (8 variants), `PermissionDecision` (4 variants), all
   in `echo-core/src/tools/permission.rs`.
2. `check_with_permissions_in_mode` contains step 0 (protected paths),
   steps 1–8 (bypass / plan / rules / cache / denial / handler-check /
   mode-dispatch / post-processing), each returning a
   `PermissionDecision`.
3. Exactly two `check_tool_approval` call sites, both in
   `pipeline.rs`, both invoking the snapshot method; the ReactAgent
   method has zero external callers.
4. Exactly one `PermissionService` definition and one
   `check_with_permissions*` entry-point family.

## Result

- Exit code: 0 (all greps and reads succeeded)
- Duration: n/a (static inspection)
- Summary: all four expectations met.
  - Step 1: `ToolPermission` at `echo-core/src/tools/permission.rs:19`
    (Read/Write/Network/Execute/Sensitive — 5);
    `PermissionMode` at `:59` (Default/Plan/AcceptEdits/BypassPermissions/
    Auto/Bubble/DontAsk/StrictConfirm — 8); `PermissionDecision` at
    `:136` (Allow / Deny{reason} / RequireApproval / Ask{suggestions}
    — 4).
  - Step 2: `check_with_permissions_in_mode` (`service.rs:484-681`)
    contains the full pipeline: step 0 protected-paths (`:512-528`,
    returns `Deny` before bypass); step 1 bypass (`:531-542`); step 2
    plan (`:545-559`); step 4 rules (`:562-568`, deny-first via
    `RuleRegistry::check`); step 5 cache (`:571-573`); step 6 denial
    tracker (`:576-585`); handler-presence gate (`:588-600`); mode
    dispatch (`:603-648`); post-processing + audit (`:651-678`). Every
    terminal arm returns a `PermissionDecision`.
  - Step 3: grep returns exactly two callers, `pipeline.rs:330` and
    `pipeline.rs:336`, both calling `snapshot.check_tool_approval`
    (3-arg, `snapshot.rs:798`). The 2-arg `ReactAgent::check_tool_approval`
    (`approval.rs:65`) has zero external callers; its impl block is
    `#[allow(dead_code)]` (`approval.rs:11`).
  - Step 4: one `pub struct PermissionService` (`service.rs:145`); the
    `check_with_permissions*` family is `check` (`:455`),
    `check_with_permissions` (`:470`),
    `check_with_permissions_in_mode` (`:484`) — a delegation chain
    (each wraps the next), not parallel implementations.
- Log/artifact: none (static inspection; no build artifacts).

## Deviations

None. The `human-loop` feature is on the root `echo_agent` package, not
on `echo_orchestration`; this affected only how tests are invoked
(documented in the task report), not the inspection results.

## Conclusion

Supports finding F-HITL-01-P3-03 (dead `ReactAgent` approval block) and
the task's primary conclusion: the model composes cleanly through one
authoritative pipeline. Does not, by itself, prove the pipeline's
runtime behavior under concurrency or recovery — that is V02/V03/V04
and F-RCT-04. The composition (mode × permission[] → registry → cache →
denial tracker → mode dispatch → decision) is fully traceable, and
there is no live duplicate authority.

## Follow-Up

None for V01. The dead `ReactAgent::check_tool_approval` is filed as
F-HITL-01-P3-03; its removal should be coordinated with F-RCT-02-P2-02
(removal of the dead `process_steps` that holds the last caller of
`ReactAgent::tool_needs_approval`).
