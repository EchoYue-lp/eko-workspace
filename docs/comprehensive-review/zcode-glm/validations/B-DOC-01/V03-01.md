# B-DOC-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa (9b0e0faf74d35c9a432370b923acabfbb5f32d63)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

A term/path search across the audit + plan + architecture documents under
review surfaces obsolete terms (`worker`), stale paths (`echo-agent-eval`,
removed `improve/store.rs`), and misframed framework-vs-EKO options
(`SqliteStore` in CLI-facing docs) so that they can be classified for the
drift index.

## Method

Run `grep -rn -i` for each target term across the documented doc sets:

1. `worker` (case-insensitive, word boundary) — AGENTS.md mandates `subagent`.
2. `echo-agent-eval` / `echo_agent_eval` — noted as stale in B-BASE-01.
3. `SqliteStore` / `SqliteConversationStore` / `SqliteRuntimeStateStore` —
   legitimate framework options per AGENTS.md, but should not be implied as
   EKO's active backing store in CLI-facing docs.
4. `todo_write` / `plan_create` / `plan_patch` / `plan_execute` — deprecated
   APIs removed by M13 + task tools migration.
5. `improve/store.rs`, `improve/evolution.rs` — AUDIT_REPORT paths.
6. `require_full_auto` — AGENTS.md historical-lesson marker for removed
   security gates.

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
grep -rn -i "<term>" docs/ echo-agent/AUDIT_REPORT.md echo-agent/CHANGELOG.md \
  echo-agent/docs/ echo-agent-cli/docs/ \
  | grep -v comprehensive-review   # exclude review-internal dirs
```

## Expected Result

- `worker`: zero hits in current MASTER-PLANs, AUDIT, CHANGELOG, sub-crate
  READMEs, system-deep-dive; possibly still in old sprint/iteration docs as
  historical narration.
- `echo-agent-eval`: stale references in old roadmap doc; correctly noted as
  removed in current deep-dive.
- `SqliteStore`: present in framework-doc contexts; in CLI docs should
  consistently carry the "EKO does not use SQLite" caveat.
- `todo_write` / `plan_*`: only in migration design docs (justificative
  context).
- `improve/store.rs` / `improve/evolution.rs`: should be referenced only in
  AUDIT_REPORT (historical).
- `require_full_auto`: should be zero outside error.rs.

## Result

- Exit code: not-applicable (static inspection)

### Term 1: `worker` (case-insensitive)

| Doc set | Hits | Notes |
|---|---|---|
| Root `docs/MASTER-PLAN.md` | 0 | Clean |
| `echo-agent/AUDIT_REPORT.md` | 0 | Clean |
| `echo-agent/CHANGELOG.md` | 0 | Clean |
| `echo-agent-cli/docs/MASTER-PLAN.md` | 0 | Clean |
| `echo-agent-cli/docs/architecture.md` | 0 | Clean |
| `echo-agent-cli/docs/system-deep-dive/*` | 0 | Clean |
| All `echo-agent/echo-*/README.md` | 0 | Clean |
| `echo-agent/docs/{en,zh,knowledge}/*` | 0 (only false positive: `LlmError::NetworkError` substrings in mock docs) | Clean |
| All `echo-agent-cli/docs/*` (full) | 0 | Clean |
| `docs/deep-iteration-plan.md` (2026-07-01) | ~30 | Historical narration of pre-unification Subagent terminology; explicitly predates the unification |
| `docs/PROJECT-ANALYSIS.md` (2026-07-01) | ~10 | Same era |
| `docs/2026-07-12-echo-agent-peer-framework-iteration.md` | 1 (`wakeup worker` in unrelated product-deployment narration) | Not the Subagent sense |
| `docs/superpowers/specs/2026-07-01-sprint-10b-and-11-design.md` | 2 | Historical spec |

Verdict: `worker` is **clean in every live document**. Remaining hits are in
pre-2026-07-15 iteration plans and analyses, which are explicitly historical
narration (the `subagent-unification-plan.md` dates the unification as
completed in M11/M13 era). No live doc needs migration.

### Term 2: `echo-agent-eval` / `echo_agent_eval`

| Location | Hit | Status |
|---|---|---|
| `docs/superpowers/plans/2026-07-10-subagent-parity-roadmap.md:74,699` | References `echo-agent-cli/echo-agent-eval` as a candidate eval location | **stale** — directory does not exist; `Cargo.toml [workspace.members]` only lists `echo-agent-app-core` |
| `echo-agent-cli/docs/system-deep-dive/06-skills.md:427` | "`SkillGateway` ... 曾引用它的 echo-agent-eval crate 也已移除" | **current** — correctly notes the removal |
| Anywhere in `echo-agent-cli/Cargo.toml`, `echo-agent-app-core/Cargo.toml`, `src/`, `echo-agent-app-core/src/` | 0 | Clean |

Verdict: stale references exist only in a `docs/superpowers/plans/*` roadmap
from 2026-07-10; live docs already note the crate was removed.

### Term 3: `SqliteStore` / `SqliteConversationStore` / `SqliteRuntimeStateStore`

| Location | Framing | Status |
|---|---|---|
| `echo-agent-cli/docs/memory-evolution-full-audit.md:75` | "❌ EKO 明确不使用 SQLite（AGENTS.md 硬约束，使用 `FileConversationStore`）" | **current** (correct caveat) |
| `echo-agent-cli/docs/memory-evolution-full-audit.md:89,90,147,653,656` | Lists `SqliteRuntimeStateStore` / `SqliteConversationStore` without re-stating the caveat in each row | **partly stale** — caveat present at top of table but not each row |
| `echo-agent-cli/docs/runtime-architecture-audit.md:45` | "SqliteConversationStore | `echo-agent/echo-state/...` | 对话历史持久化" | **stale** — this is the deprecated 2026-06-09 runtime audit; the doc carries a top-banner notice that it is superseded by `2026-07-16-agent-lifecycle-audit.md` |
| `echo-agent-cli/docs/system-deep-dive/04-memory.md:16-18` | Three-row table lists `SqliteStore` / `SqliteRuntimeStateStore` / `SqliteConversationStore` as the persistence backing with **no** caveat that EKO uses the `File*` alternatives | **stale / misleading** — implies EKO uses SQLite for these layers, contradicting AGENTS.md and B-PATH-01's evidence that EKO uses `FileConversationStore`/`FileRuntimeStateStore` |
| `echo-agent-cli/docs/2026-07-28-app-core-full-audit.md:176` | "Framework only ships `SqliteConversationStore`" | **stale** — incorrect: framework also ships `FileConversationStore` (per V02 verification) |

Code cross-check: zero `SqliteRuntimeStateStore`/`SqliteConversationStore`
imports in `echo-agent-cli/`. `echo-agent-app-core/Cargo.toml` does not enable
the `sqlite` feature. CLI uses `FileConversationStore::new` at
`echo-agent-cli/echo-agent-app-core/src/state.rs:905,1079`.

### Term 4: `todo_write` / `plan_create` / `plan_patch` / `plan_execute`

All hits in current docs are inside the migration design /
runtime-DAG-convergence closeout docs:

- `echo-agent-cli/docs/2026-07-28-task-tools-framework-migration-design.md` —
  ~20 hits, all describing the removal of `todo_write` (justificative).
- `echo-agent-cli/docs/framework-app-boundary-plan.md:261` —
  "Process-global `todo_write` was removed..." (current statement of fact).
- `echo-agent-cli/docs/2026-07-27-runtime-dag-kernel-convergence.md:175,189` —
  "Replaced the product tool surface `plan_create/plan_patch/plan_execute`..."
  (current statement of fact).

Verdict: all references are historical/justificative in design docs that
describe the migration. **None** are live instructions to use these APIs.

### Term 5: `improve/store.rs`, `improve/evolution.rs`

- AUDIT_REPORT.md §6.3 cites both paths. Both **files no longer exist** —
  `echo-agent/src/improve/` now contains only `analyzer.rs`,
  `eval_improvement.rs`, `generator.rs`, `loop.rs`, `mod.rs`, `trajectory.rs`.
- No other doc references these paths.

Verdict: **stale** within AUDIT_REPORT.md (covered in V01).

### Term 6: `require_full_auto`

- Definition: `echo-agent-cli/src/tauri/error.rs:45`
  `pub fn require_full_auto(mode: &str) -> Result<(), IpcError>`.
- Callers: **zero** (only the docstring at `error.rs:6` claims it is used).
- The actual `connect_mcp_server` (`src/tauri/commands/mcp.rs:211`) has an
  explicit comment: "we don't gate it behind a permission mode" — matching
  AGENTS.md's historical-lesson guidance.

Verdict: `IpcAuth::require_full_auto` is **dead code with a misleading
docstring**. The historical-lesson work (removing security gates from
interactive tools) was completed, but the helper and its "execute arbitrary
code are gated behind..." header comment were not cleaned up.

## Deviations

None.

## Conclusion

Supports: the doc corpus is largely clean of obsolete terminology. Three
specific drift points to record in the index:

1. **`SqliteStore` framing in `echo-agent-cli/docs/system-deep-dive/04-memory.md`
   is misleading** — the table at lines 16-18 lists SQLite-backed stores as
   the persistence for `Store` / `RuntimeStateStore` / `ConversationStore`
   without the AGENTS.md caveat that EKO uses the `File*` alternatives.
2. **`IpcAuth::require_full_auto`** is dead framework code with a misleading
   docstring (no callers; the doc claim is false).
3. **`echo-agent-eval`** is referenced as a candidate location in
   `docs/superpowers/plans/2026-07-10-subagent-parity-roadmap.md` but the
   crate has been removed.

Does not prove: that no other stale paths exist (only the targeted terms were
searched).

Used by findings: `B-DOC-01-P2-02`, `B-DOC-01-P3-04`, `B-DOC-01-P3-05`.

## Follow-Up

None. The findings spawned by this validation are documentation cleanups; the
parent task does not implement them.
