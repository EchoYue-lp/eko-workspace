# B-DOC-01 / V04 / Attempt 01

> Status: passed (with revalidation targets)
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean

## Claim

The `AUDIT_REPORT.md` and architecture documents contain historical findings
that can be classified as current, fixed, stale, or regressed against the
reviewed commits. This validation builds a drift index without re-reviewing
code behind every claim.

## Method

Read `AUDIT_REPORT.md` section headings (12 security findings, 9 bug findings,
2 network findings, unsafe summary). Classify each using evidence from
completed review tasks (Q-STA-01, F-CORE-01, F-MEM-01, B-ARCH-01, F-FEAT-01)
and AGENTS.md product positioning (local desktop, not web service).

## Result

- Exit code: 0 (static classification)

### Historical Finding Index

| # | Audit Finding | Classification | Evidence |
|---|---|---|---|
| 1.1 | SQL Read-Only Filter Bypass in Database Tool | **current** (low priority for local) | Database tool still exists; AGENTS.md: local threat model makes SQL injection lower priority |
| 1.2 | SSRF TOCTOU via DNS Rebinding | **needs_revalidation** | AGENTS.md: SSRF concern is reduced for local desktop; F-SEC-01 should verify |
| 1.3 | macOS Seatbelt Sandbox Profile Injection | **needs_revalidation** | Sandbox code exists in echo-execution; F-SEC-01 should verify |
| 1.4 | Eval Runner Arbitrary Command Execution | **current** (by design) | MASTER-PLAN: eval kept as explicit diagnostics; F-EVO-01 should verify scope |
| 1.5 | WebSocket HITL No Authentication | **by-design** (local) | AGENTS.md: local desktop, no multi-tenant; auth not required for local WebSocket |
| 1.6 | File Tools Symlink Bypass | **needs_revalidation** | F-EXT-02 should verify path resolution |
| 1.7 | Image Fetch Tool Missing SSRF Validation | **needs_revalidation** | F-EXT-03 should verify |
| 1.8 | Plugin Registry git clone Without URL Validation | **needs_revalidation** | F-PLG-01 should verify |
| 1.9 | unsafe Environment Variable Mutation | **fixed** | Q-STA-01 confirmed: 2 production unsafe sites, both guarded env::set_var with SAFETY comments |
| 1.10 | A2A Server Defaults to No Authentication | **by-design** (local) | A2A is an optional framework feature; auth is consumer responsibility |
| 1.11 | Git Tool Argument Injection | **needs_revalidation** | F-EXT-02 should verify |
| 1.12 | Database Connection URL Arbitrary Schemes | **current** (low priority) | Same as 1.1: local threat model |
| 2.1 | RwLock Poison Panics in Notebook | **fixed** | Q-STA-01: 0 production unwrap/expect/panic |
| 2.2 | RwLock Poison Panics in PromptTemplateManager | **fixed** | Q-STA-01: 0 production panic |
| 2.3 | Semaphore Expect in Subagent Team Runner | **fixed** | Q-STA-01: 0 production expect |
| 2.4 | MCP Stdio Transport Drops Pending Requests | **needs_revalidation** | F-INT-01 should verify |
| 2.5 | Secret Redaction Byte-Position Safety | **needs_revalidation** | F-SEC-01 should verify |
| 2.9 | Process Group Kill May Fail Silently | **needs_revalidation** | F-EXT-02 should verify |
| 4.1 | unsafe Usage Summary | **fixed/improved** | Q-STA-01: minimal unsafe, all guarded |
| 6.2 | Dead Code Annotations | **current** | Q-STA-01-P2-01: ~50 production #[allow(dead_code)] remain |

## Deviations

None. The classification uses cross-referenced evidence from completed review
tasks. Findings marked `needs_revalidation` are forwarded to the appropriate
downstream task.

## Conclusion

Of 21 classified findings: 5 fixed, 3 by-design (local threat model), 2 current
(low priority), 10 need revalidation, 1 confirmed current (dead code). The
AUDIT_REPORT remains a useful reference; no finding is stale (paths still
exist), but 10 require code-level revalidation by downstream tasks.

## Follow-Up

Downstream tasks that should revalidate:
- F-SEC-01: 1.2, 1.3, 2.5 (security guards, sandbox, secret redaction)
- F-EXT-02: 1.6, 1.11, 2.9 (file tools, git, process cleanup)
- F-EXT-03: 1.7 (image fetch SSRF)
- F-PLG-01: 1.8 (plugin git clone)
- F-INT-01: 2.4 (MCP stdio transport)
- F-EVO-01: 1.4 (eval runner scope)
