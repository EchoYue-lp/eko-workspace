# B-DOC-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa (9b0e0faf74d35c9a432370b923acabfbb5f32d63)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

The root `docs/MASTER-PLAN.md` states M1-M13 are complete and the CLI
`echo-agent-cli/docs/MASTER-PLAN.md` states ~40 milestones are complete. For a
sampled concrete code claim per milestone, the cited artifact exists in the
current source tree.

## Method

For each milestone M1-M13 plus three CLI-MP "Iteration" milestones plus two
representative CLI-MP "Plugin/Hook" milestones, pick one concrete code anchor
from the milestone description and verify it via `grep`/`Read`. No
re-execution; just presence + correct layer (framework vs app).

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

## Expected Result

All sampled milestone anchors resolve. The MASTER-PLAN update protocol
(AGENTS.md § "阶段归档") keeps these in sync after each commit, so drift
should be near zero.

## Result

- Exit code: not-applicable (static inspection)

### Milestone anchor matrix

| Milestone | Sampled code claim | Evidence location | Layer | Status |
|---|---|---|---|---|
| M1 | agent-lifecycle-audit doc exists | `echo-agent-cli/docs/2026-07-16-agent-lifecycle-audit.md` (file present) | doc | current |
| M2 | CLI REPL routes through `drive_chat` | `echo-agent-cli/src/cli/repl.rs:543` `echo_agent_app_core::chat_driver::drive_chat(...)` | app | current |
| M3 | TaskRuntimeStore has recovery `Paused → Running` transitions | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/store.rs:430` "Resume a paused run: `Paused → Running`"; `recover_incomplete` at `:1631` | app | current |
| M4 | Seven-class tool failure contract | `echo-agent/echo-core/src/tools/mod.rs:33-39` enum strings `invalid_arguments`/`unavailable`/`timeout`/`cancelled`/`transient`/`permanent`/`partial_side_effect` | framework | current |
| M5 | `RunCodeTool` uses SandboxCommand (no bare fallback) | `echo-agent/echo-tools/src/code.rs:18` imports `SandboxCommand`; `:277-282` constructs `SandboxCommand::code`/`program`; no `tokio::process::Command` bare path | framework | current |
| M6 | Tool-output artifact scope cleanup | `echo-agent-cli/echo-agent-app-core/src/prepared_turn.rs:149` references `cleanup_tool_output_scope`; `agent_pool.rs:207` documents "Product-owned complete tool-output artifact policy" | app | current |
| M7 | `SubagentOutcome` structured result | `echo-agent/src/agent/subagent/types.rs:331` `pub struct SubagentOutcome` | framework | current |
| M8 | Writer worktree merge commit trailer + `merge-tree` precheck | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/worktree.rs:577` `EKO-Execution-Id: {execution_id}` trailer; `:731` `"merge-tree"` arg | app | current |
| M9 | `RunStore` framework-owned; per-invocation `trace_run_id`; `UsageTrendsPanel` removed | `echo-agent/src/trace/analyzer.rs:14` imports `RunStore`; `:161` `TraceAnalyzer { run_store: Arc<dyn RunStore> }`; frontend `UsageTrendsPanel` zero hits | framework + app | current |
| M10 | `ChatDriverEvent` single ChatSink entry; CLI/TUI/channel sink impls exist | `echo-agent-cli/echo-agent-app-core/src/chat_driver.rs:30` `pub enum ChatDriverEvent`; `:53` `pub trait ChatSink`; `:585` `impl ChatSink for ChannelChatSink` | app | current |
| M11 | `DomainProfile` enum + analyst default routing | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/types.rs:33` `pub enum DomainProfile` (with General/DataAnalysis/... variants at `:42`) | app | current |
| M12 | `hypothesis_test`/`regression` removed; only `exploratory_statistics` remains | `echo-agent/echo-tools/src/registry.rs:478,492` assert non-presence; `echo-tools/src/statistics.rs:18` `TOOL_NAME = "exploratory_statistics"`; no production `hypothesis_test` registration | framework | current |
| M13 Phase 4 | `PlanSpec`/`PlanTaskSpec` framework canonical authoring artifact | `echo-agent/echo-orchestration/src/planning/plan_spec.rs:18 pub struct PlanSpec`; `:60 pub struct PlanTaskSpec` | framework | current |
| M13 Phase 5 | `task_create/update/list` are framework tools; `todo_write` removed from default agent | `echo-agent/echo-orchestration/src/tasks/task_tools.rs:27,94,155`; `echo-agent/src/agent/react/builder.rs:1181` asserts `"todo_write"` is NOT in default registration | framework | current |

### CLI MASTER-PLAN sampled anchors (Iteration + Plugin milestones)

| CLI MP milestone | Sampled code claim | Evidence location | Layer | Status |
|---|---|---|---|---|
| Iteration 1 | `InstructionProvider` sole EKO protocol owner; `learned-rules.md` rename migration | `echo-agent-cli/echo-agent-app-core/src/instruction_provider.rs:33 pub struct InstructionProvider`; `:28 LEARNED_RULES_FILE = "learned-rules.md"`; `:84-105` one-time migration logic | app | current |
| Iteration 2 | `Arc<WebhookEmitter>` shared across chat/scheduler/surface | `echo-agent-cli/src/cli/modes.rs:40,80,125` param `webhook_emitter: std::sync::Arc<echo_agent_app_core::webhook::WebhookEmitter>` | app | current |
| Iteration 3 | `FileConversationStore` migrated down to framework; `restore_messages` is `Result`-returning | `echo-agent/echo-state/src/memory/file_conversation.rs:68 pub struct FileConversationStore`; `echo-agent/echo-state/src/memory/conversation.rs:107,197 pub fn restore_message(s) -> Result<...>` | framework | current |
| Tool Schema budget | `task_create`/`update`/`list` framework default; cursor pagination in tool output | framework task tools confirmed above; `cursor`/`next_cursor` field present in `echo-tools/src/database.rs` schema at lines 76-77 | framework | current |
| PluginRuntimeService (P0-4) | `pub struct PluginRuntimeService` is process-level owner | `echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs:130 pub struct PluginRuntimeService` | app | current |
| Hook execution closure | 31 HookEvent + 7 HookAction (already verified in V01) | `echo-agent/echo-core/src/hooks/types.rs:127`; `echo-agent/echo-execution/src/skills/hooks.rs:145` | framework | current |
| Typed TaskRuntime lifecycle | `HookEventDispatcher` exists; `TodoStatus` preserves terminal states | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/hook_event_dispatcher.rs:49 pub struct HookEventDispatcher` | app | current |

### Layering check

All framework claims resolve to `echo-agent/` paths; all EKO-policy claims
resolve to `echo-agent-cli/echo-agent-app-core/` or `echo-agent-cli/src/`
paths. Zero cross-layer misclassifications in the sampled set. This is
consistent with B-ARCH-01's finding that the Cargo-level layering is clean.

## Deviations

None. No claim required re-execution.

## Conclusion

Supports: both MASTER-PLANs are reliable as a current-state index. 19 / 19
sampled milestone anchors resolve to symbols in the correct layer. The
root MASTER-PLAN's M1-M13 status table and the CLI MASTER-PLAN's ~40-row
status table are accurate at the code-anchor level.

Does not prove: the milestones' full acceptance criteria are still met (would
require re-running tests/contracts). Only the **code-anchor** claim was
verified per milestone. Behavioural drift (e.g. "M3 recovery contract holds
under crash X") belongs to A-TASK-*, F-RCT-*, and Q-* tasks.

Used by findings: none directly (this is positive evidence supporting the
Historical Claim Status table in the task report).

## Follow-Up

None. If a milestone's acceptance test matrix needs revalidation, that work
belongs to a Phase A task (e.g. A-TASK-01 for TaskRuntime recovery), not to
B-DOC-01.
