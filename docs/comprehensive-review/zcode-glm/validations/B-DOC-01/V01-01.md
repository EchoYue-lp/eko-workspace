# B-DOC-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa (9b0e0faf74d35c9a432370b923acabfbb5f32d63)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

Concrete code anchors cited by `echo-agent/AUDIT_REPORT.md`, root
`docs/MASTER-PLAN.md`, and `echo-agent-cli/docs/MASTER-PLAN.md` still resolve to
real symbols in the current source tree. When anchors fail to resolve, this
validation records the drift classification (current / fixed / stale / regressed).

## Method

For each sampled claim, run a `grep`/`Read` lookup against the current source
tree (no re-execution of code). Classify each result against the document's
wording. Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

15 specific claims sampled across the three documents:

| # | Source | Claim |
|---|---|---|
| 1 | root MP § 三 | `drive_chat` unified driver exists in `chat_driver.rs` |
| 2 | root MP § 三 | versioned `EventEnvelope`, stable tool `call_id`, terminal exactly-once |
| 3 | root MP § 三 | `TaskRuntime` has plan/task/run, pause/resume/cancel, cron, background |
| 4 | root MP § 二十四 | `echo-orchestration::RuntimeDagExecutor` is the only DAG traversal kernel |
| 5 | root MP § 九 (M13 Phase 4) | Canonical model is `TaskSpec + TaskExecution + TaskStatus + Task` |
| 6 | root MP § 三 | Framework task API is `TaskSpec + TaskExecution + TaskStatus`; rich hooks/verifier as `ManagedTask`; LLM artifact as `PlanTaskSpec` |
| 7 | CLI MP (current) | `task_create/task_update/task_list` framework tools exist |
| 8 | CLI MP (current) | EKO uses `EkoRevisionedTaskStore` + `EkoTaskToolPolicy` adapter |
| 9 | CLI MP (current) | 31 HookEvent variants and 7 HookAction variants |
| 10 | AUDIT_REPORT 1.1 | `SqlQueryTool` filter in `echo-tools/src/database.rs` lines 76-116 |
| 11 | AUDIT_REPORT 1.2 | `validate_url` SSRF check in `echo-tools/src/security.rs` lines 441-467 |
| 12 | AUDIT_REPORT 1.4 | `run_command` `sh -c` in `src/eval/runner.rs` lines 338-347 |
| 13 | AUDIT_REPORT 1.5 | WebSocket HITL "zero authentication" at `echo-orchestration/src/human_loop/websocket.rs` lines 92-131 |
| 14 | AUDIT_REPORT 6.3 | `src/improve/store.rs` silent file write errors |
| 15 | root MP § 二十四 | `echo-agent` root crate facade has real `state/`, `agent/`, `event_bus.rs`, `llm.rs` compat wrappers |

```text
For each claim:
  1. grep the cited symbol/path/line in the current tree
  2. if path resolves: verify the symbol still exists at (or near) the cited line
  3. record drift classification
```

## Expected Result

Most MASTER-PLAN code anchors resolve (these docs are kept in sync by the
project's "update MASTER-PLAN after each milestone" rule). AUDIT_REPORT
anchors are expected to show line-number drift and some recommendations to be
fixed, because the audit is dated 2026-05-31 and the codebase has had 2.5
months of milestone work since.

## Result

- Exit code: not-applicable (static inspection)
- Duration: n/a

### Detailed outcomes

| # | Anchor | Symbol exists? | Drift | Classification |
|---|---|---|---|---|
| 1 | `drive_chat` | YES — `echo-agent-cli/echo-agent-app-core/src/chat_driver.rs:202` `pub async fn drive_chat(...)` | None | **current** |
| 2 | `EventEnvelope` + call_id + terminal exactly-once | YES — `echo-agent/echo-core/src/agent/event_envelope.rs:26` `pub struct EventEnvelope` | None | **current** |
| 3 | `TaskRuntime` plan/task/run + recovery | YES — `TaskRun`/`PlanTask`/`SubagentRun` at `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/types.rs:801,985,1656`; `recover_incomplete` at `store.rs:1631` | None | **current** |
| 4 | `RuntimeDagExecutor` only DAG kernel | YES — `echo-agent/echo-orchestration/src/tasks/runtime_executor.rs:175` `pub struct RuntimeDagExecutor<C: RuntimeDagController>`; `PlanValidator` at `planning/validator.rs:14` is sole cycle check (`task_dependency_cycles` at line 354) | None | **current** |
| 5 | `TaskSpec/TaskExecution/TaskStatus/Task` | YES — all four at `echo-agent/echo-orchestration/src/tasks/runtime.rs:179,228,90,251` | None | **current** |
| 6 | `ManagedTask` / `PlanTaskSpec` | YES — `echo-agent/echo-orchestration/src/planning/plan_spec.rs:18,60` | None | **current** |
| 7 | framework `task_create/update/list` tools | YES — `echo-agent/echo-orchestration/src/tasks/task_tools.rs:27,94,155` (string literals) | None | **current** |
| 8 | `EkoRevisionedTaskStore` + `EkoTaskToolPolicy` | YES — `echo-agent-cli/.../tasks/task_runtime/revisioned_adapter.rs:26,80` | None | **current** |
| 9 | 31 HookEvent + 7 HookAction | YES — `echo-agent/echo-core/src/hooks/types.rs:127` declares 31 variants in `ALL`; `echo-agent/echo-execution/src/skills/hooks.rs:145` declares 7 HookAction variants (Command/Prompt/Permission/Http/McpTool/Agent/ActivateSkill) | None | **current** (CLI MP claim of 31/7 matches; root MP § 二十三.5 P0 audit note "33 events/7 actions" is stale) |
| 10 | `echo-tools/src/database.rs` lines 76-116 | FILE EXISTS; prefix check now at line ~95+; INTO OUTFILE/DUMPFILE + PRAGMA added to denylist (lines 149-161); SET TRANSACTION READ ONLY second layer comment (lines 88-89) | Line drift + recommendation partially applied | **stale** (line numbers); **fixed** (audit recommendation: keyword + DB-level READ ONLY) |
| 11 | `echo-tools/src/security.rs` lines 441-467 | FILE EXISTS; `validate_url` now at line 529; new `validate_url_with_addrs` at line 541 returns pinned IPs to close TOCTOU; `is_private_ip`/`is_private_v4` at lines 615/647 now include 100.64.0.0/10, 192.0.0.0/24, 198.18.0.0/15, TEST-NET-1/2/3, IPv4-mapped IPv6 via `to_ipv4_mapped` | Line drift + recommendation applied | **stale** (line numbers); **fixed** (DNS rebinding + missing ranges) |
| 12 | `src/eval/runner.rs` lines 338-347 `run_command` `sh -c` | FILE EXISTS; function now at line 695; the surrounding code now documents `sh -c` injection risk in comments at lines 349 and 680 | Line drift | **stale** (line numbers); recommendation **not applied** (still passes unsanitized cmd to sh -c, but per AGENTS.md this is a local-dev eval tool so the original severity is overstated) |
| 13 | `echo-orchestration/src/human_loop/websocket.rs` lines 92-131 "zero auth" | FILE EXISTS; `bind()` still at line 96 but the provider now generates a UUIDv4 auth token (line 108 `let auth_token = Uuid::new_v4().to_string();`) and requires it as first message; log line at 117 prints the token | Recommendation applied | **fixed** |
| 14 | `src/improve/store.rs` silent write errors | FILE DOES NOT EXIST — `improve/` directory exists but contains only `analyzer.rs / eval_improvement.rs / generator.rs / loop.rs / mod.rs / trajectory.rs` | Path removed | **stale** |
| 15 | root crate has `state/`, `agent/`, `event_bus.rs`, `llm.rs` compat wrappers | YES — all present in `echo-agent/src/`; compat wrappers `assemble_req_header`/`chat`/`stream_chat` still at `src/llm.rs:116,122,151` | None (also recorded as B-ARCH-01-P2-01 acknowledged migration debt) | **current** |

### Summary

- 15 / 15 anchors resolved to a path (no missing files except `improve/store.rs`).
- 9 / 15 fully current with no drift.
- 5 / 15 show line-number drift but the symbol still exists nearby (AUDIT_REPORT 1.1, 1.2, 1.4, plus improve/store.rs path removed).
- 3 audit findings have had their recommendations applied since 2026-05-31 (SQL filter + READ ONLY; SSRF DNS pinning + missing ranges; WebSocket auth token).
- 0 anchors pointed to a deleted or renamed symbol that would invalidate the document's argument.

## Deviations

None. Static inspection only.

## Conclusion

Supports: MASTER-PLAN code anchors (root + CLI) are highly reliable — 9/9
non-AUDIT claims resolve cleanly. AUDIT_REPORT.md code paths still resolve,
but line numbers are uniformly stale (audit is from 2026-05-31 vs
2026-08-12 baseline = ~2.5 months drift) and 3 of the 5 sampled security
findings have had their recommendations applied. The AUDIT_REPORT should be
treated as a historical snapshot, not a current bug list.

Does not prove: that all 946 unwrap/expect sites enumerated in AUDIT_REPORT §6
are still present (sampled only the 5 critical/high anchors). Does not cover
the 25+ dated design docs under `echo-agent-cli/docs/2026-07-*` (those are
classified in V04, not re-anchored here).

Used by findings: `B-DOC-01-P2-01`, `B-DOC-01-P3-01`, `B-DOC-01-P3-02`,
`B-DOC-01-P3-03`.

## Follow-Up

A targeted re-audit of AUDIT_REPORT could produce `AUDIT_REPORT-2026-08.md`
with current line numbers and re-baselined severities under the local-desktop
threat model in AGENTS.md. Not in scope for B-DOC-01.
