# A-EVO-01 / V03 / Attempt 01

> Status: passed_with_findings
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only inspection)

## Claim

There are no aspirational evolution code paths (skeleton traits, `todo!`
stubs, dead `#[cfg(test)]`-only pub fns, "future work" hooks) in EKO's
application layer. Any path that does not work today is either deleted,
clearly labeled as framework-only (and compiled out of EKO), or behind
an explicit user opt-in.

## Method

Read-only dead-code and aspirational-marker sweep across the
application-layer evolution and auto-memory surfaces, plus targeted
caller search for any `pub fn` whose only appearance is its own
definition.

```text
1. Aspirational markers:
   grep -rn "TODO\|FIXME\|XXX\|HACK\|unimplemented!\|todo!\|aspirational\
     |future work\|not yet" \
     echo-agent-cli/echo-agent-app-core/src/evolution/ \
     echo-agent-cli/echo-agent-app-core/src/auto_memory/
   → expect zero matches in production code paths.
2. Dead-code allowances:
   grep -rn "allow(dead_code)\|allow(unused" \
     echo-agent-cli/echo-agent-app-core/src/evolution/ \
     echo-agent-cli/echo-agent-app-core/src/auto_memory/
   → expect zero matches.
3. Pub fn caller search (one-by-one):
   for each pub fn in evolution/ + auto_memory/, grep workspace-wide
   for callers. Flag any pub fn with zero callers outside its own
   definition.
4. Cross-check the F-EVO-01-flagged overlap: `improve::background_review`
   re-export shim — confirm EKO does not import it.
5. Cross-check framework eval/improve features — confirm CLI still
   excludes them.
```

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

## Expected Result

- Zero aspirational markers in production code.
- Zero `#[allow(dead_code)]` / `#[allow(unused...)]` in evolution.
- Every `pub fn` has at least one live caller or is a documented
  framework-facing API.
- EKO's `Cargo.toml` excludes `eval`/`improve`.

## Result

- Exit code: 0 (inspection).
- Summary: nearly clean. One dead public function found
  (`run_auto_memory_extraction`); recorded as A-EVO-01-P2-01.
  Everything else is live or correctly framework-only.

### Aspirational marker sweep — PASS (zero matches)

`grep -rn "TODO\|FIXME\|XXX\|HACK\|unimplemented!\|todo!\|aspirational\
|future work\|not yet" echo-agent-cli/echo-agent-app-core/src/
evolution/ echo-agent-cli/echo-agent-app-core/src/auto_memory/`
returned zero matches.

### Dead-code allowance sweep — PASS (zero matches)

`grep -rn "allow(dead_code)\|allow(unused" echo-agent-cli/echo-agent-
app-core/src/evolution/ echo-agent-cli/echo-agent-app-core/src/
auto_memory/` returned zero matches.

### Pub fn caller inventory

Enumerated every `pub fn` / `pub async fn` in the two modules. All
have at least one live caller except:

- **`auto_memory::run_auto_memory_extraction`**
  (`echo-agent-app-core/src/auto_memory/mod.rs:17-24`).
  - Definition site only: `grep -rn "run_auto_memory_extraction"
    echo-agent-cli/` returns exactly one line — the definition.
  - No production caller, no test caller, no re-export.
  - All actual auto-memory call sites (CLI `/auto-memory extract`
    (`all.rs`), TUI `/auto-memory extract` (`events.rs:3612+`), GUI
    `extract_auto_memory` (`panels.rs:283`), and session-exit
    `run_auto_memory_on_exit` (`repl.rs:265-330`)) call
    `extract_observations` + `queue_observations` directly.
  - **This is A-EVO-01-P2-01.**

All other pub fns in `evolution/` are either:
- Directly called from CLI commands, Tauri commands, or TUI event
  handlers (the bulk of `Dashboard::generate_metrics`,
  `EvidenceStore::{upsert,list,review_items,review_item,get,edit,
  reject,accept,undo,path,scope}`, `RulePromoter::{scan_for_proposals,
  promote_rule,criteria,new,with_criteria}`,
  `ReviewIntegration::{new,set_evolution_observer,rebind,config,
  evidence_store,echo_agent_dir,curator,capture_review_outcome,
  on_session_end,run_review,create_layer_manager,
  create_layer_manager_with_observer}`, `format_review_report`,
  `workspace_curator`, `discover_echo_agent_dir`,
  `capture_review_outcome`, `capture_memory_conflict`,
  `evolution_hook_observer`, `fire_evolution_hook`).
- Trait impl methods (`MemoryTriggerSink::on_trigger`,
  `SkillLoadPolicy::allows`) invoked via dynamic dispatch from the
  framework.

### Framework overlap re-check — PASS

- The `improve::background_review` re-export shim flagged in
  `2026-07-23-memory-self-evolution-closure.md` (P1/P2 cleanup item
  "删除已被 evolution 完全取代的 improve::background_review re-export
  shim") is no longer reachable from EKO: `echo-agent-app-core/Cargo.
  toml:10-15` enables `default-features = false` and does NOT include
  `"improve"` or `"eval"`. Confirmed:

  ```text
  echo-agent = { ..., default-features = false, features = [
      "mcp", "lsp", "human-loop", "subagent", "git", "tasks",
      "shell", "files", "web", "data", "statistics", "chart",
      "research", "media", "rag",
  ] }
  ```

  No `eval`, no `improve`. The CLI binary's `Cargo.toml` (root) also
  does not enable them.

- `MemoryLayer::Cold` / `COLD_NAMESPACE`: per the 2026-07-23 closure
  doc, retained as a framework-public option for three-layer consumers;
  EKO does not use it. Not dead in the framework (per AGENTS.md "删除
  框架代码的判定"), and not active in EKO. Correct posture.

- `LegacyStoreRememberTool/RecallTool/ForgetTool/SearchMemoryTool`:
  per the closure doc, framework-public options for consumers that do
  not install an evolution layer. EKO installs the layered variants
  instead. Not dead in framework.

### Skeleton / not-yet-implemented paths

None found. Every evolution CLI command in `evolution.rs` (1850 lines)
has a working backend; every GUI Tauri command has a working backend;
the auto-memory flow produces real candidates.

## Deviations

The single dead pub fn `run_auto_memory_extraction` (A-EVO-01-P2-01).
Its presence does not violate the AGENTS.md "evolution = explicit
diagnostics/review only" anchor — it is dead, not aspirational
(it would queue to the inbox if called, not mutate state directly).
But it is misleading dead code per AGENTS.md "代码清理: 无需兼容,
过时代码可直接删".

## Conclusion

Supports the V03 claim with one exception. The application-layer
evolution surface has no aspirational traits, no `todo!` stubs, no
"future work" hooks, no `#[allow(dead_code)]` annotations. The single
dead public function `run_auto_memory_extraction` is recorded as
A-EVO-01-P2-01 (low-impact cleanup). Framework options that EKO does
not use (Cold layer, legacy Store tools, `eval`/`improve` features)
are correctly retained as framework API surface per AGENTS.md "删除
框架代码的判定", and the framework-only `improve::background_review`
shim is excluded from EKO's build.

## Follow-Up

A-EVO-01-P2-01 (delete `run_auto_memory_extraction`). `None.` otherwise.
