# A-EVO-01 / V04 / Attempt 01

> Status: passed_with_findings
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only inspection + targeted `cargo test`)

## Claim

The product design documents (`echo-agent-cli/docs/2026-07-15-self-
evolution-review-and-roadmap.md`, `2026-07-23-memory-self-evolution-
closure.md`) accurately describe what the application-layer code does
in commit `b3b2e81`: review-gated mutations, no metric platform,
on-demand diagnostics, daily Dreaming, mode parity across CLI/TUI/GUI.
Any drift is minor (file-name labels).

## Method

Read the two design documents, extract every falsifiable claim, and
verify each against the code. Then run the application-layer evolution
test set to confirm the runtime claims still hold.

```text
1. Read docs/2026-07-15-self-evolution-review-and-roadmap.md
   → extract Phase A-F claims.
2. Read docs/2026-07-23-memory-self-evolution-closure.md
   → extract the closure wiring claims.
3. For each claim, anchor it to a code path and verify behavior.
4. Run: cargo test --lib --locked -p echo-agent-app-core evolution::
   → expect 19 tests pass (no regressions in the boundary tests).
5. Check user-facing strings for drift (rule-promote help text, etc.).
```

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/
echo-agent-cli`.

## Expected Result

- All Phase A-F claims in the 2026-07-15 doc are current.
- All closure wiring claims in the 2026-07-23 doc are current.
- 19 evolution tests pass.
- User-facing strings match the files they modify.

## Result

- Exit code: 0 (test command); 0 (inspections).
- Duration: 10m39s for the cargo test (cold build); subsequent grep
  passes were negligible.
- Summary: Phase A-F claims current; closure claims current; tests
  pass; one user-facing string drift (rule-promote help mentions
  AGENTS.md instead of learned-rules.md — A-EVO-01-P3-01).

### Cargo test execution

Command: `cargo test --lib --locked -p echo-agent-app-core evolution::`

Result: 19 passed, 0 failed, 0 ignored, 630 filtered out, finished in
0.26s after build.

Notable passing tests that anchor the doc claims:

- `evolution::rule_promoter::tests::scan_hits_memories_written_to_
  warm_namespace` — anchors Phase A's "RulePromoter namespace 死链已修".
  Confirms `scan_for_proposals` reads `WARM_NAMESPACE = ["agent",
  "memories"]`.
- `evolution::rule_promoter::tests::scan_ignores_low_confidence_memory`
  and `scan_skips_already_promoted_memory` — anchor the 0.95 confidence
  threshold and the `PROMOTED_TO_RULE` marker idempotency.
- `evolution::evidence::tests::accept_and_undo_are_backed_by_layered_
  memory` — anchors Phase C's "accept 才通过共享 MemoryLayerManager 写
  typed memory, undo 删除对应 memory 并恢复 pending".
- `evolution::evidence::tests::memory_merge_candidate_accepts_and_
  restores_exact_metadata` — anchors Phase E's "合并执行前重新核对
  topic/type/member/content/status/confidence/推荐 primary, 过期建议
  fail-before-mutation; accept 保存 before snapshot, undo 恢复内容和
  typed metadata".
- `evolution::evidence::tests::stale_memory_merge_candidate_fails_
  before_mutation` — anchors Phase E's stale-proposal fail-before-
  mutation guarantee.
- `evolution::evidence::tests::deduplicates_by_scope_kind_and_normalized_
  content` and `rejected_candidate_is_not_resurrected_by_duplicate_
  evidence` — anchor Phase B's "同一事实不会因多入口重复写入".
- `evolution::review_integration::tests::memory_conflicts_are_queued_
  without_mutating_the_store` — anchors the propose/apply split.
- `evolution::review_integration::tests::evolution_observer_survives_
  rebind_and_manager_rebuild` — anchors the workspace-switch rebind
  closure property.
- `evolution::review_integration::tests::test_review_integration_
  session_end_disabled` — anchors `review_on_session_end = false`
  default.
- `evolution::review_integration::tests::skill_policy_blocks_drafts_
  and_foreign_workspaces` — anchors Phase D's SkillLoadPolicy.
- `evolution::dashboard::tests::diagnostics_require_cross_run_
  repetition` — anchors Phase F's "occurrence_count >= 3 && distinct_
  run_count >= 2" filter.

### Phase A-F claims vs code

| Phase | Doc claim | Code anchor | Status |
|---|---|---|---|
| A | EvalRunner/trajectory/`eval`/`improve` removed from EKO product paths | `Cargo.toml` excludes both features; no `eval`/`improve` grep hits in `echo-agent-app-core` | current |
| A | BackgroundReviewer proposal-only, default temperature 0, max 512 tokens | Per F-EVO-01 V03 framework side; EKO consumes defaults | current |
| A | `/review`, TUI `/run-review`, GUI Evolution Panel functional parity | CLI `/review` (`evolution.rs:36`), GUI review entry points present | current |
| B | Unified `EvidenceCandidate` JSONL protocol with `candidate_id`/`kind`/`scope`/`evidence`/`confidence`/`status` | `evidence.rs:144-215` defines exactly these fields; `SCHEMA_VERSION = 3` | current |
| B | SHA-256 fingerprint by `scope + kind + normalized content`; duplicate sources merge evidence | `candidate_fingerprint` (`evidence.rs:1013-1027`); `EvidenceStore::upsert` (`:335-402`) | current |
| B | Rejected candidates not resurrected by duplicate detection | Test `rejected_candidate_is_not_resurrected_by_duplicate_evidence` passes | current |
| C | GUI/TUI/CLI share candidate list/accept/edit/reject/undo | CLI `/evidence-inbox` (`evolution.rs:1677`), GUI `evidence_candidate_action` (`panels.rs:1329`), TUI shares via `EvidenceStore` | current |
| C | Accept routes through shared `MemoryLayerManager` | `evidence.rs:515,571` use `layer_manager.write_memory` / `apply_merge_proposal` | current |
| D | Curator state at `{workspace}/.eko/evolution/curator-state.json`, identity by SKILL.md path | `workspace_curator` (`review_integration.rs:367-372`) joins `evolution/curator-state.json`; `SkillMeta` records path | current |
| D | SkillLoadPolicy blocks Draft/Deprecated/Archived and foreign-workspace skills | `review_integration.rs:324-348`; test `skill_policy_blocks_drafts_and_foreign_workspaces` | current |
| E | Dreaming is the only deterministic maintenance executor; promote/revive/archive by recall | `infra.rs:1143-1200` calls `Dreaming::run`; no other auto-mutator | current |
| E | MemoryReviewer analysis-only; conflicts become `EvidenceCandidate(action=MergeMemories)` | `capture_memory_conflict` (`evidence.rs:266-307`) wraps `MemoryConflictProposal` as `EvidenceAction::MergeMemories` | current |
| E | every-N-writes cadence/counter deleted | No `every_n_writes` / write-counter grep hits; cadence is `tokio::time::interval(86400s)` | current |
| F | No background task, no active notification, no accept/reject/undo rates | Zero hits on `accept_rate\|rejection_rate\|undo_rate\|metric_loop\|reward\|score_loop` in `evolution/` or `auto_memory/` | current |
| F | Dashboard scans at most 200 runs, shows at most 3 reminders with `occurrence_count >= 3 && distinct_run_count >= 2` | `dashboard.rs:16-17` (`MAX_DIAGNOSTIC_RUNS = 200`, `MAX_REPEATED_FAILURE_REMINDERS = 3`); `tool_diagnostics_from_report` (`:261-274`) applies both filters | current |
| F | No LLM call in Dashboard | `dashboard.rs` imports no `llm_client`; only `TraceAnalyzer::tool_reliability_report` (deterministic) | current |

### Closure doc (2026-07-23) wiring claims

| Claim | Code anchor | Status |
|---|---|---|
| Dreaming fires 60s after boot settles, then daily; same wiring in GUI/TUI/CLI | `infra.rs:1156-1158` (60s sleep, 86400s interval); three spawn sites | current |
| `MemoryContext.memories` and the unused `refresh_hot()` deleted | Not present in `unified_memory.rs` | current (negative verification) |
| hot memory → `eko:instruction-context` replaceable projection | `unified_memory.rs:28-29` defines both `eko:instruction-context` and `eko:hot-memory-context` (the latter split happened post-closure; A-MEM-01) | current with refinement |
| EKO CLI/TUI/GUI `/remember` all call `MemoryLayerManager::write_memory` | Verified in V01 trigger table | current |
| Framework raw Store tools kept as fallback for non-evolution consumers; default namespace unified to `['agent', 'memories']` | Framework-side; out of scope here, F-MEM-01 / F-EVO-01 own | not re-audited |
| `improve::background_review` re-export shim deleted | Confirmed not reachable from EKO build (V03-01) | current |

### User-facing string drift

| String | Location | Code reality | Status |
|---|---|---|---|
| "Successfully promoted memory '{}' to AGENTS.md" | `evolution.rs:1473-1476` | Actually writes `learned-rules.md` via `InstructionProvider::save_agents_instructions` (`rule_promoter.rs:221`) | stale (A-EVO-01-P3-01) |
| "Promote high-confidence memories to agent rules in AGENTS.md" (command help) | `evolution.rs:1520` | Same | stale (A-EVO-01-P3-01) |
| "不会改 AGENTS.md. 复刻 CLI" (GUI source comment) | `panels.rs:1513` | Actually does change `learned-rules.md` | stale (A-EVO-01-P3-01) |

## Deviations

The three user-facing string drifts (A-EVO-01-P3-01). They do not
affect correctness but mislead anyone reading the CLI output or the
source comment.

## Conclusion

Supports the V04 claim. The product design documents accurately describe
the code, with one minor drift: user-facing strings still name
`AGENTS.md` when the actual written file is `learned-rules.md`
(A-EVO-01-P3-01). All Phase A-F boundary claims are current; the daily
Dreaming cadence and three-mode parity wiring are verified; the 19
in-tree evolution tests pass without regression.

## Follow-Up

A-EVO-01-P3-01 (rename user-facing "AGENTS.md" strings to
"learned-rules.md"). `None.` otherwise.
