# A-TSK-03 / V04-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

The EKO→framework integration actually executes DAGs correctly: a run
with dependencies dispatches in dependency order, retries within budget,
propagates failure to downstream blockers, survives cancellation,
reuses durable results on restart, defers in-flight Running tasks,
inserts revisions only at safe points, and preserves completed siblings
when one task blocks. The 46-test executor suite passes (0 failures).

## Method

Run the executor test subset of `echo-agent-app-core`'s lib tests, then
inspect the tests that exercise the kernel integration boundary.

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib task_runtime::executor
```

Then read the kernel-integration tests:

```text
1. runtime_plan_respects_dependency_order (executor.rs:5524) — frontier
   honors depends_on.
2. runtime_plan_failure_propagates_and_blocks_downstream (executor.rs:5638)
   — Failed upstream blocks downstream + run ends Failed.
3. runtime_plan_cancellation_propagates_to_cancelled_outcome
   (executor.rs:5727) — pre-dispatch cancel → kernel returns Cancelled,
   zero dispatches.
4. cancellation_preserves_completed_tasks_and_finalizes_the_run
   (executor.rs:5375) — completed siblings preserved; run finalized.
5. runtime_plan_does_not_redispatch_in_flight_running_tasks
   (executor.rs:5824) — externally Running task is NOT re-dispatched;
   kernel polls it via the external-in-flight branch.
6. runtime_plan_applies_inserted_revision_after_active_wave
   (executor.rs:5564) — revision is applied only at a safe point
   between waves.
7. real_execution_failure_retries_within_budget (executor.rs:6125) —
   retry budget exhausted → Failed; exactly 1 + max_retries dispatches.
8. wave_processes_all_results_when_one_task_blocks (executor.rs:6183) —
   sibling Completion is preserved when one task resolves Blocked.
9. runtime_plan_reuses_durable_subagent_result_after_restart
   (executor.rs:5459) — durable result short-circuits dispatch.
10. invalid_cycle_is_rejected_before_scheduler_dispatch (executor.rs:5785)
    — cyclic plan rejected before the kernel sees it.
```

Toolchain: cargo + rust toolchain installed locally. No special feature
flags (lib default features).

## Expected Result

- All executor tests pass.
- Dependency order: upstream dispatched before downstream.
- Failure propagation: downstream marked Blocked, run Failed.
- Cancellation: zero dispatches when cancel fires pre-dispatch.
- In-flight: externally Running task not re-dispatched.
- Revision insertion: applied between waves, never mid-wave.
- Retry: bounded by `max_retries`; status flips via re-claim.
- Sibling preservation: completed siblings not replayed.
- Restart: durable result reused, single dispatch.
- Cycle rejection: structural error before any dispatch.

## Result

- Exit code: 0.
- Duration: ~1.22 s for the executor subset.
- Summary: 46 passed; 0 failed; 0 ignored.
- Tests covering the kernel integration boundary, with their kernel-side
  counterparts from F-TSK-03:

| Behavior | EKO test (executor.rs) | Kernel test (echo-agent runtime_executor.rs) |
|---|---:|---:|
| Dependency ordering | `runtime_plan_respects_dependency_order` (5524) | `wave_respects_dependency_order` (769) |
| Failure propagation + downstream blocking | `runtime_plan_failure_propagates_and_blocks_downstream` (5638) | `exhausted_graph_failure_propagates` (957) |
| Pre-dispatch cancel | `runtime_plan_cancellation_propagates_to_cancelled_outcome` (5727) | `cancelled_snapshot_as_interrupted` (822) |
| Mid-wave cancel + sibling preservation | `cancellation_preserves_completed_tasks_and_finalizes_the_run` (5375) + `wave_processes_all_results_when_one_task_blocks` (6183) | `cancellation_drains_wave_and_preserves_completed_siblings` (837) |
| In-flight external task | `runtime_plan_does_not_redispatch_in_flight_running_tasks` (5824) | (no direct kernel test — F-TSK-03-P3-03) |
| Revision insertion at safe point | `runtime_plan_applies_inserted_revision_after_active_wave` (5564) | `revision_inserted_between_waves` (753) |
| Retry within budget | `real_execution_failure_retries_within_budget` (6125) | (kernel delegates retry to controller — F-TSK-03 handoff item 6) |
| Restart durable result | `runtime_plan_reuses_durable_subagent_result_after_restart` (5459) | (controller-level concern — EKO only) |
| Structural cycle rejection | `invalid_cycle_is_rejected_before_scheduler_dispatch` (5785) | (kernel re-validates via `PlanValidator` at line 214) |

- The integration boundary works: EKO's `EkoRuntimeDagController`
  correctly implements the framework trait contract; the kernel drives
  the safe-point loop, claims, waves, cancellation, and resolution, and
  EKO's callbacks (persistence + dispatch + product policy) respond
  correctly. No scheduling-loop duplicate was needed to make any of
  these scenarios pass.
- Log/artifact: none (test output retained only in cargo's stdout).

## Deviations

None. All 46 tests pass; the integration boundary behaves as designed.

## Conclusion

Supports: the "executor actually works" claim. The EKO→framework
integration correctly executes single-task plans, multi-task DAGs with
dependencies, failure/cancellation/retry/revision scenarios, and the
durable-result restart path. The 46-test suite covers every behavior
flagged in F-TSK-03's handoff as needing application-layer verification.

Supports: the "no scheduling duplicate is needed for correctness"
invariant — every scenario passes with EKO implementing only the eight
controller callbacks and the post-outcome reconciliation sweeps. There
is no scenario in the test suite whose correctness depends on EKO
re-implementing frontier/wave/cancel/stall logic.

Does NOT support (and is not asked to support): the framework kernel's
stall-timeout correctness for the in-flight branch (F-TSK-03-P2-01).
`runtime_plan_does_not_redispatch_in_flight_running_tasks` (executor.rs:5824)
explicitly works around this by spawning a background task that
completes the in-flight task — i.e., the test never exercises the
"in-flight task never resolves" scenario. That gap is owned by
F-TSK-03-P2-01 and F-TSK-03-P3-03 at the framework layer.

Findings using this validation: none (this is the executable confirmation).

## Follow-Up

None. This validation closes A-TSK-03. The framework-layer gaps that
affect the kernel's stall behavior remain owned by F-TSK-03's follow-up
items; A-TSK-04 should verify the resume / recovery path stays sound
under the EKO reconciliation sweeps.
