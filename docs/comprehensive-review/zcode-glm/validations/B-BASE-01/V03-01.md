# B-BASE-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean

## Claim

All binary, example, bench, and test targets are explicitly declared with
correct paths and `required-features`. The CLI workspace's framework feature
selection is enumerated and excludes `sqlite` and `database`.

## Method

Read-only inspection of both `Cargo.toml` trees and directory listings.

```text
1. grep -n '\[\[bin\]\]\|\[\[example\]\]\|\[\[bench\]\]\|\[\[test\]\]\|\[lib\]' echo-agent/Cargo.toml
2. grep -n '\[\[bin\]\]\|\[\[example\]\]\|\[\[bench\]\]\|\[\[test\]\]\|\[lib\]' echo-agent-cli/Cargo.toml
3. find echo-agent/examples -name '*.rs' | wc -l   # auto-discovery check
4. grep -c '\[\[example\]\]' echo-agent/Cargo.toml  # declared count
5. Read both [[bin]] blocks in echo-agent-cli/Cargo.toml
6. Read echo-agent features list and CLI-side echo_agent dependency features
```

## Expected Result

- Each binary target has a path and `required-features` if feature-gated.
- `echo-agent-tauri` requires `gui`.
- Auto-discovered examples have no `required-features` (potential gap).
- CLI never enables `sqlite` or `database`.

## Result

- Exit code: 0
- Summary: Target inventory matches expectations. 13 auto-discovered examples
  lack explicit `[[example]]` blocks (finding B-BASE-01-P2-03). CLI binary
  gating is correct. Feature selection excludes sqlite/database.

### echo-agent targets

| Target | Type | Path | required-features |
|---|---|---|---|
| (lib) | library | src/lib.rs | none |
| agent_bench | bench | benches/agent_bench.rs (harness=false) | none |
| 55 × demoXX_* | example | examples/demoXX_*.rs | per-block (see manifest) |
| 13 auto-discovered | example | examples/{demo00..demo40} | **none** (gap) |

### echo-agent-cli targets

| Target | Type | Path | required-features |
|---|---|---|---|
| echo-agent-cli | bin | src/main.rs | none |
| echo-agent-tauri | bin | src-tauri/src/main.rs | `["gui"]` |

### CLI framework feature selection (union across both packages)

| Source | Features enabled |
|---|---|
| echo-agent-cli/Cargo.toml:50 | mcp, lsp, human-loop, subagent, tasks |
| echo-agent-app-core/Cargo.toml:10 | mcp, lsp, human-loop, subagent, git, tasks, shell, files, web, data, statistics, chart, research, media, rag |
| app-core dev-dep :59 | testing |
| gui feature → channels | channels |
| telemetry feature | telemetry |

**Excluded**: `sqlite`, `database`, `a2a`, `handoff`, `topology`, `content-guard`,
`project-rules`, `eval`, `improve`, `evolution`. The `sqlite` exclusion confirms
AGENTS.md invariant ("echo-agent-cli 不需要 SQLite").

## Deviations

13 auto-discovered examples may fail under `--no-default-features` if they
reference feature-gated APIs. Not verified here (deferred to F-FEAT-01).

## Conclusion

Target inventory is accurate and complete. Binary gating is correct. The
13 auto-discovered example gap is recorded as finding B-BASE-01-P2-03.
CLI feature selection is correctly curated without sqlite.

## Follow-Up

`F-FEAT-01` should verify whether the 13 unguarded examples compile under
`--no-default-features`.
