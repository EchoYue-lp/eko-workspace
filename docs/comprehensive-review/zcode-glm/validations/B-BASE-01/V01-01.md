# B-BASE-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

The two repositories contain exactly the workspace members, package
metadata, binary targets, and cross-repository path dependencies documented
in `AGENTS.md` and the task card. All `Cargo.toml` path dependencies use
relative paths; no worktree or absolute-path leaks exist.

## Method

Read-only inspection of every `Cargo.toml` outside `target/` and
`.worktrees/`, cross-referenced with `find`, `grep`, and direct file reads.
Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
1. find <repo> -name Cargo.toml -not -path "*/target/*" -not -path "*/.worktrees/*"
2. Read each Cargo.toml in full.
3. grep -rn "worktrees\|/Users/" <every Cargo.toml>   # AGENTS.md path rule
4. grep -n 'path = "\.\./' <cli manifests>             # cross-repo deps
5. Verify bin source files exist on disk.
6. git -C <repo> log --oneline -1 ; git -C <repo> status --short
```

## Result

- Exit code: 0 (all inspections succeeded; `find`, `grep`, and `ls` returned
  their data; no command failed)
- Summary: 10 `Cargo.toml` files found and read (8 in `echo-agent`, 2 in
  `echo-agent-cli`). All path dependencies are relative. No worktree or
  absolute-path leaks. Both bin entry-point files exist on disk.

### echo-agent workspace (commit 9b0e0fa)

Root manifest `echo-agent/Cargo.toml` is simultaneously the root package
`echo_agent` and the workspace root.

- `[workspace] resolver = "3"`
- `[workspace] members` = `echo-core`, `echo-macros`, `echo-execution`,
  `echo-integration`, `echo-tools`, `echo-state`, `echo-orchestration`
  (7 entries; root package `echo_agent` is implicitly a member because
  `[package]` sits at the workspace root — total 8 crates, matching
  `AGENTS.md`)
- `[workspace] default-members` explicitly lists `.` plus the same 7
  sub-crates (so bare `cargo build` builds all 8).
- No `[[bin]]` in root → pure library crate; `src/lib.rs` exists,
  `src/main.rs` does not.

| Crate | name | version | edition | rust-version | license | path |
|---|---|---|---|---|---|---|
| root | `echo_agent` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/` |
| sub | `echo_core` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-core/` |
| sub | `echo_macros` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-macros/` |
| sub | `echo_execution` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-execution/` |
| sub | `echo_integration` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-integration/` |
| sub | `echo_tools` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-tools/` |
| sub | `echo_state` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-state/` |
| sub | `echo_orchestration` | 0.2.0 | 2024 | 1.95 | MIT | `echo-agent/echo-orchestration/` |

`echo_macros` declares `[lib] proc-macro = true`. Every other crate is a
plain library. No `[[bin]]`, `[[test]]`, or `[[bench]]` targets exist in any
sub-crate. The root declares one bench (`agent_bench`, `harness = false`,
file `benches/agent_bench.rs`).

Intra-workspace path dependencies (all relative):

| From | To | Path |
|---|---|---|
| `echo_agent` (root) | `echo_core` | `echo-core` (+ `features = ["reqwest","permission"]`) |
| `echo_agent` (root) | `echo_macros`/`echo_execution`/`echo_integration`/`echo_tools`/`echo_state`/`echo_orchestration` | respective dirs |
| `echo_execution` | `echo_core` | `../echo-core` |
| `echo_execution` | `echo_tools` | `../echo-tools` (`default-features = false`) |
| `echo_integration` | `echo_core` | `../echo-core` |
| `echo_tools` | `echo_core`, `echo_macros` | `../echo-core`, `../echo-macros` |
| `echo_state` | `echo_core` | `../echo-core` |
| `echo_orchestration` | `echo_core` | `../echo-core` |

### echo-agent-cli workspace (commit b3b2e81)

Root manifest `echo-agent-cli/Cargo.toml` is simultaneously the root package
`echo-agent-cli` and the workspace root.

- `[workspace] resolver = "3"`
- `[workspace] members` = `echo-agent-app-core` (1 entry)
- No `default-members` key → defaults to root package + the one member.
- `default-run = "echo-agent-cli"`

| Crate | name | version | edition | rust-version | license | path |
|---|---|---|---|---|---|---|
| root | `echo-agent-cli` | 1.0.0 | 2024 | 1.95 | MIT | `echo-agent-cli/` |
| sub | `echo-agent-app-core` | 1.0.0 | 2024 | 1.95 | MIT | `echo-agent-cli/echo-agent-app-core/` |

Binary targets (both live in the root package; `src-tauri/` is **not** a
separate workspace member):

| name | path | required-features | exists on disk |
|---|---|---|---|
| `echo-agent-cli` | `src/main.rs` | _(none)_ | yes (21 227 bytes) |
| `echo-agent-tauri` | `src-tauri/src/main.rs` | `["gui"]` | yes (201 bytes) |

No `[[example]]`, `[[test]]`, or `[[bench]]` targets in either CLI manifest.
`echo-agent-app-core` is a plain library (no explicit `[lib]`).

Cross-repository path dependencies (all relative, AGENTS.md-compliant):

| Declaring manifest | Dep package | path | default-features | features |
|---|---|---|---|---|
| `echo-agent-cli/Cargo.toml:50` | `echo_agent` | `../echo-agent` | `false` | `mcp, lsp, human-loop, subagent, tasks` |
| `echo-agent-cli/Cargo.toml:51` | `echo_core` | `../echo-agent/echo-core` | _(implicit true)_ | _(none)_ |
| `echo-agent-app-core/Cargo.toml:10` | `echo_agent` | `../../echo-agent` | `false` | `mcp, lsp, human-loop, subagent, git, tasks, shell, files, web, data, statistics, chart, research, media, rag` |
| `echo-agent-app-core/Cargo.toml:16` | `echo_core` | `../../echo-agent/echo-core` | _(implicit true)_ | _(none)_ |
| `echo-agent-app-core/Cargo.toml:59` (dev) | `echo_agent` | `../../echo-agent` | `false` | `testing` |

CI reproduces the sibling-repo layout with a symlink:
`ln -s "$GITHUB_WORKSPACE/echo-agent" "$GITHUB_WORKSPACE/../echo-agent"`
(see `echo-agent-cli/.github/workflows/rust-ci.yml:21`). This is a CI-only
adaptation of the same relative path; the manifests themselves are clean.

### Path-leak and `.gitignore` checks

- `grep -rn "worktrees\|/Users/"` across all 10 `Cargo.toml`: **0 matches**.
- `.gitignore` in both repos contains `.worktrees/`
  (`echo-agent/.gitignore:23`, `echo-agent-cli/.gitignore:17` and `:20`).
- `Cargo.lock` exists in both repos (`echo-agent/Cargo.lock` 160 355 B,
  `echo-agent-cli/Cargo.lock` 240 188 B), even though `echo-agent/Cargo.toml`
  lists `Cargo.lock` under `exclude` (a packaging-exclude, not a workspace
  ignore; the lock file is still tracked in git for the binary-building CLI).

## Deviations

None for the claim as stated. Two observations that do not invalidate this
validation but feed later findings:

- `echo-agent` root `[workspace].members` does **not** explicitly list `.`,
  yet the root package is built because `[package]` is co-located with
  `[workspace]`. This is standard Cargo behavior; `default-members` does
  list `.` explicitly. Recorded for `B-ARCH-01`.
- `echo-agent/Cargo.toml:51` excludes `Cargo.lock` from packaging, but the
  file is present and used for `--locked` builds. Not a conflict, just worth
  noting for `Q-DEP-01`.

## Conclusion

The manifest/member inventory matches `AGENTS.md` and the task card exactly:
8 crates in `echo-agent` (root + 7), 2 in `echo-agent-cli` (root + 1), all
on edition 2024 / rust-version 1.95 / MIT. Both `echo-agent-cli` binaries
exist on disk with the documented `required-features`. Every cross-repo
dependency uses a clean relative path with no worktree or absolute-path
leakage. This validation supports findings `B-BASE-01-P2-01` (relative-path
compliance confirmed) and feeds the V02/V03/V04 matrices.

## Follow-Up

None for V01. The same inventory feeds V02 (feature graph), V03 (target
matrix), and the `B-ARCH-01` / `F-FEAT-01` downstream tasks.
