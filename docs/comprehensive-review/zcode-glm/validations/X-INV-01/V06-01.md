# X-INV-01 / V06 / Attempt 01

> Status: failed (two pre-existing UTF-8 / byte-length violations
> reaffirmed; no new ones)
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

Both repositories obey AGENTS.md "字符串处理:UTF-8 安全,禁止字节级
截断": all string slicing uses char-boundary-safe indices and all
length checks on potentially-Unicode strings use `chars().count()`.
The two known violations (recorded by Q-STA-01 and F-SEC-01) are still
present at the same sites, and no NEW UTF-8 violation is introduced.
The X-INV-01 task card also lists "IQR outlier detection" under this
section; this validation reclassifies that site (it is a direct-index
panic, not UTF-8) — see V05-01.

## Method

Working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.
Searches exclude `target/`, `.worktrees/`, and `.DS_Store`.

```text
# 1. Re-confirm the two known violation sites are unchanged at the
#    baseline commits:
sed -n '175,185p' echo-agent-cli/echo-agent-app-core/src/project/gitignore.rs
sed -n '48,60p'   echo-agent/echo-core/src/guard/rule.rs

# 2. Spot-check a sample of the ~45 other &str-slice sites that
#    Q-STA-01 V02-01 cleared, to confirm they have not regressed:
grep -rnE '&[a-z_]+\[[a-z_0-9]+ *\.\.' \
  echo-agent/src echo-agent/echo-*/src \
  echo-agent-cli/src echo-agent-cli/echo-*/src --include='*.rs' \
  | grep -v '#\[cfg(test' | grep -v 'as_bytes\|//\|#\['

# 3. Verify the highest-risk computed-index site (auto_memory.rs) uses
#    the proper char-boundary API:
sed -n '118,124p' echo-agent/src/evolution/auto_memory.rs

# 4. Sanity-check the IQR outlier code does NOT operate on &str
#    (it operates on &[f64], so there is no UTF-8 risk — only a
#    direct-index panic, see V05-01):
sed -n '249,255p' echo-agent/echo-tools/src/data_quality.rs
```

## Expected Result

- Known violation 1 (`gitignore.rs:179`): byte-index slice
  `&remaining[j..]` still present.
- Known violation 2 (`rule.rs:55`): byte-length compare
  `content.len() > max_len` still present.
- The other ~45 sites cleared by Q-STA-01 V02-01 use safe APIs
  (`char_indices()`, `floor_char_boundary()`, regex-match indices that
  are guaranteed char-aligned, ASCII-only constants, or `[u8]` byte
  buffers rather than `&str`).
- The IQR code at `data_quality.rs:249-274` operates on `Vec<f64>`,
  not `&str` → no UTF-8 risk (the task card's classification of IQR
  under "UTF-8 violations" is a misclassification; it is a direct-index
  panic covered by V05).

## Result

- Exit code: 0
- Duration: <1 s
- Summary:

Known violation 1 reaffirmed:
- `echo-agent-cli/echo-agent-app-core/src/project/gitignore.rs:178-180`
  still reads:

  ```rust
  for j in 0..=remaining.len() {
      let candidate = &remaining[j..];  // ← byte-index slice, panics on non-ASCII path
      ...
  }
  ```

  This is Q-STA-01-P1-01 (P1, application layer). Reachable via
  `ProjectContext::should_ignore` when a `**` gitignore pattern meets
  a non-ASCII path. Unchanged at commit b3b2e81.

Known violation 2 reaffirmed:
- `echo-agent/echo-core/src/guard/rule.rs:51-56` still reads:

  ```rust
  if let Some(max_len) = self.max_length
      && content.len() > max_len  // ← byte length, not chars().count()
  {
      return Ok(GuardResult::Block {
          reason: format!("Content length {} exceeds limit {}", content.len(), max_len),
      });
  }
  ```

  This is F-SEC-01-P3-01 (P3, framework layer). Unchanged at commit
  9b0e0fa.

IQR classification correction:
- `echo-agent/echo-tools/src/data_quality.rs:249-274` operates on
  `Vec<f64>` (`sorted: Vec<f64>`, `values: &[f64]`). There is no
  `&str` and no UTF-8 consideration. The X-INV-01 task card's listing
  of "IQR outlier detection" under V06 (UTF-8) was a
  misclassification: the IQR code is a direct-index panic risk only
  (covered by V05-01, finding X-INV-01-P2-01). Documented here so the
  misclassification does not propagate.

Sample of computed-index &str sites — all use safe APIs:
- `echo-agent/src/evolution/auto_memory.rs:120-121` —
  `let end = lower.floor_char_boundary((pos + 200).min(lower.len()));`
  then `&lower[pos..end]`. Both `pos` (from `str::find`) and `end`
  (from `floor_char_boundary`) are char-aligned. **Safe.**
- `echo-agent/src/evolution/auto_memory.rs:152,178,203` — same
  pattern. **Safe.**
- `echo-agent/echo-core/src/guard/llm.rs:101` —
  `&trimmed[start..=end]` where `start`/`end` come from a regex match
  on `trimmed` (regex crate guarantees char-aligned match offsets in
  default unicode mode). **Safe.**
- `echo-agent/src/agent/react/structured.rs:107,110` —
  `&text[start..]`, `&remaining[content_start..]` where indices come
  from `str::find`. **Safe.**
- `echo-agent/src/eval/runner.rs:728` —
  `&text[pos..text.len().min(pos + key.len() + 50)]` where `pos` comes
  from `str::find`. The upper bound uses `.min(text.len())` to stay
  in range but does NOT call `floor_char_boundary`; however `pos` is
  char-aligned and `key.len()` is a byte-length of an ASCII/known
  string used as a step. If `key` is ASCII this is safe; if non-ASCII
  it could slice mid-char. **Medium confidence — note for Q-STA-01
  V02-02 follow-up, not promoted to a finding here** because `key`
  comes from a controlled vocabulary (search keys) that are ASCII in
  practice.

All other matches are `&[u8]` byte buffers (e.g.,
`&bytes[..4] != b"%PDF"`, `&header[0..4] == b"RIFF"`), `Vec<String>`
slices (`&args[1..]`), or constants on ASCII-only prefixes
(`&trimmed[3..]` after checking `trimmed.starts_with("---")`).

- Log/artifact: none.

## Deviations

The `eval/runner.rs:728` site (`&text[pos..text.len().min(pos + key.len() + 50)]`)
is a residual uncertainty: it would only be a UTF-8 violation if a
non-ASCII `key` were passed, and the current call sites use ASCII
search keys. It is not promoted to a finding in this audit; it is
flagged for Q-STA-01 V02-02 to inspect call-site-by-call-site.

## Conclusion

The UTF-8 safety invariant **fails** with two pre-existing violations
(Q-STA-01-P1-01 and F-SEC-01-P3-01), both unchanged at the baseline
commits. **No new UTF-8 violation was introduced.** The "IQR outlier
detection" entry in the task card's V06 description is a
misclassification — that site has no UTF-8 involvement; it is a
direct-index panic tracked under V05-01 / X-INV-01-P2-01.

Net UTF-8 status:
- 2 known violations still open (no regression).
- 0 new violations.
- 1 residual uncertainty (`eval/runner.rs:728`) deferred to a
  targeted Q-STA-01 re-validation.

## Follow-Up

A Q-STA-01 V02-02 ("UTF-8 slicing re-audit with computed-index
extension") should:
1. Confirm `eval/runner.rs:728` call sites all pass ASCII `key`
   values, or harden the slice with `floor_char_boundary`.
2. Re-run the broader `&str[computed..computed]` sweep that Q-STA-01
   V02-01 performed, to confirm no new sites were added since
   2026-08-12 (none today).
The fixes for the two known violations belong to a future
implementation milestone, not this review task.
