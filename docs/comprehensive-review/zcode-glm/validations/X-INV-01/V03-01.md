# X-INV-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

No parallel task CRUD API exists in either repository. Per AGENTS.md
"任务关系只有一个权威 API": the framework provides
`task_create`/`task_update`/`task_list` as the single authority; the
application may add `task_execute`; the deleted global `todo_write`
must not be reintroduced; and `plan_create`/`plan_patch`/`plan_execute`
(or `PlanCreateTool`/`PlanPatchTool`/`PlanExecuteTool` types) must not
exist. Any matches found must be classifiable as test-only guards,
adapter type-conversion to the framework's authoritative
`TaskPlanPatch`, or false positives — never a live parallel tool.

## Method

Working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.
Searches exclude `target/` and `.worktrees/`.

```text
# Parallel-tool-name search across both repos:
grep -rn 'todo_write\|TodoWrite\|plan_create\|plan_patch\|plan_execute\|PlanCreateTool\|PlanPatchTool\|PlanExecuteTool' \
  echo-agent echo-agent-cli --include='*.rs'

# Confirm the authoritative framework API exists at its canonical home:
grep -rn '"task_create"\|"task_update"\|"task_list"' \
  echo-agent/echo-orchestration/src/tasks/task_tools.rs

# Confirm EKO's only task-tool addition is task_execute (permitted):
grep -rn '"task_execute"' echo-agent-cli --include='*.rs'

# Verify framework tool-registration list (no parallel CRUD tool wired in):
sed -n '1170,1185p' echo-agent/src/agent/react/builder.rs
```

## Expected Result

- Zero production-source matches for `todo_write`, `plan_create`,
  `plan_patch`, `plan_execute`, or the `PlanCreateTool`/`PlanPatchTool`/
  `PlanExecuteTool` types.
- Any matches are inside `#[cfg(test)]` or are adapter calls into the
  framework's authoritative `echo_agent::tasks::TaskPlanPatch` /
  `TaskPlanPatchOp` API (permitted for plan-as-artifact editing).
- Framework authority confirmed at
  `echo-agent/echo-orchestration/src/tasks/task_tools.rs` lines 27/94/155.
- CLI addition is `task_execute` only (permitted by AGENTS.md).

## Result

- Exit code: 0
- Duration: <1 s
- Summary: 5 raw matches, **all classifiable as non-violations**:

| # | Path:line | Match | Classification |
|---|---|---|---|
| 1 | `echo-agent/src/agent/react/builder.rs:1181` | `assert!(!names.iter().any(\|name\| name == "todo_write"));` | **test-only positive guard** — `#[test] fn default_agent_uses_one_task_relation_api` (line 1170) explicitly asserts the deleted `todo_write` is NOT registered. This test guards the invariant; it is not a violation. |
| 2 | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/types.rs:1284` | `pub(crate) fn to_task_plan_patch(&self) -> Result<echo_agent::tasks::TaskPlanPatch, String>` | **adapter type conversion** — converts the CLI's `TaskUpdateRequest` into the framework's authoritative `echo_agent::tasks::TaskPlanPatch` (the versioned-artifact edit API). The method delegates to `TaskPlanPatchOp::Insert/Update/Skip/Reorder/...` from the framework; it does not define a parallel API. This is exactly the "adapter must convert to the authority" pattern AGENTS.md requires. |
| 3 | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/store.rs:913` | `.to_task_plan_patch()` | **call site** of the adapter method above. Not a parallel tool. |
| 4 | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/store.rs:2327` | `fn attach_plan_creates_tasks_and_todos()` | **test fn name** inside `#[cfg(test)]`. The test asserts that attaching a plan correctly creates tasks and todos (the UI-projection concept). Not a tool name. |
| 5 | `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/revisioned_adapter.rs:328` | `.to_task_plan_patch()` | **call site** of the same adapter method. Not a parallel tool. |

- Framework authority confirmed:
  `echo-agent/echo-orchestration/src/tasks/task_tools.rs:27` →
  `"task_create"`; line 94 → `"task_update"`; line 155 →
  `"task_list"`. These are the canonical task-relation API.
- CLI addition confirmed:
  `echo-agent-cli/echo-agent-app-core/src/tool_exposure.rs:69` →
  `TASK_TOOLS = &["task_create", "task_update", "task_list", "task_execute"]`.
  Only `task_execute` is added on top of the framework trio, exactly
  as AGENTS.md permits.
- Log/artifact: none.

## Deviations

None.

## Conclusion

The "no parallel task CRUD" invariant **holds**. The framework's
`task_create`/`task_update`/`task_list` trio at
`echo-orchestration/src/tasks/task_tools.rs` is the single authority.
The CLI's only addition is `task_execute` (permitted). The banned
`todo_write`/`plan_create`/`plan_patch`/`plan_execute` identifiers
appear only in (a) a positive invariant-guard test, (b) adapter
methods that convert CLI types into the framework's authoritative
`TaskPlanPatch` artifact-edit API, and (c) a test-function name. No
parallel CRUD tool is registered or reachable.

## Follow-Up

None.
