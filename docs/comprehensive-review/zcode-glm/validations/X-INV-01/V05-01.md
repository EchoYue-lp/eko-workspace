# X-INV-01 / V05 / Attempt 01

> Status: failed (one new panic-safety violation found)
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

Both repositories obey AGENTS.md "禁止任何会导致系统 panic 的 API":
zero production `.unwrap()`/`.expect()`/`panic!`/`unreachable!`/`todo!`
calls outside test/example/comment context (Q-STA-01 V01-01 baseline),
AND zero computed-index vector/slice direct indexing that can
out-of-bounds panic on reachable inputs. This validation cross-refs
Q-STA-01's panic-keyword conclusion (still holds — same commits) and
extends the check to computed vector indexing, surfacing one new
finding.

## Method

Working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.
Searches exclude `target/` and `.worktrees/`.

```text
# 1. Confirm baseline commits match Q-STA-01 (so its V01-01 conclusion
#    still holds without re-running the full 1710-match classifier):
cd echo-agent && git rev-parse --short HEAD     # 9b0e0fa
cd echo-agent-cli && git rev-parse --short HEAD # b3b2e81

# 2. Targeted re-audit of computed-index vector indexing on data tools
#    (Q-STA-01's direct-index audit focused on &str byte-slicing;
#    computed Vec indices with arithmetic on .len() were not exhaustively
#    enumerated). The IQR outlier detector was flagged as a hypothesis
#    by the X-INV-01 task card:
sed -n '249,275p' echo-agent/echo-tools/src/data_quality.rs

# 3. Reproduce the index arithmetic to confirm the off-by-one for n=4:
cat > /tmp/iqr_test.rs <<'EOF'
fn detect_iqr_idx(n: usize) -> Option<(usize, usize)> {
    if n < 4 { return None; }  // mirror caller guard at data_quality.rs:232
    let q1_idx = n / 4.min(n - 1);
    let q3_idx = 3 * n / 4.min(n - 1);
    Some((q1_idx, q3_idx))
}
fn main() {
    for n in 4..=12 {
        if let Some((q1, q3)) = detect_iqr_idx(n) {
            let flag = if q1 >= n || q3 >= n { "  <-- OUT OF BOUNDS PANIC" } else { "" };
            println!("n={:>3}: q1_idx={:>3} q3_idx={:>3} (len={}){}", n, q1, q3, n, flag);
        }
    }
}
EOF
rustc /tmp/iqr_test.rs -o /tmp/iqr_test && /tmp/iqr_test

# 4. Verify the IQR tool is registered & reachable (not dead code):
grep -rn 'OutlierDetectionTool' echo-agent/echo-tools/src/registry.rs
grep -B 1 'pub mod data_quality' echo-agent/echo-tools/src/lib.rs  # feature gate
```

## Expected Result

- Baseline commits match Q-STA-01's review commits (9b0e0fa / b3b2e81)
  → Q-STA-01 V01-01's "0 production panic-keyword calls" conclusion
  still holds.
- The IQR arithmetic produces in-bounds indices for all `n >= 4`
  (the caller guard).
- `OutlierDetectionTool` is reachable.

## Result

- Exit code: 0 (the /tmp/iqr_test binary itself does not panic — it
  only prints; the panic would occur in the real `detect_iqr_outliers`
  when `sorted[4]` is actually indexed on a 4-element Vec).
- Duration: <1 s
- Summary:

Q-STA-01 baseline reaffirmed:
- Both repos are at the same commits Q-STA-01 reviewed. Q-STA-01 V01-01's
  conclusion ("zero production unwrap/expect/panic/unreachable/todo")
  still holds. No drift.

NEW finding — IQR direct-index panic for n=4:
- The Rust reproducer prints:

```text
n=  4: q1_idx=  1 q3_idx=  4 (len=4)  <-- OUT OF BOUNDS PANIC
n=  5: q1_idx=  1 q3_idx=  3 (len=5)
n=  6: q1_idx=  1 q3_idx=  4 (len=6)
n=  7: q1_idx=  1 q3_idx=  5 (len=7)
n=  8: q1_idx=  2 q3_idx=  6 (len=8)
n=  9: q1_idx=  2 q3_idx=  6 (len=9)
n= 10: q1_idx=  2 q3_idx=  7 (len=10)
n= 11: q1_idx=  2 q3_idx=  8 (len=11)
n= 12: q1_idx=  3 q3_idx=  9 (len=12)
```

- For `n == 4` exactly, `q3_idx = 3 * 4 / min(4, 3) = 12 / 3 = 4`,
  but `sorted.len() == 4` so valid indices are `0..=3`. The
  expression `sorted[3 * n / 4.min(n - 1)]` at
  `echo-agent/echo-tools/src/data_quality.rs:254` evaluates to
  `sorted[4]` and panics with
  `index out of bounds: the len is 4 but the index is 4`.
- Reachability: `OutlierDetectionTool` is registered in
  `echo-agent/echo-tools/src/registry.rs:151` and again at line 374
  (under the `data` feature gate, since `pub mod data_quality` is
  `#[cfg(feature = "data")]` at `echo-tools/src/lib.rs:42`). The
  caller guard at `data_quality.rs:232` is `if values.len() < 4 {
  continue; }`, which admits `n == 4` — exactly the boundary case
  that panics. Any agent (or EKO user) invoking the `outlier_detection`
  tool with method=iqr (the default) on a numeric column with exactly
  4 values will crash the process.
- Existing test `test_outlier_detection_iqr` at line 562 uses 9 values
  and so does not exercise the n=4 boundary.
- Log/artifact: `/tmp/iqr_test.rs`, `/tmp/iqr_test` (reproducer; not
  committed to the repo).

## Deviations

The cross-reference to Q-STA-01 V01-01 is taken at face value because
the baseline commits match exactly. A full re-run of the 1710-match
panic-keyword classifier was not performed — that is Q-STA-01's
territory and X-INV-01 is a summary task. The extension to computed
vector indexing was the focus here.

## Conclusion

The panic-safety invariant **fails** with one new violation
(X-INV-01-P2-01). Q-STA-01's panic-keyword conclusion still holds, but
Q-STA-01's direct-index search focused on `&str` byte-slicing patterns
and did not exhaustively enumerate computed vector indices of the form
`v[arithmetic_on_v.len()]`. The IQR outlier detector at
`echo-agent/echo-tools/src/data_quality.rs:253-254` is one such site
that slipped through. This validation surfaces it for the first time.
See X-INV-01 task report finding X-INV-01-P2-01 for the fix direction
(replace `sorted[3 * n / 4.min(n - 1)]` with a checked `.get(...)` or
switch to the existing safe `quantile()` helper in
`echo-tools/src/statistics.rs:195`).

## Follow-Up

A targeted Q-STA-01 re-validation (V02-02) should be opened to extend
the direct-index audit to all `v[expr_involving_v.len()]` patterns
across both repos, since this class was under-covered and may contain
additional sites. The IQR fix itself belongs to a future implementation
milestone, not this review task.
