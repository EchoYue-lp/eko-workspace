# X-INV-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

Both repositories obey the Subagent-only terminology invariant: no
production source file (`echo-agent/**/src/**/*.rs`,
`echo-agent-cli/**/src/**/*.rs`, `src-tauri/src/**/*.rs`) defines,
imports, calls, or comments a `worker`/`Worker` identifier (the banned
term per AGENTS.md "统一术语:只有 Subagent,没有 Worker"). All raw
matches are classifiable as violation, test-only, third-party wire
exception, or false positive; the goal is zero violations.

## Method

Working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.
Searches exclude `target/`, `.worktrees/`, and `.DS_Store` everywhere.

```text
# Word-boundary production-source search (the strict invariant check):
grep -rn '\bworker\b' --include='*.rs' \
  echo-agent/src echo-agent/echo-*/src \
  echo-agent-cli/src echo-agent-cli/echo-*/src
grep -rn '\bWorker\b' --include='*.rs' \
  echo-agent/src echo-agent/echo-*/src \
  echo-agent-cli/src echo-agent-cli/echo-*/src

# Broader case-insensitive sanity sweep across both repos:
grep -rni 'worker' --include='*.rs' --include='*.toml' --include='*.md' \
  echo-agent echo-agent-cli | grep -v '/target/' | grep -v '/.worktrees/'

# Cargo.toml and frontend TS/TSX sweep (the invariant extends beyond .rs):
grep -rni '\bworker\b' --include='*.toml' --include='*.ts' --include='*.tsx' \
  echo-agent echo-agent-cli | grep -v '/target/' | grep -v '/.worktrees/'
```

## Expected Result

`\bworker\b` and `\bWorker\b` (word-boundary, case-sensitive) return zero
matches in production source. Any case-insensitive substring hits are
expected to be either (a) substrings of larger identifiers such as
`NetworkError`/`background_worker`/`worker_pool` (false positives), or
(b) third-party protocol field names. No framework or application
identifier is named `Worker`.

## Result

- Exit code: 0
- Duration: <1 s
- Summary:
  - Production-source word-boundary search returned **0 matches** for
    both `\bworker\b` and `\bWorker\b`.
  - Broader case-insensitive `worker` substring search returned **31
    matches**, all of which are substrings of `NetworkError`
    (`LlmError::NetworkError`, `ChannelError::NetworkError`,
    `ReactError::Llm(Box::new(LlmError::NetworkError…))`). These are
    not the banned `Worker`/`worker` concept; they are part of the word
    "Network" + suffix. Word boundaries correctly exclude them.
  - No `Worker` type, struct, enum, trait, function, field, variable,
    module name, doc comment, or user-facing string exists in either
    repo.
- Log/artifact: none.

Classification of the 31 case-insensitive hits (all `NetworkError`):
all **false positive** — substring of `NetworkError`. Zero violations.

## Deviations

None.

## Conclusion

The Subagent-only terminology invariant **holds with zero violations**
across both repositories. The framework uses `Subagent`/`subagent`
consistently (e.g., `echo-agent/src/agent/subagent/`,
`SubagentRun`, `subagent_runtime_state_store`,
`agent/subagent/executor.rs`). No `Worker` parallel terminology was
detected at the word-boundary level in any production source file,
Cargo.toml, or doc. This supports the X-INV-01 task report's V01 row.

## Follow-Up

None. The invariant should be re-validated only after a commit that
introduces a new identifier containing the substring `worker`/`Worker`
in production source; such a commit would warrant a word-boundary grep
to confirm it is a third-party wire name with the required adapter
boundary note.
