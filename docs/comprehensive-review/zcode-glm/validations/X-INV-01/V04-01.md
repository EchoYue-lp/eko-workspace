# X-INV-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repos `git status --short` empty)

## Claim

All `Cargo.toml` files in both repositories use only correct relative
`path = "..."` declarations. No `path` is an absolute `/Users/...`
(Leak-of-machine-path) or a `.worktrees/...` (worktree-temporary-path
that fails on fresh clone / CI / other developers). Per AGENTS.md
"Worktree 并行开发与合并规范 §1" this is mandatory on the main
branch.

## Method

Working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
# The strict invariant check — zero matches required:
grep -rn 'worktrees\|/Users/' \
  echo-agent/Cargo.toml echo-agent/echo-*/Cargo.toml \
  echo-agent-cli/Cargo.toml echo-agent-cli/echo-*/Cargo.toml

# Enumerate every path = declaration for visual sanity check:
grep -rn 'path = ' \
  echo-agent/Cargo.toml echo-agent/echo-*/Cargo.toml \
  echo-agent-cli/Cargo.toml echo-agent-cli/echo-*/Cargo.toml

# Confirm .gitignore still contains .worktrees/ (rule §2):
grep -n 'worktrees' echo-agent/.gitignore echo-agent-cli/.gitignore
```

## Expected Result

- The first grep returns **zero matches**.
- Every `path = "..."` declaration is one of:
  - `path = "../echo-agent"` (CLI → sibling framework repo)
  - `path = "../../echo-agent"` (CLI sub-crate → sibling framework)
  - `path = "../echo-core"` (framework sub-crate → sibling sub-crate)
  - `path = "echo-core"` (framework root → its own sub-crate)
  - `path = "./echo-agent-app-core"` (CLI root → its sub-crate)
  - `path = "src/main.rs"`, `path = "src-tauri/src/main.rs"`,
    `path = "examples/...rs"` (target paths inside the same crate)
- `.gitignore` retains `.worktrees/` (separate rule §2, related to
  path hygiene).

## Result

- Exit code: 0
- Duration: <1 s
- Summary:
  - **Path-leak grep: 0 matches.** No `worktrees` or `/Users/`
    substring appears in any `Cargo.toml` of either repository.
  - **All 25 `path =` declarations reviewed**: every one is a
    workspace-relative path. Representative entries:
    - `echo-agent/echo-integration/Cargo.toml:28` →
      `path = "../echo-core"`
    - `echo-agent/Cargo.toml:106-112` →
      `path = "echo-core"`, `path = "echo-macros"`, ... (root-to-subcrate)
    - `echo-agent-cli/Cargo.toml:49` →
      `path = "./echo-agent-app-core"`
    - `echo-agent-cli/Cargo.toml:50` →
      `path = "../echo-agent"` (cross-repo, correct)
    - `echo-agent-cli/Cargo.toml:51` →
      `path = "../echo-agent/echo-core"` (cross-repo sub-crate)
    - `echo-agent-cli/echo-agent-app-core/Cargo.toml:10,16,59` →
      `path = "../../echo-agent"` and `path = "../../echo-agent/echo-core"`
      (sub-crate to sibling framework repo)
  - **`.gitignore` rule §2 (worktrees) check**: both
    `echo-agent/.gitignore` and `echo-agent-cli/.gitignore` retain the
    `.worktrees/` entry (not deleted — separate rule, but related
    hygiene; included for completeness).
- Log/artifact: none.

## Deviations

None.

## Conclusion

The relative-path invariant **holds with zero violations**. Every
`Cargo.toml` in both repositories uses workspace-relative `path`
declarations only. No absolute `/Users/...` paths or
`.worktrees/...` paths leaked into the committed manifests. A fresh
clone of either repo at these commits will resolve all path
dependencies correctly without a worktree present. The result is
consistent with B-BASE-01-P2-01 ("Cross-repo path hygiene clean"
positive) recorded on the same baseline commits.

## Follow-Up

None. Re-validate only after a worktree-merge commit that touches any
`Cargo.toml` (per AGENTS.md rule §1, worktree development may
temporarily use absolute paths; the merge-to-main checkpoint must
restore relative paths).
