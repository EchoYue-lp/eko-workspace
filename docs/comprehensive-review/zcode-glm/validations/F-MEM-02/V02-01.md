# F-MEM-02 / V02-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: not-applicable
> Worktree state: clean (read-only review; `cargo check`/`cargo test` only)

## Claim

The `sqlite` feature is properly isolated: it gates exactly the two SQLite
store modules (and their re-exports) behind `#[cfg(feature = "sqlite")]`, it
activates `dep:rusqlite` cleanly (no leakage), it is separate from the
`database` (sqlx) feature, and both `echo_state` and the root `echo_agent`
crate compile standalone with `--features sqlite` alone and with
`--no-default-features` (sqlite off).

## Method

Read the feature definitions and gating, then execute the four standalone
compile checks.

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# static: feature wiring
grep -n "sqlite\|database" echo-agent/echo-state/Cargo.toml
grep -n "^sqlite\|^database\|^full " echo-agent/Cargo.toml
grep -n 'cfg(feature = "sqlite")' echo-agent/echo-state/src/memory/mod.rs
grep -n "SqliteStore\|SqliteConversationStore\|SqliteRuntimeStateStore" \
  echo-agent/src/lib.rs echo-agent/src/state/mod.rs

# dynamic: standalone compiles (macOS arm64, rust-toolchain pinned)
cd echo-agent
cargo check -p echo_state --no-default-features --features sqlite --locked
cargo check -p echo_state --no-default-features --locked
cargo check -p echo_agent --no-default-features --features sqlite --locked
cargo check -p echo_agent --no-default-features --locked
```

Tool versions: cargo from `rust-toolchain.toml` (pinned), macOS 15.5 arm64.

## Expected Result

- `echo-state/Cargo.toml`: `sqlite = ["dep:rusqlite"]`, `rusqlite` optional
  with `bundled`.
- Root `Cargo.toml`: `sqlite = ["echo_state/sqlite", "dep:rusqlite"]`;
  `database = ["dep:sqlx", "echo_tools/database"]` (no conflation).
- `mod.rs`: both sqlite modules AND their re-exports under
  `#[cfg(feature = "sqlite")]`.
- All four `cargo check` commands exit 0.

## Result

- Exit code: 0 (all four commands)
- Duration: echo_state sqlite 15.25s; echo_state no-default 1.73s;
  echo_agent sqlite 11.16s; echo_agent no-default 0.24s (incremental cache).
- Summary: Claim holds.
  - Feature wiring confirmed clean:
    - `echo-state/Cargo.toml:22` `sqlite = ["dep:rusqlite"]`; line 34
      `rusqlite = { version = "0.32.1", features = ["bundled"], optional = true }`.
    - Root `Cargo.toml:71` `sqlite = ["echo_state/sqlite", "dep:rusqlite"]`;
      line 87 `database = ["dep:sqlx", "echo_tools/database"]` — `sqlite` and
      `database` are fully separate (rusqlite vs sqlx, no shared activation).
      Both are in `full` (line 67).
    - `echo-state/src/memory/mod.rs:23-26` gates both modules; lines 47-50
      gate both re-exports. `util.rs` gates only `memory_io_error` behind the
      feature (sqlite-gated `use MemoryError`); `expand_tilde` is always
      compiled (used by both code paths).
    - Root facade: `src/lib.rs:199` gates `SqliteStore`;
      `src/state/mod.rs:291-296` gates the `sqlite` module +
      `SqliteRuntimeStateStore` re-export.
  - All four standalone compiles succeeded with zero errors and zero warnings.
- Log/artifact: none (transient `target/` output only).

## Deviations

None. The compile output showed no warnings. (A `Blocking waiting for file lock
on package cache` line appeared during the root no-default run because the
preceding echo_state run still held the build cache lock momentarily; it
resolved and the check finished cleanly — not a real deviation.)

## Conclusion

Supports the F-FEAT-01 conclusion that `sqlite` is a live, correctly-isolated
framework feature, and confirms the static-wiring analysis holds dynamically:
`--features sqlite` alone pulls in exactly `rusqlite` (bundled SQLite, with
FTS5 available for the `store_fts USING fts5(...)` DDL) and compiles the two
store modules without dragging in `sqlx`/`database`. `--no-default-features`
excludes them cleanly. No feature-isolation finding arises; V02 is the clean
half of this task. Does not address concurrency (V03) or numerical/schema
correctness (V04).

## Follow-Up

None.
