# B-ARCH-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa (9b0e0faf74d35c9a432370b923acabfbb5f32d63)
> `echo-agent-cli` commit: not-applicable
> Worktree state: clean

## Claim

The README files in each of the 8 workspace crates accurately describe
the actual contents of the crate (modules, types, features, public API
examples). Drift between docs and code is limited to cosmetic wording,
not broken examples or invented APIs.

## Method

For each sub-crate:

1. Read the README.md end-to-end.
2. Read the corresponding `src/lib.rs` end-to-end.
3. For each API mentioned in the README (type, function, module, feature
   flag), confirm it exists in code with the same name and signature.
4. For each module in `lib.rs`, check whether the README mentions it.
5. For each example in the README, check whether it would compile
   against the actual API.

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`.

## Expected Result

All README examples compile. All listed types/modules/features exist
with the documented names. All `pub mod` items in `lib.rs` are mentioned
in the README contents.

## Result

- Exit code: not-applicable (static inspection — no compile attempted)

### echo-core README drift

README (`echo-core/README.md`) vs `echo-core/src/lib.rs`:

1. **README mentions a `prelude` module that does not exist.**
   - README line 21-22: `Use the prelude to import all core traits:` followed
     by `use echo_core::prelude::*;`.
   - `echo-core/src/lib.rs:21-40` declares `pub mod` for `agent`, `audit`,
     `budget`, `circuit_breaker`, `compression`, `error`, `guard`,
     `hooks`, `llm`, `lsp`, `memory`, `plugin`, `project_rules`,
     `retry`, `sandbox`, `tokenizer`, `tools`, `utils`. **No `prelude`
     module.**
   - Verified by `grep -n "pub mod prelude\|pub use.*prelude"
     echo-core/src/lib.rs` → zero hits.
   - The only `prelude` in the framework is at the root facade
     (`echo-agent/src/lib.rs:137`). The README confuses the facade's
     prelude with `echo_core`'s surface.

2. **README contents list mentions `ReActAgent`.**
   - README line 34: "Agent traits: `Agent`, `ReActAgent`, `SubAgent`".
   - `echo-core/src/agent/` defines the `Agent` trait and event types
     only. `ReactAgent` is defined in the root crate at
     `echo-agent/src/agent/react/`. echo_core does not export it.

3. **README contents list omits modules present in `lib.rs`.**
   - `lib.rs:23` declares `pub mod budget;` — not mentioned in README.
   - `lib.rs:28` declares `pub mod hooks;` — README only mentions
     `AgentCallback` "lifecycle hooks" (line 41), not the `hooks`
     module.
   - `lib.rs:34` declares `pub mod plugin;` — not mentioned.
   - `lib.rs:30` declares `pub mod lsp;` (feature-gated) — not in
     README's Feature Flags table.
   - `lib.rs:31` declares `pub mod memory;` — mentioned in contents
     only as "Memory traits" (line 37) but with no link to the actual
     `pub mod memory` module.
   - `lib.rs:36` declares `pub mod sandbox;` — README mentions only
     "Sandbox execution trait" (line 43), not the module name.
   - `lib.rs:38` declares `pub mod tokenizer;` — not in README.

4. **README's Feature Flags table is incomplete.**
   - README lists `reqwest`, `guard`, `permission`.
   - `echo-core/Cargo.toml:23-28` also defines `mcp`, `lsp`, `channels`.
     All three are absent from the README.

### echo-execution README drift

README (`echo-execution/README.md`) vs `echo-execution/src/lib.rs`:

1. **Contents list omits `risk` module.**
   - `echo-execution/src/lib.rs:16` declares `pub mod risk;` — not
     mentioned in README.

2. **README example uses APIs that exist but does not show imports
   needed for them to compile.**
   - README line 17-32 imports `LocalSandbox`, `SkillRegistry`,
     `ToolManager`, then calls `registry.discover("./skills")?` and
     `tools.register(Box::new(sandbox))`.
   - These methods/types exist in code, but the example is marked as
     illustrative (`// Execute code...`) rather than a runnable doctest.
     Acceptable.

### echo-integration README drift

README (`echo-integration/README.md`) vs
`echo-integration/src/lib.rs`:

1. **README example references a nonexistent function.**
   - README line 23: `let provider = ProviderFactory::create_openai(
     "gpt-5.5", std::env::var("OPENAI_API_KEY")?)?;`
   - `echo-integration/src/providers/config.rs:245` defines
     `pub fn openai(api_key: impl Into<String>, model: impl Into<String>)
     -> Self`. There is no `create_openai` method on `ProviderFactory`
     (`grep -rn "create_openai" echo-integration/src/` → zero hits).
     The README would not compile.

2. **README example references an unverified `mcp.connect_stdio`.**
   - README line 27: `mcp.connect_stdio("my-server", &["node",
     "./server.js"]).await?;`
   - `McpManager` is defined at `echo-integration/src/mcp/mod.rs:56`.
     The `connect_stdio` method name is plausible but was not verified
     in this validation; it is not the primary drift finding.

3. **Contents list overstates provider coverage.**
   - README line 32: "LLM Providers: OpenAI, Anthropic, DeepSeek, Qwen
     (DashScope), Ollama".
   - `echo-integration/src/providers/` contains only `openai.rs` and
     `anthropic.rs` as native clients. DeepSeek/Qwen/Ollama are
     reachable through the OpenAI-compatible `OpenAiClient` config, but
     there are no dedicated provider modules for them. The README is
     misleading about provider breadth.

### echo-tools README drift

README (`echo-tools/README.md`) vs `echo-tools/src/lib.rs`:

1. **README Features table omits `statistics` feature.**
   - README line 12-23 lists `files`, `shell`, `web`, `media`, `chart`,
     `data`, `database`, `git`, `rag`, `research`.
   - `echo-tools/Cargo.toml:34` defines `statistics = ["data"]`, and
     `echo-tools/src/lib.rs:72-74` declares `pub mod statistics;` under
     that feature. Missing from README.

2. **README Features table omits the `full` aggregate.**
   - `echo-tools/Cargo.toml:17` defines `full = [...]`. Not mentioned.

3. **README Features table describes `git` as a single `GitTool`.**
   - README line 21: "`git` | GitTool | —".
   - `echo-tools/src/lib.rs` declares `git`, `git_worktree`,
     `worktree_tool` modules under the `git` feature. The crate-level
     doc comment (lib.rs lines 1-18 — different from the README) says
     "`git` (6 git CLI tools)". The README's "GitTool" singular is
     stale.

4. **README does not mention modules `code`, `data_quality`,
   `diff_pagination`, `excel`, `git_checkpoint`, `image`, `media`,
   `pdf`, `security`, `text`, `word`, `worktree_tool`, `registry`.**
   All exist in `lib.rs`.

### echo-state README drift

README (`echo-state/README.md`) vs `echo-state/src/lib.rs`:

1. **Contents list omits `profiles` and `skill_telemetry`.**
   - `echo-state/src/lib.rs:24,26` declares `pub mod profiles;` and
     `pub mod skill_telemetry;`.
   - README's Contents list (lines 30-33) only mentions Memory,
     Compression, Audit. The crate-level doc comment in lib.rs (lines
     9-13) does mention both — so the README is stale relative to the
     lib.rs header doc.

2. **README's "Memory" contents line understates the module.**
   - README line 31: "Memory: `Store` (long-term KV) +
     `ConversationStore` (transcript persistence)".
   - `echo-state/src/memory/` actually contains Store, FileStore,
     InMemoryStore, SqliteStore, ConversationStore, Embedder,
     SnapshotManager, etc. The README picks two of these.

### echo-orchestration README drift

README (`echo-orchestration/README.md`) vs
`echo-orchestration/src/lib.rs`:

1. **README example would not compile.**
   - README line 23-27: `GraphBuilder::new()` then chained
     `.add_node("analyze", analyze_step)`.
   - Actual API at `echo-orchestration/src/workflow/graph.rs:214-216`:
     `pub fn new(name: impl Into<String>) -> Self` — **requires a name
     argument**. `GraphBuilder::new()` without args does not compile.

2. **README example references a nonexistent type.**
   - README line 30: `let approval = ConsoleApproval::new();`.
   - `grep -rn "pub struct ConsoleApproval"` → zero hits. The actual
     type is `ConsoleHumanLoopProvider`
     (`echo-orchestration/src/human_loop/console.rs:27` —
     `pub struct ConsoleHumanLoopProvider;`). It has no `new()` method
     (it's a unit struct, constructed by literal `ConsoleHumanLoopProvider`).

3. **README example references an unverified API.**
   - README line 33-34: `let mut tasks = TaskManager::new();
     tasks.on_complete(|task| println!("Done: {}", task.id));`
   - `TaskManager::new()` exists; the `on_complete` closure-taking
     method was not verified. Not the primary drift finding.

4. **Contents list omits `planning` and `scheduler`.**
   - `echo-orchestration/src/lib.rs:23,25` declares `pub mod planning;`
     and `pub mod scheduler;`.
   - README's Contents list (lines 37-41) only mentions Workflow,
     Human-in-the-Loop, Task Management. Both `planning` and
     `scheduler` are documented in the lib.rs header (lines 8-13) but
     not in the README.

5. **README's Feature Flags table only lists `websocket`.**
   - That is consistent with `echo-orchestration/Cargo.toml:23` (only
     one optional feature), so this is accurate.

### echo-macros README drift

README (`echo-macros/README.md`) vs `echo-macros/src/lib.rs`:

1. **README example imports from `echo_core::tools` but README never
   tells the user to add `echo_core` as a dependency.**
   - README line 19: `use echo_core::tools::{ToolParameters,
     ToolResult};`.
   - Quickstart (line 11-14) only adds `echo_macros`. Acceptable for
     an illustrative example but slightly misleading.

2. **README Macros table omits `#[derive(Tool)]`.**
   - README lines 30-38 lists `#[tool]`, `#[callback]`, `#[guard]`,
     `#[handler]`, `#[compressor]`, `#[permission_policy]`,
     `#[audit_logger]`.
   - `echo-macros/src/lib.rs:57-99` defines `#[derive(Tool)]` as a
     separate proc-macro (with a dedicated implementation module at
     `echo-macros/src/derive_tool.rs`). It is mentioned in the lib.rs
     header doc table (lines 16) but not in the README.

3. **README Macros table description for `#[tool]` says "Generate
   `TypedTool`".**
   - The macro actually generates an `impl Tool for ...` (per
     `echo-macros/src/lib.rs:243`). `TypedTool` is not the trait name
     generated. Minor.

### Root README

The root `echo-agent/README.md` is 50,001 bytes and was not line-by-line
audited for this task (out of scope: it is a user-facing manual, not a
crate-architecture document). It is included as a primary path for
tasks B-DOC-01 and F-API-01.

## Deviations

The validation fails because two READMEs contain examples that would
not compile against the current code (`echo-integration` README uses
`ProviderFactory::create_openai`; `echo-orchestration` README uses
`GraphBuilder::new()` without the required name arg and a nonexistent
`ConsoleApproval` type). One README references a module that does not
exist (`echo-core/README.md` → `echo_core::prelude::*`).

These are factual documentation drift, not stylistic preferences.

## Conclusion

Supports: every sub-crate has a README that broadly describes its
purpose and contents. The high-level layering story told by the
READMEs matches the code (core traits → mid-layer impls → facade).

Does not prove: that the READMEs are reliable as API documentation.
Multiple READMEs have broken examples, invented APIs, missing modules,
and stale type names. The drift pattern is consistent with the
in-progress crate split: the READMEs were written before the split
settled and have not been refreshed.

Used by findings: `B-ARCH-01-P2-03`, `B-ARCH-01-P2-04`,
`B-ARCH-01-P3-01`, `B-ARCH-01-P3-02`, `B-ARCH-01-P3-03`,
`B-ARCH-01-P3-04`, `B-ARCH-01-P3-08`, `B-ARCH-01-P3-09`,
`B-ARCH-01-P3-10`.

## Follow-Up

A V04-02 attempt would be required after the documented examples are
fixed to confirm they compile. The fixes themselves belong to a
documentation cleanup task (recommended: B-DOC-01 should pick up the
broken-example fixes as part of its drift index).
