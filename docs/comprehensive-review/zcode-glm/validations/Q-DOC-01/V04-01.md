# Q-DOC-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

The public/operator documentation is free of stale terminology banned by
AGENTS.md — specifically (a) no `worker`/`Worker` concept terms (the
project standard is `Subagent` only), (b) no references to the removed
`echo-agent-eval` crate as if it currently exists, and (c) no stale
workspace/crate paths that no longer exist.

## Method

Recursive case-insensitive grep across the documentation trees (excluding
the `comprehensive-review/` review output itself and `target/`/`node_modules`).
Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
grep -rni '<term>' echo-agent/docs/ echo-agent/README.md echo-agent/README.zh.md \
                   echo-agent-cli/docs/ echo-agent-cli/README.md \
                   docs/MASTER-PLAN.md AGENTS.md
terms: 'worker[s]?', 'echo-agent-eval', 'echo-agents' (phantom crate),
       'improve/store' (path removed per B-DOC-01)
then classify each hit as REAL-stale vs historical-mention vs false-positive.
```

## Expected Result

Per AGENTS.md's "只有 Subagent,没有 Worker" rule and B-DOC-01-P2-02 (which
flagged echo-agent-eval as stale), the docs should be clean of `worker`
concept terms and should not present `echo-agent-eval` / `echo-agents` as
current structure. Some historical design notes may legitimately mention
removed components in past tense.

## Result

- Exit code: not-applicable (static grep)
- Duration: n/a

### Term 1: `worker` / `Worker` — CLEAN (positive)

| Scope | Hits (excluding false positives) | Verdict |
|---|---|---|
| echo-agent/docs/, echo-agent/README*.md, echo-agent-cli/docs/, echo-agent-cli/README.md | 0 real concept hits (the only 2 raw matches were `NetworkError` strings inside mock-testing docs, not worker concepts) | **CLEAN** |

The subagent unification is fully reflected in the documentation. This is a
positive contrast to the historical concern that motivated the AGENTS.md
rule.

### Term 2: `echo-agent-eval` — STALE references found

| File:line | Context | Classification |
|---|---|---|
| `AGENTS.md:139` | "三个项目的定位" table lists `echo-agent-eval(评测)` as a current submodule of echo-agent-cli | **STALE — presents as current** |
| `AGENTS.md:370` | Worktree Cargo.toml path rule references `echo-agent-cli/echo-agent-eval/Cargo.toml` | **STALE — presents as current** |
| `echo-agent-cli/docs/system-deep-dive/06-skills.md:427` | "...曾引用它的 echo-agent-eval crate 也已移除" | historical-mention (past tense, documents the removal) — acceptable |
| `docs/superpowers/plans/2026-07-10-subagent-parity-roadmap.md:74,699` | lists echo-agent-eval as a future eval location | historical plan doc — low impact |

The two AGENTS.md hits are the highest-impact: AGENTS.md is the first file
every reviewer/agent reads and the highest-priority instruction file. The
`echo-agent-eval` directory does not exist at b3b2e81 (verified: `ls
echo-agent-cli/echo-agent-eval` → no such directory; the CLI workspace
`Cargo.toml` members list only `echo-agent-app-core`). This was already
flagged by B-DOC-01-P2-02 and is still unfixed at this review baseline.

### Term 3: `echo-agents` (phantom workspace crate) — STALE

| File:line | Context | Classification |
|---|---|---|
| `echo-agent/README.md:291` | Workspace Structure diagram: `├── echo-agents/  Agent implementations: ReactAgent, PlanExecute, Subagent` | **STALE — phantom crate** |
| `echo-agent/README.zh.md:252` | same, Chinese | **STALE — phantom crate** |

`echo-agent/Cargo.toml` workspace members (lines 2-10) list 7 crates
(echo-core, echo-macros, echo-execution, echo-integration, echo-tools,
echo-state, echo-orchestration); there is no `echo-agents/` directory.
ReactAgent/PlanExecute/Subagent implementations live under the root
`src/agent/` and the orchestration crate, not a separate `echo-agents`
crate.

### Term 4: `improve/store` (path removed per B-DOC-01) — not re-checked

B-DOC-01-V01 #14 already classified `src/improve/store.rs` as a removed
path. Not re-grepped here (owned by B-DOC-01).

### Summary

- `worker` terminology: CLEAN across all docs (positive).
- `echo-agent-eval`: 2 stale "presents-as-current" references in AGENTS.md
  (the highest-priority doc), plus 2 acceptable historical mentions.
- `echo-agents` phantom crate: 2 stale references in the echo-agent README
  workspace diagram (EN + ZH).

## Deviations

None. Static grep only. The grep intentionally excluded
`docs/comprehensive-review/` (this review's own output) to avoid
self-matches; the historical plan/skill docs that mention removed
components in past tense were classified as acceptable rather than stale.

## Conclusion

Supports: the documentation is NOT free of stale architecture claims.
`worker` is clean (positive, subagent unification holds), but
`echo-agent-eval` (AGENTS.md, the project's highest-priority instruction
file) and the `echo-agents` phantom crate (public README) remain stale.
Promoted to findings `Q-DOC-01-P3-03` (echo-agent-eval in AGENTS.md,
re-flag of B-DOC-01-P2-02) and `Q-DOC-01-P2-04` (echo-agents phantom
crate, grouped with the broken links).

Does not prove: that no other stale paths exist in the 40+ dated design
docs under `echo-agent-cli/docs/2026-07-*` (those are historical design
notes, out of scope for "current public/operator documentation"). Does not
scan source-code comments for `worker` (that is Q-STA-01's scope).

Used by findings: `Q-DOC-01-P2-04`, `Q-DOC-01-P3-03`, and the positive
note in `Q-DOC-01-P3-04`.

## Follow-Up

Remove `echo-agent-eval` from AGENTS.md's project-positioning table and
Cargo.toml-path rule (or create the module — but AGENTS.md's "echo-agent-cli
does not need SQLite" and the eval-system living in the framework suggest
removal is correct). Remove the `echo-agents/` line from the echo-agent
README workspace diagram. Neither is in scope to implement here.
