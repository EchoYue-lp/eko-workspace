# Q-DOC-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa (review target; HEAD=3aa7929 is test-only re-basing, no doc/example/toml files changed between the two)
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

All relative links, example paths, and doc paths referenced by the public
READMEs (`echo-agent/README.md`, `echo-agent/README.zh.md`,
`echo-agent-cli/README.md`), `echo-agent/examples/README.md`, and the EKO
setup doc (`echo-agent-cli/docs/getting-started.md`) resolve to files that
actually exist in the repository at the reviewed commits.

## Method

For each referenced relative path, run a filesystem existence check against
the current tree. Working directory:
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
For each referenced path P in {README.md, README.zh.md, examples/README.md,
                              echo-agent-cli/README.md, getting-started.md}:
  1. extract the relative target from the markdown link / code-fence path
  2. test -e <repo-root>/<P>
  3. record OK / BROKEN with the referencing file:line
```

Concrete link/path targets checked (representative, not exhaustive over
external URLs — only repository-relative paths):

| Group | Target | Referenced from |
|---|---|---|
| doc link | `docs/en/16-plan-execute.md` | echo-agent README Documentation table |
| doc link | `docs/en/19-self-reflection.md` | echo-agent README Documentation table |
| doc link | `docs/zh/16-plan-execute.md` | echo-agent README Documentation table (zh) |
| doc link | `docs/zh/19-self-reflection.md` | echo-agent README Documentation table (zh) |
| example link | `examples/demo14_memory_isolation.rs` | README examples table + examples/README.md |
| example link | `examples/demo16_testing.rs` | README examples table + examples/README.md |
| example link | `examples/demo22_plan_execute.rs` | README examples table + examples/README.md |
| workspace crate | `echo-agents/` | README:291 + README.zh.md:252 Workspace Structure |
| asset path | `examples/mcp.json.example` | demo42_playwright_mcp.rs doc comment |
| license link | `LICENSE` (echo-agent-cli) | echo-agent-cli README badge + footer |
| setup links | `docs/configuration.md`, `docs/gui-status.md`, `docs/getting-started.md`, `docs/architecture.md` | echo-agent-cli README |

## Expected Result

All repository-relative paths resolve. The READMEs are curated public
documentation and are expected to be link-clean.

## Result

- Exit code: not-applicable (static filesystem inspection)
- Duration: n/a

### Detailed outcomes

| Target | Exists? | Referenced from | Verdict |
|---|---|---|---|
| `docs/en/16-plan-execute.md` | NO | echo-agent README:1170 `[EN](docs/en/16-plan-execute.md)` | **BROKEN** |
| `docs/en/19-self-reflection.md` | NO | echo-agent README:1173 `[EN](docs/en/19-self-reflection.md)` | **BROKEN** |
| `docs/zh/16-plan-execute.md` | NO | echo-agent README:1170 `[ZH](docs/zh/16-plan-execute.md)` | **BROKEN** |
| `docs/zh/19-self-reflection.md` | NO | echo-agent README:1173 `[ZH](docs/zh/19-self-reflection.md)` | **BROKEN** |
| `examples/demo14_memory_isolation.rs` | NO | README:1078 table row; examples/README.md Conditional list | **BROKEN** |
| `examples/demo16_testing.rs` | NO | README:1080 table row; examples/README.md Acceptance list | **BROKEN** |
| `examples/demo22_plan_execute.rs` | NO | README:1086 table row; examples/README.md Teaching list | **BROKEN** |
| `echo-agents/` (workspace crate dir) | NO | README:291, README.zh.md:252 Workspace Structure | **BROKEN (phantom crate)** |
| `examples/mcp.json.example` | NO (file is at repo root `./mcp.json.example`) | demo42_playwright_mcp.rs:22,28 | **BROKEN path** |
| `LICENSE` (echo-agent-cli) | NO | echo-agent-cli README:6 badge, :601 footer | **BROKEN** |
| `docs/configuration.md` | YES | echo-agent-cli README:97,340,578 | OK |
| `docs/gui-status.md` | YES | echo-agent-cli README:219 | OK |
| `docs/getting-started.md` | YES | echo-agent-cli README:579 | OK |
| `docs/architecture.md` | YES | echo-agent-cli README:577 | OK |
| `docs/en/README.md`, `README.zh.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `LICENSE`, `echo-agent.yaml` (echo-agent) | YES | echo-agent README:17 header | OK |

### Summary

- 10 distinct repository-relative path targets are BROKEN across the public
  READMEs / setup docs.
- 4 broken doc links: the echo-agent README Documentation table advertises
  `16-plan-execute.md` and `19-self-reflection.md` in both EN and ZH, but
  neither file exists in `docs/en/` or `docs/zh/` (the directory jumps
  14 → 15 → 17 → 18 → 20).
- 3 broken example links: the README examples table and `examples/README.md`
  list `demo14_memory_isolation.rs`, `demo16_testing.rs`,
  `demo22_plan_execute.rs` as acceptance/teaching entries, but none of the
  three `.rs` files exist (nor are they registered as `[[example]]` in
  `echo-agent/Cargo.toml`). The actual example file set is demo00–demo70
  with these three numbers (and 63) absent.
- 1 phantom workspace crate: README:291 and README.zh.md:252 list
  `echo-agents/  Agent implementations: ReactAgent, PlanExecute, Subagent`
  in the Workspace Structure diagram, but `echo-agent/Cargo.toml` declares
  only 7 workspace members (echo-core, echo-macros, echo-execution,
  echo-integration, echo-tools, echo-state, echo-orchestration) and no
  `echo-agents/` directory exists.
- 1 wrong asset path: demo42 instructs `cp examples/mcp.json.example
  mcp.json`, but `examples/mcp.json.example` does not exist; the example
  file lives at the repository root `./mcp.json.example`.
- 1 missing license file: `echo-agent-cli/README.md` links `LICENSE` twice
  (badge + footer) but the echo-agent-cli repository has no `LICENSE` file
  (echo-agent root does have one).

## Deviations

None. Static inspection only; no network fetches (external URLs in badges
were intentionally excluded — only repository-relative paths are in scope).

## Conclusion

Supports: the public READMEs and setup docs do NOT have link/path integrity.
The failures cluster into four categories, each promoted to a finding:
broken doc links + broken example links + phantom crate
(`Q-DOC-01-P2-04`); the mcp.json.example path slip and demo42 example-name
mismatch (`Q-DOC-01-P2-04` / `Q-DOC-01-P3-02` context); the missing CLI
LICENSE (`Q-DOC-01-P2-04`).

Does not prove: that every external URL (crates.io, docs.rs, GitHub
Actions badges) is reachable — only repository-relative paths were checked.
Does not prove the 40+ design docs under `echo-agent-cli/docs/2026-07-*`
are link-clean (out of scope; those are historical design notes).

Used by findings: `Q-DOC-01-P2-04`.

## Follow-Up

Fix the broken links in one documentation pass: either create the missing
`16-plan-execute.md` / `19-self-reflection.md` docs and the missing
demo14/16/22 example files, or remove the table rows / README references
that point at them. Add a `LICENSE` file to echo-agent-cli (or drop the
badge/footer link). Correct the demo42 `cp` path to `cp mcp.json.example`.
None of these are in scope to implement in this review task.
