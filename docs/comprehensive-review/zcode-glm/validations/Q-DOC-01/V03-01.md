# Q-DOC-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: ZCode (builtin:bigmodel-coding-plan/GLM-5.2)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (read-only review)

## Claim

The feature-flag tables and config/feature claims in the public READMEs
match the canonical `[features]` definitions in `echo-agent/Cargo.toml`
and `echo-agent-cli/Cargo.toml`, and the "echo-agent dependency features"
claimed by the CLI match the `echo-agent` dependency declaration in
`echo-agent-cli/Cargo.toml`.

## Method

Diff each README feature table (and inline feature claim) against the
corresponding `Cargo.toml [features]` stanza. Working directory:
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
1. Extract the canonical feature set from:
   - echo-agent/Cargo.toml lines 65-103 ([features]: default, full, individuals)
   - echo-agent-cli/Cargo.toml lines 21-37 ([features]) and line 50 (echo-agent dep features)
2. For each README feature table row, compare (feature name, "in full?" flag,
   enabled-by-CLI?) against the canonical set.
3. Record MATCH / MISMATCH / PHANTOM (listed but not a feature) / OMITTED
   (a real feature not listed).
```

Canonical echo-agent feature set (from `echo-agent/Cargo.toml:65-103`):
- `default = []`
- `full =` a2a, mcp, lsp, telemetry, human-loop, handoff, topology, tasks,
  subagent, web, media, data, sqlite, channels, git, database, rag, chart,
  content-guard, project-rules, shell, files, research, eval, improve,
  statistics (27 features; notably NO `sandbox`, NO `testing`, NO
  `semantic-memory`, NO `macros`, NO `provider-factory`, NO `workflow`,
  NO `multimodal` inside `full`)
- 33 individual features defined (the 27 in `full` plus sandbox, testing,
  semantic-memory, macros, provider-factory, workflow, multimodal)

Canonical echo-agent-cli (from `echo-agent-cli/Cargo.toml`):
- `default = ["tui"]`; features: tui, gui (includes `channels`), channels,
  telemetry, devtools
- `echo-agent` dep features enabled (line 50): `["mcp", "lsp", "human-loop",
  "subagent", "tasks"]` — i.e. NOT sqlite, NOT eval, NOT improve

## Expected Result

README feature tables accurately enumerate features and their `full`
membership / CLI enablement. Given AGENTS.md's "echo-agent-cli does not use
SQLite" rule, the CLI README should NOT advertise sqlite as an enabled
echo-agent feature.

## Result

- Exit code: not-applicable (static manifest-vs-doc diff)
- Duration: n/a

### echo-agent README Feature Flags TABLE 1 (lines 113-141, "In `full`?")

| README row | README "in full?" | Cargo.toml truth | Verdict |
|---|---|---|---|
| `plan-execute` | yes | NOT A FEATURE (no such feature defined) | **PHANTOM** |
| `self-reflection` | yes | NOT A FEATURE | **PHANTOM** |
| `research` | no | IN `full` | **MISMATCH** |
| `database` | no | IN `full` | **MISMATCH** |
| `content-guard` | no | IN `full` | **MISMATCH** |
| `project-rules` | no | IN `full` | **MISMATCH** |
| `sandbox` | yes | NOT in `full` (feature exists but `full` omits it) | **MISMATCH** |
| `lsp` | (not listed) | IN `full` | **OMITTED** |
| `statistics`, `eval`, `improve`, `testing`, `semantic-memory`, `macros`, `provider-factory`, `workflow`, `multimodal` | (not listed) | real features | **OMITTED (9)** |
| web, mcp, media, channels, human-loop, subagent, sqlite, telemetry, a2a, handoff, topology, tasks, data, rag, chart, git, shell, files | yes | correct | MATCH |

Table 1 totals: 2 phantom features, 5 wrong "in full" flags, 10 omitted real
features (notably `lsp`, which is in `full`).

### echo-agent README Feature Flags TABLE 2 (lines 246-277, "Feature | Enables | Key Dependencies")

| README row | Cargo.toml truth | Verdict |
|---|---|---|
| `plan-execute` | NOT A FEATURE | **PHANTOM** |
| `self-reflection` | NOT A FEATURE | **PHANTOM** |
| `lsp`, `statistics`, `eval`, `improve`, `testing` | real features, absent from table | **OMITTED (5)** |
| remaining 28 rows | correct feature names | MATCH |

### echo-agent README inline claim (lines 104, 239-241)

| Claim | Truth | Verdict |
|---|---|---|
| line 104 "zero default features (`default = []`)" | `default = []` | MATCH |
| line 239-241 comment "# Full (default) — all features enabled" over bare `echo-agent = "0.2.0"` | bare dep = `default = []` = ZERO features, NOT full | **MISLEADING** (internal contradiction with line 104) |

### echo-agent-cli README Feature Flags table (lines 225-231)

| README row | Cargo.toml truth | Verdict |
|---|---|---|
| `tui` (default ✅), `gui` (includes channels), `channels`, `telemetry`, `devtools` | `echo-agent-cli/Cargo.toml:22-37` matches exactly | MATCH |

### echo-agent-cli README "echo-agent 依赖 Features" table (lines 237-247)

| README claims enabled | Cargo.toml line 50 actually enables | Verdict |
|---|---|---|
| `sqlite` | NOT enabled (absent from dep features) | **MISMATCH** (also contradicts AGENTS.md "echo-agent-cli 不需要 SQLite") |
| `eval` | NOT enabled | **MISMATCH** |
| `improve` | NOT enabled | **MISMATCH** |
| `mcp`, `lsp`, `human-loop`, `subagent`, `tasks` | enabled | MATCH |

### Summary

- echo-agent README feature tables are materially inaccurate: 2 phantom
  features (`plan-execute`, `self-reflection`) advertised in both tables;
  5 wrong `full`-membership flags in table 1; ~10-14 real features omitted
  across the two tables (including `lsp`, which is in `full`).
- echo-agent README line 239-241 "Full (default)" comment directly
  contradicts line 104 and the manifest (`default = []`).
- echo-agent-cli README "echo-agent 依赖 Features" table over-states the
  enabled echo-agent features by 3 (sqlite, eval, improve are listed but
  not enabled). The `sqlite` claim is the most consequential: it
  contradicts both `Cargo.toml:50` and AGENTS.md's hard rule that
  echo-agent-cli does not use SQLite.
- echo-agent-cli's own feature table (tui/gui/channels/telemetry/devtools)
  is fully accurate.

## Deviations

None. Pure manifest-vs-doc diff. The `full` membership of each feature was
read directly from `echo-agent/Cargo.toml:67`; no feature-gate compilation
was performed (that is F-FEAT-01's scope).

## Conclusion

Supports: the public feature/config documentation does NOT match the
canonical manifests. Promoted to findings `Q-DOC-01-P2-01` (echo-agent
feature tables), `Q-DOC-01-P2-02` (CLI sqlite/eval/improve over-statement,
especially the sqlite claim contradicting AGENTS.md), and `Q-DOC-01-P3-02`
(the "Full (default)" inline contradiction).

Does not prove: that every individual feature compiles in isolation
(F-FEAT-01). Does not prove the `Key Dependencies` column in README table 2
is exhaustive (only feature names and `full`-membership were diffed).

Used by findings: `Q-DOC-01-P2-01`, `Q-DOC-01-P2-02`, `Q-DOC-01-P3-02`.

## Follow-Up

Refresh both echo-agent README feature tables against `Cargo.toml [features]`
(remove plan-execute/self-reflection, correct the `full` flags, add the
omitted features), and correct the CLI "echo-agent 依赖 Features" table to
drop sqlite/eval/improve. Not in scope to implement here.
