# Q-STA-01 / V06 / Attempt 01 — Dead-code audit (`#[allow(dead_code)]` and unreferenced production items)

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every `#[allow(dead_code)]` and every uncalled production item found is
classified (delete target vs retain-by-contract), and the classification
respects the framework rule that a pub API is not dead merely because the CLI
does not call it.

## Method

```text
# 1. grep -B1 'allow(dead_code)' across all member dirs of both repos
# 2. For each hit: inspect the item, search full-repo callers
#    (grep -rn <name> <repo>), classify:
#    delete-target | retain (wire format / DTO / pub API / by-design)
# 3. Reference F/A-phase dead-code findings via their canonical IDs
```

## Result

- Exit code: 0 (scans); classification by context inspection
- Summary — 11 `#[allow(dead_code)]` sites + 1 module-level, classified:
  - **Delete targets (framework)**:
    - `echo-agent/src/agent/react/mod.rs:1765-1799` — `MAX_READ_FILES`,
      `READ_FILES_TTL`, `record_file_read`, `was_file_read` (read-before-edit
      enforcement): **zero callers**; the live implementation is
      `snapshot.rs:871 record_file_read` + `run/pipeline.rs:363,375,627`
      (`recently_read_files` checks). Dead duplicate authority of a
      documented safety feature (`config.force_read_before_edit`,
      `config.rs:126`) → finding Q-STA-01-P2-02.
    - `echo-agent/src/agent/mod.rs:47` — `pub(crate) type SubAgentMap`:
      zero use. Internal dead alias → part of finding Q-STA-01-P3-02.
    - `echo-agent/src/agent/react/subsystems/tool_exec.rs:59,74,80,85` —
      `tool_manager_arc`, `mcp_manager_arc` (returns `None` — pure stub),
      `subagent_registry`, `progressive_skill_registry`: zero callers →
      part of finding Q-STA-01-P3-02.
    - `echo-agent/echo-core/src/plugin/variables.rs:186-203` — `export_to_env`
      (also the dead unsafe site from V05) → finding Q-STA-01-P3-01.
  - **Retain (wire format / DTO / pub API / by-design)**:
    - `echo-agent/src/trace/mod.rs:361` — `TraceEvent::Error` variant never
      constructed: serialized event envelope; keep for wire compatibility.
    - `echo-agent/src/tools/builtin/spawn_task.rs:39` — `with_default_spawner`:
      pub framework API option; keep.
    - `echo-agent-cli/echo-agent-app-core/src/types/response.rs:75` —
      `McpConnectionStatus::Disconnected` never constructed in Rust:
      `#[ts(export)]` DTO contract consumed by the frontend; keep (note: Rust
      never emits it — verify with A-INT-01 whether the GUI can display it).
    - `echo-agent-cli/src/tui/mod.rs:336` — clipboard lease field: intentional
      keep-alive (documented in the comment); keep.
    - `echo-agent-cli/echo-agent-app-core/src/output/mod.rs:9` —
      module-level `#![allow(dead_code)]`: `OutputRenderer` is live
      (`src/cli/repl.rs:96,480`); the attribute suppresses warnings for
      unused renderer helpers — clean up opportunistically, not a defect.
  - **Cross-referenced archived dead-code findings (current, not re-filed)**:
    F-HITL-01-P1-01/02 (the `src/agent/react/run/approval.rs` impl block —
      `#[allow(dead_code)]` at approval.rs:11, 456 lines incl. the uncalled
      `tool_needs_approval`; the approval subsystem field at
      `react/mod.rs:119` is also dead), F-SUB-01-P1-01 (per-role
      `tool_filter`), F-INT-01-P1-01 (202-async path), F-TST-01-P1-02.
    All confirmed still present at these commits.

## Deviations

The scan covers `#[allow(dead_code)]` markers plus full-repo caller greps of
the affected items; it is not a whole-repo `pub fn` reachability proof (that
would be Q-X phase work).

## Conclusion

Dead-code surface is small and fully classified. Two delete clusters deserve
roadmap entries (read-before-edit duplicate pair in react/mod.rs; the
approval.rs dead impl block already filed as F-HITL-01-P1-01/02), plus the
dead unsafe `export_to_env`. No data-path impact: every delete target is
unreachable today.

## Follow-Up

S-RDM-01: add deletion items for react/mod.rs:1765-1799, agent/mod.rs:47,
tool_exec.rs:59-88, variables.rs export_to_env. Next: V07.
