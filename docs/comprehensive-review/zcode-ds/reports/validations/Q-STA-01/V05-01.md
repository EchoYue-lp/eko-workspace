# Q-STA-01 / V05 / Attempt 01 — Unsafe-block inventory and rationale

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every `unsafe` block in production code of both repositories is accounted for
with a documented rationale, and none performs memory-unsafety beyond
`std::env::set_var`/`remove_var` (unsafe only because both crates use edition
2024). Test-only unsafe blocks are listed for completeness.

## Method

```text
# 1. grep for lines starting with `unsafe` (block openers, fn/impl/trait
#    declarations) across all member dirs of both repos, excluding test dirs
grep -rnE '^\s*unsafe\s*(\{|fn|impl|trait|static|const|extern|struct|enum|type|union)' <repo>/<member-dirs>
# 2. Inspect each hit's enclosing fn and module for cfg(test) context,
#    callers, and SAFETY comments
# 3. grep all callers of the two production fns (export_to_env,
#    load_shell_env) across the full repos
```

## Expected Result

A complete, small inventory; production unsafe sites are only edition-2024
`set_var`/`remove_var` with SAFETY comments; no pointer/`transmute`/raw-address
unsafe anywhere.

## Result

- Exit code: 0 (scans); classification by context inspection
- Summary — **production unsafe sites: exactly 3**, all `std::env::set_var`
  (edition 2024, see `echo-agent/Cargo.toml:1` and
  `echo-agent-cli/Cargo.toml:1` for `edition = "2024"`):
  1. `echo-agent/echo-core/src/plugin/variables.rs:190` — `export_to_env`
     (production, **zero callers** — see V06). SAFETY comment documents the
     single-threaded init guarantee; the guarantee is currently enforced by
     nobody because the fn is unreachable. → finding Q-STA-01-P3-01.
  2. `echo-agent-cli/echo-agent-app-core/src/infra.rs:1491` — `load_shell_env`
     (production, live: called from `src/tauri/desktop.rs:130` at GUI startup).
     SAFETY comment + `std::sync::Once` guarantee single execution before
     background threads. Validated: the once-guard makes the
     thread-safety claim sound; env var names are whitelisted (`API_KEY_VARS`,
     infra.rs:1446-1464), values come from the user's own shell (`zsh -lc
     'env'`-style capture). No finding.
  3. `echo-agent/src/config.rs:803,811-812` and
     `echo-agent/echo-integration/src/providers/config.rs:1296,1380,1389,
     1401,1419,1434`, `echo-core/src/plugin/variables.rs:276,279`,
     `echo-execution/src/skills/external/prompt_exec.rs:732,746`,
     `echo-orchestration/src/human_loop/protected.rs:528,548` — all inside
     `#[cfg(test)] mod tests` / `#[test]` fns (test-only EnvGuard helpers;
     `config.rs:788-795` and `providers/config.rs:1273-1281` show the
     `#[cfg(test)]` module headers). Not production.
- No `unsafe fn`/`unsafe impl`/pointer/`transmute`/raw-address operations
  anywhere in production code; the only other `unsafe` matches are the string
  "unsafe" in messages/comments.

## Deviations

None.

## Conclusion

Unsafe usage is minimal, fully inventoried, and confined to edition-2024
environment mutation. The two production sites are both covered by SAFETY
comments; one (infra.rs load_shell_env) is sound as written; one
(variables.rs export_to_env) is dead code with an unenforceable-by-construction
SAFETY premise — delete or wire it (Q-STA-01-P3-01). Supports the claim.

## Follow-Up

S-RDM-01: decide deletion vs wiring for `export_to_env`. Next: V06.
