# Q-STA-01 / V03 / Attempt 01 — UTF-8 slice audit (byte-slicing `&str`/`String` at non-boundary offsets)

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

No live production code byte-slices a `&str`/`String` at a non-char-boundary
offset. X-INV-01-V05 established two live violations (pdf.rs, eval/runner.rs);
this execution re-scans both repositories at the same commits, re-verifies the
archived sites, and hunts for sites X-INV-01-V05 missed.

## Method

```text
# 1. Full production-region range-slice scan (regex \w+\[..range..\]), both repos
python3 /tmp/scan_slice.py <repo> <member-dirs>     # 115 framework hits
# 2. Classify each: Vec/&[T] slice (safe), find()-derived offsets on the SAME
#    string (safe: char boundaries), floor_char_boundary/char_indices (safe),
#    ASCII-prefix guarantee (safe), or candidate violation
# 3. Reproduce new candidates with standalone rustc programs mirroring the
#    exact source logic (read-only; /tmp scratch only)
```

## Expected Result

Archived violations still present; zero additional live violations.

## Result

- Exit code: 0 for scans; **101 (panic) for both new reproductions**
- Summary:
  - CLI: zero byte-slice hits in production (consistent with X-INV-01-V05-01).
  - Framework 115 slice hits classified safe except the archived two and **two
    new live violations**:
    1. **`echo-tools/src/web/providers/utils.rs:32` — `percent_decode`**
       `let hex = &input[i + 1..i + 3];` inside `while i < bytes.len()` where
       `bytes[i] == b'%' && i + 2 < bytes.len()`. `input` is a `&str`; when a
       multibyte char follows the `%`, bytes `i+1..i+3` split it.
       Reproduction (standalone rustc, exact source logic):
       `percent_decode("https://example.com/%中文")` → panic
       "end byte index 23 is not a char boundary; it is inside '中'
       (bytes 21..24 of string)" — exit 101. Live path: `web_search` tool
       (`WebSearchTool`, registered `echo-tools/src/registry.rs:88,301`, `web`
       feature, **enabled by EKO**: `echo-agent-app-core/Cargo.toml:10-15`)
       → `search.rs:74` DuckDuckGo fallback → `duckduckgo.rs:48,54`
       `extract_url` passes a remote third-party href's `uddg=` value →
       `percent_decode`. → finding Q-STA-01-P1-01.
       (X-INV-01-V05 classified `[..n]`/`[n..]` patterns but missed this
       computed-offset slice; it is a third instance of the same invariant
       violation family as X-INV-01-P1-01/02.)
    2. **`echo-core/src/guard/llm.rs:101` — `parse_guard_response`**
       `&trimmed[start..=end]` where `start = trimmed.find('{')` and
       `end = trimmed.rfind('}')`. Both are char boundaries (safe axis), but
       nothing enforces `start <= end`: an LLM reply with a `}` before the
       first `{` panics. Reproduction (standalone rustc):
       `"Sure, here it is: } actually the JSON is { safe: true"` → panic
       "byte range starts at 41 but ends at 19" — exit 101. Live path:
       `LlmGuard` (public API re-exported at `src/lib.rs:229`, `guard`
       feature, `echo-core/Cargo.toml:24` non-default) runs `chat_simple` and
       parses the model reply. → finding Q-STA-01-P2-01.
  - Archived sites re-confirmed present (referenced, not re-filed):
    X-INV-01-P1-01 (`pdf.rs:225-227`), X-INV-01-P1-02 (`eval/runner.rs:728`),
    X-INV-01-P3-01 (`eval/regression.rs:80`), F-EXT-02-P1-01
    (`files/edit.rs:263-265`), A-PROJ-01-P3-01 (`project/gitignore.rs:178-183`).
  - Safe patterns re-confirmed: `auto_memory.rs:121,152,178,203`
    (`floor_char_boundary`-clamped), `classifier.rs:586` (`char_indices`
    byte offsets), `guard/llm.rs` sibling callers, `prompt_template.rs`,
    `skills/registry.rs` + `loader.rs`, `providers/config.rs:1150-1162`,
    `a2a/serve.rs:300-314`, `security.rs:593,806-810`, `media/image_fetch.rs:293`,
    `media/web_fetch_enhanced.rs:331`, `data.rs:2054-2055,2857-2858` (all
    `find()`-derived on the same string).

## Deviations

`percent_decode` was not in X-INV-01-V05's scan results — either its pattern
set or its classification missed the computed-offset slice `[i + 1..i + 3]`;
this execution treats it as a new sample, not a duplicate claim.

## Conclusion

The UTF-8 safety invariant **fails** with two additional live violations at the
reviewed commits: Q-STA-01-P1-01 (percent_decode, framework, live in EKO via
`web_search`) and Q-STA-01-P2-01 (parse_guard_response, framework opt-in
`guard` feature). Together with the two archived X-INV-01 P1s and the archived
F-EXT-02-P1-01, the invariant now has five live violations.

## Follow-Up

Fixes land in S-RDM-01: percent_decode must not slice `&str` at byte offsets
(parse `%xx` only when `input[i+1..i+3]` is a char-boundary-safe ASCII window,
e.g. check `input.is_char_boundary(i+1) && input.is_char_boundary(i+3)` or
work on the byte vec with `u8::from_str_radix` on `input.as_bytes()`);
parse_guard_response must require `start <= end` (fall back to `trimmed` when
the brace order is invalid). Add both fixtures to Q-FLT-01.
