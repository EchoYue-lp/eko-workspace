# A-PLG-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documents about plugin/skill/hook lifecycle map to current code:
claims are current, fixed, stale, or regressed, with no silently carried-over
assertions.

## Method

Compare documented claims against verified current code for the EKO plugin
runtime, skill hub, hook loader/dispatcher, and watcher.

```text
1. echo-agent-cli/docs/MASTER-PLAN.md :51 (skill sync), :69 (config watcher
   hot reload), :72 (PluginRuntimeService P0-4), :73 (plugin component
   wiring P1), :75 (HookEventDispatcher queue), :237-243 (skill marketplace
   model), :370-408 (plugins section, incl. project_root/workspace claim),
   :412-425 (task hook delivery)
2. echo-agent-cli/docs/skill-sync.md (paths, sync semantics)
3. echo-agent-cli/docs/system-deep-dive/06-skills.md (:263, :272 hub root;
   :180/:316 two-path activation asymmetry)
4. Cross-referenced: F-PLG-01 V05-01 (framework plugin docs), A-CFG-01 V05-01
   (config docs)
```

## Expected Result

Each claim classifies as current/fixed/stale/regressed with code anchors.

## Result

- Exit code: not-applicable (document comparison)
- Summary of classifications (full table in the task report):
  - MASTER-PLAN:72/:73 plugin runtime transactional claims — **current**
    (V04-01 dynamic tests green).
  - MASTER-PLAN:75 + :412-425 HookEventDispatcher bounded queue, flush,
    idempotent shutdown — **current** (V04-03).
  - MASTER-PLAN:69 watcher hot-reload of hooks+webhooks incl. removal — **current**
    (config_watcher.rs:227-278 handles Remove; A-CFG-01 P1-01 cross-ref for the
    workspace-switch freeze).
  - MASTER-PLAN:375 "project_root derived from the agent's working_dir so
    workspace switches are reflected without recreating the service" —
    **partial/stale**: candidate scans read `agent.working_dir()` at call time
    (plugin_runtime.rs:986-991, current), but the apply swap base and the LSP
    manager root use the boot-fixed `self.lsp.project_root`
    (plugin_runtime.rs:608, :946-984; runtime.rs:504-591), and no switch
    path invokes the runtime — finding P1-01.
  - MASTER-PLAN:51 skill sync completion — **current** with caveat: the
    post-sync runtime refresh is a silent no-op for existing skills
    (F-SKL-01-P2-01 cross-ref) — feeds P2-02.
  - skill-sync.md user skill paths `~/.echo-agent/skills/` and
    `~/.echo-agent/enabled-skills.json` — **stale**: runtime uses `~/.eko`
    (main.rs:66, registry.rs:101-103) — finding P3-02.
  - 06-skills.md:263/:272 hub root `~/.echo-agent/skills/` — **stale** (same
    rename) — P3-02. 06-skills.md two-activation-path asymmetry — **current**
    (F-SKL-01-P1-02, capabilities.rs:659-730).
  - F-PLG-01-P2-01 (config discovery `.eko/plugins` vs runtime
    `.echo-agent/plugins`) — **current**, cross-referenced.
- Log/artifact: none

## Deviations

None beyond the recorded stale claims (findings P1-01, P3-02).

## Conclusion

Supports: MASTER-PLAN plugin/hook lifecycle claims are mostly current with
one stale workspace-rebinding claim (P1-01); skill-hub docs carry the
pre-EKO `~/.echo-agent` root (P3-02); all other historical claims checked
map to code or are already captured by dependency-task findings.

## Follow-Up

Task report A-PLG-01.md.
