# Q-PERF-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every archived cancellation-cleanup defect class is accounted for in the
Q-PERF-01 matrix, and any additional resource-leak sites reachable on cancel,
timeout, drop, or crash paths are either newly filed or shown to be bounded.

## Method

```text
1. Grep both repositories for cancellation/lifecycle primitives:
   CancellationToken, tokio::spawn without token, Drop impls, timeout,
   abort_all, detach, cancel propagation.
2. Inspect EKO-specific cleanup paths: AgentPool acquire/release/eviction,
   run driver release (run_driver.rs), cron fire release
   (scheduler/runner.rs), TaskAgentLease Drop (tasks/service.rs:89-104),
   run cancel-token registration Drop (store.rs:141-159),
   HookEventDispatcher queue bounds, frontend module-level intervals
   (authStore.ts), chat turn cancel flows (A-SRF-03/A-SRF-04 paths).
3. Map every archived cancel/timeout/leak finding to the task question
   "cancellation cleanup" and record canonical IDs in the matrix.
```

## Expected Result

Archived cancel defects are all current (not fixed/stale), and any new leak
paths found in the sweep are either filed as new findings or explicitly
bounded.

## Result

- Exit code: not-applicable (static inspection)
- Duration: ~20 min
- Summary:

Archived cancellation defects verified current and folded into the matrix:
- F-SUB-02-P1-01/P1-02 — Team has no cancel token; timeout/abort detaches
  spawned member tasks (members keep running/writing after the parent ends).
- F-MAG-01-P1-01 — handoff target runs as detached uncancellable spawn
  (handoff/mod.rs:262-273).
- F-INT-02-P1-01..03 — LSP no timeout (blocks shutdown forever); QQ channel
  busy-spins after stop (qq/channel.rs:108-132); A2A cancel never propagates.
- F-HITL-01-P1-02 — blocking `read_line` defeats the shared deadline
  (A-HITL-01-P1-02 canonical).
- A-SRF-03-P1-01 — interrupt_prompt strands frontend turn state.
- A-SRF-04-P1-01 — REPL/channel turns not cancellable; Ctrl+C skips shutdown.
- X-EVT-01-P1-01/P1-02 — cancel/timeout class lost at the envelope boundary;
  timed-out turns end `'completed'`.
- F-OPS-01-P3-04 — scheduler fires unbounded and serial; cancellation never
  reaches in-flight fires; EKO never cancels its scheduler token.
- A-TSK-03-P2-01 / A-TSK-04-P1-02 — mid-wave store fault orphans Running
  claims; same-process resume hangs.
- F-RCT-03-P2-04 — abandoned streams leave trace runs Running with no
  terminal and no finalization.
- F-RCT-05-P1-01 — cancel mid-tool-batch saves a checkpoint with unpaired
  tool calls; resume validator rejects and wipes conversation.
- A-FE-03-P3-05 — authStore module-level interval + focus listener never
  cleaned (frontend leak).
- A-TSK-04-P3-01 — hook events enqueued at crash never replayed.

Resource-leak sweep results (no new unfiled leaks):
- AgentPool: run driver (run_driver.rs:100), cron fire (scheduler/runner.rs:
  106-109), and TaskAgentLease Drop (tasks/service.rs:89-104) all release;
  5-min idle monitor evicts (agent_pool.rs:749-806). Bounded.
- Run cancel-token map: entries removed on Drop (store.rs:141-159). Bounded.
- HookEventDispatcher: bounded queue with deliberate backpressure
  (hook_event_dispatcher.rs:88,127). Bounded.
- Tool artifact partial files: removed in Drop (artifact.rs:286-293). Bounded.
- New growth seen on cancel-adjacent paths is filed as:
  Q-PERF-01-P3-01 (unbounded fanout queue retains events while a slow
  consumer is alive) and Q-PERF-01-P3-03 (run-store caches never evict).

## Deviations

One stale comment: scheduler/runner.rs:103-105 claims "drive_run_async
notably does NOT release — a pre-existing minor leak", but run_driver.rs:100
now releases. No behavioral leak; recorded as historical drift in V05.

## Conclusion

Supports the V03 matrix rows of the task report: the cancellation defect
space is already densely archived (17 canonical IDs), the pool/store
lifecycles are bounded, and the only new cancel-adjacent growth points are
the fanout queue and cache-eviction gaps filed as P3 findings.

## Follow-Up

None.
