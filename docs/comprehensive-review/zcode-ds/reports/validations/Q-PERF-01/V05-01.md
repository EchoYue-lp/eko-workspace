# Q-PERF-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every performance/resource finding from the dependency reports (React,
TaskRuntime, persistence, frontend) is either cross-verified against current
code (classified current/fixed/stale/regressed) or proven duplicate-free
before being filed as new.

## Method

```text
1. Extracted all performance-relevant findings from dependency reports:
   F-RCT-02/03/04 (streaming/loop), F-CMP-01 (compression),
   F-OPS-01 (scheduler/trace), F-SUB-02, F-MAG-01 (subagent/handoff),
   F-MEM-01, F-INT-02, F-HITL-01 (stores/transport/HITL),
   A-TSK-01/03/04/05/06 (TaskRuntime), A-STATE-01, X-STA-01,
   A-FE-02/03 (frontend), Q-STA-01 (modules), X-EVT-01 (envelope).
2. Re-verified each against current source (key ones re-anchored; see matrix).
3. Confirmed the four new Q-PERF-01 findings do not duplicate any of them:
   searched per store/path/behavior (rewrite_plan, save_messages,
   UnboundedSender, cache eviction) in both repositories.
```

## Expected Result

Canonical findings all classify as `current`; no new finding duplicates a
canonical ID.

## Result

- Exit code: not-applicable (static)
- Duration: ~25 min
- Summary:

Canonical cross-verification (all `current`):
- F-OPS-01-P2-02 JsonlRunStore O(N²): current — re-anchored
  trace/mod.rs:733-801, now measured against real data (V02-01).
- A-TSK-06-P3-02 full_output unbounded in events.jsonl: current — measured
  (V02-01).
- F-CMP-01-P1-01/02/03: current — sliding_window.rs:48-75 keeps window by
  count only; summary.rs:346 immortal summary per pass.
- F-RCT-02-P2-02 LoopDetector dead: current — config plumbed
  (config.rs:1025-1031), no loop consultation; `max_iterations=0` = unlimited
  (stream_channel.rs:521-527) with EKO default 0 (config.rs:469) and
  `run_budget: None` (chat_driver.rs:509) so wind-down never fires.
- F-RCT-03-P1-01 bounded-channel drops: current (stream_channel.rs:41,
  stream_macros.rs:40-48).
- F-RCT-03-P2-01 whole-response buffered: current (stream_channel.rs:597-657).
- F-RCT-04-P3-01/02 batch bounds: current (executor/processor paths).
- F-SUB-02-P1-01/02, F-MAG-01-P1-01, F-INT-02-P1-01..03, F-HITL-01-P1-02:
  current (see V03-01).
- F-OPS-01-P3-04 scheduler fires unbounded/serial, cancel never reaches
  in-flight fires, EKO never cancels its scheduler token: current
  (scheduler/runner.rs:75-101 — new token per fire, pool-acquired agent, no
  runner-level cancel wiring).
- X-STA-01-P1-01 deletion gap: current (no delete API on RuntimeStateStore;
  state/file.rs has no per-run delete).
- A-TSK-05-P2-02 worktree sweep gap: current.
- A-TSK-01-P1-01 torn tail: current (file_shadow.rs:115-117 promises future
  truncation pass).
- F-MEM-01-P1-01/02, A-STATE-01-P2-02: current.
- A-FE-03-P2-01 O(n²) per-token Set construction: current (chatStore
  reducer, whole-chat subscriptions).
- A-FE-03-P3-04/05, Q-STA-01-P3-04 oversized modules: current.
- X-EVT-01-P1-01/02 envelope cancel/timeout class loss: current.

Stale classification:
- scheduler/runner.rs:103-105 comment "drive_run_async notably does NOT
  release" — STALE (run_driver.rs:100 now releases the pool entry). Doc
  drift only; no behavior.

Duplicate check for new findings:
- Q-PERF-01-P2-01 (file_shadow rewrite_plan full rebuild): distinct from
  F-OPS-01-P2-02 (different store, different repo layer, different impact
  surface: DAG executor critical path) and from A-TSK-01-P2-01 (read-path
  rebuild gap, not write-path rebuild). Not duplicated.
- Q-PERF-01-P3-01 (UnboundedSender fanout): distinct from F-RCT-03-P1-01
  (framework channel is bounded, drops instead). Not duplicated.
- Q-PERF-01-P3-02 (FileConversationStore full-record rewrite): distinct from
  F-MEM-01-P2-01 (write protocol/fsync hardening, not the O(N²) rewrite) and
  F-MEM-01-P3-02 (ordering contract). Not duplicated.
- Q-PERF-01-P3-03 (run-store cache never evicts): the disk side of
  F-OPS-01-P2-02 mentions "no size caps or retention"; the in-memory cache
  and per-run map retention is a separate RAM-growth aspect, filed with
  explicit backlink. Not a duplicate filing.

## Deviations

None.

## Conclusion

Supports the canonical matrix and the four new finding IDs in the task
report; all archived performance findings remain current; one stale comment
identified without behavioral impact.

## Follow-Up

None.
