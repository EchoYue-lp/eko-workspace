# S-QA-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash, S-QA-01 synthesis task)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories (verified before and after the re-runs in V02-01)

## Claim

The zcode-ds review ledger is arithmetically exact: 95 task reports (5 B + 38
F + 29 A + 10 X + 13 Q) and 729 immutable validation reports (21 B + 282 F +
243 A + 57 X + 126 Q) exist on disk, every validation link in every task
report resolves to a real report file, every validation report carries a
valid protocol status, and no placeholder/overwritten/duplicate-attempt
artifact exists.

## Method

```text
cd docs/comprehensive-review/zcode-ds/reports
ls tasks/*.md | wc -l                                          # 95
find validations -name "*.md" | wc -l                          # 729
for d in validations/*; do echo "$d: $(ls $d | wc -l)"; done   # per-task counts
grep -rh "^> Status:" validations/ | normalize | sort | uniq -c  # status census
python3: for each task report, regex-extract every ](validations/<task>/<file>.md)
  link (styles ../validations/ and validations/) and check file existence
python3: parse each task report's "## Validation Matrix" table rows (IDs
  starting with V) vs files on disk, excluding declared "not applicable" rows
find validations -name "*.md" -size -400c     # placeholder check
find validations -name "*.md" | awk duplicate-attempt-ID check
```

Environment: macOS arm64, zsh. All inspection is file-system-level.

## Expected Result

- 95 task reports and 729 validation reports; per-phase sums equal the totals.
- Zero missing links across all task reports.
- Status census sums to 729 with only protocol statuses (passed / failed /
  inconclusive / not_run) or passed-with-caveat variants.
- Any matrix-row-vs-file mismatch is explainable (multi-attempt files, folded
  conditional validations, not-applicable rows).

## Result

- Exit code: not-applicable (read-only file-system inspection)
- Summary:
  - Task reports on disk: **95** = B 5 + F 38 + A 29 + X 10 + Q 13
    (S: 0 — this task produces the first S-phase report).
  - Validation reports on disk: **729** = B 21 + F 282 + A 243 + X 57 + Q 126.
    Exact match with the zcode-ds/README.md claim ("95 task reports + 729
    immutable validation reports total").
  - All **1,894** validation-file links found in the 95 task reports resolve
    to existing files; **zero missing**.
  - Status census of all 729 reports: **673 passed** (including passed-with-
    caveat variants), **52 failed**, **1 inconclusive**, **3 not_run**.
    Sum = 729. No UNKNOWN status string.
  - The 52 failed reports are all system-under-test invariant violations
    recorded as findings (19 Q-E2E-01 scenario rows, 6 Q-FLT-01, 5 Q-FLT-02,
    2 Q-FW-02, 1 Q-STA-01, plus 18 earlier-phase static/deep validations in
    A-/B-/F-/X- tasks). None is a harness/execution failure.
  - Matrix-row-vs-file differences in 15 task reports were each classified:
    multi-attempt immutability (A-BOOT-01, A-CFG-01, A-CHAT-01, A-DOM-01,
    A-INP-01, A-SRF-02, A-SRF-03, F-EVO-01, F-INT-02, Q-FLT-02, X-TSK-01,
    etc. — 58 task directories contain more files than unique validation IDs);
    folded conditional validations (F-LLM-01 V05, F-MEM-01 V05);
    not-applicable rows without files (Q-GUI-01 V03–V05); one report covering
    two commands (A-FE-03 V04: vitest + tsc in one report).
  - No placeholder reports (zero files < 400 bytes); no duplicate attempt IDs;
    attempt convention respected (e.g., F-EVO-01 V04-05-01/02, A-BOOT-01
    V03-01..03, Q-FLT-02 V03-01/V03-02 — failed attempts preserved, corrected
    runs got new attempt numbers).

## Deviations

None. All expected counts matched exactly; the 15 matrix-vs-disk differences
are fully explained by the immutable-attempt and conditional-validation
conventions, not by missing reports.

## Conclusion

Supports: the zcode-ds review ledger is exact — 95 task reports and 729
validation reports, per-phase as claimed in the track README; every report
link resolves; every report has a valid status; no overwrite or placeholder
artifacts. The synthesis may therefore base its per-phase tables and status
statistics on these counts. Does not prove: that each report's prose is
accurate (exit-code semantics of the Q-* gates are re-verified in V02-01 for
the two commands this task re-runs); content-level quality of individual
reports.

## Follow-Up

None in this validation. V02-01 (unexecuted-matrix audit + gate/doctest
contradiction resolution) and V03-01 (flaky/inconclusive/not_run
classification) complete the S-QA-01 validation set.
