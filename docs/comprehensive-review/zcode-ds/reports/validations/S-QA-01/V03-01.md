# S-QA-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash, S-QA-01 synthesis task)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories

## Claim

Every validation report whose status is not a plain pass or a finding-bearing
fail is properly classified: the 3 `not_run` reports carry concrete
environmental reasons with attempted prerequisite checks, the 1 `inconclusive`
report is a documented static-only handoff that was subsequently resolved, and
the review corpus contains no flaky result (no ignored-as-flaky-escape tests,
no retry wrappers, no nondeterministic failures recorded as pass/fail).

## Method

```text
# 1. Full status census (see V01-01): grep "^> Status:" over all 729 reports.
# 2. Full reads of the 4 special-status reports:
#    validations/Q-E2E-01/V47-01.md, V48-01.md, V49-01.md (not_run)
#    validations/F-FEAT-01/V04-01.md                       (inconclusive)
# 3. Full read of validations/Q-TST-01/V03-01.md (ignored/flaky/platform-gated
#    inventory) and validations/Q-DEP-01/V03-01.md (advisory scan method).
# 4. Grep over the corpus for rerun/flake markers in reports' Deviations
#    sections and cross-check the two declared not_run-without-file matrix
#    rows (A-DOM-01 V04, F-EXT-03 V04) against their task reports.
```

## Expected Result

- 3 not_run with concrete environmental reasons (credentials/services/GUI
  absent) and evidence the prerequisites were checked at attempt time.
- 1 inconclusive traceable to a needs_evidence handoff that a later executed
  validation resolved.
- A flaky inventory that is empty of actual flaky tests; platform-gated tests
  are standard POSIX/Windows splits with both arms runnable on the CI host;
  any nondeterminism risk is documented, not silently treated as green.

## Result

- Exit code: not-applicable (read-only inspection of reports)
- Summary:

**not_run (3)** — all in Q-E2E-01, all recorded with concrete environmental
reasons and attempted prerequisite checks on 2026-08-12:
- V47-01: live GUI + real-LLM turn — env scan zero credential matches, no
  `.env`, no `~/.echo-agent/` model config, GUI launch forbidden by the
  review protocol.
- V48-01: real IM channel traffic — no app tokens/bots, no channel config,
  no gateway connectivity.
- V49-01: real browser sidecar + MCP server — `playwright` not installed, no
  browser cache, no `~/.echo-agent/mcp.json`, no server process.
Per the Q-E2E-01 card ("unavailable credentials or external services are
explicit `not_run` reports only when the scenario was attempted and
environmental prerequisites were checked"), these make the task completable
legitimately. All static scenario pairs (V01–V46) stand as authoritative.

**inconclusive (1)** — `F-FEAT-01/V04-01`: static-only by task definition
(feature-topology task); rows handed to Q-FW-02 as `needs_evidence` in the
task report's handoff (F-FEAT-01.md:150,175). Resolution recorded: Q-FW-02
V01–V12 executed all 12 standalone feature compiles with exit code 0,
resolving the needs_evidence rows. The inconclusive report was preserved
immutably; the conclusion it deferred is now supported.

**Advisory scanning (Q-DEP-01 V03-01)** — classified `passed`, not
inconclusive and not skipped: github.com was unreachable from the host
(curl 000; git RPC failed), so the rustsec DB was snapshotted from the
codeload tarball (1216 advisories, 2026-08-12) and served locally with
`--stale`; exit code 1 (vulns found) was recorded as the expected tool
semantics; per-crate reachability was traced with `cargo tree -i`. The
network workaround and the DB-freshness window are documented deviations,
not silent skips. Similarly, `npm audit` was run against the npmjs registry
override because the configured mirror (npmmirror) returns 404 on the audit
endpoint — recorded as deviation → finding Q-DEP-01-P3-02.

**Flaky/platform-gated inventory (Q-TST-01 V03-01)** — clean:
- 5 `#[ignore]` tests, all documented opt-in live smokes (LSP smoke via
  `EKO_LSP_SMOKE=1`, provider smokes via `EKO_PROVIDER_SMOKE=1`, 2 Zotero
  credential-gated); none hides a required unit invariant.
- Platform splits (`#[cfg(unix)]`/`#[cfg(target_os)]` in a2a/serve.rs:176,
  config.rs:714, sandbox/local.rs, tui/clipboard.rs:24-154, tauri/ipc.rs:
  135-138, etc.) are standard POSIX/Windows test arms; both CI host arms run.
- Zero frontend `describe.skip`/`it.skip`/`test.skip`.
- Zero flaky-test escape hatches: no `#[ignore]`-as-flake, no retry wrappers,
  no sleep-tolerance assertions.
- One minor nondeterminism risk documented, not filed: pipeline.rs:1634
  controlled-delay test with a biased select (Q-TST-01 V02 sample 2 note).
- One harness-quoting failure recorded as an immutability-correct attempt
  pair: F-EVO-01 V04-05-01 (failed, harness quoting) → V04-05-02 (passed) —
  not product flakiness.
- 3 ignored tests in the Q-FW-01 V04 tally (echo-tools web/media groups +
  1 other) and 3 GUI-matrix ignored doctests (CLI-side, pre-existing) are
  documented, reasons owned by Q-TST-01/Q-GUI-01.

**Declared not_run matrix rows without separate files** — A-DOM-01 V04
(live-network provider/Zotero) and F-EXT-03 V04 (live-network providers) are
marked `conditional | not_run` in their matrices with stated reasons (opt-in
`#[ignore]`, API keys required, read-only review); they comply with the
REPORTING.md conditional rule. Noted here for completeness.

## Deviations

None. All four special-status reports conform to the protocol: `not_run` only
with concrete environmental reasons and attempted prerequisite checks;
`inconclusive` only for a documented static-only handoff; no flaky result in
the corpus.

## Conclusion

Supports: the review corpus has exactly 3 not_run (Q-E2E-01 V47–V49, all
credential/environment-gated with checked prerequisites), 1 inconclusive
(F-FEAT-01 V04-01, resolved by Q-FW-02 V01–V12), 52 deterministic
invariant-violation failures, 673 passes, and zero flaky results. The
platform-gated and opt-in-live-test inventory is clean and cannot inflate the
green gates. Q-DEP-01's advisory scan is a passed validation with documented
network workarounds, not a skipped check. Does not prove: absence of flakiness
in future runs (only the reviewed executions were inspected); the live
behavior of the 5 opt-in smokes (never executed here).

## Follow-Up

Synthesis report `quality-and-validation-review.md` (not_run/inconclusive
classification section and flaky/platform-gated list); S-RDM-01 (note the
pipeline.rs:1634 nondeterminism risk inside the F-RCT-04-P1-01 test
reconciliation item).
