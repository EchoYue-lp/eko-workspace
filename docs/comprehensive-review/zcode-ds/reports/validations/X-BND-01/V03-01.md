# X-BND-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Per the AGENTS.md framework-deletion rule ("dead" = framework-internal
unreachable AND not a reasonable public option; capability menus and trait
multi-implementations are retained), each suspected framework duplicate
classifies as either a retained public option (capability menu /
multi-implementation / documented convergence phase) or a true dead parallel
authority that is a deletion candidate.

## Method

Public-API and caller checks:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# store impls (capability menu?)
grep -rn "pub struct InMemoryStore\|pub struct FileStore\|pub struct SqliteStore\|pub struct FileConversationStore\|pub struct SqliteConversationStore" echo-agent/echo-state/src/memory/*.rs
grep -rln "impl Store for\|impl ConversationStore for" echo-agent/echo-state/src/memory/*.rs
# legacy task executor surface (retained option?)
grep -rn "pub struct TaskExecutor\|pub struct TaskManager" echo-agent/echo-orchestration/src/tasks/*.rs
grep -rn "execute_ready_tasks" echo-agent echo-agent-cli --include="*.rs" | grep -v "fn execute_ready_tasks"
# dead parallel framework APIs (deletion candidates?)
grep -rn "pub struct ContextAssembler\|pub struct ContextSelector" echo-agent/src/context/*.rs
grep -rn "pub struct ToolRiskClassifier\|pub enum ToolRiskCategory" echo-agent/echo-execution/src/risk.rs
grep -rn "pub struct HandoffManager\|pub struct HandoffTool" echo-agent/src/handoff/mod.rs
grep -rn "pub struct NotebookTracker" echo-agent/src/notebook/mod.rs
```

## Expected Result

Store impls and the legacy `TaskExecutor`/`TaskManager` surface are `pub` and
are reasonable framework options (retain); `ContextAssembler`/`ContextSelector`,
`ToolRiskClassifier`, `HandoffManager`, `NotebookTracker` are `pub` but have no
production callers or duplicate an existing authority (deletion candidates).

## Result

- Exit code: not applicable (static grep)
- Summary:
  - **Retained as framework option (capability menu / multi-impl)**:
    - `InMemoryStore` (store.rs:20), `FileStore` (:220), `SqliteStore`
      (sqlite_store.rs:65), `FileConversationStore` (file_conversation.rs:68),
      `SqliteConversationStore` (sqlite_conversation.rs:20) — 4 `Store` impls +
      2 `ConversationStore` impls, all `pub` (F-MEM-01/02 verdict: menu, keep).
    - 6 compressors (`Summary`/`SlidingWindow`/`Hybrid`/`Adaptive`/
      `IncrementalSummary`/`VisibilityHorizon` + `ContextManager`) — menu with
      documented dormant entries (F-CMP-01 verdict: keep; delete only the
      never-registered macro `#[compressor]` extension per roadmap).
    - Legacy `TaskExecutor` (echo-orchestration/src/tasks/executor.rs:311) +
      `TaskManager` (manager.rs:13) + `TaskHooks`/`VerifierFactory` — `pub`,
      all `execute_ready_tasks` callers are in the same file's `#[cfg(test)]`
      modules (executor.rs:1888, :1938, :1962, :2104, :2133); documented
      convergence Phase 3 surface (F-TSK-01-P3-01 / F-TSK-03 verdict: retain
      as framework option until the iteration roadmap deletes it).
  - **Deletion candidates (pub but dead parallel authority)**:
    - `ContextAssembler` (src/context/mod.rs:118) / `ContextSelector`
      (selector.rs:10) — parallel context-selection authority with byte-based
      estimate and dead budget fields (F-CTX-01-P2-03).
    - `ToolRiskClassifier`/`ToolRiskCategory` (echo-execution/src/risk.rs:66, :7)
      — third risk-classification authority, zero live callers (F-EXT-01-P3-02).
    - `HandoffManager`/`HandoffTool` (src/handoff/mod.rs:144) — second
      name→agent map and second routing tool, feature-gated, never registered
      by default (F-MAG-01-P2-01).
    - `NotebookTracker` (src/notebook/mod.rs) — public module whose documented
      contract no code honors, no EKO counterpart (F-NBK-01-P2-01).
  - **Mixed**: `ProviderAdapter`/`AdapterClient` (F-LLM-01-P2-03) is a dead
    second provider contract — delete with the thinking-protocol authority
    moved to the live adapter.

## Deviations

None. No `#[allow(dead_code)]`-masked surface was misclassified as live; the
EKO-side `IpcAuth`/`IpcPermission` dead gates (A-HITL-01-P2-04, A-TOOL-01-P3-01)
are application deletion candidates, already filed under their canonical IDs.

## Conclusion

Supports: the framework "capability menu" retention rule is correctly applied
to stores/compressors/legacy task surface; the dead parallel authorities are
distinct deletion candidates with defined scopes. The task report's
deletion-target matrix therefore separates "keep as option" from "delete".

## Follow-Up

None.
