# X-BND-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The documented semantic duplicates are real at the reviewed commits: each one
has a live or dead-but-public second implementation whose call path can be
traced, and no duplicate documented in the dependency reports has vanished or
regressed into a third variant.

## Method

Behavior/call-path checks on the highest-impact documented duplicates:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# 1. three diff engines (A-PROJ-01-P2-03)
grep -rn "generate_unified_diff\|fn diff_file" echo-agent/echo-tools/src/files/diff.rs echo-agent-cli/echo-agent-app-core/src/diff.rs echo-agent-cli/src/tauri/commands/files.rs
# 2. three retry authorities (F-REL-01-P2-01)
grep -rn "pub fn with_retry_if\|pub struct RetryPolicy" echo-agent/echo-core/src/retry.rs
grep -rn "fn retry_llm_call" echo-agent/src/agent/react/run/retry.rs
grep -rn "retry_llm_call(" echo-agent/src/agent/react/run/react_loop.rs
# 3. four URL-download tools (F-EXT-03-P2-01)
grep -rn "struct WebFetchTool\|struct WebFetchToolEnhanced\|struct ImageFetchTool\|struct PdfFetchTool" echo-agent/echo-tools/src
# 4. handoff second registry / registration (F-MAG-01-P2-01)
grep -rn "pub struct HandoffManager" echo-agent/src/handoff/mod.rs
grep -rn "handoff" echo-agent/src/agent/react/builder.rs echo-agent/src/agent/react/mod.rs
# 5. save_trace second trace authority (A-OBS-01-P2-01)
grep -rn "fn save_trace\|save_trace(" echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs
# 6. execution_mode sequential inert (F-TSK-02-P2-02)
grep -rn "execution_mode" echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs
# 7. dual config surface (F-SUB-01-P2-01)
grep -rn "pub struct SubagentDefinition" echo-agent/src/agent/subagent/types.rs
grep -rn "pub fn model\|pub fn tools\|pub fn timeout" echo-agent/src/agent/subagent/builder.rs
# 8. safe_segment duplication (F-MEM-01 layering)
grep -rn "fn safe_segment" echo-agent/echo-state/src/memory/file_conversation.rs echo-agent/src/state/file.rs
# 9. three project-root resolvers (A-MEM-01 layering; X-BND-01-P2-01)
grep -rn "fn find_project_root\|fn discover_project_root\|fn discover_echo_agent_dir" echo-agent-cli/echo-agent-app-core/src
grep -rn "pub struct InstructionResolver" echo-agent/echo-core/src/project_rules.rs
# 10. context-window constant duplication (A-CFG-01-P2-05)
grep -rn "DEFAULT_CONTEXT_WINDOW" echo-agent-cli/echo-agent-app-core/src/infra.rs echo-agent-cli/echo-agent-app-core/src/model_config.rs
```

## Expected Result

Every listed duplicate resolves to the same conclusion as its canonical
finding: a live second implementation (diff, retry, download, save_trace),
a public-but-inert surface (handoff, execution_mode, SubagentDefinition
fields), or a code-level copy (safe_segment, resolvers, constants).

## Result

- Exit code: not applicable (static grep)
- Summary:
  1. **Diff engines**: `generate_unified_diff` live in app-core `diff.rs:70`
     (REPL); framework `DiffTool` in echo-tools; GUI inline engine in tauri
     `files.rs` `diff_file` — three engines confirmed, hunk-type families
     duplicated (A-PROJ-01-P2-03 current).
  2. **Retry authorities**: `RetryPolicy` (echo-core/src/retry.rs:35) and
     `retry_llm_call` (`src/agent/react/run/retry.rs:13`, called live at
     `react_loop.rs:81,124`) — two live authorities on the same LLM stack;
     third (echo-orchestration tasks backoff) dormant (F-REL-01-P2-01 current).
  3. **URL-download tools**: `WebFetchTool` (web/fetch.rs:53),
     `WebFetchToolEnhanced` (media/web_fetch_enhanced.rs:39),
     `ImageFetchTool` (media/image_fetch.rs:17), `PdfFetchTool`
     (research/pdf_fetch.rs:14) + `PdfExtractTool` (pdf.rs:19) — four-way
     parallel download family with divergent caps (F-EXT-03-P2-01 current).
  4. **Handoff**: `HandoffManager` (handoff/mod.rs:144) exists; `handoff`
     tool is not registered by default anywhere in `react/mod.rs`/`builder.rs`
     (feature-gated re-exports only, lib.rs:77-78, 300-301) — second registry
     inert by default (F-MAG-01-P2-01 current).
  5. **save_trace**: defined executor.rs:3504 and called live at :449, :491,
     :509, :562 — parallel trace ledger writing task runs a second time for
     the diagnostics panel (A-OBS-01-P2-01 current).
  6. **execution_mode**: only hits in app-core are `#[cfg(test)]` constructions
     (executor.rs:4736, :5213, :5812); the framework field is accepted/stored
     but never consulted by the frontier (F-TSK-02-P2-02 current).
  7. **SubagentDefinition dual config surface**: struct types.rs:130 with
     builder setters `model` (:113), `tools` (:125), `timeout` (:155) — fields
     never read in production while EKO's loader drives construction
     (F-SUB-01-P2-01 current).
  8. **safe_segment**: identical logic at
     `echo-state/src/memory/file_conversation.rs:465` and
     `echo-agent/src/state/file.rs:253` — verbatim copy (X-BND-01-P3-01).
  9. **Project-root resolvers**: `find_project_root` (utils.rs:12, VCS-first),
     `discover_project_root` (project/context.rs:22, wrapper), `discover_echo_agent_dir`
     (evolution/review_integration.rs:378, `.eko`/`.git` only, returns `.eko`
     subdir), framework `InstructionResolver` (echo-core/src/project_rules.rs:57,
     git-root + AGENTS chain, used by instruction_provider.rs:201) — four
     resolvers with divergent marker sets (X-BND-01-P2-01).
  10. **DEFAULT_CONTEXT_WINDOW**: 396_000 duplicated at
      infra.rs:23 (usize) and model_config.rs:6 (u32) (A-CFG-01-P2-05 current).

## Deviations

None. No documented duplicate was found fixed, missing, or regressed at the
reviewed commits.

## Conclusion

Supports: the deletion-target matrix in the task report reflects live or
public-but-inert code at the reviewed commits; the canonical findings are
current, not stale. Does not prove: which public-but-inert surfaces are
framework capability-menu options vs deletion targets (see V03).

## Follow-Up

None.
