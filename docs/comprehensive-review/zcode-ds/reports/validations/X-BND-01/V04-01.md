# X-BND-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The EKO adapter layer is thin and lossless per the AGENTS.md gate 4: adapters
implement only conversion/metadata/policy-hook logic and do not re-own the
framework's ready frontier, DAG loop, generic retry/cancel, deadlock detection,
or a second validator.

## Method

Inspection of the four principal adapters' surface area:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# 1. EkoRevisionedTaskStore — method inventory
grep -n "impl RevisionedTaskStore for EkoRevisionedTaskStore\|async fn load\|async fn compare_and_commit" echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/revisioned_adapter.rs
# 2. EkoRuntimeDagController — callback inventory
grep -n "impl.*RuntimeDagController\|async fn load_snapshot\|async fn claim_task\|async fn resolve_task\|async fn block_task" echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs
# 3. EkoSubagentPromptCompiler — trait impl surface
grep -n "impl SubagentPromptCompiler for EkoSubagentPromptCompiler\|fn compile_system\|fn compile_invocation" echo-agent-cli/echo-agent-app-core/src/subagent_prompt.rs
# 4. apply_compressor — dispatcher, not engine
grep -rn "fn apply_compressor" echo-agent/src/config.rs
sed -n '186,200p' echo-agent/src/config.rs
```

## Expected Result

`EkoRevisionedTaskStore` implements only `load`/`compare_and_commit`;
`EkoRuntimeDagController` implements only the six callback methods;
`EkoSubagentPromptCompiler` implements only the two compile methods;
`apply_compressor` is a strategy match with no compression math of its own.

## Result

- Exit code: not applicable (static grep)
- Summary:
  1. **EkoRevisionedTaskStore** (revisioned_adapter.rs:26-56): `impl
     RevisionedTaskStore` contains exactly `load` (:38) and
     `compare_and_commit` (:47) — no patch engine, no validator, no frontier,
     no retry/cancel logic. CAS boundary only. Thin, lossless, conforms.
  2. **EkoRuntimeDagController** (executor.rs:1222-1564): implements
     `load_snapshot` (:1227), `claim_task` (:1254), `resolve_task`,
     `block_task` (:1564) plus the remaining framework callbacks — each maps
     1:1 onto store CAS. No loop, no frontier computation, no deadlock logic.
     The boundary's behavior defects (unguarded `set_task_status` in
     `block_task`/finalizers = A-TSK-04-P2-01; per-task cancel dead =
     A-TSK-03-P2-02) are inside-boundary defects, not second authority.
  3. **EkoSubagentPromptCompiler** (subagent_prompt.rs:177-237):
     `compile_system` (:178) + `compile_invocation` (:237) only — pure
     prompt content policy, no scheduling/state.
  4. **apply_compressor** (echo-agent/src/config.rs:186): framework `AppConfig`
     method matching `compress_strategy` ("summary"/"sliding"/"adaptive"/...)
     to framework compressor constructors; zero compression logic of its own;
     EKO call sites (runtime.rs:114, agent_pool.rs:948) invoke the framework
     method. Not even an EKO-owned dispatcher — fully framework-side.
  5. **EkoTaskToolPolicy** (reported by A-TSK-01/A-TSK-02): policy hook
     (schema extensions, scope resolution, capability checks, metadata round
     trip) invoked inside the framework `finalize_and_validate` path; no
     structural validation re-implementation (V01 of A-TSK-01 confirmed the
     only validator is framework `PlanValidator`).

## Deviations

None. The adapters conform to gate 4; no adapter re-owns ready-frontier,
DAG loop, retry/cancel, or deadlock logic.

## Conclusion

Supports: the adapter boundary verdict in the task report ("thin, lossless,
conforms") for the task graph, subagent prompt, and compression boundaries;
the boundary defects already filed (A-TSK-03-P2-02, A-TSK-04-P2-01,
F-HITL-01-P1-03) remain behavior defects inside the boundary, not duplicates.
Does not prove: absence of defects in adapter *behavior* beyond the cited
canonical findings (covered by their own tasks).

## Follow-Up

None.
