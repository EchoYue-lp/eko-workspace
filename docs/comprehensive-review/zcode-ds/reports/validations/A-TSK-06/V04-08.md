# A-TSK-06 / V04 / Attempt 08

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Framework subagent result-contract tests pass with the `subagent` feature:
outcome parsing bounds summary/remaining-work/touched files (UTF-8 safe), the
rendered result contract round-trips through the parser, and
`filter_history` drops tool and reasoning turns (no thinking leakage into
inherited history).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --features subagent --locked subagent::types
```

Note: the filter matches `src/agent/subagent/types.rs` tests (11 tests). The
default-feature run of the same filter matched 0 tests because the subagent
code is feature-gated; the feature-enabled run is the meaningful one.

## Expected Result

11 passed, exit code 0.

## Result

- Exit code: 0
- Duration: ~0.02 s (test run)
- Summary: `test result: ok. 11 passed; 0 failed; 0 ignored; 0 measured;
  579 filtered out`.
- Log/artifact: none

## Deviations

An initial run without `--features subagent` matched 0 tests (feature-gated
module); recorded here rather than as a separate failed attempt because it was
a filter-discovery run, not a claim validation.

## Conclusion

Supports: framework-side summary bounds (`DEFAULT_SUMMARY_CHARS=1200`,
`MAX_RESULT_ITEMS=64`, `MAX_DETAIL_CHARS=500`, `MAX_PATH_CHARS=2048`) and the
reasoning-drop in history filtering (V01-01/V03-01 thinking-isolation claim).

## Follow-Up

None.
