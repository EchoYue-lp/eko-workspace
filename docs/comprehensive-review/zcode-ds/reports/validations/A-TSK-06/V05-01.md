# A-TSK-06 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical design documents' claims about review input, thinking isolation,
acceptance/check separation, artifact retention, and bounded parent summaries
can be classified against current code (current / fixed / stale / regressed).

## Method

Grep and read of the specific claim lines in:

- `echo-agent-cli/docs/MASTER-PLAN.md` (lines 55-70, 155-215)
- `echo-agent-cli/docs/2026-07-17-subagent-results-and-completion.md`
  (lines 20-45, 90-160)
- `echo-agent-cli/docs/2026-07-27-runtime-dag-kernel-convergence.md`
  (lines 80-120, 148-165)
- `docs/MASTER-PLAN.md` (lines 80-115, 190-250)

Each claim was checked against the source anchors in V01-01/V02-01/V03-01.

## Expected Result

Claims classify cleanly; any contradiction becomes a finding.

## Result

- Exit code: not applicable (static document comparison)
- Duration: not applicable
- Summary:
  - MASTER-PLAN (app): "final answer and structured summary cannot refer to
    reasoning ... without the thinking trace" and "never promotes a thinking
    trace into the result" -> **current** (framework
    subagent/executor.rs:1200-1222, EKO executor.rs:3155-3182, prompt.rs:161-163).
  - MASTER-PLAN (app): "TaskRuntime review consumes the complete output rather
    than the bounded parent summary, and persists that output on the terminal
    boundary so restart recovery receives identical evidence" -> **current**
    (executor.rs:1461-1474; store.rs:1950-1966, :2039-2078; V04-04 test).
  - MASTER-PLAN (app): "right rail reports Subagent execution progress
    separately from Task acceptance progress" -> **current** (SubagentRun
    events vs `assess_task_execution`/review gate).
  - results-and-completion doc: "SubagentReleased 同时保存 structured result
    和完整最终 output;恢复后继续从 review boundary 执行" and "review prompt
    必须包含 summary 边界之后的完整 output" -> **current** (store.rs:1933-1970,
    executor.rs:1293-1313, V04-04).
  - results-and-completion doc: completion gate items (status completed,
    summary non-empty, remaining_work empty, observed-pass verification,
    required artifact with hash/producer id) -> **current**
    (executor.rs:695-765).
  - convergence doc: "Preserved `required_artifacts`, executable
    `execution_checks`, and semantic `acceptance_criteria` as distinct fields;
    removed the lossy verification-list flattening" -> **current**
    (types.rs round-trip test V04-05).
  - root MASTER-PLAN: "超长工具结果已统一为完整 artifact + 有界模型/会话投影"
    -> **current** for the task_execute path (`compact_completed_task_result`,
    task_execute_tool.rs:505-555).
  - root MASTER-PLAN: "完成判定同时检查 task node、required artifacts、
    verification 和 unresolved failures" -> **current**
    (`run_completion_blockers`, executor.rs:587-641).
  - No document claim was found asserting that EKO runtime `Artifact` records /
    `ArtifactProduced` events are produced by task completion; the dead
    projection (P2-01) is therefore an implementation gap, not a doc-drift
    contradiction. The memory-bridge plan reference (plan §991 "record the
    issue class for future avoidance") is **regressed** by the dead
    `ReviewFoundIssue`/`RepeatedTaskFailure` arms (P3-01).

## Deviations

None beyond the two findings noted above.

## Conclusion

The documented review/thinking/acceptance/summary claims are current; no
stale or fixed classification applies. The memory §991 claim is regressed
(unimplemented), which is finding P3-01.

## Follow-Up

None.
