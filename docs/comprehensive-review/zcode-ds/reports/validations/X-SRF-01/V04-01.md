# X-SRF-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Every already-filed surface-parity gap folded into this task's capability
matrix is (a) re-anchored at the reviewed commits, (b) classified current
(not fixed/stale/regressed), and (c) not re-filed under an X-SRF-01 ID.

## Method

For each canonical ID used in V01-01/V03-01, the primary evidence anchor was
re-read in source at the reviewed commits and checked against the finding's
claim; the finding's priority/layer were not re-derived. Findings newly
discovered by this task (X-SRF-01-P2-01, P2-02, P3-01) were double-checked
for overlap with any canonical finding before filing.

## Expected Result

All canonical IDs classify `current`; the three new findings are disjoint
from every canonical finding.

## Result

- Exit code: not-applicable (static cross-check)
- Summary:

| Canonical ID (owner) | Claim folded into X-SRF-01 | Anchor re-verified at reviewed commits | Classification |
|---|---|---|---|
| A-SRF-01-P1-01 | TUI no workspace surface; spill dir global | `src/tui/commands.rs` (no workspace variant, read); `src/tui/events.rs:1368,3266` ("TUI has no workspace concept" comments) | current |
| A-CFG-01-P1-03 | workspace switching GUI-only; REPL stub | `src/cli/cmd_impls/workspace.rs:114-146` (`ws_switch` prints "Switched to workspace" with no state/CWD change); `ws_link` comment "For now, link to 'default' workspace" (`:165-166`) | current |
| A-SRF-04-P1-01 | REPL/channel turns not cancellable | `src/cli/repl.rs:533` + `src/cli/channels.rs:244` (fresh `CancellationToken`, zero `.cancel()` producers — grep); no signal handler on CLI path (only `modes.rs:228`) | current |
| A-SRF-04-P1-02 / A-BOOT-01-P2-02 | channels-only boot omits scheduler + background service | `src/cli/modes.rs:118-235` (`run_channels_mode` never calls `start_task_service`/`start_scheduler_with_store`); `src/main.rs:357-411` channels-only branch | current |
| A-SRF-04-P2-01 | interrupted cron runs unresumable from CLI/channels | consumed from A-SRF-04 (recovery filter `service.rs:556-558`) — matrix row only | current |
| A-SRF-04-P2-02 | scheduler/background cancel tokens never fired | consumed from A-SRF-04 (`state.rs:662/692/699`) — matrix row only | current |
| A-OUT-01-P2-04 | TUI no research/export; CLI `/papers`; GUI workbench | `src/tui/commands.rs` (no research variant, read); REPL `src/cli/cmd_impls/research.rs`; channels NL papers routing `src/cli/channels.rs:171-179` | current |
| A-EVO-01-P2-03 | TUI evolution reduced (dashboard only) | `src/tui/commands.rs:130` (EvolutionDashboard variant only); handler `src/tui/events.rs:3972-3998` (metrics dump); REPL `src/cli/cmd_impls/evolution.rs` (18 `cmd!` blocks) | current |
| A-SRF-02-P1-01 | GUI browser panel dead (double `.setup()`) | `src/tauri/mod.rs:40` and `:311` (two `.setup(` closures) | current |
| A-SRF-03-P1-01 | GUI interrupt prompt strands turn state | `src/tauri/commands/chat.rs:511-534` (early return `{kind:"interrupt_prompt"}` before turn registration) | current |
| A-SRF-03-P1-02 | GUI error/cancel ends 'completed', partial content wiped | consumed from A-SRF-03 (`chatStore.ts:354-362`, `chatEventHandler.ts:140-150`) — matrix row only | current |
| A-SRF-01-P2-03 | TUI queue not drained on Cancelled | `src/tui/events.rs:657-660` (FinalAnswer drains) vs `:661-675` (Cancelled does not) | current (latent) |
| A-HITL-01-P1-02 | REPL provider EOF auto-approve + blocking read | `echo-agent-app-core/src/hitl/repl_provider.rs:69-77` (`read_line`; `"" => Approved`) | current |
| A-HITL-01-P2-03 | unattended runs auto-reject via empty dispatcher | consumed from A-HITL-01 (`agent_pool.rs:960-975`, `dispatcher.rs:82-87`) — matrix row only | current |
| A-HITL-01-P2-02 | channel approvals not per-sender | consumed from A-HITL-01 — matrix row only | current |
| A-INT-01-P2-01 | CLI `/mcp connect|disconnect` stubs | `src/cli/cmd_impls/skills.rs:236-274` (`connect` branch prints only) | current |
| A-INT-01-P2-05 | browser confirmations inherit REPL EOF defect | consumed from A-INT-01 — matrix row only | current |
| A-TOOL-01-P1-01 | writer subagents read-only (plan_mode) | consumed from A-TOOL-01 (`infra.rs:963`) — matrix row only | current |
| A-INT-01-P3-02 | LSP tools primary-agent-only | consumed from A-INT-01 (`runtime.rs:574-589`) — matrix row only | current |
| A-TOOL-01-P2-01 | GUI tool toggle cosmetic | consumed from A-TOOL-01 — matrix row only | current |
| A-SRF-04-P3-02 | REPL interactive-only (no single-shot) | consumed from A-SRF-04 — matrix row only | current |
| A-CHAT-01-P2-01 | `ChatDriverEvent::Interrupt` dead | consumed from A-CHAT-01 — matrix row only | current |

New-finding disjointness check:

| New X-SRF-01 finding | Overlap check | Verdict |
|---|---|---|
| X-SRF-01-P2-01 (REPL zero browser management surface) | A-INT-01 covers browser *reachability/recovery/over-gating* (P2-05 is the HITL arm) and CLI *MCP* stubs (P2-01); no finding covers the REPL browser management surface; A-SRF-01's matrix notes "user-driven browser panel vs /browser (matrix only)" — TUI-side only | disjoint, new |
| X-SRF-01-P2-02 (channels zero task-run management surface) | A-SRF-04-P1-02 covers the channels-only *boot* missing services; A-SRF-04-P2-01 covers the *recovery filter*; neither covers the absence of run-control (pause/cancel/resume) on the channel surface itself in combined mode | disjoint, new |
| X-SRF-01-P3-01 (REPL/channel turns cannot be steered) | A-SRF-04-P1-01 covers *cancellation* (token never fired) — steer is a distinct control (new instruction to an in-flight turn) with zero producers on REPL/channels; no finding covers steer on these surfaces | disjoint, new (same adapter family as A-SRF-04-P1-01, referenced) |

## Deviations

- Rows marked "consumed from" were not re-anchored line-by-line in this
  session (the dependency reports' anchors were accepted as facts per the
  task card); the five rows whose anchors matter most to the matrix verdict
  (workspace, cancel, channels services, interrupt, REPL provider) were
  re-verified in source directly.

## Conclusion

The cross-check supports: all canonical parity gaps are current at the
reviewed commits; the three new findings are disjoint from the canonical
set and legitimately new to this task.

## Follow-Up

None.
