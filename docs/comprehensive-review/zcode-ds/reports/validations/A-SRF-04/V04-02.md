# A-SRF-04 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The BackgroundTaskService (submit/driver/resume_pending path) tests pass at
the reviewed commit, including the resume filter and cancel-through-store
semantics.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked tasks::service
```

Default features (tui). Note: `tasks::background` was attempted as a second
filter in the same invocation and rejected by cargo (one TESTNAME filter
only); it was rerun separately (V04-03).

## Expected Result

The four tests in `tasks/service.rs::tests` pass:
`pipeline_submission_creates_only_task_runtime_run`,
`auto_resume_only_accepts_boot_recovery_pause`,
`dependency_wait_can_be_cancelled_through_runtime_store`,
`retry_registers_driver_immediately_instead_of_leaving_fake_running`.

## Result

- Exit code: 0
- Duration: 1m 06s compile + 0.28s test
- Summary: test result: ok. 4 passed; 0 failed; 0 ignored; 645 filtered out.
- Log/artifact: `/Users/ls/.zcode/cli/exec/sess_subagent_agent_4231337d-7184-45bc-921f-cc2d47d7a786/call_01_1btNjN6cgIWpjIKXDa7S2491-stdout.log` (contains the cargo filter error line for the malformed combined invocation, then the rerun)

## Deviations

The first invocation with two test filters errored ("unexpected argument
'tasks::background'") — a command-line mistake, exit code reported as 0 by
the wrapping shell for the combined command; the valid single-filter rerun is
the authoritative result above.

## Conclusion

Supports: background-service submit creates one TaskRuntime run; resume_pending
accepts only boot-recovery Paused runs; dependency-wait cancel propagates
through the runtime store; retry registers the driver immediately. This is the
behavioral baseline for the background trigger; it does not cover the
channels-only absence of the service (V02/V03).

## Follow-Up

None.
