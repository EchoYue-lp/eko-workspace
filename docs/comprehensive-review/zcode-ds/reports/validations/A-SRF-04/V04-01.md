# A-SRF-04 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The EKO scheduler fire_fn adapter (`build_fire_fn`) routes cron tasks through
`launch_cron_run` and persists terminal facts, per its unit tests at the
reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked scheduler::runner
```

Default features (tui). Cargo version: stable toolchain on macOS arm64.

## Expected Result

The three tests in `scheduler/runner.rs::tests` pass:
`build_fire_fn_routes_non_plan_cron_to_launch_cron_run`,
`build_fire_fn_strips_plan_marker_and_routes_to_launch_cron_run`,
`cron_stream_failure_is_persisted_and_returned_as_error`.

## Result

- Exit code: 0
- Duration: 0.45s compile (warm) + 0.23s test
- Summary: test result: ok. 3 passed; 0 failed; 0 ignored; 646 filtered out.
- Log/artifact: `/Users/ls/.zcode/cli/exec/sess_subagent_agent_4231337d-7184-45bc-921f-cc2d47d7a786/call_00_yrn2iXOWvmzkO4wBrzXJ3396-stdout.log`

## Deviations

None. These tests exercise the fire_fn directly (manual trigger semantics),
not the automatic scheduler tick; the tick is covered by F-OPS-01-V04-01
(empirical proof the tick never fires).

## Conclusion

Supports: cron fire_fn -> launch_cron_run -> one persisted run (Completed or
Failed) when invoked manually (e.g. `/cron run`); failure is persisted. Does
not prove the automatic tick fires (it does not — F-OPS-01-P1-01).

## Follow-Up

None.
