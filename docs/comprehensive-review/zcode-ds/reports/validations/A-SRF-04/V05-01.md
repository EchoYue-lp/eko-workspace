# A-SRF-04 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical documents about CLI/channel/cron/background triggers are classified
as current/fixed/stale/regressed against the reviewed code.

## Method

Compare claims in `echo-agent-cli/docs/2026-07-17-surface-parity-closeout.md`
(M10), `echo-agent-cli/docs/MASTER-PLAN.md`, and root `docs/MASTER-PLAN.md`
against current code:

```text
grep -rn "cron|channels|CLI" docs/2026-07-17-surface-parity-closeout.md
grep -n "cron|Cron|channels|Channels" docs/MASTER-PLAN.md
sed -n '10,30p;325,350p' docs/MASTER-PLAN.md
grep -n "cron|scheduler" src/tui/commands.rs src/tui/events.rs   # TUI /cron
sed -n '118,152p;150,230p;333,360p' src/cli/cmd_impls/all.rs     # /remember /forget /mode
```

## Expected Result

Each claim maps to current code or a known gap.

## Result

- Exit code: 0 (grep suite)
- Duration: < 5 min
- Summary:
  - M10 closeout:70-71 "CLI `/remember`/`/forget` print success without
    touching the installed memory store; CLI interaction mode fixed to Auto"
    -> **fixed**: `/remember` writes via layer_manager (all.rs:118-147),
    `/forget` deletes via layer_manager (all.rs:158-227), `/mode chat|task|auto`
    mutates the shared interaction-mode lock (all.rs:333-361).
  - M10 closeout:76 "TUI `/cron` sends prose to the agent instead of
    controlling the scheduler" -> **fixed**: TUI has a real cron command
    (`src/tui/commands.rs:118,193,259,290`; `handle_tui_cron`
    events.rs:2232+, operates the `app.scheduler` runner with
    list/create/delete/pause/resume/run/reload).
  - M10 closeout:117 "TUI controls the real scheduler directly; cron failures
    persist a Failed run" -> **current** (TUI uses the shared runner; failure
    persistence proven by V04-01).
  - M10 closeout:14 "Foreground, background, and cron runs" required on all
    surfaces -> **regressed (channels)**: channels-only mode has no
    BackgroundTaskService and no SchedulerRunner (V02-01), so background
    runs do not resume and cron does not exist on that surface; matches
    A-BOOT-01's classification of the same doc.
  - `docs/MASTER-PLAN.md:18` "TUI, GUI, CLI, and channels share the same
    Agent capabilities and differ only in input, rendering, and event
    projection" -> **regressed (channels services)** for the scheduler and
    background service; otherwise current (shared drive_chat per V02-01).
  - `docs/MASTER-PLAN.md:333` "All six entry points switched [to
    PreparedUserTurn] ... CLI REPL (chat_with_agent), channel" -> **current**
    (repl.rs:509, channels.rs:208).
  - `docs/MASTER-PLAN.md:69` iteration-2 webhook claim "cron emits
    CronTaskCompleted" -> **current but blocked**: the emitter is wired
    (scheduler/runner.rs:115-121) but the automatic trigger never fires
    (F-OPS-01-P1-01); manual `/cron run` emits.
  - EKO MASTER-PLAN M13 (line 219) cancellation semantics -> framework-owned
    (F-TSK-03), outside this task; not reclassified here.

## Deviations

None. Historical claims about the framework scheduler (F-OPS-01) are consumed
from that report, not re-derived from docs.

## Conclusion

Supports: M10's CLI memory/mode and TUI cron gaps are fixed; the
"foreground/background/cron on all surfaces" parity claim is regressed on the
channels-only surface; the parity invariant (MASTER-PLAN:18) is violated only
by the channels services gap.

## Follow-Up

None.
