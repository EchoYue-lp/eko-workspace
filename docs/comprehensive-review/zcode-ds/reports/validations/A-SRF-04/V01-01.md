# A-SRF-04 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The non-GUI/TUI trigger surface (CLI REPL, channel handlers, cron, background
service) has exactly one definition per trigger adapter, one shared chat
driver, and no parallel chat/background authority in `echo-agent-cli`; every
concept searched was classified as defined / registered / reachable or dead.

## Method

Repository-wide grep sweep over `echo-agent-cli` (src/ + echo-agent-app-core/)
plus targeted greps in `echo-agent` for the framework scheduler:

```text
cd echo-agent-cli
# drive_chat definitions + callers (excl. tests)
grep -rn "drive_chat" --include="*.rs" src/ echo-agent-app-core/src/
# ChatSink impls
grep -rn "impl.*ChatSink" --include="*.rs" src/ echo-agent-app-core/src/
# BackgroundTaskService constructions
grep -rn "BackgroundTaskService::new|with_pool|with_agent_provider" --include="*.rs"
# SchedulerRunner construction / start_scheduler callers
grep -rn "start_scheduler_with_store|SchedulerRunner::new|new_scheduler_runner"
# launch_cron_run callers; bind_scheduler callers
grep -rn "launch_cron_run|bind_scheduler" --include="*.rs"
# worker terminology (Subagent-only rule)
grep -rni "worker" --include="*.rs" src/ echo-agent-app-core/src/
# duplicate slash-command registration: "cron" name
grep -rn '"cron"|CronCommand' src/cli/cmd_impls/cron.rs src/cli/cmd_impls/advanced.rs
# framework scheduler tick predicate (dependency fact re-anchor)
sed -n '1,120p' ../echo-agent/echo-orchestration/src/scheduler/runner.rs
```

## Expected Result

One `drive_chat` definition with exactly the four known production call sites
(TUI, GUI, REPL, channel); three production `ChatSink` implementations; one
`BackgroundTaskService` construction path (AppState::start_task_service); one
scheduler construction path (start_scheduler_with_store) reachable from
TUI/CLI/GUI; `launch_cron_run` reachable only via the scheduler fire_fn; zero
`worker` terminology; any duplicate command registration identified.

## Result

- Exit code: 0 (grep suite; all commands completed)
- Duration: < 1 min
- Summary:
  - `drive_chat`: 1 definition (`echo-agent-app-core/src/chat_driver.rs:202`),
    4 production callers — TUI `src/tui/events.rs:2226`, GUI
    `src/tauri/commands/chat.rs:688`, REPL `src/cli/repl.rs:543`, channel
    `src/cli/channels.rs:262` (plus test call sites).
  - `ChatSink` impls: 3 production — `TuiChatSink` (events.rs:2031),
    `TauriChatSink` (chat.rs:1341), `ChannelChatSink` (chat_driver.rs:585);
    plus test-only `MockChatSink`.
  - `BackgroundTaskService`: production construction only inside
    `AppState::start_task_service` (state.rs:690/697); the service.rs
    constructions at :887/:930/:1042 are tests.
  - Scheduler: `start_scheduler_with_store` called at `src/cli/modes.rs:62`
    (CLI + TUI via `start_headless_services`), `src/tauri/desktop.rs:232`
    (GUI), and internally from `state.rs:640`; `SchedulerRunner::spawn`
    at state.rs:672. `new_scheduler_runner` (scheduler/runner.rs:164) builds
    the fire_fn that calls `launch_cron_run` (executor.rs:3895).
  - `launch_cron_run`: single production caller = `build_fire_fn`
    (scheduler/runner.rs:101); `surface_contract.rs:56` is a contract-test
    string.
  - `bind_scheduler` (plugin monitors): TUI main.rs:270, GUI desktop.rs:236,
    CLI modes.rs:93; NOT on the channels-only path.
  - `worker` terminology: zero matches in src/ + echo-agent-app-core/src
    (excluding network/worker_threads/test contexts).
  - Duplicate slash-command registration found: `CronCommand` is defined
    twice with the same name "cron" — `cmd_impls/advanced.rs:286-299`
    (stub that only prints "Use /cron add|remove|enable|disable|run") and
    `cmd_impls/cron.rs:45-111` (full subcommand implementation). Both are
    registered: repl.rs:168 (advanced) then repl.rs:179 (cron);
    `CommandRegistry::register` (command.rs:294-299) overwrites the name map
    (last wins = cron.rs) but the shadowed stub stays in `commands` and is
    listed by `by_category` used by `/help` (all.rs:25-49).
  - Framework scheduler re-anchor: `echo-agent/echo-orchestration/src/
    scheduler/runner.rs:80-93` tick predicate `next >= window_start && next
    <= now`; `run_once` (:169) is the only trigger that bypasses tick
    (confirms F-OPS-01-P1-01 dependency fact at current commits).

## Deviations

None. Note: the duplicate `cron` registration and the channels-only missing
service wiring (verified in V02) are captured as findings, not counted as
"duplicate authority" of the chat driver (there is none).

## Conclusion

Supports: one shared chat driver with four live surface call sites; three
sinks; one background-service construction path; one scheduler construction
path (CLI/TUI/GUI only); cron fire_fn is the single cron→TaskRuntime adapter;
no worker terminology; one duplicate slash-command registration (P3 finding).
Does not prove runtime behavior (V02/V03) or test results (V04).

## Follow-Up

None.
