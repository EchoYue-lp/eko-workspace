# A-SRF-04 / V04 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The surface-contract tests (capability matrix evidence per surface; shared
driver wire contract; cron runtime wire contract) pass at the reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked surface_contract
```

Default features (tui).

## Expected Result

`capability_matrix_has_evidence_for_every_surface`,
`shared_driver_wire_contract_preserves_product_facts`,
`cron_runtime_wire_contract_preserves_terminal_facts` pass.

## Result

- Exit code: 0
- Duration: 0.45s compile (warm) + 0.02s test
- Summary: test result: ok. 3 passed; 0 failed; 0 ignored; 646 filtered out.

## Deviations

None. Note the capability matrix is a static evidence table (GUI/TUI/CLI/
channel/cron columns); it does not execute any trigger — the cron column
("SchedulerRunner to launch_cron_run", surface_contract.rs:56) certifies the
wiring, not that the tick fires (F-OPS-01-P1-01).

## Conclusion

Supports: the shared-driver and cron wire contracts serialize/preserve the
product facts the trigger adapters depend on; the capability matrix documents
the intended parity. Does not prove runtime parity (V03 failed).

## Follow-Up

None.
