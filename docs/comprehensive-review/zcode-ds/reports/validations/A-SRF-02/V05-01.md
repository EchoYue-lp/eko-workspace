# A-SRF-02 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents describing the Tauri layer match the current code;
stale claims are classified.

## Method

Grepped `docs/MASTER-PLAN.md`, `echo-agent-cli/docs/MASTER-PLAN.md`,
`echo-agent-cli/docs/browser-runtime-design.md`, `docs/PROJECT-ANALYSIS.md`
(root) for Tauri/IPC/browser-event/setup/gate claims and compared against the
reviewed code anchors (V01-V04).

## Expected Result

Documented contracts are either live in code or explicitly classified stale.

## Result

- Exit code: not-applicable (doc comparison)
- Duration: ~30 min
- Summary of classified claims:
  - `echo-agent-cli/docs/browser-runtime-design.md:215` — "forwards the
    existing `BrowserEvent` stream over `browser://event`" — **regressed**:
    the only producer (mod.rs:58) sits in the overwritten first `.setup()`
    (V02-01); the contract is documented but the stream is dead (A-SRF-02-P1-01).
  - `docs/MASTER-PLAN.md:380,733,771` — "Tauri/TUI/CLI/channel/cron 共享事件
   与持久事实合同已建立" / "TaskRuntime task-local trace 与 framework external
   trace 都桥接到同一交互事件" — **current with a caveat**: the two bridges
   exist but are not single-authority (duplicate subagent tool projection,
   A-SRF-02-P2-01); the shared-contract claim remains true in shape, false in
   uniqueness.
  - `docs/MASTER-PLAN.md:483` — "Subagent tool start/completed 事件保留稳定
    `call_id`，Tauri 执行事件同步透传" — **current**: call_id is preserved on
    both projection paths (V03-02); the claim does not mention duplication.
  - `src/tauri/error.rs:3-10` — "Phase 6.2" header describing `IpcAuth`
    gates as active — **stale**: `require_full_auto`/`require_not_strict`
    have zero callers (V01-01); AGENTS.md documents that such gates were
    removed from `create_terminal`/`connect_mcp_server`. The dead machinery
    remains (A-SRF-02-P3-01).
  - `docs/MASTER-PLAN.md:772` — "Tauri 补齐 budget/guard/memory/safety/
    parameter/chart 等 notice 投影" — **current** (agent_event_to_chat_event,
    chat.rs:1511-1570).
  - `docs/MASTER-PLAN.md:789` — "Tauri 桌面首屏浏览器检查无 JS 控制台错误"
    — unverifiable statically (browser panel would show no sessions due to
    P1-01; a "no console errors" check would still pass vacuously); classified
    **stale for browser-panel behavior**.
  - `docs/MASTER-PLAN.md:962` — hook bridge wiring includes
    "tauri-task_runtime" — **current** (task_runtime.rs resume/retry pass
    hook bridges through execute_run).

## Deviations

None beyond the classifications above.

## Conclusion

Supports: the documented `browser://event` contract is regressed; the
"Phase 6.2" gate doc is stale; shared-event-contract claims are current in
shape with a duplicate-authority caveat. These classifications feed the
Historical Claim Status table of the task report.

## Follow-Up

None.
