# A-PROJ-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Each project/diff/coding symbol's definition → registration → live-caller chain is traced: live surfaces are (a) GUI file browser `diff_file`/`workspace_changes`, (b) REPL `/diff`/`/test`/`/fix`/`/code-review`, (c) TUI `@`-completion, (d) agent-facing framework `diff`/`repo_map`/`git_*` tools; `ProjectIndex` and `FileChangeTracker` writers are unreachable.

## Method

Static call-graph tracing from entry points:

```text
# GUI: tauri command registration
grep -n "diff_file\|workspace_changes\|list_files" echo-agent-cli/src/tauri/mod.rs
# frontend consumers
grep -n "filesApi.diff\|filesApi.changes\|filesApi.tree" echo-agent-cli/web-frontend/src/stores/fileStore.ts
grep -n "loadDiff\|loadChanges\|loadTree" echo-agent-cli/web-frontend/src/components/file-browser/FileBrowser.tsx
# REPL: command registration + CodingLoop construction
grep -n "register_all\|CodingLoop::new" echo-agent-cli/src/cli/repl.rs
# TUI: project_files single assignment
grep -n "project_files" echo-agent-cli/src/tui/mod.rs echo-agent-cli/src/tui/events.rs
# framework tool registration
grep -n "DiffTool\|RepoMapTool\|GitDiffTool" echo-agent/echo-tools/src/registry.rs
# project-context refresh sites
grep -rn "refresh_dynamic_context" echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src --include="*.rs"
```

## Expected Result

Every live surface resolves its data from current files at call time (or from a documented, bounded snapshot); no live surface consumes a never-invalidated cache.

## Result

- Exit code: not applicable (static trace)
- Summary:
  - GUI `diff_file` (files.rs:267-380, registered tauri/mod.rs:122): old content via `git show {ref}:{path}` (`spawn_blocking`, files.rs:296-308) and new content via fresh `read_to_string` per call — derived from current files at call time. Failure of `git show` silently yields `old_content = ""` (files.rs:303, `_ => String::new()`).
  - GUI `workspace_changes` (files.rs:211-231, registered tauri/mod.rs:124): fresh `git status --porcelain` per call; frontend `fileStore.loadChanges` polls every 2.5 s (FileBrowser.tsx:38-45) and re-fetches after save (fileStore.ts:205).
  - GUI root resolution `get_workspace_root` (files.rs:451-457) reads `state.app_state.current_workspace()` at call time → follows workspace switches; falls back to process CWD otherwise.
  - REPL `/diff` (diff_cmd.rs) uses `ctx.coding_loop.project_root` (captured once at session boot, repl.rs:183-189) — never rebound; `/diff` git modes run `git diff` live; file modes read both files live.
  - REPL `/test`/`/fix` (coding.rs:298-326, 554-634) run the project-type test command live (`run_test_command`, test_runner.rs:27-54, `sh -c`). `/code-review` runs `git diff HEAD` live; the fallback `diff_summary()` (coding.rs:358-360) is dead-output because `FileChangeTracker` has no writers (V01-01).
  - TUI `@`-completion: `project_files` assigned exactly once at boot (mod.rs:1966 `collect_project_files(Path::new("."), 10_000)`); sole consumer `complete_file_reference` (events.rs:1632-1662) on Tab (events.rs:1287). Never invalidated or rebuilt during the session.
  - Framework `diff`/`repo_map` tools registered (registry.rs:38/236, 31/222) and reachable to EKO agents via `register_all_tools` (infra.rs:901 comment); `git_diff`/`git_status`/`git_log`/`git_blame` registered (registry.rs:263-271).
  - Project-context projection: refreshed at agent creation (infra.rs:444), on workspace switch (state.rs:883, with the new workspace root), cleared on exit (state.rs:1072); rebuilt fresh from disk each time (`load_project_context` → fresh tree walk + live `git status --short`/`git diff --stat`, context.rs:91-122, prompt.rs:352-367).
  - `ProjectIndex`: zero production call sites (V01-01) — never built, never registered, never persisted.
  - `WorkspaceRegistry`: constructed once in `AppState` (state.rs:577-587); `detect_from_cwd`/`detect_from_manifest` zero callers.
- Log/artifact: none.

## Deviations

- `git show` failure in `diff_file` (untracked files, deleted-from-HEAD edge, non-UTF-8 ref) degrades to `old_content=""` → full-file "added" diff instead of an explicit error; the binary-file case was hardened (files.rs:284-290) but the git-failure case was not.
- REPL and TUI capture their project root / file index once at boot; within a session there is no refresh path (REPL has no workspace switch per A-CFG-01-P1-03; TUI has no workspace surface).

## Conclusion

Supports: all live diff/index surfaces except TUI `project_files` read current files at call time; the project-context projection follows the workspace switch correctly (no stale project context after switch/exit — the A-CFG-01 CWD issues remain, cross-referenced). Does not prove: dynamic behavior of the 2.5 s poll and switch flows (static only).

## Follow-Up

None; feeds findings P2-02, P2-03, P2-04 and task-report Current Path.
