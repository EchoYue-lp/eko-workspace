# A-PROJ-01 / V05 / Attempt 01

> Status: passed (drift classified)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical claims about project indexing, diff, coding mode, and workspace layout either match current code or are classified current/fixed/stale/regressed with anchors.

## Method

Grep + targeted reads of the EKO docs and module docs against the reviewed code:

```text
grep -n "Coding\|diff\|index\|索引" echo-agent-cli/docs/MASTER-PLAN.md
sed -n '108,135p' echo-agent-cli/docs/MASTER-PLAN.md
grep -n "文件引用\|项目文件索引" echo-agent-cli/docs/2026-07-11-tui-parity-design.md
grep -n "diff.rs\|project/prompt" echo-agent-cli/docs/2026-07-28-app-core-full-audit.md
grep -rn "SQLite" echo-agent-cli/echo-agent-app-core/src/workspace/mod.rs
# module docs treated as hypotheses:
sed -n '1,8p' echo-agent-cli/echo-agent-app-core/src/project/index.rs   # "Built once at startup and refreshed on demand ... ~/.eko/cache"
```

## Expected Result

Every claim maps to a live code site or is explicitly stale/regressed with a path anchor.

## Result

- Exit code: not applicable (static grep)
- Summary of classifications (details in the task report's Historical Claim Status table):
  - `MASTER-PLAN.md:112-120` (Coding section): LSP discovery + Tree-sitter repo map — **current** (framework `repo_map` uses tree-sitter, `echo-tools/src/files/repo_map.rs:1-25`; registered registry.rs:31/222). No claims about an EKO `ProjectIndex` — the dead index is undocumented.
  - `docs/2026-07-11-tui-parity-design.md:50` — "`@query` + Tab 在项目文件索引中补全 | 完成" — **current but incomplete-by-design**: the completion exists (`tui/events.rs:1287,1632-1662`) yet its index is a boot snapshot that never refreshes (tui/mod.rs:1966) — finding P2-02.
  - `docs/2026-07-28-app-core-full-audit.md:297` — "`output/`, `diff.rs`, `context_window.rs` — zero framework dependency" — **current** (app-core `diff.rs` uses the `similar` crate directly, no `echo-agent` imports).
  - `echo-agent-app-core/src/workspace/mod.rs:15` — "`tasks/` # 后台任务 SQLite DB" — **stale** (EKO has no SQLite; A-CFG-01-P2-02/P3-01 already cover the task-store side; cross-referenced, not duplicated).
  - `echo-agent-app-core/src/project/index.rs:44-46` — module doc "Built once at startup and refreshed on demand. Can be serialized to `~/.eko/cache/{project_hash}.json`" — **regressed/stale doc**: no startup build, no refresh path, no persistence exists; the type has zero production callers (V01-01) — finding P2-01.
  - `echo-agent-cli/docs/MASTER-PLAN.md:43` — "LSP automatic discovery and AST-aware repo map | Complete" — **current** (repo_map registered and reachable via `register_all_tools`).
- Log/artifact: none.

## Deviations

None.

## Conclusion

Supports: MASTER-PLAN coding claims are current; two doc sites drift (index.rs self-doc, workspace/mod.rs SQLite wording); the tui-parity "完成" claim hides the boot-snapshot design. No historical document describes a live project-index cache that could silently go stale — the only stale-cache risk is the undocumented TUI snapshot (P2-02).

## Follow-Up

None; feeds P2-01, P2-02 and the task report's Historical Claim Status table.
