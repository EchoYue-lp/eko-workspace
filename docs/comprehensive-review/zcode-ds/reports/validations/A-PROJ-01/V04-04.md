# A-PROJ-01 / V04 / Attempt 04

> Status: passed (both replicas behaved as predicted)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories; replicas live in /tmp only)

## Claim

(a) `GitIgnore::globstar_match` (gitignore.rs:159-192) panics on a UTF-8 (multibyte) path via `&remaining[j..]` byte slicing, and `src/**/*.rs`-style patterns do not match real files even on ASCII paths. (b) GUI `diff_file` (files.rs:322-372) emits a single hunk containing every line of the file with hunk headers counting only changed lines.

## Method

Two scratch replicas of the exact reviewed algorithms (verbatim copies) run outside the repositories:

```text
/tmp/aproj-check/globstar.rs   # simple_glob + globstar_match copied verbatim from gitignore.rs:125-192
/tmp/aproj-check/diffgui.rs    # hunk-assembly loop copied verbatim from files.rs:322-372, using similar 2.x
cd /tmp/aproj-check && rustc -O globstar.rs -o globstar && ./globstar
cargo run -q   # diffgui (cargo init'd with `similar = "2"`)
```

Fixture (a): pattern `src/**/*.rs` against `src/main.rs` (ASCII) and `src/中文.rs` (UTF-8). Fixture (b): 10-line old content with one replaced line.

## Expected Result

(a) ASCII: `false` (semantic bug — git treats `**` as zero-or-more dirs); UTF-8: panic "byte index ... is not a char boundary". (b) One hunk with header `@@ -1,1 +1,1 @@` containing all 11 lines.

## Result

- Exit code: (a) ASCII path `0`, UTF-8 path `101` (panic); (b) `0`
- Summary:
  - (a) `src/**/*.rs vs src/main.rs (ASCII): false` — documented `**` matching does not match real files (matches F-EXT-02-P2-01's doublestar defect class in the EKO gitignore copy).
  - (a) `src/**/*.rs vs src/中文.rs (UTF-8):` → `thread 'main' (13626467) panicked at globstar.rs:42:43: start byte index 5 is not a char boundary; it is inside '中' (bytes 4..7 of string)` — byte-slice panic proven.
  - (b) `hunks: 1` and `hunk @@ -1,1 +1,1 @@ lines_in_hunk: 11` — single giant hunk; header counts (1,1) reflect only the changed lines, not the 11 lines actually included.
- Log/artifact: `/tmp/aproj-check/globstar.rs`, `/tmp/aproj-check/diffgui.rs`, `/tmp/aproj-check/globstar` (binary).

## Deviations

None. Note: `globstar_match` is currently unreachable in production (zero callers of `is_ignored`), so the panic is latent — reachability is recorded in V01-01/V03-01, not claimed here.

## Conclusion

Supports: P3-01 (UTF-8 panic + broken `**` semantics in `GitIgnore`, latent) and the hunk-structure/header-count arm of P2-03 (GUI diff). Does not prove: the same defects end-to-end through the compiled CLI/GUI (no process launch in this read-only review).

## Follow-Up

None; Q-STA-01/Q-FLT-02 candidate fixtures.
