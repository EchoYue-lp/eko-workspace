# A-PROJ-01 / V01 / Attempt 01

> Status: passed (duplicate authorities found and classified)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

There is exactly one live project-index authority and one live file-diff authority per product surface in `echo-agent-cli`, with no second live implementation of the same semantics; any parallel definition is classified as duplicate, dead, or distinct-purpose.

## Method

Repository-wide grep over both repositories (excluding `target/`) for concept names and behavior names:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
grep -rn "ProjectIndex" echo-agent echo-agent-cli --include="*.rs"
grep -rln "TextDiff\|udiff" echo-agent echo-agent-cli --include="*.rs"
grep -rn "generate_unified_diff\|struct FileDiff\|struct DiffHunk\|struct DiffLine" echo-agent echo-agent-cli --include="*.rs"
grep -rn "collect_project_files\|project_files" echo-agent-cli/src --include="*.rs"
grep -rn "record_file_write\|record_file_delete\|clear_changes\|with_type\|FileChangeTracker" echo-agent-cli echo-agent --include="*.rs"
grep -rn "should_ignore_path\|GitIgnore\|gitignore" echo-agent-cli --include="*.rs"
grep -rn "run_lint_command\|format_failures_as_prompt\|status_summary" echo-agent-cli --include="*.rs"
grep -rn "DiffViewer" echo-agent-cli/web-frontend/src --include="*.tsx"
grep -rn "WorkspaceRegistry\|detect_from_cwd\|detect_from_manifest" echo-agent echo-agent-cli --include="*.rs"
```

## Expected Result

One index authority and one diff authority per surface; any duplicate is either a distinct-purpose framework tool or dead code.

## Result

- Exit code: not applicable (static grep)
- Summary:
  - **Project index**: `echo-agent-cli/echo-agent-app-core/src/project/index.rs` (`ProjectIndex`) has zero production constructors or consumers — the only hits outside the file itself are its own `#[cfg(test)]` tests (index.rs:459, :482). No code anywhere writes or reads `~/.eko/cache/` or calls `ProjectIndex::load/save/build`. It is dead code whose module doc claims startup build, on-demand refresh, and cache persistence.
  - **Second index implementation**: `echo-agent-cli/src/tui/mod.rs:1675` `collect_project_files` (live, boot-only snapshot for `@`-completion, consumed at `tui/events.rs:1632-1662`) is a separate project-file index distinct from `ProjectIndex`. Framework `repo_map` (`echo-tools/src/files/repo_map.rs`, registered `registry.rs:31/222`) is a third, model-facing, distinct-purpose index (Tree-sitter symbol map).
  - **Diff implementations (3 live)**: (1) framework `DiffTool` (`echo-tools/src/files/diff.rs`, registered `registry.rs:38/236`); (2) EKO app-core `diff.rs` (`generate_unified_diff`/`parse_unified_diff`/`render_*`), sole consumer `src/cli/cmd_impls/diff_cmd.rs:14-16` (REPL `/diff`); (3) inline algorithm in `src/tauri/commands/files.rs:310-372` (`diff_file`, GUI file browser), with its own `DiffHunk`/`DiffLine` types (files.rs:40-63) duplicating app-core `DiffHunk`/`DiffLine`/`DiffStats` (diff.rs:14-61). `TextDiff` used in 4 files across both repos (edit.rs, files/diff.rs, app-core diff.rs, tauri files.rs).
  - **DiffViewer twins**: `web-frontend/src/components/coding/DiffViewer.tsx` (string-based parser, zero production usages) vs `web-frontend/src/components/file-browser/DiffViewer.tsx` (hunks-based, live in FileBrowser.tsx:242).
  - **Change tracker**: `FileChangeTracker`/`CodingLoop::record_file_write/record_file_delete/clear_changes/change_count/status_summary/with_type` (coding_loop.rs:28-85, file_tracker.rs) — zero production callers; only `diff_summary()` is called (coding.rs:359) and always returns "No file changes" because no writer exists.
  - **test_runner**: `run_test_command` live (coding.rs:309); `run_lint_command` and `format_failures_as_prompt` zero callers.
  - **GitIgnore**: `is_ignored`/`should_ignore_path` have zero production callers (only `GitIgnore::load` is reached via `load_project_context`).
  - **Workspace registry**: single `WorkspaceRegistry` (`workspace/registry.rs`), consumed by `state.rs:413/577`; `detect_from_cwd`/`detect_from_manifest` have zero production callers.
- Log/artifact: none (grep output inline above).

## Deviations

- Three diff authorities (framework DiffTool / app-core diff.rs / Tauri diff_file) with divergent semantics — the claimed single authority does not hold (finding A-PROJ-01-P2-03).
- Two project-file index implementations in EKO (dead `ProjectIndex` + boot-snapshot `collect_project_files`) — findings A-PROJ-01-P2-01/P2-02.
- `FileChangeTracker` is write-only (finding A-PROJ-01-P2-04); `GitIgnore.is_ignored` unreachable (finding A-PROJ-01-P3-01).

## Conclusion

Supports: no second LIVE worktree/file authority in the project/diff path (the worktree authority is A-TSK-05's single `task_runtime/worktree.rs`; the GUI file panel reads git state live); the duplicates found are: one dead index, one stale-by-design live index, three divergent diff implementations, and a write-only change tracker. Does not prove: whether the GUI diff divergence is user-visible (checked in V03-01 via replica).

## Follow-Up

None; feeds task report layering decision and findings P2-01..P2-04, P3-01.
