# F-CORE-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-CORE-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The echo_core event-envelope test suite (serialization round-trip,
sequence/correlation, terminal normalization, resume-with-stable-ids,
trajectory validation) passes at the reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_core --lib --locked agent::event
```

Environment: default features (`--locked`), local toolchain per
`rust-toolchain.toml`, no network. Expected: 6 tests pass.

## Expected Result

6 passed; 0 failed.

## Result

- Exit code: 0
- Duration: ~3m05s compile (included a build-directory file-lock wait);
  test execution 0.00s
- Summary: 6 passed; 0 failed; 0 ignored; 282 filtered out. Tests:
  `serializes_versioned_contract`,
  `sequences_events_and_correlates_tool_lifecycle`,
  `resumes_after_persisted_sequence_with_stable_ids`,
  `normalizes_missing_and_duplicate_terminals`,
  `validates_canonical_success_cancel_error_and_hitl_budget_prefixes`,
  `trajectory_validator_reports_sequence_identity_parent_and_terminal_drift`.
- Log/artifact: none (captured to stdout).

## Deviations

None.

## Conclusion

Supports: serialization round-trip (schema_version + tagged payload),
sequence monotonicity, tool correlation, missing/duplicate terminal
normalization, deterministic event_id after resume, and the trajectory
validator's detection of schema/sequence/identity/parent/terminal drift —
all green at `9b0e0fa`.

Does not prove: all-feature compilation of echo_core (Q-FW-01), or
behavior under concurrent producers (out of scope; envelope streams are
single-producer by design).

## Follow-Up

None.
