# X-TOL-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories (verified `git status --porcelain` empty)

## Claim

Every already-filed finding that touches the four-layer tool conformance
(schema, classification, output integrity, artifact metadata, terminal
reason) is cross-referenced here with its canonical ID and its consequence at
each of the four layers, so the phase synthesizer can merge without re-deriving
and X-TOL-01 does not duplicate existing findings.

## Method

For each dependency report finding, determine: (a) canonical ID and layer;
(b) the layer(s) of the tool pipeline it affects; (c) whether X-TOL-01 adds
any new evidence; (d) classification (current / confirmed / not revalidated).
Also check historical-document claims used by this task:
`echo-agent-cli/docs/MASTER-PLAN.md` and root `docs/MASTER-PLAN.md` tool
terminal/artifact claims referenced by the dependency reports.

Dependency reports re-read for this task (all zcode-ds, complete):
F-RCT-04, F-EXT-01, F-EXT-02, F-EXT-03, A-TOOL-01, A-FE-02 (plus the
cross-referenced canonical IDs A-SRF-02-P2-01, A-SRF-03-P2-01, F-REL-01,
A-TSK-06-P2-01 as cited therein).

## Expected Result

- A matrix that maps each canonical finding to the tool-conformance
  dimension(s) it governs, with the layer consequence and any X-TOL-01
  delta (new evidence or none).
- Historical claims about tool terminal states and artifacts classified
  current / fixed / stale / regressed.

## Result

- Exit code: not-applicable (static cross-reference)
- Duration: not-applicable
- Summary: Consistency matrix (dimensions: S = schema/exposure, C =
  classification/terminal reason, O = output integrity/artifact metadata,
  I = invocation identity):

| Canonical ID | Priority | Dimension | Layer consequence at each layer | X-TOL-01 delta |
|---|---|---|---|---|
| F-RCT-04-P1-01 | P1 | O/C | Framework: results inserted in completion order (phases/tools.rs:207-240) vs call order (processor.rs:141-147); provider rejects the next request 400 after tools ran. EKO: tools recorded Succeeded; the turn then fails with no tool-level failure. GUI: success cards + failed turn. | Confirmed (V01-01 trace); no new evidence |
| F-RCT-04-P1-02 | P1 | C | Framework: batch timeout/cancel end turn with no typed terminal (tools.rs:284-300); trace run stays Running. EKO: Chat mode receives only `AgentEvent::Error` and compensates by cancelling in-flight tools (chat.rs:1325-1328); Task mode compensates the run status (chat_driver.rs:355-383). GUI: tools show "cancelled". | Extended: EKO-side collapse is the new finding X-TOL-01-P2-01 (cancel() has no failure parameter) |
| F-RCT-04-P2-02 | P1 | C/O | Framework: batch-killed tools produce no failure record and the sanitize placeholder blames compression (compression/mod.rs:1562-1718). EKO: no partial-side-effect warning anywhere. | Same extension; see X-TOL-01-P2-01 |
| F-RCT-04-P2-01 | P2 | all (test) | No concurrent batch fixture anywhere; `then_tool_calls` zero usages; the task's required fixtures do not exist. | Confirmed by V04-01; fixture gaps also at EKO/frontend layers (no failure-category rendering fixture) |
| F-EXT-01-P1-01 (= A-TOOL-01-P1-01, canonical framework side) | P1 | S | Framework plan-mode filter hides write tools (snapshot.rs:227-236,282-285; pipeline.rs:1004-1018); EKO writer subagent builder sets plan_mode=true (infra.rs:963). A write tool invocation cannot even start for Implementation/Debugging writer tasks — schema/exposure conformance broken at the top. | Confirmed (V01-01 exposure trace); no new evidence |
| F-EXT-01-P2-01 | P2 | S | WRITE_TOOLS static list drifted (src/tools/mod.rs:118-139): run_code/git writes/exports absent; plan-mode and read-before-edit classification inconsistent with the registry. | Confirmed; no new evidence |
| F-EXT-02-P1-01 | P1 | C/I (invalid input) | `edit_file` empty `old_content` panics (files/edit.rs:260-270) — an invalid argument produces a process abort instead of an `InvalidArguments` failure; no failure record reaches any layer; run task dies. | Cross-layer consequence: EKO projection leaves the tool Running until turn-end cleanup marks Cancelled — no InvalidArguments anywhere (see V04-01 gap) |
| F-EXT-02-P2-02 | P2 | C (false success) | `exit_worktree`/`remove_worktree` swallow errors and return Ok(()) (git_worktree.rs:130-146; worktree_tool.rs:195-210) — a false success crosses all four layers unchanged (EKO Succeeded, GUI check). | Confirmed; false terminal reason propagates unmodified |
| F-EXT-02-P2-03 | P2 | O (side effect) | `enter_worktree` path_suffix escapes `.worktrees/` (git_worktree.rs:44-51) — out-of-scope writes with no partial-side-effect marking. | Recorded as partial-side-effect scenario; no new evidence |
| F-EXT-03-P1-01 | P1 | C/O | research_remember/recall stubs fabricate success and discard data (research/memory.rs:96-121,159-180) — fabricated success at every layer. | Confirmed; no new evidence |
| F-EXT-03-P1-03 | P1 | C/I (invalid input) | IQR panic on exactly-4-value columns (data_quality.rs:249-255) — same invalid-input panic class as F-EXT-02-P1-01; no failure record reaches any layer. | Confirmed; same cross-layer consequence |
| F-EXT-03-P2-02 | P2 | S/C | Schema enums not enforced (data.rs:3221,3240-3243,3406-3408; data_quality.rs:162,177-180; chart.rs:35-39,69-73): unknown values silently fall back while the result echoes the REQUESTED value — schema (layer 1) and result label disagree, and the false label flows unchanged through EKO and UI. | Confirmed as the schema-honesty defect of this task's question; no new evidence |
| A-TOOL-01-P2-01 | P2 | S | GUI enable/disable toggle is cosmetic (state.rs:27,348; commands/tools.rs:33-65) — "disabled" tools remain callable; exposure conformance broken at the GUI surface. | Confirmed; no new evidence |
| A-SRF-02-P2-01 (canonical, cross-ref A-FE-02-P2-01) | P2 | I | Two producers (bridge mod.rs:353-768 + projector chat.rs:957-1114) allocate two detail_refs for one logical call; live ingest keys by detail_ref (toolExecutionStore.ts:206-217) — one invocation renders as two tool cards with inflated counts. | Confirmed (V01-01 deviation note); identity divergence of the task's question |
| A-FE-02-P2-02 | P2 | I | `latestSubagentRunsByTask` ignores plan_revision (subagentRunStore.ts:407-441) — old-revision attempt rendered as current after plan revision. | Not tool-level; recorded for completeness |
| A-TSK-06-P2-01 | P2 | O | Run-level artifact projection has zero writers — GUI artifact list always empty. | Recorded; no new evidence |

Historical claims used by this task (from dependency reports, re-checked
against current code at the cited anchors):

| Source claim | Classification | Current evidence |
|---|---|---|
| `echo-agent-cli/docs/MASTER-PLAN.md:98` — tools have explicit success/failure/cancelled terminal states | regressed for kill paths | batch/subagent kill paths end as bare Cancelled with no failure (tool_execution.rs:360-392; chat.rs:1106-1110,1325-1328); X-TOL-01-P2-01 |
| Root `docs/MASTER-PLAN.md:115/:275/:602` — oversized results as artifact + bounded preview, shared path/hash/retention | current | artifact.rs writer + extend_metadata + EKO manifest preservation (V03-01); output_bytes underreporting is a reporting defect, not an integrity break (X-TOL-01-P3-01) |
| `echo-agent-cli/docs/MASTER-PLAN.md:196-203` — frontend keeps terminal states monotonic | regressed only on the live ingest path | A-FE-02-P2-01 (canonical) |
| `docs/MASTER-PLAN.md:472` (M4) — unified failure classification with partial-side-effect category | current at manager level / bypassed on kill paths | F-RCT-04-P2-02 + X-TOL-01-P2-01 |

## Deviations

- F-RCT-04-P1-01's provider-rejection consequence at the EKO layer
  (successful tools then a failed turn) was confirmed statically only; the
  live provider behavior was not re-run (read-only review, provider keys
  unavailable).
- A-FE-02-P2-02, A-TSK-06-P2-01, and A-TOOL-01-P2-01 were not re-audited in
  depth (their reports are complete); they are listed only for the matrix.

## Conclusion

This execution supports: the cross-layer consistency matrix is complete for
the six dependency reports plus their cited canonical IDs; every dimension of
the X-TOL-01 question (schema, classification, output integrity, artifact
metadata, terminal reason) is governed by at least one canonical finding; the
only new evidence X-TOL-01 adds is the EKO-boundary taxonomy collapse
(X-TOL-01-P2-01) and the two projection defects (X-TOL-01-P3-01, P3-02).

## Follow-Up

The phase synthesizer should keep the canonical IDs listed above and merge
X-TOL-01-P2-01 with F-RCT-04-P1-02/P2-02 under one fix direction that covers
both the framework terminal and the EKO cancel() signature.
