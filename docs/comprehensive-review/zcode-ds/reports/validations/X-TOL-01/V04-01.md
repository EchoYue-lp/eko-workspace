# X-TOL-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean before and after execution (verified `git status
> --porcelain` empty; no command that regenerates
> `web-frontend/src/generated/*.ts` was executed)

## Claim

The required fixture families — invalid arguments, timeout, cancellation,
and partial side effects — exist somewhere in the four layers with enough
coverage to protect the cross-layer conformance claims of X-TOL-01, and the
tool-projection test suites are green at the reviewed commits.

## Method

Inventory of existing fixtures per layer (grep + targeted read), then one
executed command:

1. Framework fixtures: `src/agent/react/run/stream_channel.rs`
   (`test_run_stream_cancelled_mid_llm_call :1972`,
   `cancellation_drains_running_tool_before_abandoning_turn :2041` — single
   tool); `echo-execution/src/tools.rs` (`internally_timed_tool_bypasses_ordinary_
   execution_timeout :1259`, `internally_timed_tool_stream_bypasses_ordinary_
   execution_timeout :1281`); `echo-core/src/tools/mod.rs` tests
   (`ToolFailure` classification incl. PartialSideEffect assertions
   `:1204-1209`); grep for `PartialSideEffect` producers: only
   `echo-tools/src/files/files.rs:97` and the `from_error` conversion
   (`echo-core/src/tools/mod.rs:174`).
2. EKO fixtures: `echo-agent-cli/echo-agent-app-core/src/tool_execution.rs`
   tests (`preview_is_utf8_safe :843`, `complete_output_is_read_in_pages
   :851`, `malformed_final_journal_line_is_removed :889`,
   `removing_conversation_drops_summary_and_detail_indexes :926`,
   `artifact_pages_end_on_utf8_boundaries :956`);
   `src/tauri/commands/chat.rs` `task_runtime_tools_are_persisted_with_output_
   and_terminal_cleanup :1607` (start -> output -> complete with metadata;
   running tool -> Cancelled on subagent terminal).
3. Frontend fixtures: `web-frontend/src/stores/toolExecutionStore.test.ts`
   (9 tests: TaskRuntime recovery, terminal monotonicity, identity merging,
   stale-vs-live status, conversation isolation); `web-frontend/src/components/
   chat/InlineToolCall.test.tsx` (3 tests); `web-frontend/src/stores/
   chatStore.toolExecution.test.ts` (duplicate-start dedupe). Grep for
   `failure|partial_side_effect|timeout` in these tests: no failure-category
   rendering fixture exists.
4. TUI fixtures: `src/tui/events.rs` tests (`file_tools_render_compact_titles
   :426`, `read_detail_uses_requested_line_range :441`,
   `failed_read_keeps_error_tail :452`, `missing_artifact_is_visible_without_
   marking_tool_failed :466`, `mcp_detail_uses_framework_result_metadata :532`,
   `log_channel_remains_distinct_and_visible :546`, cancel-on-Cancelled
   handling `:661-675`).
5. Executed command (frontend tool-projection suites):

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli/web-frontend
npx vitest run src/stores/toolExecutionStore.test.ts src/components/chat/InlineToolCall.test.tsx
```

## Expected Result

- At least one fixture per family (invalid / timeout / cancel / partial side
  effect) per layer where the layer implements the semantics.
- The two frontend suites pass (exit 0).

## Result

- Exit code: 0
- Duration: 370 ms (12 tests)
- Summary:
  - Executed: `vitest run` on toolExecutionStore + InlineToolCall -> 2 files,
    12 tests passed.
  - Framework layer: cancel fixtures exist (single-tool drain, mid-LLM
    cancel); timeout fixtures exist at the manager level (internally-timed
    bypass tests) and per-tool timeout is exercised in echo-execution tests
    (recorded green in F-RCT-04 V04-02); NO end-to-end batch-timeout fixture,
    NO concurrent batch fixture, NO partial-side-effect end-to-end fixture,
    NO oversized-result-in-batch fixture — the canonical gap F-RCT-04-P2-01
    (zero usages of `MockLlmClient::then_tool_calls`).
  - EKO layer: cancel fixture exists (projector terminal cleanup -> Cancelled,
    chat.rs:1607); paging/UTF-8/cursor fixtures exist (tool_execution.rs
    tests); NO timeout fixture, NO invalid-arguments fixture, NO
    partial-side-effect fixture, NO failure-category fixture at the EKO
    projection layer (grep of the test modules for `timeout`,
    `PartialSideEffect`, `InvalidParameter`: zero hits outside the
    framework crate).
  - Frontend layer: status/cancel/monotonicity/identity fixtures exist in
    toolExecutionStore.test.ts (including two 'cancelled' rows, `:96,:131`);
    NO fixture renders or asserts `failure.category` (no
    `partial_side_effect` / `timeout` in frontend tests) and no
    `output_bytes`/artifact-metadata rendering fixture — the P3-01/P3-02
    projection defects are therefore unguarded.
  - TUI layer: error-tail fixture, artifact-missing fixture, metadata
    fixtures exist; no failure-category mapping fixture was found in the
    events tests (the mapping code at events.rs:726-740 has no direct test).

## Deviations

- Fixture coverage is uneven across the four layers: cancel is covered
  everywhere; timeout is covered only at the framework manager level and
  partially at the EKO projection (subagent terminal -> Cancelled); invalid
  arguments and partial side effects have no cross-layer fixture that
  exercises the taxonomy from classification to rendering. This is the
  direct consequence of the canonical F-RCT-04-P2-01 fixture gap plus the
  absence of EKO/frontend fixtures for the classification path.
- No framework cargo tests were re-executed in this validation; their green
  state at these commits is recorded in the dependency reports (F-RCT-04
  V04-01/02, F-EXT-01 V04-01/02, A-TOOL-01 V04-01..04).

## Conclusion

This execution supports: cancel fixtures exist at all four layers; timeout
fixtures exist at the framework manager level only; invalid-arguments and
partial-side-effect families have no cross-layer fixture; the two frontend
tool-projection suites are green at the reviewed commits (exit 0, 12
passed). The fixture gaps leave X-TOL-01-P3-01/P3-02 and the taxonomy
collapse (X-TOL-01-P2-01) unguarded by tests.

## Follow-Up

Q-FLT-01 / Q-TST-01 should build the missing fixture families from
F-RCT-04-P2-01's list extended with: a framework->EKO->GUI end-to-end
failure-category fixture, an artifact-spill output_bytes fixture
(X-TOL-01-P3-01 regression), and a streamed-failure error-text fixture
(X-TOL-01-P3-02 regression).
