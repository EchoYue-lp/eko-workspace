# A-TSK-04 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical documents' claims about claims/recovery/event replay are
classifiable as current, fixed, stale, or regressed against current code.

## Method

Read the claims-bearing sections of
`echo-agent-cli/docs/MASTER-PLAN.md` (:120-235) and
`echo-agent-cli/docs/2026-07-27-runtime-dag-kernel-convergence.md`
(:140-230), and anchor each claim to current source:

```text
1. MASTER-PLAN:176-182 (events.jsonl recovery authority; safe-point reload;
   completed attempts never restarted implicitly)
2. MASTER-PLAN:196-215 (execution identity {run}:{task}:{revision}:{attempt};
   frontend terminal monotonicity; run-state.json authoritative for Todo reads;
   restart recovery receives identical evidence)
3. MASTER-PLAN:229-235 (atomic TaskClaim; claim conflict reloads; writes
   accepted only for the still-running claim)
4. convergence:150-166 (same claims + durable result cannot reuse older
   TaskSpec output)
5. file_shadow.rs:114-117 comment (torn tail tolerated/truncated by a
   future hardening pass)
```

## Expected Result

Each claim maps to a live anchor; deviations are classified.

## Result

- Exit code: not-applicable (document drift inspection)
- Duration: ~20 min
- Summary of classifications:
  | Claim | Classification | Anchor |
  |---|---|---|
  | events.jsonl recovery authority; plan.json immutable spec; run-state.json execution projection | current | file_shadow.rs:3-7,208-280; file_store.rs:37-55; V01/V02 |
  | Safe-point revision reload; completed attempts never restarted implicitly | current | runtime_executor.rs:211-230 (active_revision), ready frontier runtime.rs:438-458; drain gate executor.rs:366-434 |
  | Execution identity `{run}:{task}:{plan_revision}:{attempt}`; durable-result lookup cannot reuse older-TaskSpec output | current | runtime.rs:221-223; store.rs:1665-1674; test store.rs:3148 |
  | run-state.json authoritative for Todo status; older Task events only restore metadata | current | file_store.rs:190-199 |
  | Atomic TaskClaim; conflict -> ReloadSnapshot (never task failure); completion/failure/retry writes claim-guarded | current | store.rs:986-1121; runtime_executor.rs:352-366,422-426; tests store.rs:3060/3100 |
  | "Every completion, failure, **block**, and retry write carries the claim" (convergence:153) | **stale/regressed** | `block_task` writes via the unguarded `set_task_status` (executor.rs:1564-1580, store.rs:953-983) — no claim carried; see P2-01 |
  | file_shadow comment: torn partial tail tolerated, truncation in a future hardening pass | regressed (unimplemented) | file_shadow.rs:362-379 hard-errors; no truncation; see P1-03 (matches A-TSK-01 P1-01) |
  | Boot recovery resets interrupted runs with identical evidence | current | store.rs:1631-1776; tests store.rs:2677/2721/2738 |
  | Pause keeps the run resumable (store.rs:595-597 doc comment) | regressed on the in-wave path | store.rs:598-622 + runtime_executor.rs:390-416 + executor.rs:643-661; see P1-01 |

## Deviations

Two claims are regressed (block-write claim-guard wording; torn-tail
tolerance) and one documented contract (pause resumability) is violated on
the common path — all three became findings with their own evidence.

## Conclusion

Supports: the doc-to-code drift is limited to the block-write wording, the
torn-tail promise, and the pause contract; the core claim protocol
documentation is current.

## Follow-Up

None.
