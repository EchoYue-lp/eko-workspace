# A-TSK-04 / V03 / Attempt 01

> Status: failed (5 findings)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Stale revisions/attempts, cancellation, restart, and event replay update state
only through valid claims, and no path regresses a run or task from a
resumable state to a terminal state (or from terminal to non-terminal).

## Method

Static invariant inspection with exact source anchors, plus cross-check of the
three dependency findings the task card asks to re-verify independently
(A-TSK-03-P1-01 pause-in-wave, A-TSK-03-P2-01 mid-wave store fault,
A-TSK-01-P1-01 torn events.jsonl tail):

```text
1. Claim-guarded writes: store.rs claim_task(:986-1029) / set_claimed_task_status
   (:1032-1062) / requeue_claimed_task (:1066-1105) / task_claim_is_current (:1107-1121);
   framework reload-on-conflict runtime_executor.rs:352-366; resolve_dispatch
   executor.rs:1348-1562 (Superseded on guard failure)
2. Unguarded set_task_status surface: store.rs:953-983; callers executor.rs:551
   (pause reset), :651 (cancel finalize), :1571 (block_task), store.rs:1706
   (recover_incomplete), :2163/:2170 (resolve_recovery_task); grep for any other
   production callers
3. Terminal monotonicity: types.rs:517-527 TaskRunStatus::can_transition_to;
   transition_run store.rs:387-428 as the only run-status write path; TodoStatus
   has no transition table — guard is claim-based for executor writes
4. Crash/restart: recover_incomplete store.rs:1631-1776 (Running -> Paused;
   Running tasks -> Pending/Blocked + RecoveryBlocked for non-replay-safe
   boundaries; durable Subagent result reuse)
5. Cancel/retry: request_cancel/request_pause store.rs:577-622; in-wave cancel
   runtime_executor.rs:390-416; finalize_cancelled_run_state executor.rs:643-661;
   requeue budget executor.rs:1396-1431; retry_blocked_task store.rs:1220-1340
6. Event replay ordering: event_rebuild.rs fold in seq order; file_shadow.rs
   read_events (file order = seq order under per-run lock); file_store.rs:212-222
   list_events seq delta; seq uniqueness test file_shadow.rs:1035-1089
7. Cross-checks: framework interruption_outcome consult sites runtime_executor.rs:
   207-209/:267-269/:278-281 (never in-wave); pause test coverage executor.rs:
   5757-5783 (loop-top only); torn-tail read path file_shadow.rs:362-379 and
   seq seed :289-299; in-process recovery surface (retry_blocked_task Running
   rejection store.rs:1244-1251; TaskUpdateOperation ops types.rs:1257-1272;
   EkoTaskToolPolicy allow_manual_progress_updates absent revisioned_adapter.rs:106)
```

## Expected Result

All executor state writes claim-guarded; run transitions legal per the state
machine; pause durable; restart heals; replay deterministic in seq order.

## Result

- Exit code: not-applicable (static inspection)
- Duration: ~45 min
- Summary (verified invariants that HOLD):
  - Executor terminal writes are claim-guarded; `Superseded` is honored by
    the framework (resolve_dispatch returns Superseded -> wave continues,
    runtime_executor.rs:422-426). Tests pin: `claim_reloads_when_task_update_wins_revision_race`
    (store.rs:3060), `stale_claim_cannot_overwrite_cancelled_task` (store.rs:3100),
    `patched_spec_uses_new_execution_identity_without_retry_bump` (store.rs:3148).
  - Replay is deterministic: per-run write lock serializes appends
    (file_shadow.rs:132-133), seq strictly increasing and contiguous (test
    file_shadow.rs:1035-1089), fold applies later events last, projections
    rebuilt from the full stream at every affecting write (file_shadow.rs:208-280);
    run-state.json is authoritative for Todo status so older events cannot
    override a later skip/reset (file_store.rs:190-199).
  - Run-terminal monotonicity enforced by `can_transition_to`
    (types.rs:517-527): Cancelled/Completed have no outgoing transitions;
    every run-status write goes through `transition_run`.
  - Boot recovery is the sole orphaned-claim healer and it is claim-aware
    (execution_id from the rebuilt claim, store.rs:1665-1674), resets Running
    tasks to Pending (replay-safe) or Blocked + RecoveryBlocked
    (non-replay-safe boundary), preserves Completed tasks (test store.rs:2677).
  - Stale revision at the tool boundary is rejected:
    `task_execute` requires the exact latest committed revision
    (task_execute_tool.rs:277-282); `task_update` CAS on revision
    (store.rs:786-792); terminal runs reject plan modification
    (store.rs:764-772).
- Findings (invariants VIOLATED or gap), each with evidence in the task report:
  1. P1-01 pause during an active wave -> durable Paused run force-transitioned
     to terminal Cancelled; tasks written Cancelled; resume impossible
     (independent re-verification of A-TSK-03-P1-01).
  2. P1-02 mid-wave store fault aborts the wave, siblings keep durable
     Running claims; no in-process recovery surface; same-process resume
     hangs in external polling; only boot recovery heals
     (re-verification of A-TSK-03-P2-01).
  3. P1-03 torn final line in events.jsonl bricks both reads and writes for
     that run, so replay/recovery never begin (re-verification of
     A-TSK-01-P1-01).
  4. P2-01 `block_task` (executor.rs:1564-1580) and the terminal finalizers
     write through the unguarded public `set_task_status` (store.rs:953-983),
     bypassing the claim guard; convergence doc:153 claims "every ... block
     ... write carries the claim" — false as written; concrete casualty is
     the cross-process dual-driver case (GUI+TUI, A-TSK-01-P3-02), where a
     live Running claim can be overwritten with Blocked and the completed
     dispatch result silently discarded via Superseded.
  5. P3-01 hook events enqueued but not yet fired at crash are never replayed
     at boot (`recover_incomplete` fires no hooks); hook consumers can
     permanently miss persisted lifecycle events across a restart.
- Log/artifact: none

## Deviations

The pause-during-wave and mid-wave-fault behaviors are established by code
trace only (read-only review; no fault-injection fixture exists — Q-FLT-02 is
the follow-up); the torn-tail behavior was executed in A-TSK-01 V03-02 and the
parsing logic is unchanged here.

## Conclusion

Supports: the steady-state claim protocol works (stale writes rejected,
replay deterministic, run-terminal monotonicity enforced, boot recovery
claim-aware). Does NOT support: the claim that state updates happen only
through valid claims in every scenario — the pause path, the error path, the
torn-tail path, and the unguarded `set_task_status` surface violate or
bypass it, producing the 5 findings. Task card's required validations
(claim identity persistence, stale write rejection, event replay ordering,
crash/restart/cancel/retry scenarios) are all covered by this report plus
V01/V02/V04.

## Follow-Up

Q-FLT-02 fixtures: pause-during-wave; mid-wave poisoned store; torn-tail
append/rebuild; cross-process dual-driver claim overwrite.
