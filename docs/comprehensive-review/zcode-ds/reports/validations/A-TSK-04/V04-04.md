# A-TSK-04 / V04 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: not-applicable (app-core only)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

`file_shadow` tests pass, including shadow/SQL parity, per-run seq
isolation, concurrent-append unique strictly-increasing seq, and
concurrent atomic-write no-tmp-collision — the ledger write path is
correctly serialized and seq-monotonic on the covered paths.

## Method

```text
cd echo-agent-cli
cargo test -p echo-agent-app-core --locked tasks::task_runtime::file_shadow
```

## Expected Result

Exit 0; 9 tests green, including
`concurrent_append_produces_unique_strictly_increasing_seq` and
`concurrent_atomic_write_no_tmp_collision`.

## Result

- Exit code: 0
- Duration: 2.49 s (test phase)
- Summary: `test result: ok. 9 passed; 0 failed; 0 ignored; 0 measured;
  640 filtered out`.
- Log/artifact: `/Users/ls/.zcode/cli/exec/.../call_00_lIrukswsuxW63QXxQrhp7118-stdout.log`

## Deviations

None. The torn-tail tolerance the module comment promises
(file_shadow.rs:114-117) is NOT implemented — `read_events`
(file_shadow.rs:362-379) hard-errors on a malformed line; no test covers
torn-tail recovery (P1-03).

## Conclusion

Supports: seq allocation and projection rebuild are race-free within one
process, and replay order equals file order. Does not cover crash
consistency of the append (torn tail) — that is P1-03.

## Follow-Up

None.
