# F-LLM-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-LLM-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repos clean except untracked `echo-agent/tests/f_rct_01_probe.rs` (not owned by this task)

## Claim

The LLM contract unit-test suites compile and pass at the reviewed commits,
and the malformed-provider-response coverage (missing fields, wrong types,
invalid JSON) is assessed: which shapes are tested, which are not.

## Method

Commands (working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`,
`--locked`, default features; first command was run in background due to
expected workspace compile contention, then awaited):

```text
cargo test -p echo_core --lib --locked llm
cargo test -p echo_integration --lib --locked providers
```

Followed by static inventory of malformed-response tests:

```text
grep -rln "#[test]|#[tokio::test]" echo-core/src/llm/ echo-integration/src/providers/
grep -n "malformed|missing field|invalid|null|empty response|deserialize" <the two crates' llm/provider sources>
```

## Expected Result

Both commands exit 0. Malformed-coverage assessment: null-delta usage chunks
are known-tested; wrong-typed fields / invalid JSON / object-shape content
are expected to be untested given the silent-drop design in
`parse_sse_chunk`.

## Result

- Command 1 `cargo test -p echo_core --lib --locked llm`:
  - Exit code: 0
  - 56 passed; 0 failed; 0 ignored; 0 measured; 232 filtered out; 0.01s.
- Command 2 `cargo test -p echo_integration --lib --locked providers`:
  - Exit code: 0
  - 62 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; 0.05s.
- Malformed-response test inventory (present):
  - `types.rs:1233` `chunk_choice_handles_null_delta_in_usage_chunk` and
    `types.rs:1262` `chunk_choice_handles_missing_delta_in_usage_chunk` —
    null `delta` and omitted `choices` in a usage chunk (the two shapes the
    `deserialize_null_as_default` fix addresses).
  - `client.rs:367-393` — SSE framing tests: `parse_data_without_space`,
    `parse_data_with_crlf_and_keepalive`, `parse_done_marker` ([DONE] →
    Err sentinel).
  - `processor.rs:192-245` — `parse_tool_args` repair suite (trailing `}`,
    whitespace, multiple braces, genuinely broken → Err).
  - Usage/cache normalization suite (types.rs:1086-1231, 9 tests) and
    thinking-translation suite (thinking_translate.rs:167-416, 22 tests).
- Malformed-response gaps (absent):
  - No test feeds a wrong-typed field chunk (e.g. `"content": 123`, `"usage":
    "x"`, `"choices": {}`) through `parse_sse_chunk` and asserts error-vs-drop
    behavior — the silent-drop path at `client.rs:99-105` is untested.
  - No test asserts the streaming/non-streaming malformed-response divergence
    (stream drops, `post()` returns `LlmError::InvalidResponse`,
    `client.rs:163-164`).
  - No test asserts `MessageContent::deserialize` behavior for object/number
    content (silent `Empty`, types.rs:69-88).
  - No test asserts `AgentEvent::LlmUsage` emission (or `usage_reported`
    value) from the `direct_answer` path (react_loop.rs:763-813).
  - No contract-level test asserts `ChatCompletionResponse` unknown-field
    behavior (extra field `extra` never populated).

## Deviations

None. Both commands completed without warnings affecting the result.

## Conclusion

Supports: both suites compile and pass at the reviewed commits (exit 0).
Supports: the known malformed shapes (null delta, missing choices, tool-arg
trailing corruption) are covered; the silent-drop path, the streaming/
non-streaming divergence, and the object-content silent-empty behavior are
untested — consistent with F-LLM-01-P1-01; the direct_answer usage-event
absence is untested — consistent with F-LLM-01-P2-02.

Does not prove: adapter protocol fidelity (F-LLM-02/03 own openai.rs/
anthropic.rs fixture tests) or loop-level stream handling (F-RCT-02/03).

## Follow-Up

None.
