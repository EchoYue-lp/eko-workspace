# F-INT-02 / V05 / Attempt 01 (historical-document drift)

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-INT-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical claims about LSP/channels/A2A in `echo-agent/AUDIT_REPORT.md`,
`docs/MASTER-PLAN.md`, and `docs/PROJECT-ANALYSIS.md` can be classified
against current code.

## Method

1. `grep -n -i "lsp|channel|a2a|IM 渠道"` over
   `echo-agent/AUDIT_REPORT.md`, `docs/MASTER-PLAN.md`,
   `docs/PROJECT-ANALYSIS.md`, `docs/deep-iteration-plan.md`.
2. Read AUDIT_REPORT.md §1.10 (A2A no-auth) and §7 good-practice #7
   (JWT middleware).
3. Compare each claim to the code at baseline commits.

## Expected Result

Each claim classifiable as current/fixed/stale/regressed.

## Result

| Claim | Classification | Evidence |
|---|---|---|
| AUDIT §1.10: "A2A Server Defaults to No Authentication ... recommend deprecate serve() or at minimum emit a warning log when auth is disabled on non-loopback addresses" | fixed (warning added; serve() retained by design) | `src/a2a/serve.rs:103-117` warns on non-loopback bind without auth; `serve()` remains the documented entry (`serve.rs:55-63`) — retention is consistent with the AGENTS.md local-first threat model (no permission gating for the user's own machine) |
| AUDIT §7 #7: "JWT auth middleware — Proper bearer token extraction, algorithm restriction, expiration validation, and secret redaction in Debug output" | regressed (RS256 config cannot validate) | `src/a2a/auth.rs:247` always builds `DecodingKey::from_secret`, even for `JwtConfig::rs256` (:75-83); finding F-INT-02-P2-08 |
| MASTER-PLAN:987 "P1 插件组件接入 ... LSP/monitors/themes/output styles discovery (框架无消费者,只 resolve + 报告)" | current | plugin component discovery resolves LSP config files without starting servers — distinct from the LSP server stack reviewed here |
| MASTER-PLAN:1030 "src/cli/repl.rs、src/cli/channels.rs:CLI/channel 消费者" | current | `echo-agent-cli/src/cli/channels.rs` + `modes.rs:110-255` still the channel consumer |
| PROJECT-ANALYSIS.md:19 IM channel projection table (cli/modes.rs:58 run_channels_mode, ChannelChatSink) | current | `run_channels_mode` at `echo-agent-cli/src/cli/modes.rs:58,110-255` |
| echo-core/src/lsp/mod.rs:11-12 architecture doc: "echo-tools/src/lsp/ ← Tool implementations" | stale | no `echo-tools/src/lsp/` exists; tools live at `echo-agent/src/tools/lsp.rs`; finding F-INT-02-P3-03 |

## Deviations

None.

## Conclusion

Supports: only the AUDIT JWT claim is regressed (P2-08) and the
echo-core LSP module doc is stale (P3-03); other historical claims are
current or fixed. The AUDIT 1.10 recommendation was partially
implemented (warning, not deprecation), which this review accepts per the
local threat model.

## Follow-Up

None.
