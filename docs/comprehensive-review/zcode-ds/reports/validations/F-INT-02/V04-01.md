# F-INT-02 / V04 / Attempt 01 (LSP tests)

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-INT-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The LSP module tests (jsonrpc framing, config parsing, manager
bookkeeping) pass; the live-server smoke test is opt-in and skipped by
default.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_integration --features lsp,channels --lib lsp::
```

## Expected Result

All tests pass; `live_installed_servers_initialize_on_real_project_fixture`
is `#[ignore]`d.

## Result

- Exit code: 0
- Duration: ~0.1s (compilation reused incremental cache)
- `9 passed; 0 failed; 1 ignored`:
  - jsonrpc.rs: test_encode_message, test_parse_content_length,
    test_parse_response, test_parse_error_response
  - manager.rs: test_load_config, test_extension_mapping
  - config.rs: test_parse_config, discovers_installed_server_for_project_language,
    rejects_executable_that_fails_probe
  - ignored: live_installed_servers_initialize_on_real_project_fixture
    (requires `EKO_LSP_SMOKE=1` and installed servers)

## Deviations

`StdioLspClient` itself has no unit tests (client.rs contains none) —
request/response matching, pending-map behavior, timeout/cancel paths,
and diagnostics caching are untested; the ignored smoke test would cover
only happy-path initialization.

## Conclusion

Supports: framing/config/manager logic is green. Does not prove: client
runtime behavior (untested), which is where findings P1-01/P2-02 live.

## Follow-Up

None.
