# F-INT-02 / V02 / Attempt 03 (A2A family)

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-INT-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

A2A is a framework-only capability: public API (`A2AServer`, `A2AClient`,
`serve*`, `JwtConfig`) exists behind the optional `a2a` feature; no EKO
consumer exists; reachability is via the public API, examples (demo23),
and the agent-facing `serve` entry points.

## Method

1. `grep -rn "a2a|A2A"` over `echo-agent-cli/echo-agent-app-core/src` and
   `echo-agent-cli/src` — no hits (A2A absent from EKO).
2. Read `echo-agent/Cargo.toml` feature wiring (`a2a = ["dep:axum",
   "dep:jsonwebtoken"]`, `examples/demo23_a2a.rs` required-features).
3. Read `src/a2a/mod.rs` exports, `src/a2a/serve.rs`, `server.rs`,
   `client.rs`, `auth.rs`.
4. `grep -rn "cleanup_completed_tasks"` for production callers.

## Expected Result

A2A compiles standalone under its feature, is documented via
`src/a2a/mod.rs` doc example, and is exercised by demo23 and unit tests;
no EKO path.

## Result

- `src/a2a/mod.rs:59-63` re-exports `JwtClaims/JwtConfig/get_claims`,
  `A2AClient`, `serve/serve_from_config/serve_with_auth/
  serve_from_config_with_auth`, `A2AServer`, all `types::*`; gated by
  `#[cfg(feature = "a2a")]` in `src/lib.rs:69-71`.
- No A2A references in echo-agent-cli (definition not registered in any
  EKO surface); reachable only by framework consumers and `demo23_a2a`.
- `A2AServer::cleanup_completed_tasks` (server.rs:605-625) has zero
  production callers; `serve_inner` installs no periodic cleanup.
- `A2AServer` executes the wrapped `dyn Agent` directly
  (`agent.execute(&input_text)` server.rs:414; `execute_stream`
  server.rs:207); `execute_stream_with_cancel`
  (echo-core/src/agent/mod.rs:560-568) exists but is not used.
- Unit tests: serve (parse_method + proptests), auth, types, server
  (V04-03) — 38 passed.

## Deviations

Task registry entries created by `handle_request`/`handle_request_stream`
are never removed by any live path (only `cleanup_completed_tasks`, which
is uncalled) — finding F-INT-02-P2-09.

## Conclusion

Supports: A2A is a valid standalone framework API with no EKO consumer and
no live cleanup of its in-memory task registry.

## Follow-Up

None.
