# F-REL-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Definition/duplicate search across both repositories finds exactly the set of
retry/backoff/budget/circuit_breaker implementations and their callers, and
determines which are live, dormant, or dead.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
grep -rn --include="*.rs" -l "struct RetryPolicy\|fn with_retry\|backoff\|Backoff" --exclude-dir=target .
grep -rn --include="*.rs" -l "CircuitBreaker\|circuit_breaker" --exclude-dir=target .
grep -rn --include="*.rs" -l "TokenBudget\|budget" --exclude-dir=target .
grep -rn --include="*.rs" "RetryPolicy\|with_retry_if\|with_retry\b\|echo_core::retry" --exclude-dir=target .
grep -rn --include="*.rs" "retry_llm_call\|compute_concurrent_tool_batch_timeout" --exclude-dir=target .
grep -rn --include="*.rs" "TaskExecutor" --exclude-dir=target .
grep -rn --include="*.rs" "try_advance\|record_rejected\|is_open\|consecutive_failures\|rejected_count" --exclude-dir=target .
grep -rn --include="*.rs" "record_success\|record_failure" --exclude-dir=target .
grep -rn --include="*.rs" "CircuitBreaker" echo-agent-cli/... ; grep -rn "TaskExecutor" echo-agent-cli/...
```

Full command outputs captured in the review session; per-file conclusions below.

## Expected Result

- `echo_core::retry` is the single authoritative retry implementation and the
  root `src/retry.rs` is a pure re-export.
- No second retry/backoff implementation exists in the engine.

## Result

- Exit code: 0 (grep matches; inspection-based)
- Duration: n/a
- Summary: **Expected invariant does not hold.** The root `src/retry.rs`
  (11 lines) is a pure re-export of `echo_core::retry` — confirmed. But the
  root engine carries a second request-retry implementation, and a third
  backoff exists in `echo_orchestration`. Inventory:

| # | Implementation | Location | Live callers | Status |
|---|---|---|---|---|
| 1 | `RetryPolicy`/`with_retry[_if]` | `echo-core/src/retry.rs` | `echo-integration/src/providers/client.rs:122,203`, `anthropic.rs:374,431`, `examples/demo44` | live |
| 2 | `retry_llm_call` + `compute_concurrent_tool_batch_timeout` | `src/agent/react/run/retry.rs` | `src/agent/react/run/react_loop.rs:81,124`, `phases/think.rs:289,354`, `react_loop.rs:310`, `phases/tools.rs:170` | live (engine LLM path) |
| 3 | `TaskExecutorConfig::retry_delay_for_attempt` | `echo-orchestration/src/tasks/executor.rs:125-141` | none — `TaskExecutor` constructed only in its own `#[cfg(test)]` (`executor.rs:1854-1855`); zero refs outside `mod.rs:37` re-export | dormant |
| 4 | reconnect backoffs | `echo-integration/src/channels/channels/feishu/long_poll.rs:137-189`, `qq/channel.rs:137-188` | their own loops | live, different semantic (reconnect) |

- Budget: single authority `echo-core/src/budget.rs::TokenBudget`; live callers
  `src/agent/react/mod.rs:340-346` (context wiring), `echo-state/src/compression/mod.rs:474,1300,1311` (`budget.allocate`).
- Circuit breaker: single authority `echo-core/src/circuit_breaker.rs`.
  `record_success`/`record_failure` called only from `src/agent/react/run/retry.rs:61,63`.
  **Zero call sites** for `try_advance`/`record_rejected`/`is_open`/`consecutive_failures`/`rejected_count` outside `circuit_breaker.rs` itself.
  Breaker defaults to `None` (`src/agent/react/builder.rs:172`, `src/agent/react/mod.rs:521`); EKO never constructs it (grep in echo-agent-cli finds only the unrelated `CircuitBreakerTripped` task-status enum in `echo-agent-app-core/src/tasks/task_runtime/types.rs:656`).
- EKO side: no use of framework `TaskExecutor` (grep zero hits); EKO uses `task_create/task_update/task_list/task_execute` tools (`echo-agent-cli/.../runtime.rs:701`) which are name-based tools over `TaskRevisionService` (`src/tasks.rs` facade).

## Deviations

None. Grep is case-sensitive on identifiers as listed; doc-comment-only matches
(`echo-core/src/tools/mod.rs`, `src/evolution/patch.rs`, `src/agent/config.rs`) were excluded from the inventory.

## Conclusion

Supports: (a) root `src/retry.rs` is a pure facade — no second authority in the facade; (b) **the engine holds a second request-retry implementation** (`retry_llm_call`) — duplicate-authority finding F-REL-01-P2-01; (c) **the circuit breaker gate is never called** — finding F-REL-01-P1-01; (d) `TaskExecutor` retry pipeline is dormant (test-only construction). Does not prove: runtime behavior or arithmetic safety (V02), cancellation behavior (V03).

## Follow-Up

V02-01, V03-01, V04-01/02.
