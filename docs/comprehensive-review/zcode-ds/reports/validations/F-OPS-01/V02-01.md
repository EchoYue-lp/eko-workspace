# F-OPS-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Each operational adapter (scheduler trigger, headless entry, telemetry init,
trace producer/finalizer, audit logger) is registered and reachable at runtime
through a live call path in the current code.

## Method

Numbered inspection procedure (working dir
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

1. Scheduler registration: `grep -rn 'SchedulerRunner\|CronTask\|run_once'
   echo-agent/src echo-agent/echo-orchestration echo-agent/examples
   echo-agent-cli/echo-agent-app-core echo-agent-cli/src --include='*.rs'`;
   read `echo-agent-cli/src/cli/modes.rs:32-88`,
   `echo-agent-cli/echo-agent-app-core/src/state.rs:639-676`,
   `src/tauri/desktop.rs:232`, `plugin_runtime.rs:209-217,590-680`.
2. Scheduler cancellation wiring: `grep -rn -E 'scheduler.cancel_token|cancel_token.cancel'
   echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src --include='*.rs'`;
   read `echo-agent-cli/src/main.rs:325-345,390-410,435-450`.
3. Headless registration: `grep -rn -E 'run_headless|HeadlessConfig'
   echo-agent/src echo-agent/examples echo-agent-cli --include='*.rs'`;
   read `echo-agent/examples/demo54_headless.rs`.
4. Telemetry registration: `grep -rn -E 'init_telemetry|TelemetryConfig|shutdown_telemetry|Metrics::(record|get)'
   echo-agent/src echo-agent-cli --include='*.rs'`; read
   `echo-agent-cli/echo-agent-app-core/src/infra.rs:1500-1560`;
   feature defaults from both `Cargo.toml` files.
5. Trace registration: `grep -rn -E 'with_run_store|finalize_run|record_event|start_trace_run|start_scoped_trace_run'
   echo-agent/src echo-agent-cli --include='*.rs'`; read
   `echo-agent/src/agent/react/mod.rs:1890-1960`,
   `echo-agent/src/agent/react/run/stream_channel.rs:75-120,205-245`,
   `echo-agent-cli/echo-agent-app-core/src/infra.rs:370-390`,
   `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:3505-3545`.
6. RunEvent producer inventory: for each variant, `grep -rn "RunEvent::<Variant> {"
   echo-agent/src echo-agent/echo-orchestration/src
   echo-agent-cli/echo-agent-app-core/src --include='*.rs'` excluding
   `trace/mod.rs`, `trace/analyzer.rs`, `improve/`, `evolution/`, `eval/`, tests.
7. Audit registration: `grep -rn -E 'audit_logger|AuditCallback|AuditLogger'
   echo-agent/src --include='*.rs'`; read
   `echo-agent/src/agent/react/builder.rs:767-768,1001-1002`,
   `echo-agent/src/agent/react/run/pipeline.rs:424-457,935-953`.

## Expected Result

Every adapter has a live registration-to-call path; producers match the
documented `RunEvent` contract.

## Result

- Exit code: not applicable (inspection procedure)
- Duration: n/a
- Summary:
  - Scheduler: registered in EKO TUI/CLI (`state.rs:644` via
    `start_headless_services` modes.rs:62) and GUI (`desktop.rs:232`);
    `runner.clone().spawn()` at state.rs:671. Plugin monitors are bound to the
    same runner (`plugin_runtime.rs:209-217,590-680`). The runner's cancellation
    token (`state.rs:542`) is **never cancelled** anywhere in EKO — `main.rs`
    cancels a different token; shutdown relies on process exit.
  - Headless: registered as public framework API (`lib.rs:265` prelude) with one
    live caller: `examples/demo54_headless.rs`. EKO has no caller.
  - Telemetry: `init_telemetry`/`shutdown_telemetry`/`Metrics` exported under
    feature (`lib.rs:296-298`); **zero callers in echo-agent**; in EKO only
    `infra.rs:1539` under `#[cfg(feature = "telemetry")]` inside a
    `OnceLock::get_or_init` guard; the CLI result is discarded (`let _ =`).
    `Metrics::record_*` have **zero callers in either repository**.
  - Trace: `with_run_store` (`builder.rs:801`) wired in EKO
    (`infra.rs:377` JsonlRunStore at `user_data_path("runs")`). Run creation:
    `start_scoped_trace_run` (`mod.rs:1907`) stores the raw `input`. Producers
    live: LlmCall (think.rs:149, stream_channel.rs:444), ToolCall via
    `new_tool_call` (pipeline.rs:480), ToolResult/ToolError (pipeline.rs:830,861),
    BudgetDecision (stream_channel.rs:565,633), ContextCompression
    (compact.rs:73, capabilities.rs:265), Checkpoint (snapshot.rs:617),
    CheckpointResumed (mod.rs:1732), PhaseTransition (react_loop.rs:550).
    Finalization: `finalize_run` called from text/no-response/max-iterations and
    direct-answer paths (finalize.rs:175,216,261; stream_channel.rs:226,235) —
    the tools-branch terminal (`finalize_completed_run`) never calls it
    (confirmed by F-RCT-02-P2-01). EKO never calls `finalize_run` itself; its
    `save_trace` (executor.rs:3512) writes a second, event-less terminal `Run`
    to the same store.
  - Never-produced `RunEvent` variants (no construction anywhere, tests
    included): `Error` (`#[allow(dead_code)]`, trace/mod.rs:361), `FileEdit`,
    `TestRun`, `SubAgentRun`; `PermissionDecision` is constructed only in the
    dead module `run/approval.rs:179` (F-RCT-02-P3-02) — the live
    `PermissionStage` (pipeline.rs:272-330) records no trace event.
  - Audit: `audit_logger` builder option → `snapshot.guard.audit_logger`
    (builder.rs:1001-1002, snapshot.rs:308) → producers `AuditStage`
    (pipeline.rs:439), guard-block paths (snapshot.rs:901, execution.rs:244).
    EKO never sets `audit_logger` (no CLI-side matches) → audit inactive in EKO.

## Deviations

None observed. `RunEvent::ToolCall` has no literal construction site because
the live producer uses the `new_tool_call` constructor (expected).

## Conclusion

Supports: scheduler/telemetry/trace/audit registration facts; headless is
demo-only; telemetry metrics and the EKO scheduler cancel token are unwired;
trace terminal asymmetry (F-RCT-02-P2-01) and five never-produced `RunEvent`
variants. Does not prove behavioral correctness (V03).

## Follow-Up

None. Evidence used by F-OPS-01-P1-01, P2-01, P2-02, P2-04, P3-01, P3-03,
P3-04 and the cross-references to F-RCT-02-P2-01/P3-02.
