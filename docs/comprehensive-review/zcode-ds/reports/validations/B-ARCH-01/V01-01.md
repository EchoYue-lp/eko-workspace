# B-ARCH-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-ARCH-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable (framework-only task)
> Worktree state: clean

## Claim

The eight `echo-agent` workspace members form an acyclic Cargo dependency
graph in which no sub-crate depends on the root `echo_agent` facade, and no
sub-crate source references `echo_agent` or uses `extern crate`.

## Method

1. Re-derive the inter-crate path dependency graph from all 8 manifests
   (records already captured in `B-BASE-01/V01-01`; re-verified here against
   the manifest text).
2. `grep -rn "use echo_agent"` in all sub-crate `src/` trees; exclude lines
   starting with `//`, `///`, `//!`.
3. `grep -rn "extern crate"` in all sub-crate `src/` trees.
4. `grep -rn "echo_agent"` in all sub-crate `Cargo.toml` files.

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`.

## Expected Result

- Path deps: `echo_core`/`echo_macros` (none); `echo_tools` → echo_core,
  echo_macros; `echo_integration`/`echo_state`/`echo_orchestration` →
  echo_core; `echo_execution` → echo_core, echo_tools; root → all 7.
- Zero `use echo_agent` and zero `extern crate` in sub-crate source.
- Zero `echo_agent` entries in sub-crate manifests.

## Result

- Graph re-verified from manifests (`echo-core/Cargo.toml:30-48`,
  `echo-tools/Cargo.toml:40-41`, `echo-integration/Cargo.toml:28`,
  `echo-state/Cargo.toml:25`, `echo-orchestration/Cargo.toml:26`,
  `echo-execution/Cargo.toml:27-28`, root `Cargo.toml:106-112`): matches the
  expectation exactly. The graph is acyclic (each edge's target never
  re-depends on its source).
- `use echo_agent` in sub-crate source: zero hits.
- `extern crate` in sub-crate source: zero hits. The only `extern crate` in
  the workspace is the root's own `extern crate self as echo_agent;`
  (`src/lib.rs:26`).
- `echo_agent` in sub-crate `Cargo.toml`: zero hits.
- `proc_macro_crate::crate_name(...)` usage: `echo-macros/src/lib.rs:44`
  (`crate_name("echo_agent")`) and `echo-macros/src/derive_tool.rs:38,46`
  (`crate_name("echo_core")` with `echo_agent` fallback). This is runtime
  resolution of the *consumer's* manifest, not a Cargo dependency, and is the
  subject of finding `B-ARCH-01-P3-01`.

## Deviations

None.

## Conclusion

Supports: clean acyclic layering at Cargo level; sub-crates never depend on
or reference the root facade; feature forwarding does not create cycles.

Does not prove: module-level placement correctness of types (V03), public
facade coherence (V02), or documentation accuracy (V04).

## Follow-Up

None. Rerun as `V01-02` if manifests change.
