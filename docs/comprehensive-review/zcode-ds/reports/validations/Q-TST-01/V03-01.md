# Q-TST-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean (both repositories)

## Claim

The inventory of `#[ignore]` tests, platform-gated (`#[cfg(target_os)]` /
`#[cfg(unix)]`) tests, and skipped frontend tests is complete for both
repositories and every entry is classified.

## Method

```text
grep -rn "#\[ignore" --include="*.rs" <repo>/src <repo>/echo-core ... (all crates)
grep -rn "cfg(target_os|cfg(windows|cfg(unix|cfg(not(target_os" --include="*.rs" ...
grep -rn "\.skip|describe\.skip|it\.skip|test\.skip" web-frontend/src --include="*.test.ts*"
```

Classify each hit: is it a test-only gate, and does it skip a required
validation or is it a documented opt-in live test?

## Expected Result

A small, well-gated inventory with no silent skips in the mandatory gates.

## Result

- Exit code: not-applicable (read-only static inspection)
- Duration: ~10 minutes
- Summary:

### `#[ignore]` tests (total 5, all opt-in live smoke tests)

| Location | Reason | Class |
|---|---|---|
| `echo-integration/src/lsp/manager.rs:236` | `opt-in live LSP smoke test; set EKO_LSP_SMOKE=1` | Documented opt-in; env-gated |
| `echo-tools/src/research/clients.rs:979` | `opt-in live smoke test; set EKO_PROVIDER_SMOKE=1` | Documented opt-in |
| `echo-tools/src/research/clients.rs:1005` | `requires ZOTERO_API_KEY and ZOTERO_LIBRARY_ID` | Credential-gated live test |
| `echo-agent-cli/echo-agent-app-core/src/research_connectors.rs:616` | `opt-in live provider ingestion smoke test; set EKO_PROVIDER_SMOKE=1` | Documented opt-in |
| `echo-agent-cli/echo-agent-app-core/src/research_connectors.rs:649` | `requires ZOTERO_API_KEY and ZOTERO_LIBRARY_ID` | Credential-gated live test |

None of the 5 is a unit-level invariant that the mandatory gate silently
skips; all require explicit environment or credentials. No finding.

### Platform-gated tests

`#[cfg(unix)]` / `#[cfg(target_os = "...")]` test modules exist in:
`src/a2a/serve.rs:176`, `src/config.rs:714`, `echo-core/src/project_rules.rs:342`,
`src/state/file.rs:238`, `echo-core/src/tools/artifact.rs:586`,
`echo-core/src/plugin/registry.rs:921`, `echo-state/src/memory/file_conversation.rs:525`,
`echo-execution/src/sandbox/local.rs:587,596,911,1221`,
`echo-integration/src/lsp/config.rs:368,392`, `echo-tools/src/code.rs:782`,
`echo-tools/src/shell.rs:577`, `echo-agent-cli/src/tui/events.rs:2582-2590`,
`src/tui/clipboard.rs:24-154`, `src/tauri/ipc.rs:135-138`.

These are standard POSIX-vs-Windows test splits (path, sandbox, clipboard,
keyboard codes); both the CI host (macOS/Linux) and the mandatory gates run
the unix arms. No `#[cfg(any())]`-style permanently-false guards found in
test modules (the framework task `manager.rs`/`dag.rs`/`store.rs`/`events.rs`/
`time.rs` zero-test files are plain absence, not gated-away tests — V01).

### Frontend skips

Zero `describe.skip` / `it.skip` / `test.skip` in `web-frontend/src` test
files.

### Flaky-test evidence

No `#[ignore]` used as a flaky-test escape hatch, no retry wrappers, no
sleep-tolerance-based assertions found in the sampled suites (the
pipeline.rs:1634 test uses controlled delays with a biased select; margin
analysis noted in V02 sample 2 as a minor nondeterminism risk, not filed).

## Deviations

- The GUI matrix (`cargo test --no-default-features --features gui`) reports
  3 doctests "ignored" (CLI-side, cli/command.rs, logging/mod.rs) — these are
  Cargo doctest exclusions, pre-existing and CLI-side; recorded by
  Q-GUI-01 V02-01, not re-filed.
- `echo-agent/src/config.rs:714` unix-gated test was listed but not deep-read
  (peripheral config test).

## Conclusion

Supports: no ignored/platform-gated/flaky test hides a required invariant;
the ignored inventory is 5 documented opt-in live tests; the gate greenness
numbers in the dependency reports are not inflated by skips. The credibility
problem in this suite is absence (untested modules) and wrong-invariant tests,
not silently skipped tests.

## Follow-Up

None.
