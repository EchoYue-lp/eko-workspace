# A-CHAT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The historical claims about `drive_chat`/`ChatSink`/`ChatDriverEvent` in
`docs/MASTER-PLAN.md` and `docs/PROJECT-ANALYSIS.md` can each be classified as
current, fixed, stale, or regressed against the reviewed code.

## Method

Grep `drive_chat|ChatSink|ChatDriverEvent|极简入口` in
`docs/MASTER-PLAN.md` and `docs/PROJECT-ANALYSIS.md`, then compare each claim
against the code anchors verified in V02-01/V03-01.

## Expected Result

Each historical claim maps to a classification with code evidence.

## Result

- Exit code: 0 (grep completed)
- Summary of classifications:
  - MASTER-PLAN:93 "GUI/TUI/channel 已通过 drive_chat 统一驱动,差异主要在
    ChatSink" — current (CLI REPL now also joins; 4 call sites, V02-01).
  - MASTER-PLAN:431 "CLI REPL 绕过 drive_chat" — fixed (repl.rs:543 calls
    drive_chat; MASTER-PLAN:440 planned this).
  - MASTER-PLAN:770 "ChatSink 收敛为单一、穷尽的 ChatDriverEvent 入口;Agent
    (EventEnvelope)、Execution(ExecEvent)、turn status、requested/observed
    path 与 interrupt 不再通过默认 no-op 静默丢失" — regressed for interrupt:
    `ChatDriverEvent::Interrupt` has zero producers (V01-01); the GUI emits
    `ChatEvent::InterruptPrompt` directly (chat.rs:516-534), bypassing the
    shared sink; sinks still consume the variant (dead paths).
  - MASTER-PLAN:341 "交互入口消费同一 ChatDriverEvent" — current (V02-01).
  - MASTER-PLAN:454 "GUI/TUI/CLI/channel 共用 drive_chat" — current.
  - MASTER-PLAN:371 "M2 已完成 … CLI 已进入 drive_chat" — current.
  - PROJECT-ANALYSIS:13-19 "drive_chat 单一驱动点… 差异只在 ChatSink" —
    current in semantics; stale anchors (TauriChatSink at chat.rs:781 ->
    now 1341; drive_chat call chat.rs:599 -> now 688; TUI events.rs:888 ->
    now 2226; channel channels.rs:156 -> now 262).
  - PROJECT-ANALYSIS:22 "drive_chat(agent, message, multimodal, res:
    Arc<ChatResources>)" — stale: signature is now
    `drive_chat(agent, &PreparedUserTurn, Arc<ChatResources>)`.
  - PROJECT-ANALYSIS:23 "ChatSink trait 核心方法仅 on_agent_event(event) ->
    bool,其余 on_run_status/on_worker_trace/trace_sink() 等可选" — stale: the
    trait is now a single `on_event(ChatDriverEvent) -> bool`
    (chat_driver.rs:53-56); the doc also uses the forbidden "worker" term for
    a code shape that no longer exists.
  - PROJECT-ANALYSIS:244 "drive_chat 统一驱动 + task_local ChatResources,三
    模式解耦干净" — current.
  - `echo-agent-cli/src/main.rs:31` comment "drive_chat takes
    Option<&TaskRuntimeStore>" — stale: drive_chat takes
    `&PreparedUserTurn` + `Arc<ChatResources>` (store lives inside
    ChatResources).

## Deviations

None.

## Conclusion

Supports: the unified-driver design claims are current; the CLI-REPL
integration is fixed; the sink contract description and the interrupt
delivery claim are stale/regressed, matching findings A-CHAT-01-P2-01 (dead
Interrupt variant) and A-CHAT-01-P3-01 (doc drift).

## Follow-Up

None (classifications recorded in the task report's Historical Claim Status).
