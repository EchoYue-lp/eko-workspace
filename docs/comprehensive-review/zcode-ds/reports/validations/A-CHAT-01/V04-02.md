# A-CHAT-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The `surface_contract` test module — the shared `ChatDriverEvent` wire
contract (agent/execution/turn_status/execution_path/interrupt serialization)
and the cron terminal wire contract — passes at the reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked surface_contract
```

## Expected Result

All 3 `surface_contract` tests pass, exit code 0.

## Result

- Exit code: 0
- Duration: 0.00 s test time (build cached)
- Summary: 3 passed, 0 failed
  (capability_matrix_has_evidence_for_every_surface,
  shared_driver_wire_contract_preserves_product_facts,
  cron_runtime_wire_contract_preserves_terminal_facts).
- Log/artifact: none.

## Deviations

The wire-contract test constructs `ChatDriverEvent::Interrupt` synthetically
(`surface_contract.rs:198-203`) — it verifies serialization only, not that any
production path produces the variant (V01-01/V03-01: zero producers).

## Conclusion

Supports: the `ChatDriverEvent` wire contract serializes all five variants
with the declared `source` tags and preserves identity/artifact facts. Does
not prove: production reachability of the `Interrupt` variant.

## Follow-Up

V04-03 (workspace compile check).
