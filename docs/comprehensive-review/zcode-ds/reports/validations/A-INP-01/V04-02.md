# A-INP-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (A-INP-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The attachment unit suite (path-traversal rejection, image and text-file
save/build round trips, no-attachment fast path) passes on the reviewed
commit.

## Method

```text
cd echo-agent-cli && cargo test -p echo-agent-app-core --lib attachments::tests --locked
```

Environment: macOS arm64, workspace lockfile, default feature set.

## Expected Result

Exit code 0; 4 tests pass; zero failures.

## Result

- Exit code: 0
- Summary: `test result: ok. 4 passed; 0 failed; 0 ignored; 0 measured; 645
  filtered out`. Cases: `sanitize_rejects_traversal`,
  `save_and_build_image_roundtrip`, `save_and_build_text_file`,
  `build_message_no_attachments_is_text`.
- Log/artifact: command stdout (not persisted; short output).

## Deviations

None.

## Conclusion

Supports: attachment persistence identity (path + name + mime + source) and
the rebuild of `ContentPart::ImageUrl`/`File` from refs behave as documented.
Does not prove: the GUI/TUI staging adapters end to end, or uploads-dir
retention (finding A-INP-01-P3-01), which no test covers.

## Follow-Up

None.
