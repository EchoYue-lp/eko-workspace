# A-INP-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (A-INP-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical design claims about prepared-turn normalization, the spill
strategy, the grep artifact-root extension, and session-deletion cleanup still
match the current code (classification: current/fixed/stale/regressed per
REPORTING.md).

## Method

Grep `docs/MASTER-PLAN.md` (workspace root) and the CLI docs tree for
`PreparedUserTurn`, `user-input`, `spill`, `attachments`, `multimodal`, and
compare each claim with the reviewed source anchors.

```text
grep -n "PreparedUserTurn|user-input|spill|attachments" docs/MASTER-PLAN.md
grep -n "attachment|multimodal|paste" echo-agent-cli/docs/2026-07-28-app-core-full-audit.md echo-agent-cli/docs/2026-07-16-tool-output-artifacts.md
```

## Expected Result

Claims either match current code or are explicitly documented as deferred.

## Result

- Exit code: 0 (grep invocations)
- Summary:
  - MASTER-PLAN.md:291-297 (Phase E layering + Phase 1) — `PreparedUserTurn`
    normalization, `InputResourceRef`, 32 KiB threshold, user-input artifact
    dir, UTF-8-safe preview, atomic write, SHA-256: **current** (prepared_turn.rs
    matches line by line).
  - MASTER-PLAN.md:302-306 (Phase 2 — five entries + steer switched,
    `to_message` single merge, `match multimodal` deleted, `build_message`
    removed with only `build_message_from_refs` kept for executor rebuild):
    **current** (V01-01, V02-01).
  - MASTER-PLAN.md:310-311 (Phase 3 — grep candidate-root extension in
    `echo-agent` commit 8fd9b6a; `delete_conversation` cleanup of user-input
    scope): **current** (grep.rs:148-198, conversations.rs:617-628,
    events.rs:3078-3095).
  - MASTER-PLAN.md:313-314 ("暂缓" — persistence de-data-URL:
    `SavedAttachment.url` -> `artifact_path`, frontend contract): **current
    (deferred as documented)** — the frontend still renders data URLs
    (`MessageBubble.tsx:241` per the plan; A-STATE-01/A-FE scope).
  - MASTER-PLAN.md:98/275 (tool-result truncation + artifact spill): framework
    mechanism, **current** (out of A-INP-01 scope; F-EXT-01).
  - echo-agent-cli/docs/2026-07-28-app-core-full-audit.md:182 (attachment data
    URLs as separate concern): **current** — consistent with the deferred item.
  - echo-agent-cli/docs/2026-07-16-tool-output-artifacts.md:25-50 (tool-output
    spill): **current** — generic mechanism, unchanged.
  - No historical document claims a per-conversation uploads cleanup or a
    project-scoped REPL spill; the two mismatches found (A-INP-01-P2-01,
    A-INP-01-P3-01) are therefore new observations, not regressions of
    documented behavior.

## Deviations

None.

## Conclusion

Supports: the reviewed code is the delivered Phase E design; the "五入口 +
steer" and cleanup claims are current. The gaps found by this task were never
documented (either as designed or deferred), so they classify as new defects
rather than historical drift.

## Follow-Up

None.
