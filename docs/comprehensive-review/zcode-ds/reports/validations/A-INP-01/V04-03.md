# A-INP-01 / V04 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (A-INP-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

`echo-agent-app-core` (the crate owning `prepared_turn.rs` and
`attachments.rs`) compiles cleanly at the reviewed commit, so the input
pipeline code is in a buildable state for the surfaces reviewed in A-INP-01.

## Method

```text
cd echo-agent-cli && cargo check -p echo-agent-app-core --locked
```

Environment: macOS arm64, workspace lockfile, default feature set.

## Expected Result

Exit code 0; no errors or warnings; `Finished dev profile`.

## Result

- Exit code: 0
- Duration: 57.26 s (warm incremental build after the V04-01/02 test builds)
- Summary: `Finished `dev` profile [unoptimized + debuginfo] target(s) in
  57.26s`. No warnings emitted.
- Log/artifact: not persisted (short output).

## Deviations

None.

## Conclusion

Supports: the input-normalization code compiles; the static claims in V01/V02/
V03 are about buildable source. Does not prove: `src/tauri/**`, the GUI bin, or
the channels feature — those surfaces were inspected statically and their
compile health belongs to A-SRF-02/A-SRF-04/Q-GUI-01/Q-CLI-01.

## Follow-Up

None.
