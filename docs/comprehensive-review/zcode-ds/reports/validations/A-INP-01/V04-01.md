# A-INP-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (A-INP-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The `prepared_turn` unit suite (spill thresholds, CJK/emoji UTF-8 safety,
paste semantics, round trip, cleanup, path sanitization) passes on the
reviewed commit.

## Method

```text
cd echo-agent-cli && cargo test -p echo-agent-app-core --lib prepared_turn::tests --locked
```

Environment: macOS arm64, `cargo` with workspace lockfile; no feature flags
(default app-core feature set).

## Expected Result

Exit code 0; 14 tests pass; zero failures — the UTF-8 and spill invariants
documented in AGENTS.md hold at unit level.

## Result

- Exit code: 0
- Duration: ~30 s (build + run, tests themselves 0.02 s)
- Summary: `test result: ok. 14 passed; 0 failed; 0 ignored; 0 measured; 635
  filtered out`. Notable cases: `long_original_message_is_spilled_without_losing_
  instruction_semantics`, `long_cjk_text_spills_without_byte_slicing`,
  `emoji_preview_is_utf8_safe`, `pasted_text_is_always_a_tool_reference_and_may_
  contain_instructions`, `artifact_reference_survives_framework_projection_
  round_trip`, `cleanup_removes_expired_artifacts_and_conversation_scope`,
  `path_component_sanitizes_separators`, `should_spill_byte_threshold`,
  `should_spill_token_threshold_for_ascii`, `reference_instruction_is_utf8_safe`.
- Log/artifact: test output tail
  `/Users/ls/.zcode/cli/exec/sess_subagent_agent_49af83d3-7c86-44f8-a221-327e84173bb5/call_00_qbCLyDFLY6X00cxbY4uE8919-stdout.log`

## Deviations

None.

## Conclusion

Supports: the normalization core (spill decision, UTF-8-safe preview, atomic
write, reference semantics, scope cleanup, path sanitization) behaves as
documented for long/Unicode/paste inputs. Does not prove: the TUI/GUI adapter
behaviors (finding A-INP-01-P1-01 is an adapter-level defect, not covered by
these unit tests), nor the REPL spill-dir mismatch (A-INP-01-P2-01) which
depends on boot-time configuration.

## Follow-Up

None.
