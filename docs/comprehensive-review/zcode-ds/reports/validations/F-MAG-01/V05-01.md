# F-MAG-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical documentation claims about handoff, topology, and multi-agent
coordination can be classified against current code as current, fixed, stale,
or regressed, and the framework's multi-agent documentation describes one
coherent model.

## Method

Document-to-symbol comparison, executed 2026-08-12:

```text
1. echo-agent/README.md:70 (feature-parity table "SubAgent + Handoff"),
   :127-128 and :260-262 (feature lists), :207 (Agent Handoff row),
   :639-643 (multi-agent orchestration section).
2. echo-agent/docs/en/26-multi-agent.md:1-60, 78-100, 253-286 (multi-agent patterns,
   dispatch tool, mailbox communication, TeamConfig).
3. echo-agent/docs/zh/26-multi-agent.md and docs/zh/README.md:206-209 (parallel claims).
4. docs/MASTER-PLAN.md:106, :385 (term uses) — checked for relation to HandoffManager.
5. echo-agent-cli/docs/2026-07-17-ownership-dependency-scheduling.md:10-14 ("Handoff"
   as worktree integration term) — checked for relation to HandoffManager.
6. echo-agent/src/workflow/dsl.rs:88 ("Declarative Agent topology builder" — StateGraph).
7. echo-agent-cli/echo-agent-app-core/src/config_watcher.rs:8, 251 ("MCP server topology").
```

## Expected Result

Each claim is classifiable; a claim is stale when the code paths or semantics it
names do not exist or behave differently.

## Result

- Exit code: not-applicable (static comparison; source anchors from V01-V03)
- Duration: ~15 min
- Summary (classifications in the task report's Historical Claim Status table):
  - README:70 "Multi-agent = SubAgent + Handoff" — stale/misleading: handoff is
    an example-only API with a parallel registry and detached lifecycle, not a
    peer of the subagent dispatch model it is compared against.
  - README:207 "Agent Handoff — Context-aware transfer between agents" —
    stale: history transfer is a no-op on the LLM tool path and lossy text
    flattening on the programmatic path (F-MAG-01-P2-01).
  - README:127-128/260-262 "handoff — Agent handoff/collaboration",
    "topology — Multi-agent topology tracking" — stale in part: topology cannot
    record agent-to-agent calls (F-MAG-01-P2-02).
  - README:639-643 "Teammate — collaborative mode with shared Mailbox" and
    docs/en/26-multi-agent.md:43-49, 253-272 (Mailbox Communication, per-member
    Mailbox with capacity 64) — stale: mailbox machinery is dead public API with
    zero production callers (matches F-SUB-02-P2-03; doc-fiction).
  - docs/zh/26-multi-agent.md, docs/zh/README.md:206-209 — same claims in
    Chinese; same classification.
  - docs/MASTER-PLAN.md:106 ("TaskRuntime handoff" GUI projection) and :385
    ("TaskManager 拓扑查询") — unrelated term reuse (GUI/task-graph meaning),
    not claims about HandoffManager/TopologyTracker; classified as not-applicable.
  - echo-agent-cli/docs/2026-07-17-ownership-dependency-scheduling.md:10-14
    ("Handoff" = Codex-style worktree code return) — unrelated term reuse,
    not-applicable to the framework HandoffManager.
  - workflow/dsl.rs:88 `StateGraph` documented as "Declarative Agent topology
    builder" — a second "topology" concept in the framework (workflow DAG), plus
    EKO's "MCP server topology" (config_watcher.rs:8,251) — term collision
    across three unrelated meanings (F-MAG-01-P3-01).
  - No document claims a cancel/timeout/event contract for HandoffManager; the
    absence is consistent with the code, but README:70/207 present the API as a
    production coordination feature, which the code does not deliver.

## Deviations

None. (zcode-glm and codex reviewer directories were not read, per review rules.)

## Conclusion

The framework's multi-agent documentation is not one coherent model: README and
the 26-multi-agent doc present handoff (stale claims), mailbox communication
(fiction, consistent with F-SUB-02-P2-03), and topology tracking (stale in
part) alongside the live subagent model. Classifications feed the task report's
Historical Claim Status table. Does not re-validate the subagent claims
themselves (F-SUB-01/F-SUB-02 already did).

## Follow-Up

Q-DOC-01 should sample the README/docs multi-agent sections after fixes;
S-RDM-01 carries the doc-deletion targets.
