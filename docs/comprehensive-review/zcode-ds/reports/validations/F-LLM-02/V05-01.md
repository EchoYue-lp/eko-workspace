# F-LLM-02 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every F-LLM-01 finding that intersects the OpenAI adapter boundary is confirmed (or refuted) at the reviewed commit, and the dependency task's handoff items assigned to F-LLM-02 are settled.

## Method

```text
read docs/comprehensive-review/zcode-ds/reports/tasks/F-LLM-01.md (findings P1-01/P2-01/P2-02/P2-03/P3-01, handoff)
re-verify each anchor against source:
  client.rs:76-107 (parse_sse_chunk), :163-164 (post), :329/:347 ([DONE])
  types.rs:618-638 (extra), :802-822 (deserialize_null_as_default)
  react_loop.rs:763-813, think.rs:199-211 (LlmUsage)
  traits.rs/adapter_client.rs (ProviderAdapter)
  openai.rs:346/:357/:445 (streaming path)
```

## Expected Result

F-LLM-01-P1-01 trigger surface covers `OpenAiClient::chat_stream`; P2-01/P2-02/P2-03/P3-01 are either confirmed at the adapter boundary or out of adapter scope with cross-reference.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:

| F-LLM-01 finding | Status at F-LLM-02 boundary | Evidence |
|---|---|---|
| P1-01 silent drop of malformed SSE chunks | **confirmed; trigger surface = every OpenAI streaming call** (`OpenAiClient::chat_stream` openai.rs:357, `DefaultLlmClient::chat_stream` openai.rs:445 → `stream_post` → `parse_sse_chunk` warn+None client.rs:99-105). New severity detail: while-let chain exit + multi-event tail merge at client.rs:336-350 (see V02-01). No malformed-chunk fixtures exist (V04-02). | V02-01, V04-02 |
| P2-01 `ChatCompletionResponse.extra` dead / `raw` drops unknown fields | confirmed at adapter: `OpenAiClient::chat` reads only `raw.choices`/`raw.usage` (openai.rs:310-316); `extra` never populated (types.rs:635-637). Adapter adds no loss beyond raw's modeled-subset behavior. | V01-01, V02-01 |
| P2-02 LlmUsage not emitted on direct_answer paths | confirmed: stream_channel.rs (direct-answer) emits usage events (stream_channel.rs:439-473, F-LLM-01's anchor set), but the OpenAI adapter is not the cause — it delivers usage in both modes; the gap is the framework's `direct_answer`/`react_loop` non-streaming paths. Adapter-side faithful. | V02-01 |
| P2-03 ProviderAdapter/AdapterClient dead contract | confirmed: `openai.rs` never references `ProviderAdapter`/`AdapterClient`; both concrete clients implement `LlmClient` directly (openai.rs:263, :401). | V01-01 |
| P3-01 sentinel-as-error / no typed timeout-cancel | confirmed at transport: `[DONE]` → `LlmError::NetworkError(sentinel)` + string-containment match (client.rs:81-86, :329, :347); cancel → silent end (client.rs:251-256); timeout → text in NetworkError (client.rs:29-34). Adapter adds no typed distinction. | V03-01 |
| Handoff: "F-LLM-02 must check whether the OpenAI adapter's request assembly can produce the malformed-shape cases P1-01 warns about" | settled: assembly cannot produce malformed chunks (all wire fields are typed serde); the malformed shapes come from the provider. Adapter's own untested risk is the request-body fidelity (max_tokens reasoning-model 400 — F-LLM-02-P1-01) and the hardcoded `stream_options`. | V01-01 |

## Deviations

- None.

## Conclusion

Supports: F-LLM-01's adapter-boundary findings all hold at the reviewed commit; the new F-LLM-02 findings (P1-01 max_tokens, P2-01 cache_hints, P3-01/P3-02 adapter hardcodes) are disjoint from F-LLM-01's and do not duplicate any finding.

## Follow-Up

None.
