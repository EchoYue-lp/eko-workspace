# A-FE-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 files modified, all `web-frontend/src/generated/*.ts` (formatting-only). No other dirty paths.

## Claim

Historical design/audit documents describing the frontend type and event contracts still match current code, or their drift is classified.

## Method

Read-only comparison of documented claims against current code:

1. `echo-agent-cli/web-frontend/src/types/api.ts` header (lines 1-36): "Phase 6.4: canonical types are auto-generated by ts-rs in src/generated/"; "hand-written types either (a) extend generated types with UI-only fields, (b) are not yet generated, or (c) are legacy types pending migration"; "(6-10) re-export generated types that were previously duplicated here".
2. `echo-agent-cli/docs/MASTER-PLAN.md:75`: "TodoStatus and RuntimeEventKind preserve ... through ... generated TypeScript".
3. `echo-agent-cli/docs/2026-07-16-agent-lifecycle-audit.md:19`: chat `run_status` overwrites `taskRuntimeStore.activeRun.status`; `:139`: ordinary chat turns emit fake `execution://event kind=run/run_started` with `message_key` as run_id.
4. `echo-agent-cli/docs/2026-07-25-gui-tool-execution-lazy-loading.md:91-92`: `execution://event kind=tool` carries only `ToolExecutionSummary`; `chat://event` and Subagent lifecycle events no longer carry full args.
5. `echo-agent-cli/docs/browser-runtime-design.md:215`: browser event stream forwarded over `browser://event` (cross-checked with A-SRF-02).

## Expected Result

Each claim is classified current / fixed / stale / regressed with code anchors.

## Result

- Exit code: not-applicable (static comparison)
- Duration: ~20 min
- Summary:
  - api.ts header "canonical types generated; hand-written only supplements" — **stale/regressed**: 7 same-name hand-written types still duplicate generated ones and have diverged (ToolInfo, SkillInfo, McpServerInfo, McpToolInfo, ConnectMcpRequest, McpTransportConfig, FullConfigResponse — see V01-01); the "(6-10) re-export previously duplicated" note lists only 5 of the previously duplicated names.
  - MASTER-PLAN.md:75 generated TypeScript for TodoStatus/RuntimeEventKind — **current**: both are generated and consumed consistently (V03-01).
  - lifecycle-audit:19 (run_status pollutes taskRuntimeStore) — **fixed**: `chatStore.setRunStatus` (chatStore.ts:391-397) writes only the chat store; taskRuntimeStore status is driven by task-runtime data.
  - lifecycle-audit:139 (chat turns fake kind=run/run_started) — **fixed**: the current sink emits `ChatDriverEvent::TurnStatus` -> `ChatEvent::RunStatus` (chat.rs:620, 1365-1373); `execution://event kind=run` now originates only from `ExecEvent`/task-runtime emission (chat.rs:921, executor.rs:352-360).
  - gui-tool-execution-lazy-loading:91-92 — **current**: kind=tool carries only the summary (emit_tool_execution_summary chat.rs:185-208); chat://event carries ChatEvent payloads only.
  - browser-runtime-design.md:215 — **regressed**: producer sits in the overwritten first `.setup()` (mod.rs:40-68 vs :311-772), cross-verified A-SRF-02-P1-01 (V02-01).

## Deviations

None.

## Conclusion

The event-contract historical claims are mostly current or fixed; the type-generation claims in api.ts's own header are regressed (half-finished dedupe + drift), supporting A-FE-01-P2-01/P2-02; browser://event regression is already filed as A-SRF-02-P1-01.

## Follow-Up

None.
