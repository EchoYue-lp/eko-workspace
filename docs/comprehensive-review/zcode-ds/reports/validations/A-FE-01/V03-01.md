# A-FE-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 files modified, all `web-frontend/src/generated/*.ts` (formatting-only). No other dirty paths.

## Claim

Emitted event payloads and their TypeScript types match variant-for-variant and field-for-field, with consistent optional/null semantics, across the live Tauri surface.

## Method

Read-only field/variant matrix comparison:

1. `ChatEvent` Rust enum (src/tauri/commands/chat.rs:30-112, `#[serde(tag = "type")]`, 19 variants) vs hand-written TS `ChatEvent` (types/api.ts:125-177) — per-variant field comparison; then the dispatcher `chatEventHandler.ts` switch cases (grep `case '...'`).
2. `run_status` value set: emitters chat.rs:322-324 (`waiting_approval`), :370-372, :409-411 (`waiting_input`), :1203-1205 (`using_tool`), :1460-1462 (`thinking`), :1371 (TurnStatus `running`/`completed`/`failed`/`cancelled` from chat.rs:663-668) vs TS `ChatRunStatus` union (api.ts:38-47).
3. `execution://event` kind=tool: Rust `ToolExecutionSummary` (tool_execution.rs:64-78) + `ToolExecutionOwner` (`#[serde(tag = "kind", rename_all = "snake_case")]`, tool_execution.rs:38-46) + `ToolExecutionStatus` (`rename_all = "snake_case"`, :58-65) vs TS `ToolExecution` (api.ts:80-93) and `ToolExecutionOwner` (api.ts:51-53).
4. `execution://event` kind=subagent: bridge payloads (mod.rs:560-768: started/usage/isolation_observed/completed/failed/cancelled + task_id/run_id/agent fields) vs TS `ExecutionEvent` (subagentRunStore.ts:52-100) and `SubagentRunEventKind` (:32-40).
5. kind=run: `ExecEvent` (executor.rs:71-101) -> emit_tauri_execution_event (chat.rs:1419-1446) vs listener useTauriChat.ts:113-152 (`run_started` + `conversation_id` present in producer payload executor.rs:352-360).
6. Optional/null semantics: `SelectionRequest.task_id/context/phase` (`Option<...>` chat.rs:92-99) vs TS `task_id?: string|null, context?: unknown, phase?: string|null`; `emit_chat_event` always inserts `message_key` + `conversation_id` (possibly null) chat.rs:128-139 vs TS optional base fields.
7. Terminal events: terminal.rs:7-8,148-166 (`terminal-output` `{id, data}` base64, `terminal-exit` `{id}`) vs Terminal.tsx:112-121.
8. TaskRuntime DTOs: generated `TaskRun`/`TodoItem`/`RuntimeTaskEvent`/`TaskRunStatus` vs store consumption (taskRuntimeStore.ts) and Rust sources (types.rs:480-488 `TaskRunStatus` snake_case; seq-as-string contract types.rs `RuntimeTaskEvent.seq`).

## Expected Result

All live contracts match variant-for-variant and field-for-field.

## Result

- Exit code: not-applicable (static matrix)
- Duration: ~45 min
- Summary:
  - `ChatEvent`: 19/19 variants match (Token, ThinkingStart, ThinkingEnd, LlmUsage, ContextCompressed, ToolBatchStart, ToolBatchEnd, Chart, FinalAnswer, Cancelled, Error, RunStatus, Notice, ExecutionPath, ApprovalRequest, InputRequest, SelectionRequest, Done, InterruptPrompt). `chatEventHandler.ts` dispatches all 19 (`case` list verified). Base fields `message_key`/`conversation_id` optional in TS, always-present-or-null in Rust — compatible.
  - `run_status` strings emitted ("waiting_approval", "waiting_input", "using_tool", "thinking", "running", "completed", "failed", "cancelled") all inside `ChatRunStatus`; handler additionally whitelists and falls back to `idle` (chatEventHandler.ts:159-178) — null-safe.
  - kind=tool payload = `ToolExecutionSummary` fields (id, call_id, owner, conversation_id, run_id, name, args_preview, status, started_at, finished_at, duration_ms, detail_ref) flat-merged with kind/run_id/event/agent — matches TS `ToolExecution`; `owner` internally tagged `{kind: 'chat'|'subagent', ...}` — matches TS.
  - kind=subagent payloads: `started` (mode/task/background), `usage` (model/tokens/cache/usage_event_id), `completed`/`failed`/`cancelled` (terminal_status/contract_version/summary/artifacts/verification/remaining_work/touched_files), `isolation_observed` — all fields present in TS `ExecutionEvent`; event names within `SubagentRunEventKind`.
  - kind=run `run_started` carries `conversation_id` (executor.rs:355-359) as the listener expects (useTauriChat.ts:139-149).
  - kind=task events are emitted but deliberately not listened to (frontend consumes task events via `list_task_events` polling, taskRuntimeStore.ts:104,135-154) — design decision, not a gap.
  - Terminal events match (base64 `data` on `terminal-output`).
  - TaskRuntime DTOs are generated + used directly; `seq` as string matches `list_task_events(since_seq: Option<String>)`.
  - `TurnStatus` terminal derivation: error-normalized turns get `completed` (chat.rs:663-666, outcome always Ok for envelope errors) — cross-verified A-CHAT-01-P1-01, not re-filed here.

## Deviations

None beyond the pre-existing generated-dir dirty state (irrelevant to payload shapes).

## Conclusion

This validation supports: the emitted-payload contract (chat/execution/terminal channels) matches field-for-field and variant-for-variant on the live Tauri surface; the drift findings are confined to the command DTO families identified in V01-01 (ToolInfo, SkillInfo/McpServerInfo dual shapes, ConnectMcpRequest, FullConfigResponse phantom field). It does not prove dynamic wire behavior (no GUI launch).

## Follow-Up

None.
