# A-FE-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 files modified, all `web-frontend/src/generated/*.ts` (formatting-only; see V04-03). No other dirty paths.

## Claim

Every frontend-visible DTO name has exactly one authoritative definition whose wire shape matches the Rust type the backend actually serializes; hand-written duplicates of generated (ts-rs) types are absent, or, if present, field-for-field identical.

## Method

Read-only inspection, no commands that regenerate `generated/*.ts` were run:

1. Inventory of `web-frontend/src/generated/` (82 files incl. `index.ts` and `serde_json/JsonValue.ts`) vs the ts-rs export list (79 Rust `#[derive(TS)]` types across `echo-agent-app-core/src/types/request.rs`, `types/response.rs`, `types/error.rs`, `tasks/task_runtime/types.rs`, plus `src/`).
2. Full read of the hand-written supplement file `web-frontend/src/types/api.ts` (797 lines) and comparison of every same-name type against the corresponding generated file and the Rust definition.
3. For the 79 modified generated files, normalized comparison of HEAD vs working tree (quotes/whitespace/commas stripped) to classify the drift as semantic or formatting-only.
4. Grep for the generation mechanism: `__ts_rs` feature (`echo-agent-app-core/Cargo.toml:56`, `src/workspace/mod.rs:236`) and prettier config (`web-frontend/.prettierrc`).
5. Reachability of each consumer: grep for imports of the drifted names in `src/` (components/stores/hooks).

## Expected Result

No same-name type drift between hand-written `types/api.ts` and generated `generated/*.ts` / Rust wire types; generated files either clean or semantically identical to HEAD.

## Result

- Exit code: not-applicable (static inspection)
- Duration: ~40 min
- Summary: **Drift confirmed in 5 type names plus one commit-state inconsistency.**

Generated-vs-HEAD diff: all 79 modified files are formatting-only (single-quote + multiline + `trailingComma: es5` prettier style committed at HEAD vs raw ts-rs double-quote compact output in the working tree). After normalization (quotes, whitespace, commas), 68 files are identical and the remaining 11 differ only in union layout/quote style — zero semantic differences.

Same-name hand-written/generated pairs that DIVERGE (hand-written in `types/api.ts`, generated from Rust):

| Name | Hand-written (api.ts) | Generated (Rust source) | Consumer |
|---|---|---|---|
| `ToolInfo` | api.ts:179-185: `{name, description, source: string, input_schema?, enabled}` | ToolInfo.ts from `types/response.rs:40-47`: `{name, description, parameters: JsonValue, enabled, need_approval, source: ToolSource}` | live — `toolsApi.list` (endpoints.ts:141) typed from api.ts; `ToolsPanel.tsx:89` reads `tool.input_schema` |
| `SkillInfo` | api.ts:187-206: `{name, description, file, loaded?, source?, triggers?, tags?, version?, author?, has_sandbox?, depends_on?, category?, is_baseline?, is_builtin?, upstream_version?, has_updates?, missing_dependencies?}` | SkillInfo.ts from `types/response.rs:93-99`: `{name, description, enabled, tool_names, source: SkillSource}` | live — `skillsApi.list` (endpoints.ts:155) typed from api.ts; matches the Tauri command's hand-rolled JSON (panels.rs:346-377) |
| `McpServerInfo` | api.ts:241-250: `{name, status: 'connected'\|'disconnected'\|'error'\|'disabled', transport, tool_count?, tools?, connected_at?, error?, enabled?}` | McpServerInfo.ts from `types/response.rs:59-69`: `{name, status: McpConnectionStatus, transport, tool_count, tools, connected_at: string\|null, error: string\|null}` (no `enabled`) | live — `mcpApi.list` (endpoints.ts:199) typed from api.ts; matches Tauri wire (mcp.rs:60-85) |
| `ConnectMcpRequest` / `McpTransportConfig` | api.ts:258-266: `{name, transport: {stdio: {...}} \| {http: {...}} \| {sse: {...}}}` (variant-name-keyed, nested) | ConnectMcpRequest.ts / McpTransportConfig.ts from `types/request.rs:25-53`: `{name} & {transport: 'stdio'\|'http'\|'sse', command/url, ...}` (internally tagged, flattened) | dead — `mcpApi.connect` (endpoints.ts:204) has zero callers; `McpManagerPanel` is never imported |
| `FullConfigResponse` | api.ts:504-536: adds phantom `agent.enable_tools` | FullConfigResponse.ts from `types/response.rs:127-134`: `agent: AgentConfigResponse` (no `enable_tools`) | live — `ConfigPanel.tsx:3,7` via `get_full_config` (config.rs:156-161 returns the Rust `FullConfigResponse`) |

Rust wire authorities verified by reading the producer code: `get_tool_infos` (state.rs:722-760) builds `parameters`/`need_approval`/`source`; `list_skills` (panels.rs:459-486) builds the hand-written-shaped JSON via `skill_descriptor_json`/`hub_skill_json` (panels.rs:346-377); `list_mcp_servers` (mcp.rs:7-94) builds lowercase-status + `enabled` JSON; `connect_mcp_server` (mcp.rs:210-258) deserializes `McpTransportConfig` as `#[serde(tag = "transport", rename_all = "lowercase")]` (request.rs:34-35).

Duplicate-search terms (repo-wide): `ToolInfo`, `SkillInfo`, `McpServerInfo`, `McpToolInfo`, `ConnectMcpRequest`, `McpTransportConfig`, `FullConfigResponse`, `ChatRequest`, `ChatResponse`, `ToolCallInfo`, `ContextStats`, `SessionInfo`, `StreamingEvent`, `ServerMessage`, `ClientMessage`, `__ts_rs`. Result: `ChatRequest/ChatResponse/ToolCallInfo/ContextStats/SessionInfo` were deduplicated (re-exported from `generated` at api.ts:11-17 and endpoints.ts:5); the five names above remain hand-written and diverged; `StreamingEvent/ServerMessage/ClientMessage` exist only in generated/ with zero app consumers.

## Deviations

The working-tree generated files were already modified before this review (pre-existing dirty state, not caused by this review — no generation command was executed). Semantic content equals HEAD; formatting does not.

## Conclusion

This validation supports: (a) the ToolInfo wire-shape drift with a live consumer (A-FE-01-P2-01); (b) the dual-wire-shape families SkillInfo/McpServerInfo (A-FE-01-P2-02); (c) dead + wrong-shape ConnectMcpRequest (A-FE-01-P3-01); (d) the FullConfigResponse phantom field (A-FE-01-P3-03); (e) the generated-dir commit-state inconsistency (A-FE-01-P3-02). It does not prove runtime rendering behavior (static only; Q-E2E-01 owns dynamic confirmation).

## Follow-Up

None for this validation; see task report for findings and downstream task IDs.
