# A-FE-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 files modified, all `web-frontend/src/generated/*.ts` (formatting-only). No other dirty paths.

## Claim

Every command name and event channel the frontend invokes or listens to is defined, registered, and produced by the live Rust surface; every listener has a producer (or is explicitly dormant by design).

## Method

Read-only static tracing:

1. Extract all frontend invoke command names: `grep -rhoE "apiInvoke\('[a-z_0-9]+'|invoke\('[a-z_0-9]+'|apiInvoke<[^>]*>\('[a-z_0-9]+'" web-frontend/src` -> 184 unique names.
2. Extract all Rust `#[tauri::command]` fn names in `src/tauri/` (239 pub-fn names incl. non-command helpers) and the `invoke_handler` registration list (`src/tauri/mod.rs:69-310`, 212 registered entries incl. `terminal::*` at :304-309).
3. Diff frontend vs Rust definitions and frontend vs registered lists.
4. Trace each event channel producer/listener pair: `chat://event` (emitters chat.rs:114-143, 322, 370, 409, 516-534, 620, 656, 709, 1203, 1371, 1460; listeners useTauriChat.ts:93), `execution://event` (emitters chat.rs:153-183, mod.rs:353-768 bridge, chat.rs:1419-1446 projector; listener useTauriChat.ts:109), `terminal-output`/`terminal-exit` (terminal.rs:148-166; Terminal.tsx:112-121), `browser://event` (producer mod.rs:53-58 — see A-SRF-02-P1-01; listener useBrowserEvents.ts:12).
5. Spot-check invoke arg keys against Rust command parameters for the chat/task/mcp/terminal families (Tauri camelCase conversion of snake_case params).

## Expected Result

All frontend-invoked commands exist and are registered; arg keys match; each live listener has a live producer.

## Result

- Exit code: not-applicable (static tracing)
- Duration: ~30 min
- Summary:
  - 184 frontend-invoked command names: 184/184 exist as Rust command fns; 184/184 registered in `invoke_handler` (the 6 terminal commands are registered as `terminal::*`, mod.rs:304-309). Zero missing.
  - 212 registered commands; 34 are never invoked from the frontend (e.g. `browser_*` — blocked by the dead `browser://event` producer, cross-ref A-SRF-02-P1-01; `get_system_info`, `native_*`, `send_*_response` helpers invoked via other paths, `listRuns` unused wrapper).
  - Arg keys match Rust params (verified samples): `send_chat_message(message, conversation_id, message_key, attachments)` vs useTauriChat.ts:214-226; `get_task_run(run_id)` vs endpoints.ts:523; `list_task_events(run_id, since_seq: Option<String>)` vs endpoints.ts:552 (seq-as-string contract, task_runtime.rs:101-112); `connect_mcp_server(name, transport)` vs endpoints.ts:204-209; terminal commands vs `Terminal.tsx` invoke sites. The frontend defensively sends both camelCase and snake_case keys for `request_id`/`message_key`; extra keys are ignored by Tauri's typed arg deserialization (benign redundancy).
  - Event channels: `chat://event` single producer family -> single listener (useTauriChat.ts:93) — live. `execution://event` producers: bridge (mod.rs:353-768, kind=subagent/tool) + projector (chat.rs:921, kind=run/task/subagent) + emit_tool_execution_summary (kind=tool) -> listener useTauriChat.ts:109 handling kind=subagent/tool/run — live. `terminal-output`/`terminal-exit` -> Terminal.tsx:112-121 — live. `browser://event` listener live but producer dead (A-SRF-02-P1-01) — dormant surface.
  - Generated HTTP-only types (`ServerMessage`, `StreamingEvent`, `ClientMessage`, `ChatRequest`, `ChatResponse`, `UpdateConfigRequest`): defined (ts-rs from request.rs/response.rs) but zero frontend consumers and no Axum server exists anywhere in the CLI workspace (`axum` appears only in app-core as a dependency for error conversion) — registered-but-dormant definitions, not wire contracts in effect.

## Deviations

None.

## Conclusion

This validation supports: command surface is complete and registered (no missing/renamed commands); event channels are producer/consumer-matched except the already-filed `browser://event` dead producer (A-SRF-02-P1-01, cross-verified); the HTTP/WebSocket wire shapes described by the generated types are dormant (no server, no consumers). It does not prove runtime execution of any command.

## Follow-Up

None.
