# X-INV-01 / V01 / Attempt 01 — Subagent-only terminology (worker/Worker) audit

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Neither `echo-agent` nor `echo-agent-cli` contains the term `worker`/`Worker`
in any tracked source, documentation, manifest, or frontend file. Every
grep match is a false positive (e.g., the `NetworkError` identifier), an
IDE-local artifact, or a legitimate distinct concept (git worktrees).

## Method

Full-tree substring grep, case-insensitive, over both repositories,
excluding `target/`, `node_modules/`, `.git/`, and `.worktrees/`:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
grep -rni "worker" echo-agent          # all file types
grep -rni "worker" echo-agent-cli      # all file types
grep -rni "worker" echo-agent-cli/web-frontend/src
# residual non-network hits re-examined individually with
grep -rni "worker" <path> | grep -vi network
git check-ignore / git ls-files for IDE-local hits
```

Each match was classified manually: violation / test-only / third-party
wire exception / false positive.

## Expected Result

Zero violations. A violation is any tracked repo file using `worker`/
`Worker` for the subagent role (types, traits, fields, functions, modules,
variables, tool params, events, logs, comments, docs, UI copy, or internal
implementation names) without a third-party fixed-wire-name exception.

## Result

- Exit code: 0 (greps); classification by inspection
- Duration: ~1 min
- Summary:
  - `echo-agent`: 32 total `worker` substring hits; 31 are the
    `NetworkError` identifier family (substring `worke` inside
    `Network`+`Error`) in `echo-core/src/error.rs:90,283,400,404,409`,
    `echo-integration/src/providers/*.rs` (client.rs, adapter_client.rs,
    anthropic.rs, channels feishu/qq api.rs), `src/agent/react/mod.rs:79`,
    `src/testing/mock_llm.rs:246`, and two doc pages
    (`docs/zh|en/12-mock.md:81`). All classified as **false positives**
    (spurious substring match inside `NetworkError`).
  - The single residual non-network hit is
    `echo-agent/.idea/shelf/Uncommitted_changes_before_rebase_[Changes]/shelved.patch:246`
    — an IntelliJ IDEA shelved-changes artifact containing `worker_a`/
    `worker_b` node names in a demo YAML string. `.idea/` is git-ignored
    (`git check-ignore` exit 0) and zero `.idea` files are tracked
    (`git ls-files .idea` = 0). Classified as **false positive**
    (IDE-local, untracked, not repo content).
  - `echo-agent-cli`: **zero** `worker` substring hits across all tracked
    files (Rust, TS/TSX, JSON, TOML, docs).
  - Tracked `worktree*` identifiers (`echo-tools/src/git_worktree.rs`,
    `worktree_tool.rs`, `src/agent/subagent/worktree.rs`,
    `examples/demo58_git_worktree.rs`, `.worktrees/` directory naming) are
    the legitimate **git worktree** feature (AGENTS.md's own worktree
    section uses the same term) — classified as **false positive** for the
    Subagent/Worker terminology rule.

## Deviations

None. Q-STA-01's term inventory (pending in this track) may re-run the same
search with additional term variants; this report covers `worker`/`Worker`
in all tracked files of both repositories at the reviewed commits.

## Conclusion

The Subagent-only terminology invariant **holds**: no tracked file in
either repository uses `worker`/`Worker` for the subagent role, including
internal implementation, comments, events, docs, and UI copy. All matches
are `NetworkError` substring coincidences, an untracked IDE shelf file, or
the distinct git-worktree concept. No violation finding is raised.

## Follow-Up

`Q-DOC-01` (stale terminology search) and `S-RDM-01` may consume this
result. A rerun is required only if a commit adds `worker`/`Worker` terms.
