# X-INV-01 / V06 / Attempt 01 — Relative-path invariant audit (Cargo.toml path fields, worktree residue)

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean (echo-agent-cli has one
> stale prunable git worktree in metadata — see Deviations)

## Claim

Every Cargo-manifest `path` dependency in both repositories is relative;
no tracked file references `.worktrees/` or machine-absolute (`/Users/...`)
paths that would break a fresh checkout or CI build.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
# 1. manifest path deps
grep -rn 'path =' echo-agent/*/Cargo.toml echo-agent/Cargo.toml \
  echo-agent-cli/Cargo.toml echo-agent-cli/echo-agent-app-core/Cargo.toml \
  echo-agent-cli/echo-agent-eval/Cargo.toml
# 2. absolute/worktree residue across source + manifests + frontend + lockfile
grep -rn 'worktrees\|/Users/' echo-agent echo-agent-cli \
  --include="*.toml" --include="*.rs" --include="*.json" --include="*.ts" \
  --include="*.tsx" --include="*.yml" --include="*.yaml"
grep -n 'worktrees\|/Users/' echo-agent/Cargo.lock echo-agent-cli/Cargo.lock
# 3. tracking state of every hit
git -C echo-agent check-ignore .claude/settings.local.json
git -C echo-agent ls-files .idea
git -C echo-agent-cli worktree list
git -C echo-agent worktree list
```

## Expected Result

All manifest `path` fields relative; zero tracked absolute-path/worktree
residue; any remaining hits are git-ignored local files, test fixtures, or
the legitimate git-worktree feature code.

## Result

- Exit code: 0
- Summary:
  - **Manifest path deps — all relative.**
    - CLI: `echo-agent-cli/Cargo.toml:49-51` →
      `./echo-agent-app-core`, `../echo-agent`, `../echo-agent/echo-core`;
      `echo-agent-app-core/Cargo.toml:10,16` →
      `../../echo-agent`, `../../echo-agent/echo-core`.
    - Framework: `echo-agent/Cargo.toml:106-112` →
      `echo-core`, `echo-macros`, `echo-execution`, `echo-integration`,
      `echo-tools`, `echo-state`, `echo-orchestration`; member crates →
      `../echo-core`, `../echo-tools`, `../echo-macros`.
  - `worktrees|/Users/` grep hits — all classified:
    - `.claude/settings.local.json:17,20,32,37` (echo-agent) — local
      Claude Code settings with personal `/Users/ls` paths;
      **git-ignored** (`git check-ignore` exit 0), untracked. False
      positive for the invariant (not repo content, cannot break CI).
    - `echo-core/src/project_rules.rs:368` — test fixture string
      `gitdir: ../git/worktrees/test` (git-dir content, test-only).
    - `echo-tools/src/git_worktree.rs:45-53,131,149-160`,
      `worktree_tool.rs` (multiple), `src/agent/subagent/worktree.rs`,
      `examples/demo58_git_worktree.rs` — the framework's **git worktree**
      feature implementation (legitimate distinct concept, explicitly used
      by AGENTS.md's worktree development rules). False positive for the
      path-invariant; also not a V01 terminology violation.
    - `echo-tools/src/files/glob.rs:445` — a test fixture path
      `/Users/ls/照片/北京/景山.png` (multibyte glob test data,
      test-only; incidentally contains a personal absolute path in tracked
      test data — cosmetic, no build impact).
  - `Cargo.lock` (both repos): zero `worktrees`/`/Users/` references.
  - Frontend `package.json`: zero absolute/worktree references.
  - `git worktree list`: echo-agent — single main worktree (clean).
    echo-agent-cli — main worktree plus one **stale prunable** worktree
    `echo-agent-cli-eko-fork-implementer-implement-script-1-7d733efdc93f`
    (branch `eko-fork-implementer-...`, commit 12ec934) registered in git
    metadata. It is outside the repo tree, referenced by no manifest, and
    does not affect builds; per AGENTS.md worktree cleanup rules it should
    be pruned (`git worktree remove --force`, `git worktree prune`,
    `git branch -D`). Observation, not a violation.
  - No `.gitmodules` in either repo (no submodules).

## Deviations

Stale prunable CLI worktree in git metadata (observation above). No
manifest or lockfile impact.

## Conclusion

The relative-path invariant **holds**: every manifest path dependency is
relative; zero tracked absolute-path or `.worktrees/` build residue in
either repository at the reviewed commits. A fresh checkout + CI (B-BASE-01
V01 already verified the checkout-layout compatibility) resolves all
dependencies without machine-specific paths.

## Follow-Up

Optionally prune the stale CLI worktree in a maintenance commit;
`S-RDM-01` may list it as P3 housekeeping. The AGENTS.md merge-gate check
(`grep -rn "worktrees|/Users/" */Cargo.toml` = zero hits) is confirmed.
