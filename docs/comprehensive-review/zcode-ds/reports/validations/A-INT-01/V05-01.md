# A-INT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical claims about EKO browser/MCP/LSP integration in the planning and
audit documents still match current code (or are classified current/fixed/
stale/regressed with evidence).

## Method

Grep of historical documents and cross-check against current source:

```text
1. docs/MASTER-PLAN.md lines 99,107,167,170,184,188,333,374,532,533,539,
   570,733,993,997
2. echo-agent-cli/docs/MASTER-PLAN.md lines 69,74,114-115,383-404,430
3. echo-agent-cli/docs/browser-runtime-design.md lines 42-44,54-56,84-108
4. echo-agent-cli/docs/2026-07-17-surface-parity-closeout.md
5. echo-agent-cli/docs/2026-07-28-app-core-full-audit.md lines 30,249,265-267
```

## Expected Result

Claims classify as current/fixed/stale/regressed with a source anchor or a
validation link; no unverified adoption of historical claims into findings.

## Result

- Exit code: not-applicable (doc comparison)
- Summary of classifications (detail in the task report Historical Claim
  Status table):
  - root MASTER-PLAN:188 "MCP 重连…均有专项测试" — stale: no reconnect/
    disconnect tests exist (F-INT-01 V04-02); EKO adds no such tests either
    (V04-01/02/03 inventory); EKO recovery is manual (P2-04).
  - root MASTER-PLAN:533 browser retry policy — current:
    `browser_mcp_retry_safe` (browser/mod.rs:1031-1045) + `browser_failure`
    (mod.rs:1057-1086) implement exactly the documented split.
  - root MASTER-PLAN:993 "按本地威胁模型审查 HTTP/MCP 限制(保留密钥脱敏/
    超时/明显错误校验)" — pending/partially applied: secret redaction
    (mcp.rs:402-474) and timeouts (mcp.rs:503) are in; the SSRF-style
    private-range rejection (mcp.rs:169-208) is the restriction this review
    flags as over-gating (P2-03).
  - EKO MASTER-PLAN:74 "User-configured MCP tools have no deny-list" —
    current for the config-file path (framework `to_server_config` has no
    URL deny-list); contradicted by the GUI dialog validator (P2-03).
  - EKO MASTER-PLAN:69 "Model/MCP/runtime topology still requires restart" —
    current: config watcher does not hot-reload MCP (V02-01).
  - browser-runtime-design.md:95-108 "restart after unexpected exit; one
    restart + one retry; shutdown closes the sidecar" — current:
    mod.rs:867-938, 185-204; entry-point shutdown wiring (V03-02).
  - app-core-full-audit.md:30 "MCP = restart required" — current.
  - browser-runtime-design.md:67 "Manual MCP configuration remains supported
    through the existing MCP loader" — current at boot; the GUI config editor
    not being persisted is a NEW defect not covered by any historical claim
    (P1-01).
- Log/artifact: none

## Deviations

None beyond the classifications above.

## Conclusion

Supports: the historical browser retry/session claims are current; the MCP
test-coverage claim is stale; the EKO MCP config persistence defect (P1-01)
and the URL over-gating (P2-03) are new findings not described by any
historical document.

## Follow-Up

None. Feeds the task report Historical Claim Status table.
