# X-MEM-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every canonical finding consumed by X-MEM-01 is current at the reviewed
commits (anchor re-trace, not copied), and the historical claims X-MEM-01
relies on classify as current/regressed with current evidence.

## Method

Anchor-by-anchor re-trace of each canonical finding plus historical-document
rows, at the exact commits above:

```text
1. A-MEM-01-P1-01: refresh site inventory (see V02-01); refresh_hot_memory_projection
   zero production callers (unified_memory.rs:154-167, test :261);
   refresh_memory_projections only from infra.rs:528-531 (boot :444,
   switch state.rs:883, exit state.rs:1072, pool agent_pool.rs:553).
2. F-CMP-01-P1-01: sliding_window.rs:48-66 / summary.rs:299-317 early return;
   prepare no re-check mod.rs:1531-1538; verifier token check verifier.rs:100-120.
3. F-CMP-01-P1-02: summary.rs:292-296 partition-keep, :346-348 append.
4. F-CMP-01-P1-03: levels.rs:392-396 L1 fold user-role insertion (re-traced,
   not re-executed).
5. F-CTX-01-P1-01: infra.rs:23 (DEFAULT_CONTEXT_WINDOW 396_000), :218/:261
   (0 -> 396K); react/mod.rs:336-343 (ContextManager built with
   config.token_limit); agent_pool.rs:433-436 (window applied only when
   user-configured).
6. F-CTX-01-P2-02: echo-core/src/compression.rs:385 (rules chars().take(2000)),
   :376-401 (skill_injections never emitted); react/mod.rs:379 (Vec::new());
   capabilities.rs:587-597 add_skill vs set_working_dir refresh
   (react/mod.rs:907-921).
7. F-EVO-01-P2-02: memory_promoter.rs:71/:91 put_typed to WARM_NAMESPACE,
   no change-log record.
8. F-MEM-01-P1-01: echo-state/src/memory/store.rs:235-238 (corrupt -> warn +
   empty map), :254-278 (flush overwrites); EKO guard sites
   infra.rs:1319-1349 only react to Err.
9. A-MEM-01-P3-02: dreaming.rs:126-176 (no marker check) vs
   rule_promoter.rs:137 (marker check only in rule scan).
10. A-MEM-01-P3-03: instruction_provider.rs:291-297 (agents_instructions_path
    CWD-derived), rule_promoter.rs:197-222 (promote writes there);
    exit_workspace (state.rs:1053-1185) never restores CWD.
11. Historical rows: echo-agent-cli/docs/MASTER-PLAN.md (251-256 refresh
    contract, 259-260 one-fact-one-persistence), 2026-07-23-memory-self-
    evolution-closure.md (11/19/20 trigger + single-projection rows),
    echo-agent-cli/docs/configuration.md:61 (token_limit 0 = disabled).
```

## Expected Result

All listed canonical findings are current at these commits (their evidence
anchors match); historical rows classify as in the dependency reports
(regressed rows stay regressed); no finding silently drifted.

## Result

- Exit code: 0 (read-only inspection)
- Duration: n/a
- Summary:
  - All eight canonical findings revalidated current at commits 9b0e0fa /
    b3b2e81; the P1-01/P1-02 anchors were additionally reproduced
    dynamically (see V03-01) and the P1-01 refresh inventory independently
    rebuilt from scratch in V02-01.
  - New: EKO feature set (app-core Cargo.toml:10-15) does not enable
    `project-rules`, so framework `auto_project_rules` duplication is compiled
    out in EKO — the AGENTS chain has exactly one in-context producer (the
    EKO instruction projection). This was not stated in any dependency report
    and is recorded here (supports "no duplicate persistence" conclusion).
  - Historical classification: MASTER-PLAN 251-256 (immediate refresh of
    primary+pool on Dreaming/hot-mutation) = regressed (A-MEM-01-P1-01);
    MASTER-PLAN 259-260 (one content-derived key, one persistence) = current
    for L3, not extended to hot/rule channels (A-MEM-01-P3-02);
    closure-doc 19 (single projection) = stale (two projections);
    closure-doc 11/20 = regressed; configuration.md:61 (token_limit 0
    disables) = regressed (F-CMP-01-P3-02). All classifications match the
    dependency reports at current code.

## Deviations

None.

## Conclusion

The canonical finding set consumed by X-MEM-01 is current at the reviewed
commits; no drift or regression of the findings themselves was found. The
cross-repository conformance conclusion (see task report) is built on
revalidated evidence plus the new findings X-MEM-01-P2-01/P2-02/P3-01.

## Follow-Up

None.
