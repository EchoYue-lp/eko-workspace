# X-MEM-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every in-session hot-layer (MEMORY.md) mutation immediately refreshes the
correct projection (`eko:hot-memory-context`) on the primary agent and on
pooled agents; instruction-tier changes refresh `eko:instruction-context`;
refresh latency is one mutation → one projection update.

## Method

Full call-site inventory of every projection refresh and every hot-layer
mutation path, plus a check for any file-watcher-driven refresh:

```text
1. grep -rn refresh_instruction_projection | refresh_hot_memory_projection |
   refresh_memory_projections | refresh_instruction_context across
   echo-agent-cli (all .rs) and echo-agent-app-core — classify each site by
   (a) which projection it targets, (b) which mutation fired it.
2. grep -rn "MEMORY.md" in echo-agent-cli for any notify/watcher/debounce
   on the hot file (config_watcher.rs inspected for scope).
3. Trace mutation paths with no refresh: framework remember tool
   (echo-agent/src/tools/builtin/memory.rs:222), task memory bridge
   (echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/memory_bridge.rs:88-165),
   hot-budget demotions (echo-agent/src/evolution/layer.rs:711-810),
   write-time auto-promotion (layer.rs:837-886 -> 685-707 -> 1109-1144).
4. Check the evolution observer: echo-agent/src/evolution/runtime_integration.rs
   HookEvolutionObserver::on_memory_layer_change (127-140) — which subscribers
   does it invoke?
5. Verify boot/switch/exit refresh: infra.rs:444/528-531, state.rs:883/1072,
   agent_pool.rs:553.
```

## Expected Result

After a hot-layer change (Dreaming promotion, GUI add/delete, TUI/CLI
/remember /forget, remember-tool auto-promotion, task memory bridge, budget
demotion), the `eko:hot-memory-context` projection content matches the new
MEMORY.md on primary and pooled agents within one refresh.

## Result

- Exit code: 0 (read-only inspection)
- Duration: n/a
- Summary (invariant violated):
  - All eight hot-change refresh sites fire `refresh_instruction_projection`
    (instruction-only) or nothing:
    - Dreaming pass: infra.rs:1182 (primary) + agent_pool.rs:1191 → pool
      `refresh_instruction_context` (agent_pool.rs:701, instruction-only).
    - GUI add_memory (promotion): src/tauri/commands/memory.rs:134/143.
    - GUI delete_memory (hot): memory.rs:227/236.
    - TUI /remember: src/tui/events.rs:2846/2855; /forget hot: 2917/2926.
    - CLI /remember: src/cli/cmd_impls/all.rs:130; /forget hot: 201.
  - `refresh_hot_memory_projection` (unified_memory.rs:154-167) has zero
    production callers; the only call is its own test (unified_memory.rs:261).
  - No refresh at all on: remember-tool auto-promotion (memory.rs:222 →
    write_memory → consider_promotion → promote_warm_to_hot), task memory
    bridge candidate writes (memory_bridge.rs:153-165), hot-budget demotions
    (layer.rs:711-810). The only notification is the user-hook
    `on_memory_layer_change` (layer.rs:1224-1230 → runtime_integration.rs:133-140),
    which does not touch projections.
  - No file watcher covers MEMORY.md: the only notify-based watcher is
    `config_watcher.rs` (hooks/config scope).
  - Boot/switch/exit paths refresh BOTH projections correctly
    (refresh_memory_projections via infra.rs:528-531; callers infra.rs:444,
    state.rs:883/1072, agent_pool.rs:553) — hot memory is therefore stale
    only between boot/switch/exit and the next such event.
- Correct-target sites (for contrast): rule promotion refresh
  (panels.rs:1556-1570, evolution.rs:1485-1495) targets the instruction
  projection, which is right because learned-rules.md is an instruction tier.

## Deviations

None (result matches the pre-existing finding A-MEM-01-P1-01, which this
validation revalidates at the reviewed commits).

## Conclusion

The invariant does not hold: every in-session hot-layer mutation either
refreshes the wrong projection or none. Hot memory is stale in the live
context until boot/workspace-switch/exit. This is the "lost update" arm of
the X-MEM-01 question and is owned by canonical finding A-MEM-01-P1-01
(revalidated current at these commits). Supports: task report finding
A-MEM-01-P1-01 (cross-referenced), X-MEM-01-P3-01.

## Follow-Up

None (fix ownership stays with A-MEM-01-P1-01).
