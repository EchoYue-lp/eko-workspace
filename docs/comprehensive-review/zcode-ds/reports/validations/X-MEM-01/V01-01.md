# X-MEM-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

EKO's instruction/memory layers have exactly one authoritative source per
artifact (instruction tiers, MEMORY.md, warm store, learned-rules.md), the
source/precedence map of instruction tiers and hot memory is coherent, and no
semantic is persisted twice by a live path.

## Method

Repository-wide definition and duplicate search across both repositories plus
static reading of the authority chain:

```text
1. grep -rn across echo-agent-cli/src, echo-agent-cli/echo-agent-app-core/src,
   echo-agent/src, echo-agent/echo-core/src, echo-agent/echo-state/src for:
   InstructionProvider, UnifiedMemory, refresh_instruction_projection,
   refresh_hot_memory_projection, refresh_memory_projections, MemoryLayerManager,
   write_memory, WARM_NAMESPACE, replace_projection, eko:instruction-context,
   eko:hot-memory-context, MEMORY.md, learned-rules.md, memory_context_suffix,
   project_rules, auto_project_rules, PROMOTED_TO_RULE, l3_.
2. Read instruction_provider.rs (full), unified_memory.rs (full),
   infra.rs (create_agent 200-330, refresh_dynamic_context 528-531, memory store
   resolution 1269-1367), state.rs switch_workspace/exit_workspace (844-1185),
   echo-agent/src/evolution/layer.rs (685-810, 837-886, 1059-1191, 1239-1341),
   echo-agent/src/evolution/dreaming.rs (full),
   echo-agent/src/memory_promoter.rs (promotion key + write),
   echo-agent-cli echo-agent-app-core/src/evolution/rule_promoter.rs (95-268),
   echo-core/src/compression.rs (376-401), echo-core/src/project_rules.rs
   (176-218), react/mod.rs (322-384, 707-719, 907-921).
3. Verify EKO feature set: echo-agent-cli/echo-agent-app-core/Cargo.toml
   (echo_agent feature list — project-rules absent).
```

## Expected Result

A single live authority per semantic; any second implementation is either a
thin adapter or dormant and documented; the instruction-tier precedence
(user > repository > project > learned-rules > local) and hot-memory scope
(workspace `.eko/MEMORY.md` > user `~/.eko/MEMORY.md`) are unambiguous.

## Result

- Exit code: 0 (grep/read-only, no commands other than searches)
- Duration: n/a
- Summary:
  - Instruction precedence map confirmed: `InstructionProvider::load_for`
    (instruction_provider.rs:61-82) — user.md (`~/.eko`), repository AGENTS
    chain via `InstructionResolver::agents_files_only()` (:195-207), project.md,
    learned-rules.md (legacy AGENTS.md fallback :267-284, one-time rename
    :90-113), local.md, hot MEMORY.md (project root, user fallback :251-259).
    Two projections `eko:instruction-context` / `eko:hot-memory-context`
    (unified_memory.rs:28-29) split instruction (:141-163) from hot memory
    (:170-174).
  - No duplicate live persistence found: projections are derived from the files
    at boot/switch/exit (never written back); L3 promotion dedups by content
    hash `l3_{hash:016x}` (memory_promoter.rs:57, 91); warm→hot promotion
    deletes the warm entry only after the hot write (layer.rs:1109-1121) and
    hot→warm writes warm first (layer.rs:1146-1172) — at most a transient
    duplicate on crash, by design.
  - Framework `auto_project_rules` (default true, config.rs:259) does NOT
    duplicate the AGENTS chain in EKO: the injection is
    `#[cfg(feature = "project-rules")]` (react/mod.rs:363, 707) and EKO's
    feature list (app-core Cargo.toml:10-15) does not enable it.
  - Divergences found (→ findings):
    - `memory_context_suffix` / `PromptAssembler::add_instruction_context`
      (infra.rs:229-231) is a dormant parallel instruction-context mechanism;
      all four production construction sites pass `None` (agent_pool.rs:841,
      src/main.rs:161, src/main.rs:492, src/tauri/desktop.rs:150) — X-MEM-01-P2-01.
    - MEMORY.md has two independent parsers: EKO fence-strip
      (utils.rs:60-86) and framework frontmatter+bullet parse
      (layer.rs:1239-1284, extract at :1320-1331) — X-MEM-01-P2-02.
    - Rule promotion intentionally persists a fact in two files (warm
      store.json with `<!-- PROMOTED_TO_RULE` marker, rule_promoter.rs:234-250,
      + learned-rules.md) — drift-prone, canonical A-MEM-01-P3-02 family.
  - No other duplicate term found: one `InstructionProvider`, one
    `UnifiedMemory`, one `MemoryLayerManager` writer of MEMORY.md, one
    `replace_projection` authority.

## Deviations

None. The two divergences above are recorded as findings, not deviations of
the validation itself.

## Conclusion

The claim "single authority per artifact, no live duplicate persistence"
holds on the main path. The precedence map is unambiguous. The dormant
parallel instruction-context mechanism (P2-01), the dual MEMORY.md parser
(P2-02), and the rule/hot dual-file persistence (canonical A-MEM-01-P3-02)
are the recorded exceptions. Supports: task report layering decision,
X-MEM-01-P2-01, X-MEM-01-P2-02.

## Follow-Up

None.
