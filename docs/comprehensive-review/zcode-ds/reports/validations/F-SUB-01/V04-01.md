# F-SUB-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean (only `target/` artifacts written)

## Claim

The framework's subagent unit-test suites compile and pass with the
`subagent` feature enabled, including the result-protocol round-trip tests,
the executor cancellation/timeout/terminal-status tests, and the dispatch
catalog schema tests.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --features "subagent,tasks" --locked agent::subagent
cargo test -p echo_agent --lib --features "subagent,tasks" --locked agent_dispatch
```

Feature note: the subagent module is `#[cfg(feature = "subagent")]` and
requires `tasks` (declared in `Cargo.toml:77-79`); the default-feature build
compiles it out (verified in V01). Default feature set for `echo_agent` per
`Cargo.toml:57` is all-features; `subagent`+`tasks` is the minimal
self-sufficient set for this module.

## Expected Result

Both commands exit 0; the round-trip tests
(`types::tests::rendered_result_contract_round_trips_through_parser`,
`structured_result_ignores_model_status_and_hydrates_artifact`,
`structured_result_bounds_utf8_evidence`,
`agent_dispatch::tests::synchronous_parent_result_preserves_structured_contract`)
and the executor timeout/cancel tests pass.

## Result

- Exit code: 0 (both commands)
- Duration: ~5 min total (incremental build, 13 s compile + 1.07 s + 0.08 s test runs)
- Summary:
  - `agent::subagent` filter: **127 passed; 0 failed** (463 filtered out).
    Includes `structured_result_ignores_model_status_and_hydrates_artifact`,
    `structured_result_bounds_utf8_evidence`,
    `rendered_result_contract_round_trips_through_parser`,
    `fork_timeout_cancels_detached_stream_producer`,
    `sync_timeout_cancels_detached_stream_producer`,
    `teammate_timeout_cancels_detached_stream_producer`,
    `timed_out_dispatch_emits_timed_out_terminal_status`,
    `dispatch_background_returns_before_completion`,
    `dispatch_uses_injected_prompt_compiler`,
    `invocation_allowlist_hides_every_unlisted_tool`,
    registry/factory/definition-only tests, `filter_history` and
    `with_compiled_task` tests, context-inheritance tests, builder tests.
  - `agent_dispatch` filter: **11 passed; 0 failed** (579 filtered out),
    including
    `agent_dispatch_tool_schema_lists_registered_subagents`,
    `react_agent_register_agent_dispatch_tool`,
    `synchronous_parent_result_preserves_structured_contract`.
- Log/artifact: none persisted; output in task conversation.

## Deviations

None. (A first default-feature run matched 0 tests because the module is
feature-gated — expected, recorded in V01.)

## Conclusion

Supports: current code compiles under the subagent feature set and the
subagent unit-test suite is green; result-contract parse/render round-trip,
UTF-8 bounding, terminal-status ownership, timeout/cancel, and catalog
schema behaviors are covered by passing tests. Does not prove
end-to-end LLM behavior or the negative cases of the dead definition fields
(no test asserts `tool_filter`/`inherit_history=None` semantics, which is
itself evidence for the F-SUB-01-P1-01 / F-SUB-01-P2-02 gaps).

## Follow-Up

V05 (document drift) and task report.
