# F-SUB-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

There is exactly one Subagent concept/authority in both repositories; no
`worker` legacy terminology remains; and no parallel implementation of
subagent definition/registry/prompt/context/result exists under another name.

## Method

Repository-wide grep (both repositories, production sources only):

```text
# worker legacy terminology
grep -rniE '\bworker[s]?\b' echo-agent/src echo-agent/Cargo.toml
grep -rniE '\bworker[s]?\b' echo-agent-cli/src echo-agent-cli/echo-agent-app-core echo-agent-cli/echo-agent-eval

# duplicate concepts / naming variants
grep -rniE 'subagent|sub-agent|SubAgent' echo-agent/src echo-agent-cli --include='*.rs' | grep -v '//' | head
grep -rn 'SubAgentMap' echo-agent/src echo-agent-cli --include='*.rs'
grep -rn 'AgentDispatchTool|SubagentRegistry|SubagentDefinition|ContextBuilder|MemoryScope|OutputSchema' echo-agent/src echo-agent-cli --include='*.rs'

# per-field dead-or-live search (production, excluding definition/init/test sites)
for f in tool_filter lightweight 'def\.model' 'def\.system_prompt' 'def\.max_iterations' \
         'def\.token_limit' 'def\.inherit_memory' 'def\.can_delegate' 'compile_system'; do
  grep -rnE "$f" echo-agent/src echo-agent-cli/echo-agent-app-core/src --include='*.rs'
done
```

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`. No feature
flags (source-level grep). Full command outputs are in the task conversation;
only the counts and conclusions are recorded here.

## Expected Result

- Zero matches for `worker` in both repositories.
- One authoritative subagent module (`echo-agent/src/agent/subagent/`), one
  registry type, one dispatch tool; `SubAgentMap` is only an internal type
  alias consumed by that registry.
- Framework `SubagentDefinition` fields that are documented as configuration
  must each have at least one production reader.

## Result

- Exit code: 0 (grep exits 0 on matches; all invocations succeeded)
- Duration: < 1 min
- Summary:
  - `worker`: zero matches in both repositories. The Subagent-only
    terminology cleanup is complete on both source trees.
  - Subagent authority: single module `echo-agent/src/agent/subagent/`
    (19k lines incl. teams); single `SubagentRegistry`
    (`registry.rs:72`); single LLM dispatch tool `AgentDispatchTool`
    (`tools/builtin/agent_dispatch.rs:57`); `SubAgentMap` is a
    `pub(crate)` type alias (`src/agent/mod.rs:48`) consumed only by
    `registry.rs` (`from_subagent_map`, `agents_map`) and its tests — not a
    parallel authority.
  - `ContextBuilder` (`context_builder.rs`), `SubagentOutput`,
    `OutputSchema`, `MemoryScope`: zero production callers in either repo
    (only the module re-export `subagent/mod.rs:26` and tests).
  - `SubagentDefinition` fields with zero production readers:
    `tool_filter` (`types.rs:144`), `model` (`:140`), `system_prompt`
    (`:141`), `max_iterations` (`:146`), `token_limit` (`:148`),
    `inherit_memory` (`:153`), `can_delegate` (`:157`), `lightweight`
    (`:164`). Only definition/initializer/setter/test sites matched.
    `lightweight` has no setter anywhere.
  - `SubagentPromptCompiler::compile_system`: no production call site inside
    `echo-agent` (only trait def, default impl, and tests); the application
    calls it at registration (`echo-agent-cli/.../infra.rs:627` via
    `EkoSubagentPromptCompiler`, `subagent_prompt.rs:177-235`).
  - Naming variants: `SubAgent` (capital A) survives only inside
    `agent_dispatch.rs` user-facing strings (lines 321, 340, 348, 364-376,
    396-402, 424-438); `sub-agent` (hyphenated) survives only in the dead
    `isolated.rs` module (lines 1-4, 28-29) and `subagent/usage.rs:1` doc.
  - The executor honors only: `name`, `description`, `execution_mode`,
    `inherit_history` (as a history limit), `timeout_secs`,
    `isolate_worktree`, `isolate_workspace`, `team`, `is_background`
    (`executor.rs:444-452, 1475-1485, 1569-1583, 1603-1636, 992-1113`;
    `agent_dispatch.rs:244-253`).

## Deviations

`cargo test --list` needed the `subagent`+`tasks` features to see the module
tests; the default-feature build compiles the subagent module out
(`src/agent/mod.rs:63` is `#[cfg(feature = "subagent")]`). This does not
change the source-level conclusions.

## Conclusion

Supports: one subagent authority per repository; worker terminology fully
removed; a set of framework `SubagentDefinition` fields and one whole
context-construction API that are dead in production (basis for
F-SUB-01-P2-01, F-SUB-01-P2-02, F-SUB-01-P3-01); `compile_system` is an
application-exercised contract only. Does not prove runtime reachability —
that is V02.

## Follow-Up

V02 (registration and reachability), V03 (invariant checks), V04 (test run),
V05 (document drift).
