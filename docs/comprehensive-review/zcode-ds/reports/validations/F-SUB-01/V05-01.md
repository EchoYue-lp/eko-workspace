# F-SUB-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The subagent-related claims in `docs/MASTER-PLAN.md` still match current
code (or are explicitly classified as stale/regressed), per the
REPORTING.md historical-document protocol.

## Method

```text
grep -n "subagent\|Subagent\|SubAgent" /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/docs/MASTER-PLAN.md
```

then each matched claim was checked against the anchors verified in V01-V04.

## Expected Result

Each claim classified as `current`, `fixed`, `stale`, or `regressed` with
code evidence.

## Result

- Exit code: 0
- Duration: ~15 min
- Summary of classifications:
  - MASTER-PLAN:54 "Subagent 使用独立上下文、独立工具/权限配置,主会话只接收汇总"
    — **partial / regressed in part**: independent context and structured
    parent summary are current (`executor.rs:1148-1453` outcome contract;
    `agent_dispatch.rs:336-343` outcome JSON); "独立工具/权限配置" at the
    *definition* level is regressed — `tool_filter` is dead (V01/V03), the
    live tool restriction is per-task `allowed_tools` on the programmatic
    path and build-time readonly/writer split (F-SUB-01-P1-01).
  - MASTER-PLAN:100 "Subagent 已有 Sync/Fork/Teammate/Team、独立上下文、
    timeout、checkpoint、worktree/tmpdir 隔离和 writer 文件锁基础" — **current**
    for Sync/Fork/Teammate/Team routing (`executor.rs:552-564`), timeout
    (three modes), worktree/tmpdir isolation (`:1603-1735`); checkpoint is
    team-only (`runtime_state_store`, `:331-336`); "writer 文件锁" is EKO
    TaskRuntime territory (A-TSK-05), not framework.
  - MASTER-PLAN:101 "框架 agent_tool 的子取消令牌以当前 invocation 的
    ToolContext.cancel 为权威" — **current**
    (`agent_dispatch.rs:164-178, 283-284`; test
    `invocation_cancel_token_overrides_stale_shared_handle`).
  - MASTER-PLAN:212-233 "Phase A 结构化 Subagent 结果合同 ... Subagent 失败
    不得被主 Agent 汇总文本掩盖; 最终 task 状态以结构化结果为准" — **current**
    (`types.rs:386-532` contract; `parse_subagent_outcome` status override
    `:456-465`; observed/reported evidence `:481-485` + `executor.rs:229-254`).
  - MASTER-PLAN:245 "主 Agent 汇总使用结构化 Subagent result,不重新扫描
    聊天文本猜结论" — **current** at framework level
    (`agent_dispatch.rs:13-17, 336`).
  - MASTER-PLAN:482 "Subagent result 显式携带 cancelled,不再把取消文本当
    成功结果" — **current** (`types.rs:757-779`; `executor.rs:1410-1412,
    1443-1445`).
  - MASTER-PLAN:54 (Claude Code reference) "写入型 subagent 可使用 worktree
    隔离" — **current** (`types.rs:166-176`, `executor.rs:1601-1619`).
  - zcode-ds B-ARCH-01-era claim that `SubAgentMap` is a legacy live map —
    **stale as an authority**: it is now only a `pub(crate)` alias consumed
    by `SubagentRegistry` (`agent/mod.rs:48`; V01).

## Deviations

None; MASTER-PLAN contains no other subagent-identity/registry/prompt/result
claims that contradict current code.

## Conclusion

Supports: the structured-result, cancellation, timeout, and isolation claims
are current; the "independent per-subagent tool/permission configuration"
claim is only partially delivered (definition-level tool/permission
selection regressed — F-SUB-01-P1-01). This V05 report feeds the Historical
Claim Status table of the task report.

## Follow-Up

None — task report writing.
