# Q-CLI-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline anchor, not reviewed here)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (`feat(plugins): activate hooks and preferences`)
> Worktree state: clean before and after (0 dirty files; `git status --porcelain` empty after the run)

## Claim

`echo-agent-app-core` compiles with no default features and `--locked`:
`cargo check -p echo-agent-app-core --no-default-features --locked` exits 0
with zero warnings.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo check -p echo-agent-app-core --no-default-features --locked > /tmp/qcli01_v05_nodefault.log 2>&1
echo "EXIT_CODE=$?"
grep -c "warning" /tmp/qcli01_v05_nodefault.log
grep -c "^error" /tmp/qcli01_v05_nodefault.log
```

- Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli`
- Toolchain: rustc 1.97.1 (8bab26f4f 2026-07-14), cargo 1.97.1 (c980f4866 2026-06-30)
- Feature flags: none (`--no-default-features`); `--locked` requires Cargo.lock
  consistency.
- Full log: `/tmp/qcli01_v05_nodefault.log` (external temp file, not committed)

## Expected Result

Exit code 0 and zero warnings/errors. A default-feature dependency or
cfg-inconsistent module would break the no-default build and fail the gate.

## Result

- Exit code: 0
- Duration: 14.0 s (warm cache)
- Summary: PASS. `Finished dev profile`, zero `warning` lines, zero `error`
  lines. `echo-agent-app-core` (and its no-default dependency slice) compiles
  cleanly without default features.
- Log/artifact: `/tmp/qcli01_v05_nodefault.log`

## Deviations

None. No change to tracked files (`git status --porcelain` empty after the
run).

## Conclusion

Supports: the AGENTS.md no-default app-core check passes at commit b3b2e81 —
the EKO application core compiles warning-free with default features disabled.
Does not prove: SQLite absence in the dependency tree (V06) or any behavioral
claim about the default-feature build (covered by V02/V04).

## Follow-Up

None.
