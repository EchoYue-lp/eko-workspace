# A-TSK-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

The documented file-authority and adapter contracts (EKO MASTER-PLAN,
`docs/2026-07-21-dynamic-plan-runtime.md`,
`docs/2026-07-28-task-tools-framework-migration-design.md`) match current
code on the reviewed commits, and no historical claim about crash repair is
silently assumed to hold.

## Method

Compare each documented claim against the reviewed source:

```text
sed -n '170,240p' echo-agent-cli/docs/MASTER-PLAN.md
sed -n '1,60p'  echo-agent-cli/docs/2026-07-21-dynamic-plan-runtime.md
grep -n "lossless\|second patch engine\|second task validator" echo-agent-cli/docs/2026-07-28-task-tools-framework-migration-design.md
```

Cross-checked against `file_shadow.rs`, `file_store.rs`, `event_rebuild.rs`,
`store.rs`, `revisioned_adapter.rs` (reviewed at the commits above).

## Expected Result

Every claim classified as current/fixed/stale/regressed with a source anchor.

## Result

- Exit code: not-applicable (static comparison)
- Classification table:

  | Claim | Classification | Current evidence |
  |---|---|---|
  | MASTER-PLAN: "`events.jsonl` is the recovery authority, `plan.json` is the latest immutable plan specification, and `run-state.json` is the execution projection" | current | file_shadow.rs:6-7, 208-280; file_store.rs:3-11; event_rebuild.rs:1-9 |
  | MASTER-PLAN: "File-backed Todo reads take TaskExecution status from `run-state.json`; older Task events only restore metadata and cannot override a later plan skip/reset" | current | file_store.rs:190-199; test `task_update_status_overrides_earlier_task_events` (passed, V04-04) |
  | MASTER-PLAN: "`run-state.json` stores shared `TaskStatus` detail independently from `failure_fingerprint`" | current | types.rs:1015-1019 (status_detail), 922-929 (failure_fingerprint in EkoTaskExecution) |
  | MASTER-PLAN (M13): framework owns revision safe points/ready frontier/waves/stall; EKO validates only its Subagent/tool catalog and file-ownership policy | current | store.rs:96-101 + 921-923 (framework PlanValidator); task_tools.rs:49-81 (capability); planner.rs:8-9 (ownership policy) |
  | dynamic-plan-runtime: authority split + "`TaskRuntimeStore` validates and atomically commits plan revisions" | current | store.rs:755-885 (CAS under per-run lock) |
  | task-tools-migration: "adapters must not retain a second patch engine or task validator"; assumptions/risks/execution_mode "preserve losslessly" | current | revisioned_adapter.rs:26-56 (thin); no EKO patch engine/validator (V01-01); commit_eko_task_plan/CAS map all envelope fields (V03-01) |
  | file_shadow.rs code comment: crash mid-append "at worst lose[s] the last partial line" and a "future hardening pass (gate 2) will truncate a partial tail" | regressed (not implemented) | read_events hard-errors on a torn line (file_shadow.rs:362-379; executed proof V03-02) — finding A-TSK-01-P1-01 |
  | F-TSK-01/F-TSK-02 handoffs: EKO store CAS is the only unverified conversion boundary; no EKO second validator/frontier | current | store.rs:676-885 (CAS verified here); V01-01 |

## Deviations

The "gate 2" truncation pass referenced by the code comment is absent — this
is a code-comment claim, not a document claim, but it is the only place the
project records an intent to handle torn tails, and current behavior
contradicts it.

## Conclusion

Supports: documented authority and adapter contracts are current; the one
crash-repair intent is not implemented (P1-01). No other historical claims
for this task were found stale or regressed.

## Follow-Up

None.
