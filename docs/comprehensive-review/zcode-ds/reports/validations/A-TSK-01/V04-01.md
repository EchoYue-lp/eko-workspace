# A-TSK-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

The `task_runtime::types` unit tests (including the PlanTask <-> framework
Task round-trip and status projection tests) pass on the reviewed commit.

## Method

```text
cd echo-agent-cli && cargo test -p echo-agent-app-core --locked tasks::task_runtime::types
```

## Expected Result

Exit 0; all types tests pass, including
`plan_task_round_trips_through_framework_task`,
`plan_task_status_projection_preserves_failure_detail`,
`task_status_projection_preserves_cancelled_and_timed_out`,
`terminal_subagent_result_bounds_utf8_failure_text`.

## Result

- Exit code: 0
- Summary: `test result: ok. 51 passed; 0 failed; 0 ignored` (598 filtered);
  ts-rs export_bindings tests included. Second harness target
  `runtime_state_e2e` ran 0 tests (filtered).

## Deviations

None.

## Conclusion

Supports the round-trip and UTF-8-bound evidence in V03-01 for the types
layer. Does not cover the store CAS or rebuild (separate reports).

## Follow-Up

None.

## Correction (2026-08-12)

Running this command has a side effect: the `export_bindings_*` tests
regenerate `web-frontend/src/generated/*.ts` via ts-rs in unformatted shape
(double quotes, single-line unions). The committed bindings are
prettier-formatted (single quotes), so `git status` showed 38 modified
generated files after the run. All were restored with
`git checkout -- web-frontend/src/generated/`; both repositories are clean
again. The drift is cosmetic and belongs to the A-FE-01 frontend contract
scope; recorded here only to explain the transient dirty state.
