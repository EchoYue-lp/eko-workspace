# A-TSK-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

The file-backed `TaskRuntimeStore`, the framework `TaskRevisionService`
built over it, and the EKO registration/plan-commit entry points are live on
every EKO surface (TUI, GUI, Tauri commands, pooled agents, planning
service), and the framework revision path reaches the EKO file CAS.

## Method

Registration and runtime reachability trace (read-only greps + code trace):

```text
grep -rn "TaskRuntimeStore::new()\|TaskRuntimeStore::open()" echo-agent-cli/src/ echo-agent-cli/echo-agent-app-core/src/
grep -rn "register_task_tools_on_agent\|task_revision_service_for_agent\|bind_task_execute_to_pool" echo-agent-cli/src/ echo-agent-cli/echo-agent-app-core/src/
grep -rn "commit_eko_task_plan" echo-agent-cli/src/ echo-agent-cli/echo-agent-app-core/src/
grep -rn "recover_incomplete" echo-agent-cli/src/ echo-agent-cli/echo-agent-app-core/src/
```

Then traced `task_create/task_update` -> `TaskRevisionService` ->
`EkoRevisionedTaskStore::load/compare_and_commit` -> `TaskRuntimeStore`
`load_revisioned_task_graph` / `compare_and_commit_revisioned_task_graph`
(store.rs:676-885), and `commit_eko_task_plan` (revisioned_adapter.rs:344-388)
-> framework `create_prepared` -> same CAS (store.rs:755-885).

## Expected Result

One store per process, constructed at bootstrap, used by both GUI and TUI
entry points; the revision service and file CAS on the main tool path;
boot recovery live.

## Result

- Exit code: 0 (grep exit)
- Summary of live call sites:
  - `TaskRuntimeStore::new()`: `src/main.rs:37` (TUI bootstrap),
    `echo-agent-app-core/src/state.rs:548` (GUI runtime state),
    `echo-agent-app-core/src/tool_exposure.rs:306` (pooled-agent exposure).
  - `register_task_tools_on_agent`: `src/main.rs:177`, `src/tauri/desktop.rs:201`,
    `tool_exposure.rs:307`. `bind_task_execute_to_pool`: `main.rs:192`,
    `desktop.rs:217`. `task_revision_service_for_agent`:
    `src/tauri/commands/task_runtime.rs:406`.
  - `commit_eko_task_plan` (planner -> framework revision 1):
    `echo-agent-app-core/src/tasks/service.rs:332`.
  - `recover_incomplete` (boot recovery): `src/main.rs:52`,
    `state.rs:557`.
  - Framework tool path: `register_task_tools` registers
    `task_create/task_update/task_list` on the primary agent
    (register.rs:63-75) over the EKO-built service
    (`build_eko_task_revision_service`, revisioned_adapter.rs:309-317);
    both tools funnel through `TaskRevisionService` -> `EkoRevisionedTaskStore`
    -> `TaskRuntimeStore` CAS, which persists a `PlanRevisionCommitted`
    event and rebuilds the projections (store.rs:755-885).
  - Executor status writes all go through store methods
    (`claim_task`, `set_claimed_task_status`, `requeue_claimed_task`,
    `set_task_status` — executor.rs:551,651,1197,1254,1405,1571), never
    writing framework `TaskStatus` values directly.

## Deviations

None. Note: GUI and TUI each construct their own store over the same
default root (`~/.eko/tasks/`); the per-run write locks and seq cache are
per-process (cross-process edge recorded as A-TSK-01-P3-02).

## Conclusion

Supports: the file authority and the revisioned adapter are live on all EKO
surfaces; the CAS is on the main `task_create/task_update` path; boot
recovery is live. Does not prove behavior of the framework executor loop
(F-TSK-03) or the EKO controller boundary (A-TSK-03).

## Follow-Up

None.
