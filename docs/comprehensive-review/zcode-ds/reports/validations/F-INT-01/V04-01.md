# F-INT-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

`echo_agent` compiles with the `mcp` feature disabled (`--no-default-features`;
the crate's `[features] default = []`, so this is the plain no-feature build),
i.e. the MCP facade does not leak into other feature sets.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
CARGO_TARGET_DIR=/tmp/eko-nd-check cargo check -p echo_agent --no-default-features --locked
```

Fresh target directory to force a from-scratch compile (the default target dir
has cached artifacts that could mask stale fingerprints). Exit code captured
from the command itself.

## Expected Result

Exit 0. (Secondary expectation checked during review: `load_mcp_from_file` is
feature-gated — its `#[cfg(feature = "mcp")]` attribute sits at
`src/agent/react/capabilities.rs:1206` above its 34-line doc comment, so the
apparent "ungated method referencing gated items" is a false positive.)

## Result

- Exit code: 0
- Duration: 1m 06s (from-scratch compile)
- Summary: `Checking echo_core ... echo_agent` all succeeded; `Finished dev
  profile` with no warnings/errors. MCP-free feature set compiles cleanly.

## Deviations

None.

## Conclusion

Supports: no feature-isolation defect on the MCP facade (the suspected
`load_mcp_from_file` gate violation does not exist — attribute
`capabilities.rs:1206` gates the method). Combined with V04-03, both sides of
the mcp feature boundary compile.

## Follow-Up

None.
