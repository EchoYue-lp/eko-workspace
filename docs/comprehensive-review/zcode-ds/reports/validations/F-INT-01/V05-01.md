# F-INT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical audit/plan claims about MCP are classifiable against current code
as current, fixed, stale, or regressed.

## Method

Grep `-i mcp` in `echo-agent/AUDIT_REPORT.md` and `docs/MASTER-PLAN.md`; read
the matched sections in context; revalidate each claim against the code at
baseline commits (stdio EOF drain, error classification, annotations mapping,
retry contract, tests).

## Expected Result

Each claim gets a classification with current-code anchors.

## Result

- Exit code: not applicable (document comparison)
- Summary of classifications:
  1. AUDIT_REPORT.md 2.4 "MCP Stdio Transport Drops Pending Requests on
     Stdout Close" (MEDIUM) -> **fixed**: current `stdio.rs:124-143` drains
     the pending map and sends each waiter a typed error response
     (`-32000`, "MCP transport closed: stdout reached EOF"), so callers learn
     which request failed; the old `map.clear()` behavior is gone. The audit
     file still cites old line numbers (113-118).
  2. MASTER-PLAN Phase B acceptance "网络瞬断、MCP 重连…均有专项测试"
     (`docs/MASTER-PLAN.md:188`) -> **stale**: no reconnect/disconnect tests
     exist anywhere in `echo-integration/src/mcp` (V04-02 inventory); the
     acceptance claim is aspirational.
  3. MASTER-PLAN M4/line 532 "MCP 保留 JSON-RPC code，区分协议/参数错误与
     isError 工具执行错误；按 MCP read-only/destructive annotation 决定副作用
     风险" -> **current**: `client.rs:240-243` preserves code+message;
     `echo-core/src/tools/mod.rs:226-245` maps `-32602` to InvalidArguments
     and `-32603/-32099..-32000` to Transient+retryable with
     possible-side-effect conversion; `tool_adapter.rs:139-149` maps
     annotations to `ToolRiskLevel`; adapter `is_error` + structured content
     map to `StructuredError` (`tool_adapter.rs:172-181`).
  4. MASTER-PLAN line 184/M4 "有限重试…Browser/MCP 根据 tool contract 判断
     能否重试" -> **partially regressed on the HTTP transport**: the
     contract-level gate works at the adapter/manager layer
     (`allows_automatic_retry`), but `HttpTransport` re-POSTs on ambiguous
     failures (`http.rs:96-147,251-270`) before any side-effect
     classification, bypassing the contract (F-INT-01-P1-02); the SSE
     reconnect loop (`sse.rs:65-127`) is a hand-rolled retry not using
     `echo_core::retry::RetryPolicy` (same class as F-REL-01's unification
     finding).
  5. MASTER-PLAN line 570 (EKO startup must not block MCP on sandbox
     absence) -> EKO product policy, out of framework scope; current code
     (`infra.rs:1091-1103`) loads MCP config with warn-only failure.
  6. MASTER-PLAN line 997 "MCP 暂不 disconnect(无 per-server API…)" ->
     **fixed/current**: `disconnect_mcp` per-server API now exists
     (`capabilities.rs:1315-1331`) and plugin unload uses it
     (`echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs:1173`).

## Deviations

None.

## Conclusion

Supports: the historical stdio-disconnect finding is fixed; the M4 error/
annotation contract is implemented; Phase B reconnect-test acceptance and the
unified-retry contract are not met for MCP transports. These feed the task
report's Historical Claim Status table.

## Follow-Up

None.
