# F-INT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every MCP concept (client/manager/server/transport/tool adapter/config/schema
types) has exactly one authoritative definition in `echo-agent`, the root
facade is a thin re-export, and `echo-agent-cli` contains no parallel MCP
protocol implementation.

## Method

Grep-based inventory across both repositories (working dir
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`), excluding `target`:

1. Type definitions: `struct McpManager|struct McpClient|struct McpTool\b|
   struct McpToolAdapter|struct McpServer|struct StdioTransport|
   struct SseTransport|struct HttpTransport|trait McpTransport`.
2. JSON-RPC wire types: `JsonRpcRequest|JsonRpcResponse|JsonRpcNotification|
   NotificationReceiver` outside `echo-integration/src/mcp`.
3. Naming/schema conventions: `mcp__` prefix, `mcpServers`, `McpConfigFile`,
   `connect_mcp*`, `get_all_tools`, `refresh_tools`, `McpServerInfo`.
4. Module declarations: `mod mcp` and facade re-exports (`src/mcp.rs`,
   `echo-integration/src/lib.rs`, `src/lib.rs` advanced exports).
5. `echo-agent-cli` MCP consumers (`browser/mod.rs`, `tauri/commands/mcp.rs`,
   `state.rs`, `infra.rs`, `tui/events.rs`, `plugin_runtime.rs`).

## Expected Result

One authoritative MCP client/manager/server/tool-adapter implementation in
`echo-integration/src/mcp`; root `src/mcp.rs` contains only re-exports; the CLI
consumes framework types and defines only thin DTO projections
(`McpServerInfo`, `McpTransportConfig`).

## Result

- Exit code: 0 (grep inventory, not applicable as a test)
- Summary: Single authoritative definitions found:
  `echo-integration/src/mcp/{mod.rs,client.rs,server.rs,server_config.rs,
  config_loader.rs,tool_adapter.rs,types.rs,transport/{mod,stdio,http,sse}.rs}`.
  Root facade `echo-agent/src/mcp.rs:7-18` is a pure re-export module; the
  `mcp` feature gates `pub mod mcp` (`src/lib.rs:88-91`,
  `echo-integration/src/lib.rs:16-19`). CLI side: `browser/mod.rs:16,45,258`
  reuses the framework `McpClient`; `tauri/commands/mcp.rs` builds framework
  `McpServerConfig` values and calls `agent.connect_mcp_from_config`;
  `types/response.rs:59` (`McpServerInfo`) and `types/request.rs:35`
  (`McpTransportConfig`) are serialization projections. Zero `JsonRpc*`
  definitions outside `echo-integration/src/mcp`. Zero parallel transport or
  adapter implementations in `echo-agent-cli`.
- Intentional same-name pairs: none for MCP (EKO `McpServerInfo` vs framework
  `McpServer` are distinct concepts; no semantic overlap).

## Deviations

None for the duplicate-search claim. Noted, not findings:
- `echo-agent/src/agent/react/subsystems/tool_exec.rs:74-77` contains a dead
  `mcp_manager_arc()` stub returning `None` (see F-INT-01-P3-04).
- `McpManager::get_all_tools` (`mod.rs:126-139`) has no callers in either
  repository (retained public framework API, not a duplicate).
- The EKO browser module implements its own retry-on-restart policy
  (`browser/mod.rs:875-911`) on top of the framework client — EKO product
  policy, out of framework scope (A-INT-01).

## Conclusion

Supports: single-authority MCP implementation in `echo-integration`, thin root
facade, no parallel client/transport/schema implementation in the CLI. Does
not prove runtime reachability (see V02-01) or behavioral correctness (see
V03-01, V04-*).

## Follow-Up

None.
