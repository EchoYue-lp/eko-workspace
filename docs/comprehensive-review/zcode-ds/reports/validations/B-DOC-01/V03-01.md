# B-DOC-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-DOC-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Obsolete terms and paths documented as removed are actually gone: `worker`
terminology (Subagent-only rule), `plan_create`/`plan_execute`/EKO `run_dag`
loops, SQLite in CLI source, `echo-agent-eval` directory, and the web-mode
server path.

## Method

1. `grep -rni "worker"` across `echo-agent/echo-orchestration/src`,
   `echo-agent/echo-execution/src`, `echo-agent/src/agent/subagent/`,
   `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/`.
2. `grep -rn "plan_create\|plan_execute\|run_dag"` in
   `echo-agent-cli/echo-agent-app-core/src/tasks/`.
3. `grep -rni "sqlite"` in `echo-agent-cli/src` and
   `echo-agent-cli/echo-agent-app-core/src`.
4. Directory checks for `echo-agent-eval` / `evals` in the CLI repo.
5. Web-mode server search: `run_web`, `web_server`, `0\.0\.0\.0` in
   `echo-agent-cli/src` and app-core, plus the `--web` dispatch behavior in
   `src/main.rs`.

## Expected Result

Zero `worker`; zero `plan_create`/`plan_execute`/`run_dag`; zero `sqlite`;
no `echo-agent-eval` dir; no live web server path.

## Result

- `worker`: zero hits in framework orchestration/execution/subagent and in
  app-core TaskRuntime. (Also zero in `src/agent/subagent/` earlier check.)
- `plan_create`/`plan_execute`/`run_dag`: zero hits in app-core tasks (the
  only nearby hit is a test fn name `attach_plan_creates_tasks_and_todos`
  in `store.rs:2327`, not a CRUD tool).
- `sqlite` in CLI `src/` and app-core `src/`: zero hits.
- `echo-agent-eval`: no directory; only an empty `echo-agent-cli/evals/`
  remains (stale claim in AGENTS.md:139, already recorded as B-BASE-01
  finding).
- Web mode: `src/main.rs:351-353` hard-exits with "Web 模式已移除" for
  `--web`; no `run_web`/`web_server` function exists in `src/` or app-core;
  the only remnant is the legacy-named config struct `WebConfig`
  (`echo-agent-app-core/src/state.rs:34,42,340`, `AppState.config.web_config`)
  which is constructed in `from_shared` (`state.rs:460-467`) but has no
  server behind it.

## Deviations

None. (`WebConfig` is a naming remnant, not a live server — recorded as a
P3 finding in the task report.)

## Conclusion

Supports: terminology convergence is complete for the searched terms;
obsolete paths are gone; the web server path is dead code with a hard-exit
front door.

Does not prove: `worker`-term absence in the frontend and in documents
(X-INV-01 owns the repository-wide invariant audit), or the absence of other
legacy names beyond the searched set.

## Follow-Up

X-INV-01 should extend this search to `web-frontend` and all documents;
A-SRF-04 owns the `--web`/`--port`/`--host` dead-argument cleanup.
