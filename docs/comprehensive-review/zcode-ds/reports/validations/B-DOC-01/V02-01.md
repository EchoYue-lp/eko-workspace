# B-DOC-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-DOC-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The "completed milestone" claims in both MASTER-PLAN documents (root
`docs/MASTER-PLAN.md` and `echo-agent-cli/docs/MASTER-PLAN.md`) still point
at existing code: `drive_chat` over `PreparedUserTurn`, file-backed stores in
the framework, one framework DAG executor with an EKO controller, removed
`todo_write`, removed app-core task tools, `run_code` minimum-isolation, and
EKO eval/improve not enabled.

## Method

Sampled 10 milestone claims and checked each against current code at the
reviewed commits:

1. `drive_chat` takes `&PreparedUserTurn`; used by TUI and GUI.
2. `FileRuntimeStateStore` lives in the framework, not app-core.
3. `FileConversationStore` and `restore_messages` live in the framework.
4. `RuntimeDagExecutor` exists in `echo_orchestration`.
5. `EkoRuntimeDagController` exists in app-core.
6. `todo_write` is removed from production code.
7. App-core `TaskCreateTool`/`TaskUpdateTool`/`TaskListTool` are gone.
8. `run_code` has no bare fallback (minimum `OsSandbox`).
9. EKO tool-output artifact config exists (`tool_output_artifact_config`).
10. EKO does not enable framework `eval`/`improve` features.

## Expected Result

All 10 anchors resolve.

## Result

1. `chat_driver.rs:202-204` — `drive_chat` signature includes
   `turn: &crate::prepared_turn::PreparedUserTurn`; called from
   `src/tui/mod.rs`, `src/tui/events.rs`, `src/tauri/commands/chat.rs`.
2. `echo-agent/src/state/file.rs:34` — `pub struct FileRuntimeStateStore`
   (framework root crate); app-core `infra.rs:1256` uses
   `echo_agent::state::FileRuntimeStateStore::new`.
3. `echo-agent/echo-state/src/memory/file_conversation.rs:68` and
   `conversation.rs:197` (`pub fn restore_messages`); app-core
   `infra.rs:1219` uses `echo_agent::memory::FileConversationStore::new`.
4. `echo-agent/echo-orchestration/src/tasks/runtime_executor.rs` —
   `RuntimeDagExecutor` (also re-exported in `tasks/mod.rs`, `executor.rs`).
5. `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs`
   contains `EkoRuntimeDagController`.
6. `todo_write`: the only occurrence in both repositories is a *negative*
   assertion in a framework test (`echo-agent/src/agent/react/builder.rs:1181`:
   `assert!(!names.iter().any(|name| name == "todo_write"))`).
7. `struct TaskCreateTool|TaskUpdateTool` in app-core: zero hits.
8. `echo-tools/src/code.rs:313` — `with_minimum_isolation(IsolationLevel::OsSandbox)`;
   `:324` — guard rejects isolation below `OsSandbox`.
9. `echo-agent-cli/echo-agent-app-core/src/infra.rs:61-70` —
   `tool_output_artifact_config` (common artifact root, `user-input/` subtree
   documented).
10. B-BASE-01 V02 evidence: neither CLI workspace member enables
    `eval`/`improve` on `echo_agent` (root: mcp/lsp/human-loop/subagent/tasks;
    app-core: …/research/media/rag).

## Deviations

None.

## Conclusion

Supports: the sampled MASTER-PLAN milestone claims are `current` — the
documented convergences (drive_chat, framework file stores, one DAG
executor, removed todo_write/app-core task tools, run_code isolation) are
real in the code at the reviewed commits.

Does not prove: claims not sampled (e.g., plugin lifecycle details,
worktree identity, frontend projections) — those are covered by the A-* and
X-* tasks.

## Follow-Up

Sampled claims that are verified here can be trusted by downstream tasks;
the remaining MASTER-PLAN claims are covered by their owning A-*/F-*/X-*
tasks.
