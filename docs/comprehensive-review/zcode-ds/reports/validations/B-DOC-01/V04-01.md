# B-DOC-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-DOC-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Every historical finding in `AUDIT_REPORT.md` is classified
(current/fixed/stale) with evidence, and no finding is carried forward
unclassified.

## Method

1. Enumerated all 25 numbered findings (4 Critical, 8 High, 12 Medium, 1
   Low-sectioned 2.9) plus §6.1/§6.2/§6.3 quality items from AUDIT_REPORT.md.
2. For each, applied the classification rules from REPORTING.md using the
   V01 evidence.

## Result

Classification index (anchors in V01):

**fixed (19):** 1.1 SQL filter (DB READ ONLY), 1.2 SSRF TOCTOU (pinned IPs),
1.3 Seatbelt injection (path validation), 1.5 WS HITL auth (UUID token),
1.6 file symlink (canonicalize), 1.7 image-fetch SSRF (ssrf_safe_get), 1.8
plugin git URL (https/ssh only), 1.10 A2A serve (warning + serve_with_auth),
1.11 git `--` separators, 1.12 DB URL exact scheme, 2.1 notebook poison
recovery, 2.2 prompt_template expects, 2.3 team semaphore expect, 2.4 MCP
stdio per-request errors, 2.5 redact char boundaries, 2.6 prompt_exec
LazyLock, 2.7 eval clone/checkout errors, 2.9 process-group kill warn, 3.1
private ranges.

**current (6):** 1.4 eval `sh -c` unsanitized command (runner.rs:695-704;
mitigated only by repo_url https check at :340 — eval-data trust question),
1.9 production `unsafe { set_var }` in echo-core plugin variables
(variables.rs:179-190, acknowledged in-comment; providers/config.rs
EnvGuard is test-only and mutex-guarded), 2.8 hand-rolled `parse_method`
(serve.rs:296-320; now proptest-covered at :399-405), 3.2 fallback
`Client::new()` (fetch.rs:44-46), 6.1 security.rs TODOs (:3,:9), 6.2
dead-code annotations (image_fetch/web_fetch_enhanced/git).

**stale (1):** 6.3 missing error context — referenced `src/improve/store.rs`
and `src/improve/evolution.rs` no longer exist; the improve module was
restructured (analyzer/eval_improvement/generator/loop/mod/trajectory).
Current error handling there is F-EVO-01's question.

Not re-audited here (deferred by design): §5 dependency analysis
(Q-DEP-01), §6.4 unwrap distribution (Q-STA-01), §7 positive observations
(used as sanity signals only).

## Deviations

None.

## Conclusion

Supports: the unresolved-finding index. All CRITICAL items except the
eval-runner command path are fixed; the remaining open items are lower
severity or trust-boundary questions that F-SEC-01 must re-rate under the
local threat model.

Does not prove: absence of regression in the fixes (Q-STA-01/Q-FLT-01), or
that the framework's dependency claims are current (Q-DEP-01).

## Follow-Up

F-SEC-01 re-rates the 6 current items; F-EVO-01 reviews the restructured
improve module; Q-DEP-01 and Q-STA-01 own the deferred sections.
