# F-TSK-03 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

EKO's claim/retry/recovery store tests — stale-claim rejection,
claim-vs-revision race, boot recovery of orphaned Running tasks, and
execution identity per changed spec — compile and pass.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked "task_runtime::store::tests::claim"
cargo test -p echo-agent-app-core --lib --locked "stale_claim_cannot_overwrite_cancelled_task"
cargo test -p echo-agent-app-core --lib --locked "boot_recovery"
cargo test -p echo-agent-app-core --lib --locked "patched_spec_uses_new_execution_identity_without_retry_bump"
```

Environment: macOS arm64, default features (no SQLite), `--locked`.

## Expected Result

Exit code 0 for every command; all tests pass.

## Result

- Exit code: 0 (all four commands)
- Duration: < 1 min
- Summary:
  - `claim_reloads_when_task_update_wins_revision_race`: 1 passed.
  - `stale_claim_cannot_overwrite_cancelled_task`: 1 passed (stale
    claim returns `Superseded`, cannot overwrite a Cancelled task).
  - `boot_recovery` (incl. `auto_resume_only_accepts_boot_recovery_pause`):
    4 passed (Running run -> Paused at boot; orphaned Running task
    requeued to Pending; completed durable Subagent result reused
    without redispatch).
  - `patched_spec_uses_new_execution_identity_without_retry_bump`:
    1 passed.
- Log/artifact: standard cargo output, not persisted.

## Deviations

None.

## Conclusion

Supports: the CAS claim discipline (revision race -> ReloadSnapshot;
stale claim -> Superseded), the boot-time recovery of orphaned Running
claims, and attempt-identity separation on spec change. These are the
fixtures named by the convergence doc verification list and the task
card's "stale claim/attempt" scenario. Does not cover a claim-error
mid-wave scenario (the P2-01 trigger — no such test exists).

## Follow-Up

Q-FLT-02: claim-error-mid-wave fixture (fails today per F-TSK-03-P2-01).
