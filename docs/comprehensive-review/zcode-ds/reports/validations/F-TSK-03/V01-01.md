# F-TSK-03 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

There is exactly one live DAG-execution authority (`RuntimeDagExecutor`)
with no second ready-frontier / retry / stall loop in either repository;
no `worker` terminology exists; and the legacy `hooks`/`verifier`/
`TaskExecutor` machinery plus the per-task cancel trigger have no
production callers.

## Method

Repository-wide grep (both repos, `--include="*.rs"`, `target/` excluded):

```text
cd echo-agent
grep -rn "RuntimeDagExecutor|RuntimeDagController|claim_task|TaskClaim|RuntimeTaskClaimOutcome|RuntimeTaskResolution" --include="*.rs" .
grep -rn "execute_all|execute_all_async|execute_ready_tasks|get_ready_tasks|get_next_task|get_dependency_chain_recursive" --include="*.rs" .
grep -rn "TaskExecutor::new|TaskExecutor {" --include="*.rs" .
grep -rn "Retrying" --include="*.rs" .
grep -rn "ManagedTask|VerifierFactory|CommandVerifier|TaskHookRegistry|RetryDecision|TaskHooks" --include="*.rs" .
grep -rn "create_task_hook_bridge|TaskHookBridge" --include="*.rs" .
grep -rniE "\bworker\b" --include="*.rs" echo-orchestration/src src echo-core/src
grep -rn "stalled|note_stalled|DAG stalled" --include="*.rs" .
cd echo-agent-cli
grep -rn "TaskClaim|claim_task|RuntimeDagExecutor|RuntimeDagController|RuntimeTaskResolution" --include="*.rs" echo-agent-app-core/src/tasks
grep -rn "\.cancel_task(" --include="*.rs" echo-agent-app-core/src src
grep -rn "failure_fingerprint|Retrying|TimedOut" --include="*.rs" echo-agent-app-core/src
```

Terms searched across both repos: `RuntimeDagExecutor`, `RuntimeDagController`,
`claim_task`, `TaskClaim`, `RuntimeTaskClaimOutcome`, `RuntimeTaskResolution`,
`note_stalled`, `stalled`, `DAG stalled`, `ready_task_ids`, `select_ready_wave`,
`external_progress_poll_interval`, `cancellation_grace`, `Retrying`,
`retry_count`, `requeue`, `refresh_in_flight`, `DagRefresh`, `get_ready_tasks`,
`execute_ready_tasks`, `execute_all`, `worker`, `TaskHooks`, `VerifierFactory`,
`ManagedTask`, `RetryDecision`, `execution_mode`, `max_concurrent_subagents`.

## Expected Result

- One production `RuntimeDagExecutor::execute` path; legacy
  `execute_ready_tasks`/`get_ready_tasks`/`execute_all` used only inside
  their defining module and `#[cfg(test)]`.
- Zero `worker` matches in production code.
- Zero production callers of the `ManagedTask` verifier family,
  `TaskHookBridge`, and EKO `cancel_task`.
- `TaskStatus::Retrying` produced only by the legacy executor.

## Result

- Exit code: not-applicable (static grep)
- Duration: < 1 min
- Summary:
  - `RuntimeDagExecutor` is constructed at `runtime_executor.rs:182` and
    used by EKO `execute_runtime_plan` (`executor.rs:1645-1654`) and by
    the legacy `TaskExecutor::execute_all` (`executor.rs:1422-1442`, zero
    production callers — only `#[cfg(test)]` constructions at
    executor.rs:1885-2518 and mod.rs tests).
  - `execute_ready_tasks`/`get_ready_tasks`/`get_next_task`/
    `get_dependency_chain_recursive`: only the defining modules, module
    tests (mod.rs:312-338), and `#[cfg(test)]` tests. Zero production
    callers in either repository.
  - EKO drain loop (`executor.rs:366-434`) is a completion gate — no
    second frontier/retry/stall computation; EKO retry is
    `requeue_claimed_task` -> `Pending` (single store write).
  - Zero `worker` matches in `echo-agent` (echo-orchestration/src, src,
    echo-core/src) and `echo-agent-cli` (echo-agent-app-core/src).
  - Verifier family (`VerifierFactory`/`CommandVerifier`/`FileExistsVerifier`/
    `DiffCheckVerifier`/`TestVerifier`/`HumanReviewVerifier`/`LlmReviewVerifier`/
    `NoopVerifier`): re-exported at `tasks/mod.rs:77-78`; zero production
    callers (self-tests only). `TaskHooks`/`TaskHookRegistry`/`RetryDecision`:
    used by legacy `manager.rs`/`executor.rs` (production-unreachable) and
    re-exported; `TaskHookBridge` constructed at
    `react/capabilities.rs:1098-1117` with zero callers outside itself.
  - `TaskStatus::Retrying` producers: only legacy `executor.rs:1193`;
    EKO never produces it (`types.rs` maps it only for projection).
  - EKO `TaskRuntimeStore::cancel_task` (store.rs:515-531): zero callers
    in `echo-agent-app-core/src` and `src`.

## Deviations

None.

## Conclusion

Supports: single live execution authority; legacy machinery
production-unreachable (confirms F-TSK-01-P3-01 and the convergence doc
Phase-3 "retained framework capability" claim); zero worker terminology;
per-task cancel trigger dead; `Retrying` variant dead in the live path.
Does not prove behavior correctness of any path (see V03/V04).

## Follow-Up

None.
