# F-EVO-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Eval/improve/evolution side effects are explicit and bounded; mutations go
through review/security/audit boundaries; the framework surface carries no EKO
product-policy coupling.

## Method

Static inspection (working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`,
plus `echo-agent-cli` where noted):

```text
1. src/eval/runner.rs — side-effect inventory: create_dir_all (workspace_root:28),
   fixture copy (setup_fixture ~:172-186, copy_dir :740-748), git clone/checkout/apply
   for SweBench (:362,:388,:413), sh -c test commands (run_command :695-702,
   TestPass :227, SweBench :438), agent.execute with timeout (:70-85)
2. Command sanitization: validate_shell_command (:681-692) — callers SweBench only (:350);
   TestPass branch (:227) NOT validated
3. src/eval/comparator.rs — AbComparator::compare (pub, zero consumers/tests)
4. src/evolution/layer.rs:845-869 — write_memory enforces EvolutionSecurityGuard
   (deny → Err), uses sanitized content, records change (:869)
5. src/evolution/audit.rs — JsonlChangeLog append+sync_all (:256), corrupt lines
   skipped with warn (:209-240)
6. src/memory_promoter.rs:70-94 — StoreMemoryPromoter writes via
   TypedMemoryStore::put_typed DIRECTLY (no MemoryLayerManager, no change log,
   no security guard); invoked from echo-state/src/compression/mod.rs:836-838
   on every compression; contrast audited paths: tools/builtin/memory.rs:222,
   react/run/context.rs:101,:794, evolution/auto_memory.rs:349-379, dreaming.rs,
   background_review.rs:502 (all via layer_manager.write_memory)
7. Mutation/review boundaries: background_review.rs:478-498 (auto-persist only when
   config flag + UserPreference + confidence>=0.95, written as MemoryStatus::Draft);
   EKO rule promotion user-triggered only (src/cli/cmd_impls/evolution.rs:1415,
   src/tauri/commands/panels.rs:1525 promote_rule); apply_transitions called only
   from example demo51; Curator auto-touch limited to telemetry (run/execution.rs:127-137)
8. EKO coupling check: grep eko/echo-agent-cli strings in src/eval|evolution|improve → zero;
   paths.rs exposes set_user_data_dir_name(".eko") as the documented brand-root override
9. Panic/UTF-8 safety scan of reviewed files (AGENTS.md rules)
```

## Expected Result

Every mutation path is either user-gated or passes security+audit; command
execution is consistently validated or documented as trusted; no hidden metric
loop or automatic semantic mutation without a boundary; no EKO strings in
framework modules.

## Result

- Exit code: not applicable (static)
- Summary:
  - Eval side effects are explicit and documented: workspace fixture
    creation/copy, git operations, `sh -c` test commands, agent execution with
    timeout; criteria without a trace fail with explicit violations
    (runner.rs:258-280, "Cannot verify required tool without trace").
  - Evolution write path is audited + security-scanned for all
    layer-manager-mediated writes; audit is append-only with sync and explicit
    corrupt-line warnings.
  - Two contract violations found (become findings):
    (a) `SuccessCriteria::TestPass` executes `sh -c` without
    `validate_shell_command` while the SweBench branch is validated
    (runner.rs:227 vs :350) — inconsistent command-safety contract;
    (b) `StoreMemoryPromoter` (L3 promotion, main path) writes typed memories
    directly to the Store, bypassing the change log and the security guard,
    contradicting the documented invariant "All mutations to memories, skills,
    and rules are recorded in the audit log" (evolution/mod.rs:20-22).
  - Mutation/review boundaries hold elsewhere: background auto-persist is
    opt-in + high-confidence + Draft status; rule promotion is
    user-triggered on both GUI and CLI; curator lifecycle transitions are not
    auto-applied in the main path; Dreaming promotion is bounded
    (max_promoted_per_run=20) and deterministic.
  - No EKO coupling in framework modules (grep step 8: zero hits).
  - Panic/UTF-8 scan: reviewed code uses `unwrap_or`/`unwrap_or_else`/
    `is_some_and`/chars-iteration; `unwrap_or(false)` at runner.rs:702 and
    `unwrap_or_else(|| self.workspace_root.clone())` are safe; no byte-slice
    truncation on untrusted text (replay/report/security use chars-based
    truncation); no reachable panic paths found.
- Log/artifact: none

## Deviations

None beyond the two violations recorded as findings
(F-EVO-01-P2-01, F-EVO-01-P2-02).

## Conclusion

Supports: side effects are explicit and bounded; mutation/review/security
boundaries hold for the audited paths; no EKO product-policy coupling. Does not
prove the two violations' runtime impact (static evidence; deterministic
behavior exercised by existing tests in V04).

## Follow-Up

V04-01..06 (test/compile evidence), findings P2-01/P2-02.
