# F-EVO-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Under feature gating, eval/improve/evolution are registered and reachable:
`eval`/`improve` are feature-gated optional modules whose option value is
independent of EKO CLI usage; `evolution` is always compiled and wired into the
framework main runtime path; per AGENTS.md deletion rules none is "dead code"
merely because a consumer does not call it.

## Method

Static source tracing (working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`):

```text
1. src/lib.rs:36-45 — #[cfg(feature="eval")] pub mod eval; #[cfg(feature="improve")] pub mod improve; ungated pub mod evolution;
2. Cargo.toml:95-96 — eval = [], improve = []; :57 docs.rs metadata includes eval/improve; full includes both (:67)
3. src/eval usage: only src/improve/{analyzer,eval_improvement,loop}.rs + examples/demo50_eval.rs, demo51_self_improvement.rs
4. src/improve usage: zero cross-module consumers in src/ (mod.rs gates submodules on #[cfg(feature="eval")])
5. src/evolution usage in main path: src/agent/react/mod.rs:235-264 (state fields), :755-778 (layer-manager/trigger-sink wiring), :1076/:1139 (WARM_NAMESPACE), src/agent/react/run/execution.rs:54-128 (tool sequence/success/failure records, Curator telemetry), run/context.rs:61-101 (TriggerDetector capture), :273 (MemoryRecaller), src/tools/builtin/memory.rs:14,222 (layered memory tools), src/memory_promoter.rs, src/agent/snapshot.rs:401-405 (snapshot persistence)
6. echo-agent-cli consumption: agent_pool.rs:662,916; state.rs:959,1111; infra.rs:1205-1213; run_driver.rs:25; runtime.rs:23 (evolution only)
7. Feature isolation compile matrix — see V04-05-02 and V04-06
```

## Expected Result

`eval`/`improve` compile standalone and together; examples with matching
`required-features` compile; `evolution` compiles with zero features and is
reachable from agent construction/run paths.

## Result

- Exit code: not applicable (static; compile evidence in V04-05-02/V04-06)
- Summary:
  - `eval` (feature gate): registered via `src/lib.rs:36-38`; reachable via
    `EvalRunner`/`EvalCase`/`SuccessCriteria` types, `examples/demo50_eval.rs`
    (required-features `["eval"]`, Cargo.toml:304-305) and the eval-driven
    improvement loop (`src/improve/loop.rs` etc., compiled only with both
    features). Its option value does not depend on EKO: EKO has no eval
    consumer; the API is exercised by examples, tests, and the improve loop.
  - `improve` (feature gate): registered via `src/lib.rs:43-45`; `improve`
    alone exposes `TrajectorySaver` only; `+eval` adds Analyzer/
    PromptGenerator/ImprovementLoop/EvalDrivenImprovement (`src/improve/mod.rs:42-49`).
    Zero EKO consumers — capability menu.
  - `evolution` (always compiled): main-path reachability proven —
    auto-memory wiring at `src/agent/react/mod.rs:755-778`, memory promotion
    during compression via `StoreMemoryPromoter` (react/mod.rs:1033,1047,
    1123,1177 → echo-state/src/compression/mod.rs:836-838), trigger capture at
    run/context.rs:61-101, curator telemetry at run/execution.rs:127-137,
    snapshot persistence (snapshot.rs:401-405). EKO also consumes it directly.
  - Not "dead code": eval/improve/evolution are documented pub APIs with
    doctests/examples (README + docs/en|zh/24-eval-system.md,
    25-self-improvement.md), feature-gated, exercised by tests and examples.
    Exception noted as finding: `AbComparator` (exported, zero consumers,
    zero tests — F-EVO-01-P3-01).
- Log/artifact: none

## Deviations

None. The `eval = []` / `improve = []` empty-marker features do gate real code
at `src/lib.rs` (unlike the six no-op markers of F-FEAT-01-P2-02).

## Conclusion

Supports: eval/improve/evolution are valid optional framework APIs, correctly
feature-gated or always-compiled, reachable independent of CLI use; evolution
is additionally a first-class main-path capability. Does not prove behavioral
correctness (V03-01/V04).

## Follow-Up

V03-01 (side effects / mutation-review boundaries), V04 test runs.
