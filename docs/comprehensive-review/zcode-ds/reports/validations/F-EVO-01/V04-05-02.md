# F-EVO-01 / V04 / Attempt 05 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The eval/improve feature-isolation compile matrix passes: no-default-features
with no features, `eval` only, `improve` only, and `eval,improve` all compile.

## Method

Working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent` (one
command per feature set, correct quoting):

```text
cargo check -p echo_agent --no-default-features --locked
cargo check -p echo_agent --no-default-features --features eval --locked
cargo check -p echo_agent --no-default-features --features improve --locked
cargo check -p echo_agent --no-default-features --features eval,improve --locked
```

## Expected Result

Exit 0 for all four invocations. `improve` alone must compile only the
trajectory surface (eval-gated submodules excluded); `eval,improve` must
compile the full eval-driven improvement set; zero-features must compile
`evolution`.

## Result

- Exit code: 0, 0, 0, 0
- Duration: 0.33s / 14.87s / 46.16s / 2m 06s
- Summary: all four configurations compile cleanly. Logs:
  `/tmp/evochk1.log` (eval-only), `/tmp/evochk2.log` (improve-only),
  `/tmp/evochk3.log` (eval+improve).
- Log/artifact: /tmp/evochk1.log, /tmp/evochk2.log, /tmp/evochk3.log

## Deviations

None.

## Conclusion

Supports: feature isolation is real — `eval`/`improve` gate exactly their
modules (improve-without-eval compiles, so the `#[cfg(feature = "eval")]`
submodule gating in `src/improve/mod.rs:42-49` holds), and `evolution` builds
with zero features. Combined with V04-06, the API option value is independent
of any consumer feature combination.

## Follow-Up

V04-06 (examples compile).
