# F-EVO-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The `evolution` module's deterministic fixture tests pass under `--features eval,improve`.

## Method

Working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`:

```text
cargo test -p echo_agent --features eval,improve --lib evolution --locked
```

## Expected Result

Exit 0; all evolution tests pass (layer transitions, audit, security guard,
curator lifecycle, triggers, dreaming, merge, review, runtime integration).

## Result

- Exit code: 0
- Duration: ~0.30s (incremental compile already warm from V04-01)
- Summary: `test result: ok. 144 passed; 0 failed; 0 ignored; 327 filtered out`
  covering `evolution::security`, `evolution::layer` (incl.
  `test_concurrent_add_to_hot_does_not_lose_entries`,
  `test_write_memory_and_consider_promotion`), `evolution::triggers`,
  `evolution::curator` (incl. `test_full_lifecycle`), `evolution::auto_memory`,
  `evolution::review`, `evolution::merge`, `evolution::patch`, `evolution::health`,
  `evolution::candidate`, `evolution::background_review`, `evolution::dreaming`,
  `evolution::draft`, `evolution::audit`, and
  `evolution::runtime_integration::builder_wires_typed_observer_and_change_log`.
- Log/artifact: none

## Deviations

None.

## Conclusion

Supports: the evolution layer's mutation/review/audit/security machinery is
deterministic and green under fixtures (concurrency, lifecycle transitions,
security verdicts, change-log wiring). Does not prove main-path behavior with a
live React agent (covered by V04-04 for the promoter and by runtime wiring
evidence in V02-01).

## Follow-Up

Sibling V04 attempts 03-06.
