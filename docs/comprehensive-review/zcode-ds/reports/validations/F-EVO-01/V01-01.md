# F-EVO-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The eval/improve/evolution concepts have exactly one authoritative definition
each in `echo-agent` (framework), no parallel implementation in
`echo-agent-cli` (EKO), and no concept referenced in code that does not exist.

## Method

Repository-wide greps across both repositories (working dir
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`), excluding `target/`:

```text
1. grep -rniE "evolution|eval" --include="*.rs" -l echo-agent echo-agent-cli (file census)
2. grep -rn "improve" --include="*.rs" -l echo-agent (file census)
3. grep -rn "Curator|Dreaming|SkillCandidateDetector|TrajectorySaver|AbComparator|TriggerAccuracy|RegressionSuite|LlmGrader|TrajectoryReplay|generate_html|generate_review_html|CritiqueStore" --include="*.rs" across both repos (concept overlap search)
4. grep -rln "echo-agent-eval|echo_agent_eval" --include="*.toml" --include="*.rs" --include="*.md" . (removed-crate references)
5. ls echo-agent-cli; sed -n '1,60p' echo-agent-cli/Cargo.toml (workspace members)
6. grep -rn "echo_agent::eval|echo_agent::improve|echo_agent::evolution" --include="*.rs" echo-agent-cli (framework-API consumers)
```

## Expected Result

Single framework definitions in `echo-agent/src/{eval,improve,evolution}`;
EKO consumes framework APIs without re-implementing them; no code reference to
a nonexistent type; workspace member inventory matches reality.

## Result

- Exit code: not applicable (static inspection)
- Summary:
  - Framework definitions: `src/eval/` (9 files), `src/improve/` (6 files),
    `src/evolution/` (17 files) — single authorities. Related-but-distinct:
    `src/agent/critic` (per-turn agent feedback tools) and EKO task-review
    (`echo-agent-app-core/src/tasks/task_runtime/review.rs`) — different
    semantics, not duplicates of eval.
  - EKO side: `echo-agent-app-core/src/evolution/` (dashboard, evidence,
    hook_fire, review_integration, rule_promoter) is a product adapter over
    framework APIs; `src/cli/cmd_impls/evolution.rs` (1850 lines) registers
    user slash commands. EKO consumes `echo_agent::evolution::*` in 15+ sites
    (agent_pool.rs:662,916; state.rs:959,1111; infra.rs:1205; run_driver.rs:25;
    runtime.rs:23; memory_bridge.rs; executor.rs; rule_promoter.rs; etc.).
  - EKO consumes `echo_agent::eval` / `echo_agent::improve`: ZERO sites
    (grep step 6).
  - `echo-agent-eval` crate does NOT exist: `echo-agent-cli/Cargo.toml`
    workspace `members = ["echo-agent-app-core"]`; `evals/` directory is
    empty. References remain only in docs (AGENTS.md:139,370;
    docs/superpowers/plans/2026-07-10-subagent-parity-roadmap.md:74,699;
    echo-agent-cli/docs/system-deep-dive/06-skills.md:427 documents the
    removal). See V05-01.
  - `CritiqueStore` (referenced by `echo-agent/README.md:1109`) has zero code
    references.
  - `AbComparator` (src/eval/comparator.rs:34, re-exported at
    src/eval/mod.rs:40) has zero consumers repo-wide (no tests, no examples,
    no framework or EKO call sites).
  - No second Curator/Dreaming/Reviewer/promoter implementation in either
    repository.
- Log/artifact: none

## Deviations

None beyond the documented stale references (handled as findings
F-EVO-01-P3-01/P3-03 and V05-01).

## Conclusion

Supports: single authoritative definitions for eval/improve/evolution in the
framework; EKO evolution is a thin product adapter, not a duplicate authority;
the historical `echo-agent-eval` crate premise in the task briefing is stale
(it was removed — 06-skills.md:427). Does not prove runtime reachability
(that is V02-01).

## Follow-Up

V02-01 (registration/reachability), V05-01 (doc drift classification).
