# F-EVO-01 / V04 / Attempt 05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The eval/improve feature-isolation compile matrix passes: no-default-features
with no features, `eval` only, `improve` only, and `eval,improve` all compile.

## Method

Working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`:

```text
for f in "eval" "improve" "eval,improve" ""; do
  cargo check -p echo_agent --no-default-features ${f:+--features "$f"} --locked
done
```

## Expected Result

Exit 0 for all four invocations.

## Result

- Exit code: 1 (three invocations), 0 (no-features invocation)
- Duration: seconds (no compilation started for the failing invocations)
- Summary: the three feature-carrying invocations printed
  `Usage: cargo check --package [<SPEC>] --no-default-features --features <FEATURES>`
  — cargo rejected the invocation. The no-features invocation
  (`cargo check -p echo_agent --no-default-features --locked`) finished
  successfully (exit 0), confirming `evolution` (always compiled) builds with
  zero features. Logs: `/tmp/evocheck_eval.log`, `/tmp/evocheck_improve.log`,
  `/tmp/evocheck_eval,improve.log`.
- Log/artifact: /tmp/evocheck_*.log (usage errors)

## Deviations

The failure is a harness artifact: the `${f:+--features "$f"}` shell expansion
passed the `--features` flag and value as separate words (or an empty value),
so cargo saw an incomplete option — not a code failure. This attempt is kept
as immutable evidence; the corrected run is V04-05-02.

## Conclusion

Supports nothing about the code. Records the harness quoting defect and the
one success: evolution compiles under `--no-default-features`.

## Follow-Up

V04-05-02 reruns the matrix with correct quoting.
