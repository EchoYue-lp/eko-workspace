# A-FE-02 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 modified files, all `web-frontend/src/generated/*.ts` (pre-existing formatting-only drift). A transient test file
> `web-frontend/src/__fe02_tmp__/fe02-fixtures.test.tsx` was created for this
> execution and DELETED afterwards; `git status` (0 non-generated dirty paths)
> and the generated-dir md5 (`779af40c3770faba1e97d3f118754be3`) were verified
> identical before and after.

## Claim

Replaying wire-like fixtures through the REAL reducers preserves attempt
identity, terminal monotonicity, lazy output, results, and Task acceptance
distinctions: (a) (owner, call_id) is the only tool identity key used by the
dedupe/merge paths; (b) duplicate and out-of-order events cannot regress
terminal state; (c) old-attempt completion cannot affect the current attempt;
(d) large tool output is never loaded into the store (lazy via `detail_ref`);
(e) result views keep verification/artifacts/remaining work distinct.

## Method

Transient vitest file `src/__fe02_tmp__/fe02-fixtures.test.tsx` (12 tests)
importing the production modules (`toolExecutionStore`, `subagentRunStore`,
`subagentResult`, `SubagentResultView`) and replaying fixtures:

```text
npx vitest run src/__fe02_tmp__/fe02-fixtures.test.tsx
```

Scenarios: (1) identity-key equality for (owner, call_id); (2) hydration
dedupe of two summaries with identical (owner, call_id) but different ids;
(3) live `ingest` of the same pair — documents current behavior; (4) live
`ingest` same-id 'succeeded' then 'running' — documents current behavior;
(5) subagentRunStore terminal-then-started/failed drop; (6) terminal arriving
before started (lazy creation); (7) old attempt 'completed' arriving after new
attempt 'started' — keys isolated; (8) `latestSubagentRunsByTask` keeps full
history, selects highest attempt; (9) `taskRuntimeToolExecutions` runtime-only
rows carry empty `detail_ref`/no output; (10) merged full detail wins over the
empty runtime boundary row; (11) `SubagentResultView` renders 验证/产物/未完成
as distinct sections with check status + source; (12) `stripTerminalContract`
removes the internal envelope, keeps user output.

## Expected Result

Hydration dedupe, monotonic guards, lazy-output boundaries, and the result
sections behave as documented; the two live-ingest scenarios (3) and (4) were
expected to expose the identity/monotonicity gap recorded in A-SRF-03-P2-01
and its defensive extension.

## Result

- Exit code: 0
- Duration: 0.42 s
- Summary: 12/12 tests passed. Behavior documented:
  - (1)/(2) identity helpers produce identical keys for identical
    (owner, call_id) and hydration dedupes to one row.
  - (3) live `ingest` keeps TWO rows for the same logical call (two ids,
    `idsByOwner` holds both) — duplicate projection reaches the UI.
  - (4) live `ingest` with the same id regresses a 'succeeded' row back to
    'running' — the live path has no status-rank guard (the merge path does).
  - (5)/(6) subagentRunStore terminal monotonicity holds; lazy creation from a
    terminal event works; the terminal result is preserved.
  - (7) old-attempt terminal events land on the old key; the new attempt stays
    'running' — attempt identity preserved.
  - (8) `latestSubagentRunsByTask` returns the highest-attempt record while
    keeping both records in the store.
  - (9)/(10) runtime-boundary rows never carry output; full persisted details
    win in the merge.
  - (11) verification/artifacts/remaining_work render as distinct sections,
    with `failed · observed` source labels.
  - (12) the protocol envelope is stripped; the user-facing output is kept.
- Log/artifact: none kept (transient file deleted; output above).

## Deviations

Scenarios (3) and (4) confirm the P2-01 defects (wire-id keying on the live
path + absent monotonicity guard). No false expectations elsewhere.

## Conclusion

Supports: attempt identity (per-attempt keys, old-attempt isolation),
subagent-run terminal monotonicity, lazy output boundaries, and
result/acceptance section separation hold on the data path; the tool live
ingest violates the (owner, call_id)-key and terminal-monotonicity invariants
(P2-01, cross-ref A-SRF-03-P2-01/A-SRF-02-P2-01). Does not prove: DOM-level
collapse/expand behavior (covered by existing component tests in V04-01 and
static inspection of `InlineToolCall` — 256 KiB `LIVE_DETAIL_AUTOLOAD_CHARS`
cap, 500 ms cursor polling, expand-to-load, collapse-on-terminal in
`SubagentStreamBlock`/`ExecutionProcessGroup`).

## Follow-Up

V03-02 (revision-aware latest-attempt selector, empirical); V04-01 (existing
suite). No rerun needed.
