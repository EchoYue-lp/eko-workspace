# A-FE-02 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 modified files, all `web-frontend/src/generated/*.ts` (pre-existing formatting-only drift); no other dirty paths.

## Claim

Historical documents describing the frontend task/subagent/tool projections
still match current code: attempt identity and "defaults to the latest
attempt", terminal monotonicity, lazy tool output (256 KiB pause, cursor
paging), result view without the protocol envelope, and the right rail
separating execution from acceptance.

## Method

Claim-to-code comparison against current source:

```text
echo-agent-cli/docs/MASTER-PLAN.md:190-215 (Subagent execution identity,
  latest-attempt default, monotonic terminal, result view envelope, right
  rail execution vs acceptance)
echo-agent-cli/docs/2026-07-25-gui-tool-execution-lazy-loading.md:15-23,
  81-106 (detail_ref opaque id, 500 ms cursor polling, 256 Ki pause,
  collapsed single-line, expand-to-load)
echo-agent-cli/docs/subagent-unification-plan.md §6 (referenced by
  subagentRunStore.ts:1-14 header)
Current code: subagentRunStore.ts:407-441, toolExecutionStore.ts:46-117,
  InlineToolCall.tsx:23-105, SubagentStreamBlock.tsx:52-58,
  subagentResult.ts:7-34, TaskRuntimePanel.tsx:429-486
```

## Expected Result

Claims are classified as current/fixed/stale/regressed per REPORTING.md;
regressed claims become finding evidence.

## Result

- Exit code: not applicable (inspection)
- Summary (classifications):
  - MASTER-PLAN "frontend stores all attempts independently" — current
    (per-execution-id keys, subagentRunStore.ts:452-521; V03-01 scenario 7).
  - MASTER-PLAN "keeps terminal state monotonic" — regressed on the tool
    live path (toolExecutionStore.ingest overwrites without a status-rank
    guard, toolExecutionStore.ts:206-217; V03-01 scenario 4); current for
    subagentRunStore (guard at :458-460) and for the merge/hydration paths.
  - MASTER-PLAN "defaults to the latest attempt when rendering a task" —
    regressed: `latestSubagentRunsByTask` ignores `plan_revision`
    (subagentRunStore.ts:407-441; V03-02 empirically shows the superseded
    revision's attempt being selected) → A-FE-02-P2-02.
  - MASTER-PLAN "result view uses complete terminal output without its
    internal protocol envelope; never promotes a thinking trace" — current
    (subagentResult.ts:7-34, SubagentResultView.tsx; V03-01 scenario 12).
  - MASTER-PLAN "Subagent process view contains only the same lazy
    tool-execution rows" — current (InlineToolCall lazy detail;
    runtime-boundary rows carry empty detail_ref, V03-01 scenario 9).
  - MASTER-PLAN "TaskRuntime loads start polling automatically so a completed
    backend snapshot cannot remain displayed as Pending" — current
    (taskRuntimeStore.ts:133-155, 263-265; taskRuntimeStore.test.ts
    "starts polling…stops after loading its terminal snapshot").
  - MASTER-PLAN "right rail reports Subagent execution progress separately
    from Task acceptance progress" — current at the summary level
    (TaskRuntimePanel.tsx:560-567, "评审未通过" description at :496-498);
    structured acceptance/check/artifact fields are not rendered anywhere
    (→ A-FE-02-P2-03).
  - Lazy-loading doc "toolExecutionStore 按 detail_ref 归一化 summary" —
    current as implemented (store keyed by wire id); the doc's single-producer
    assumption is what A-SRF-02-P2-01 breaks (→ P2-01).
  - Lazy-loading doc "运行中工具展开后每 500 ms 用 next_cursor 增量读取;累计
    256 Ki characters 后暂停,保留手动继续加载" — current
    (InlineToolCall.tsx:23, 91-105, 244-258).
  - Lazy-loading doc "折叠的 InlineToolCall 只渲染单行摘要;展开时并行读取
    manifest 和首屏" — current (InlineToolCall.tsx:86-89, 177-261).
  - subagent-unification-plan §6 (single source of truth, aggregated by
    execution id, task_id as stable join key) — current
    (subagentRunStore.ts:1-14, 417-441).

## Deviations

Two regressed claims (latest-attempt default; tool terminal monotonicity)
feed findings P2-02 and P2-01 respectively. All other claims are current.

## Conclusion

Supports: the doc-to-code mapping above; lazy-output, envelope-stripping,
polling, and per-attempt storage claims hold; two documented invariants are
regressed in current code with executable evidence (V03-01/V03-02). Does not
prove: the acceptance-separation claim beyond summary level (the structured
acceptance/check gap is a finding, not doc drift).

## Follow-Up

None; classifications are carried into the task report's Historical Claim
Status table.
