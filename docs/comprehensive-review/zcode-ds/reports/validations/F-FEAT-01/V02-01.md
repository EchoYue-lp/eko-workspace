# F-FEAT-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-FEAT-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Every declared feature in the eight framework manifests can be classified as [code gate + dependency], [code gate only], [dependency forward only], [empty marker with zero code effect], or [meta], with file:line evidence.

## Method

1. For each declared feature, merge the V01 reference census with the manifest edges (`[features]` and `[dependencies]` optional `dep:` entries) to determine whether it gates code, forwards dependencies, or does neither.
2. For root features, verify actual usage of each optional dependency in root code (`grep -rln` for `axum`, `jsonwebtoken`, `opentelemetry`, `rusqlite`, `sqlx`, `polars`, `scraper`, `html2text`, `url`, `pdf_extract`, `lopdf`, `calamine`, `docx_rs`, `encoding_rs` in `echo-agent/src`).
3. Verify gated-module existence for the marker features (V01 data) and echo_tools/echo_state/echo_orchestration gating (`echo-tools/src/lib.rs:22-65`, `echo-state/src/memory/mod.rs:23-49`, `echo-orchestration/src/human_loop/mod.rs:61,84`).

## Expected Result

A complete per-feature classification table; most root features classify as [代码 gate + 依赖/转发]; six markers classify as [empty marker — zero code effect]; `files`/`shell` show an anomaly where the feature is always-on regardless of selection.

## Result

Classification per feature (root `echo_agent` unless noted):

| Feature | Class | Evidence |
|---|---|---|
| a2a | code gate + dep | `src/lib.rs:69-71`, `advanced:306-312`; axum/jsonwebtoken used in `src/a2a/auth.rs:26,33`, `src/a2a/serve.rs:29` |
| mcp | code gate + forward | `src/lib.rs:88`, `src/plugin.rs:311,429`, `src/agent/react/mod.rs:24,512`; → `echo_integration/mcp` |
| lsp | code gate + forward (double) | `src/lib.rs:92`, `src/tools/mod.rs:101`, `src/tools/lsp.rs`; → `echo_core/lsp` + `echo_integration/lsp` (`echo-integration/Cargo.toml:24` also forwards `echo_core/lsp` — redundant but consistent) |
| sqlite | code gate + dep | `src/lib.rs:197-199`, `src/state/mod.rs:291-296`; rusqlite used in `src/state/sqlite.rs`; → `echo_state/sqlite` |
| telemetry | code gate + dep | `src/lib.rs:98-100`, `advanced:296-298`; opentelemetry crates used only in `src/telemetry.rs` |
| human-loop | code gate + forward | 68 refs (`src/agent/snapshot.rs:378,518,...`, `src/agent/react/mod.rs:21,38,...`); → `echo_orchestration/websocket` |
| channels | code gate + forward | `src/lib.rs:73-75`, `advanced:292-294`; `src/channels.rs:69` uses async_trait (non-optional root dep); → `echo_integration/channels` |
| content-guard | code gate + forward | `src/lib.rs:228-231`; → `echo_core/guard` (`echo-core/src/guard/mod.rs:3-7` gates content/llm/rule) |
| handoff | code gate (no deps) | `src/lib.rs:77-79`, `advanced:300-304` |
| topology | code gate (no deps) | `src/lib.rs:102-104`, `advanced:314-319` |
| tasks | code gate (no deps; module always compiled) | `src/tools/builtin/mod.rs:9,14`, `src/agent/callbacks/mod.rs:3`, `src/agent/react/mod.rs:31,36,44,403,447`; `pub mod tasks` ungated at `src/lib.rs:96` |
| project-rules | code gate (no deps) | `src/lib.rs:106-107`, `src/agent/react/mod.rs:363,376,706` |
| subagent | code gate + redundant edge | 97 refs; `src/agent/mod.rs:62`, `src/tools/builtin/mod.rs:6`; edge `subagent = ["tasks"]` (`echo-agent/Cargo.toml:80`) unnecessary per V01 |
| eval | code gate (no deps) | `src/lib.rs:36-38`; internal gates in `src/improve/mod.rs:42-136` |
| improve | code gate (no deps) | `src/lib.rs:43-45`; eval-dependent parts gated (`src/improve/mod.rs:42,91,...`) |
| testing | code gate (no deps) | `src/lib.rs:59,274` with `any(test, ...)` |
| web | code gate + forward + redundant deps | `src/lib.rs:173-175`, `src/tools/mod.rs:83-86`; `dep:scraper/html2text/url` unused in root code (grep zero) |
| media | code gate + forward + redundant deps | `src/lib.rs:178-180`, `src/tools/mod.rs:89-92`; `dep:pdf-extract/lopdf/calamine/docx-rs/encoding_rs` unused in root code (grep zero) |
| research | code gate + forward | `src/tools/mod.rs:95-98`; → `echo_tools/research` |
| data | dependency-forward only (root) | zero root code refs; `dep:polars` unused in root code; → `echo_tools/data` |
| statistics | dependency-forward only (root) | zero root code refs; `dep:polars` unused in root code; → `echo_tools/statistics` |
| git / database / rag / chart | dependency-forward only (root) | zero root code refs; `dep:sqlx` (database) unused in root code (grep zero) |
| files | dependency-forward only (root) + always-on | zero root cfg refs; ungated re-export `src/tools/mod.rs:58-59`; see V05 |
| shell | code gate (1 site) + forward + always-on | `src/tools/builtin/spawn_task.rs:262-264` (with fallback path at :270+); ungated re-export `src/tools/mod.rs:78-79`; see V05 |
| sandbox / semantic-memory / macros / provider-factory / workflow / multimodal | empty marker — zero code effect | zero cfg refs (V01); `sandbox`/`workflow`/`macros` modules always compiled (`src/lib.rs:54,65,111`); no module exists for semantic-memory/multimodal/provider-factory; effect limited to example `required-features` (`echo-agent/Cargo.toml:181,192,204,206,213,216,227,232,233,237,301`) |
| full | meta | 26 entries (`echo-agent/Cargo.toml:67`); omits exactly 7 markers: testing, sandbox, semantic-memory, macros, provider-factory, workflow, multimodal |
| default | meta | `[]` at root; NOT `[]` at echo-execution (`echo-execution/Cargo.toml:22`: `default = ["files", "shell"]`) |

Sub-crates: echo_core — reqwest (dep-only gate, `error.rs:396`), guard (code+dep, `guard/mod.rs:3-7`), permission (code+dep, `tools/permission.rs:262`), mcp/lsp/channels (code gates, `error.rs:35,48`, `tools/mod.rs:227`, `lib.rs:30`). echo_tools — all 11 features are code gates with correct per-module gating (`echo-tools/src/lib.rs:20-65`); `statistics = ["data"]` edge declared (`echo-tools/Cargo.toml:34`); `full` covers all 11. echo_integration — mcp (dep+forward), lsp (forward), channels (dep+forward); all used only in gated modules. echo_state — sqlite (code+dep, `memory/mod.rs:23,47-49`, `util.rs:3,23`). echo_orchestration — websocket (code+dep, `human_loop/mod.rs:61,84`). echo_macros — no features.

## Deviations

The B-BASE-01/V02 label "13 empty marker features" is refined: 7 of the 13 carry real code gates; only 6 are truly empty.

## Conclusion

Supports the classification table and the finding that six marker features are pure no-ops while `files`/`shell` are effectively permanently enabled (V05 details the mechanism).

## Follow-Up

Feeds task findings F-FEAT-01-P2-01, F-FEAT-01-P2-02, F-FEAT-01-P3-02, F-FEAT-01-P3-03.
