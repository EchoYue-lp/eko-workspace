# F-FEAT-01 / V04 / Attempt 01

> Status: inconclusive (static analysis only; compile verification delegated to Q-FW-02 as needs_evidence)
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-FEAT-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Static determination of which feature combinations risk failing to compile — specifically `subagent` standalone (tasks coupling), `sqlite` in echo_state isolation, `files`/`shell` under no-default, and per-feature standalone builds — with real compilation explicitly out of scope for this task (owned by Q-FW-02).

## Method

1. Trace imports of every cfg-gated module for the risk combinations: `--features subagent` (does subagent-gated code reference tasks-gated symbols?), `--features eval` / `--features improve` alone (does improve reference crate::eval ungated?), `--features sqlite` (root state/memory + echo_state isolation), `--features testing` alone, `--no-default-features` at workspace level (feature-union semantics of echo_execution defaults), `-p echo_tools --no-default-features`, `-p echo_execution --no-default-features`.
2. Cargo feature-union reasoning for the no-default case: command-line `--no-default-features` applies to top-level selections only; the dependency edge `echo_agent → echo_execution` (`echo-agent/Cargo.toml:108`, no `default-features = false`) still contributes echo_execution's `default = ["files", "shell"]`, so the union keeps them enabled.
3. Grep-only verification; no `cargo` command was executed (per task instruction V04 is static; Q-FW-02 owns real compiles).

## Expected Result

A risk table: which combinations are statically safe, which are statically at risk, and which need a real compile to confirm.

## Result

Static risk table (each row needs Q-FW-02 real-compile confirmation):

| Combination | Static verdict | Reasoning |
|---|---|---|
| `cargo check -p echo_agent --no-default-features` | likely passes | echo_execution defaults stay on via dependency edge (union) → echo_tools files+shell on → ungated re-exports at `src/tools/mod.rs:58-59,78-79` resolve |
| `cargo check --workspace --lib --no-default-features --locked` (AGENTS.md gate) | likely passes but vacuous for files/shell | same union semantics; files/shell always enabled regardless of the flag |
| `cargo check -p echo_agent --no-default-features --features subagent` | likely passes | `subagent = ["tasks"]` declared; subagent code only touches always-compiled `crate::tasks::{Evidence, NestedDelegationPolicy}` (`context_builder.rs:7`, `executor.rs:30`); no subagent-gated file references tasks-gated symbols (V01) |
| `cargo check -p echo_agent --no-default-features --features tasks` (no shell) | likely passes | `spawn_task.rs:262` gates shell usage with inline fallback (:270+) |
| `cargo check -p echo_agent --no-default-features --features eval` | likely passes | eval module gates its own imports |
| `cargo check -p echo_agent --no-default-features --features improve` (no eval) | likely passes | eval-driven parts all `#[cfg(feature = "eval")]` (`src/improve/mod.rs:42-136`) |
| `cargo check -p echo_agent --no-default-features --features sqlite` | likely passes | root `state/mod.rs:291-296` + `memory` glob re-export (`src/memory.rs:44,47`) + echo_state gates (`memory/mod.rs:23,47-49`) are consistent |
| `cargo check -p echo_agent --no-default-features --features data` / `statistics` | likely passes | zero root code references polars; echo_tools gates own modules |
| `cargo check -p echo_tools --no-default-features --features statistics` | likely passes | `statistics = ["data"]` declared (`echo-tools/Cargo.toml:34`) |
| `cargo check -p echo_execution --no-default-features` | likely passes | no ungated reference to echo_tools gated modules in echo-execution code |
| `cargo check -p echo_agent --features full` | likely passes | all 26 forwarded features gate their own code; `testing`/`sandbox`/etc. omitted from full but their code is always compiled anyway |
| Consumer build of `echo_agent` that expects files/shell OFF | **impossible — always-on** | ungated re-exports + echo_execution defaults; finding F-FEAT-01-P2-01 |

## Deviations

None. All rows are static predictions; the task instruction explicitly forbids executing `cargo check` here.

## Conclusion

Supports: no combination is statically predicted to fail; the only isolation violation is the reverse — `files`/`shell` cannot be disabled by any consumer. Real compile evidence (exit codes) is required from Q-FW-02 to confirm each row; this report is the needs_evidence handoff.

## Follow-Up

Q-FW-02 should execute the rows above, especially: workspace `--no-default-features` gate, `-p echo_agent --no-default-features --features subagent`, and a consumer-style build with only `mcp` to confirm tree-sitter is compiled (verifying F-FEAT-01-P2-01).
