# F-RCT-05 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

The steering mailbox semantics are as documented: lease scoping, FIFO drain,
turn-id mismatch rejection, and same-turn steer during an LLM call.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --locked steer
```

## Expected Result

Exit 0; 3 tests pass (lease_scopes_active_turn_and_preserves_fifo,
rejects_mismatch_and_non_steerable_turns,
steer_during_llm_call_continues_same_turn_with_new_input).

## Result

- Exit code: 0
- Duration: ~30 s
- Summary: `test result: ok. 3 passed; 0 failed; 0 ignored` (444 filtered
  out). The loop-level steer test uses `run_id == turn_id == "turn-steer-1"`
  (stream_channel.rs:2106-2107) — the non-invocation/key-aligned shape only.

## Deviations

None — but note the coverage gap: no test drives steer through an invocation
with `turn_id != run_id` or with `run_id == None`, which is the EKO production
shape (V02-01). The mismatch in `drain_steer_into_context` keying
(stream_channel.rs:333 vs 111-122) is therefore invisible to the suite.

## Conclusion

Supports the mailbox mechanics (FIFO, lease, error types) and the finding
F-RCT-05-P1-02's negative: the green tests cannot observe the production-path
drain mismatch.

## Follow-Up

None; covered by F-RCT-05-P1-02.
