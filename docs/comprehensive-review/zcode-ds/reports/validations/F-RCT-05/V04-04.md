# F-RCT-05 / V04 / Attempt 04

> Status: failed (system invariant violated — becomes finding F-RCT-05-P1-01)
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean; probe crate `/tmp/frct05-probe` outside both repos

## Claim

Interrupting (cancelling) an agent mid-tool-batch leaves a restorable
checkpoint: the cancel arm's `save_runtime_checkpoint` (tools.rs:203/296)
persists only paired assistant tool call / tool result messages, so the next
`resume_from_state_store` succeeds.

## Method

Standalone probe crate (pattern established by F-SKL-01):

```text
/tmp/frct05-probe/Cargo.toml
  echo_agent = { path = ".../lp-agent/echo-agent", features = ["testing"] }
  tokio (rt-multi-thread, macros, time), futures, serde_json
/tmp/frct05-probe/src/main.rs
  1. RecordingStateStore implementing echo_agent::state::RuntimeStateStore
     (checkpoint kept in memory; get/save round-trip).
  2. SlowWriteTool (name/description/parameters/execute; sleeps 10 s).
  3. MockLlmClient::new().then_tool_call("write-1", "slow_write", "{}")
     .with_response("done").
  4. ReactAgentBuilder: model "test-model", llm_client, tool, conversation_id
     "probe-conv", state_store.
  5. agent.execute_stream_with_cancel("write it", cancel):
     on AgentEvent::ToolCall → cancel.cancel(); collect until stream ends
     (30 s outer timeout).
  6. Assertions on the stored AgentCheckpoint: blocked_reason, per-message
     pairing scan (assistant tool_call without tool_result), and
     cp.restore_messages() result.
```

Environment: macOS arm64, cargo build + run, no network.

## Expected Result

The cancel checkpoint contains the assistant tool call AND its result (paired)
or the turn is not checkpointed at all; `restore_messages()` returns Ok.

## Result

- Exit code: 0 (probe itself; the system behavior under test FAILED)
- Duration: ~7 s (5 s cancellation grace + run)
- Summary (probe stdout):
  ```
  [probe] ToolCall observed; cancelling now
  [probe] ToolBatchEnd observed
  [probe] stream ended: cancelled=true saw_batch_end=true timeout=false
  [probe] blocked_reason=Some("Tool batch cancelled")
  [probe] message_count=3
  [probe] unpaired tool call ids in checkpoint: ["write-1"]
  [probe] restore_messages() = Err(Runtime State Error: Serialization error: checkpoint has tool calls without results: write-1)
  ```
- Log/artifact: probe source `/tmp/frct05-probe/src/main.rs` (reproducible).

The cancel arm did exactly what the static trace (V03-03) predicted: it saved a
checkpoint whose messages contain assistant tool call `write-1` with no result
(the tool was still in flight at grace expiry), and `restore_messages()`
rejected it with the typed `SerializationError`. Via `restore_thread_context`
(context.rs:245-248) this rejection falls back to `reset_messages()` — full
context wipe on the next Execute-mode turn.

## Deviations

None. Probe exited 0; the claim failed, which is the observed deviation from
the expected invariant.

## Conclusion

Empirically supports F-RCT-05-P1-01 (cancel mid-batch → unresumable checkpoint
→ conversation-context wipe on next restore) and the "interrupt at each safe
point" required validation: cancellation during a tool batch is NOT a safe
point despite the explicit checkpoint the cancel arm writes. Does not cover
the tool-error checkpoint arm (tools.rs:258-262/414) dynamically, but the
message-state logic is identical (assistant message pushed before execution,
tools.rs:101-113).

## Follow-Up

None — finding filed; Q-FLT-01 may convert this scenario into a permanent
fixture (cancel-mid-batch then resume, assert context preserved).
