# F-RCT-05 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

Historical design documents (root `docs/MASTER-PLAN.md`,
`docs/PROJECT-ANALYSIS.md`, `echo-agent-cli/docs/2026-07-11-running-input-interrupt-design.md`)
either match current steer/checkpoint/resume code or their drift is classified.

## Method

Grep the documents for steer/checkpoint/resume claims and compare each claim
against the code anchors verified in V02/V03.

```text
grep -n "resume\|checkpoint\|steer\|快照\|恢复\|中断" docs/MASTER-PLAN.md
grep -n "resume\|checkpoint\|steer\|snapshot" docs/PROJECT-ANALYSIS.md
grep -n "steer\|mailbox\|补充\|turn" echo-agent-cli/docs/2026-07-11-running-input-interrupt-design.md
```

## Expected Result

Claims classify as current/fixed/stale/regressed with code evidence.

## Result

- Exit code: not-applicable (static doc comparison)
- Duration: ~10 min
- Summary of classifications (full table in the task report):
  - MASTER-PLAN:67/96/147/185 ("resume 按 call_id 跳过已完成工作;resume 不重新
    发射已完成副作用") — **stale/regressed**: no call_id-based skip exists
    (V03-02); the only guard rejects whole checkpoints.
  - MASTER-PLAN:148 ("interrupted/stream error/process crash 后恢复时,先校验
    tool_call/tool_result 配对") — **current** for the validation step
    (state/mod.rs:186-231), but its failure handling contradicts the intent
    (V03-04).
  - MASTER-PLAN:373 M3 acceptance ("已完成结果不重放,写副作用不确定时阻塞自动
    恢复并要求 retry/skip 决策") — **regressed**: mid-batch cancel checkpoints
    fail restore entirely (no retry/skip decision), and completed work after a
    pre-completion checkpoint can be re-issued.
  - MASTER-PLAN:146 ("cancel 是不可继续的终态") — **stale**: the cancel arms
    deliberately save a "Tool batch cancelled" checkpoint implying
    resumability, which then fails.
  - MASTER-PLAN:157 ("路由错误允许用户同 turn steer") + design doc
    2026-07-11:9/98 ("注入当前 turn,在下一个 Agent 安全点生效;ReAct 在模型
    调用前、工具批次后和最终文本提交前消费 steer") — **regressed** on the EKO
    main path: `drain_steer_into_context` keyed on `current_run_id` while the
    lease is keyed on `turn_id` (V02-01, F-RCT-05-P1-02).
  - PROJECT-ANALYSIS:166-167 (checkpoint/transcript mechanism) — **current**
    in mechanism; anchor `snapshot.rs:312` is stale (now snapshot.rs:570).
  - PROJECT-ANALYSIS:129 (runtime checkpoint table,
    `FileRuntimeStateStore`/`~/.echo-agent/runtime_state/`) — **current**;
    EKO overrides the directory per workspace (infra.rs:126, state.rs:924).

## Deviations

None.

## Conclusion

The docs describe an intended resume/steer contract that the current code
partially implements; three claim families are regressed/stale. Supports
F-RCT-05-P1-01/P1-02 and F-RCT-05-P2-01 with documentation evidence.

## Follow-Up

None; consumed by the task report's Historical Claim Status section.
