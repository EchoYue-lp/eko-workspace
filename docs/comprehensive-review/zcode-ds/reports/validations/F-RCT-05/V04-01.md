# F-RCT-05 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

The `AgentCheckpoint` pairing validator and field round-trip behave as
documented: paired histories restore, unpaired/duplicate histories are
rejected.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --locked 'state::checkpoint'
```

Environment: macOS arm64, default features (includes `testing`), `--locked`.

## Expected Result

Exit 0; 2 tests pass (checkpoint_restores_paired_tool_history,
checkpoint_rejects_unpaired_or_duplicate_tool_results).

## Result

- Exit code: 0
- Duration: ~40 s (compile) + instant test run
- Summary: `test result: ok. 2 passed; 0 failed; 0 ignored` (445 filtered out).
  The rejection test covers: assistant call without result, duplicate result
  for one id. It does NOT cover name-mismatch or orphan-result variants
  (state/mod.rs:213-218 are untested branches).

## Deviations

None.

## Conclusion

Confirms the round-trip invariant (V03-01) empirically: the pairing validator
rejects histories with result-less tool calls — the exact shape the
cancel/error checkpoint arms persist (V03-03). Supports F-RCT-05-P1-01's
rejection-side claim. Does not cover the save side — that is V04-04.

## Follow-Up

V04-02, V04-03, V04-04.
