# Q-FW-02 / V13 / Attempt 01

> Status: failed (7 of 8 examples pass; demo45_customer_service fails under its declared required-features)
> Validation date: 2026-08-12
> Executor: ZCode-ds (Q-FW-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Grouped example compilation: examples build with exactly their manifest-declared `required-features` (`--no-default-features --features <required> --locked`), i.e. each example demonstrates its stated capability contract independently. Sample of 8 examples spanning 10 distinct features, chosen to cover different feature combinations; examples sharing an identical required-feature set are treated as one group and one representative is compiled (sqlite group: demo27, also demo46/demo31/demo08; mcp group: demo06, also demo30/demo42).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo check -p echo_agent --example <name> --no-default-features --features <required> --locked
```

Executed serially for:

| Example | Declared required-features (`Cargo.toml` line) | Features covered |
|---|---|---|
| demo27_sqlite_memory | `sqlite` (:164-165) | sqlite |
| demo45_customer_service | `sqlite,human-loop` (:168-169) | sqlite, human-loop |
| demo48_personal_assistant | `sqlite,tasks,subagent` (:172-173) | sqlite, tasks, subagent |
| demo06_mcp | `mcp` (:184-185) | mcp |
| demo58_git_worktree | `git` (:332-333) | git |
| demo41_web_tools | `web` (:243-244) | web |
| demo60_data_quality | `data,statistics` (:340-341) | data, statistics (polars) |
| demo55_lsp_tools | `lsp` (:321-322) | lsp |

Supplementary runs for the failing example (same session):
- A: `cargo check -p echo_agent --example demo45_customer_service --locked` (default features, no flags)
- B: `cargo check -p echo_agent --example demo45_customer_service --no-default-features --features sqlite,human-loop,content-guard --locked` (fix hypothesis)

Environment: rustc 1.97.1 (stable), cargo 1.97.1, warm incremental cache from V01-V12. Full logs at `/tmp/qfw02-v13-examples.log`, `/tmp/qfw02-v13b-default.log`, `/tmp/qfw02-v13c-fixhyp.log`.

## Expected Result

All 8 examples exit 0; a failure is expected to indicate a misleading or incomplete `required-features` declaration or a source/feature mismatch in the example.

## Result

- Exit codes (per example):

| Example | Exit code | Status |
|---|---|---|
| demo27_sqlite_memory | 0 | passed |
| demo45_customer_service | 101 | **failed** |
| demo48_personal_assistant | 0 | passed |
| demo06_mcp | 0 | passed |
| demo58_git_worktree | 0 | passed |
| demo41_web_tools | 0 | passed |
| demo60_data_quality | 0 | passed |
| demo55_lsp_tools | 0 | passed |

- Supplementary: A (default features) exit 101 — but this is Cargo's expected `required-features` refusal ("target `demo45_customer_service` requires the features: `sqlite`, `human-loop`"), not a source error. B (+`content-guard`) exit 0 — fix hypothesis confirmed.
- Duration: approximately 20 minutes total (demo60 pulled polars + proptest + criterion dev-dependencies).
- Log/artifact: `/tmp/qfw02-v13-examples.log` (full), `/tmp/qfw02-v13b-default.log`, `/tmp/qfw02-v13c-fixhyp.log`

Failure detail (from `/tmp/qfw02-v13-examples.log`):

```text
error[E0433]: cannot find type `RuleGuardBuilder` in this scope
   --> examples/demo45_customer_service.rs:309:9
    |
309 |         RuleGuardBuilder::new("content-filter")
    |         ^^^^^^^^^^^^^^^^ use of undeclared type `RuleGuardBuilder`
error: could not compile `echo_agent` (example "demo45_customer_service") due to 1 previous error
```

## Deviations

One example failed under its declared feature contract. Root-cause trace (source inspection, not conjecture): `RuleGuardBuilder` is re-exported from the prelude only under `#[cfg(feature = "content-guard")]` (`src/lib.rs:230-231`); demo45 imports the prelude glob at `examples/demo45_customer_service.rs:36` and calls `RuleGuardBuilder::new("content-filter")` at `:309`; the manifest declares only `required-features = ["sqlite", "human-loop"]` (`echo-agent/Cargo.toml:168-169`) and `content-guard` is not in `default` (`Cargo.toml:66`) nor implied by sqlite/human-loop. Adding `content-guard` to the feature set compiles cleanly (run B, exit 0).

## Conclusion

Supports: 7 of 8 sampled examples compile under exactly their declared required-features — the sqlite, tasks+subagent, mcp, git, web, data+statistics (polars on stable 1.97.1 compiles), and lsp contracts hold. Refutes for demo45_customer_service: its declared contract (`sqlite,human-loop`) cannot build it; the example silently requires `content-guard`, which is neither declared nor implied. Finding Q-FW-02-P2-01 (example required-features incomplete — content-guard API used undeclared). Does not prove runtime behavior of any example. Note: polars compiling on stable 1.97.1 is consistent with the `data` docs.rs exclusion comment (`Cargo.toml:57-63`) being a nightly-only limitation.

## Follow-Up

V14 (doctest, all features) next. The demo45 finding should be cross-referenced by F-API-01 (example/manifest contract) and Q-DOC-01 (example documentation); F-API-01-P3-02's claim that default-feature example builds pass was checked only for undeclared examples and is unaffected.
