# Q-FW-02 / V12 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (Q-FW-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The `media` feature compiles standalone: `cargo check -p echo_agent --no-default-features --features media --locked` exits 0 (root `media = ["dep:pdf-extract", "dep:lopdf", "dep:calamine", "dep:docx-rs", "dep:encoding_rs", "echo_tools/media"]` at `echo-agent/Cargo.toml:85` — the largest root-level optional-dep bundle, unused by root code per F-FEAT-01-P3-03 — plus the echo_tools media module, compiling with no other optional feature).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo check -p echo_agent --no-default-features --features media --locked
```

Environment: rustc 1.97.1 (stable), incremental cache. Full log at `/tmp/qfw02-v12-media.log`.

## Expected Result

Exit code 0.

## Result

- Exit code: 0
- Duration: 22.57 s incremental (echo_agent crate)
- Summary: build succeeded; log ends with `Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 22.57s` and `EXIT_CODE=0`. Zero warnings.
- Log/artifact: `/tmp/qfw02-v12-media.log`

## Deviations

None.

## Conclusion

Supports: `media` compiles independently on the reviewed commit. Does not prove media tool runtime behavior (F-EXT-03 owns that).

## Follow-Up

V13 (examples grouped compile) next.
