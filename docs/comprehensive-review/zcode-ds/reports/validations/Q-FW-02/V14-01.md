# Q-FW-02 / V14 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds (Q-FW-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The framework's public doc examples compile and run: `cargo test --doc -p echo_agent --all-features --locked` exits 0. A failure means a stale or incorrect doc example — a public documentation-contract defect (this is also a component of the AGENTS.md mandatory gate `cargo test --workspace --all-targets --all-features`, which includes lib doctests).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test --doc -p echo_agent --all-features --locked
```

Environment: rustc 1.97.1 (stable), cargo 1.97.1, warm incremental cache. Full log at `/tmp/qfw02-v14-doctest.log`.

## Expected Result

Exit code 0, all doctests pass (some `ignored` is normal and acceptable).

## Result

- Exit code: 101
- Duration: 26.10 s for doctests (plus prior full all-features lib compile)
- Summary: `test result: FAILED. 81 passed; 1 failed; 25 ignored`. The single failure:

```text
---- src/testing/mod.rs - testing (line 24) stdout ----
error[E0063]: missing field `focus_instructions` in initializer of `CompressionInput`
  --> src/testing/mod.rs:39:13
   |
39 | let input = CompressionInput {
   |             ^^^^^^^^^^^^^^^^ missing `focus_instructions`
error: aborting due to 1 previous error
Couldn't compile the test.
```

- Log/artifact: `/tmp/qfw02-v14-doctest.log`

## Deviations

One compile-failing doctest. Source inspection confirms: the `## Testing a compressor (MockLlmClient)` example in `src/testing/mod.rs` (doctest starts at line 24, fence `rust,no_run`) initializes `CompressionInput` with three fields (`messages`, `token_limit`, `current_query`), but the struct (`echo-core/src/compression.rs:40-48`) has a fourth field `focus_instructions: Option<String>` that every other initializer in the repo sets (`echo-state/src/compression/mod.rs` and `invariants.rs` — 9 initializer sites inspected, all include it). The doctest is the only stale site. The doctest is `no_run`, so this is a compile-only failure (doc example cannot even compile). It is gated by the `testing` feature (module `src/testing`), so default-feature doc builds are unaffected; all-features doc builds fail.

## Conclusion

Supports: 81 doctests pass under all features — the doc surface is broadly compile-clean. Refutes the claim that the doc-test suite is green: the `echo_agent::testing` module's flagship example cannot compile because `CompressionInput` grew a `focus_instructions` field without updating the doc example. Finding Q-FW-02-P2-02. Consequence for the mandatory gate: `cargo test --workspace --all-targets --all-features --locked` (AGENTS.md) includes lib doctests and is therefore currently RED on this failure (Q-FW-01 should confirm the whole gate). Does not prove runtime behavior of the doc examples that pass.

## Follow-Up

None in this task. Cross-reference: F-API-01 (doc contract), F-CMP-01 (CompressionInput ownership), Q-DOC-01 (docs rewrite), Q-FW-01 (full gate).
