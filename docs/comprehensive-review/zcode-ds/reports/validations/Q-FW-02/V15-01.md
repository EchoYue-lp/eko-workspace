# Q-FW-02 / V15 / Attempt 01

> Status: passed (with deviations: 28 unresolved intra-doc link warnings)
> Validation date: 2026-08-12
> Executor: ZCode-ds (Q-FW-02)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Document link validation: `cargo doc -p echo_agent --all-features --no-deps --locked` completes and the generated documentation has no broken intra-doc links (rustdoc unresolved-link warnings are the failure criterion). This validates the framework's published docs surface (docs.rs builds `no-default-features + metadata features`; the all-features doc build here is the superset that includes every gated module).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo doc -p echo_agent --all-features --no-deps --locked
grep -c "unresolved link" /tmp/qfw02-v15-doc.log
```

Environment: rustc 1.97.1 (stable), cargo 1.97.1, warm incremental cache. Full log at `/tmp/qfw02-v15-doc.log`.

## Expected Result

Exit code 0 and zero `unresolved link` warnings. (Broken intra-doc links are rustdoc warnings, not errors, so exit 0 alone is not sufficient; the warning count is the claim under test.)

## Result

- Exit code: 0
- Duration: 49.70 s (doc generation; lib already compiled)
- Summary: `Generated .../target/doc/echo_agent/index.html`; `warning: echo_agent (lib doc) generated 29 warnings`, of which **28 are `unresolved link`** warnings across 26 source locations.
- Log/artifact: `/tmp/qfw02-v15-doc.log`; generated docs at `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent/target/doc/echo_agent/index.html`

Unresolved targets (22 distinct names, 28 occurrences): `ReactAgent` ×3, `ExternalRunContext` ×3, `with_locked_state` ×2, and one each of `save`, `dispatch_background`, `delegate_to_agent`, `delegate_to_agent_with_parent_context_and_cancel`, `delegate_task`, `ToolExecutionPipeline`, `SubagentResult`, `SubagentRegistry`, `SkillDescriptor`, `SkillCandidateDetector`, `ReactAgentBuilder`, `PipelineStage`, `Message`, `ManagerSubagentOrchestrator`, `LayeredForgetTool`, `DefaultAgentFactory`, `AgentFactory`, `AgentFactoryConfig`, `AgentEvent::LlmUsage`, `AgentCheckpoint`.

Affected locations (26): `src/agent/default_factory.rs:5`, `src/agent/mod.rs:76-77` (×3), `src/agent/react/capabilities.rs:429`, `src/agent/react/mod.rs:300,1281,2104,2173,2237,2400,2426` (×7), `src/agent/snapshot.rs:324,409,565` (×3), `src/agent/subagent/events.rs:35,40,43,50` (×4), `src/agent/subagent/team/mod.rs:304`, `src/agent/subagent/usage.rs:4`, `src/evolution/curator.rs:225,248`, `src/evolution/draft.rs:3`, `src/evolution/layer.rs:498`, `src/trace/mod.rs:671`.

## Deviations

Symbol-existence check (grep over `src/` + `echo-core/src/` + `echo-tools/src/`): every unresolved target exists in code — e.g. `ReactAgent` at `src/agent/react/mod.rs:104`, `ExternalRunContext` at `echo-core/src/tools/mod.rs:964`, `delegate_task` at `src/agent/react/mod.rs:2100`, `AgentCheckpoint` at `src/state/mod.rs:117`, `SkillDescriptor` re-exported at `src/lib.rs:217` and `src/evolution/health.rs:19`, `AgentEvent::LlmUsage` at `src/agent/react/run/stream_channel.rs:466`. The one exception is `PipelineStage` (`src/agent/react/run/pipeline.rs:59`) which is `pub(crate)` — rustdoc cannot resolve a link to a private item. All 28 warnings are therefore path/scoping defects in doc comments (bare type names that are not in the referencing module's scope), not references to nonexistent APIs.

## Conclusion

Supports: `cargo doc` completes (exit 0) and the documentation surface is structurally buildable. Refutes: the docs are not link-clean — 28 broken intra-doc links will render as dead links in published docs (docs.rs embeds the same doc comments). Documentation-only severity; finding Q-FW-02-P3-01. Does not prove the docs.rs `no-default-features` metadata build (F-FEAT-01-P3-04) or README external-link validity (Q-DOC-01).

## Follow-Up

Q-DOC-01 should own the doc-link rewrite; F-API-01 P3-04 metadata build remains with Q-FW-02's V01-V12 evidence that all features compile.
