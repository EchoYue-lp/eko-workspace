# Q-DOC-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: both source repositories clean

## Claim

Commands documented in the README/example catalog ("cargo run --example demoXX"
and "every feature has a demo you can `cargo run` immediately",
`echo-agent/README.md:389`) execute successfully, and the examples classified
as locally verifiable acceptance examples run without external prerequisites.

## Method

All runs from `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent` with
`cargo run --example <name>`, exit code captured from the cargo process itself
(`; echo "exit: $?"` on the unpiped command):

```text
1. cargo run --example demo22_plan_execute --quiet     (phantom: README.md:1086 row)
2. cargo run --example demo14_memory_isolation --quiet (phantom: README.md:1078 row)
3. cargo run --example demo16_testing --quiet          (phantom: README.md:1080 row + examples/README.md:15 "Acceptance example")
4. cargo run --example demo32_token_budget             (examples/README.md:17 "Acceptance example")
5. cargo run --example demo37_declarative_workflow     (header command at demo37 file :11-13)
6. cargo run --example demo37_declarative_workflow --features workflow
7. cargo run --example demo00_quickstart               (README quickstart, catalog row)
```

Environment: no LLM API keys set in the shell; network available but not used
by these checks. Rust toolchain per `rust-toolchain.toml`; target cache warm
from prior review compiles.

## Expected Result

Phantom catalog rows fail fast (exit 101 "no example target named ...").
Locally verifiable examples (demo32 per examples/README.md acceptance list)
run to completion with exit 0. demo37's documented command runs to completion.

## Result

| # | Command | Exit | Outcome |
|---|---|---|---|
| 1 | `cargo run --example demo22_plan_execute` | 101 | `error: no example target named 'demo22_plan_execute'` — README.md:1086 + examples/README.md:72 reference a nonexistent file |
| 2 | `cargo run --example demo14_memory_isolation` | 101 | `error: no example target named 'demo14_memory_isolation'` — README.md:1078, README.zh.md:783, examples/README.md:31 |
| 3 | `cargo run --example demo16_testing` | 101 | `error: no example target named 'demo16_testing'` — README.md:1080, examples/README.md:15 lists it as an Acceptance example ("deterministic local verification") but no such file exists |
| 4 | `cargo run --example demo32_token_budget` | 1 | Part 1 (config demo) prints; Part 2 aborts: `Config(ConfigFileError("模型 'qwen3-max' 需要设置环境变量 DASHSCOPE_API_KEY（当前为空）..."))` — requires a live dashscope model + network despite examples/README.md:11-18 acceptance classification |
| 5 | `cargo run --example demo37_declarative_workflow` | 101 | `error: target 'demo37_declarative_workflow' ... requires the features: 'workflow'` — the command documented inside the example header (file :11-13) is incomplete |
| 6 | `cargo run --example demo37_declarative_workflow --features workflow` | 0 | Full run: YAML/JSON parsing, graph build, error handling all `✓`, "Demo Complete" |
| 7 | `cargo run --example demo00_quickstart` | 1 | `Config(ConfigFileError("Model 'deepseek-v4-flash' missing required config: api_key 依赖的环境变量未设置: DEEPSEEK_API_KEY"))` — expected: README.md:300-315 documents `${DEEPSEEK_API_KEY}`; an LLM key is a documented prerequisite, not counted as a defect |

Logs kept at `/tmp/demo32.log`, `/tmp/demo37.log`, `/tmp/demo37b.log`,
`/tmp/demo00.log` (repo-external, ephemeral).

## Deviations

- The README.md:389 headline "66 runnable examples — every feature has a demo
  you can `cargo run` immediately" is not falsifiable as stated: 3 of the
  65 catalog rows are phantom (exit 101), demo37 needs an undocumented
  `--features workflow`, and LLM-dependent examples need keys (documented in
  the config section but not in the headline claim).
- `examples/README.md` "Acceptance Examples" list (demo16, demo32) does not
  match local verifiability: demo16 does not exist; demo32 requires
  DASHSCOPE_API_KEY and a network LLM call.
- No frontend/CLI commands were sampled this face (CLI README documents
  `cargo run --bin` style usage; running the full TUI/GUI is out of scope for
  Q-DOC-01 — see Q-GUI-01/Q-E2E-01). Not filed as a gap because the task card
  asks for example-level sampling, which was delivered.

## Conclusion

Supports: the "cargo run immediately" example claim is broken in three
distinct ways (phantom catalog rows; incomplete documented feature flags;
acceptance-class examples that require live LLM credentials). demo22 phantom
row is canonical A-TSK-02-P3-03 (cross-referenced, not re-filed); demo14 and
demo16 phantom rows and the demo32/demo37 classification/command defects are
new evidence. New findings: Q-DOC-01-P3-04 (phantom catalog rows demo14/demo16
+ examples/README.md acceptance list), Q-DOC-01-P3-05 (demo37 documented
command missing `--features workflow`), Q-DOC-01-P3-06 (demo32 acceptance
classification vs live-LLM dependency).

## Follow-Up

Faces V03 (feature/config matrix), V04 (stale terminology), V05 (canonical
aggregation) continue; Q-FW-02 owns full example-matrix compilation across
features.
