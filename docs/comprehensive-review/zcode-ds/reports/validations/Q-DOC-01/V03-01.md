# Q-DOC-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: both source repositories clean

## Claim

Documented features and configuration options (framework README feature table,
README "Configuration" sample `echo-agent.yaml`, `docs/zh/28-config-reference.md`,
CLI README + `docs/configuration.md` config paths) match the manifest
`[features]` and the actual config schema/parsing code.

## Method

1. Feature matrix: compared `echo-agent/README.md:121-147` feature table with
   `echo-agent/Cargo.toml:65-103` `[features]` (static diff). Canonical
   feature findings already filed by F-API-01-P2-01 and F-FEAT-01-P2-01/P2-02/
   P3-01 were cross-referenced, not re-verified from scratch.
2. Config schema: mapped every key of the README `echo-agent.yaml` sample
   (README.md:306-345) and the CLI `docs/configuration.md` sample to fields of
   `echo-agent/src/config.rs` `AppConfig` (:72-96), `ModelConfig` (:297),
   `AgentYamlConfig` (:423-488), `McpYamlConfig` (:492), `ChannelsConfig`
   (:501), `QqChannelConfig` (:514), `FeishuChannelConfig` (:526),
   `SessionYamlConfig` (:560), `ServerConfig` (:590), and
   `src/agent/config.rs` `AgentConfig` (:44-202).
3. Config file locations: compared CLI README:105-108/531/539/551 and
   `echo-agent-cli/docs/configuration.md:8-10,69,190-192,198-206` against
   `echo-agent-cli/echo-agent-app-core/src/config_discovery.rs:12-13,220-258`
   and `echo-agent-cli/src/main.rs:64-70` (`set_user_data_dir_name(".eko")`),
   plus `echo-integration/src/providers/config.rs:7,691-710`
   (echo-agent-models.yaml search chain).
4. `docs/zh/28-config-reference.md` field table (defaults) checked against
   `AgentConfig::new` defaults (`src/agent/config.rs:213-270`).

## Expected Result

Every documented key/feature/default resolves to a manifest feature or schema
field; documented file locations match discovery code.

## Result

- Feature table: canonical drift re-confirmed and cross-referenced only —
  `plan-execute`/`self-reflection` phantom rows + `full` membership errors
  (F-API-01-P2-01, F-FEAT-01-P3-01); six no-op marker features
  (F-FEAT-01-P2-02); files/shell always-on (F-FEAT-01-P2-01). No new feature
  finding.
- README `echo-agent.yaml` sample key validity (`echo-agent/src/config.rs`):
  - `model.{name,max_tokens,temperature}` — valid (ModelConfig).
  - `agent.{name,system_prompt,max_iterations,enable_tools,enable_memory}` —
    valid (AgentYamlConfig).
  - `channels.qq.{enabled,app_id,client_secret}` — valid; `channels.feishu.
    {enabled,app_id,app_secret,mode}` — valid; `channels.session.
    {timeout_minutes,reset_keywords,reset_commands}` — valid.
  - `mcp.config_path` — valid; `server.{host,port}` — valid.
  - `models:` and `embedding:` top-level sections — **INVALID placement**:
    `AppConfig` has no `models`/`embedding` fields (verified: zero
    occurrences); serde default drops unknown keys silently. These sections
    belong to the separate `echo-agent-models.yaml`
    (`echo-integration/src/providers/config.rs:7,691-710`; the runtime error
    seen in V02-01 tells users exactly this: "请在 echo-agent.yaml 中设置
    model.auth_token，或在 echo-agent-models.yaml 中配置该模型").
- CLI config locations (`echo-agent-cli`):
  - README:105-108 `./.echo-agent/echo-agent.yaml`, `~/.echo-agent/config.yaml`,
    MCP chain `./mcp.json → ./.echo-agent/mcp.json → ~/.echo-agent/mcp.json`
    — stale: actual global root `~/.eko/echo-agent.yaml`
    (config_discovery.rs:220-232; main.rs:66 override), actual MCP discovery
    `~/.eko/mcp.json` (config_discovery.rs:244-247) + `<root>/.mcp.json`
    (config_discovery.rs:254-258); no `./.echo-agent/` layer exists.
  - `docs/configuration.md:8-10,69,190-192` repeats the same stale chain;
    `:198-206` browser profile/output under `~/.echo-agent/browser/` — same
    stale root.
- `docs/zh/28-config-reference.md` field table vs `AgentConfig`:
  - `max_iterations` default documented as `10`; actual
    `AgentConfig::new` default is `100` (`src/agent/config.rs:217`, struct
    doc "default: 100"). All other sampled documented fields
    (`model_name/agent_name/system_prompt/role/temperature/max_tokens/
    enable_tool/enable_task/enable_human_in_loop/enable_subagent/enable_memory/
    enable_cot/allowed_tools/tool_error_feedback/force_read_before_edit/
    plan_mode/max_tool_output_tokens/tool_execution/memory_path/session_id/
    conversation_id`) exist in the struct; constructor signatures
    `minimal(model, prompt)`/`standard(model, name, prompt)`/
    `full_featured(model, name, prompt)` match
    (`src/agent/config.rs:278,288,297`).
- `memory_path` default `~/.echo-agent/store.json` documented in
  28-config-reference.md — consistent with the framework default
  (`paths.rs:63-66`); EKO overrides the root to `~/.eko` at startup
  (main.rs:64-66), which is precisely why EKO-facing docs must not use
  `~/.echo-agent`.

## Deviations

No commands executed in this face (static matrix). The `models:`/`embedding:`
silent-drop behavior was observed at runtime indirectly via V02-01
(demo32/demo00 both failed exactly on missing model config) but not re-run
with a config fixture here.

## Conclusion

Supports: documented config keys are mostly valid, but two families of config
drift exist — (a) the README Configuration sample places `models:`/`embedding:`
in the wrong file (silently dropped in `echo-agent.yaml`), and (b) all
EKO-facing `~/.echo-agent/` path claims (CLI README + configuration.md) are
stale vs the `~/.eko` runtime root. New findings: Q-DOC-01-P3-07 (README
models/embedding misplacement), Q-DOC-01-P3-08 (28-config-reference
max_iterations default), Q-DOC-01-P3-02 (CLI `~/.echo-agent` path claims —
shared evidence with V01). Feature-side drift is canonical
(F-API-01-P2-01, F-FEAT-01-P2-01/P2-02/P3-01) and not re-filed.

## Follow-Up

Faces V04 (stale terminology/architecture) and V05 (canonical aggregation)
continue; Q-FW-02 owns real per-feature compile verification.
