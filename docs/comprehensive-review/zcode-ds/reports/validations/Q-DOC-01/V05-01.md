# Q-DOC-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (synthesized by the main reviewer from the four completed
> validation faces after the reviewing session was interrupted by network
> failures)
> `echo-agent` commit: 9b0e0fa
> `echo-agent-cli` commit: b3b2e81
> Worktree state: clean (both repositories)

## Claim

The canonical V05 doc-drift findings archived by earlier tasks are aggregated
into one table so Q-DOC-01 does not re-file them, and any un-archived drift is
identified.

## Method

1. Read the four completed Q-DOC-01 faces (V01-V04) — they already fold in
   canonical references instead of re-filing.
2. Search task reports under `zcode-ds/reports/tasks/` for doc-drift
   findings filed with canonical IDs (grep `drift`/`stale`/`nonexistent`/
   `phantom` across the F-/A-/X- reports).

## Expected Result

A canonical-ID aggregation table; every previously filed doc-drift finding
appears exactly once (by ID), and Q-DOC-01's own P3 findings are disjoint
from it.

## Result

- Exit code: not-applicable (static aggregation)
- Duration: not-applicable
- Summary:

Canonical doc-drift findings aggregated (filed by earlier tasks, referenced
here, not re-filed):

| Canonical ID | Drift summary | Filed in |
|---|---|---|
| B-DOC-01 (V01-V04) | Historical audit/plan claims pointing at removed code paths; resolved to targeted revalidation | B-DOC-01 |
| A-TSK-02-P3-03 | Framework docs still reference removed `demo22_plan_execute` example (README.md:1086, README.zh.md:791, docs/zh/README.md:207, docs/en/README.md:212, examples/README.md:72) | A-TSK-02 |
| F-EVO-01-P3-03 | AGENTS.md:139,370 + roadmap still reference the removed `echo-agent-eval` crate; README:1109 references nonexistent `CritiqueStore` | F-EVO-01 |
| A-FE-01-P3-02 | Generated-artifact commit-state inconsistency: 79 `generated/*.ts` files formatting-only drift (raw ts-rs vs committed prettier); `prettier --check` fails after regeneration | A-FE-01 |
| A-BOOT-01-P3-06 | Stale "sqlite-backed"/`runtime_state.db` comments (infra.rs:125, state.rs:931) contradict file-backed stores | A-BOOT-01 |
| A-CFG-01-P3-01 | Same sqlite comment family in config docs (workspace/mod.rs:15) | A-CFG-01 / A-PROJ-01-P3-02 |
| A-CHAT-01-P3-01 | Stale docs: main.rs:31, PROJECT-ANALYSIS:13-23 contains a forbidden "worker" term | A-CHAT-01 |
| F-MAC-01-P3-05 | echo-macros README quickstart doesn't compile (missing dep + ToolResult mismatch); demo25_macros excluded from default example builds | F-MAC-01 |
| F-INT-01-P3-03 | Stale MCP doc references | F-INT-01 |
| F-INTENT-01-P3-04 | EKO doc drift on DomainProfile "intent router inference" and ChainedClassifier comment | F-INTENT-01 |
| F-SUB-02-P2-03 | Documented "member/mailbox lifecycle" doesn't exist — TeamCoordinator/TeamRunner/mailbox are dead public APIs (doc-vs-code drift) | F-SUB-02 |
| A-OUT-01-P3-01 | `output/mod.rs:9` `#![allow(dead_code)]` masks dead cluster with false "single facade for REPL and TUI" doc claim | A-OUT-01 |
| F-API-01-P2-01 | Public facade doc contract drift (feature/documentation consistency) | F-API-01 |

Q-DOC-01's own new findings (disjoint from the above): P3-01 (link
inventory), P3-02 (CLI README data-root/config paths + LICENSE), P3-03
(workspace tree claims + example counts), P3-04 (phantom catalog rows
demo14/demo16/demo63), P3-05 (demo37 documented command missing
`--features workflow`), P3-06 (demo32 acceptance classification vs live-LLM
dependency), P3-07 (README models/embedding misplacement), P3-08/09
(28-config-reference feature table drift: phantom rows `self-reflection`/
`plan-execute`/`sqlite`, missing real features a2a/lsp/telemetry/handoff/
topology/channels/statistics/eval/improve/testing/content-guard/
project-rules/rag), P3-10 (29-long-running-tasks.md and
knowledge/agent-patterns.md instruct nonexistent `execute_with_planning()`/
`src/agents/plan_execute/mod.rs`), P3-11 (CLI docs/architecture.md SQLite
storage claims contradict the no-SQLite invariant).

## Deviations

None.

## Conclusion

Supports: Q-DOC-01's finding set is complete and disjoint from the canonical
aggregation; all previously filed doc drift is carried by canonical ID.
Does not support: link *semantic* correctness beyond sampled faces, or
feature-compile truth (deferred to Q-FW-02).
