# Q-DOC-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: both source repositories clean

## Claim

Documentation contains no stale terminology or architecture claims: no
`worker` terminology, no references to the removed `echo-agent-eval` crate or
deleted `plan_*`/`todo_write` APIs, no SQLite claims in EKO docs, and no
references to nonexistent files, outside of already-canonical findings.

## Method

Case-insensitive recursive greps over docs/README trees of both repositories
(excluding `.git`, `target/`, and the other reviewers' directories):

```text
1. "worker" — echo-agent READMEs + docs + sub-crate READMEs + examples/README.md;
   echo-agent-cli README + docs + CHANGELOG
2. "echo-agent-eval|echo_agent_eval" — root AGENTS.md, docs/, both READMEs, CLI docs
3. "plan_execute|plan-execute|execute_with_planning|PlanExecute" — both READMEs,
   framework docs trees, CLI docs
4. "todo_write|plan_create|plan_patch|run_dag" — both READMEs + docs trees
5. "SQLite|sqlite" — CLI README + docs + CHANGELOG
6. Manifest cross-check: `echo-agent/Cargo.toml:65-103` `[features]` vs the
   feature rows in `docs/{zh,en}/28-config-reference.md:440-470`.
```

Every match was context-inspected and classified as real drift, canonical
(cross-referenced only), historical/migration note, or false positive.

## Expected Result

Zero new stale-term hits beyond canonical findings; feature tables in all docs
match the manifest.

## Result

| Search | Result |
|---|---|
| `worker` | Zero real hits. Only false positives: `docs/{zh,en}/12-mock.md:81` — the substring `work` inside `NetworkError` ("Net**work**"). Docs are consistent with the Subagent-only invariant (X-INV-01 zero-worker finding extends to documentation). |
| `echo-agent-eval` | Root `AGENTS.md:139,370` + `docs/superpowers/plans/2026-07-10-subagent-parity-roadmap.md:74,699` still reference the removed crate — **canonical F-EVO-01-P3-03**, cross-referenced only; CLI `docs/system-deep-dive/06-skills.md:427` correctly records the removal. |
| `plan_execute` family | `echo-agent/README.md` + `README.zh.md` (canonical F-API-01-P2-01 / A-TSK-02-P3-03); **new hits**: `docs/{zh,en}/28-config-reference.md:449/451` (`plan-execute`, `self-reflection` phantom feature rows), `docs/{zh,en}/29-long-running-tasks.md:27,42` (`execute_with_planning()` as the task-planning method entry — method deleted in M13), `docs/knowledge/{zh,en}/agent-patterns.md:3-4,142` (correction note tells users to use nonexistent `execute_with_planning()` and shows a code block headed `// src/agents/plan_execute/mod.rs` — nonexistent path); CLI `docs/2026-07-27-runtime-dag-kernel-convergence.md` — historical design doc, not drift. |
| `todo_write/plan_create/plan_patch/run_dag` | Only in CLI historical design docs and `docs/MASTER-PLAN.md:66` (which correctly records the removal as Complete) — classified current/historical, not drift. |
| `SQLite` in CLI docs | `echo-agent-cli/README.md` ("sessions/ 会话管理（SQLite + FTS）" — filed in V01 as part of Q-DOC-01-P3-02 evidence family); **new**: `docs/architecture.md:160` ("会话历史持久化（SQLite）"), `:190-191` ("存储后端: SQLite（默认）"), `:222` ("memory/ # 记忆存储 (SQLite)") — all contradict AGENTS.md CLI no-SQLite rule and X-INV-01's zero-SQLite finding; `:347-349` sqlite/postgres/mysql row is the agent's database-tool capability list (framework feature `database`), not storage architecture — not drift. |
| 28-config-reference feature table | `docs/zh/28-config-reference.md:440-470` / en equivalent lists `subagent`, `mcp`, `tasks`, `self-reflection` (phantom), `human-loop`, `plan-execute` (phantom), `web`, `shell`, `files`, `git`, `database`, `media`, `data`, `chart`, `research`, `sandbox` (no-op marker, F-FEAT-01-P2-02), `sqlite` — omits real features `a2a/lsp/telemetry/handoff/topology/channels/statistics/eval/improve/testing/content-guard/project-rules/rag`. |
| Nonexistent-file refs | `demo63_tiered_memory` in `examples/README.md:96` (file absent); `src/agents/plan_execute/mod.rs` in knowledge agent-patterns.md:142 (absent); `tool-permissions.md`/`16-plan-execute.md`/`19-self-reflection.md` (V01 inventory). |
| "67 registered tools" (README.md:181,389) | Not re-verified; counting requires tracing `echo_tools::register_all_tools` (echo-tools crate) — deferred to Q-FW-02 per F-API-01. |
| "8 crates, 1 import" (README.md:390) | Consistent: workspace = root `echo_agent` + 7 sub-crates. |

## Deviations

Only greps over `.md` files were performed; `.ts`/`.rs` source doc comments were
not re-searched for stale terms (F-*/A-* tasks and X-INV-01 covered source).
Cross-repo path references in CLI MASTER-PLAN tables were treated as
informational (V01).

## Conclusion

Supports: worker terminology is fully clean in docs; canonical drift
(F-API-01-P2-01, A-TSK-02-P3-03, F-EVO-01-P3-03, F-FEAT-01-P2-02) is confirmed
in place. New findings: Q-DOC-01-P3-09 (28-config-reference phantom feature
rows + missing real features), Q-DOC-01-P3-10 (29-long-running-tasks.md and
knowledge/agent-patterns.md instruct nonexistent `execute_with_planning()`
and `src/agents/plan_execute/mod.rs`), Q-DOC-01-P3-11 (CLI docs/architecture.md
SQLite storage claims contradict the no-SQLite invariant). Also confirmed:
`demo63` phantom row (V01 inventory, part of Q-DOC-01-P3-04 evidence).

## Follow-Up

Face V05 (canonical V05-drift aggregation) and the task report remain;
Q-FW-02 verifies tool counts and feature matrix compiles.
