# Q-DOC-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: both source repositories clean

## Claim

Relative links and code-path references in both repositories' READMEs, `docs/`
trees, and `examples/` catalog resolve to existing files, and the paths
described there match current code. Broken links are filed as findings.

## Method

1. Extracted every relative markdown link `[text](target)` (excluding anchors,
   `http(s)://`, `mailto:`) from the primary READMEs and checked existence of
   the target relative to the document directory, with `os.path.normpath`
   resolution (script `/tmp/check_links.py`):
   - `echo-agent/README.md` (149 relative links), `README.zh.md` (111),
     `examples/README.md`, `docs/en/README.md`, `docs/zh/README.md`,
     `echo-core/README.md`, `echo-agent-cli/README.md`.
2. Recursively checked all `.md` files under `echo-agent/docs/en`,
   `echo-agent/docs/zh`, `echo-agent/docs/knowledge`, `echo-agent-cli/docs`
   (88 + 88 + 12 + 74 relative links).
3. Cross-checked the README example catalog (`examples/demoNN_*.rs` mentions)
   against the on-disk `examples/` directory (68 files, numbered demo00-demo70
   with demo14/demo16/demo22/demo63 missing).
4. Verified the CLI README's `~/.echo-agent/` data-root and `sessions/`
   (SQLite + FTS) claims against `echo-agent-app-core/src/config_discovery.rs`,
   `infra.rs` doc comments, and `sessions/search.rs` header.

## Expected Result

All links resolve; README-described paths (`echo-agents/`, `skills/`,
`~/.echo-agent/`, `demoNN` catalog rows) match the repositories' actual layout.

## Result

- Exit code: not applicable (static inspection)
- Duration: ~10 minutes
- Summary: 33 broken relative links + 3 stale structural path claims found.
  Counts per document:
  - `echo-agent/README.md`: 7 broken (demo14, demo16, demo22 example links;
    `docs/{en,zh}/16-plan-execute.md` and `19-self-reflection.md` missing).
  - `echo-agent/README.zh.md`: 7 broken (same targets).
  - `echo-agent/docs/en/README.md`: 1 broken (`tool-permissions.md`).
  - `echo-agent/docs/zh/README.md`: 1 broken (`tool-permissions.md`).
  - `echo-agent/docs/en/`: 7 broken — `tool-permissions.md` (02-tools.md:270,
    README.md:75); `../examples/` wrong-depth links to
    demo64/demo53/demo65/demo66 and the `examples/` dir from
    getting-started.md:222 (resolve to nonexistent `docs/examples/`).
  - `echo-agent/docs/zh/`: 12 broken — `tool-permissions.md`
    (02-tools.md:270, README.md:73); wrong-depth `../examples/` links
    (04-compression.md:214, 40-context-system.md:403-404,
    getting-started.md:222); five wrong-filename learning links in
    getting-started.md:206-211 (`02-tool-system.md`, `04-streaming.md`,
    `05-context-compression.md`, `06-mcp.md`, `07-multi-agent.md` — actual
    names `02-tools.md`, `10-streaming.md`, `04-compression.md`, `08-mcp.md`,
    `06-subagent.md`).
  - `echo-agent/docs/knowledge/{en,zh}/skill-system.md`:130-131 —
    `references/checklist.md` and `references/style_guide.md` do not exist.
  - `echo-agent-cli/docs/getting-started.md:239` — `../echo-agent/README.md`
    is a cross-repository reference that cannot resolve inside the CLI repo
    (echo-agent is a sibling repository, not a subdirectory). Verified as the
    only cross-repo path reference in CLI docs; `docs/MASTER-PLAN.md` uses
    `../echo-agent/...` anchors only as cross-repo file pointers in tables
    (not links) — informational, not filed.
  - `echo-agent-cli/README.md` — `LICENSE` (badge :6 and "详见 LICENSE 文件"
    :601) does not exist in the CLI repo (exists only in echo-agent).
- Stale structural claims:
  - `echo-agent/README.md:291` tree lists `echo-agents/` (no such directory)
    and `:293` lists `skills/` (no such directory); the tree row names
    "PlanExecute" as an agent implementation, but the plan-execute API was
    deleted in M13 (docs/MASTER-PLAN.md:387).
  - `echo-agent/README.md:294` "64 runnable demos" vs `:389` "66 runnable
    examples" vs 68 files on disk vs 65 rows in the catalog (demo14/demo16/
    demo22 referenced but absent; demo63 also absent from catalog rows but
    present in `examples/README.md:96`).
  - `echo-agent-cli/README.md:105-108,531,539,551` document `./.echo-agent/`,
    `~/.echo-agent/config.yaml`, `~/.echo-agent/mcp.json`,
    `~/.echo-agent/workspaces/` — actual data root is `.eko`
    (`config_discovery.rs:12,220-221`, `infra.rs:1283-1298,1361`, user-facing
    strings `hooks.rs:23,63`).
  - `echo-agent-cli/README.md` structure tree: "sessions/ # 会话管理
    (SQLite + FTS)" — `sessions/search.rs:3-5` explicitly documents "EKO is
    local — no SQLite/FTS5"; no SQLite in CLI manifests.
- False positives identified and excluded: `docs/code-review-2026-07-03.md:31`
  `javascript:fetch(...)` is a code snippet inside a security write-up, not a
  link; `echo-agent/README.md` config-file references
  (`echo-agent.yaml`, `mcp.json`, `echo-agent.example.yaml`) all resolve.

## Deviations

None beyond those listed (all deviations are the broken links themselves).
Only existence of link targets was checked; anchor fragments (`#...`) inside
existing files were not validated.

## Conclusion

This validation supports: README/docs/example catalog link and path drift is
real and measurable — 33 broken links and 3 stale path claims across both
repositories. The demo22/demo14/demo16 catalog rows and missing
16/19 doc rows corroborate previously filed drift (A-TSK-02-P3-03 for demo22,
F-API-01-P2-01 for phantom features). New findings from this face:
Q-DOC-01-P3-01 (link inventory), Q-DOC-01-P3-02 (CLI README data-root/config
paths + LICENSE), Q-DOC-01-P3-03 (workspace tree claims + example counts).
This does not prove that all resolved links point to semantically correct
content (Face 3 samples that).

## Follow-Up

Faces V02 (command/example execution), V03 (feature/config matrix), V04
(stale terminology/architecture search), V05 (canonical drift aggregation)
continue under Q-DOC-01; the V02 example-run face will exercise a sample of
catalog examples whose links were checked here.
