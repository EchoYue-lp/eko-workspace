# A-TSK-03 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

There is exactly one live DAG execution/scheduling authority (`RuntimeDagExecutor` in `echo-orchestration`); EKO contains no second ready-frontier computation, no second retry state machine/loop, no second stall-detection loop, and no second cancellation loop — and no `worker` terminology anywhere in the touched surface.

## Method

Repository-wide definition + duplicate search across `echo-agent` and `echo-agent-cli` (working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

1. `grep -rn -E "ready_task_ids|select_ready|select_ownership|note_stalled|stalled|DAG stalled|external_progress_poll|cancellation_grace|refresh_in_flight|DagRefresh|RuntimeDagExecutor|RuntimeDagController|claim_task|requeue|retry_count|max_retries|max_concurrent_subagents" echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/ --include="*.rs"` and classify every hit as policy-adapter vs duplicate-authority.
2. Same-term sweep over all of `echo-agent-cli/echo-agent-app-core/src/` and `echo-agent-cli/src/` excluding `task_runtime/`, then inspect every non-task_runtime hit (`run_driver.rs`, `plugin_runtime.rs`, `hitl/dispatcher.rs`).
3. Legacy second-loop reachability: `grep -rn "RuntimeDagExecutor|RuntimeDagController" echo-agent/echo-orchestration/src/`, then trace who calls `TaskExecutor::execute_all` / `ManagedTaskDagController` and confirm production reachability.
4. `grep -rni "\bworker\b"` over `echo-agent/echo-orchestration/src/` and `echo-agent-cli/echo-agent-app-core/src/tasks/`.
5. Per-task cancel surface: `grep -rn "cancel_task"` in both repos; classify each hit as definition vs caller vs unrelated surface (`tauri/commands/tasks.rs:157` legacy TaskManager path).
6. Check EKO task_update surface for a status-reset path: `TaskUpdateOperation` variants in `task_runtime/types.rs:1255-1272`, framework `TaskPlanPatchOp` (`revisioned.rs:100-113`, `:547-560` Skip guard), `EkoTaskToolPolicy` (`revisioned_adapter.rs:80-130`) override of `allow_manual_progress_updates` (framework default `revisioned.rs:275-277`).

## Expected Result

- All scheduling-loop machinery (frontier, wave, retry re-dispatch, stall, cancellation grace, external polling) resolves to `echo_orchestration::tasks::RuntimeDagExecutor`; EKO only filters the framework frontier (`select_ready_wave`), writes one CAS requeue, writes a stall note, and owns run/pause cancel tokens.
- The legacy `TaskExecutor::execute_ready_tasks` / `ManagedTaskDagController` loop has no production caller in `echo-agent-cli`.
- Zero `worker` hits.
- `TaskRuntimeStore::cancel_task` has zero callers; EKO task_update cannot reset a Running/Cancelled task (no `SetStatus` op).

## Result

- Exit code: 0 (grep-based inspection; no commands failed)
- Summary: every claim held.
  1. `task_runtime/` scheduling hits are all policy adapters: `select_ownership_safe_wave` (executor.rs:1127-1145) is a product filter over the framework frontier, invoked only from the trait method `select_ready_wave` (executor.rs:1265-1282); retry = one CAS requeue (`requeue_claimed_task`, store.rs:1066-1105) invoked from `resolve_dispatch` policy (executor.rs:1396-1431); `note_stalled` (executor.rs:1615-1619) only writes a note; run cancel = `register_run_cancellation`/`request_cancel`/`request_pause` (store.rs:532-622) over the token passed into `RuntimeDagExecutor::execute`.
  2. Outside `task_runtime/`, the only matches are comments about mutex deadlock safety (`run_driver.rs:14`, `plugin_runtime.rs:1814`, `hitl/dispatcher.rs:73`) — not scheduling loops.
  3. `RuntimeDagExecutor`/`RuntimeDagController` are consumed by `runtime_executor.rs` (the live loop) and `executor.rs` (legacy `TaskExecutor::execute_all` → `ManagedTaskDagController`, `executor.rs:1415-1424, 1609`); `execute_all`/`execute_all_async` have zero production callers in either repository (grep over `echo-agent-cli` returned zero `TaskExecutor`/`TaskManager` usages).
  4. Zero `worker` term hits.
  5. `cancel_task`: EKO definition only (`store.rs:519`, zero callers); Tauri `commands/tasks.rs:157` is the legacy TaskManager surface (separate from task_runtime). `register_task_cancel_token`/`unregister_task_cancel_token` are live at executor.rs:1887-1907.
  6. EKO `TaskUpdateOperation` = Insert/Update/Skip/Reorder only (types.rs:1257-1272); framework `Skip` rejects non-Pending/Blocked tasks (revisioned.rs:554-557); `EkoTaskToolPolicy` does not override `allow_manual_progress_updates` (framework default `false`, revisioned.rs:275-277) so `set_status` is not exposed on EKO's task_update; framework `SetStatus` op exists only in the internal patch engine (revisioned.rs:109-112).

## Deviations

None. Note: the framework `TaskPlanPatchOp::SetStatus` (revisioned.rs:109-112) is a *latent* framework capability with no EKO tool exposure; it is not a second scheduling authority.

## Conclusion

Supports: "no second ready-frontier / retry / stall / cancellation loop in EKO; one live DAG execution authority". Does not prove runtime behavior of the loop itself (V03/V04 cover that). Finding F-TSK-03-P2-02's "per-task cancel is dead" is re-confirmed with EKO-side evidence (live registration, dead trigger, no status-reset tool surface).

## Follow-Up

Used by task report A-TSK-03 findings P2-01/P2-02 and layering table. None.
