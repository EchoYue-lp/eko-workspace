# A-TSK-03 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every live execution entry point in EKO (TUI resume, GUI resume, `task_execute` tool, background service, cron/agent-driven path) reaches exactly one executor — `echo_agent::tasks::RuntimeDagExecutor` — through `execute_run` → `execute_runtime_plan`, with the `EkoRuntimeDagController` as the only `RuntimeDagController` implementation wired in, and no other path constructs a DAG driver.

## Method

Registration + runtime reachability trace (static; working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

1. `grep -rn "execute_run("` over `echo-agent-cli/echo-agent-app-core/src/` and `echo-agent-cli/src/`; for each caller, read the surrounding block (`src/tauri/commands/task_runtime.rs:230-290`, `src/tui/events.rs:4700-4790`, `echo-agent-app-core/src/tasks/service.rs:380-480`, `task_execute_tool.rs:284-417`).
2. Confirm tool registration: `task_runtime/register.rs:45-154` (`register_task_tools_on_agent` registers framework `task_create/update/list` + EKO `CreateComplexTaskTool`/`CheckRunStatusTool`/`CancelRunTool` + `ExecuteTaskTool`), wired from GUI (`src/tauri/desktop.rs`) and TUI (`src/main.rs`) per register.rs module doc.
3. Trace `task_execute` execution: `task_execute_tool.rs:236-487` → run lock (`acquire_run_execution_lock`, :87-127) → `register_run_cancellation` (:379-389) → `execute_run` (:401).
4. Trace `execute_run` (executor.rs:321-585): drain loop (:366-434) → `execute_runtime_plan` (:1622-1683) → `RuntimeDagExecutor::new(...).execute(run_id, parent_cancel)` (:1645-1654) with `EkoRuntimeDagController` (:1147-1620) implementing `echo_agent::tasks::RuntimeDagController` (:1222).
5. Verify `EkoRuntimeDagController` implements the six callbacks the framework invokes: `load_snapshot` (:1227-1252), `claim_task` (:1254-1263), `select_ready_wave` (:1265-1282), `dispatch_task` (:1284-1346), `resolve_dispatch` (:1348-1562), `block_task` (:1564-1580), `failed_task_disposition` (:1582-1593), `interruption_outcome` (:1595-1613), `note_stalled` (:1615-1619).
6. Confirm no other `RuntimeDagExecutor::execute` construction path: grep `RuntimeDagExecutor::new` in both repos.

## Expected Result

- Exactly four `execute_run` call sites (Tauri resume, TUI resume, background service, task_execute tool) plus the agent-driven `drive_agent_run` path that can invoke `task_execute` from within a ReAct loop; all funnel into `RuntimeDagExecutor::execute`.
- Only `EkoRuntimeDagController` implements the controller trait on the EKO side (framework's second controller, `ManagedTaskDagController` in legacy `executor.rs`, has no production caller — V01-01).

## Result

- Exit code: 0 (inspection-based)
- Summary: chain confirmed end to end.
  - `execute_run` callers: `task_execute_tool.rs:401`; `src/tauri/commands/task_runtime.rs:262,364` (resume/drive); `src/tui/events.rs:4737` (TUI driver spawn); `echo-agent-app-core/src/tasks/service.rs:418` (background). `drive_agent_run` (executor.rs:3649) is the L1 ReAct path and can call `task_execute` as a tool; it performs no DAG scheduling itself.
  - Each caller registers the run cancel token (`store.register_run_cancellation`) so `request_cancel`/`request_pause` reach the live driver token.
  - `execute_run` requires the run to already be `Running` (executor.rs:337-339); resume callers call `store.resume_task_run` first (tauri command :244, tui events :4710, service paths).
  - `RuntimeDagExecutor::new` appears only in `execute_runtime_plan` (executor.rs:1645) and in the legacy framework `executor.rs:1422` (unreachable) — one live driver construction.

## Deviations

None.

## Conclusion

Supports: all EKO execution entry points share one `RuntimeDagExecutor` instance per `execute_runtime_plan` call, with a single controller implementation; per-run execution serialization is provided by the Weak-mutex `RUN_EXECUTION_LOCKS` (task_execute_tool.rs:55-127). Does not prove the loop's terminal correctness (V03/V04).

## Follow-Up

Used by task report "Current Path" section. None.
