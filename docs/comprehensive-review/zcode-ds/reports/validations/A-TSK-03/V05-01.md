# A-TSK-03 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical documents describing the EKO/framework execution boundary (`echo-agent-cli/docs/MASTER-PLAN.md` M13 section and `echo-agent-cli/docs/2026-07-27-runtime-dag-kernel-convergence.md`) still match current code: framework owns the single DAG loop (safe points, frontier, waves, cancellation, failure propagation, external polling, stall); EKO owns only product policy through `EkoRuntimeDagController`; no second loop/validator/retry state machine exists in EKO.

## Method

Read `MASTER-PLAN.md:216-234` ("As of 2026-07-27, dependency traversal is no longer an EKO-owned `run_dag` loop... The old 596-line application scheduling loop was deleted...") and the convergence doc ownership table (:53-66: framework owns revision safe-point reload, atomic claim, ready frontier, wave bound, cancellation, external in-flight observation, stall; EKO owns file/event projection, capability validation, review/acceptance, resource limits, worktree, durable result recovery, event mapping; "The adapter may select a product-safe subset of the ready frontier and resolve a dispatch... It may not implement another DAG loop, dependency validator, or generic retry state machine"). Compare against current source: `echo-agent/echo-orchestration/src/tasks/runtime_executor.rs:196-449`, `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:1147-1683`, `:366-434`, `:587-641`, `:1127-1145`.

## Expected Result

Every ownership claim maps to a live code anchor; no claim is contradicted.

## Result

- Exit code: 0 (inspection-based)
- Summary:
  - "Framework owns the ready frontier / waves / cancellation / failure propagation / external in-flight polling / stall detection" — **current**: `runtime_executor.rs:206-314` (loop top, cancelled-snapshot, external poll, stall), `:339-448` (wave, in-wave cancel, resolve, stop outcome).
  - "EKO injects file-backed snapshots, review, resource/file ownership policy, worktree integration, events through `EkoRuntimeDagController`" — **current**: `executor.rs:1147-1620` (controller), `:1622-1683` (execute_runtime_plan), `select_ownership_safe_wave` `:1127-1145`.
  - "EKO may not implement another DAG loop, dependency validator, or generic retry state machine" — **current**: V01-01 found no such loop; EKO retry is a single CAS requeue-to-Pending (`store.rs:1066-1105`) inside `resolve_dispatch` policy; drain loop is a completion gate (`executor.rs:366-434`).
  - "Stall/deadlock outcome" framework-owned — **current**: `runtime_executor.rs:287-313`; EKO `note_stalled` (`executor.rs:1615-1619`) only writes a note.
  - Convergence doc claim "Fixed skipped-plan nodes so they count as deliberately resolved instead of producing a false DAG stall" — **current but incomplete**: all-skipped graphs complete (`runtime_executor.rs:271-273`); mid-DAG skip-with-dependents still stalls (F-TSK-02-P1-01, re-confirmed in V03-01 context).
  - MASTER-PLAN claim about atomic `TaskClaim` and claim-conflict reload ("A claim conflict reloads the snapshot instead of failing the task") — **current**: `runtime.rs:58-63`, `store.rs:986-1029`, `runtime_executor.rs:352-358`.

## Deviations

None. No historical claim about the ownership boundary was contradicted; the new findings (A-TSK-03-P1-01/P2-01/P2-02) concern behavior *inside* the documented boundary (how the framework consults the controller on in-wave cancellation; fault handling at the boundary), not a boundary violation.

## Conclusion

Supports: the layered authority described by the historical documents is intact and matches current code. The documents do not make any claim about pause-during-wave or mid-wave fault semantics, so no drift is recorded there.

## Follow-Up

Used by the task report's Historical Claim Status table.
