# A-TSK-03 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The EKO adapter/controller honors the documented product invariants when the framework executor drives it: (a) the controller is consulted for every cancellation (pause vs cancel); (b) a per-task controller/persistence fault mid-wave cannot leave sibling Running claims unresolved in a non-recoverable way; (c) the advertised per-task cancel surface works or is not advertised; (d) basic DAG execution (dependency order, revision insertion, no re-dispatch of in-flight tasks, partial-wave blocking) behaves per the documented contract.

## Method

Static invariant inspection (working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`) of:

1. Framework cancel paths in `echo-agent/echo-orchestration/src/tasks/runtime_executor.rs`: loop-top cancel check (:206-209), in-wave cancel branch (:390-414), `pending_outcome` hardcode (:416), post-wave return (:443-447), external-poll cancel (:276-285); and which paths consult `controller.interruption_outcome` (:207-209, :278-281) vs not (in-wave).
2. EKO pause contract: `store.rs:595-622` (`request_pause`: transition-to-Paused first, then cancel token; comment "The executor observes the durable Paused status and leaves the run resumable"), `executor.rs:1595-1613` (`interruption_outcome` returns Paused iff durable status Paused), `executor.rs:499-526` + `:643-661` (`finalize_cancelled_run_state` on the Cancelled outcome), run transition table `types.rs:517-527` (Paused→Cancelled legal), pause triggers `src/tauri/commands/task_runtime.rs:463`, `src/tui/events.rs:4346`, existing test `executor.rs:5757-5783` (`runtime_plan_cancellation_preserves_explicit_pause` seeds Paused before a pre-cancelled token — loop-top path only).
3. Mid-wave fault propagation: framework wave closure error propagation `runtime_executor.rs:348-365` (`?` on semaphore/claim errors), `:379-381` (wave aborts on first `Err`), `:418-421` (`resolve_dispatch(...).await?` drops remaining wave results), JoinSet-drop abort of siblings; EKO terminal handling `executor.rs:570-582` (Err → run Failed, no Running-task reset) vs `:643-661` (cancel path resets tasks) vs `:546-559` (pause path resets Running→Pending); in-process recovery surface: `TaskUpdateOperation` variants `types.rs:1255-1272`, framework Skip guard `revisioned.rs:547-560`, `EkoTaskToolPolicy` no set_status override (`revisioned_adapter.rs:106`, framework default `revisioned.rs:275-277`), `retry_blocked_task` guard `store.rs:1244-1251`, resume-hang mechanics `runtime_executor.rs:276-285` + `runtime.rs:353-363, 438-458` (Running = in_flight), boot recovery `store.rs:1631-1774`.
4. Per-task cancel surface: registration/unregistration `executor.rs:1883-1907`, `store.rs:497-528`, zero `cancel_task` callers (V01-01), framework Cancelled semantics `runtime_executor.rs:267-269, 435-439`.
5. Basic DAG behavior cross-check against the EKO test suite (tests present in executor.rs and store.rs, executed in V04): `runtime_plan_respects_dependency_order`, `runtime_plan_applies_inserted_revision_after_active_wave`, `runtime_plan_does_not_redispatch_in_flight_running_tasks`, `wave_processes_all_results_when_one_task_blocks`, framework `executor_follows_dependencies_and_safe_point_revision`, `claim_conflict_reloads_without_failing_or_dispatching_stale_work`.

## Expected Result

- (a) Every cancellation path consults `interruption_outcome` so a durable Paused run stays Paused even when the cancel fires mid-wave; the pause path never force-transitions Paused→Cancelled.
- (b) A wave fault resolves or explicitly abandons sibling claims; the run is left in a state recoverable in-process.
- (c) The per-task cancel comment matches framework semantics or the surface is absent.
- (d) The DAG tests above pass.

## Result

- Exit code: 0 (inspection; test execution in V04-01..04)
- Summary: (a) **failed** — the in-wave cancellation branch (runtime_executor.rs:390-416) hardcodes `pending_outcome = Cancelled` and never consults `interruption_outcome`; EKO `execute_runtime_plan` maps Cancelled → `RunOutcome::Cancelled` and `execute_run`'s Cancelled branch runs `finalize_cancelled_run_state` (executor.rs:508, 643-661), which force-transitions the durably-Paused run to Cancelled (legal per types.rs:522). `interruption_outcome` is consulted only at the loop top (:207-209) and the external-poll branch (:278-281) — i.e., only when no wave is in flight. A user pause request during an active wave (the common case; `request_pause` cancels the shared run token, store.rs:620) therefore terminates the run as Cancelled: in-flight subagent work aborted after the grace period, tasks flipped to terminal Cancelled, run not resumable. The existing test only covers the loop-top case. → finding A-TSK-03-P1-01.
  (b) **failed** — any `claim_task`/`resolve_dispatch`/semaphore error aborts the whole wave (runtime_executor.rs:348-365, 379-381, 418-421); siblings whose durable claims were CAS'd but whose dispatch futures were aborted are never resolved. EKO then marks the run Failed (executor.rs:570-582) without resetting the Running siblings, unlike the cancel (:643-661) and pause (:546-559) paths. In-process recovery is impossible: `task_update` cannot reset Running tasks (Skip rejects non-Pending/Blocked at revisioned.rs:554-557; EKO has no SetStatus op, types.rs:1257-1272; EkoTaskToolPolicy does not enable set_status), `retry_blocked_task` rejects Running (store.rs:1244-1251), and a same-process resume re-enters the framework executor which treats the orphaned Running tasks as in-flight (runtime.rs:353-363) and polls `external_progress_poll_interval` forever (runtime_executor.rs:276-285). Only boot-time `recover_incomplete` (store.rs:1631-1774) resets them. EKO store faults that trigger the abort include the torn `events.jsonl` tail (A-TSK-01-P1-01, file_shadow.rs:362-379) and run-lock poison. → finding A-TSK-03-P2-01 (EKO-side manifestation/aggravation of F-TSK-03-P2-01).
  (c) **failed** — per-task token registration is live (executor.rs:1887-1907) but the trigger `store.cancel_task` has zero callers; the comment at executor.rs:1883-1886 ("stop this Subagent promptly without cancelling sibling tasks") contradicts the framework's rule that any Cancelled task cancels the run (runtime_executor.rs:267-269, 435-439). → finding A-TSK-03-P2-02 (re-verification of F-TSK-03-P2-02).
  (d) **passed** — dependency-order, inserted-revision, no-redispatch-in-flight, partial-wave-blocking, safe-point-revision, and claim-conflict-reload invariants are covered by tests that pass (V04-01..04).

## Deviations

The pause-during-wave behavior was established by code trace only; no live run was executed (read-only review). Confidence in the trace is high (the select! branch, the hardcoded `pending_outcome`, and the legal Paused→Cancelled transition are all deterministic). EKO store-fault triggers for (b) were not provoked dynamically.

## Conclusion

This validation failed on three invariants (a, b, c) and produced findings A-TSK-03-P1-01, A-TSK-03-P2-01, A-TSK-03-P2-02; it passed on the basic DAG execution invariant (d). The failure does not contradict the task's core authority question (no second scheduling loop — V01-01/V02-01); it shows the controller boundary mishandles cancellation and fault outcomes.

## Follow-Up

Used by all three findings in the task report. A dynamic pause-during-wave fixture is proposed in A-TSK-03-P1-01's regression validation (Q-FLT-02).
