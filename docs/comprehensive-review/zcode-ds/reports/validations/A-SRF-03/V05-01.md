# A-SRF-03 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: see V01-01.

## Claim

Historical documents describing the frontend chat state contract are
classifiable as current/fixed/stale/regressed against the reviewed code, and
no doc claim hides a current defect.

## Method

Grep + targeted reads of the frontend-relevant historical claims:

```text
grep -n "chat://event\|TurnStatus\|run_status\|前端\|ChatEvent\|interrupt" docs/MASTER-PLAN.md
grep -n "TurnStatus\|chat://event\|run_status" echo-agent-cli/docs/2026-07-16-agent-lifecycle-audit.md
sed -n '1,80p' echo-agent-cli/docs/2026-07-11-running-input-interrupt-design.md
sed -n '1,60p' echo-agent-cli/docs/gui-status.md
```

## Expected Result

Each claim maps to current code with a classification; no claim asserts
behavior that current code contradicts without that contradiction being
captured as a finding.

## Result

- Exit code: not-applicable (doc comparison)
- Summary:
  - `docs/MASTER-PLAN.md:124/:342` "从用户输入到最终结果只存在一条权威生命
    周期;…前端只改变策略或渲染,不改变状态语义" / "GUI 也不得维护只在前端存
    在的权威运行状态" — **regressed** by A-SRF-03-P1-01 (frontend turn state
    stranded with no backend lifecycle) and A-SRF-03-P1-02 (reducer writes
    `completed` on error without a backend fact).
  - `docs/MASTER-PLAN.md:411` "投影:Tauri events…前端 Zustand store" —
    **current** (V02-01/V03-02).
  - `docs/MASTER-PLAN.md:453` "普通 chat 只有 turn terminal,不再伪造
    TaskRun;前端 chat status 不再覆写 TaskRuntime" — **current**: the
    `run_status` handler writes only chatStore (V01-01) and a regression
    test locks it.
  - `docs/MASTER-PLAN.md:770` "…turn status…interrupt 不再通过默认 no-op 静默
    丢失" — **regressed**: the GUI interrupt path returns before a turn
    lifecycle exists and strands frontend state (P1-01); the
    `ChatDriverEvent::Interrupt` wire variant is dead (A-CHAT-01-P2-01,
    cross-verified).
  - `echo-agent-cli/docs/2026-07-16-agent-lifecycle-audit.md:19/:142/:187`
    "chat run_status 会直接覆写 taskRuntimeStore.activeRun.status" — **fixed**:
    current `chatEventHandler.ts:159-180` touches only the chat store; test
    "does not project a chat terminal status onto the active TaskRun" exists.
  - `echo-agent-cli/docs/2026-07-11-running-input-interrupt-design.md:16-20`
    (queued inputs must dispatch "当前 turn 终态后" one at a time) —
    **regressed** in the interrupt case: no terminal ever arrives for the
    ghost key, so the visible FIFO never drains (P1-01); the doc's steering
    concepts (`turn/steer` with `expectedTurnId`) are **stale** relative to
    the Tauri command implementation, though `steer_chat_message` does carry
    the expected-turn-id precondition (chat.rs:745-751).
  - `echo-agent-cli/docs/gui-status.md` "Chat and streaming: Connected" —
    **current** (surface exists and streams), with the caveats in this
    report's findings.
  - `web-frontend/src/types/api.ts:27-31` "ChatRunStatus (enum with
    frontend-only states like 'connecting')" — **stale**: no `connecting`
    member exists (P3-01).

## Deviations

None.

## Conclusion

Supports the classification table in the task report; two doc-declared
invariants (single authoritative lifecycle; interrupt not silently lost) are
regressed by current code and are now findings. Does not prove: any claim not
listed above.

## Follow-Up

None.
