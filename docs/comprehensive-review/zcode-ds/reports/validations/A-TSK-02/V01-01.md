# A-TSK-02 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repos)

## Claim

There is no forbidden parallel task CRUD (`todo_write`, `plan_create`,
`plan_patch`, `plan_execute`) anywhere in either repository's production code,
no independent Todo/Plan store or CRUD tool, no global todo search, and no
hidden process-global task-authoring state; the only `plan_*`-sounding names
are conversion helpers (`to_task_plan_patch`) and documentation/examples.

## Method

1. `grep -rn "todo_write|plan_create|plan_patch|plan_execute" echo-agent/ echo-agent-cli/` over all `.rs/.ts/.tsx/.md` excluding `target/` and `docs/comprehensive-review/`.
2. `grep -rn "search_todo|search_task|find_todo|all_todos|global_todo|list_all|search_runs|find_task|todo.*search"` over `echo-agent-cli/echo-agent-app-core/src/tasks/` and `echo-agent/echo-orchestration/src/tasks/`.
3. Grep for process-global state in `echo-agent-cli/echo-agent-app-core/src/tasks/`: `static | OnceLock | LazyLock | thread_local`.
4. Grep for direct TodoStatus writes outside `store.rs`/`executor.rs`.
5. Grep for parallel plan CRUD tool names in `echo-agent-cli/src/` (Tauri/CLI surfaces).
6. Check `examples/` directory listing vs README references.

## Expected Result

Zero production definitions of the forbidden tool names; no global todo search
function; global statics in `tasks/` are operational only (locks, per-turn RAII
registry, task_locals), never task-graph state; status writes outside
store/executor are tests or different concepts (cron).

## Result

- Exit code: not-applicable (static grep)
- Duration: < 5 min
- Summary:
  - Forbidden names: `todo_write` appears only in `echo-agent/src/agent/react/builder.rs:1181` as a negative assertion (`assert!(!names.iter().any(|name| name == "todo_write"))`), in historical docs, and in `echo-agent-cli/docs/2026-07-28-task-tools-framework-migration-design.md`; `plan_create`/`plan_patch`/`plan_execute` appear only in docs and README/demo references (`demo22_plan_execute`, see V05-01). Zero production definitions, zero registrations.
  - `to_task_plan_patch` (`types.rs:1284`, called `revisioned_adapter.rs:328`, `store.rs:913`) is a DTO-to-framework-patch conversion, not a `plan_patch` tool.
  - Global todo search: zero matches. Reads are per-run (`list_todos(run_id)`) or run-level (`list_runs_in`); no cross-run task/todo search anywhere.
  - Process-global state in `tasks/`: `RUN_EXECUTION_LOCKS` (task_execute_tool.rs:55-56, per-run Weak<TokioMutex>, lazy-reclaimed), `TASK_RUNTIME_PROJECTION_REGISTRY` (compact_context.rs:126-127, per-turn RAII `register` at chat_driver.rs:255-258 with `Drop` unregister, only tests call `register` directly), `tokio::task_local!` scopes (CURRENT_RUN_ID/CURRENT_CANCEL/CURRENT_CHAT_RESOURCES). None holds task-graph state; the graph itself lives in `~/.eko/tasks/` files (A-TSK-01 V01-01).
  - TodoStatus writes outside store/executor: `retry_blocked_task` (store method, atomic) and tests (ledger.rs:237, memory_bridge.rs:326, file_store.rs:482); `cron.rs:297-306` is a different `CronTaskStatus` concept.
  - Registered authoring tool inventory: framework `task_create/task_update/task_list` (`echo-orchestration/src/tasks/task_tools.rs:15-203`) + EKO `task_execute`/`create_complex_task`/`check_run_status`/`cancel_run`; `tool_exposure.rs:69` `TASK_TOOLS = ["task_create","task_update","task_list","task_execute"]` visible in Chat/Task/Auto; `create_complex_task/check_run_status/cancel_run` disabled in Chat/Task, enabled in Auto only (tool_exposure.rs:140-160).
- Log/artifact: none created.

## Deviations

None. `echo-agent/examples/demo22_plan_execute.rs` does not exist (referenced
only in docs — see V05-01 finding A-TSK-02-P3-03).

## Conclusion

Supports: forbidden parallel CRUD is absent in production code; no global todo
search; no hidden global task-authoring state; authoring tool inventory is the
single task family + EKO run-level tools. Does not prove runtime behavior of
those tools (V02/V03/V04).

## Follow-Up

V02-01 (registration/reachability), V03-01 (schema parity).
