# A-TSK-02 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repos)

## Claim

The historical claims about the single task-relation API — `todo_write`
removed, `plan_create/plan_patch/plan_execute` replaced, `TaskPlan`/`TodoItem`
only artifact/UI projections, all modes exposing the same task family — still
match current code, and no stale `plan_*` tool reference misleads consumers
except documentation references to a removed example.

## Method

1. Read `echo-agent-cli/docs/MASTER-PLAN.md` lines ~66, ~285, ~442-445.
2. Read `echo-agent-cli/docs/2026-07-27-runtime-dag-kernel-convergence.md`
   lines ~174-191 (Phase 5: Unified Task Relation API).
3. Read `echo-agent-cli/docs/2026-07-28-task-tools-framework-migration-design.md`
   relevant claims (lines ~47, ~141).
4. Verify each claim against code (grep for `todo_write`, `plan_*` tools;
   registration sites; tool_exposure mode groups; TaskPlan/TodoItem type roles
   in types.rs:835,1323).
5. Check `echo-agent/examples/` directory listing against README example table.

## Expected Result

Claims current; the only stale `plan_*` reference is the missing
`demo22_plan_execute` example in framework docs.

## Result

- Exit code: not-applicable (doc-to-code comparison)
- Duration: < 10 min
- Summary:
  - MASTER-PLAN.md:442-445 "task tools replaced todo_write; TaskPlan and
    TodoItem remain only artifact/UI projections; do not reintroduce plan_*
    CRUD tools or an independent todo store" — **current** (V01-01 zero
    definitions; types.rs:835,1323 doc comments classify the projections).
  - MASTER-PLAN.md:66 migration row (framework 38da658 / app 64da422, EKO
    TaskRuntimeStore implements RevisionedTaskStore) — **current**
    (revisioned_adapter.rs:36-56).
  - runtime-dag-kernel-convergence.md:174-191 — **current**: no
    plan_create/plan_patch/plan_execute tools or aliases anywhere;
    `update_tasks`/`apply_eko_task_update` share `TaskUpdateRequest`
    (revisioned_adapter.rs:321-339); Chat/Auto/Task expose the same
    task_create/task_update/task_list/task_execute family
    (tool_exposure.rs:69,80-100, mode snapshot tests at :218-275).
  - task-tools-migration-design.md:47,141 (separate task tools instead of one
    action-switched todo_write) — **current** (four distinct framework tools).
  - `demo22_plan_execute` is referenced in `echo-agent/README.md:1086`,
    `README.zh.md:791`, `docs/zh/README.md:207`, `docs/en/README.md:212`,
    `examples/README.md:72` but the file does not exist in `examples/` (no
    demo14/demo22 files; newest is demo41). **stale** — the example was
    removed with the plan_* API but its name remains in docs. Recorded as
    finding A-TSK-02-P3-03.
- Log/artifact: none.

## Deviations

The missing-example reference is the only deviation found.

## Conclusion

Supports: the single-API convergence claims are current; the only drift is a
P3 documentation reference to a removed example whose name carries the
forbidden `plan_execute` term.

## Follow-Up

None; feeds A-TSK-02 report findings.
