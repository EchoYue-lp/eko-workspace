# F-SEC-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Guard/security/sandbox paths contain no AGENTS.md-banned constructs (panic-on-input APIs, byte slicing on UTF-8 text, path traversal escapes) that are reachable from untrusted input, and path validators prevent traversal on the canonical paths.

## Method

1. Manual scan of every reviewed file for `unwrap`/`expect`/indexing/byte-slice patterns: `echo-core/src/guard/*.rs`, `echo-core/src/sandbox.rs`, `echo-execution/src/sandbox/*.rs`, `echo-execution/src/risk.rs`, `echo-agent/src/security.rs`, `echo-tools/src/security.rs`, `echo-tools/src/shell.rs`, `echo-tools/src/code.rs`, `src/a2a/serve.rs` (`parse_method`), `echo-core/src/plugin/variables.rs`, `src/eval/runner.rs`.
2. Path traversal analysis: `echo-tools/src/security.rs` `validate_file` (:196-263), `validate_within_base` (:272-343), `validate_output_file` (:352-398), `normalize_absolute_path` (:829-850); `echo-tools/src/files/mod.rs` (independent resolver); callers of each.
3. Re-rated the 6 B-DOC-01 current AUDIT items against the local threat model with fresh anchors.

## Expected Result

No reachable panic path; `validate_file`/`validate_within_base` are symlink-safe (canonicalize + starts_with); `validate_output_file` may have a lexical-only gap for not-yet-existing outputs; each of the 6 current AUDIT items is re-rated P3 or below under the local model.

## Result

- Exit code: not-applicable (static inspection; targeted tests in V05-01..04 all pass)
- Summary of the panic/UTF-8 scan (reachable-input paths only):
  - GuardManager joins with error mapping, no panic (`echo-core/src/guard/mod.rs:155-158`); ContentGuard `replace_range` uses regex match ranges, which are char boundaries (`content.rs:102-117`); LlmGuard `parse_guard_response` slices at ASCII `{`/`}` indices (`llm.rs:99-107`) — safe; fail-closed on parse failure.
  - Sandbox: `RetainedPipeOutput::push` uses `bytes.get(..)` (`local.rs:817`); `IncrementalUtf8Decoder` handles split multibyte sequences (`local.rs:828-880`, tested by `local_sandbox_stream_decoder_preserves_split_unicode`); `retain_utf8_prefix` is char-safe (`echo-core/src/sandbox.rs:309-322`); k8s `saturating_mul` (`k8s.rs:177`); all `unwrap_or_default`/`unwrap_or` usages are defensive.
  - `redact_secrets` checks `is_char_boundary` before `replace_range` and warn-skips otherwise (`security.rs:113-125`); `ssrf_safe_request_full` relative-redirect byte slicing is boundary-safe because `/` (0x2F) cannot occur as a UTF-8 continuation byte (`echo-tools/src/security.rs:800-811`); `parse_method` slices only at ASCII offsets of `"` and `:` (`src/a2a/serve.rs:299-318`) — safe.
  - Seatbelt profile injection: `validate_sandbox_path` rejects `(`, `)`, `\n`, `;`, `#`, `"`, NUL before embedding paths (`local.rs:458-490`), with tests (`:1456-1475`); bubblewrap uses argv, not profile text.
  - Path traversal: `validate_file` canonicalizes then `starts_with` denied/roots (`security.rs:213-246`); `validate_within_base` rejects `..` lexically, canonicalizes the nearest existing ancestor, reconstructs the path, re-checks denies (:285-341) — sound. `validate_output_file` is lexical-only (`normalize_absolute_path`, :829-850) — a not-yet-existing output under a symlinked subdirectory can resolve outside the declared root at write time (e.g., root `/workspace`, output `/workspace/link/out.txt` where `/workspace/link -> /etc` passes the lexical check). `validate_within_base` — the documented "canonical validator" that closes this class (:269-271) — has zero callers (grep). Callers of `validate_output_file`: `excel.rs:351,925,1356`, pdf/image/word/statistics use `validate_file`. Local-model impact: unintended write outside workspace (visible, undoable, single-user) — finding F-SEC-01-P3-06.
- Re-rating of the 6 B-DOC-01 current AUDIT items (local model):
  1. Eval `sh -c` (B-DOC-01 P2-01): partially stale — `SweBench` now validates `test_command` via `validate_shell_command` (`src/eval/runner.rs:343-346`, `:680-689`), but (a) the list omits `\n` (newline is a separator in `sh -c`) and (b) `SuccessCriteria::TestPass` still calls `run_command` unvalidated (`runner.rs:226-237`, `:695-704`). Re-rated P3 (local datasets, user-to-self; rises to P2 only if eval datasets are loaded from untrusted remote sources) — finding F-SEC-01-P3-01.
  2. Plugin `unsafe { set_var }` (B-DOC-01 P3-01): current; single-thread contract documented + key charset validated (`echo-core/src/plugin/variables.rs:177-211`); stays P3 — finding F-SEC-01-P3-08.
  3. `parse_method` hand-rolled (AUDIT 2.8): current, proptest-hardened (`src/a2a/serve.rs:296-321`, tests :323+); first-occurrence heuristic can misroute `tasks/sendSubscribe` detection when `"method"` appears earlier inside params; functional (SSE vs sync routing) only — P3, finding F-SEC-01-P3-09.
  4. Fallback `Client::new` (AUDIT 3.2): current at 5 sites (`web/fetch.rs:43-46`, `media/web_fetch_enhanced.rs:29`, `media/image_fetch.rs:125`, `web/providers/tavily.rs:39`, `duckduckgo.rs:37`, `brave.rs:33`); reachable only when `Client::builder()` fails (rare); the fetch.rs fallback result is stored in a `#[allow(dead_code)]` field never used for requests (fetch path uses `ssrf_safe_get`, `fetch.rs:201`); provider fallbacks drop only the timeout. P3 — finding F-SEC-01-P3-05.
  5. Security TODOs (AUDIT 6.1): current (`src/security.rs:3-12`); local impact is false-positive redaction ("Password in URL"/"Generic Token" match harmless docs), no exposure — P3, finding F-SEC-01-P3-10.
  6. Dead-code annotations (AUDIT 6.2): 37 `#[allow(dead_code)]` in `src/`; security-relevant live example: `execution.rs:225` (dead duplicate of live `snapshot.rs:878` output guard) — P3, finding F-SEC-01-P3-07; plus `echo-tools/src/web/fetch.rs:56` dead client field.

## Deviations

B-DOC-01's P2-01 anchor (`runner.rs:340` "validates only repo_url, not the command") does not match current code at the same commit: `runner.rs:343-346` now validates `test_command` for the SweBench variant. The finding is only partially current (TestPass variant + newline gap). Recorded as a historical-claim correction in the task report.

## Conclusion

Supports: no reachable panic/UTF-8 hazards in the reviewed security paths; `validate_file`/`validate_within_base` are traversal-safe; the 6 current AUDIT items are all P3-or-below under the local threat model with the eval finding narrowed to the TestPass variant and the newline gap. Does not prove: exhaustive absence of panics in files outside the reviewed set (37 dead-code annotations and other modules remain for Q-STA-01).

## Follow-Up

Findings F-SEC-01-P3-01, -05, -06, -07, -08, -09, -10 reference this report.
