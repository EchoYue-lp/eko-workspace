# F-SEC-01 / V05 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

`cargo test -p echo_core --lib --locked guard` exits 0 when the `guard` feature is enabled; without the feature the module is compiled out by design.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_core --lib --locked guard
cargo test -p echo_core --lib --locked --features guard guard
```

## Expected Result

First invocation (default features, `default = []` in echo-core/Cargo.toml): 0 matching tests, exit 0 — confirms guards are opt-in. Second invocation: guard content/rule/llm tests run, exit 0.

## Result

- Exit code: 0 (both invocations)
- Duration: ~3s each (incremental)
- Summary:
  - default features: `running 0 tests ... 288 filtered out`, exit 0.
  - `--features guard`: `test result: ok. 16 passed; 0 failed; 0 ignored; 0 measured; 288 filtered out` (content guard detect/redact/reject, rule guard keyword/pattern/length/direction, LLM guard parse incl. fail-closed cases).
- Log/artifact: none

## Deviations

None (feature-gated compile-out behaves exactly as designed; the task card's command needed `--features guard` to reach the guard tests).

## Conclusion

Supports: guard module is feature-opt-in (no default over-gating, V01), and its unit tests — including LLM-guard fail-closed parsing and ContentGuard redaction — are green with the feature enabled.

## Follow-Up

None.
