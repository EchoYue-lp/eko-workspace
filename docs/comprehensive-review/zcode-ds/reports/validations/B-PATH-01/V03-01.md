# B-PATH-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-PATH-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Feature gating is reachability-consistent: `tui` gates the TUI module and
TUI entry; `gui` gates the tauri module, the desktop binary and desktop
routing; `channels` gates the channels module and IM mode; a no-feature
build is a compile error; GUI-only builds route the default binary to the
desktop runtime.

## Method

1. Read `src/lib.rs` (18 lines) module gates.
2. Read `src/main.rs` feature branches (`:15-23`, `:75-91`, `:94`, `:114-124`,
   `:240-345`, `:358-411`, `:459`).
3. Read `echo-agent-cli/Cargo.toml:21-46` feature/[[bin]] definitions and
   `src/cli/mod.rs` channels gate.
4. Grepped for `cfg(feature = "gui")`, `cfg(feature = "tui")`,
   `cfg(feature = "channels")` across both binary crates and app-core.

## Expected Result

- `gui` gates: `src/tauri` module (lib.rs), `echo-agent-tauri` bin
  (manifest required-features), desktop routing in main.rs, `src/tauri`
  commands.
- `tui` gates: `src/tui` module, TUI entry block, tui-specific deps.
- `channels` gates: `src/cli/channels.rs`, `run_channels_mode` export,
  channel entry block.
- app-core is feature-agnostic (no cfg on these three features).

## Result

- `src/lib.rs`: `tauri` module behind `#[cfg(feature = "gui")]`
  (with `#[allow(clippy::unreachable)]` note for generated IPC glue,
  `:9-13`); `tui` module behind `#[cfg(feature = "tui")]` (`:15-16`).
- Manifest: `tui` feature brings ratatui/crossterm/arboard/etc.
  (`Cargo.toml:23-28`); `gui` brings tauri + plugins + `channels`
  (`Cargo.toml:29-34`); `echo-agent-tauri` bin `required-features = ["gui"]`
  (`Cargo.toml:43-46`).
- main.rs: `:75-76` gui-only → `run_desktop_entry`; `:88-91` no-mode
  `compile_error!`; `:114-124` tui logging split; `:240-345` TUI block
  gated on `tui` with a `#[cfg(not(feature = "tui"))]` fallback error
  (`:341-345`); `:358-411` channels block with `#[cfg(not(feature =
  "channels"))]` error log (`:406-411`); tests gated on
  `#[cfg(all(test, feature = "tui"))]` (`:459`).
- `src/cli/mod.rs:4-5`: `channels` module gated on `channels`; `:12-13`
  `run_channels_mode` re-export gated.
- `src/cli/channels.rs` and `src/cli/modes.rs::run_channels_mode` are the
  only channels-mode bodies; app-core contains no `cfg(feature =
  "tui"/"gui"/"channels")` (grep: zero hits).
- GUI-only build sanity: `main.rs:75-76` returns
  `run_desktop_entry().await`, so the packaged default binary behaves as the
  desktop app (Tauri CLI renames it per `desktop.rs` doc `:1-4`).

## Deviations

None.

## Conclusion

Supports: consistent feature gating of modules, binaries, entry routing, and
error paths; app-core stays surface-agnostic.

Does not prove: that a `gui`-only `cargo test` compiles/runs (Q-GUI-01), or
that every tauri command is correctly gated inside `src/tauri` (A-SRF-02).

## Follow-Up

`Q-GUI-01` owns the GUI compile/test matrix; `A-SRF-02` owns command-level
gating review.
