# Q-E2E-01 / V04 / Attempt 01

> Status: passed (static) — with the canonical cancel gap
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 1 (Chat: send -> reply -> terminal) on the IM channel surface
completes: inbound message -> `drive_chat` -> per-sentence outbound stream ->
terminal flush, with facts equivalent to the shared driver contract.

## Method

Static code-path determination (X-SRF-01 V02-01 S1; A-SRF-04 V02-01/V04-05).

1. `src/cli/channels.rs:102-265` `handle_stream`: per-message spawn `:237`,
   `PreparedUserTurn::build` `:208-229`, `ChatResources` with fresh token
   `:244`, `drive_chat` spawned `:262` into `ChannelChatSink` ->
   `aggregate_by_sentence` `:515-654` (UTF-8-safe sentence chunking, terminal
   flush semantics; covered by 27 channel-feature tests, A-SRF-04 V04-05).
2. Per-sender pool agent (`pool.acquire` `:135-139`).

## Expected Result

A typed terminal is flushed into the outbound stream and the surface exposes
the same control semantics (cancel) as TUI/GUI.

## Result

- Exit code: not-applicable (static determination); the aggregate/terminal
  behavior is pinned by the executed `channels`-feature test suite (exit 0,
  27 ok, A-SRF-04 V04-05)
- Summary: send -> reply -> terminal flush is fully wired; the aggregator
  flushes on FinalAnswer/Cancelled and errors on Error.

## Deviations

Canonical A-SRF-04-P1-01: the per-turn token (`channels.rs:244`) has zero
`.cancel()` producers — an in-flight `drive_chat` runs detached until process
exit; no steer path (X-SRF-01-P3-01). Concurrent turns from one sender on the
same pool agent are unresolvable statically (A-SRF-01/X-SRF-01 residual).

## Conclusion

The channel Chat scenario is judged **passed** for completion and terminal
flush, with cancel equivalence broken by canonical A-SRF-04-P1-01. Dynamic
confirmation (real IM traffic) is `not_run` (V48-01, no credentials).

## Follow-Up

Fix ownership: A-SRF-04-P1-01 (fire per-turn token on manager stop).
Re-validate with the channel fixture in A-SRF-04's regression validations.
