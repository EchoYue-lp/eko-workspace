# Q-E2E-01 / V25 / Attempt 01

> Status: passed (static) — with wildcard and dead-rules defects
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 5 (HITL: approval -> execute / reject) on the GUI surface completes:
an approval request reaches the user, an approve executes the tool, a reject
denies it.

## Method

Static code-path determination (A-HITL-01 V02-01/V03-02).

1. Request: `PermissionStage` (pipeline.rs:268-347) ->
   `check_tool_approval` (snapshot.rs:798-857) -> per-conversation
   `TauriHumanLoopHandler` (`chat.rs:261-435`, installed `:570-582`) ->
   `chat://event` ApprovalRequest -> `ApprovalCard.tsx`.
2. Response: `send_approval_response` -> `PENDING_RESPONSES`
   (`chat.rs:210-213`) -> provider resolves -> `PermissionService` returns
   Allow/Deny -> tool executes or is denied.

## Expected Result

Approve executes the tool; reject denies it; the chosen scope is enforced.

## Result

- Exit code: not-applicable (static determination)
- Summary: the round-trip is fully wired; approve/reject resolve the pending
  request and the framework decision follows.

## Deviations

Canonical: A-HITL-01-P1-03 — "approve all" (本会话同意) sends
`SessionAllTools` -> framework `"*"` wildcard rule, unlocking ALL tools for
the session on every surface (service.rs:898-908, permission.rs:253-255);
A-HITL-01-P1-01 — GUI permission-rule management stores rules that never
reach `PermissionService` (panels.rs:102-151, zero consumers of
`PermissionRuleConfig` helpers, state.rs:145-201); A-HITL-01-P2-03 — after 3
consecutive denials the 4th approval-worthy tool fails with an opaque error
instead of asking (service.rs:575-585 + snapshot.rs:841-846); A-HITL-01-P2-01
— the GUI bypasses the shared dispatcher (parallel transport, no shared
deadline).

## Conclusion

The GUI HITL scenario is judged **passed** for the basic approve/reject
round-trip, with the wildcard, dead-rules, and escalation defects changing
scope and escalation facts. Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: F-HITL-01-P1-03/A-HITL-01-P1-03, A-HITL-01-P1-01,
A-HITL-01-P2-03. Re-validate with the GUI approval round-trip scope fixture.
