# Q-E2E-01 / V26 / Attempt 01

> Status: passed (static) — cleanest interactive HITL surface
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 5 (HITL: approval -> execute / reject) on the TUI surface completes:
`TuiHumanLoopProvider` requests are answered by real keys with a bounded,
fail-closed timeout.

## Method

Static code-path determination (A-HITL-01 V02-01/V03-01; A-SRF-01
cross-checked rows).

1. Provider: `TuiHumanLoopProvider` swapped in at boot (`main.rs:249-257`);
   request flow + timeout (`hitl/tui_provider.rs:180-222`, 300 s -> `Timeout`
   fail-closed).
2. Keys: `y`/`n`/`a` handlers (`src/tui/events.rs:36-260`,
   `send_pending_response`); `a` -> `ApprovedWithScope { SessionAllTools }`
   (`events.rs:242-244`).

## Expected Result

Approve executes, reject denies, timeout rejects; scope is enforced as
chosen.

## Result

- Exit code: not-applicable (static determination)
- Summary: the round-trip is wired and fail-closed; the TUI is not affected
   by the REPL EOF defect (REPL provider unregistered in TUI mode).

## Deviations

Canonical: A-HITL-01-P1-03 — the TUI `a` key is one of the four producers of
the session-wide `"*"` wildcard (SessionAllTools scope).

## Conclusion

The TUI HITL scenario is judged **passed** at the code level — the most
faithful interactive surface — with the shared wildcard caveat. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-HITL-01-P1-03 (scope mapping). Re-validate with the
approval-key fixture.
