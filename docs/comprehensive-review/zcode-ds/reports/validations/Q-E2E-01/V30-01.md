# Q-E2E-01 / V30 / Attempt 01

> Status: failed (static; deterministic auto-reject — same as V29)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 5 (HITL: approval -> execute / reject) on the background surface
resolves deterministically with a comprehensible denial.

## Method

Static code-path determination (A-HITL-01 P2-03; A-SRF-04 V02-01).

1. Background runs execute on pool agents with an empty dispatcher — every
   approval auto-rejects (`agent_pool.rs:960-975`, `dispatcher.rs:82-87`).
2. Denial tracker escalation (`service.rs:575-585` + `snapshot.rs:841-846`)
   produces an opaque error after 3 auto-rejections.

## Expected Result

Approvals in background runs fail closed with an explicit reason.

## Result

- Exit code: not-applicable (static determination)
- Summary: deterministic auto-reject for all approvals; the 4th
   approval-worthy tool errors opaquely; the "execute" branch is unreachable
   for approval-worthy tools (Write/Execute/Network).

## Deviations

Canonical: A-HITL-01-P2-03. Also reachable: A-HITL-01-P1-02 context (b) — a
GUI window with no per-conversation provider routes browser confirmations to
the default dispatcher, whose REPL leaf auto-approves on EOF
(A-INT-01-P2-05): the background/browser confirmation chain can auto-approve
consequential actions when stdin is /dev/null.

## Conclusion

The background HITL scenario is judged **failed** for equivalence: approvals
are deterministic auto-rejects (with an escalation error), and the browser
confirmation path inherits the REPL EOF auto-approve. Dynamic confirmation is
`not_run` (V47-01).

## Follow-Up

Fix ownership: A-HITL-01-P1-02/P2-03; A-INT-01-P2-05. Re-validate with the
closed-stdin browser-confirmation fixture.
