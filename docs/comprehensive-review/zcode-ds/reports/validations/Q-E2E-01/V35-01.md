# Q-E2E-01 / V35 / Attempt 01

> Status: failed (static; browser panel dead + MCP config not persisted)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 7 (Browser/MCP: connect -> tools available) on the GUI surface
completes: the user connects a browser session or MCP server and the tools
become available durably.

## Method

Static code-path determination (A-SRF-02 V02-01/V03-02; A-INT-01 V02-01).

1. Browser: `BrowserRuntime` per process (`runtime.rs:93-100`); tools
   installed on primary agent (`infra.rs:454-455`); GUI panel wired to
   `browser://event` (useBrowserEvents.ts:12) — whose producer lives in the
   first `.setup()` of `build_tauri_app` (`mod.rs:40-68`) that Tauri
   overwrites (A-SRF-02-P1-01): the channel is dead, the panel cannot
   populate. `browser_navigate`-family commands exist but the workspace panel
   is non-functional.
2. MCP: `connect_mcp_server` un-gated (`mcp.rs:210-258`); `update_mcp_config`
   (`mcp.rs:476-559`) writes only the in-memory `plugins.mcp_config`
   (`state.rs:357`) — no file write (A-INT-01-P1-01): every GUI-created
   server vanishes on restart while the UI says "配置已保存并应用".

## Expected Result

Connect works and the configured capability survives; panel state reflects
backend state.

## Result

- Exit code: not-applicable (static determination)
- Summary: agent-level browser/MCP tools are reachable (chat-mediated), but
   the GUI connect/manage surface is broken in two ways: the browser panel
   cannot render (dead event bridge) and MCP edits are non-durable.

## Deviations

Canonical: A-SRF-02-P1-01 (double `.setup()` — browser://event dead),
A-INT-01-P1-01 (MCP config never persists), A-INT-01-P2-03 (dialog rejects
loopback/private HTTP endpoints the config file allows), A-INT-01-P2-04
(dropped servers never auto-recover), A-INT-01-P2-05 (browser confirmations
inherit REPL EOF auto-approve).

## Conclusion

The GUI Browser/MCP scenario is judged **failed** for the connect-and-manage
surface (browser panel dead; MCP config non-durable), though the underlying
tools are registered and callable. Live connect is `not_run` (V49-01, no
sidecar/server/credentials).

## Follow-Up

Fix ownership: A-SRF-02-P1-01 (single `.setup()`), A-INT-01-P1-01
(persist + seed), A-INT-01-P2-03. Re-validate with the browser-panel session
fixture and the MCP save-restart fixture.
