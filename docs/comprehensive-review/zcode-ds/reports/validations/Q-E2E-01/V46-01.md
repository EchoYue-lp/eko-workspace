# Q-E2E-01 / V46 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 9 (Large output: large output -> fold/cursor) on the IM channel
surface completes: large output is chunked into bounded, UTF-8-safe outbound
messages.

## Method

Static code-path determination (A-SRF-04 V04-05; source grep at the reviewed
commits).

1. `aggregate_by_sentence` (`src/cli/channels.rs:515-654`) chunks outbound
   text into sentences with a character cutoff (`buf.chars().take(cut)`,
   `:552`) — UTF-8-safe; event/tool previews capped at 500 chars
   (`:599, :630`); terminal flush semantics covered by the 27 channel-feature
   tests (`truncated_to_512_chars`, `:681`).

## Expected Result

Large output arrives as bounded messages that respect platform size caps,
without UTF-8 panics.

## Result

- Exit code: not-applicable (static determination); the chunking/UTF-8
   behavior is pinned by the executed channel-feature test suite (exit 0,
   A-SRF-04 V04-05)
- Summary: sentence chunking + preview caps are wired and tested.

## Deviations

None for this scenario.

## Conclusion

The channel Large-output scenario is judged **passed** at the code level.
Dynamic confirmation (real IM traffic) is `not_run` (V48-01).

## Follow-Up

None.
