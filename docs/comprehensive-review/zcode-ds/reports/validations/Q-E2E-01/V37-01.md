# Q-E2E-01 / V37 / Attempt 01

> Status: failed (static; MCP connect is a stub, browser management absent)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 7 (Browser/MCP: connect -> tools available) on the CLI REPL surface
completes: `/mcp connect|disconnect` connect a server and the tools become
available; a browser management path exists.

## Method

Static code-path determination (A-INT-01 V02-01/P2-01; X-SRF-01 V01-01).

1. MCP: `src/cli/cmd_impls/skills.rs:236-274` — `"connect"` prints
   "Connecting to MCP server: {name}" and returns; `"disconnect"` likewise —
   **no connection call** (canonical A-INT-01-P2-01). `list` works.
2. Browser: repository grep of `src/cli/` for browser management — zero hits
   (X-SRF-01-P2-01): no status/restart/navigate surface.

## Expected Result

Connect performs the operation; the capability is manageable from the CLI.

## Result

- Exit code: not-applicable (static determination)
- Summary: `/mcp connect|disconnect` are no-op stubs printing false-success
   messages; there is no browser management surface; the only usable path is
   chat-mediated tool calls on already-registered tools (boot-loaded MCP,
   browser tools on the agent).

## Deviations

Canonical: A-INT-01-P2-01 (CLI MCP stubs), X-SRF-01-P2-01 (REPL browser
management absent).

## Conclusion

The CLI Browser/MCP scenario is judged **failed** at the connect layer: the
scenario's first step (connect) cannot be performed on this surface. Live
connect is `not_run` (V49-01).

## Follow-Up

Fix ownership: A-INT-01-P2-01 (implement or remove the subcommands),
X-SRF-01-P2-01 (add `/browser` to the CLI). Re-validate with the
`/mcp connect` regression fixture.
