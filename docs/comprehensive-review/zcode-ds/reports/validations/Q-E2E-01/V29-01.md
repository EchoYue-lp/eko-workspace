# Q-E2E-01 / V29 / Attempt 01

> Status: failed (static; deterministic auto-reject — canonical A-HITL-01-P2-03)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 5 (HITL: approval -> execute / reject) on the cron surface resolves
deterministically: an approval request in a scheduled run is answered
fail-closed (reject) with a comprehensible reason.

## Method

Static code-path determination (A-HITL-01 V02-01/P2-03).

1. Unattended runs execute on pool agents with an empty dispatcher
   (`agent_pool.rs:960-975`, `dispatcher.rs:82-87`): every approval
   auto-rejects.
2. Escalation: `max_consecutive_denials` tracker (`service.rs:575-585`)
   upgrades the 4th approval-worthy tool to an opaque error via
   `snapshot.rs:841-846` (`"Tool '...' requires user approval"`).
3. Trigger: tick predicate never fires (F-OPS-01-P1-01).

## Expected Result

Approvals in unattended runs fail closed with an explicit denial reason; the
scenario's "execute" branch is unreachable (no human in the loop).

## Result

- Exit code: not-applicable (static determination)
- Summary: every approval deterministically rejects (no human), and the
  3rd auto-rejection degrades the 4th tool to an opaque error that hides the
  policy state. The scenario can only resolve to "reject".

## Deviations

Canonical: A-HITL-01-P2-03, F-OPS-01-P1-01 (trigger blocked).

## Conclusion

The cron HITL scenario is judged **failed** for equivalence: unattended
approvals are deterministic auto-rejects with an opaque escalation path, and
the trigger never fires. Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-HITL-01-P2-03 direction (explicit denial reason without
tracker escalation); F-OPS-01-P1-01.
