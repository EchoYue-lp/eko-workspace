# Q-E2E-01 / V36 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 7 (Browser/MCP: connect -> tools available) on the TUI surface
completes: `/browser status|managed|chrome` and `/mcp list|load|disconnect`
drive the real shared runtimes, and the tools are registered.

## Method

Static code-path determination (A-SRF-01 V02-01; A-INT-01 V02-01).

1. Browser: `/browser status|managed|chrome` (`src/tui/events.rs:4551-4615`)
   on the shared `BrowserRuntime`; browser tools on primary + subagents
   (`infra.rs:454-455, 951-952, 1028-1029`).
2. MCP: `/mcp list|load|disconnect` (`events.rs:3434-3450`) call
   `load_mcp_from_file`/`disconnect_mcp` (functional, unlike the CLI stubs);
   boot loader (`infra.rs:1069-1108`) connects configured servers.

## Expected Result

Connect/manage works and tools become available.

## Result

- Exit code: not-applicable (static determination)
- Summary: management commands are functional; tools register on the shared
   agent; sidecar restart (`/browser managed|chrome`) is the documented
   recovery path (browser-runtime-design, A-INT-01 V05-01 current).

## Deviations

Canonical: A-INT-01-P2-02 (boot MCP load all-or-nothing and unbounded — a
malformed entry disables all servers; a hung server stalls boot ~120 s),
A-INT-01-P2-04 (dropped servers never auto-recover), F-INT-01-P1-01 (202
async HTTP servers hang every call 60 s).

## Conclusion

The TUI Browser/MCP scenario is judged **passed** at the code level — the
most complete management surface. Live connect is `not_run` (V49-01).

## Follow-Up

Fix ownership: A-INT-01-P2-02/P2-04; F-INT-01-P1-01. Re-validate with the
invalid-entry boot fixture.
