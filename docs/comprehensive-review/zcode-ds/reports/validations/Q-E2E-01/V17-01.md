# Q-E2E-01 / V17 / Attempt 01

> Status: failed (static; trigger blocked — same as V05/V11)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 3 (Subagent: dispatch -> result -> review) on the cron surface
completes: a scheduled run's plan tasks dispatch subagents and their results
are recorded durably.

## Method

Static code-path determination (A-SRF-04 V02-01; F-OPS-01-P1-01 re-anchored).

1. `launch_cron_run` (`executor.rs:3895`) -> `drive_agent_run` (`:3649`):
   plan tasks execute as subagents through the shared executor; results
   persist via `record_subagent_released` (executor.rs:2337-2343) and replay
   monotonically (X-EVT-01 V03-01).
2. Trigger: tick predicate `runner.rs:80-93` — never satisfiable.

## Expected Result

A scheduled run dispatches subagents automatically and their results are
durable and reviewable.

## Result

- Exit code: not-applicable (static determination)
- Summary: the dispatch and durable-result paths are complete; the automatic
  trigger is dead (canonical F-OPS-01-P1-01), so the scenario cannot start
  without a manual `/cron run`. There is no subagent review surface on any
  non-GUI surface for unattended runs (A-SRF-01-P2-01 family).

## Deviations

Canonical: F-OPS-01-P1-01, X-EVT-01-P1-01 (cancel/timeout mislabeled
`failed`), A-HITL-01-P2-03 (auto-reject approvals).

## Conclusion

The cron Subagent scenario is judged **failed** at the trigger layer.
Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: F-OPS-01-P1-01. Re-validate with a scheduled-fire fixture.
