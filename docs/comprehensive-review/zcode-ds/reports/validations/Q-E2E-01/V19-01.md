# Q-E2E-01 / V19 / Attempt 01

> Status: passed (static) — with duplicate-projection and cosmetic-toggle defects
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 4 (Tool: call -> result rendering) on the GUI surface completes: the
agent calls a tool through the framework executor and the result renders with
the same facts as other surfaces.

## Method

Static code-path determination (A-TOOL-01 V02-01/V03-01; X-SRF-01 V02-01 S1).

1. Execution: one framework `ToolManager` + 16-stage
   `ToolExecutionPipeline` shared across pool agents (`agent_pool.rs:882-899`);
   invocation-scoped visibility (`chat_driver.rs:465-475`).
2. Rendering: `TauriChatSink::handle_tool_event` (`chat.rs:1218-1308`) ->
   `ToolExecutionRepository` (durable journal, paged UTF-8-safe reads) +
   `execution://event` -> `toolExecutionStore` -> tool cards.

## Expected Result

Every tool call renders exactly one result row with a truthful terminal.

## Result

- Exit code: not-applicable (static determination)
- Summary: call -> persist -> render is fully wired; the journal survives
  crashes (Running -> Cancelled recovery at boot, tool_execution.rs:527-545).

## Deviations

Canonical: A-SRF-02-P2-01 + A-SRF-03-P2-01 (two live producers persist the
same subagent tool events — duplicate cards and inflated counts; live ingest
keys by per-producer `detail_ref` not (owner, call_id),
toolExecutionStore.ts:206-217); A-TOOL-01-P2-01 (GUI enable/disable toggle is
cosmetic — a "disabled" tool remains callable); A-CHAT-01-P2-02 (sink guesses
cancellation from any terminal, chat.rs:1325-1328); A-TOOL-01-P3-02 (bridge
lag drops leave a card Running until restart).

## Conclusion

The GUI Tool scenario is judged **passed** at the code level for call and
result rendering, with the filed duplicate-projection and toggle defects.
Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-SRF-02-P2-01 (single producer), A-TOOL-01-P2-01 (wire the
toggle or delete it). Re-validate with the one-card-per-call fixture.
