# Q-E2E-01 / V20 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 4 (Tool: call -> result rendering) on the TUI surface completes: tool
calls stream into the reducer and results render with UTF-8-safe bounds.

## Method

Static code-path determination (A-SRF-01 V02-01; A-SRF-03 cross-check).

1. Reducer arms: `run_event_loop` match on ToolCall/ToolProgress/
   ToolComplete/ToolResult (`events.rs:602-908`); tool status transitions
   tracked (`truncated` flag per tool, `events.rs:691-748`).
2. Bounds: `append_bounded` caps per-tool output at 131,072 chars with a
   truncation marker (`events.rs:350-377, 717`).

## Expected Result

Tool calls and results render completely and truthfully.

## Result

- Exit code: not-applicable (static determination)
- Summary: streaming render and UTF-8-safe bounds are wired; the TUI has no
  durable tool-detail projection (GUI-only, A-TOOL-01 coverage note — not a
  capability gap per A-TOOL-01's parity verdict).

## Deviations

Canonical: A-SRF-01-P3-03 (no reducer terminal fixtures — tool-status
transitions untested), X-EVT-01-P1-01 (subagent tool outcomes mislabeled on
cancel/timeout).

## Conclusion

The TUI Tool scenario is judged **passed** at the code level. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-SRF-01-P3-03 (extract reducer + fixtures). Re-validate with
the ToolCall->ToolResult sequence fixture.
