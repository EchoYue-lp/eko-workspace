# Q-E2E-01 / V31 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 6 (Attachment: file -> model projection) on the GUI surface
completes: a native dialog/drag-drop attachment is staged and projected into
the model prompt via `PreparedUserTurn`.

## Method

Static code-path determination (X-SRF-01 V02-01 S5; A-SRF-01 V01-01).

1. Staging: native dialogs/drag-drop -> `src/tauri/commands/files.rs`;
   workspace-relative uploads dir when a workspace is switched
   (A-SRF-01-P1-01 evidence).
2. Projection: `send_chat_message` -> `PreparedUserTurn::build`
   (`src/tauri/commands/chat.rs:536-589`) — attachment refs
   (`attachments::AttachmentRef`) become part of the model prompt assembly
   (prepared_turn.rs module doc: every entry point builds the same turn).

## Expected Result

The attachment reaches the model projection on the shared path.

## Result

- Exit code: not-applicable (static determination)
- Summary: staging -> `PreparedUserTurn::build` is fully wired; the model
   projection is the shared `PreparedUserTurn` used by all four interactive
   surfaces (MASTER-PLAN:333 current, X-SRF-01 V01-01).

## Deviations

Canonical: A-SRF-01-P1-01 (spill/uploads dir divergence across the
workspace gap — GUI is workspace-relative, TUI/REPL/channel are not).

## Conclusion

The GUI Attachment scenario is judged **passed** at the code level. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

None beyond the canonical workspace-gap fix (A-SRF-01-P1-01/A-CFG-01-P1-03).
