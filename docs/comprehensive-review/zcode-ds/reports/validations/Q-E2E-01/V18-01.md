# Q-E2E-01 / V18 / Attempt 01

> Status: passed (static) — durable subagent results, no review surface
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 3 (Subagent: dispatch -> result -> review) on the background surface
completes: `submit` -> `execute_run` -> plan-task subagents -> durable
results.

## Method

Static code-path determination (A-SRF-04 V02-01).

1. `submit` (`service.rs:219`) -> `start_run_driver` (`:359-407`) ->
   `execute_run`; plan tasks dispatch subagents via the shared executor;
   `record_subagent_released` persists terminals; replay monotone
   (X-EVT-01 V03-01).

## Expected Result

Subagent results from background runs are durable and truthful.

## Result

- Exit code: not-applicable (static determination)
- Summary: the dispatch and durable-result paths are complete; results replay
  after restart (store replay, taskRuntimeStore).

## Deviations

Canonical: X-EVT-01-P1-01 (cancel/timeout mislabeled `failed`),
A-TOOL-01-P1-01 (writer subagents read-only — results are no-op),
A-HITL-01-P2-03 (auto-reject approvals feed the denial tracker).

## Conclusion

The background Subagent scenario is judged **passed** for dispatch and
durable results; review is via the GUI task panels or the store, not from the
submitting surface. Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

None beyond the canonical fixes.
