# Q-E2E-01 / V02 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 1 (Chat: send -> reply -> terminal) on the TUI surface completes with
a truthful terminal (FinalAnswer / Error / Cancelled) and equivalent facts to
the shared driver contract.

## Method

Static code-path determination; anchors re-verified at the reviewed commits
(X-SRF-01 V02-01 S1/S2 replay; A-SRF-01 V02-01).

1. Enter -> `handle_enter` -> `QueuedTurn` -> `dispatch_turn`
   (`src/tui/events.rs:1341-1436`): `PreparedUserTurn::build` `:1347`,
   `ChatResources` incl. cancel token, `send_to_agent` spawns `drive_chat`
   `:2212-2230` (call site `:2226`) into `TuiChatSink` `:2021-2210`.
2. Reducer terminal arms: `run_event_loop` match on AgentEvent `:602-908` —
   FinalAnswer `:657-660`, Cancelled `:661-675`, Error `:805-843` (cancel
   masked via `active_cancel.is_cancelled()`).
3. Cancel: Esc -> `handle_esc` fires `active_cancel.cancel()` `:1937-1958`.

## Expected Result

One truthful terminal per turn; a user cancel renders "Cancelled by user."
without a fabricated error (A-CHAT-01-P1-01 does not apply to the TUI).

## Result

- Exit code: not-applicable (static determination)
- Summary: all three terminal arms are reachable and truthful; the TUI never
  labels turns from `drive_chat`'s Result (A-SRF-01 cross-check table row 1);
  cancel is a real producer (Esc). The envelope yields exactly one terminal
  per turn and the reducer clears `is_processing`/`active_cancel`/
  `active_turn_id` on each.

## Deviations

Queued-turn drain asymmetry is latent: `dispatch_next_queued` is called on
FinalAnswer/Error but not on Cancelled (`events.rs:661-675`), masked today by
the framework never emitting `Cancelled` (A-SRF-01-P2-03, F-RCT-03-P1-02).
The `TurnStatus`/`Interrupt` reducer arms are dead (A-SRF-01-P3-02).

## Conclusion

The TUI Chat scenario is judged **passed** at the code level with the two
filed caveats (latent queue drain, dead arms). Dynamic confirmation is
`not_run` (V47-01).

## Follow-Up

None beyond the canonical fixes (A-SRF-01-P2-03 after F-RCT-03-P1-02).
