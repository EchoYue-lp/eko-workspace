# Q-E2E-01 / V12 / Attempt 01

> Status: failed (static; writer defect applies to the run's plan tasks)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 2 (Task: create -> execute -> complete) on the background surface
completes: `submit` -> `execute_run` -> durable terminal with the intended
side effects.

## Method

Static code-path determination (A-SRF-04 V02-01).

1. `submit`/`submit_run` (`service.rs:219/253`) -> `start_run_driver`
   `:359-407` -> `execute_run` (plan exists, shared DAG executor);
   `resume_pending` (`:556-558`) resumes interrupted runs at next boot
   (conversation_id `"background:"` prefix).
2. Writer plan tasks dispatch `build_writer_subagent_agent` (`infra.rs:963`).

## Expected Result

The background run reaches Completed and its writer plan tasks modify the
worktree.

## Result

- Exit code: not-applicable (static determination)
- Summary: the trigger, execution, durable terminal, and restart-resume paths
  are fully wired; the writer side effect is broken by canonical
  A-TOOL-01-P1-01 (the same plan-mode collision), so Implementation/
  Debugging plan tasks complete without writing.

## Deviations

Canonical: A-TOOL-01-P1-01, A-HITL-01-P2-03 (approval auto-reject + escalation
to opaque error after 3 auto-rejections — write/execute tools deny in
unattended runs), A-SRF-04-P2-02 (service cancel token never fired).

## Conclusion

The background Task scenario is judged **failed** at the completion-facts
level (writer defect + auto-reject policy). Dynamic confirmation is `not_run`
(V47-01).

## Follow-Up

Fix ownership: A-TOOL-01-P1-01; A-HITL-01-P2-03 direction (explicit denial
reason without tracker escalation). Re-validate with the mock-LLM writer-task
fixture.
