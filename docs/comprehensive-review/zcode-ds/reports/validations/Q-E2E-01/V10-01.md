# Q-E2E-01 / V10 / Attempt 01

> Status: failed (static; no run-control surface, channels-only services absent)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 2 (Task: create -> execute -> complete) on the IM channel surface
completes: a channel agent creates a task run and the run reaches a durable
terminal controllable from the channel.

## Method

Static code-path determination (X-SRF-01 V02-01 S4; X-SRF-01-P2-02 evidence;
A-SRF-04 V02-01).

1. Create: channel agent tool set includes `create_complex_task` /
   `task_create/update/list/execute` (Auto-mode exposure) — a channel message
   can start a background run.
2. Control: `src/cli/channels.rs` has no slash surface and no NL routing for
   run control (trace/analysis/papers/skills only, `:153-188`); no
   `task_cancel`/`task_pause` agent tool exists anywhere.
3. Services: channels-only boot (`main.rs:365`, `modes.rs:118-235`) never
   calls `start_task_service`/`start_scheduler_with_store` (A-SRF-04-P1-02).

## Expected Result

A run started from the channel can be inspected/stopped from the channel, and
the run survives a restart on this surface.

## Result

- Exit code: not-applicable (static determination)
- Summary: creation is possible; execution runs in the shared executor; the
  run is **uncontrollable from the channel** (no list/pause/cancel/resume
  path — X-SRF-01-P2-02); in channels-only mode no background service exists,
  so nothing resumes after restart and no cron exists (A-SRF-04-P1-02).

## Deviations

Canonical: X-SRF-01-P2-02 (channel task-run management gap), A-SRF-04-P1-02
(channels-only services absent), A-SRF-04-P2-01 (interrupted runs unresumable
from CLI/channel), A-TOOL-01-P1-01 (writer defect applies to the run's
subagents).

## Conclusion

The channel Task scenario is judged **failed**: creation/execution exist but
the completion is uncontrollable and non-recoverable from the owning surface.
Dynamic confirmation is `not_run` (V48-01).

## Follow-Up

Fix ownership: X-SRF-01-P2-02 (NL run-control routing or a run-control agent
tool), A-SRF-04-P1-02 (channels services). Re-validate with the channel
"cancel my task" fixture in X-SRF-01-P2-02's regression validation.
