# Q-E2E-01 / V14 / Attempt 01

> Status: passed (static) — with the detail-browsing gap
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 3 (Subagent: dispatch -> result -> review) on the TUI surface
completes: subagent events render live and results are reviewable.

## Method

Static code-path determination (A-SRF-01 V02-01/V03-01).

1. Subscription: `run_event_loop` subscribes to the subagent event bus
   (`events.rs:562-564`); `update_subagent_runs` (`events.rs:5343-5436`)
   updates the live projection on every `SubagentEvent` (`:571`).
2. Review: `TaskRuntimeView`/`SubagentRuntimeView` projections
   (`mod.rs`), last-10 subagent summaries (`/tasks` text,
   `events.rs:4225`).

## Expected Result

The user sees subagent progress and can inspect results/artifacts.

## Result

- Exit code: not-applicable (static determination)
- Summary: live projection and summary rendering are wired; per-subagent
  detail browsing (events/artifacts/reviews) is absent (A-SRF-01-P2-01 —
  GUI-only via `list_task_todos/events/artifacts/reviews`).

## Deviations

Canonical: A-SRF-01-P2-01 (no task/subagent detail browsing on TUI),
X-EVT-01-P1-01 (cancel/timeout class loss also mislabels TUI-rendered
subagent outcomes), A-TOOL-01-P1-01 (writer subagents read-only).

## Conclusion

The TUI Subagent scenario is judged **passed** for dispatch and result
rendering, with the review depth limited by A-SRF-01-P2-01. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-SRF-01-P2-01; X-EVT-01-P1-01.
