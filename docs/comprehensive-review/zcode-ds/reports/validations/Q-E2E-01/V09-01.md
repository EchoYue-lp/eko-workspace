# Q-E2E-01 / V09 / Attempt 01

> Status: failed (static; writer defect + resume filter)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 2 (Task: create -> execute -> complete) on the CLI REPL surface
completes: `/tasks` create/control -> shared TaskRuntime store -> terminal
facts with equivalent outcomes.

## Method

Static code-path determination (A-SRF-04 V02-01; X-SRF-01 V02-01 S4).

1. `/tasks` full surface `src/cli/cmd_impls/coding.rs:50-289`; TaskRuntime
   store built in `main.rs:175`; agent tool `create_complex_task` also
   available in-chat.
2. Execution: same `execute_run` path; writer tasks -> `build_writer_subagent_agent`
   (`infra.rs:963`).
3. Resume path: `service.rs:469-489` `resume` rejects any run whose
   conversation_id does not start with `"background:"` (A-SRF-04-P2-01).

## Expected Result

The run completes with the intended side effects and, after a restart, is
resumable from the surface that owns it.

## Result

- Exit code: not-applicable (static determination)
- Summary: create/execute/project (execution events printed, repl.rs:566-574)
  are wired; the writer side effect is broken (A-TOOL-01-P1-01); the CLI
  resume path rejects non-background runs, so an interrupted task/cron run
  paused at boot is unresumable from the CLI (A-SRF-04-P2-01).

## Deviations

Canonical: A-TOOL-01-P1-01, A-SRF-04-P2-01, A-SRF-04-P1-01 (REPL turns not
cancellable — affects the in-chat task tools' turns).

## Conclusion

The CLI Task scenario is judged **failed** at the completion-facts level
(writer defect) and at the restart-resume level (P2-01). Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-TOOL-01-P1-01; A-SRF-04-P2-01 (generalize the ownership
filter beyond `"background:"`). Re-validate with the `/tasks resume` cron-run
acceptance fixture.
