# Q-E2E-01 / V41 / Attempt 01

> Status: failed (static; no conversation persistence on the CLI surface)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 8 (Restart: state/identity after restart) on the CLI REPL surface
completes: the chat conversation identity and history survive a restart.

## Method

Static code-path determination (X-SRF-01 V01-01; A-SRF-04 V02-01; grep at the
reviewed commits).

1. Conversation persistence: grep of `conversation_store`/
   `ConversationStore`/`save_conversation` across `src/cli/repl.rs`,
   `src/cli/channels.rs`, `src/cli/modes.rs` — **zero hits**; the REPL's
   conversation_id is a per-boot uuid (A-SRF-04 V03-e), so neither identity
   nor history survives.
2. Facts that do persist: TaskRuntime store (built in `main.rs:175`) with
   `recover_incomplete` (store.rs:1631-1776); memory `/remember`/`/forget`
   via layer manager (A-SRF-04 V05-01 current).

## Expected Result

The REPL restores the conversation the user was in, like TUI/GUI do.

## Result

- Exit code: not-applicable (static determination)
- Summary: no conversation store is wired into the CLI surface; after a
   restart the REPL starts a fresh conversation. Task runs and memory facts
   survive; chat history and turn identity do not. The CLI also has no
   single-shot mode, so nothing replays non-interactively either
   (A-SRF-04-P3-02).

## Deviations

This is a new observation for this task (REPL restart continuity); it
complements X-EVT-01-P2-02 (GUI schema-level terminal gap) and A-SRF-04-P3-02
(interactive-only). Canonical anchor for the identity side: A-SRF-04 V03-e
(per-boot uuid identity).

## Conclusion

The CLI Restart scenario is judged **failed**: chat identity/history is
ephemeral on the REPL surface while TUI and GUI restore it. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix direction in Q-E2E-01-P2-01: wire the shared conversation store into the
REPL entry (modes.rs/repl.rs), mirroring TUI `mod.rs:1967` and the GUI
`conversations.rs` path; re-validate with a REPL restart fixture asserting
conversation restore.
