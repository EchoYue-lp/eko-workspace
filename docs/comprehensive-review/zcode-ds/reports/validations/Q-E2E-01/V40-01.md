# Q-E2E-01 / V40 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 8 (Restart: state/identity after restart) on the TUI surface
completes: stored conversations resume and interrupted runs recover with
their identities.

## Method

Static code-path determination (A-SRF-01 V02-01; A-SRF-04 V02-01).

1. Conversations: `run_tui` loads stored conversation messages
   (`src/tui/mod.rs:1967-1991`); `/sessions|/resume|/fork` and
   `--resume`/`--continue` (`main.rs:96-147`).
2. Runs: boot recovery `recover_incomplete` transitions Running -> Paused
   (`store.rs:1631-1776`); TUI `/task-resume`/`/task-recovery` re-drive
   `execute_run` (`events.rs:4696-4755`); memory `/remember` persists via
   layer manager (A-SRF-04 V05-01).

## Expected Result

Conversation identity and interrupted runs survive a restart on the TUI.

## Result

- Exit code: not-applicable (static determination)
- Summary: conversation load/resume and run recovery are wired; the TUI
   resumes runs through the store directly (no prefix filter — the
   A-SRF-04-P2-01 filter does not apply here).

## Deviations

Canonical: A-SRF-04-P2-01 (only CLI/channel resume paths filter by
conversation-id prefix; TUI/GUI resume through the store), X-EVT-01-P2-02
(turn terminal not persisted — the same reload limitation applies to the
TUI's stored messages).

## Conclusion

The TUI Restart scenario is judged **passed** at the code level. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

None beyond the canonical fixes.
