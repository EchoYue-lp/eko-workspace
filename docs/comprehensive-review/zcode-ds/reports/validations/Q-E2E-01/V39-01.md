# Q-E2E-01 / V39 / Attempt 01

> Status: passed (static) — with terminal-fact and MCP-config gaps
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 8 (Restart: state/identity after restart) on the GUI surface
completes: conversations, task runs, subagent facts, and tool executions
survive a restart with their identities.

## Method

Static code-path determination (X-EVT-01 V02-01/V03-01; A-SRF-03 V03-01).

1. Conversations: `save_conversation`/`get_conversation`
   (`conversations.rs:392-470`) persist `SavedMessage[]`; reload rebuilds
   messages with deterministic ids (`conversationStore.ts:296-388`) and
   hydrates tool rows identity-deduped.
2. TaskRuntime: runs persist `TaskRunStatus` and replay from `events.jsonl`
   (`taskRuntimeStore.ts:219-283`); subagent cards re-projected
   (`subagentRunStore.ts:330-405`); tool journal recovers Running -> Cancelled
   at boot (`tool_execution.rs:527-545`).
3. MCP: `~/.echo-agent/mcp.json` boot loader (`infra.rs:1069-1108`) — but
   GUI-created edits are in-memory only (A-INT-01-P1-01).

## Expected Result

All durable facts and identities survive; the restored conversation shows the
same outcome the user saw.

## Result

- Exit code: not-applicable (static determination)
- Summary: task/tool/subagent facts replay monotonically; conversation
   content restores. Two fact classes do not survive: the chat-turn terminal
   (messages persist without a status field; reload resets `runStatus:
   'idle'` — X-EVT-01-P2-02) and GUI-created MCP config (A-INT-01-P1-01).

## Deviations

Canonical: X-EVT-01-P2-02 (chat-turn terminal absent from persistence —
failed/cancelled turns indistinguishable after reload), A-SRF-03-P1-02 (the
persisted body is the wiped `[Error] ...` content on error/cancel),
A-INT-01-P1-01 (GUI MCP config lost).

## Conclusion

The GUI Restart scenario is judged **passed** for the durable core
(task/subagent/tool/identity) with two filed fact-loss gaps (turn terminal,
MCP config). Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: X-EVT-01-P2-02 (persist per-turn terminal), A-INT-01-P1-01.
Re-validate with the reload-a-failed-conversation fixture.
