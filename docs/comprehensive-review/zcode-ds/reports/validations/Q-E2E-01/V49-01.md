# Q-E2E-01 / V49 / Attempt 01

> Status: not_run (environmental prerequisites absent)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

A real browser session (Playwright sidecar) and a real user-configured MCP
server could be connected in this environment to dynamically confirm the
Browser/MCP scenario verdicts (V35-V38).

## Method

Environmental prerequisite check performed on 2026-08-12 (the attempt):

1. Browser sidecar: `which playwright` — not found; `~/.cache/ms-playwright`
   — absent; the sidecar is launched as `npx -y @playwright/mcp@latest`
   (app-core browser/sidecar.rs) and requires network package fetch plus a
   real browser download, unavailable/absent here.
2. MCP servers: `~/.echo-agent/mcp.json` — absent (only cache/curator/runs
   dirs); no local MCP server process is configured or running.
3. Boot loader would find no config (`infra.rs:1069-1108`), so no
   `mcp__`/browser tool could be exercised against a live server.

## Expected Result

A real browser sidecar session and a real MCP server could be started to
verify connect -> tools-available end to end.

## Result

- Exit code: not-applicable (no command executed; prerequisites absent)
- Summary: no sidecar, no browser cache, no MCP config, no server — the
   live connect scenarios are `not_run`. Static verdicts (V35-V38) stand,
   including the known management-surface defects (A-SRF-02-P1-01,
   A-INT-01-P1-01, A-INT-01-P2-01, X-SRF-01-P2-01).

## Deviations

None.

## Conclusion

Records the attempted environmental check for Browser/MCP live sessions.
The connect wiring itself is statically verified (registration + handshake
path) in V35-V38.

## Follow-Up

When a network + browser download and a local MCP server are available, run
the browser-panel session fixture (A-SRF-02-P1-01 regression) and the
MCP save-restart fixture (A-INT-01-P1-01 regression) dynamically.
