# Q-E2E-01 / V32 / Attempt 01

> Status: passed (static) — with the global spill-dir divergence
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 6 (Attachment: file -> model projection) on the TUI surface
completes: `/attach` + Ctrl+V staging flows into `PreparedUserTurn::build`.

## Method

Static code-path determination (X-SRF-01 V02-01 S5; A-SRF-01 V02-01).

1. Staging: `/attach` + Ctrl+V (`src/tui/events.rs:1510-1597, 3261-3300`);
   long-paste spill via `resolve_user_input_spill_dir(None)`
   (`events.rs:1370`) — global `~/.eko` dir.
2. Projection: `dispatch_turn` -> `PreparedUserTurn::build`
   (`events.rs:1341-1436`, build at `:1347`).

## Expected Result

The attachment reaches the model projection.

## Result

- Exit code: not-applicable (static determination)
- Summary: staging -> build is fully wired; the same shared projection as the
   GUI.

## Deviations

Canonical: A-SRF-01-P1-01 — the TUI has no workspace concept, so attachments
and pastes land in the global user-input dir instead of a workspace uploads
dir (the same divergence the workspace gap causes).

## Conclusion

The TUI Attachment scenario is judged **passed** at the code level with the
storage-location divergence. Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-SRF-01-P1-01 (workspace surface + spill dir routing).
