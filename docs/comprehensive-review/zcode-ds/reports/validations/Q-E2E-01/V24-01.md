# Q-E2E-01 / V24 / Attempt 01

> Status: passed (static) — with the auto-reject policy caveat
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 4 (Tool: call -> result rendering) on the background surface
completes: a background run calls tools and records results durably.

## Method

Static code-path determination (A-SRF-04 V02-01; A-TOOL-01 V02-01).

1. `submit` (`service.rs:219`) -> `start_run_driver` (`:359-407`) ->
   `drive_unattended_run`/`execute_run`; Auto-mode exposure
   (`executor.rs:3686-3713`); results in the durable run store + webhook
   (runner.rs:111-122).

## Expected Result

Tool results from background runs are durable and truthful.

## Result

- Exit code: not-applicable (static determination)
- Summary: exposure/execution/persistence are wired.

## Deviations

Canonical: A-HITL-01-P2-03 — approvals auto-reject via the empty dispatcher
(agent_pool.rs:960-975, dispatcher.rs:82-87), so Write/Execute/Network tools
resolve to denials in unattended runs; the 3rd auto-rejection upgrades the
4th tool to an opaque "requires user approval" error (service.rs:575-585 +
snapshot.rs:841-846). This changes the *facts* of a tool scenario in
background mode vs interactive surfaces.

## Conclusion

The background Tool scenario is judged **passed** for wiring with a
deterministic policy divergence for approval-worthy tools (auto-deny).
Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-HITL-01-P2-03 direction (explicit denial reason, no tracker
escalation). Re-validate with the background write-tool fixture.
