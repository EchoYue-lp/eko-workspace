# Q-E2E-01 / V07 / Attempt 01

> Status: failed (static; writer-task capability broken)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 2 (Task: create -> execute -> complete) on the GUI surface completes:
a task is created in the TaskRuntime store, executed by the shared executor,
and reaches a durable terminal with the intended side effects.

## Method

Static code-path determination (X-SRF-01 V02-01 S4; A-TOOL-01 V02-01;
A-SRF-02 V02-01).

1. Create: `task_execute` + GUI commands (`src/tauri/commands/task_runtime.rs`)
   -> shared `TaskRuntimeStore`; task tools registered on the primary agent
   (`desktop.rs:200-223`), `task_execute` bound to the pool (`:217`).
2. Execute: `execute_run` -> `RuntimeDagExecutor`; `Implementation|Debugging`
   -> `run_writer_subagent` (`executor.rs:2114-2213, 2889`) ->
   `delegate_to_agent_with_prompt_payload` -> fork factory
   (`infra.rs:791-809`) -> `build_writer_subagent_agent` with
   `set_plan_mode(true)` (`infra.rs:963`).
3. Complete: `TaskRunStatus` persists and replays monotonic (X-EVT-01
   V03-01); projection `TauriExecutionProjector` (`chat.rs:957-1114`) +
   `taskRuntimeStore` poll.

## Expected Result

The run reaches Completed and the writer task actually modified its worktree
(the task's contract per `executor.rs:282`).

## Result

- Exit code: not-applicable (static determination)
- Summary: create/execute/complete and durable terminal are fully wired; the
  intended side effect is **not**: `set_plan_mode(true)` at `infra.rs:963`
  collides with the framework plan-mode tool filter (`snapshot.rs:227-236`,
  `tools_for_llm` `:282-285`, `PlanModeStage` `pipeline.rs:1004-1018`), so
  write tools are invisible and blocked — the run "completes" with a pristine
  worktree (canonical A-TOOL-01-P1-01, EKO-side confirmation of
  F-EXT-01-P1-01).

## Deviations

Canonical: A-TOOL-01-P1-01 (writer read-only), A-TSK-03-P1-01 (pause during a
wave permanently cancels, reachable via GUI resume/pause), A-SRF-02-P2-01 +
A-SRF-03-P2-01 (duplicate tool cards for subagent tools), A-HITL-01-P2-03
(approval auto-reject in runs).

## Conclusion

The GUI Task scenario is judged **failed** at the completion-facts level: the
run terminal exists but the write contract is silently broken on every
writer task. Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-TOOL-01-P1-01 (remove `set_plan_mode(true)` from
`build_writer_subagent_agent`); A-TSK-03-P1-01. Re-validate with the
mock-LLM writer-task fixture in A-TOOL-01's regression validations.
