# Q-E2E-01 / V23 / Attempt 01

> Status: failed (static; trigger blocked)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 4 (Tool: call -> result rendering) on the cron surface completes: a
scheduled run calls tools under Auto-mode exposure and records results.

## Method

Static code-path determination (A-SRF-04 V02-01; A-TOOL-01 V02-01).

1. Exposure: unattended runs carry Auto-mode exposure
   (`executor.rs:3686-3713`); same framework executor.
2. Trigger: tick predicate `runner.rs:80-93` — never satisfiable (canonical
   F-OPS-01-P1-01).

## Expected Result

A scheduled run's tool calls execute and their results persist.

## Result

- Exit code: not-applicable (static determination)
- Summary: the exposure and execution paths are complete; the automatic
   trigger is dead. Tool executions in unattended runs that need approval
   auto-reject (A-HITL-01-P2-03), and the 3rd auto-rejection escalates to an
   opaque tool error.

## Deviations

Canonical: F-OPS-01-P1-01, A-HITL-01-P2-03.

## Conclusion

The cron Tool scenario is judged **failed** at the trigger layer. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: F-OPS-01-P1-01; A-HITL-01-P2-03 direction.
