# Q-E2E-01 / V01 / Attempt 01

> Status: failed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories (git status --porcelain empty)

## Claim

Scenario 1 (Chat: send -> reply -> terminal) on the GUI surface completes with
the same facts every other surface produces: a truthful terminal (completed /
failed / cancelled / timeout) and preserved partial content.

## Method

Static code-path determination at the reviewed commits; definition ->
registration -> reachability per X-SRF-01 V01-01/V02-01, X-EVT-01 V01-01.

1. `echo-agent-cli/src/tauri/commands/chat.rs:443-733` `send_chat_message`:
   interrupt branch `:511-535` returns `{kind:"interrupt_prompt"}` before any
   turn registration; normal path registers turn/cancel-token/HITL `:536-589`,
   spawns `drive_chat` `:688`, emits `TurnStatus{completed|failed|cancelled}` +
   `Done` `:690-711`.
2. Sink -> wire: `TauriChatSink` (`chat.rs:1148-1417`) -> `chat://event`
   (message_key injected `:137-149`).
3. Frontend: `useTauriChat.ts:84-167` listener -> `chatEventHandler.ts`
   terminals `:140-158` -> `chatStore.ts:354-362` `finalizeAssistantMessage`.
4. Envelope contract: every raw stream Err becomes an Error payload
   (echo-core event_envelope.rs:134-140); timeout class is collapsed
   (X-EVT-01-P1-02).

## Expected Result

The turn ends with one truthful terminal and the streamed partial content
intact; error != cancel != timeout on the wire and in the store.

## Result

- Exit code: not-applicable (static determination; no process launched)
- Summary: the success path (token -> FinalAnswer -> 'completed' + Done) is
  fully wired. The error/cancel path ends `runStatus 'completed'` with the
  body replaced by `[Error] ...` (`chatEventHandler.ts:140-150` ->
  `chatStore.ts:354-362`); the timeout class has no typed terminal at any
  layer (`ChatEvent` has no timeout variant; reducer ends `'completed'`);
  the interrupt branch (`chat.rs:511-535`) never emits any terminal for the
  ghost message_key, so the input queues forever
  (`useTauriChat.ts:253-259` drain only on `done`).

## Deviations

Three canonical P1s make the "equivalent facts" clause fail on the flagship
surface: A-SRF-03-P1-01 (interrupt strands turn state), A-SRF-03-P1-02
(error/cancel mislabeled 'completed' + partial content wiped and persisted
wiped), X-EVT-01-P1-02 (timeout has no typed terminal). Backend half of the
mislabeling: A-CHAT-01-P1-01 (`TurnStatus` derived from an always-`Ok`
`drive_chat` Result, chat.rs:690-696).

## Conclusion

The send->reply path is real and complete on code paths; the terminal-facts
equivalence is broken on the GUI for error, cancel, timeout, and interrupt
turns. The scenario is judged **failed** at the code level; dynamic
confirmation is `not_run` (see V47-01, no credentials/GUI environment).

## Follow-Up

Fix ownership: canonical A-SRF-03-P1-01/P1-02, X-EVT-01-P1-02, A-CHAT-01-P1-01
(S-RDM-01). Re-validate after those land with the frontend fixtures in
A-SRF-03's regression validations.
