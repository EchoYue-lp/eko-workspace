# Q-E2E-01 / V21 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 4 (Tool: call -> result rendering) on the CLI REPL surface completes:
tool calls and results are rendered to stdout.

## Method

Static code-path determination (A-SRF-04 V02-01).

1. Execution: same framework executor via `drive_chat`; REPL's agent carries
   the Chat-mode exposure (16 tools, pinned by A-TOOL-01 V04-01).
2. Rendering: `ChannelChatSink` render loop prints tool events with 500-char
   previews (`repl.rs:568, 725`).

## Expected Result

Tool results are visible to the user with bounded output.

## Result

- Exit code: not-applicable (static determination)
- Summary: call -> render is wired; previews are UTF-8-safe
   (`chars().take(500)`).

## Deviations

Canonical: A-SRF-04-P1-01 (turn not cancellable mid-tool), A-HITL-01-P1-02
(approval prompts in piped stdin auto-approve — a tool-permission
consequence), A-TOOL-01-P1-01 (writer subagent tool surface broken in task
runs, not the REPL's own Chat mode).

## Conclusion

The CLI Tool scenario is judged **passed** at the code level. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

None beyond the canonical fixes.
