# Q-E2E-01 / V43 / Attempt 01

> Status: passed (static) — cursor paging for tool output, fold groups for
> tool/thinking/execution/subagent blocks
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 9 (Large output: large output -> fold/cursor) on the GUI surface
completes: large tool output is paged via a cursor and large display groups
fold on completion.

## Method

Static code-path determination (A-TOOL-01 V03-01; source grep at the
reviewed commits).

1. Tool output paging: `readOutput(detailRef, cursor, limit=64 KiB)`
   (`web-frontend/src/api/endpoints.ts:461`) over the paged journal
   (`DEFAULT_DETAIL_PAGE_BYTES = 64 KiB`, tool_execution.rs:17-19,
   `read_detail_page` `:415-420`); UTF-8-safe slicing in the store.
2. Fold/collapse: `ExecutionProcessGroup.tsx:9,29` (collapse on completion),
   `ThinkingSegment.tsx:17-37`, `ToolExecutionGroup.tsx:73`,
   `SubagentStreamBlock.tsx:83`, `MessageBubble.tsx:131` (tool-call runs
   collapsed into one item); truncated-manifest marker
   (`InlineToolCall.tsx:206` — "Agent 上下文已截断").

## Expected Result

Large output stays navigable: bounded pages, collapsible groups, truncation
markers.

## Result

- Exit code: not-applicable (static determination)
- Summary: tool output is cursor-paged (64 KiB pages) and the four display
   group types fold; the truncated manifest marker is rendered.

## Deviations

No cap found for a single huge *assistant text* message (rendered in full by
the DOM); this is residual rather than a finding (browser-level rendering,
no correctness impact). The cursor-paging path is GUI-only by design
(TUI/CLI render inline, A-TOOL-01 coverage note — not a capability gap).

## Conclusion

The GUI Large-output scenario is judged **passed** at the code level for the
fold/cursor requirement. Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

None.
