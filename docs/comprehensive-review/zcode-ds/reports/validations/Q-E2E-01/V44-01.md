# Q-E2E-01 / V44 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 9 (Large output: large output -> fold/cursor) on the TUI surface
completes: large tool output is bounded with a truncation marker and never
panics on UTF-8 boundaries.

## Method

Static code-path determination (A-SRF-01 V03-01; source grep at the reviewed
commits).

1. `append_bounded` (`src/tui/events.rs:350-377`): keeps at most `max_chars`
   (131,072 for tool output, `events.rs:717`), appends "… (truncated)" when
   truncated, returns the flag; per-tool `truncated` state rendered
   (`events.rs:691-748`).
2. UTF-8 safety: all slicing is `chars()`-based (AGENTS.md rule; tests at
   `events.rs:382-405`).

## Expected Result

Large output folds into a bounded buffer with an explicit truncation marker;
no byte-slice panic.

## Result

- Exit code: not-applicable (static determination)
- Summary: the bound + marker mechanism is wired at the reducer level and
   covered by `append_bounded` unit tests.

## Deviations

None for this scenario.

## Conclusion

The TUI Large-output scenario is judged **passed** at the code level.
Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

None.
