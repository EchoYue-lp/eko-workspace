# Q-E2E-01 / V05 / Attempt 01

> Status: failed (static; blocked by canonical F-OPS-01-P1-01)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 1 (Chat: scheduled turn -> reply -> terminal) on the cron surface
completes automatically: a scheduled tick launches a run that reaches a
durable terminal state with equivalent facts.

## Method

Static code-path determination; tick predicate re-anchored at the reviewed
commits (A-SRF-04 V02-01; F-OPS-01-P1-01 canonical).

1. Tick: `echo-agent/echo-orchestration/src/scheduler/runner.rs:80-93` —
   `if next >= window_start && next <= now` with `next` from
   `task.next_run()` (cron 0.12.1 `upcoming()` is strictly-future, so
   `next <= now` is unsatisfiable) — the automatic tick can never fire.
2. Manual path: `/cron run` -> `runner.run_once` (`src/cli/cmd_impls/cron.rs:
   410`) -> `build_fire_fn` (runner.rs:100-125) -> `launch_cron_run`
   (`echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:
   3895`) -> `launch_unattended_run` `:3571` -> `drive_unattended_run` `:3616`
   -> `drive_agent_run` `:3649`; `CronTaskCompleted` webhook wired
   (runner.rs:111-122).

## Expected Result

A scheduled cron entry fires and its run reaches a durable terminal
(completed/failed) with the same facts as any other trigger.

## Result

- Exit code: not-applicable (static determination)
- Summary: the run-launch chain is fully wired and durable; the *automatic
  trigger* is dead — the tick predicate at runner.rs:80-93 can never be
  satisfied. The scenario cannot start on its own; only a manual `/cron run`
  (or `run_once`) fires the run.

## Deviations

Canonical F-OPS-01-P1-01 (framework; all cron tasks silently never run);
consequence confirmed at the EKO surface (A-SRF-04 V05-01: webhook wired but
automatic tick blocked). Interrupted cron runs additionally become Paused at
boot and are unresumable from CLI/channel surfaces (A-SRF-04-P2-01).

## Conclusion

The cron Chat scenario is judged **failed** at the trigger layer: the
scheduled surface cannot complete any scenario automatically. Dynamic
confirmation is `not_run` (V47-01); even with credentials the automatic
trigger would not fire.

## Follow-Up

Fix ownership: F-OPS-01-P1-01 (tick predicate); A-SRF-04-P2-01 (resume
filter). Re-validate with a scheduled-fire fixture once the predicate is
fixed.
