# Q-E2E-01 / V42 / Attempt 01

> Status: failed (static; no history + no services on channels-only boot)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 8 (Restart: state/identity after restart) on the IM channel surface
completes: conversation identity and durable state (runs/cron) survive a
restart.

## Method

Static code-path determination (A-SRF-04 V02-01/V03-e; grep at the reviewed
commits).

1. Identity: conversation_id is deterministic (`channel:{cid}:{sid}`,
   channels.rs — A-SRF-04 V03-e) and is the pool key, so the *identity* is
   recomputable after restart; no conversation history is stored anywhere
   on the channel surface (no conversation_store wiring in channels.rs).
2. Services: channels-only boot never starts scheduler/background services
   (A-SRF-04-P1-02): cron never exists and background runs never resume on
   this surface.

## Expected Result

A channel user's state (history + runs) survives a restart.

## Result

- Exit code: not-applicable (static determination)
- Summary: identity recomputes; history is ephemeral; in channels-only mode
   runs submitted before a restart stay Paused with no resumer (service
   absent), and cron does not exist.

## Deviations

Canonical: A-SRF-04-P1-02 (channels-only boot omits services),
A-SRF-04-P2-01 (interrupted runs unresumable from the channel surface),
X-SRF-01-P2-02 (no run-control surface on channels).

## Conclusion

The channel Restart scenario is judged **failed**: history is ephemeral and
durable runs are unrecoverable on the channels-only surface. Dynamic
confirmation is `not_run` (V48-01).

## Follow-Up

Fix ownership: A-SRF-04-P1-02 (unify channels boot with
`start_headless_services`), A-SRF-04-P2-01 (generalize resume ownership).
Re-validate with the channels-only restart fixture.
