# Q-E2E-01 / V13 / Attempt 01

> Status: passed (static) — with canonical cancel-class and duplication defects
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 3 (Subagent: dispatch -> result -> review) on the GUI surface
completes: a subagent is dispatched (plan task or delegate tool), its result
arrives, and the user can review it (cards + detail view).

## Method

Static code-path determination (X-EVT-01 V01-01/V02-01; X-SRF-01 V02-01 S4).

1. Dispatch: TaskRuntime plan tasks -> `execute_task` ->
   `delegate_to_agent_with_cancel`/`run_writer_subagent` (`executor.rs:1843-2268,
   2114-2213`); execution ids `{run_id}:{task_id}:{plan_revision}:{attempt}`
   consistent backend-to-frontend (X-EVT-01).
2. Result: `SubagentEventBus` -> bridge (`src/tauri/mod.rs:347-768`) ->
   `subagentRunStore` monotone ingest (`subagentRunStore.ts:450-460`);
   durable `SubagentReleaseRecord` replay (X-EVT-01 V03-01).
3. Review: `SubagentDetailView.tsx` + subagent cards; `update_subagent_runs`
   equivalent on TUI; GUI task panels.

## Expected Result

Every dispatched subagent reaches a truthful terminal (completed/failed/
cancelled/timed_out) and the review surface shows it.

## Result

- Exit code: not-applicable (static determination)
- Summary: dispatch -> result -> review is fully wired; terminals persist and
  replay monotonically.

## Deviations

Canonical X-EVT-01-P1-01: mid-stream cancelled/timed-out subagents surface as
`failed` (envelope normalization bypasses `subagent_status_from_error`,
executor.rs:138-147 vs event_envelope.rs:134-140) — the review card lies
about cancel/timeout outcomes. A-SRF-02-P2-01/A-SRF-03-P2-01: duplicate tool
projection inflates subagent tool counts. A-TOOL-01-P3-02: bridge lag drops
can leave a card Running until restart.

## Conclusion

The GUI Subagent scenario is judged **passed** for the dispatch->result->
review path with the filed terminal-class and duplication defects. Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: X-EVT-01-P1-01 (with F-RCT-03-P1-02), A-SRF-02-P2-01.
Re-validate with the subagent cancel/timeout fixture.
