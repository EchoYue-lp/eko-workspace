# Q-E2E-01 / V11 / Attempt 01

> Status: failed (static; trigger blocked, same as V05)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 2 (Task: create -> execute -> complete) on the cron surface
completes: a scheduled task run launches, executes plan tasks, and reaches a
durable terminal.

## Method

Static code-path determination (A-SRF-04 V02-01; F-OPS-01-P1-01 re-anchored
at `echo-orchestration/src/scheduler/runner.rs:80-93`).

1. `launch_cron_run` (`executor.rs:3895`) -> `launch_unattended_run` `:3571`
   (conversation_id `"cron:{task}:{fire}"` `:3580-3582`) ->
   `drive_unattended_run` `:3616` -> `drive_agent_run` `:3649` (plan tasks
   executed by the shared DAG executor); `CronTaskCompleted` webhook
   (runner.rs:111-122); failures persist `Failed` (A-SRF-04 V04-01).

## Expected Result

A scheduled task run fires automatically and completes durably.

## Result

- Exit code: not-applicable (static determination)
- Summary: the launch chain is complete and durable; the automatic trigger is
  dead (tick predicate `next <= now` unsatisfiable — canonical
  F-OPS-01-P1-01). The scenario can only run via manual `/cron run`
  (`cron.rs:410`).

## Deviations

Canonical: F-OPS-01-P1-01; A-SRF-04-P2-01 (interrupted cron runs Paused at
boot, unresumable from CLI/channel); A-HITL-01-P2-03 (approval auto-reject in
unattended runs).

## Conclusion

The cron Task scenario is judged **failed** at the trigger layer (same as the
cron Chat row). Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: F-OPS-01-P1-01; A-SRF-04-P2-01. Re-validate with a
scheduled-fire fixture.
