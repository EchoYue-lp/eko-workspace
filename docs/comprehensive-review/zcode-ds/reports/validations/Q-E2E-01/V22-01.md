# Q-E2E-01 / V22 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 4 (Tool: call -> result rendering) on the IM channel surface
completes: tool calls on the per-sender agent render to the sender.

## Method

Static code-path determination (X-SRF-01 V01-01; A-TOOL-01 exposure rows).

1. Exposure: channel agents carry the Auto-mode exposure (A-TOOL-01:
   Auto 19 tools, default mode on channels); same framework executor.
2. Rendering: `aggregate_by_sentence` projects tool events with 500-char
   previews (`channels.rs:599-630`).

## Expected Result

Tool results are projected to the sender in bounded, chunked messages.

## Result

- Exit code: not-applicable (static determination)
- Summary: call -> projection is wired; sentence chunking is UTF-8-safe
   (`channels.rs:552`, covered by the 27 channel-feature tests).

## Deviations

Canonical: A-HITL-01-P2-02 (approval responses not per-sender — another
sender's text can resolve a pending approval), A-SRF-04-P1-01 (turn not
cancellable mid-tool).

## Conclusion

The channel Tool scenario is judged **passed** at the code level. Dynamic
confirmation is `not_run` (V48-01).

## Follow-Up

None beyond the canonical fixes.
