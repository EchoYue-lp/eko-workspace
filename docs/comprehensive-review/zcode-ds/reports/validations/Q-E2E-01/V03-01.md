# Q-E2E-01 / V03 / Attempt 01

> Status: passed (static) — with the canonical cancel gap
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 1 (Chat: send -> reply -> terminal) on the CLI REPL surface completes:
input -> `drive_chat` -> streamed reply -> terminal rendered to stdout, with
facts equivalent to the shared driver contract.

## Method

Static code-path determination (X-SRF-01 V02-01 S1; A-SRF-04 V02-01/V03-01).

1. Input loop `src/cli/repl.rs:190-237`; `chat_with_agent` `:477-560`:
   `PreparedUserTurn::build` `:509-524`, `ChatResources` with fresh
   `CancellationToken` `:533`, `drive_chat` spawned `:541-545` (call site
   `:543`) into `ChannelChatSink` render loop `:563-838` (terminal error
   print at `:813`).

## Expected Result

A typed terminal is rendered for the turn and the surface exposes the same
control semantics (cancel) as TUI/GUI.

## Result

- Exit code: not-applicable (static determination)
- Summary: send -> reply -> terminal render is fully wired; the sink prints
  the terminal (Error/FinalAnswer/Cancelled arms at repl.rs:627/:813). The
  REPL keeps the default HITL provider (`runtime.rs:130-131`).

## Deviations

Canonical A-SRF-04-P1-01: the turn token (`repl.rs:533`) has zero `.cancel()`
producers and the input loop awaits the turn inline (`repl.rs:234-237`), so a
mid-turn Ctrl+C takes the default SIGINT disposition and kills the process
(no signal handler in CLI mode; the only ctrl_c site is `modes.rs:228` in
channels mode), skipping shutdown hooks. No noninteractive single-shot mode
(A-SRF-04-P3-02): the terminal exists only as stdout text, not a typed
process exit.

## Conclusion

The CLI Chat scenario is judged **passed** for completion and terminal
rendering, with the cancel/shutdown equivalence broken by canonical
A-SRF-04-P1-01 (a P1 capability failure on this surface). Dynamic
confirmation is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-SRF-04-P1-01 (retain token + signal handler + select).
Re-validate with the A-SRF-04 regression fixtures (signal mid-turn).
