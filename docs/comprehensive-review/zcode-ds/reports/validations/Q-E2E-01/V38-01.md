# Q-E2E-01 / V38 / Attempt 01

> Status: failed (static; no connect/management surface on channels)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 7 (Browser/MCP: connect -> tools available) on the IM channel
surface completes: a channel user can connect/manage a browser or MCP server
and the tools become available.

## Method

Static code-path determination (X-SRF-01 V01-01 matrix; A-INT-01 V02-01).

1. Channel surface: no slash surface at all (`parse_channel_mode_command`
   `channels.rs:118` handles only mode commands; NL special handlers
   `:153-188` cover trace/analysis/papers/skills — nothing for browser/MCP).
2. Tools: browser/MCP tools are registered on per-sender pool agents
   (infra.rs:454-455 registration; channels agents are full-bootstrap), so
   *chat-mediated* use works; the *connect* step has no user path.

## Expected Result

The connect step of the scenario is performable from the channel.

## Result

- Exit code: not-applicable (static determination)
- Summary: tools are available via agent chat; there is no connect/
   manage/restart path (the scenario's first step) on the channel surface,
   and no NL routing for it. Per the parity invariant (AGENTS.md; X-SRF-01),
   an absent management surface on an NL-only surface is a gap unless an NL
   route exists — none does for browser/MCP.

## Deviations

Canonical: X-SRF-01-P2-01/P2-02 family (management-surface gaps), the
capability matrix rows "browser (management): absent / MCP: via agent tools
only" (X-SRF-01 V01-01).

## Conclusion

The channel Browser/MCP scenario is judged **failed** for the connect step;
tool availability itself is wired. Live connect is `not_run` (V49-01).

## Follow-Up

Fix ownership: X-SRF-01 family (add NL routing for browser/MCP management or
document the scope decision). Re-validate with the channel NL-connect
fixture after a fix.
