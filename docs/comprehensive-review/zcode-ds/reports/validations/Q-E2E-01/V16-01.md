# Q-E2E-01 / V16 / Attempt 01

> Status: passed (static) — result review is per-sentence projection only
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 3 (Subagent: dispatch -> result -> review) on the IM channel surface
completes: a per-sender pool agent dispatches subagents and their results are
projected to the sender.

## Method

Static code-path determination (X-SRF-01 V01-01; A-SRF-04 V02-01).

1. Dispatch: per-sender pool agent (`channels.rs:135-139`) carries the full
   bootstrap-equivalent tool surface (modes.rs:112-116 doc) — subagent
   delegate tools and task-run dispatch are available in-channel.
2. Result: `drive_chat` -> `ChannelChatSink` -> `aggregate_by_sentence`
   (`channels.rs:515-654`) projects subagent/tool events as sentences;
   durable subagent records persist in the TaskRuntime store.

## Expected Result

Subagent dispatch and result facts reach the sender.

## Result

- Exit code: not-applicable (static determination)
- Summary: dispatch and per-sentence result projection are wired; there is no
  structured subagent review surface on channels (no run-control — canonical
  X-SRF-01-P2-02 — and no introspection).

## Deviations

Canonical: X-SRF-01-P2-02 (no run control from the channel), X-EVT-01-P1-01
(cancel/timeout class loss in subagent terminals), A-SRF-04-P1-01 (in-flight
turns not cancellable), A-HITL-01-P2-02 (approvals not per-sender).

## Conclusion

The channel Subagent scenario is judged **passed** for dispatch->result, with
review limited to per-sentence text. Dynamic confirmation is `not_run`
(V48-01).

## Follow-Up

None beyond the canonical fixes.
