# Q-E2E-01 / V27 / Attempt 01

> Status: failed (static; canonical A-HITL-01-P1-02)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 5 (HITL: approval -> execute / reject) on the CLI REPL surface
completes with fail-closed semantics: a no-response approval is rejected and
the shared deadline bounds the wait.

## Method

Static code-path determination (A-HITL-01 V01-01/V03-01).

1. Provider: `ReplHumanLoopProvider` registered on the shared dispatcher at
   bootstrap (`runtime.rs:130-131`) and kept on the CLI path.
2. Response: `hitl/repl_provider.rs:69-77` — `read_line`; `"y" | "yes" | "" =>
   Approved` (`:74-77`).

## Expected Result

EOF/empty stdin fails closed (Rejected); the 5-minute dispatcher deadline is
enforceable.

## Result

- Exit code: not-applicable (static determination)
- Summary: interactive `y`/`n` works; EOF or empty input **auto-approves**
   (piped/scripted stdin silently consents to destructive tools); the
   blocking `read_line` cannot be interrupted by
   `tokio::time::timeout_at`, defeating the dispatcher's shared deadline
   (dispatcher.rs:96-101 claim is false for this provider).

## Deviations

Canonical: A-HITL-01-P1-02 (EOF auto-approve + deadline defeat), A-HITL-01-P1-03
(REPL `a` -> SessionAllTools wildcard), A-HITL-01-P2-03 (escalation after 3
denials).

## Conclusion

The CLI HITL scenario is judged **failed** at the code level: the reject
branch is unreachable under EOF and the deadline is unenforceable. Dynamic
confirmation (piped-stdin run) is `not_run` (V47-01).

## Follow-Up

Fix ownership: A-HITL-01-P1-02 (EOF -> Rejected; async stdin read).
Re-validate with the closed-stdin provider test and the piped-CLI fixture.
