# Q-E2E-01 / V06 / Attempt 01

> Status: passed (static)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean in both repositories

## Claim

Scenario 1 (Chat: background prompt -> reply -> terminal) on the background
surface completes: submit -> unattended run -> durable terminal state with
equivalent facts.

## Method

Static code-path determination (A-SRF-04 V02-01; X-SRF-01 V01-01).

1. `echo-agent-app-core/src/tasks/service.rs` `submit` `:219/:253` ->
   `submit_prompt_run` -> `start_run_driver` `:359-407` -> `execute_run`
   (plan exists) or `drive_unattended_run` (no plan); conversation_id
   `"background:{source}:{uuid}"` `:270-272`.
2. Auto-mode tool exposure `executor.rs:3686-3713`; run cancel token
   registered `:3662-3664`; terminal facts persist in the durable run store
   (`TaskRunStatus`) and replay monotonic (X-EVT-01 V03-01).

## Expected Result

The background run reaches a durable terminal and its facts are equivalent to
a foreground run.

## Result

- Exit code: not-applicable (static determination)
- Summary: the chain is fully wired; terminal statuses persist and replay;
  `resume_pending` (`service.rs:556-558`, `background:` prefix filter)
  resumes interrupted runs at next boot.

## Deviations

Canonical notes: approvals in unattended runs auto-reject via the empty
dispatcher (A-HITL-01-P2-03 — write/execute tools are denied, so a
tool-dependent background "chat" degrades to denials); cancel tokens for the
service loop are never fired (A-SRF-04-P2-02); killed runs become Paused at
boot (A-SRF-04-P2-01, background runs resume via `resume_pending`).

## Conclusion

The background Chat scenario is judged **passed** at the code level for
completion and durable terminals, with the approval-policy caveat above.
Dynamic confirmation is `not_run` (V47-01).

## Follow-Up

None beyond the canonical fixes (A-HITL-01-P2-03 direction, A-SRF-04-P2-02).
