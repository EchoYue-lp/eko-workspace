# F-SKL-01 / V04 / Attempt 08

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable
> Worktree state: both source repositories clean; probe crate in `/tmp/fskl-probe`

## Claim

Malformed frontmatter edges are handled deterministically per the loader contract: closing `---` with trailing content on the same line is rejected; a block-scalar description containing an indented `---` line is not mistaken for the terminator.

## Method

```text
cd /tmp/fskl-probe && cargo build
./target/debug/fskl_probe d-frontmatter-edge
```

Probe calls `parse_skill_md` with (a) `--- ... --- extra\nbody` and (b) a block-scalar description containing a line `  ---`.

## Result

- Exit code: 0
- Summary:
  - (a) `[probe] trailing-content REJECTED: SKILL.md frontmatter closing --- has trailing content on same line`
  - (b) `[probe] block-scalar-with-rule-line accepted: desc="a\n---\nb"`

## Deviations

None.

## Conclusion

Supports: the strict closing-`---` heuristic works for these edge cases and is deterministic; no UTF-8 panic risk found in the slicing (ASCII-only boundaries, `loader.rs:417-450`).

## Follow-Up

None.
