# F-SKL-01 / V04 / Attempt 07

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable
> Worktree state: both source repositories clean; probe crate in `/tmp/fskl-probe`

## Claim

Discovery precedence is deterministic: when the same skill name exists in multiple discovery scopes, the earlier scope wins (loader doc: "earlier scopes take precedence", `loader.rs:118-119`).

## Method

```text
cd /tmp/fskl-probe && cargo build
./target/debug/fskl_probe c-precedence
```

Probe creates `dup-skill` in two directories (`proj/`, `user/`) with different descriptions, discovers with `[Custom(proj), Custom(user)]`, and prints the winning description.

## Result

- Exit code: 0
- Summary: `[probe] precedence winner: project-scope (expected project-scope)`

## Deviations

None. (Caveat: the same-name collision *within* one directory at different nesting depths resolves by `read_dir` order, which is filesystem-nondeterministic; recorded as a minor note in V03-01, not exercised here.)

## Conclusion

Supports: cross-scope discovery precedence is deterministic and matches the documented contract.

## Follow-Up

None.
