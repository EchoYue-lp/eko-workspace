# F-SKL-01 / V04 / Attempt 06

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable
> Worktree state: both source repositories clean; probe crate in `/tmp/fskl-probe`

## Claim

Frontmatter metadata written as a YAML list for `requires-binaries` parses into the descriptor and is handled by `dependency_probe::extract_dependencies`; the in-code comment (`dependency_probe.rs:52-65`: "YAML lists like `[soffice, pdftoppm]` come through as `"[soffice, pdftoppm]"`") is accurate.

## Method

```text
cd /tmp/fskl-probe && cargo build
./target/debug/fskl_probe b-yaml-list
```

Probe calls `parse_skill_md` with `metadata:\n  requires-binaries: [soffice, pdftoppm]` and with the comma-string form.

## Expected Result

Both forms parse; `metadata["requires-binaries"]` yields the expected string.

## Result

- Exit code: 0 (probe process; the checked behavior FAILED)
- Summary:
  - YAML list form: `[probe] yaml list PARSE FAILED: SKILL.md YAML parse error: metadata.requires-binaries: invalid type: sequence, expected a string at line 4 column 22`
  - Comma-string form: `[probe] comma string parsed OK; metadata requires-binaries = Some("soffice, pdftoppm")`

## Deviations

The documented input form is rejected at parse time; because `parse_skill_file_with_variables` treats a parse error as "skip the skill" (`loader.rs:270-276`), the entire skill is silently dropped from discovery (only a `warn!` log). The EKO `skills_hub` naive parser, by contrast, accepts the same line and splits it (incorrectly keeping the brackets: `[soffice`), so the marketplace can list a skill the agent never loads.

## Conclusion

This execution **fails** the documented-format contract and is the empirical basis for finding **F-SKL-01-P2-03** (YAML-list metadata silently drops the whole skill; the `dependency_probe.rs` doc claim is wrong; EKO hub and framework diverge on the same input).

## Follow-Up

None (finding recorded; fix direction: tolerant `metadata` deserialization or doc correction + cross-surface consistency test).
