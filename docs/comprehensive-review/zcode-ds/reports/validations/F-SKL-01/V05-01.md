# F-SKL-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical skill-related claims in planning/audit documents point at current code, or are explicitly classified as fixed/stale/regressed with evidence.

## Method

Grep of the planning/audit documents for skill claims, then code verification of each claim:

```text
grep -rn -i "skill" docs/MASTER-PLAN.md
grep -rn "Skill System|load_skills_from_dir|skill" echo-agent/README.md
grep -rn "wire_all|PluginIntegrator" echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs
```

## Expected Result

Each claim classifiable as current/fixed/stale/regressed with anchors.

## Result

- Exit code: not-applicable (inspection)
- Summary (claim -> classification -> evidence):
  - MASTER-PLAN:266-269 "workspace、skill catalog ... marker/latest-wins 更新,不重复 append;验收:相同 workspace/skills 下连续 turn 的稳定前缀 hash 不变" -> **current (in-process)** for the catalog projection (`SKILL_CATALOG_PROJECTION` via `replace_projection`, capabilities.rs:741-746, replace-only; test pins single projection, V04-03). The "stable prefix" acceptance holds only within one process: the baseline methodology `<skill>` blocks are injected in HashMap iteration order (enabled_skills.rs:110-116 and registry.rs:615) which varies across processes -> finding F-SKL-01-P2-02.
  - MASTER-PLAN:991 "SkillRegistry 无来源追踪,需加 source 字段/by_source 索引" (old gap) -> **fixed**: `SkillDescriptor.source` (serde skip), `by_source`, `unregister_by_source`, `tag_source` all implemented (`registry.rs:101-190, 133-151`), plugin unload wired (`plugin_runtime.rs:1167`), tests green (`source_group_unload_returns_exact_skill_names`).
  - MASTER-PLAN:1002 "runtime.rs::load_plugins 退化为薄 adapter:直接调 PluginIntegrator::new().wire_all(...),删除手写的 skills/hooks/mcp 三段装配循环" -> **current**: `plugin_runtime.rs:818-854` calls `PluginIntegrator::wire_all`.
  - MASTER-PLAN:1009 "wire_skills/wire_mcp 单组件入口仍存在,非重复,保留" -> **current**: `plugin.rs:398-410` retained.
  - echo-agent README:220 "Skill System ... agent.load_skill('web_research')" -> **stale**: the public API is `load_skills_from_dir`/`discover_skills`; no `load_skill(single)` method exists (grep: zero matches). Minor doc drift.
  - README:686 "Pre-built skills: code_review, data_analyst, project-stats, python-linter, web_researcher" -> **stale**: these skill names do not exist under `echo-agent/skills/` (directory listing shows no such packs; builtin code skills are `filesystem`/`shell`). Verified by `ls echo-agent/skills` — directory is empty of such packs.

## Deviations

Two README claims are stale (above). Neither affects runtime behavior; classified as P3 documentation drift input for Q-DOC-01.

## Conclusion

Supports: planning-document skill lifecycle claims are current or fixed; two README claims are stale; the "stable prefix" acceptance is in-process only (basis for F-SKL-01-P2-02).

## Follow-Up

Q-DOC-01 (README skill API/name drift).
