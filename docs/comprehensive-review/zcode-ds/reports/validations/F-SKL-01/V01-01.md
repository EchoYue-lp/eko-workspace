# F-SKL-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean (probe crate lives in `/tmp/fskl-probe`, outside both repos)

## Claim

Skill discovery/loading/execution has one coherent definition set per layer (framework vs EKO), and the inventory of concepts, types, and implementations across both repositories is complete enough to decide layering and to detect parallel implementations.

## Method

Repository-wide grep over both repos for skill concepts, then full reads of the skill modules:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
grep -rn "discover_skills|SkillRegistry|SkillInfo|add_skill" --include="*.rs" -l  (echo-agent, echo-agent-cli)
grep -rn "SKILL\.md|frontmatter|front_matter|agentskills" --include="*.rs" -il (both)
grep -rn "skill" --include="*.rs" -il echo-agent/src echo-agent/echo-execution/src echo-agent/echo-core/src
grep -rni "skill" --include="*.rs" -il echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src
```

Files then read in full or by behavior slice:
`echo-core/src/tools/skill.rs`, `echo-execution/src/skills/{mod,registry,dependency_probe}.rs`,
`echo-execution/src/skills/external/{mod,types,loader,activate_tool,resource_tool,run_script_tool,prompt_exec}.rs`,
`echo-execution/src/skills/hooks.rs` (core engine), `echo-execution/src/skills/builtin/*`,
`echo-agent/src/skills/mod.rs`, `echo-agent-cli/echo-agent-app-core/src/skills_hub/{mod,registry,enabled_skills,install}.rs`.

## Expected Result

One framework authority for the skill runtime (`echo_core` trait + `echo_execution` loader/registry/tools/hooks, root facade re-exporting) and EKO owning only marketplace/enablement policy (`skills_hub`). Parallel implementations of the same semantic should be listed with their degree of divergence.

## Result

- Exit code: not-applicable (inspection)
- Summary:
  - Framework definition set (single authority, correctly layered): `Skill` trait + `SkillInfo` + `is_path_safe`/`minimal_env` helpers (`echo-core/src/tools/skill.rs`), loader/registry/tools/hooks (`echo-execution/src/skills/`), root facade (`echo-agent/src/skills/mod.rs` re-export only, no logic).
  - EKO definition set (`echo-agent-cli/echo-agent-app-core/src/skills_hub/`): marketplace registry (`registry.rs`), enablement config (`enabled_skills.rs`), installer/sync (`install.rs`). `SkillsHub` is EKO product policy (scans `~/.eko/skills/`), correctly placed.
  - Consumers of the framework API: `echo-agent/src/agent/react/capabilities.rs` (`add_skill`/`discover_skills`/`activate_skill`/`reconcile_skill_load_policy`/`unregister_skills_by_source`), `echo-agent/src/plugin.rs` (`load_plugin_skills_from_dir`, `wire_skills`), `echo-agent/src/agent/react/{mod.rs, snapshot.rs, run/context.rs, run/react_loop.rs, run/stream_channel.rs}`, `echo-agent/src/intent/trigger_supervisor.rs` (skills from hooks), EKO `runtime.rs`, `state.rs`, `agent_pool.rs`, `tool_exposure.rs`, `plugin_runtime.rs`, `src/cli/cmd_impls/skills.rs`, `src/tauri/commands/plugins.rs`.
  - Parallel implementations found (all framework-internal or framework/EKO duplications):
    1. Five frontmatter parse/strip implementations: `loader.rs:407-483` (`parse_frontmatter`, `extract_instructions`), `registry.rs:657-709` (`strip_frontmatter`, `extract_body`), `skills_hub/registry.rs:331-388` (naive line parser). Behavior diverges (strict vs lenient closing `---`; missing-name fallback).
    2. Two binary-probing implementations: `dependency_probe.rs:101-109` (`binary_available`) and `skills_hub/registry.rs:317-325` (explicitly inlined copy, comment at :315-316). Parsing of the `requires-binaries` list also diverges (`parse_metadata_list` strips brackets, hub split does not).
    3. Two `SkillRegistry` instances per agent (tracking + progressive shared), holding the same descriptors and independent activation state (see V02-01).
  - `evolution/*` "skill" references are a different concept (memory-layer skill extraction), not the file-skill runtime; no overlap found.

## Deviations

None (beyond the divergences themselves, recorded as findings F-SKL-01-P3-01, F-SKL-01-P3-02).

## Conclusion

Supports: definition/duplicate inventory is complete for the layering decision; the framework skill runtime is one authority; EKO owns marketplace policy; specific duplicate implementations exist and are enumerated. Does not prove: runtime reachability (V02-01) or edge-case behavior (V03-01/V03-02).

## Follow-Up

V02-01, V03-01, V03-02.
