# A-CFG-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Config discovery, precedence resolution, provider/model selection, workspace
switch, and config-file loading each have a single authoritative definition in
echo-agent-cli + echo-agent, with no parallel implementation of the same
semantic across TUI/GUI/CLI entry points.

## Method

Repository-wide grep (both repositories) for definition and call sites of the
following term families, then classification of each match as
definition/registration/live-caller:

```text
ConfigDiscovery, ConfigInventory, discover_config, config_search_paths,
load_config, load_config_file, save_config, resolve_config_path,
resolve_runtime_model, set_default_model, upsert_configured_model,
effective_context_window, DEFAULT_CONTEXT_WINDOW, to_agent_config,
resolve_context_window, switch_workspace, exit_workspace, WorkspaceRegistry,
WorkspaceLayout, LegacyMigrator, spawn_config_watcher, HookConfigLoader,
load_merged, load_merged_from_disk, WebConfig, web_config, tasks_db_path,
worker (terminology check)
```

## Expected Result

One definition per semantic; at most one live consumer chain per semantic;
any second construction path is either test-only or a recorded duplicate.

## Result

- Exit code: not-applicable (grep-based inspection)
- Summary:
  - `ConfigDiscovery` / `ConfigInventory`: one definition
    (`echo-agent-app-core/src/config_discovery.rs`), re-exported once
    (`lib.rs:9`), one IPC registration `discover_config`
    (`src/tauri/mod.rs:99`, `src/tauri/commands/config.rs:311-335`). Zero
    frontend callers (`discover_config`/`discoverConfig` absent from
    `web-frontend/src`). One command — `switch_workspace` etc. — see V02.
  - Framework config loader: single authority `echo_agent::config`
    (`load_config` config.rs:725, `load_config_file` :681,
    `save_config` :691, `config_search_paths` :666). EKO consumers:
    main.rs:100, desktop.rs:133, config_watcher.rs:254-257,
    hook_config_loader.rs:133-147, infra.rs:1757, cli/cmd_impls/advanced.rs:308.
    No second loader implementation.
  - Model selection: single app-core authority `model_config.rs`
    (`resolve_runtime_model` :237, `set_default_model` :187,
    `upsert_configured_model` :161, `effective_context_window` :153).
    Consumers: infra.rs:201, tauri/commands/providers.rs, main.rs:289 (TUI
    list). Duplicate constant: `DEFAULT_CONTEXT_WINDOW` exists twice with the
    same value 396_000 — infra.rs:23 and model_config.rs:6 (recorded).
  - Workspace: single `switch_workspace`/`exit_workspace` implementation
    (state.rs:844, :1053); callers: tauri/commands/workspace.rs:137/:113,
    frontend `workspaceApi.switch` (endpoints.ts:1422). CLI/TUI paths do not
    call it (see V02).
  - Watcher: one `spawn_config_watcher` (config_watcher.rs:62), two spawn
    sites (main.rs:232, desktop.rs:166) — boot only, never restarted.
  - Hooks: one loader `HookConfigLoader` (hook_config_loader.rs), used by
    infra.rs:1919 (boot), config_watcher.rs:258 (reload), /hooks commands
    (cli/cmd_impls/hooks.rs:39, tauri/commands/hooks.rs:128, tui/events.rs:3506).
  - `WebConfig`/`web_config`: only three references (state.rs:340, :479;
    config.rs:118) — write-only store, no readers (recorded).
  - `tasks_db_path` (state.rs:1197): definition only, zero callers.
  - `worker` terminology: zero matches in the config/workspace/provider
    domain files searched.
- Log/artifact: none (grep output inspected inline)

## Deviations

The `DEFAULT_CONTEXT_WINDOW` constant duplication and the `web_config`
write-only store are duplicates found by this search; they are recorded as
findings, not as additional authorities that needed merging in this task.

## Conclusion

Supports: single-authority claim for config loading, hook loading, model
resolution, and workspace CRUD; three intra-domain duplicates exist
(`DEFAULT_CONTEXT_WINDOW` x2, `web_config` orphan, dead `tasks_db_path`).
Does not prove runtime reachability — that is V02.

## Follow-Up

V02-01 (reachability), V03-01 (invariants).
