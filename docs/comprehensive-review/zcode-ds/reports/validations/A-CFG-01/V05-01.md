# A-CFG-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documents describing config file locations, workspace layout,
provider selection, and hot-reload scope still match the reviewed code;
references to removed components are classified rather than carried forward.

## Method

Grep + read of the config/workspace-related claims in the following documents
and cross-check against the code anchors found in V01/V02/V03:

```text
echo-agent-cli/docs/configuration.md, docs/getting-started.md,
docs/MASTER-PLAN.md, docs/system-deep-dive/06-skills.md,
docs/system-deep-dive/07-cross-cutting.md (config mentions),
echo-agent-cli/echo-agent-app-core/src/workspace/mod.rs (module doc),
root AGENTS.md (echo-agent-eval rows),
echo-agent/docs/en|zh/28-config-reference.md (framework-side paths)
```

## Expected Result

Each document claim maps to a current code path or is explicitly classified
as stale/regressed with the discrepancy recorded.

## Result

- Exit code: not-applicable (grep + read)
- Summary of classifications (details in the task report Historical Claim
  Status table):
  - `docs/configuration.md:8-10` — search order includes a nonexistent
    `./.echo-agent/echo-agent.yaml` tier and the pre-EKO root
    `~/.echo-agent/config.yaml`; the file never mentions `~/.eko` (11
    `.echo-agent` occurrences, zero `.eko`). STALE (EKO root is `~/.eko`).
  - `docs/getting-started.md` — same root drift (8 `.echo-agent`
    occurrences, zero `.eko`). STALE.
  - `docs/configuration.md` provider section — claims CLI/TUI read the same
    configured default model (true) but implies model switching is available
    in CLI/TUI (REPL `/model` is a print-only stub; TUI `/model` works).
    PARTIALLY STALE.
  - `docs/MASTER-PLAN.md:253` — workspace switch/exit refresh the primary
    Agent immediately. PARTIALLY CURRENT: projections refresh on both paths,
    but exit does not restore CWD, and switch does not reload config/hooks.
  - `docs/MASTER-PLAN.md:375` — plugin project_root derives from
    working_dir so switches reflect without service recreation. CURRENT for
    switch; on exit working_dir=None falls back to CWD which remains the
    exited workspace (same P1-02 family).
  - `echo-agent-app-core/src/workspace/mod.rs:15` — module doc lists
    `tasks/` as "后台任务 SQLite DB" under the workspace layout; no SQLite
    exists in EKO and the task store shadow is global `~/.eko/tasks/`
    (file_shadow.rs:83-85), not workspace-scoped. STALE (and extends the
    A-BOOT-01-P3-06 SQLite-mention family at a third site).
  - `docs/system-deep-dive/06-skills.md:427` — echo-agent-eval crate removed.
    CURRENT (matches workspace reality).
  - Root `AGENTS.md:139,370` — still list `echo-agent-eval` as an EKO
    submodule with Cargo.toml path rules. STALE (already recorded as
    F-EVO-01-P3-03; cross-referenced here per task instructions — config/
    operator docs keep referencing the removed crate, e.g. any eval config
    guidance in configuration.md: none found, so the drift is limited to
    AGENTS.md and the roadmap).
  - `echo-agent/docs/en|zh/28-config-reference.md` — `~/.echo-agent/config.yaml`
    is correct for default framework consumers (DEFAULT_USER_DATA_DIR_NAME =
    ".echo-agent", paths.rs:25) but does not mention that EKO overrides the
    root to `~/.eko` (paths.rs:58). PARTIALLY STALE at the boundary.
- Log/artifact: none

## Deviations

None.

## Conclusion

Supports: EKO config/workspace documentation is materially stale on the data
root and config filename (P2-01/P3-01); workspace-module doc promise of
per-workspace tasks contradicts the global task shadow (P2-02); the
echo-agent-eval crate references are confirmed stale and limited to AGENTS.md
+ the subagent-parity roadmap (F-EVO-01-P3-03, cross-referenced).

## Follow-Up

None.
