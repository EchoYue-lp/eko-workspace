# A-TOOL-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Every EKO tool-exposure entry point is live: invocation-scoped visibility is
applied on all three interaction modes (Chat/Task/Auto) on all reachable
drivers; the interactive terminal Tauri commands are registered; the
run_code sandbox probe is applied to the main agent and to writer subagents;
and the writer-subagent "full write tools" surface is what actually runs for
TaskRuntime Implementation/Debugging tasks.

## Method

Static call-graph trace with source reads and grep:

```text
1. chat_driver.rs:444-511 — Chat/Auto turn invocation applies initial_visible_tools + disabled_tools + record_mode_schema_budget, then execute_stream_message_with_invocation_context.
2. tasks/task_runtime/executor.rs:3084-3113 — Task-mode main-agent run applies the same trio.
3. tasks/task_runtime/executor.rs:3686-3713 — Auto-mode unattended direct run applies the same trio.
4. src/tauri/mod.rs:303-308 — create/write/confirm/resize/close/list terminal commands registered in invoke_handler.
5. infra.rs:265-272 — SandboxManager::local_sandbox() + has_local_os_sandbox() probe; infra.rs:533-543 configure_run_code_capability removes run_code; infra.rs:950 (writer builder) and infra.rs:445 (primary) both call it.
6. Writer task path: executor.rs:2114-2118 (is_writer_task = Implementation|Debugging) -> executor.rs:2213 -> run_writer_subagent (executor.rs:2889) -> delegate_to_agent_with_prompt_payload (echo-agent/src/agent/react/mod.rs:2341) -> subagent_executor.dispatch(Fork) -> registered fork factory (infra.rs:791-809) -> build_writer_subagent_agent (infra.rs:881) with subagent.set_plan_mode(true) at infra.rs:963. The pre-built handle (infra.rs:679-685, 846-850) is built by the same builder.
7. grep "set_plan_mode(false)" across both repositories: zero hits.
```

## Expected Result

All five claims above verified with concrete anchors; the writer path carries
plan_mode=true on every construction route.

## Result

- Exit code: not-applicable (inspection)
- Duration: about 40 minutes
- Summary:
  - Tool exposure is applied on 3 distinct driver paths (chat_driver for
    Chat/Auto interactive turns; executor.rs Task-mode; executor.rs Auto-mode
    unattended). GUI, TUI, and CLI all reach these drivers (A-BOOT-01).
  - Terminal commands registered and reachable from the GUI webview.
  - Sandbox probe/removal applied to primary agent and writer subagents
    (both builder and fork-factory route); readonly subagents never get
    run_code (readonly_tools registry).
  - Writer task dispatch chain verified end-to-end; plan_mode=true is set in
    the single builder used by both the registered handle and the fork
    factory; nothing resets it.

## Deviations

The framework `ToolVisibility` (echo-agent/src/agent/snapshot.rs:224-236)
additionally filters the visibility whitelist by `plan_mode`, and
`tools_for_llm` (snapshot.rs:282-285) and `PlanModeStage`
(echo-agent/src/agent/react/run/pipeline.rs:1004-1018) enforce the same
filter — meaning the writer subagent's write tools are excluded at three
independent layers despite being registered (see task report P1-01).

## Conclusion

Supports: exposure is live on all modes/drivers; terminal is registered and
ungated; sandbox fail-closed is applied per agent; the writer plan_mode
conflict is on the live TaskRuntime writer path. Does not prove end-to-end
dynamic behavior (no process launched; read-only review).

## Follow-Up

None.
