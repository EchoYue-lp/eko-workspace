# A-TOOL-01 / V04 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The sandbox-probe removal test and the writer-subagent registry test pass on
the current commit, and the writer registry test does NOT cover the
LLM-visible plan-mode surface (the P1-01 blind spot).

## Method

```text
cd echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked unavailable_os_sandbox
cargo test -p echo-agent-app-core --lib --locked writer_subagent
```

Both are separate commands with separate terminal results; recorded here as one
indivisible pair of related registry checks.

## Expected Result

Exit 0 for both; `unavailable_os_sandbox_removes_run_code` proves run_code is
removed when the probe fails; `writer_subagent_inherits_sandbox_manager`
asserts the writer registry contains run_code, sandbox_manager is injected,
and agent_tool is absent.

## Result

- Exit code: 0 (both commands)
- Duration: about 3 minutes
- Summary:
  - `unavailable_os_sandbox_removes_run_code` ... ok (1 passed)
  - `writer_subagent_inherits_sandbox_manager` ... ok (1 passed)
  - Inspection of `writer_subagent_inherits_sandbox_manager`
    (infra.rs:2120-2153) shows it only asserts `subagent.list_tools()`
    (the registry), never the LLM-visible definitions or a write-tool call —
    the plan_mode=true at infra.rs:963 is invisible to this test.
- Log/artifact: none (console output only)

## Deviations

None (the blind spot is expected and recorded as part of P1-01).

## Conclusion

Supports: EKO fail-closed sandbox probing is real and tested; the writer
subagent's registry does carry full tools and the sandbox manager. Does NOT
support the claim that the writer subagent can actually write — the visible
surface is plan_mode-filtered (P1-01, static chain in V02-01/V03-01).

## Follow-Up

None.
