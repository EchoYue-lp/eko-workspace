# A-TOOL-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical tool-exposure/sandbox/terminal claims in the project docs point at
current code (classifiable as current/fixed/stale/regressed), and the
`require_full_auto` removal lesson is not regressed anywhere.

## Method

```text
1. grep -rn "full-auto|full_auto|require_full_auto|权限门控" docs/ echo-agent-cli/docs/ (exclude AGENTS.md and other reviewers' dirs)
2. Read echo-agent-cli/docs/2026-07-16-run-code-sandbox.md (M5 closure) claims vs current code.
3. Read echo-agent-cli/docs/2026-07-11-tool-execution-rendering-implementation-plan.md key claims vs ToolExecutionRepository.
4. docs/MASTER-PLAN.md:114 run_code/bare-fallback/terminal claim vs current code.
5. Read echo-agent-cli/docs/2026-07-17-surface-parity-closeout.md terminal/tool parity claims.
```

## Expected Result

M5 claims current; MASTER-PLAN:114 current; no re-gating of the interactive
terminal; the tool-rendering plan implemented.

## Result

- Exit code: not-applicable (inspection)
- Duration: about 15 minutes
- Summary:
  - `2026-07-16-run-code-sandbox.md` (M5): all contract claims verified
    current — framework fail-closed floor (`echo-tools/src/code.rs:313,324`);
    EKO probe + removal from main agent and writer subagents
    (`infra.rs:269-272, 533-543, 950`); readonly registry excludes run_code;
    "Interactive terminal and shell UI actions are user-controlled
    capabilities, not the Agent-only run_code primitive" — matches
    `src/tauri/terminal.rs:286-290` (no permission gate).
  - `docs/MASTER-PLAN.md:114` — "run_code: 框架已删除 bare fallback ... 交互式
    terminal 仍是用户主动能力,不受此合同或 agent permission_mode 门控":
    current (code.rs floor; infra probe/removal; terminal.rs ungated; zero
    `require_full_auto` call sites, V01-01).
  - `2026-07-11-tool-execution-rendering-implementation-plan.md` —
    ToolExecutionView persisted projection implemented as
    ToolExecutionRepository + Tauri sinks; "恢复历史时直接构造终态
    ToolExecutionView" matches `rebuild_index_and_recover` (Running ->
    Cancelled at boot): current.
  - `2026-07-17-surface-parity-closeout.md` — tool/terminal capability on all
    five entry classes: current at capability level (execution identical;
    PTY widget GUI-only, TUI/CLI use the user's own terminal; the GUI-only
    persistent tool-detail view is a projection, not a capability gap).
  - `src/tauri/error.rs:6-9` doc ("Commands that spawn processes ... are
    gated behind IpcAuth::require_full_auto()") is STALE: the gates have zero
    callers (desired state per AGENTS.md), but the module and doc survive —
    a doc-drift/dead-code finding (P3-01).

## Deviations

None beyond the recorded P3-01 (stale error.rs doc + dead IpcAuth module).

## Conclusion

Supports: no regression of the permission-gate removal lesson; M5 and
MASTER-PLAN sandbox/terminal claims are current; the tool-rendering plan is
implemented. One stale doc/dead module found (P3-01).

## Follow-Up

None.
