# A-TOOL-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The four required edge-case invariants hold: (1) per-mode/per-role registry
diff is explicit and pinned by tests; (2) sandbox probing has no bare fallback;
(3) the interactive terminal is gated by user action, not by agent
permission_mode; (4) large output/cancel/error fixtures exist and pass.

## Method

Static inspection of the exposure tables, sandbox code, terminal code, and
test fixtures:

```text
1. tool_exposure.rs:57-135 — group tables per InteractionMode (Chat/Task/Auto) + initial_visible_tools whitelist + disabled_tools_for_mode blacklist; tests mode_exposure_snapshots_are_stable (tool_exposure.rs:206-274) pin exact per-mode surfaces; chat_driver.rs:611-646 pin disabled sets per mode.
2. Per-role diff: infra.rs:881-965 build_writer_subagent_agent (full registry, comment "NO .readonly_tools() -> full tool set (write capability)") vs infra.rs:968-1042 build_readonly_subagent_agent (.readonly_tools()); both call subagent.set_plan_mode(true) (infra.rs:963, 1040).
3. Sandbox no-bare fallback: infra.rs:269-272 (probe), infra.rs:533-543 (remove run_code); echo-tools/src/code.rs:313 with_minimum_isolation(OsSandbox), code.rs:324 fail-closed check; echo-execution/src/sandbox/manager.rs:229-250 local_sandbox() with allow_fallback:false; panels.rs:860-918 execute_sandbox fails closed via eko_local_sandbox_available (panels.rs:791-799, 894); LocalSandbox::isolation_level/is_available (echo-execution/src/sandbox/local.rs:948-975) runs sandbox-exec/bwrap probes honestly.
4. Interactive terminal permission path: src/tauri/terminal.rs:286-297 (create_terminal has NO permission gate, explicit local-model comment), terminal.rs:310-359 (write_terminal requires per-session user consent set by confirm_terminal_consent; 64 KiB write cap; audit log), web-frontend/src/components/terminal/Terminal.tsx:128-153 (consent confirmed on the first genuine keystroke via onData, then write). Zero IpcAuth/require_full_auto call sites anywhere (V01-01).
5. Large output/cancel/error fixtures: tool_execution.rs tests complete_output_is_read_in_pages, artifact_pages_end_on_utf8_boundaries, malformed_final_journal_line_is_removed, removing_conversation_drops_summary_and_detail_indexes, preview_is_utf8_safe; chat.rs execution_projector_tests::task_runtime_tools_are_persisted_with_output_and_terminal_cleanup (start/output/completed-without-result/completed-with-result ordering, terminal cleanup).
```

## Expected Result

All four invariants hold and are test-pinned, with the exception that the
writer role's declared full-write surface is contradicted by plan_mode
(recorded as P1-01) and the GUI tool toggle has no execution effect (P2-01).

## Result

- Exit code: not-applicable (inspection)
- Duration: about 30 minutes
- Summary:
  - Per-mode diff: Chat = 16 tools, Task = 17, Auto = 19 (snapshot test); the
    whitelist is the primary gate and the disabled list is a redundant second
    gate; both are enforced by ToolVisibilityStage
    (echo-agent/src/agent/react/run/pipeline.rs:172-210) and
    snapshot.rs:224-236.
  - Per-role diff: readonly registry vs full registry are distinct; writer
    subagents carry run_code only when the OS sandbox probe passes
    (infra.rs:950); however plan_mode=true (infra.rs:963) removes all write
    tools from the LLM-visible surface and blocks them in the pipeline —
    the declared writer capability is not delivered (P1-01).
  - Sandbox: no bare fallback on any EKO code-execution path (agent run_code
    removed when probe fails; tool-level floor; panel fail-closed).
  - Terminal: no permission_mode gate; user-action consent; write bounds and
    audit log. Confirmed separate from agent run_code policy.
  - Fixtures: present for paging, UTF-8 boundaries, journal repair, cascade
    deletion, and projector terminal ordering; all pass (V04).

## Deviations

The GUI tool enable/disable toggle (state.rs:348, commands/tools.rs:33-65,
ToolsPanel.tsx:71-84) mutates only a display map consumed by get_tool_infos
(state.rs:722-747); no execution gate reads ToolState — a user-visible control
with no effect (P2-01). The GUI subagent event bridge drops lagged broadcast
events with only a warning (src/tauri/mod.rs:762-764), which can leave
projected tool records stuck "Running" until restart (P3-02).

## Conclusion

Supports: mode surfaces are explicit and stable; sandbox probing is fail-closed
with no bare fallback; the interactive terminal is separate from agent
permission policy and is user-gated; large-output/cancel/error fixtures exist
and pass. The role diff reveals the writer plan_mode defect; the toggle and
lag-drop behaviors are recorded as findings.

## Follow-Up

None.
