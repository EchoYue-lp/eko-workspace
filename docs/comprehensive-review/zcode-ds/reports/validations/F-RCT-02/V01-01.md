# F-RCT-02 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

There is exactly one authoritative ReAct loop and one terminal/loop-detection
mechanism in the non-streaming turn path; any second definition found across
either repository is classified as dead code or an adapter, never a parallel
authority.

## Method

Repository-wide grep (both `echo-agent` and `echo-agent-cli`, `--include=*.rs`,
excluding `target/`) for the following names and behaviors:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
grep -rn "run_core_loop|run_react_loop|run_stream_channel|run_chat_direct|run_direct\b|process_steps"
grep -rn "LoopDetector|loop_detector|loop_detector_config|get_loop_detector_config"
grep -rn "MaxIterationsExceeded|max_iterations"
grep -rn "execute_tool_with_policy|execute_tool_feedback|execute_tool_feedback_raw|execute_tool("
grep -rn "AgentEvent::FinalAnswer|AgentEvent::Cancelled|finalize_run|finalize_trace_run"
grep -rn "StreamMode|IterOutcome|ThinkOutcome|CompactOutcome|PrepareOutcome|LoopVerdict"
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
grep -rn "run_react_loop|run_core_loop|process_steps|LoopDetector|loop_detector|execute_tool_feedback"
grep -rn "execute_stream|chat_driver|chat(" echo-agent-app-core/src/ src/ src-tauri/
```

Plus a read of `react/structured.rs` (StructuredAgent) and `runner.rs`
(AgentRunner) to confirm they delegate rather than re-implement a loop.

## Expected Result

- `run_core_loop` (stream_channel.rs) is the single loop body; `run_react_loop`
  and `run_stream_channel` are thin drivers over it.
- `StructuredAgent` delegates to `inner.chat()`/`inner.execute()`; `AgentRunner`
  is a deprecated builder.
- No second ReAct loop, no second loop-detection runtime, no second
  tool-execution authority in the live path; `echo-agent-cli` contains no loop
  copy.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - One loop authority: `run_core_loop` at
    `src/agent/react/run/stream_channel.rs:494`; non-streaming driver
    `run_react_loop` (react_loop.rs:598) and streaming driver
    `run_stream_channel` (stream_channel.rs:35) both delegate. `run_direct`/
    `run_chat_direct` (direct.rs:9,29) are thin wrappers over `run_react_loop`.
    `StructuredAgent` (structured.rs:61-70) delegates to the `Agent` trait.
    `AgentRunner` (runner.rs) is `#[deprecated]` and composes `ReactAgentBuilder`.
  - No loop copy in `echo-agent-cli`: EKO uses only `execute_stream_*` entry
    points (`chat_driver.rs:513`, `tasks/task_runtime/executor.rs:3119-3130`);
    zero hits for `run_react_loop`/`run_core_loop`/`process_steps`/`LoopDetector`.
  - `LoopDetector`/`LoopDetectorConfig`: defined (`loop_detector.rs`), stored in
    `AgentConfig` (config.rs:176,264), settable (`config.rs:1025`) and readable
    (`config.rs:1031`), publicly exported (`react/mod.rs:65`) — but ZERO callers
    of `LoopDetector::new`/`record_tool_call`/`record_iteration`/`check`/`reset`
    in either repository. No runtime wiring (see V02, finding P2-02).
  - `process_steps` (react_loop.rs:179, `#[allow(dead_code)]` at :177): zero
    callers. Its tool-execution dependency `execute_tool_feedback_raw`
    (react_loop.rs:306,359) and `execute_tool_feedback`/`execute_tool`
    (`run/execution.rs:404,168,196`) have no live callers; the live loop uses
    `execute_tool_with_policy` (`snapshot.rs:1189`) → `ToolExecutionPipeline`
    (pipeline.rs, 15 stages). `run/approval.rs` (`tool_needs_approval`/
    `check_tool_approval`) also has zero callers; the live approval authority is
    `snapshot.rs:1152` + pipeline `PermissionStage` (pipeline.rs:330-336).
  - `AgentEvent::Cancelled` is produced only in `src/agent/subagent/executor.rs:2013`
    (subagent dispatch), never by the main loop (finding P3-03).
- Log/artifact: none

## Deviations

None (all definitions found were classified; classification detail in the task
report Layering Decision).

## Conclusion

Supports: "one unified ReAct loop, one live tool-execution authority, one live
approval authority; loop detection is defined-but-unwired; a dead parallel
tool-execution path (`process_steps`/`execution.rs`) and dead approval path
(`run/approval.rs`) exist as cleanup targets". Does not prove runtime behavior;
reachability and behavior are covered by V02/V03/V04.

## Follow-Up

V02 (reachability trace) and the task report findings P2-02/P3-01/P3-02.
