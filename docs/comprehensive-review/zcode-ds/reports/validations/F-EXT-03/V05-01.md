# F-EXT-03 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documents that make claims about the F-EXT-03 domain tools
(statistics capability split, tool features) are classified as
current/fixed/stale/regressed against current code; no claim is carried
forward without classification.

## Method

Grep + targeted read of historical documents:

```text
grep -n "research_remember\|research_recall\|web_fetch\|rag_index\|rag_search\|sql_query\|excel\|outlier\|bibtex\|pdf_fetch\|exploratory_statistics\|analyze_image" \
  docs/MASTER-PLAN.md echo-agent/README.md echo-agent/echo-tools/README.md
grep -n "rag\|web_fetch\|research\|bibtex\|sql_query\|sqlite" docs/PROJECT-ANALYSIS.md
grep -n "web_fetch\|rag\|research_remember\|sql_query\|analyze_image\|outlier" \
  docs/2026-07-12-echo-agent-peer-framework-iteration.md docs/deep-iteration-plan.md
```

Plus full read of `echo-tools/README.md` and the MASTER-PLAN statistics lines.

## Expected Result

Every relevant historical claim is classified with a current-code anchor;
drifted claims are recorded as findings or coverage notes.

## Result

- Exit code: 0 (greps completed)
- Summary of claims found and classification:
  | Document claim | Classification | Current evidence |
  |---|---|---|
  | MASTER-PLAN:109/382/840 — statistics split: `exploratory_statistics` is descriptive-only (`inference=false`), formal inference via reviewable SciPy/statsmodels/R scripts executed with `run_code` | current | `echo-tools/src/statistics.rs:118-129` (contract_version, `inference: false`, limitations array); registry test `statistics_registry_exposes_only_exploratory_summary` (`registry.rs:467-495`) |
  | MASTER-PLAN:840 — statistics feature registers only `exploratory_statistics` with "有限数值计数、均值、样本标准差、四分位数、矩偏度和超额峰度" | current | `statistics.rs:21-24` (derived Tool), `statistics.rs:101-115` output fields |
  | echo-tools/README.md — features/tools table | current with a wording drift | `lib.rs` doc table is accurate; README's "ChartTool"/"DataTool"/"DatabaseTool" are generic names for `GenerateChartTool`/data tools/`SqlQueryTool` — naming drift only, no capability claim broken |
  | memory.rs:90 comment "In production, this would persist to SQLite" | stale | no SQLite in echo-agent-cli per AGENTS.md; the tool persists nothing at all — the comment describes a future that contradicts the project's own persistence decision (F-EXT-03-P1-01) |
  | docs/PROJECT-ANALYSIS.md, 2026-07-12 iteration doc, deep-iteration-plan.md | no claims about scope tools (no matches) | n/a |
- No regressed claims found.

## Deviations

None.

## Conclusion

Supports: the only substantive historical claim about scope tools (statistics
capability split) is current; README naming drift is cosmetic; the
research-memory "will persist to SQLite" comment is a stale/aspirational
marker pointing at the P1-01 stub defect. No other historical claims exist to
validate for this task.

## Follow-Up

None.
