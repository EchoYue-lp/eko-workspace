# F-EXT-03 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Definition/duplicate search: within the F-EXT-03 scope (echo-tools data/
statistics/research/media/web/database/rag/chart/document modules), there are
no unintended same-name tool pairs, and every parallel implementation of one
semantic is enumerated with definition sites.

## Method

Repository-wide grep across `echo-agent` and `echo-agent-cli` (read-only):

```text
grep -rn "register_all_tools\|register_readonly_tools" echo-agent-cli --include="*.rs"
grep -rn "web_fetch\|WebFetchToolEnhanced\|ImageFetchTool" echo-agent echo-agent-cli --include="*.rs"
grep -rn "fn parse_page_range\|parse_page_range(" echo-agent echo-agent-cli --include="*.rs"
grep -rn "research_remember\|research_recall" echo-agent echo-agent-cli --include="*.rs"
grep -rn "rag_index\|rag_search\|RagIndexTool" echo-agent-cli --include="*.rs"
grep -rn "generate_chart\|GenerateChartTool" echo-agent-cli --include="*.rs"
grep -rn "sql_query\|list_tables\|describe_table" echo-agent-cli --include="*.rs"
grep -rn "write_excel\|excel_load\|bibtex_generate" echo-agent-cli --include="*.rs"
grep -rho 'name = "[a-z_]*"' echo-tools/src/**/*.rs + registry.rs | sort | uniq -d
```

Plus full reads of `echo-tools/src/registry.rs`, `lib.rs`, `web/`, `media/`,
`research/`, `data.rs`, `data_quality.rs`, `statistics.rs`, `chart.rs`,
`database.rs`, `rag.rs`, `pdf.rs`, `word.rs`, `text.rs`, `image.rs`,
`excel.rs`.

## Expected Result

- No two registered tools share one name inside echo-tools.
- Duplicate/parallel implementations of one semantic (if any) are identified
  with both definition sites.

## Result

- Exit code: 0 (all greps completed)
- Summary:
  - Tool-name collision search: no duplicates inside echo-tools.
  - No CLI-side re-implementations of research_remember/rag/chart/sql
    tools; the CLI references `web_fetch` in its tool policy
    (`echo-agent-cli/echo-agent-app-core/src/tool_exposure.rs:72`),
    `write_excel` in task-runner policy
    (`tasks/task_runtime/executor.rs:249`), but defines no parallel tool.
  - Parallel implementations of one semantic found:
    1. Web-fetch family: `web_fetch` (`echo-tools/src/web/fetch.rs`),
       `web_fetch_enhanced` (`echo-tools/src/media/web_fetch_enhanced.rs`,
       with `// TODO(v0.3): replace WebFetchTool with this enhanced version`,
       line 37), `image_fetch` (`echo-tools/src/media/image_fetch.rs`) and
       `pdf_fetch` (`echo-tools/src/research/pdf_fetch.rs`) — four tools all
       performing "download URL content" with divergent size limits.
    2. `parse_page_range` defined twice with different contracts:
       `echo-tools/src/pdf.rs:234` (respects `max_preview_pages`) and
       `echo-tools/src/research/pdf_fetch.rs:194` (no page-count limit).
    3. Research memory (`research_remember`/`research_recall`,
       `research/memory.rs`) coexists with the framework memory tool family
       (`remember`/`recall`/`search_memory`) under different names — no
       collision, but an overlapping "agent memory" semantic.
- Full grep output is in this report's Method; no large logs produced.

## Deviations

None. The `name = "..."` grep only covers macro-generated names in the
`#[tool(name = ...)]` form plus literal `fn name()` sites in chart/rag/web/
database/research tools; collision freedom was cross-checked against the
registration lists in `registry.rs` (the authoritative registration surface).

## Conclusion

Supports: no same-name tool collision in scope; the duplicate/parallel
implementation inventory is complete for the searched terms. Used by
F-EXT-03-P2-01 (web-fetch family), F-EXT-03-P3-06 (page-range parsers).
Does not prove behavioral equivalence or the absence of duplicates under
different names beyond the searched terms.

## Follow-Up

None.
