# F-RCT-03 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The streaming-related unit test suites pass at the reviewed commits, and the
baseline green state claimed by the static analysis holds (no currently
failing test contradicts the findings).

## Method

Four `cargo test` invocations (working directory `echo-agent`, rust toolchain
as configured by the repo, `--locked`, lib tests only, filtered by test
path):

```text
cargo test -p echo_agent --lib --locked 'react::run::stream_channel'
cargo test -p echo_agent --lib --locked 'react::run::phases'
cargo test -p echo_agent --lib --locked 'react::run::processor'
cargo test -p echo_core --lib --locked 'event_envelope'
```

## Expected Result

Each command exits 0 with all filtered tests passing (they passed during
F-RCT-02 for stream_channel and phases; event_envelope and processor were
expected green as unit-level suites).

## Result

- Exit code: 0 / 0 / 0 / 0
- Duration: 0.44s / 0.07s / 0.00s / 0.01s (test phase)
- Summary:
  - `react::run::stream_channel`: 23 passed, 0 failed, 0 ignored (424
    filtered out). Includes `stream_guard_block_yields_single_final_answer`,
    `run_core_loop_text_only_yields_final_answer`,
    `run_core_loop_tool_call_cycle_completes`, `test_run_stream_cancelled_mid_llm_call`,
    `cancellation_drains_running_tool_before_abandoning_turn`,
    `steer_during_llm_call_continues_same_turn_with_new_input`.
  - `react::run::phases`: 22 passed, 0 failed (425 filtered out). Includes
    `prepare_turn_user_prompt_submit_block_short_circuits`,
    `finalize_no_response_sends_error_and_marks_trace_failed`,
    `finalize_max_iterations_sends_error_and_marks_trace_failed`,
    `pre_compaction_flush_writes_durable_facts_when_compression_imminent`.
  - `react::run::processor`: 7 passed, 0 failed (440 filtered out).
  - `event_envelope`: 6 passed, 0 failed (282 filtered out). Includes
    `normalizes_missing_and_duplicate_terminals` and
    `trajectory_validator_reports_sequence_identity_parent_and_terminal_drift`.
- Log/artifact: none (terminal summaries only)

## Deviations

None. Note: the green suite does not contradict the findings — the suites
contain no backpressure, duplicate-terminal, conformance, cancel-terminal, or
direct-answer-error tests (see V03), so passing tests cannot validate those
invariants.

## Conclusion

Supports the "current tests green at the reviewed commits" baseline and the
coverage-gap claims in V03. Does not prove the missing invariants; those
remain statically verified (V02/V03) with fixes deferred to the roadmap.

## Follow-Up

Task report; downstream tasks may reuse these commands as pre/post-fix
regression baselines.
