# F-RCT-03 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical documentation claims about the streaming event flow can be
classified as current, fixed, stale, or regressed against the reviewed code.

## Method

Search historical documents for streaming/terminal claims and check each
anchor against current code:

```text
grep -n -i "stream|Token|流式|delta" docs/MASTER-PLAN.md docs/PROJECT-ANALYSIS.md echo-agent/AUDIT_REPORT.md
read docs/MASTER-PLAN.md :44-58 (industry reference section incl. terminal convergence)
read docs/PROJECT-ANALYSIS.md :228 (LlmUsage emission anchors)
```

## Expected Result

- MASTER-PLAN M2-style "terminal convergence" claim: classifiable against the
  current raw-stream terminal behavior and the envelope adapter.
- PROJECT-ANALYSIS:228 LlmUsage anchors: line numbers either current or stale,
  semantics either current or changed.
- No other streaming-specific historical claim requiring revalidation.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - MASTER-PLAN:44-58 industry reference: Codex's "delta 流表达，最终由
    turn/completed 收敛状态" (delta stream + single terminal convergence) is
    the stated design target. Current code: the raw streaming path does NOT
    stream content deltas (P2-01) and CAN emit a second terminal after the
    first (P2-02). Classification: **regressed (raw stream)**; partially
    contained at the adapter boundary by `envelope_event_stream` truncation
    (event_envelope.rs:174-177) and missing-terminal normalization
    (:180-191), which only masks the defect for envelope consumers.
  - PROJECT-ANALYSIS:228 "`AgentEvent::LlmUsage` 在 `stream_channel.rs:331` /
    `phases/think.rs:185` 发出": the semantic claim (LlmUsage emitted by the
    think phase and the direct-answer streaming path) is current; both line
    anchors are stale (LlmUsage emission now at think.rs:199-211 and
    stream_channel.rs:465-475; stream_channel.rs:331 is now inside
    `drain_steer_into_context`). Classification: **stale (anchors), current
    (semantics)**.
  - AUDIT_REPORT.md: no streaming-specific claims found in the searched
    region.
  - MASTER-PLAN:775 "channel 的 streaming/non-streaming 路径统一进入
    drive_chat": application-side claim; the EKO consumption path
    (chat_driver.rs:513,538) matches — current (not re-audited beyond the
    entry point).
- Log/artifact: none

## Deviations

None.

## Conclusion

Supports the Historical Claim Status table in the task report (M2 terminal
convergence regressed at the raw-stream level; PROJECT-ANALYSIS anchors
stale). No historical claim was found that contradicts the new findings.

## Follow-Up

Task report; X-EVT-01 should treat the raw-stream double-terminal fact and
the envelope truncation as the authoritative producer/adapter behavior.
