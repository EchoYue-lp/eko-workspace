# A-HITL-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents about EKO HITL/permission behavior classify cleanly as
current, fixed, stale, or regressed against current code; no document claim
used by this task survives without a code anchor.

## Method

Document-to-symbol anchor check. Documents inspected:
- `echo-agent-cli/docs/MASTER-PLAN.md` (rows :13, :67, :69, :74, :452)
- `echo-agent-cli/docs/2026-07-28-app-core-full-audit.md` (A2 section :29-40,
  :207-221, :301-331)
- `echo-agent-cli/docs/2026-07-17-surface-parity-closeout.md` (:16, :55-70,
  :109, :125)
- Root `AGENTS.md` (over-gating history: `create_terminal` /
  `connect_mcp_server` `require_full_auto` gates removed)
- `echo-agent-cli/echo-agent-app-core/src/hitl/mod.rs:1-4` (module doc)
- `echo-agent-cli/src/tauri/error.rs:1-12` (module doc)

## Expected Result

- Audit A2's fix list (shared deadline, cancel remaining futures, snapshot
  providers before await) is implemented.
- MASTER-PLAN row :69 matches the current dispatcher.
- The closeout items 2/3 (TUI free-form input auto-approval; channel agents
  with empty dispatcher reject everything) are fixed.
- AGENTS.md's claim that the `require_full_auto` gates were removed holds.
- Module docs in `hitl/mod.rs` and `error.rs` match reality.

## Result

- Exit code: not-applicable (document/code comparison)
- Duration: ~20 minutes
- Summary (per claim):
  - `2026-07-28-app-core-full-audit.md:29` A2 "Fix: shared deadline (not
    per-provider timeout), cancel remaining futures after first response,
    clone provider snapshot before await" → **fixed/current**: all three are
    implemented in `dispatcher.rs:74-137` (V03-01). The audit's migration
    proposal (dispatcher → framework `FanOutHumanLoopProvider`, :331) is a
    recommendation; MASTER-PLAN:452 keeps it in the app layer — consistent.
  - `MASTER-PLAN.md:69` "HITL snapshots providers before await, broadcasts
    concurrently under one shared deadline, and drops remaining futures
    after the first response" → **current** (dispatcher.rs:74-137), with the
    caveat that the REPL provider's blocking stdin defeats the deadline
    (P1-02; the claim holds only for non-blocking providers).
  - `2026-07-17-surface-parity-closeout.md:63` item 2 "TUI approval is
    interactive, but free-form Input is auto-approved and Selection silently
    chooses the first option" → **fixed**: TUI Input enters `input_mode` and
    waits for real text (`tui_provider.rs:178-182`); the
    `input_request_waits_for_real_text_response` and
    `selection_request_waits_for_selected_option` tests pass (V04-01);
    Selection sends `options[selected_option]` only on explicit Enter
    (`tui/events.rs:210-219`).
  - `2026-07-17-surface-parity-closeout.md:65` item 3 "Channel agents are
    created with an empty HITL dispatcher and therefore reject every
    request" → **fixed**: channels inject the shared
    `ChannelHumanLoopProvider` on every inbound message before `drive_chat`
    (`src/cli/channels.rs:141-146`); the empty dispatcher remains only as the
    pre-injection default and for background/pool runs (V02-01).
  - Root `AGENTS.md` "create_terminal / connect_mcp_server 曾加
    require_full_auto 门控 … 已移除" → **current**: no gate exists on either
    command (V03-02); `IpcAuth::require_full_auto` remains as dead API
    (P2-04).
  - `hitl/mod.rs:1-4` "routes approval/input requests to the currently
    active interface (WebSocket, TUI, REPL, Tauri)" → **stale**: the module
    contains no WebSocket or Tauri provider, and the Tauri surface never
    registers on `HitlDispatcher` (V02-01) — the GUI provider lives in
    `src/tauri/commands/chat.rs` (P2-01).
  - `error.rs:6-8` "commands that execute arbitrary code are gated behind
    `IpcAuth::require_full_auto()`" → **stale/false**: zero callers of the
    gate (V01-01) — recorded as P2-04.
  - `MASTER-PLAN.md:13` "permission models to interactive local tools" →
    consistent with the local-threat-model gating decisions (V03-02).

## Deviations

- Two stale module docs (hitl/mod.rs, error.rs) — findings P2-01/P2-04.
- The closeout item-2 fix predates this review; verified via tests, not
  re-audited behaviorally.

## Conclusion

Supports: the historical claims relevant to A-HITL-01 classify cleanly
(current: audit fix list, MASTER-PLAN:69 with the REPL caveat, AGENTS.md
over-gating removal; fixed: closeout items 2/3; stale: hitl/mod.rs and
error.rs module docs). No regressed claim found within this task's scope.

## Follow-Up

None. X-AUT-01 should reuse the "current" over-gating classification; X-SRF-01
can reuse the closeout "fixed" items.
