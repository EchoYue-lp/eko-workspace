# A-HITL-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The app-core `hitl` module compiles and its unit tests pass at the reviewed
commit; the Tauri/GUI binary compiles at the reviewed commit (baseline from
A-BOOT-01 V04-02).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked hitl
```

Feature flags: workspace default features, `--locked` per the submission gate.
The GUI compile baseline (`cargo check --no-default-features --features gui
--bin echo-agent-tauri --locked`) was executed at the same commit by
A-BOOT-01 (V04-02, exit 0) and is referenced rather than re-run (identical
commit; read-only review).

## Expected Result

- Exit code 0; the 5 `hitl` unit tests pass (3 TUI provider tests including
  cancellation cleanup, 2 channel provider tests).
- The dispatcher itself has no unit tests — this is itself an observation
  (test gap, P3-01).

## Result

- Exit code: 0
- Duration: ~1 minute (cached build, 0.85 s compile + 0.02 s test run)
- Summary:

```text
running 5 tests
test hitl::channel_provider::tests::resolves_input_from_next_message ... ok
test hitl::tui_provider::tests::input_request_waits_for_real_text_response ... ok
test hitl::tui_provider::tests::selection_request_waits_for_selected_option ... ok
test hitl::tui_provider::tests::cancelling_request_clears_pending_approval ... ok
test hitl::channel_provider::tests::selection_rejects_invalid_then_accepts_number ... ok

test result: ok. 5 passed; 0 failed; 0 ignored; 0 measured; 644 filtered out
```

- Log/artifact: none created (test output only).

## Deviations

- `HitlDispatcher` (dispatcher.rs) has zero unit tests: no coverage of
  first-response-wins, shared deadline, fail-closed all-fail, empty-provider
  reject, or cancellation-of-remaining-futures. Recorded as A-HITL-01-P3-01.
- The REPL provider has no tests (its blocking-stdin behavior is exercised
  only manually).

## Conclusion

Supports: the app-core hitl providers compile and their leaf behaviors
(publish, wait, resolve, cleanup-on-cancel) are green at the reviewed commit;
the GUI binary compiles (A-BOOT-01 baseline). Does not prove the dispatcher's
arbitration semantics (untested; V03-01 inspected them statically) or any
live end-to-end approval run (Q-E2E-01).

## Follow-Up

None. Dynamic arbitration coverage is a Q-TST-01 / Q-E2E-01 item.
