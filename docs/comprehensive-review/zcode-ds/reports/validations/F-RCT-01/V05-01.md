# F-RCT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

`plan_mode` semantics in builder/config are read-only tool filtering, with
default false, and the F-EXT-01 P1-01 evidence chain (writer subagent
`set_plan_mode(true)` colliding with the framework filter) is consistent with
the construction-time picture established in F-RCT-01.

## Method

Static inspection and cross-reference:

1. `grep -rn "plan_mode"` across both repositories
   (`echo-agent/src`, `echo-agent-cli`); list all definition and consumer
   sites.
2. Read the config default (config.rs:128,242), the runtime setter
   (mod.rs:2031-2038), snapshot propagation (snapshot.rs:152, 223, 230-236,
   264, 282-285), and execution propagation
   (src/agent/react/run/execution.rs:293, pipeline PlanModeStage
   pipeline.rs:1000-1018).
3. Verify builder absence (V01 already established zero hits in builder.rs).
4. Re-derive the F-EXT-01 P1-01 chain from source: builder-side
   `enable_tools()/readonly_tools` vs `set_plan_mode(true)` in
   `echo-agent-cli/echo-agent-app-core/src/infra.rs:963` (writer builder) and
   :1040 (read-only builder); filter at snapshot.rs:282-285.
5. Compare `plan_mode` (config field) with `permission_mode` normalization
   ("plan"→"default", config.rs:1052-1057, mod.rs:2047-2080) to confirm they
   are distinct mechanisms.

## Expected Result

plan_mode default false; read-only filtering semantics at visibility and
execution; distinct from permission_mode; F-EXT-01 P1-01 chain reproduced
without contradiction; builder exposes no plan_mode option (gap versus the
task card's V01 assumption).

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - Definition: `config.plan_mode: bool` (config.rs:128), default false
    (config.rs:242). No builder setter; runtime setter
    `ReactAgent::set_plan_mode` (mod.rs:2031-2033); read-only planning
    comment (mod.rs:2045) confirms plan_mode is the read-only mechanism.
  - Filter consumers (consistent with F-EXT-01 evidence):
    snapshot.rs:230-236 (`available` set) and :282-285 (`tools_for_llm`):
    `!plan_mode || (!is_write_tool(name) && name != "shell" && name !=
    "delete_file")`; PlanModeStage at pipeline.rs:1000-1018 blocks write
    tools at execution; execution.rs:293 carries `config.plan_mode` into
    `ToolExecutionContext`. Framework test snapshot.rs:1610-1648 pins
    plan_mode=true ⇒ read-only surface.
  - Cross-reference to F-EXT-01-P1-01: reproduced — writer builder
    (infra.rs:963) and read-only builder (infra.rs:1040) share the same
    `set_plan_mode(true)` tail; no `set_plan_mode(false)` exists in either
    repository (grep zero hits); WRITE_TOOLS drift (F-EXT-01-P2-01: shell
    special-case compensation at snapshot.rs:231-233, 283-285) observed in
    the same lines. F-RCT-01 has no contradicting evidence; the finding
    stands and its fix location (remove set_plan_mode(true) from the writer
    builder) is unaffected by construction-path facts here.
  - Distinct mechanism: `permission_mode` ("plan"/"auto" normalize to
    "default", config.rs:1052-1057, mod.rs:2047-2080) is a separate approval
    policy channel and does not touch `plan_mode`.
  - Historical classification: root MASTER-PLAN claim "plan-mode = 只读过滤"
    (as cited by F-EXT-01) remains `current` at this commit.

## Deviations

None.

## Conclusion

Supports: plan_mode default/semantics (read-only filter, false default,
runtime-only setter), the builder option gap (task-card V01 assumption
incorrect), and consistency with F-EXT-01 P1-01/P2-01. Does not add new
findings beyond the builder gap recorded in V01.

## Follow-Up

F-SUB-01 (subagent surface) and F-RCT-02 (loop behavior under plan_mode)
should consume this classification.
