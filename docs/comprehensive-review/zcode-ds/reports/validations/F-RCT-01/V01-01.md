# F-RCT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every public option of `ReactAgentBuilder` maps to a live runtime effect in the
built `AgentConfig`/`ReactAgent`, and no declared option is inert or dead; the
task card's assumption that `plan_mode` is a builder option is checked against
reality.

## Method

Static read of `src/agent/react/builder.rs` (full, 1227 lines) and
`src/agent/config.rs` (full, 1214 lines) at
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent`, plus consumer
greps for each mapped field:

1. Enumerate all builder fields (builder.rs:26-109) and all setters
   (builder.rs:185-849).
2. Trace each setter through `build()` (builder.rs:854-1065) to
   `AgentConfig` construction (builder.rs:872-943) and post-construction
   injection calls (`set_llm_client`, `add_tool`, `set_memory_store`,
   `set_guard_manager`, ...) into `ReactAgent::new_inner`
   (src/agent/react/mod.rs:322-583).
3. Grep for runtime consumers of each target field: `grep -rn
   "<field>" src/ --include="*.rs"` excluding config.rs/builder.rs/tests.
4. Verify the claimed builder option `plan_mode` (task card V01 list) exists
   or not: `grep -n "plan_mode" src/agent/react/builder.rs src/agent/config.rs`.
5. Cross-check EKO construction defaults:
   `grep -n "max_iterations" echo-agent-cli/echo-agent-app-core/src/infra.rs`.

## Expected Result

Every builder option has a reachable consumer; any option found with zero
consumers or with a default that contradicts its config counterpart is a
defect candidate. `plan_mode` is expected to exist as a builder option per the
task card.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - Mapping verified for: name, model, system_prompt, role, llm_client,
    llm_config, tools (`add_tool`, capabilities.rs:37-40), task_revision_service
    (`register_task_tools`, src/tasks.rs:18-26), enable_builtin_tools →
    `register_feature_gated_tools` (mod.rs:740-748), readonly_tools (same),
    enable_memory → `setup_memory_store` (mod.rs:750-784), enable_task →
    background-task tools (mod.rs:447-453), enable_human_in_loop
    (mod.rs:442-445), enable_subagent (validated at builder.rs:864-870;
    subagent_executor built in new_inner), register_agent_dispatch_tool
    (mod.rs:478-497), enable_cot (config.rs:240, consumed in
    `build_system_prompt` mod.rs:683-691), tool_error_feedback (snapshot.rs:139),
    tool_execution (ToolManager::new_with_config mod.rs:386; snapshot.rs:146),
    max_iterations (stream_channel.rs:521-528), run_budget (snapshot.rs:430;
    stream_channel.rs:545,619), model_profile (mod.rs:693-701;
    snapshot.rs:130-133), token_limit (ContextManager::builder mod.rs:336),
    max_tokens/temperature (snapshot.rs:137-138), response_format (llm.rs:131,
    extract.rs), max_tool_output_tokens (snapshot.rs:144), tool_output_artifacts
    (snapshot.rs:145), callbacks (snapshot.rs:147), store/set_memory_store
    (mod.rs:1075-1131), session_id/conversation_id (snapshot.rs:134-135),
    working_dir (mod.rs:365-370; execution ToolContext), project_root
    (mod.rs:371-374, 715-719), approval_provider/permission_service
    (mod.rs:1582-1609), guards (GuardManager, mod.rs:1382-1384),
    audit_logger (mod.rs:1403-1405), snapshot_policy/max_snapshots
    (mod.rs:1427-1434), circuit_breaker (mod.rs:1366-1368), sandbox_manager
    (builder.rs:1017-1020 → mod.rs:1410-1424), run_store (mod.rs:1480-1482),
    tool_execution_pipeline (mod.rs:1467-1472), prompt_template_engine
    (mod.rs:1374-1379), intervention_callbacks (snapshot.rs:258),
    react_checkpoint_interval (snapshot.rs:148), intent_router,
    state_store, visibility_horizon (builder.rs:1048-1057).
  - `plan_mode` is NOT a builder option: zero hits in builder.rs; it is a
    config field (config.rs:128, default false at config.rs:242) mutated only
    via `ReactAgent::set_plan_mode` (mod.rs:2031-2033). The task card's V01
    assumption is wrong; semantics are covered in V05.
  - Dead/inert candidates found (details in task report findings):
    - `AgentConfig::allowed_tools` (config.rs:55,478-481): only consumer is
      `add_tools` registration filtering (capabilities.rs:48-57); no filter in
      `tools_for_llm` (snapshot.rs:269-288) or any call-time check; builder
      has no allowlist option and its batch `tools()` iterates `add_tool`
      (builder.rs:976-978) which does not filter (capabilities.rs:37-40).
    - `_reasoning_effort` (config.rs:130,243): zero readers in both
      repositories.
    - Divergent defaults: builder max_iterations=10 (builder.rs:149) vs
      AgentConfig=100 (config.rs:212, documented "default: 100") vs YAML
      config=0/unlimited (src/config.rs:469); builder enable_cot=true
      (builder.rs:146) vs AgentConfig=false (config.rs:240). EKO overrides
      max_iterations explicitly (infra.rs:290).
    - `.store(store)` registers memory tools contrary to its doc
      (builder.rs:981-983 → mod.rs:1075-1111); `.with_memory_tools` leaves
      `config.enable_memory=false` (builder.rs:948-951).
    - `enable_cot()`/default true is inert when `enable_tool=false`: COT
      injection requires `enable_tool && enable_cot` (mod.rs:683-691).
  - `enable_tool=false` does not remove framework tools: `final_answer` and
    `task_create/task_update/task_list` are registered unconditionally
    (mod.rs:393-400) and `tools_for_llm` has no enable_tool filter
    (snapshot.rs:269-288).

## Deviations

None (static inspection complete; the task card's builder `plan_mode`
assumption did not hold, which is a deviation of the card from the code, not
of the execution).

## Conclusion

Supports: the option-to-runtime map is complete for builder options with live
consumers; the dead/inert candidates listed above; the plan_mode builder-option
gap. Does not prove runtime behavior of any option (loop semantics are
F-RCT-02/03 scope). Used by findings F-RCT-01-P2-01, F-RCT-01-P2-03,
F-RCT-01-P3-01, F-RCT-01-P3-02, F-RCT-01-P3-03, F-RCT-01-P3-04.

## Follow-Up

None.
