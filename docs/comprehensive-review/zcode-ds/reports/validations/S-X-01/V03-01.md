# S-X-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The adapter-boundary rulings the synthesis carries are correct at the
reviewed commits: (a) the EKO adapters over the task authority are thin and
lossless with exactly one live executor/validator/store-CAS authority and
no adapter re-owning ready-frontier/DAG-loop/retry/cancel authority;
(b) the adapter-loss defects filed by the X phase (fabricated Pending
read-back; typed cancel/timeout class collapsed by the envelope adapter)
are live at their cited anchors; (c) the permission-boundary gap
(X-AUT-01-P1-01) is a wiring omission, not a second authority.

## Method

Executed at the baseline commits (9b0e0fa / b3b2e81, clean worktrees
verified before and after). All commands are greps / bounded `sed` reads;
no source was modified, nothing was built (read-only review; the X-phase
dependency reports already establish green gates at these commits).

```text
cd echo-agent
git rev-parse HEAD                                   # 9b0e0faf...d63
git status --porcelain                               # empty
grep -rn "RuntimeDagExecutor::new" echo-orchestration/src echo-agent/src
sed -n '770,785p' echo-orchestration/src/tasks/runtime_executor.rs   # test ctx
sed -n '1400,1430p' echo-orchestration/src/tasks/executor.rs         # legacy
grep -rn "validate_task_snapshot" --include="*.rs" .
sed -n '130,145p' echo-core/src/agent/event_envelope.rs              # P1-01/02
sed -n '136,150p' src/agent/subagent/executor.rs                     # status map
sed -n '246,273p' src/state/mod.rs                                   # trait surface
sed -n '223,228p' echo-tools/src/pdf.rs                              # P1-01 INV
sed -n '726,730p' src/eval/runner.rs                                 # P1-02 INV
cd ../echo-agent-cli
grep -n "RuntimeDagExecutor::new" echo-agent-app-core/src/tasks/task_runtime/executor.rs
grep -n "impl EkoRevisionedTaskStore\|fn load\|fn compare_and_commit" \
  echo-agent-app-core/src/tasks/task_runtime/revisioned_adapter.rs
sed -n '690,700p' echo-agent-app-core/src/tasks/task_runtime/store.rs # P3-01 TSK
grep -n "permission\|Permission" echo-agent-app-core/src/infra.rs     # AUT P1-01
grep -n "set_plan_mode" echo-agent-app-core/src/infra.rs              # TOOL P1-01
sed -n '360,368p' echo-agent-app-core/src/tool_execution.rs           # TOL P2-01
sed -n '1076,1096p' echo-agent-app-core/src/state.rs                  # STA P2-01
grep -n '"message:task"' echo-agent-app-core/src/chat_driver.rs       # STA P2-02
```

## Expected Result

Exactly one production `RuntimeDagExecutor::new` call (the EKO executor.rs
construction); framework `RuntimeDagExecutor::new` sites confined to test
modules; the legacy `TaskExecutor::execute_all` construction present but
inside the production-unreachable legacy surface (F-TSK-01-P3-01);
`EkoRevisionedTaskStore` exposing only `load` + `compare_and_commit`;
the P3-01 fabricated-Pending default, the envelope normalization, the
no-delete `RuntimeStateStore` trait, the failure-less `cancel()`, the
`"message:task"` fallback, and the zero-permission-wiring in the subagent
factories all present at their cited anchors.

## Result

- Exit code: 0 for all commands; not-applicable overall (read-only
  inspection, no build)
- Summary:
  1. Single-executor authority: all framework
     `RuntimeDagExecutor::new` sites in `runtime_executor.rs` (:780, :814,
     :829, :853, :908, :942, :969) sit inside `mod tests` (starts :512);
     the :1422 construction is inside legacy `TaskExecutor::execute_all`,
     whose surface is production-unreachable per F-TSK-01-P3-01; EKO has
     exactly one production construction (`executor.rs:1645`). One live
     execution loop.
  2. Single-validator authority: `validate_task_snapshot` production call
     sites = `revisioned.rs:1008` (commit path) and `runtime_executor.rs:214`
     (safe-point revalidation) — the same `PlanValidator` service;
     `planning/validator.rs:878` is test code; :184 is the definition.
  3. Thin store adapter: `EkoRevisionedTaskStore` implements exactly
     `load` (:38) and `compare_and_commit` (:47)
     (revisioned_adapter.rs:30-47) — no frontier/DAG/retry logic in the
     adapter.
  4. Adapter-loss defects live at anchors: `store.rs:694-696`
     (`unwrap_or_else(|| EkoTaskExecution::pending(...))` — X-TSK-01-P3-01);
     `event_envelope.rs:134-140` (every raw `Err` normalized to
     `AgentEvent::Error{source: "agent_stream", ..}` — X-EVT-01-P1-01/P1-02);
     `subagent/executor.rs:138-147` (`ReactError::Other` maps to `Failed`,
     typed Timeout/Cancelled arms unreachable from the envelope path).
  5. Permission-boundary gap is a wiring omission: `grep permission`
     over `echo-agent-app-core/src/infra.rs` returns only two comments
     (worktree/consent notes), zero `build_permission_service` /
     `set_permission_service` / `set_human_loop_provider` calls in the
     subagent factories; `set_plan_mode(true)` present at :963 (writer
     builder) and :1040 (A-TOOL-01-P1-01 anchor).
  6. X-STA-01 anchors: `RuntimeStateStore` trait (state/mod.rs:246-273)
     exposes only save_node/load_nodes/update_status/get_checkpoint/
     save_checkpoint — no delete API (P1-01); `exit_workspace` resets the
     conversation store via `Persistence::base_dir()` and the runtime store
     via `create_runtime_state_store()` — divergent roots (P2-01);
     `conversation_id.unwrap_or("message:task")` at chat_driver.rs:305
     (P2-02).
  7. X-TOL-01-P2-01 anchor: `cancel(&self, owner, call_id)` has no failure
     parameter (tool_execution.rs:360-368).
  8. X-INV-01 anchors: `pdf.rs:225-227` byte slices `rest[0..4]`/`[4..6]`/
     `[6..8]`; `eval/runner.rs:728` slices `&text[pos..…]` — both live.

## Deviations

None. (The stale prunable CLI git worktree recorded by X-INV-01 V06 is an
observation from the earlier task, not a deviation here.)

## Conclusion

The synthesis's adapter rulings are supported at the reviewed commits:
one live task authority (executor + validator + store CAS) with thin
lossless EKO adapters; the two adapter-loss classes (fabricated Pending;
typed-class collapse at the envelope) are live at their anchors; the
permission-boundary gap is a wiring omission on the EKO side with the
framework's no-service fallback as the enabling mechanism. No adapter was
found re-owning ready-frontier, DAG-loop, retry, or cancel authority.

## Follow-Up

None. S-RDM-01 will consume these anchors as the regression-validation
targets for the corresponding findings.
