# S-X-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every duplicate across the X-*, F-*, A-*, and S-* report sets is either
merged under one canonical ID with backlinks (including the cross-phase
aliases the phase syntheses flagged for S-X-01 to rule on) or explicitly
classified as a non-duplicate pair (producer/consumer, verdict/root-cause,
or related-fix) so the roadmap does not double-count any defect; no
duplicate is silently carried forward.

## Method

Full read of all 10 X-* task reports, S-FW-01, and S-APP-01. Built the
census of new X-phase findings (31 total: 6 P1, 13 P2, 12 P3) from the
reports' Findings sections (IDs listed in the synthesis). For each new
finding, compared the defect mechanism, file anchors, and invariant against
(a) every other X-phase finding, (b) the canonical merge tables of S-FW-01
(49 canonical framework P1s) and S-APP-01 (25 canonical application P1s),
and (c) the cross-phase alias pairs S-FW-01 section 4 and S-APP-01 section 6
named for S-X-01. For the flagged cross-phase alias (F-EXT-01-P1-01 vs
A-TOOL-01-P1-01), re-opened the fix surface at source:
`echo-agent-cli/echo-agent-app-core/src/infra.rs:963` (`set_plan_mode(true)`
in `build_writer_subagent_agent`) — the defect's removal point is an EKO
line, so the application-side ID is the fix owner.

## Expected Result

Zero un-merged duplicates; one canonical ruling for the F/A writer-subagent
alias; a defensible classification for every same-area pair; a reconciled
P1 census that matches the reports (the zcode-ds README claims 7 new X P1s
but enumerates 6).

## Result

- Exit code: not-applicable (static census + one source re-open)
- Summary:

1. **Census (authoritative, from the reports)**: new X-phase findings =
   31 (6 P1, 13 P2, 12 P3). The P1 set: `X-EVT-01-P1-01`,
   `X-EVT-01-P1-02`, `X-AUT-01-P1-01`, `X-STA-01-P1-01`, `X-INV-01-P1-01`,
   `X-INV-01-P1-02`. **The zcode-ds README's "7 new P1 across 4 tasks" is
   an off-by-one preliminary summary — its own enumeration lists exactly
   the 6 IDs above.** Classified as superseded (same precedent as S-APP-01
   V02's treatment of the README A-phase count); README not modified per
   task rules. The synthesis and this validation use the census-derived 6.
2. **Cross-phase alias ruled**: `F-EXT-01-P1-01` ↔ `A-TOOL-01-P1-01`
   (writer subagents silently read-only). S-FW-01 kept the F ID canonical
   "for this synthesis"; S-APP-01 merged under `A-TOOL-01-P1-01`. S-X-01
   rules: **canonical = `A-TOOL-01-P1-01`** — the removal point is the EKO
   builder (`infra.rs:963`), the roadmap must execute the EKO-side fix and
   delete the F-side alias from the count. `F-EXT-01-P1-01` retained as a
   cross-phase alias backlink with its validation evidence (F-EXT-01
   V02-01/V04-02).
3. **Producer/consumer pairs (NOT duplicates; fixes must land together)**:
   - `F-HITL-01-P1-03` (framework `"*"` wildcard + scope collapse) ↔
     `A-HITL-01-P1-03` (EKO sole producer of `SessionAllTools`) — two
     layers, one contract; S-APP-01 already classifies A-HITL-01-P1-03 as
     consumer.
   - `F-RCT-03-P1-02` (no `AgentEvent::Cancelled` producer) ↔
     `X-EVT-01-P1-01` (subagent status collapse), `X-EVT-01-P3-01` (dead
     `ChatEvent::Cancelled`), `X-EVT-01-P1-02` (no timeout terminal) — one
     producer gap, three consumer consequences; X-EVT-01 filed each arm and
     S-FW-01 cluster A owns the producer arm.
   - `F-RCT-03-P1-01` (event drops under backpressure) ↔ `X-EVT-01-P2-01`
     (wire has no sequence to detect the drop) — loss + undetectability.
   - `F-OPS-01-P1-01` (cron tick never fires) ↔ `Q-E2E-01-P1-03`
     (scenario verdict: cron can never auto-start) — verdict/root-cause
     pair; the E2E P1 is evidence of the framework P1, not a new defect.
   - `Q-E2E-01-P1-02` (Task work cannot complete on any surface) ↔
     `A-TOOL-01-P1-01` — scenario verdict of the canonical writer defect.
4. **Related-fix pairs (distinct mechanisms, same or adjacent fix
   surface — keep both IDs, bundle in the roadmap)**:
   - `X-AUT-01-P1-01` (no permission wiring on subagent factories) ↔
     `A-TOOL-01-P1-01` (plan-mode visibility on the same factories,
     `infra.rs:881-1010`) — different invariants (permission boundary vs
     tool visibility); both fixes touch `build_writer_subagent_agent`/
     `build_readonly_subagent_agent` and must be coordinated (X-AUT-01
     Direction already says so).
   - `X-TSK-01-P3-01` (fabricated Pending read-back) ↔ `A-TSK-01-P2-01`
     (read path never rebuilds) — same crash window, shared fix; X-TSK-01
     filed the round-trip angle and explicitly shares the fix with
     A-TSK-01-P2-01. Not a duplicate: distinct invariants (lossless
     projection vs crash recovery).
   - `X-STA-01-P2-01` (runtime-store root divergence at exit) ↔
     `A-STATE-01-P1-01` (conversation-store root at exit) — same
     `exit_workspace` region, two store families; both fixed by the
     "unify exit_workspace store roots" direction (state.rs:1078-1084 and
     :1090-1094).
   - `X-SRF-01-P3-01` (REPL/channel steer absent) ↔ `A-SRF-04-P1-01`
     (REPL/channel cancel absent) — same adapter family and root cause
     (no retained turn handle); X-SRF-01-P3-01 explicitly says to fix with
     A-SRF-04-P1-01.
5. **Same-area pairs confirmed non-overlapping**: `X-SRF-01-P2-01` (REPL
   browser management) vs `A-INT-01-P2-01` (CLI MCP stubs) — distinct
   capabilities, same CLI-surface family; `X-PLG-01-P2-01` (MCP name-keyed
   replacement) vs `F-INT-01` MCP transport findings — distinct layers;
   `X-EVT-01-P2-02` (chat-turn terminal absent from persistence) vs
   `A-STATE-01` conversation-store lifecycle — distinct contracts;
   `X-MEM-01-P2-01/P2-02` vs `A-MEM-01` — no prior IDs for either mechanism
   (A-MEM-01 recorded the project-root divergence without a finding; the
   dual-parser and dormant-suffix defects were unfiled); `X-INV-01-P1-01/
   P1-02` are new panic sites, distinct from `F-EXT-02-P1-01`,
   `F-EXT-03-P1-03`, `Q-STA-01-P1-01`.
6. **No X-X duplicates**: all 31 new findings were checked pairwise; none
   describes the same mechanism/invariant as another X finding.

## Deviations

None (the README count discrepancy is classified, not a deviation of this
validation).

## Conclusion

The canonical duplicate map for the cross-repository review is complete:
one alias ruled (A-TOOL-01-P1-01 canonical), four producer/consumer and
four related-fix pairs classified as non-duplicates with bundling notes,
and 31/31 new X findings confirmed non-duplicate. The synthesis's canonical
P1 table (6 rows) and its overlap table are supported by this validation.

## Follow-Up

None. S-RDM-01 must consume the alias ruling and the bundle pairs so the
roadmap counts each defect exactly once.
