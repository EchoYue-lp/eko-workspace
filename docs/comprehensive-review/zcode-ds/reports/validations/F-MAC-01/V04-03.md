# F-MAC-01 / V04 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The two production derive-generated tools (`GitStatusTool`,
`ExploratoryStatisticsTool`) compile and pass the registry tests that exercise
their generated `Tool` surface (name, schema, registration).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_tools --features "git,statistics" --locked --lib registry
```

Feature flags: `git`, `statistics` (the feature gates of the two modules);
default features of echo_tools are empty. Offline.

## Expected Result

Registry test `statistics_registry_exposes_only_exploratory_summary` passes
(exit 0); the derive-generated `exploratory_statistics` tool is registered in
`register_all_tools`/`register_readonly_tools` and its name appears in the
asserted name sets.

## Result

- Exit code: 0
- Duration: ~3m 44s (polars compile included)
- Summary: `test registry::tests::statistics_registry_exposes_only_exploratory_summary ... ok; test result: ok. 1 passed; 0 failed; 0 ignored; 55 filtered out`
- The derive-generated tool compiles under the real workspace feature set and
  its generated `name()`/`parameters()`/`ToolRunner::run` path is exercised by
  registration and assertions (`echo-tools/src/registry.rs:477,491`).

## Deviations

The full echo-tools lib test suite was not run (only the `registry` filter);
other tests in the crate may exist but are out of this validation's scope.

## Conclusion

The production derive path is green under the `git`/`statistics` features.
Note the in-repo tests only exercise the derive with `echo_core` as the
resolved crate (echo-tools depends on echo_core directly); they cannot catch
the facade-only break (P1-01).

## Follow-Up

None.
