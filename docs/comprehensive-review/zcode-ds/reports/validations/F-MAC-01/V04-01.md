# F-MAC-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean (scratch fixture lives in
> /tmp, outside the repositories)

## Claim

A compile-pass fixture exercising all documented facade import paths plus the
runtime error-handling and schema behavior of the generated Tool code builds and
behaves as documented.

## Method

Scratch crate `/tmp/mac_review/ok` (edition 2021) with path dependencies on
`echo_agent`, `echo_core`, `serde`, `serde_json`, `schemars`, `futures`,
`tokio`. Source mirrors demo25 usage: `#[tool]` attribute macro, `#[callback]`,
`#[guard]`, `#[derive(Tool)]` with `risk_level`/permissions overrides, plus a
runtime `main` that calls `execute` with valid, missing, and wrong-typed
parameters and prints the JSON schema.

Commands:

```text
cd /tmp/mac_review/ok && cargo build --offline   (exit recorded)
cargo run --offline                                (exit recorded)
```

Environment: `cargo` with offline registry cache; no network used. Baseline
crate versions resolved from the shared lockfile-independent scratch lock.

## Expected Result

Build exit 0; runtime prints: `add -> 5`; missing-field error with field name
`b`; wrong-type error; `derive -> cfg:/etc`; `danger perms: [Execute, Network]`;
`add perms: []`; `risk of cfg: ReadOnly`; JSON schema with description,
nullable Option, and `required` = non-Option fields.

## Result

- Exit code: build 0 (with the `echo_core` path dependency present and `Tool`
  traits imported); run 0.
- Summary:
  - Attribute `#[tool]` through the facade compiles and executes
    (`add -> 5`).
  - Missing-field error: `Tool Error: Invalid parameter 'b': missing field
    `b`` — field extraction works for missing-field errors.
  - Wrong-type error: `Tool Error: Invalid parameter '(deserialization)':
    invalid type: string "not-a-number", expected f64` — extraction degrades
    to the placeholder `(deserialization)` for type-mismatch errors
    (supports F-MAC-01-P3-02).
  - Derive `#[derive(Tool)]` with `echo_core` present executes
    (`derive -> cfg:/etc`); permissions/risk overrides correct.
  - JSON schema: `description` from doc comments, `Option<f64>` → nullable
    `["number","null"]`, `required: ["a"]`, `title: "AddParams"`.
  - `#[callback]` and `#[guard]` impls compile through the facade.
- Log/artifact: fixture sources in `/tmp/mac_review/ok/` (scratch, outside the
  reviewed repositories).

## Deviations

First build attempt without `echo_core` failed with
`error[E0405]: cannot find trait ToolRunner in module ::echo_agent::tools`
— this is the P1 facade gap, deliberately preserved and re-verified as its own
compile-fail case (V04-02 `facade_derive`). The fixture was then given
`echo_core` to continue exercising the remaining contract.

## Conclusion

The happy-path contract holds for the attribute macro, callback, guard, and the
derive (when echo_core is a dependency): schema generation, permissions/risk
overrides, and missing-field error mapping behave as documented. The
wrong-type error-name degradation is confirmed. This report does not cover the
compile-fail diagnostics suite (V04-02) or in-repo production tests (V04-03).

## Follow-Up

None.
