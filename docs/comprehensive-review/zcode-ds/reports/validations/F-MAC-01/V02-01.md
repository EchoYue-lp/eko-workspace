# F-MAC-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Macro-generated code is reachable at runtime through the tool registry for the
derive path; the attribute-macro path is exercised by example builds; the
remaining six attribute macros are compile-only; and the `macros` feature does
not gate any macro (feature boundary is a no-op marker).

## Method

Static registration/reachability trace:

1. `grep` for `GitStatusTool` / `ExploratoryStatisticsTool` in
   `echo-tools/src/registry.rs` to locate registration sites and their
   `#[cfg(feature = ...)]` gates.
2. `grep` for `handler|compressor|permission_policy|audit_logger` usage across
   `echo-agent` (excluding `echo-macros/src`).
3. Check the root `macros` feature and its gates:
   `echo-agent/Cargo.toml:100` (`macros = []`), `src/lib.rs:115-117` (macro
   re-exports without `#[cfg]`), and count `#[cfg(feature = "macros")]` sites.
4. Check `echo-agent-cli` for macro usage.

## Expected Result

The two derive-generated tools are registered (under their feature gates) in
both `register_all_tools` and `register_readonly_tools`; the `macros` feature
gates zero code; the other six attribute macros have no production usage.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - `GitStatusTool::default()` registered at `echo-tools/src/registry.rs:49`
    (inside `register_all_tools`, `#[cfg(feature = "git")]` at :44) and
    `:263` (readonly registry, `#[cfg(feature = "git")]` at :257).
  - `ExploratoryStatisticsTool::default()` registered at
    `echo-tools/src/registry.rs:158` (`#[cfg(feature = "statistics")]` at
    :157) and `:382` (readonly registry, `:380`).
  - Registry tests assert the derive tool's presence:
    `echo-tools/src/registry.rs:477,491` (`exploratory_statistics` in
    all_names/readonly_names) — runs under `--features statistics` (V04-03
    executed it).
  - `handler`, `compressor`, `permission_policy`, `audit_logger`: zero usage
    sites in either repository (definition-only macros).
  - `macros` feature: `macros = []` at `echo-agent/Cargo.toml:100`; zero
    `#[cfg(feature = "macros")]` sites in the workspace; the 8 proc-macro
    re-exports at `src/lib.rs:115-117` are unconditional. The `macros` marker
    only drives example `required-features` (`Cargo.toml:200,301`), which is
    why `demo25_macros` is skipped in default example builds (see V04-04).
  - `echo-agent-cli`: zero macro usage; the CLI never registers macro-generated
    tools.

## Deviations

The `#[allow(dead_code)]` + "TODO(v0.3): integrate into default tool registry"
comment on `GitStatusTool` (`echo-tools/src/git.rs:29-30`) is stale — the tool
is already registered; the attribute is explained by the `git` feature gate
(without `git` the struct is unused).

## Conclusion

The derive-generated code is a live production path (compiled, registered,
tested) whenever the `git`/`statistics` features are enabled. The six other
macros are compile-only today: their behavior is verified only by docs and by
this review's fixtures. The `macros` feature boundary does not exist (marker
feature), consistent with F-FEAT-01's marker-feature audit; the consequence
specific to macros is that the showcase example is excluded from default
example builds.

## Follow-Up

None.
