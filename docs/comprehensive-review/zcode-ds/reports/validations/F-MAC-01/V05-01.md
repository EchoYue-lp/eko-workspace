# F-MAC-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents (README, echo-macros README, MASTER-PLAN, AUDIT_REPORT,
deep-iteration-plan, PROJECT-ANALYSIS) state macro facts that match current
code, or the drift is identified and classified.

## Method

Static grep of macro-related claims:

```text
grep -n "macro|TypedTool|derive|echo_macros|11 macros|prelude" \
  echo-agent/README.md echo-agent/echo-macros/README.md \
  echo-agent/AUDIT_REPORT.md docs/MASTER-PLAN.md docs/deep-iteration-plan.md \
  docs/PROJECT-ANALYSIS.md
```

plus targeted reads of the flagged regions.

## Expected Result

Every claim matches current code, or mismatches are listed with classification
(current/fixed/stale/regressed).

## Result

- Exit code: not-applicable (static)
- Summary of claims found:
  - `echo-agent/README.md:200` — "11 macros: #[tool], agent!{}, messages![],
    ...". Current count: 8 proc macros (echo-macros/src/lib.rs) + 4
    user-facing declarative macros (agent!/messages!/tool_params!/
    chat_request!, src/macros.rs) = 12. **stale** (off by one; count source:
    `grep -c` 8 proc-macro attrs in echo-macros + 6 macro_export in
    src/macros.rs, of which 2 are doc(hidden) helpers).
  - `echo-agent/README.md:1044` — "`#[tool]` | Proc | `TypedTool` from async
    fn". `TypedTool` exists nowhere in code (0 grep hits). **stale** — same
    phantom symbol already flagged by F-API-01-P2-01/P3-01; the macro
    generates an `impl Tool`, not `TypedTool`.
  - `echo-agent/README.md:266` — feature table row "`macros` | Procedural
    macros | echo-macros" implies a feature boundary; `macros = []`
    (Cargo.toml:100) gates nothing (V04-04). **stale** as a boundary claim
    (marker feature, F-FEAT-01 audit owns it).
  - `echo-agent/README.md:96` — "`cargo run --example demo25_macros # Macro
    system" — fails as written under default features (V04-04 exit 1).
    **stale** — needs `--features macros` or a `required-features` removal.
  - `echo-macros/README.md:9-24` — quickstart lists only `echo_macros` +
    `echo_core` deps and returns `-> ToolResult`; does not compile (V04-02,
    "Could not find echo_agent in dependencies"). **stale**; also describes
    `#[tool]` as generating "`TypedTool`" (line 28 table). (F-MAC-01-P3-03.)
  - `echo-macros/src/lib.rs:29-31` — "import these macros via
    `echo_agent::prelude::*`" — prelude exports no proc macros
    (src/lib.rs:137-276; F-API-01-P3-01). **stale**.
  - `docs/MASTER-PLAN.md:616` — "echo_macros 0 [tests]" — current
    (echo-macros has no test module; confirmed by repo scan).
  - `echo-agent/AUDIT_REPORT.md`, `docs/deep-iteration-plan.md`,
    `docs/PROJECT-ANALYSIS.md` — no macro claims found. **current/none**.
  - `echo-tools/src/git.rs:29-30` — code comment "TODO(v0.3): integrate into
    default tool registry once git tool suite is stabilized" — the tool is
    already registered (registry.rs:49,263). **stale** comment.

## Deviations

None.

## Conclusion

Macro-related doc drift exists at P3 level (README counts, TypedTool phantom,
quickstart, prelude guidance, demo25 command). MASTER-PLAN's zero-test claim
for echo-macros is current. No historical claim was found that would change the
V01-V04 conclusions.

## Follow-Up

None.
