# F-MAC-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean (scratch fixtures in /tmp)

## Claim

Invalid/edge-case macro inputs produce either a targeted diagnostic or a
documented compile failure, and crate-rename resolution works for both macro
families. There is no in-repo trybuild/compile-fail suite, so this scratch
execution is the only executable evidence for diagnostics.

## Method

Scratch crates in /tmp (offline, path deps): `/tmp/mac_review/fail` (echo_agent
+ echo_core deps, one bin per case) and `/tmp/mac_review/rename` (deps renamed
`ea` = echo_agent, `core2` = echo_core), and `/tmp/mac_review/quickstart`
(echo_macros + echo_core only, README quickstart verbatim). Each case:

```text
cargo check --offline --bin <case>    # exit code + first error lines recorded
```

Cases: `facade_derive` (derive through echo_agent only — the documented path);
`cf2_tool_self` (`#[tool]` fn with `&self`); `cf3_tool_generic` (generic fn);
`cf4_guard_names` (nonstandard guard param names); `cf5_derive_tuple`;
`cf6_derive_enum`; `cf7_tool_no_name`; `cf9_risk_bogus`; `renamed_attr`;
`renamed_derive`; quickstart build.

## Expected Result

- Clean targeted diagnostics: cf5, cf6, cf7, cf9 (macro rejects input with a
  message naming the rule).
- Loud compile errors, quality to be assessed: cf2 (receiver), cf3
  (generics), cf4 (param names).
- Renamed crates compile (`renamed_attr`, `renamed_derive`).
- quickstart fails (README example is broken) or passes.

## Result

- Exit codes (cargo check, `1` = compile failure, `0` = pass):
  - `facade_derive`: 1 — `error[E0405]: cannot find trait ToolRunner in
    module ::echo_agent::tools`, originating in the derive expansion
    (supports P1-01).
  - `cf2_tool_self`: **0 — compiled**. The `&self` receiver is silently
    dropped from the params struct; `self` inside the fn body binds to the
    generated unit struct (`TTool`), not the author's receiver type. Silent
    semantic divergence (supports P2-01).
  - `cf3_tool_generic`: 1 — `error[E0425]: cannot find type T in this scope`
    (generic fn's `T` leaks into the params struct field without a declaration;
    no macro diagnostic) (supports P3 generics item).
  - `cf4_guard_names`: 1 — `error[E0425]: cannot find value text in this
    scope` / `cannot find value dir` (fixed-name contract not validated;
    confusing errors) (supports P3-01).
  - `cf5_derive_tuple`: 1 — `#[derive(Tool)] only supports named fields
    (struct with { })` — targeted diagnostic.
  - `cf6_derive_enum`: 1 — `#[derive(Tool)] only supports structs` — targeted.
  - `cf7_tool_no_name`: 1 — `#[tool] requires name = "..."` — targeted.
  - `cf9_risk_bogus`: 1 — `risk_level must be ReadOnly, Standard, or
    Dangerous, got Bogus` — targeted.
  - `renamed_attr`: 0 — attribute macro resolves the renamed `ea` crate.
  - `renamed_derive`: 0 — derive resolves renamed `core2` (echo_core first).
  - quickstart: 1 — `error: Could not find echo_agent in dependencies` (the
    `#[tool]` attribute macro hard-requires `echo_agent`; README quickstart
    lists only `echo_macros` + `echo_core`; the example also returns
    `-> ToolResult` while the macro contract is `Result<ToolResult>`)
    (supports P3-03).
- Log/artifact: fixtures in `/tmp/mac_review/{fail,rename,quickstart}/`
  (scratch, outside the repositories).

## Deviations

None beyond the recorded behaviors.

## Conclusion

Targeted diagnostics exist for struct-shape/attr-validation errors (cf5-cf9);
crate-rename hygiene works for both macro families; the derive is broken
through the facade-only path (P1-01); the `&self` receiver is silently dropped
(P2-01); generics and fixed-name contracts fail loudly with
low-diagnostic-quality messages (P3); the echo-macros README quickstart does
not compile (P3-03). These results are scratch-fixture evidence; the in-repo
absence of a compile-fail suite means these diagnostics are unprotected from
regression.

## Follow-Up

None.
