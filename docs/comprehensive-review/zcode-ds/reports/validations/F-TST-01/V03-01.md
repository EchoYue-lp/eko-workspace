# F-TST-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The mock-versus-provider contract matrix shows whether each mock can express
the real wire/behavioral shapes the framework must survive, and the scripted
ordering/error fixtures that exist cover those shapes. Expected: mocks can
script multi-chunk streaming, usage shape, mid-stream errors, tool failure
classification, cancellation, and ordering; tests exercise those scripts.

## Method

Static inspection of `src/testing/{mock_llm,mock_tool,mock_agent,mock_embedder}.rs`
(full reads), the loop consumers (`phases/think.rs:95-250`,
`stream_channel.rs:362-480,494-757`, `echo-execution/src/tools.rs:618-730`,
`echo-core/src/tools/mod.rs:319-398,154-162`), and the test inventory of
`stream_channel.rs` (23 tests), `subagent/executor.rs` (MockAgent tests),
`compact.rs`, `intent/classifier.rs`, `echo-integration` (adapter tests), plus
grep of `with_delay`, `with_failure`, `then_tool_calls`, `then_reasoning_tool_call`,
`with_response_usage`, and fixture files.

## Expected Result

A matrix row for each contract dimension with a scripted-fixture existence
check.

## Result

- Exit code: not-applicable (static inspection)
- Summary — contract matrix:

| Dimension | Real provider behavior (evidence) | MockLlmClient/MockTool/MockAgent capability | Scripted fixture exists? |
|---|---|---|---|
| Content streaming | Many deltas over time; OpenAI/Anthropic wire | `chat_stream` emits exactly one `stream::once` chunk holding the whole content (mock_llm.rs:410-425) | No multi-chunk fixture possible |
| Usage shape | OpenAI: usage with final chunk; Anthropic: `message_delta.usage` = `{output_tokens}` on a separate final chunk, fails the strict `AnthropicUsage` deserializer (F-LLM-03-P1-02) | `usage` rides on the single content/tool chunk (mock_llm.rs:421,454); no usage-only final chunk shape | `react_stream_records_real_usage_in_run_trace` (stream_channel.rs:1755-1809) asserts `usage_reported: true` in the one-chunk shape; no Anthropic-shape fixture |
| Tool-call assembly | Incremental per-index deltas; Anthropic content_block_start/delta/stop interleaving (F-LLM-03-P1-01) | All tool calls in one chunk with complete args (mock_llm.rs:426-458); index assigned by enumerate, always 0..n in order | No interleaved fixture; `then_tool_calls` (mock_llm.rs:233) has zero callers (F-RCT-04-P2-01) |
| Reasoning/thinking deltas | OpenAI reasoning_content per delta; Anthropic thinking blocks (F-LLM-03-P1-04) | `chat_stream` always `reasoning_content: None` (mock_llm.rs:416-418); `then_reasoning_tool_call` is `#[cfg(test)] pub(crate)` single-chunk only (mock_llm.rs:179-204) | 2 single-chunk deepseek tests (stream_channel.rs:1470,1537) |
| Mid-stream errors | Malformed chunk dropped (F-LLM-01-P1-01); transport disconnect mid-stream | Errors only before stream start (`pop_response()?` at mock_llm.rs:410); stream yields only `Ok` chunks | None |
| Cancellation | Transport: silent end-of-stream (client.rs:251-256; F-LLM-01-P3-01) | `with_delay` + cancel → `Err(ReactError::Other("mock LLM ... cancelled"))` (mock_llm.rs:349-357,400-408) — loud error, not silent end | 1 test (`test_run_stream_cancelled_mid_llm_call`, stream_channel.rs:1972-2038) asserts only "no FinalAnswer" |
| Timeout | Provider stall → transport/batch timeout | No hang scripting (delay always completes) | None |
| Retryable tool failure | Real tools: `ToolResult::error(msg).with_failure(Transient/Timeout/Unavailable.retryable())` (shell.rs:1061,1121; code.rs:382-405; web/search.rs:181); retry gate `allows_automatic_retry` (tools.rs:687-698) | `MockTool::with_failure` → `ToolResult::error(msg)` which hardcodes `ToolFailureCategory::Permanent` (echo-core/tools/mod.rs:373-385) — never retryable | None through MockTool; echo-execution's own tests use closures (tools.rs:997,1072) |
| Tool timeout/cancel/partial side effect | Per-tool timeout → `ToolFailure::Timeout`/`PartialSideEffect` (tools.rs:686-722) | MockTool has no delay/cancel/streaming/oversized output | None; the loop cancel test hand-writes `DelayedTerminalTool` (stream_channel.rs:2041-2082) |
| Agent event stream | Real agent: Token/ThinkStart/ToolBatch/ToolCall/... + one terminal | `MockAgent::execute_stream` emits exactly one `AgentEvent::FinalAnswer` (mock_agent.rs:237-246,317-326); cancel token ignored (`_cancel`, mock_agent.rs:255,277) | Orchestration tests consume single-event streams; executor races `with_delay_ms` against dispatch-level cancel/timeout (executor.rs:2483,2535) |
| Ordering | Provider chunk order → event order; batch result order (F-RCT-04-P1-01) | No ordering scriptable (single chunk) | None |
| Empty response | Provider may return no content | Queue exhaustion → `LlmError::EmptyResponse` (mock_llm.rs:320) | 1 test (`run_core_loop_empty_llm_response_terminates_gracefully`, stream_channel.rs:1934) |
| Rate limit / network error (request level) | 429, network failure before response | `with_network_error`/`with_rate_limit_error`/`with_error` (mock_llm.rs:245-257) | Retry tests exist (react_loop tests) |

## Deviations

None; every matrix row was verified by direct code read at the cited lines.

## Conclusion

Supports: request-level scripting (success/error/429/usage-on-content) is
faithful; every streaming/wire-shape dimension (multi-chunk ordering, usage
final chunk, mid-stream errors, incremental tool calls, silent cancel) and
every tool-behavior dimension (retryable failure, timeout, cancel, partial
side effect) is unscriptable — the loop suite structurally cannot reproduce
F-LLM-01-P1-01, F-LLM-03-P1-01, F-LLM-03-P1-02, F-RCT-04-P1-01/P1-02, or
F-RCT-03-P1-02 at the loop level. These become findings P1-01, P1-02, P2-01,
P2-02, P2-03.

## Follow-Up

None.
