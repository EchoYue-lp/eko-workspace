# F-TST-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The `testing` feature and its mocks are reachable only in test/example/dev-
dependency contexts: never compiled into production builds, no feature leakage
into `echo-agent-cli` production code, and the mocks drive real loop tests in
the reviewed commits.

## Method

```text
1. grep 'feature = "testing"' in echo-agent (Cargo.toml + src + examples + tests)
2. grep echo_agent dependency declarations in echo-agent-cli (Cargo.toml,
   echo-agent-app-core/Cargo.toml)
3. grep -rn "echo_agent::testing|use echo_agent::testing" in echo-agent-cli
   (classify cfg(test) vs production)
4. read lib.rs:59-60, 274-275 (module gating and re-export)
5. read Cargo.toml required-features blocks referencing "testing" (examples)
6. read tests/react_smoke.rs header comment
7. read snapshot.rs:343 llm_client field doc
```

## Expected Result

The module compiles only under `#[cfg(any(test, feature = "testing"))]`; the
CLI enables `testing` only in dev-dependencies; no production code path in
either repository constructs a mock; loop tests in `stream_channel.rs` drive
the full ReAct loop through `MockLlmClient`.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - `echo-agent/src/lib.rs:59-60`: `#[cfg(any(test, feature = "testing"))] pub mod testing;`
    and `lib.rs:274-275` re-exports `MockAgent/FailingMockAgent/MockEmbedder/
    MockLlmClient/MockTool` under the same cfg. The only `feature = "testing"`
    consumers inside echo-agent are example manifests
    (`Cargo.toml:253-269,329,345` — demo04_subagent, demo12_resilience,
    demo39_workflow, demo43_data_tools, demo47_enterprise, demo57_data_pipeline,
    demo61_agent_factory).
  - `echo-agent-cli/echo-agent-app-core/Cargo.toml:59`: `echo-agent` with
    `default-features = false, features = ["testing"]` **only in
    dev-dependencies**; the main CLI crate (`echo-agent-cli/Cargo.toml:50`)
    depends on `echo_agent` without `testing`. Production builds therefore
    never include the mocks.
  - All `echo_agent::testing::*` references in echo-agent-cli sources
    (chat_driver.rs:764,840,911,1006,1073; agent_pool.rs:1250; unified_memory.rs:228,257;
    scheduler/runner.rs:181,188,219,246; task_runtime/executor.rs:4503,4549,5227;
    task_runtime/register.rs:164) sit inside `#[cfg(test)]` modules.
  - `tests/react_smoke.rs:1-12` documents that full-loop mock-LLM integration
    tests were deferred ("require the testing feature plus a snapshot-level
    LlmClient field (tracked separately as a larger refactor)"); the field now
    exists (`snapshot.rs:343 llm_client`) and the loop routes through it
    (`create_llm_stream` in phases/think.rs:99-103 via snapshot.llm_client).
  - Reachable mock consumers in test builds: `src/agent/react/run/stream_channel.rs`
    (23 tests via `agent_with_mock_llm`, stream_channel.rs:1072), think.rs,
    compact.rs, builder.rs, intent/classifier.rs, react/tests.rs,
    subagent/executor.rs, team modules, eval/runner.rs, and the CLI tests
    listed above.

## Deviations

None.

## Conclusion

Supports: clean feature isolation (V04 compile checks independently confirm
the cfg gates); mocks are reachable only in test/example/dev-dependency
contexts; the mocks are the exclusive driver of the framework loop-level test
suite, so their fidelity bounds the suite's validity.

## Follow-Up

None.
