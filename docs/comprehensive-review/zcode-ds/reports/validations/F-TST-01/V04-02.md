# F-TST-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Without the `testing` feature (and outside `cfg(test)`), the mock module is
excluded from the library build: `cargo check -p echo_agent
--no-default-features --locked` compiles cleanly.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo check -p echo_agent --no-default-features --locked
```

## Expected Result

Exit code 0; no reference to `echo_agent::testing` in the compiled surface.

## Result

- Exit code: 0
- Duration: ~1m21s
- Summary: `Finished \`dev\` profile` — no-default library build is green with
  the module cfg'd out. Log: /tmp/f-tst-v04-nocheck.log
- Log/artifact: /tmp/f-tst-v04-nocheck.log

## Deviations

None.

## Conclusion

Supports: the `#[cfg(any(test, feature = "testing"))]` gate is effective; the
mocks cannot leak into feature-free production builds.

## Follow-Up

None.
