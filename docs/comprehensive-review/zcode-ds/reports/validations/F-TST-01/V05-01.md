# F-TST-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documentation claims about the mock/testing infrastructure are
checked against current code; each is classified current/fixed/stale/regressed
rather than copied.

## Method

```text
1. grep -rn "mock|src/testing|MockLlmClient|testing" docs/MASTER-PLAN.md,
   docs/PROJECT-ANALYSIS.md, echo-agent/AUDIT_REPORT.md, echo-agent/README.md
2. Read echo-agent/docs/en/12-mock.md (333 lines) in full
3. ls examples/ | grep demo16 ; grep demo16 Cargo.toml
4. Read tests/react_smoke.rs header (1-12) and snapshot.rs:330-360
5. Read src/testing/mod.rs doc block (1-95) and mock_llm.rs doc (1-29)
```

## Expected Result

Each documented claim maps to a live symbol/example or is flagged.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - `echo-agent/docs/en/12-mock.md:329-333`: "See: `examples/demo16_testing.rs`",
    `cargo run --example demo16_testing` — `examples/` contains no demo16*
    file and `Cargo.toml` has no demo16 example entry → **stale** (broken
    doc reference).
  - `docs/en/12-mock.md:35`: "Error injection: easily simulate network
    failures, rate limiting, service outages" — true at the request level
    (with_network_error/with_rate_limit_error) but there is no way to inject
    a mid-stream error or malformed chunk → **stale/partial** (overstated for
    the streaming contract).
  - `src/testing/mod.rs:13-18` design principles "Zero network requests /
    Scriptable / Observable / Thread-safe" — scriptability holds only for
    request-level response queues, not for chunk streams → **stale/partial**
    (same root as P1-02).
  - `tests/react_smoke.rs:9-12`: mock-LLM full-loop integration tests "require
    the testing feature plus a snapshot-level `LlmClient` field (tracked
    separately as a larger refactor)" — the field now exists
    (`snapshot.rs:343 llm_client`, routed by `create_llm_stream`,
    think.rs:99-103) but the deferred tests still do not exist → **stale**
    (prerequisite shipped, tests never added).
  - `docs/MASTER-PLAN.md:151` acceptance "cancel-during-tool、
    cancel-during-HITL、cancel-during-subagent 场景测试" — cancel-during-tool
    exists as one test with a hand-written tool (stream_channel.rs:2041-2082);
    cancel-during-subagent is only exercised as a dispatch-level race against
    `MockAgent::with_delay_ms` (executor.rs:2483); HITL cancel fixtures and
    mock-level cancel semantics are absent → **regressed/partial** (see P2-02,
    P2-03).
  - `README.md:1168` links `docs/en/12-mock.md` — link itself valid, content
    has the stale demo16 reference above.
  - No claims about mocks in MASTER-PLAN/PROJECT-ANALYSIS/AUDIT_REPORT beyond
    the acceptance items above.

## Deviations

None.

## Conclusion

Supports: mock-related documentation has three stale/partial spots
(demo16 reference, error-injection claim, deferred full-loop tests) and the
MASTER-PLAN cancel-scenario acceptance is only partially implemented. These
become P3-02 and the historical-claim rows of the task report.

## Follow-Up

None.
