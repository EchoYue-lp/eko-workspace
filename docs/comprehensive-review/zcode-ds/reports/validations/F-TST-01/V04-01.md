# F-TST-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The `testing` feature is self-contained: `cargo check -p echo_agent
--no-default-features --features testing` compiles the mock module without any
other feature (the mocks reference only core types available in the
no-default build).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo check -p echo_agent --no-default-features --features testing --locked
```

## Expected Result

Exit code 0 with the `src/testing` module compiled standalone; any hidden
feature dependency of the mocks would fail here.

## Result

- Exit code: 0
- Duration: ~3m13s (second run, first run blocked on a build lock)
- Summary: `Finished \`dev\` profile` — the module and its re-exports compile
  with only the `testing` feature enabled. Log: /tmp/f-tst-v04-check.log
- Log/artifact: /tmp/f-tst-v04-check.log

## Deviations

None.

## Conclusion

Supports: testing-feature isolation holds — the feature gates exactly the
mock module and pulls no transitive feature or dependency. Combined with
V02 (CLI dev-dependency only), no production build can observe the mocks.

## Follow-Up

None.
