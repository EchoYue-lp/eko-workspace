# F-CTX-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical claims in `docs/MASTER-PLAN.md` (M9, Phase C) about context
budgeting, protected context, and observability either still point at current
code or are classified as stale/regressed.

## Method

Grep `docs/MASTER-PLAN.md` for context/budget/compression claims; read the M9
archive section (lines ~697-740) and Phase C (lines ~271-290); verify each
claim against the code anchors inspected in V02/V03.

## Expected Result

Every relevant claim classifiable as current, fixed, stale, or regressed.

## Result

- Exit code: not-applicable (document comparison)
- Summary:
  - M9 "provider usage、cache fingerprint、context breakdown、protected
    context 与 compression 已进入同一 durable run 诊断" — **current**:
    `RunEvent::LlmCall` carries `protected_context_tokens`,
    `protected_message_count`, `context_limit_tokens`, `context_breakdown`
    (`src/trace/mod.rs:273-299`), with real values from
    `think.rs:91-97,149-165`.
  - M9 "provider 未返回 usage 时只展示 estimate,不进入准确 totals" —
    **current**: `usage_reported` semantics in `think.rs:147`.
  - M9 "auto/manual compression 统一写入 durable ContextCompression
    timeline" — **current**: `CompressionCheckpoint`/`PrepareResult` plumbing
    (`echo-state/src/compression/mod.rs:106-115, 931-940`).
  - Phase C "限制 protected message 数量和体积" — **stale (partially)**:
    no size/count limit on protected content exists; `protected_token_estimate`
    is observability only. The only size cap is the lossy 2000-char rules
    truncation in `to_reinjection_messages` (`echo-core/src/compression.rs:385`).
  - Phase C acceptance "protected token 超预算有明确诊断" — **current**
    (observability), but the budget check itself does not trigger on
    protected overrun (V03, F-CTX-01-P2-01).
  - Phase E (user-input artifactization) — out of F-CTX-01 scope (A-INP-01).
- Log/artifact: none

## Deviations

None.

## Conclusion

Supports the historical-claim classification table in the task report. The
review found no historical claim that is regressed in the sense of "fixed then
broken"; the Phase C size-limit claim was never implemented (stale).

## Follow-Up

None.
