# F-CTX-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The context/budget/tokenizer unit test suites pass at the reviewed commits,
including CJK/mixed-language fixtures, and the crate compiles.

## Method

Five commands, executed from the repository roots (default features, `--locked`):

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_state --lib --locked compression::
cargo test -p echo_core --lib --locked tokenizer
cargo test -p echo_core --lib --locked budget
cargo test -p echo_core --lib --locked compression
cargo test -p echo_agent --lib --locked context::

cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked project::prompt
```

Environment: macOS darwin 25.5.0 arm64; cargo (workspace toolchain);
existing `target/` caches (no network fetch needed).

## Expected Result

All commands exit 0 with zero failed tests.

## Result

- Exit code: 0 for each command (6 commands total)
- Summary:
  - `echo_state compression::`: 69 passed, 0 failed (0.05s) — includes
    protected-marker survival, projection replacement, canonical
    re-injection/checkpoint, tool-pair sanitization, CJK message tests,
    verifier and horizon tests.
  - `echo_core tokenizer`: 12 passed, 0 failed (0.02s) — Heuristic CJK weights,
    Simple/Calibrated tokenizer, TokenUsageTracker cache normalization.
  - `echo_core budget`: 1 passed, 0 failed (0.00s).
  - `echo_core compression`: 10 passed, 0 failed (0.01s).
  - `echo_agent context::`: 3 passed, 0 failed (0.01s).
  - `echo-agent-app-core project::prompt`: 3 passed, 0 failed (0.02s) —
    includes `optional_modules_use_mixed_language_token_budgets_without_prompt_noise`
    (CJK module truncation within token budget).
- Log/artifact: none saved; summary output above is from stdout.

## Deviations

None.

## Conclusion

Supports: green state of the tokenizer/budget/compression/context unit suites
and the EKO PromptAssembler multilingual fixture at the baseline commits.
Does not prove behavior for the uncovered paths (real-provider calibration,
end-to-end compression under EKO runtime, window mapping at runtime) — those
remain static claims in V03.

## Follow-Up

None.
