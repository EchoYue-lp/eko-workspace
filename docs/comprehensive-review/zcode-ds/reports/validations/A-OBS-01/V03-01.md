# A-OBS-01 / V03 / Attempt 01

> Status: failed (4 invariant violations found; 1 invariant holds)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Invariant and edge-case inspection across the four required axes: (1)
event-to-emitter reachability incl. terminal mapping; (2) configuration
identity; (3) secret and content redaction; (4) failure/retry/reporting
scenarios. Each axis is a falsifiable claim about the code at the reviewed
commits.

## Method

Static inspection of: `chat_driver.rs:83-176` (observer), `chat_driver.rs:515-568`
(driver loop + finish), `webhook/emitter.rs:125-209` (emit/deliver), `webhook/events.rs`
(enum), `executor.rs:437-584` + `executor.rs:3504-3549` (terminal save_trace),
`observability/diagnostics.rs:122-197` (status aggregation), `security.rs:105-128`
(framework redaction), `trace/mod.rs:425-444` (`new_tool_call` redaction),
`event_envelope.rs:134-191` (envelope normalization, consumed as F-RCT-03
fact), `scheduler/runner.rs:111-127` (cron webhook), `config_watcher.rs:275-276`,
plus the dependency reports A-CHAT-01 / A-TSK-04 / F-OPS-01 for cross-checked
facts (envelope never yields Err; cancel ends as NoResponse/terminal-less;
framework trace stores unredacted input).

## Expected Result

- Identity: one canonical config (`AppConfig.webhooks`) -> one shared emitter
  per process -> hot reload reaches the same instance. [HOLDS]
- Redaction: no secret-bearing field (tool args, error text) leaves the process
  unredacted on any outbound/persisted observability channel. [FAILS]
- Lifecycle truthfulness: persisted trace records and webhook events agree with
  the run's real terminal status; cancel is distinguishable from failure;
  failures are reported to observers. [FAILS]
- Failure/retry/reporting: delivery failures are visible and bounded. [FAILS]
- No second diagnostics authority. [FAILS]

## Result

- Exit code: not-applicable (static inspection)
- Duration: < 5 min
- Summary:
  - Identity [HOLDS]: `WebhookEmitter::from_config` (emitter.rs:67-80) reads
    `AppConfig.webhooks.endpoints`; main.rs:107 / desktop.rs:135 build the
    single emitter after env overrides; config watcher reloads the same
    instance (config_watcher.rs:275-276); zero globals (V01-01). Minor: the
    emitter constructed inside `state.rs:469` is always overwritten
    (desktop.rs:196) — dead construction, P3.
  - Redaction [FAILS]: `WebhookTurnObserver::observe` serializes raw tool args
    (`args.to_string().chars().take(240)`, chat_driver.rs:124) and raw error
    text (`ToolError.error` :148-151, `AgentEvent::Error.message` :153-157,
    setup-failure `e.to_string()` :521-525) into `WebhookEvent` payloads
    delivered over HTTP (emitter.rs:138-174). The framework redacts the same
    data class in trace events (`new_tool_call`, trace/mod.rs:425-444;
    security.rs:105). No redaction call exists anywhere in the EKO webhook path
    (V01-01). Cross-checked with F-OPS-01-P2-01 (framework trace raw input /
    final_output / error) and F-OPS-01-P2-04 (audit raw args) — the webhook is
    the third, outbound channel with the same defect.
  - Lifecycle truthfulness [FAILS]:
    - `save_trace` maps `"paused"` to `RunStatus::Completed`
      (executor.rs:562-568 pass "paused"; match fallback `_ =>
      RunStatus::Completed` at :3521-3526). A resumable paused run is recorded
      as terminal Completed in the same store the diagnostics panel reads
      (diagnostics.rs:174-197 `aggregate_status`; panel renders `run.status`).
    - The executor `Err` branch (executor.rs:570-582) writes no save_trace
      record at all — no terminal lifecycle fact for executor faults; combined
      with the framework trace terminal gap (F-RCT-02-P2-01) a failed run can
      be absent or stuck "running" in diagnostics.
    - `save_trace` failure records carry the generic error `Some("run failed")`
      (executor.rs:3530-3533) — the actual cause is discarded
      (F-OPS-01 handoff item).
    - Webhook: `ChatCompleted` is emitted only after `FinalAnswer`
      (chat_driver.rs:158,163-175) — no misleading success on the webhook; but
      on user cancellation the envelope-normalized terminal (F-RCT-03-P1-01/
      P1-02: NoResponse or fabricated terminal-less error) is observed as an
      `AgentEvent::Error` payload and emitted as `WebhookEvent::AgentError`
      (:153-157), and the enum (events.rs:9-33) has no cancelled/failed chat
      variant — external consumers see a phantom error per cancel and cannot
      distinguish cancel from failure. Same root cause as A-CHAT-01-P1-01,
      surfaced on the webhook channel.
    - Cron: `CronTaskCompleted` fires only on `Ok` after a real Completed
      status (runner.rs:111-122; executor.rs:3895-3928); failed cron runs emit
      no webhook event at all (runner.rs:124-127).
  - Failure/retry/reporting [FAILS]: one retry after 2s, then only
    `tracing::warn` (emitter.rs:165-171, 204-207); deliveries run in detached
    `tokio::spawn` tasks (:133,164) dropped silently at process exit; no
    in-app delivery status or UI feedback — the same silent-failure shape the
    emitter doc (emitter.rs:10-12) describes for the old global emitter.
  - Second authority [FAILS]: `save_trace` writes `parent_run_id: None` records
    keyed by the product run id (executor.rs:3512-3527), while the framework
    trace run for the same agent invocation is parented to the run id
    (stream_channel.rs:100-109); `list_diagnostic_runs` groups by
    `parent_run_id` (diagnostics.rs:21-27), so every task run appears twice —
    once as the real parent group, once as an event-less standalone record —
    contradicting MASTER-PLAN:713 ("EKO 以 framework RunStore 为唯一诊断事实").

## Deviations

None. All claims are static; the exact fabricated webhook message on cancel
depends on the envelope behavior verified in F-RCT-03 (not re-run here).

## Conclusion

Supports: configuration identity holds; four invariant violations found —
unredacted outbound webhook payloads (P1), paused-as-completed and missing
failure records in the trace surface (P1), cancel reported as webhook
agent_error with no terminal event type (P1), cron failure invisibility (P2),
duplicate trace authority (P2), log-only delivery failure reporting (P3), dead
emitter construction (P3). Does not prove test status (V04-01..04).

## Follow-Up

None.
