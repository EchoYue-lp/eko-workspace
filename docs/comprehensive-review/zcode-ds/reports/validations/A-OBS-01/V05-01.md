# A-OBS-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents describing the EKO webhook/diagnostics/observability
design agree with the current code at the reviewed commits (drift check).

## Method

Grep + spot-read of the historical claims:

```text
grep -n "webhook|observab|诊断|diagnostic|trace|Trace" docs/MASTER-PLAN.md
grep -n "webhook|Webhook|save_trace|trace" echo-agent-cli/docs/2026-07-28-app-core-full-audit.md
grep -n "webhook|Webhook" docs/PROJECT-ANALYSIS.md
```

Claims checked against code anchors in V02-01/V03-01.

## Expected Result

Every historical claim classified as current/fixed/stale/regressed with code
evidence; no claim used without classification (REPORTING.md).

## Result

- Exit code: not-applicable (static doc-vs-code comparison)
- Duration: < 5 min
- Summary of classifications:
  - MASTER-PLAN:379 "M9 已完成 … 同一 durable run 诊断;GUI/TUI/CLI 共用
    DTO/formatter" — **current** (observability/diagnostics.rs + panels.rs:1134 +
    tui/events.rs:4115 share the same app-core functions).
  - MASTER-PLAN:713 "EKO 以 framework `RunStore` 为唯一诊断事实,按 parent run
    聚合" — **regressed**: `save_trace` (executor.rs:3504) writes parentless
    parallel records into the same store → P2-01.
  - MASTER-PLAN:709 "每次 Agent invocation 使用唯一 trace_run_id;TaskRuntime
    run_id 只作为 parent correlation" — **regressed**: save_trace uses the
    product run id as the trace record's own run id with `parent_run_id: None`
    (executor.rs:3512-3519) → P2-01.
  - MASTER-PLAN:721 "业务 run 与 trace invocation 不再复用 identity" — **current**
    for the framework path (stream_channel.rs:100-109 parents by run_id);
    save_trace's identity reuse is the P2-01 exception above.
  - MASTER-PLAN:771 "TaskRuntime task-local trace 与 framework external trace
    都桥接到同一交互事件;cron/background 继续以 append-only RuntimeTaskEvent 为
    权威" — **current** (ExecEvent bridge, chat.rs emit_execution_event);
    webhook failure visibility for cron is the separate P2-02 gap.
  - MASTER-PLAN:605 "GUI 增加完整日志打开入口" — **current** (log file target
    exists; delivery failures land in logs only — P3-01).
  - app-core audit doc A1 (2026-07-28): "delete global singleton, unify
    AppState.webhook.emitter with real emit calls" — **fixed** (V01-01: global
    gone; V02-01: one shared emitter + live emit calls on all surfaces).
  - PROJECT-ANALYSIS:244 "飞书 webhook 仅处理 text" — **current** (Feishu
    channel, distinct from WebhookEmitter; out of scope).

## Deviations

None.

## Conclusion

Supports: M9's shared-diagnostics claim and the A1 fix are current; the
"single diagnostic authority" claims (MASTER-PLAN:713/709) are regressed by
`save_trace` at the reviewed commits. Does not cover other review tracks' doc
claims.

## Follow-Up

None.
