# S-FW-01 / V02 / Attempt 01 — Duplicate and contradictory finding reconciliation

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The synthesis's duplicate-merge decisions are correct (each merge is a genuine
same-semantic duplicate, canonical ID chosen with backlinks retained), and any
cross-report tension in scope is either resolved by reopening the smallest
relevant validation or explicitly preserved as an open question — no tension is
silently erased.

## Method

```text
1. Collect every explicit cross-reference between findings made inside the
   F/Q reports themselves (grep for 'cross-ref', 'canonical', 'same class',
   'same defect', 'independently confirmed', 'not re-filed', 're-verified').
2. For each candidate duplicate pair, compare: same root-cause mechanism,
   same code authority, same fix surface (report text), and the cross-reference
   language used by the owning reports.
3. Classify: MERGE (canonical + alias), FAMILY (same class, different fix
   surface — keep both, link), or DISTINCT.
4. Check for direct contradictions: pairs of reports asserting mutually
   exclusive facts about the same anchor (same file:line, opposite claims).
5. Verify the canonical choice matches the reports' own designation where one
   exists (e.g. "canonical ID" wording).
```

## Expected Result

- At least the explicitly designated duplicates are merged: F-RCT-05-P1-03 into
  F-SKL-01-P1-02 (the report text names "F-SKL-01-P1-02 (canonical ID)"),
  F-FEAT-01-P2-01 / F-API-01-P2-02 (same always-on files/shell defect),
  F-RCT-02-P2-04 / F-RCT-03-P2-02 (Stop-hook continuation), and the cross-phase
  F-EXT-01-P1-01 / A-TOOL-01-P1-01 alias.
- No merged pair combines findings with different fix surfaces or different
  code authorities.
- The Q-FW-01 / Q-FW-02-V14 green-vs-red tension, if real, is resolved by
  reopening the smallest validation (the doctest command), and every
  unresolved tension is preserved as an open question in the synthesis.

## Result

- Exit code: 0
- Summary of the reconciliation audit (all candidates found, with verdicts):

  | Candidate | Reports' own wording | Verdict |
  |---|---|---|
  | F-SKL-01-P1-02 vs F-RCT-05-P1-03 | F-RCT-05-P1-03: "independent confirmation of F-SKL-01-P1-02 … F-SKL-01-P1-02 (canonical ID); fix belongs with it" | MERGE — canonical F-SKL-01-P1-02, alias F-RCT-05-P1-03 (backlink to F-RCT-05 V02-01 evidence) |
  | F-FEAT-01-P2-01 vs F-API-01-P2-02 | Q-FW-02: "canonical F-FEAT-01-P2-01 / F-API-01-P2-02; re-verified, not re-filed"; both describe echo_execution `default = ["files","shell"]` + ungated facade re-exports | MERGE — canonical F-FEAT-01-P2-01 (feature-topology owns the fix), alias F-API-01-P2-02 |
  | F-RCT-02-P2-04 vs F-RCT-03-P2-02 | F-RCT-03: "streaming-side re-verification of F-RCT-02-P2-04, … independently confirmed rather than copied" | MERGE — canonical F-RCT-02-P2-04; F-RCT-03-P2-02 retained as evidence-extending alias (envelope truncation arm); fixes must land together |
  | F-EXT-01-P1-01 vs A-TOOL-01-P1-01 | task card example names this pair; F-EXT-01-P1-01 owns the framework-facing chain (`plan_mode` filter vs writer builder) | ALIAS — canonical F-EXT-01-P1-01 in this synthesis; A-side copy reconciled in S-APP-01/S-X-01 (out of scope here) |
  | F-EXT-01-P1-02 vs F-SKL-01-P3-04 vs F-MEM-02-P3-02 vs F-MEM-01-P3-03 | F-SKL-01-P3-04: "same class as F-EXT-01-P1-02, skill-specific entry point"; F-MEM-02-P3-02/F-MEM-01-P3-03: same silent no-op/overwrite class | FAMILY — one shared root cause (silent overwrite / no-op trait methods) but distinct registration entry points and fix surfaces; no merge |
  | F-SUB-01-P1-01 vs F-RCT-01-P2-01 | F-SUB-01: "same defect class as F-RCT-01-P2-01" (declared tool restriction never enforced) | FAMILY — different fields, different consumers; keep both, link |
  | F-EXT-01-P2-01 vs F-EXT-03-P1-02 | F-EXT-03: "the readonly subset and WRITE_TOOLS must derive from one write-classification authority" | FAMILY — one authority gap, two distinct defects (plan-mode filter drift vs readonly-surface write tools); keep both |
  | F-LLM-01-P1-01 vs F-LLM-03-P2-01 | F-LLM-03-P2-01: "same defect class as F-LLM-01-P1-01 on a separate code path"; Q-FLT-01-P2-01 extends the class to parseable-but-ignored error events | FAMILY — separate code paths; no merge (F-LLM-01-P1-01 fix does not cover the inline parser) |
  | F-CMP-01-P1-03 vs F-CTX-01-P2-02 vs F-RCT-01-P2-02 | F-CMP-01: "independent cross-verification … the finding ID and ownership stay with F-CTX-01-P2-02"; F-RCT-01-P2-02 = construction-time duplication arm | FAMILY — arms of one canonical-reinjection authority problem with separate fix surfaces; cross-linked, not merged |
  | F-RCT-03-P1-02 vs F-RCT-04-P1-02 | distinct anchors (cancel wrapper bypass vs batch exit arms) | DISTINCT |
  | F-REL-01-P2-01 vs F-EXT-01-P3-04 vs F-INT-01-P1-02 (retry authority) | F-EXT-01-P3-04: "F-REL-01's P2-01 already flags the same class"; F-INT-01: "same class as F-REL-01-P2-01" | FAMILY — one unification direction (`echo_core::retry` as single authority); F-INT-01-P1-02 additionally violates the idempotency gate (P1), kept distinct |
  | F-SUB-02-P1-01/P1-02 vs F-MAG-01-P1-01 (detached execution) | F-MAG-01: "same defect class on a different path" | FAMILY — keep both; one shared fix theme (extend cancellation ownership to every spawn site) |
  | F-RCT-02-P1-01 vs F-RCT-03-P2-04 / F-RCT-02-P2-01 (trace finalization) | cross-referenced, different anchors | DISTINCT |
  | F-TST-01-P1-01 vs F-LLM-03-P1-02 | F-TST-01-P1-01: mock shape "hides F-LLM-03-P1-02" | FAMILY — mock-fidelity finding is a distinct defect (test infra) with its own fix; not merged |
  | F-INT-01-P1-01 (HTTP 202) vs F-INT-02-P1-01 (LSP hang) | — | DISTINCT |

- Contradiction scan: no pair of in-scope reports asserts mutually exclusive
  facts about the same anchor. The one historical tension found — B-ARCH-01's
  "13 marker features are empty no-ops" vs F-FEAT-01's refined "7 gate real
  code, 6 pure no-ops" — was resolved inside F-FEAT-01 (Historical Claim
  Status: "partially stale (refined)", with V01/V02 census evidence) before
  this synthesis; it is a refinement with evidence, not a live contradiction.
  No validation needed reopening.
- Q-FW-01 vs Q-FW-02 V14 (gate green vs doctest phase red): **reopened and
  resolved, not a contradiction** —
  1. Rerun of the smallest relevant validation at the same commit
     (`cargo test --doc -p echo_agent --all-features --locked`, echo-agent
     @ 9b0e0fa, log `/tmp/sfw01_doctest.log`): exit 101,
     `test result: FAILED. 81 passed; 1 failed; 25 ignored`, failing doctest
     `src/testing/mod.rs - testing (line 24)` — Q-FW-02 V14 confirmed correct.
  2. Q-FW-01's exit 0 confirmed correct for its literal command: the preserved
     artifact `/tmp/qfw01_v04_test.log` contains zero "Doc-tests" lines;
     Cargo's documented `--all-targets` semantics exclude doc tests.
     Q-FW-01 V04's note "Doctests included in the run" is factually wrong and
     needs a dated Correction by its owner (flagged for S-QA-01).
  3. Resolution: different scopes, both exit codes truthful; the literal
     AGENTS.md gate has a doctest blind spot — recorded as open question 11 in
     the synthesis (S-FW-01 V02-01 evidence + rerun).

## Deviations

None. The synthesis's section 4 merge map matches this audit row for row.

## Conclusion

This validation supports: the five merge/alias decisions in the synthesis are
all genuine same-semantic duplicates with the canonical ID matching the owning
reports' own designations and backlinks retained; the FAMILY/DISTINCT
classifications avoid over-merging (no fix-surface loss); the only cross-report
tension found in scope (Q-FW-01 green gate vs Q-FW-02 red doctest phase) was
reopened at the smallest validation and resolved as a scope difference (the
literal gate excludes doctests) with a gate blind spot preserved as open
question 11 — nothing was silently erased. It does not prove absence of
duplicates across the A*/X* phases (S-APP-01/S-X-01 scope).

## Follow-Up

None for S-FW-01. S-X-01 should extend this audit to the A*/X* reports
(notably the F-EXT-01-P1-01 / A-TOOL-01-P1-01 alias and the
Q-E2E-01 / Q-FLT-01 / X-TOL-01 reconfirmations).
