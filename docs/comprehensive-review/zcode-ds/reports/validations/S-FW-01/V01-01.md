# S-FW-01 / V01 / Attempt 01 — Coverage of framework task catalog by the synthesis

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories; `git status --porcelain` empty)

## Claim

The framework-review synthesis (`zcode-ds/reports/synthesis/framework-review.md`)
references every framework-phase task report that its task card declares as
dependencies — all 38 F-* reports plus Q-FW-01, Q-FW-02, Q-STA-01, and
Q-FLT-01 — and does not silently drop any task's findings.

## Method

```text
1. List the F-* task catalog from docs/comprehensive-review/TASKS.md (Phase F section).
2. List the actual F-* report files under docs/comprehensive-review/zcode-ds/reports/tasks/.
3. Diff the two sets (catalog vs reports).
4. List the Q-* reports relevant to S-FW-01 (Q-FW-01, Q-FW-02, Q-STA-01, Q-FLT-01) per the task card "Dependencies: all F-*, Q-FW-*, relevant Q-STA-01 and fault reports".
5. Grep the synthesis report for each task ID (as citation or table row source) and verify at least one finding or verdict from every task is represented.
6. Count P1 findings per report (`grep -oE -- '### <TASK>-P1-[0-9]+:'`) and compare with the synthesis's canonical count and layer distribution.
```

Commands executed:

```text
ls docs/comprehensive-review/zcode-ds/reports/tasks/ | grep -cE '^F-'
for f in docs/comprehensive-review/zcode-ds/reports/tasks/F-*.md \
         docs/comprehensive-review/zcode-ds/reports/tasks/Q-FW-01.md \
         docs/comprehensive-review/zcode-ds/reports/tasks/Q-FW-02.md \
         docs/comprehensive-review/zcode-ds/reports/tasks/Q-STA-01.md \
         docs/comprehensive-review/zcode-ds/reports/tasks/Q-FLT-01.md; do
  grep -oE -- "### [A-Z0-9-]+-P1-[0-9]+:" "$f" | wc -l
done
```

## Expected Result

- 38 F-* report files exist and match the 38 F-* catalog entries in TASKS.md.
- The four Q-* reports above exist.
- Every task ID appears in the synthesis (directly or via its cluster table),
  and the sum of raw P1 findings across the 29 P1-bearing reports equals the
  synthesis's raw count of 50, with 49 after the declared F-RCT-05-P1-03 merge.

## Result

- Exit code: 0
- Summary:
  - Catalog vs reports: TASKS.md Phase F lists exactly 38 tasks
    (F-CORE-01, F-API-01, F-FEAT-01, F-REL-01, F-MAC-01, F-LLM-01..03,
    F-RCT-01..05, F-CTX-01, F-MEM-01/02, F-CMP-01, F-TSK-01..03, F-SUB-01/02,
    F-MAG-01, F-HITL-01, F-EXT-01..03, F-SKL-01, F-PLG-01, F-INT-01/02,
    F-WFL-01, F-INTENT-01, F-NBK-01, F-OPS-01, F-EVO-01, F-TST-01, F-SEC-01);
    `reports/tasks/` contains exactly 38 `F-*.md` files with the same IDs —
    full 1:1 coverage, no missing or extra F reports.
  - Q-* scope: Q-FW-01, Q-FW-02, Q-STA-01, Q-FLT-01 all exist and are consumed
    by the synthesis (Q-FW-01 as the green-gate baseline in the health
    verdict; Q-FW-02's P2-01/P2-02 as gate-adjacent findings; Q-STA-01-P1-01 as
    a canonical P1 row and the two new panic sites in Family 5; Q-FLT-01-P1-01/
    P1-02 as canonical P1 rows and Family-1 evidence). Q-FLT-02, Q-E2E-01, and
    Q-TST-01 are not S-FW-01 dependencies per the task card and were not
    consumed (their canonical IDs are referenced only as follow-up targets).
  - P1 census (raw, per report, `### <id>-P1-<n>:`): F-CMP-01=3, F-CTX-01=1,
    F-EXT-01=2, F-EXT-02=1, F-EXT-03=3, F-HITL-01=3, F-INT-01=2, F-INT-02=3,
    F-INTENT-01=1, F-LLM-01=1, F-LLM-02=1, F-LLM-03=4, F-MAC-01=1, F-MAG-01=1,
    F-MEM-01=1, F-OPS-01=1, F-RCT-02=1, F-RCT-03=2, F-RCT-04=2, F-RCT-05=3,
    F-REL-01=1, F-SKL-01=2, F-SUB-01=1, F-SUB-02=2, F-TSK-02=1, F-TST-01=2,
    F-WFL-01=1, Q-STA-01=1, Q-FLT-01=2 → total 50 raw P1 findings across 29
    reports; 11 F tasks have no P1 (F-API-01, F-CORE-01, F-EVO-01, F-FEAT-01,
    F-MEM-02, F-NBK-01, F-PLG-01, F-RCT-01, F-SEC-01, F-TSK-01, F-TSK-03).
    The synthesis's section 2 table contains one row per raw P1 except the
    declared merge (F-RCT-05-P1-03 folded into F-SKL-01-P1-02, both rows
    retained as canonical + alias), i.e. 49 canonical rows; the layer
    distribution (framework 40 / adapter 7 / application 2) was derived from
    the owning reports' declared layers and re-checked against the table rows.
  - Zero P0 findings in the whole phase (no `-P0-` IDs in any report) — matches
    the synthesis statement.
- Log/artifact: none created (read-only greps).

## Deviations

None. The only bookkeeping judgment: F-RCT-05-P1-03 is counted once (as alias
of F-SKL-01-P1-02), which the synthesis documents; the P1 census above is the
raw pre-merge count.

## Conclusion

This validation supports: the synthesis covers the full declared dependency
set with no missing task, its canonical P1 count (49) and layer distribution
are derivable from the underlying reports, and no P0 exists. It does not
validate the correctness of individual findings (owned by the F/Q reports and
their own validations) nor the A*/X* phase coverage (S-APP-01/S-X-01).

## Follow-Up

None. Re-run this census if a new F/Q report is added to the track or if the
task catalog changes.
