# X-STA-01 / V01 / Attempt 01

> Status: passed (defects found, recorded in task report)
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63 (baseline 9b0e0fa)
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5 (baseline b3b2e81)
> Worktree state: clean (both repositories, verified via `git status --porcelain` before and after)

## Claim

One falsifiable identity table exists: for conversation, snapshot/checkpoint,
task, Subagent, artifact, and frontend projections, the identity source, its
generation site, and its durable on-disk location can be stated with current
code anchors, and no identity class has a second generator or a
restart-instability (duplication or stale overwrite) beyond those already
documented.

## Method

```text
1. cd echo-agent-cli
2. grep -rn "conversation_id" echo-agent-app-core/src/chat_driver.rs src/main.rs src/tui/mod.rs src/tui/events.rs
3. sed -n '120,165p' src/main.rs                # CLI conversation id selection
4. sed -n '88,100p' web-frontend/src/stores/conversationStore.ts   # generateId
5. grep -rn "conversation_id" src/tauri/commands/conversations.rs  # create/update
6. sed -n '40,64p' /echo-agent/src/state/file.rs                   # checkpoint paths
7. sed -n '1246,1266p' echo-agent-app-core/src/infra.rs             # runtime state store root
8. sed -n '905,930p' echo-agent-app-core/src/state.rs               # workspace-switch store swaps
9. sed -n '1078,1095p' echo-agent-app-core/src/state.rs             # exit_workspace store swaps
10. grep -rn "Uuid::new_v4" echo-agent-app-core/src/tasks/task_runtime/{task_tools,revisioned_adapter,executor}.rs
11. sed -n '215,225p' /echo-agent/echo-orchestration/src/tasks/runtime.rs   # execution_id format
12. grep -rn "subagent_run_id" echo-agent-app-core/src/tasks/task_runtime/{executor,types}.rs
13. sed -n '191,247p' echo-agent-app-core/src/tool_execution.rs              # detail_ref
14. grep -rn "executionIdentity\|subagentRunStoreKey" web-frontend/src/stores/{toolExecutionStore,subagentRunStore}.ts
15. sed -n '157,166p' web-frontend/src/stores/subagentRunStore.ts            # key format
16. sed -n '400,441p' web-frontend/src/stores/subagentRunStore.ts            # executionAttempt / latest selector
17. sed -n '350,380p' /echo-agent/echo-state/src/memory/file_conversation.rs # message id counter
```

## Expected Result

Each identity class maps to exactly one generator and one persisted location;
any divergence is documented with anchors.

## Result

- Exit code: not-applicable (static inspection)
- Duration: n/a
- Summary: identity table established (see table below). Three
  restart-identity defects newly confirmed, plus the already-documented set.

| Identity | Generator / source | Persistence location | Restart continuity |
|---|---|---|---|
| Conversation id (GUI) | frontend `generateId()` = `conv-{Date.now()}-{rand}` (web-frontend/src/stores/conversationStore.ts:93-95), sent to `save_conversation` → `create_conversation`/`update_conversation` (src/tauri/commands/conversations.rs:409-436) | `<base>/conversations/<safe_id>.json` + `_meta.json` counter (framework `FileConversationStore`, echo-state/src/memory/file_conversation.rs:70-75, 350-380) | stable across restart; record keyed by id |
| Conversation id (CLI) | `--resume <id>` / `--continue` (last conversation) / fresh `Uuid::new_v4()` (src/main.rs:128-153) | same store | opt-in continuity only; fresh id per process otherwise |
| Conversation id (TUI) | fresh uuid per process (main.rs:153) on the framework agent; `app.conversation_id = None` at boot (src/tui/mod.rs:855), set only by `/resume` (mod.rs:1959), `/fork` (events.rs:4910), `/clear` (events.rs:4790) | same store | opt-in continuity; per-turn transcript saved under the agent's per-process id while TUI state stays None |
| Message id | `_meta.json` counter, ids reused when present (file_conversation.rs:350-380) | `_meta.json` | stable; duplicate ids only cross-process (A-STATE-01-P2-02) |
| Snapshot/checkpoint | keyed by conversation id; `~/.eko/runtime_state/<safe_id>/checkpoint.json` + `nodes.json` (echo-agent/src/state/file.rs:53-64; root infra.rs:1246-1247); workspace scope swaps to `{ws}/.eko/sessions/runtime_state/` (state.rs:921-928); exit resets to global (state.rs:1090-1094) | single-row upsert per conversation (file.rs:170-184, "single-row upsert" file.rs:5) | single copy; no history; corrupt → warn+reset+overwrite (F-RCT-05-P3-01); poisoned checkpoint wipes context (F-RCT-05-P1-01); NOT deleted on conversation deletion (X-STA-01-P1-01) |
| Task run | `run_id = Uuid::new_v4()` (task_tools.rs:951; executor.rs:3581); `plan_id = plan_{uuid}` (revisioned_adapter.rs:252); revision monotonic per plan; attempt = retry_count+1 (store.rs:1010-1026) | `events.jsonl` ledger + `plan.json`/`run-state.json` projections (file_shadow.rs) | durable; torn tail bricks the run (A-TSK-01-P1-01); pause-in-wave → permanent cancel (A-TSK-04-P1-01); mid-wave fault orphans claims until restart (A-TSK-04-P1-02) |
| Execution identity | `{run_id}:{task_id}:{plan_revision}:{attempt}` (echo-orchestration/src/tasks/runtime.rs:221-223) | in `TaskStarted` event payload → events.jsonl | survives rebuild (event_rebuild.rs:229-231) |
| Subagent run | `subagent_run_id` = execution id (executor.rs:174-180, types.rs:1654-1696); durable events `subagent_assigned`/`subagent_released` | TaskRuntime events.jsonl only | TaskRuntime-backed runs restore on reload; inline (non-TaskRuntime) subagents realtime-only (A-FE-02-P3-02) |
| Tool execution artifact | `detail_ref = Uuid::new_v4()` per `start()` (tool_execution.rs:200) | `<scope>/details/{detail_ref}.json` + `.jsonl` (tool_execution.rs:202-206) | two producers allocate two detail_refs for one logical call (A-FE-02-P2-01 / A-SRF-02-P2-01) |
| Task artifact | `ArtifactProduced` projection has zero production writers (A-TSK-06-P2-01) | n/a (always empty) | dead end-to-end |
| Uploads | `{uuid}_{name}` (attachments.rs:150-172) | `~/.eko/uploads` or `{ws}/.eko/uploads` (attachments.rs:57-63) | no retention/cleanup (A-INP-01-P3-01) |
| Frontend tool rows | hydration key `(owner+run_id, call_id)` (toolExecutionStore.ts:46-48); live ingest key = wire `id` (toolExecutionStore.ts:206-217) | in-memory store, hydrated from `list_tool_executions` | dedupe on reload; live path duplicates + can regress terminal rows (A-FE-02-P2-01) |
| Frontend subagent rows | `{runId}\0{subagent_run_id}` (subagentRunStore.ts:157-159); latest-attempt selector parses only trailing `:attempt` (subagentRunStore.ts:407-414) | in-memory store; TaskRuntime replay adapter (subagentRunStore.ts:330-405) | revision-blind selector can show superseded attempt (A-FE-02-P2-02) |
| Task-mode run grouping key | `conversation_id.unwrap_or("message:task")` (chat_driver.rs:305) | events.jsonl run records | fresh-TUI (no /resume) task runs share the fixed `"message:task"` bucket across processes (X-STA-01-P2-02) |

## Deviations

None relative to the claim; deviations from the expected invariants are
recorded as findings X-STA-01-P1-01, X-STA-01-P2-01, X-STA-01-P2-02 in the task
report.

## Conclusion

The identity table is established with current-code anchors for all six
identity classes. Identity generation is single-sourced per class; the
restart defects concentrate in (a) deletion/retention scope, (b) store-root
consistency across the workspace lifecycle, and (c) frontend key/selector
semantics — each detailed in the task report. This validation supports the
task report's matrix; it does not by itself prove runtime behavior (see V02).

## Follow-Up

V02 (crash-point recovery matrix) and V04 (retention/deletion cascade) deepen
the impact claims for the three new findings.
