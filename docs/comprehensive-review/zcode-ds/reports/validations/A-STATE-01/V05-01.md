# A-STATE-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (A-STATE-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents that claim conversation-persistence facts (format,
authority, deletion cascade, no-SQLite) still match current code; drift is
classified per REPORTING.md.

## Method

Read claims from `echo-agent-cli/docs/MASTER-PLAN.md`,
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/docs/MASTER-PLAN.md`, and
`echo-agent-cli/docs/2026-07-28-app-core-full-audit.md`, then check each
against current source.

```text
grep -n "U1c|conversation|Conversation|会话|持久化|persistence|deletion|删除" \
  echo-agent-cli/docs/MASTER-PLAN.md docs/MASTER-PLAN.md echo-agent-cli/docs/2026-07-28-app-core-full-audit.md
```

## Expected Result

Every claim classified current/fixed/stale/regressed with a source anchor; no
unchecked historical claim is reused in the task report.

## Result

- Exit code: 0 (greps)
- Summary of classifications:
  - `echo-agent-cli/docs/MASTER-PLAN.md:70` (Iteration 3) — file backends
    migrated with corrupt-JSON errors, path-safe ids, unique temp names,
    parent-dir sync; `FileConversationStore` serializes single-process
    read/modify/write; message projection round-trips roles/tool
    identity/multimodal/reasoning. **current** (framework V04-01/V04-02;
    F-MEM-01 V02/V03; file_conversation.rs:43-51,494-523).
  - `echo-agent-cli/docs/MASTER-PLAN.md:70` — "App-only SessionSearchEngine
    ... remains in EKO". **current** (it exists in app-core) but the module is
    dead in production (V02-01, finding A-STATE-01-P3-01) — doc claim about
    placement is true, the module's own comment that it "drives the
    sessions-search UI" (conversation_file.rs:5-8) is false.
  - `echo-agent-cli/docs/MASTER-PLAN.md:359` — "Conversation deletion removes
    both tool-output and user-input scopes; a best-effort 30-day TTL cleanup
    runs when new input artifacts are written". **current** for GUI and TUI
    (conversations.rs:609-637, events.rs:3077-3095; TTL at
    prepared_turn.rs:110-119); uploads dir is outside this claim
    (A-INP-01-P3-01).
  - `echo-agent-cli/docs/MASTER-PLAN.md:362-365` — persistence merge by
    role order; `display_content` keeps artifact references. **current**
    (V04-05 merge tests).
  - `echo-agent-cli/docs/MASTER-PLAN.md:472-475` — "conversation deletion
    returns before background detail cleanup finishes". **current** (GUI
    cleanup runs in `tokio::task::spawn_blocking`, conversations.rs:618-636).
  - `docs/MASTER-PLAN.md:95` — "对话与 runtime checkpoint 使用文件持久化;
    CLI 不启用 SQLite". **current** (V01-01: zero SQLite in EKO).
  - `docs/MASTER-PLAN.md:115` — "conversation 删除级联清理". **partially
    current** — tool-output/user-input cleaned on GUI/TUI; tool-execution
    repository cleaned on GUI only (finding A-STATE-01-P3-02); uploads never
    cleaned (A-INP-01-P3-01).
  - `docs/MASTER-PLAN.md:291` — "会话持久化去 data URL" (deferred).
    **current (deferred)** — frontend still renders/stores data URLs in
    `AttachmentsPayload.attachments` (persistence.rs:150-172 comment).
  - `echo-agent-cli/docs/2026-07-28-app-core-full-audit.md:26-27,171-181` —
    S2/S3 migration claims and the note that `persistence.rs` is a separate
    older frontend-shaped projection staying in the app. **current** for the
    migration itself; the note is confirmed by the dead legacy session methods
    (V01-01).
  - No document claims the store-root behavior across workspace switch/exit
    or cross-process conversation writes — those are new observations
    (findings A-STATE-01-P1-01 / P2-01 / P2-02), not regressions of a
    documented claim.

## Deviations

None beyond the classifications above.

## Conclusion

Supports: documented conversation-persistence facts are largely current;
the only drift is the *false self-description* inside `conversation_file.rs`
(claims to drive the sessions-search UI — it does not) and the deletion
cascade claim being surface-incomplete (TUI tool-execution, uploads). Store
roots and cross-process behavior are undocumented areas where the review
found new defects.

## Follow-Up

None.
