# F-WFL-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-WFL-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The workflow engine is registered/exported and runtime-reachable: (1)
framework export path, (2) at least one live production consumer, (3)
examples/tests exercising it, and (4) a precise inventory of
defined-but-unreachable API surface.

## Method

1. `echo-orchestration/src/lib.rs:26` `pub mod workflow;` (unconditional,
   no feature gate) and `echo-agent/src/lib.rs:65` `pub mod workflow;`
   (unconditional); facade re-export `src/lib.rs:241-246` (`workflow::*`
   incl. `WorkflowDefinition`); root `Cargo.toml:102` `workflow = []` —
   grep for any `#[cfg(feature = "workflow")]` usage: **zero hits**; the
   feature only gates examples via `required-features`.
2. EKO production consumer: `echo-agent-cli/src/tauri/mod.rs:248-252`
   registers `list_workflows/get_workflow/create_workflow/delete_workflow/
   execute_workflow`; `src/tauri/commands/panels.rs:746-782`
   `execute_workflow` calls
   `echo_agent::workflow::loader::load_graph_from_yaml_str(:760)`
   `.or_else(load_graph_from_json_str)`, builds `SharedState::new()` (:769)
   and runs `graph.run(shared_state)` — live Tauri command; frontend
   `web-frontend/src/api/endpoints.ts:348-356` and
   `web-frontend/src/components/workflow/WorkflowPanel.tsx` call
   `create_workflow`/`execute_workflow`.
3. Examples/benches using the API: `examples/demo28_workflow.rs`,
   `demo34_workflow_stream.rs`, `demo37_declarative_workflow.rs`,
   `demo39_workflow.rs`, `demo46_data_analyst.rs`, `demo47_enterprise.rs`,
   `demo57_data_pipeline.rs`, `benches/agent_bench.rs:61-62`.
4. Dead/unreachable surface inventory (grep for construction/emission
   sites):
   - `WorkflowEvent::Token` / `WorkflowEvent::NodeError`
     (`echo-orchestration/src/workflow/mod.rs:101,103`) — never emitted;
     `run_stream` emits only NodeStart/NodeEnd/Completed.
   - `InterruptType::ToolApproval` / `UserRequest`
     (`checkpoint_store.rs:181,183`) — never constructed in production
     (only `UserRequest` in a unit test, `checkpoint_store.rs:502`);
     `InterruptState::tool_approval` (`graph.rs:173-185`) — no callers.
   - `Checkpoint.pending_action` (`checkpoint_store.rs:56`) — always
     `None` (`:95`), never written.
   - `SharedAgentMutex` deprecated alias (`mod.rs:80-81`) — zero users.
   - EKO `WorkflowDef`/`WorkflowStep` (`echo-agent-cli/echo-agent-app-core/
     src/state.rs:220-240`) — defined only; no constructor or consumer
     anywhere (Rust or frontend).

## Expected Result

Export path verified; at least one live production consumer; examples
exist; each dead-surface item confirmed with zero construction sites.

## Result

- Exit code: not-applicable (static trace)
- Summary: all four claims verified. The `workflow` feature
  (`Cargo.toml:102`) gates **no code** — the engine is always compiled and
  exported; the feature only marks examples, contradicting the README
  feature table (`README.md:256`, see V05). Production reachability is
  real via the EKO GUI workflow panel (Tauri command → framework loader →
  `Graph::run`).

## Deviations

The `workflow` feature being a no-op virtual gate is itself a defect
(F-WFL-01-P3-03). The dead-surface inventory above is recorded as
findings P3-01/P3-02/P3-07/P3-08.

## Conclusion

Supports: registration and runtime reachability of the workflow engine,
and a precise dead-surface inventory. Does not prove: correctness of the
interrupt/resume paths (V03-03), behavioral results (V04).

## Follow-Up

None.
