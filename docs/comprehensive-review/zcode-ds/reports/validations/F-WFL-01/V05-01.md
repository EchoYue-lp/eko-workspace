# F-WFL-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-WFL-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical/current documentation about the workflow engine matches the
current API surface; stale claims are identified and classified.

## Method

Grep both READMEs, `docs/MASTER-PLAN.md` (root), and the module
docstrings in `src/workflow/mod.rs` / `echo-orchestration/src/workflow/
mod.rs` for workflow API claims; cross-check each code sample symbol
against the actual API (`grep -rn "fn from_yaml"`, `GraphBuilder::new`
signature, `add_node`/`edge` methods, feature table vs
`cfg(feature = "workflow")` presence).

Documents checked: `echo-agent/README.md`, `echo-agent/echo-orchestration/
README.md`, `echo-agent/src/workflow/mod.rs` (docstring),
`docs/MASTER-PLAN.md`.

## Expected Result

Documented API samples compile against the current API; feature table
matches actual gating.

## Result

- Exit code: not-applicable (static check)
- Summary:
  1. `echo-agent/README.md:212` — "Declarative Workflow ... `Graph::
     from_yaml("wf.yaml")?`": `Graph::from_yaml` **does not exist**
     (grep for `fn from_yaml` on Graph: none; the loader API is
     `WorkflowDefinition::from_yaml` / `load_graph_from_yaml`). Stale →
     F-WFL-01-P3-06.
  2. `echo-agent/src/workflow/mod.rs:55-59` — same nonexistent
     `Graph::from_yaml("workflow.yaml")?` in the module docstring's
     "Declarative YAML" quick start (marked `ignore` so it never compiles).
     Stale → same finding.
  3. `echo-agent/README.md:256` — feature table row "`workflow` | Graph
     workflow engine": no `#[cfg(feature = "workflow")]` exists anywhere;
     the feature gates only examples (`required-features`), the engine is
     always compiled (`src/lib.rs:65` unconditional). Misleading →
     F-WFL-01-P3-03.
  4. `echo-agent/echo-orchestration/README.md:18-33` — quickstart shows
     `GraphBuilder::new()` (name argument is required), `.add_node(..)`,
     `.edge(..)`, and a `TaskManager` + `on_complete` pattern; none of
     these exist in the current API (`add_function_node`/`add_agent_node`/
     `add_edge`; TaskManager legacy surface per F-TSK-01-P3-01). Stale →
     same finding P3-06.
  5. `docs/MASTER-PLAN.md` — grep for workflow-engine claims: only
     unrelated hits (Claude Code workflows link :53, data-analysis product
     line :81, CI workflow file :750, notebook :860); no workflow-engine
     milestone claims to validate. No drift.
- No workflow claims were found in `echo-agent/AUDIT_REPORT.md` or the
  `echo-agent/docs` tree.

## Deviations

Three stale/misleading documentation claims (items 1-4) — one finding
group.

## Conclusion

Supports: documentation drift is real but localized (root README feature
table + declarative API sample, orchestration README quickstart,
`src/workflow/mod.rs` docstring). MASTER-PLAN carries no workflow-engine
claims, so no plan-vs-code drift was found here.

## Follow-Up

None.
