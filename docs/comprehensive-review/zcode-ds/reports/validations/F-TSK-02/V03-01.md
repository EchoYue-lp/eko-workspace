# F-TSK-02 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The single validator/frontier satisfies the task-card invariants:
(1) cycle/missing-node/self-dependency detection exists with fixtures;
(2) the ready frontier is deterministic; (3) structural checks are
status-independent; (4) skip and blocked propagation are covered by the one
dependency analysis (this last claim is expected to FAIL — see finding
P1-01).

## Method

Deep code inspection of `validator.rs`, `plan_spec.rs`, `runtime.rs`,
`runtime_executor.rs`, `revisioned.rs`, EKO `store.rs` / `types.rs` /
`executor.rs`, plus mapping of existing test fixtures to each invariant.

Checklist per invariant:

1. Cycle detection: `task_dependency_cycles` (validator.rs:354-407), sorted
   id iteration, `CycleVisitState` DFS; fixtures: `test_circular_dependencies`
   (validator.rs:607), `test_complex_circular_dependency` / multiple cycles /
   `test_self_dependency` (tasks/mod.rs:120-170), `runtime_validation_rejects_dangling_dependencies_and_cycles`
   (validator.rs:846-863), EKO `file_path_rejects_dependency_cycle_and_appends_no_event`
   (store.rs:3398).
2. Missing nodes: `validate_task_specs` known_ids check (validator.rs:263-273),
   `PlanSpec::to_task_specs` edge-endpoint validation (plan_spec.rs:363-374)
   incl. dangling optional edges (fixture validator.rs:824-837).
3. Self-dependency: validator.rs:249-255 + 416-420; fixture tasks/mod.rs:157-172.
4. Determinism: cycle DFS sorts ids (validator.rs:356-357); Kahn queue is
   `BinaryHeap<Reverse<String>>` (validator.rs:318-322); `to_task_specs`
   sorts+dedups dependency lists (plan_spec.rs:386-389); `ready_task_ids`
   iterates the snapshot slice (runtime.rs:438-458) so frontier order equals
   snapshot order; EKO `select_ownership_safe_wave` preserves input order.
   Identified exception: `refresh_in_flight` iterates the `in_flight` HashSet
   (runtime.rs:401) — nondeterministic refresh-list order, no production
   caller (P3-01).
5. Status independence: `validate_task_specs` and `validate_task_snapshot`
   (identity check) read only `TaskSpec`/id fields, never `execution.status`
   (validator.rs:184-294). Runtime enforcement is additionally performed at
   every executor safe point (runtime_executor.rs:214-219).
6. Skip propagation: `ready_task_ids` requires every dependency in
   `completed` (runtime.rs:449-455); `blocked_by_failures` requires `failed`
   deps (runtime.rs:461-478); `all_completed` counts `Skipped` as resolved
   (runtime.rs:481-483). Code trace of A=Skipped, B=Pending(dep A):
   B is neither ready nor blocked; executor falls to
   `note_stalled` -> `RuntimeDagOutcome::Failed("<none>", "DAG stalled with
   unfinished tasks (cycle or blocked)")` (runtime_executor.rs:287-313).
   Reachable entry points: task_update Skip (EKO types.rs:1301;
   revisioned.rs:547-568), recovery skip (store.rs:2170-2173).
7. Blocked propagation: forward propagation exists
   (`blocked_by_failures` + `block_task`, runtime_executor.rs:235-246, reason
   string at :243). EKO reverse recovery (retry unblock) re-walks dependents
   and matches the Blocked summary string `"blocked: upstream task failed"`
   (store.rs:1287-1295) — cross-repo string contract (P2-01).
8. Sequential execution mode: schema `["parallel","sequential"]`
   (task_tools.rs:245), parsed (419-424), stored in `TaskGraphContext`
   (revisioned.rs:31-38) and shown to the LLM (EKO compact_context.rs:199-201),
   but zero reads in `runtime_executor.rs` and EKO executor.rs production code
   — mode never enforced (P2-02).

## Expected Result

Invariants 1-5 hold; invariant 6 (skip in the one analysis) and 7 (typed
propagation contract) and 8 (mode enforcement) are expected to show gaps
recorded as findings.

## Result

- Exit code: 0 (static inspection)
- Duration: not-applicable
- Summary: Invariants 1-5 verified by code trace + fixture mapping (fixtures
  executed in V04-01..06, all green). Invariant 6 FAILS: skip does not
  propagate and dependents of a skipped task stall the run with a misleading
  error (finding P1-01). Invariant 7 has a fragile string-based contract
  (finding P2-01). Invariant 8 fails: `sequential` is inert (finding P2-02).
  Determinism exception in `refresh_in_flight` (P3-01).

## Deviations

The stall branch message also mentions "cycle", but committed snapshots are
rejected at commit time if cyclic, and re-validated at every safe point, so the
cycle wording is effectively unreachable; the reachable trigger is a skipped
dependency (and any other Pending-but-unready state).

## Conclusion

Supports: deterministic, status-independent structural validation with
cycle/missing/self-dependency fixtures; deterministic frontier ordering.
Establishes evidence for findings F-TSK-02-P1-01, P2-01, P2-02, P3-01.

## Follow-Up

None.
