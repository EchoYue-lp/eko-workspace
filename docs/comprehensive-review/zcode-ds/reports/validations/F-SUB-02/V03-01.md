# F-SUB-02 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The four execution modes share one lifecycle with consistent cancellation
propagation, timeout ownership, partial-failure handling, and cleanup; the
task card's required fixtures (mode lifecycle matrix, parent cancellation
propagation, timeout ownership, team partial-failure and cleanup) are
verifiable against current code and tests.

## Method

Inspection procedure (working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

1. Build the mode lifecycle matrix from the traces in V02-01 (routes, cancel
   tokens, timeout sources, isolation, terminal events, member events).
2. Inspect cancellation behavior in `executor.rs`: `dispatch()` pre-cancelled
   shortcut `:417-423`; Sync `:1501-1533`; Fork `:1772-1828` (incl. spawn-level
   pre-cancelled check `:1684-1692`); Teammate `:920-971`; Team `:992-1113`
   (cancel-token usage: none).
3. Inspect timeout ownership: `team/mod.rs:343-353` (`max(60)` floor),
   `team/mod.rs:45-73` (`TeamConfig` doc "0 = no timeout"), `executor.rs:988-991`
   comment ("relies on TeamAgent::execute's own tokio::time::timeout wrapper"),
   `executor.rs:1474` comment ("one config ... governs all three modes").
4. Inspect team partial-failure and cleanup: `manager_subagent.rs:217-336`
   (`execute_sub_tasks` spawn + error collection + checkpoint),
   `manager_subagent.rs:339-377` (`synthesize` receives failures),
   `team/mod.rs:414-461` (Swarm error swallow at `:437`), `team/mod.rs:343-348`
   (outer timeout drop semantics — spawned `tokio::spawn` tasks detached when
   the outer future is dropped).
5. Inspect fixture coverage: grep test names in `executor.rs` tests
   (`:1891-3672`) and `team/` modules for team timeout/cancel/partial-failure/
   cleanup tests.
6. Inspect dead/placeholder code: `executor.rs:915` (`_permit`), `executor.rs:1`
   (module doc), `types.rs:57-58` (unproduced `ObservedIsolation` variants),
   `executor.rs:262-299` (`TeammateHandle` — no `Drop`), `executor.rs:1555-1560`
   (permit wait outside cancel).

## Expected Result

- Parent cancellation propagates into all four modes and produces exactly one
  cancelled terminal (MASTER-PLAN:101/:149).
- Timeout ownership: one config source, or at least consistent per-mode honor
  of `definition.timeout_secs` / executor default; "0 = no timeout" holds.
- Team partial failure is either surfaced truthfully or tested; cleanup of
  in-flight members occurs on failure/timeout/cancel.
- Required fixtures exist or their absence is recorded.

## Result

- Exit code: not-applicable (static inspection + V04-01/V04-02 test runs)
- Duration: n/a
- Summary (mode lifecycle matrix):

| Dimension | Sync | Fork | Teammate | Team |
|---|---|---|---|---|
| Route | `dispatch_sync` :1469 | `dispatch_fork` :1554 | `dispatch_teammate` :871 + join :557-559 | `dispatch_team` :992 |
| Fresh agent per dispatch | yes :1470 | yes :1567 | yes :877 | no — shared registry singletons (:1020-1068) |
| Parent-cancel child token | yes :1486 | yes :1671 | yes :879 | **no** (`req.cancel` unused) |
| Cancel-aware select (biased) | yes :1502-1506 | yes :1774-1777 | yes :922-926 | none |
| Timeout source | def ?: executor default :1475-1478 | same :1569-1573 | same :891-895 | TeamConfig (def + executor default ignored); floor 60 s |
| Timeout cancels producer | yes :1526 | yes :1798 | yes :928 | no (future dropped, members detached) |
| Isolation | none (reports Unknown) | worktree/workspace + hard-fail :1609-1636 | none | none (hardcoded `Subagent` :1104) |
| Terminal events via dispatch loop | yes | yes | yes | yes (only after team returns) |
| Per-member events | n/a | n/a | n/a | none |
| Detached-execution risk | none | none | handle-drop detach | cancel never; timeout/abort detaches members |

- Parent cancellation propagation: Sync/Fork/Teammate verified (tests
  `running_dispatch_cancel_emits_cancelled_terminal_status`,
  `teammate_handle_cancel_does_not_cancel_parent`,
  `*_timeout_cancels_detached_stream_producer`). Team: no cancel wiring at all
  (zero `CancellationToken` in `team/`, V01-01); a parent cancel mid-team is
  invisible until the team ends (up to 600 s).
- Timeout ownership: 4th knob for Team; `TeamConfig` field doc "0 = no timeout"
  violated by `.max(60)` at `team/mod.rs:345`; `executor.rs:1474` comment
  ("one config governs all three modes") is stale now that Team exists;
  `dispatch_team` never calls `TeamAgentBuilder::timeout_secs` despite the
  builder doc (`team/mod.rs:562-569`) saying callers thread the unified config.
- Team partial failure: `ManagerSubagentOrchestrator` collects member failures
  into `Err` entries and passes them to synthesis with truthful-reporting
  instructions (`manager_subagent.rs:356-359, 367-369`) — sound design; however
  no test exercises it, and on failure/timeout the other spawned members keep
  running (no sibling cancellation; `handles` Vec dropped detaches tasks).
  Swarm (`team/mod.rs:437`) silently swallows member failures
  (programmatic-only path).
- Team cleanup: no cleanup of in-flight members on timeout (outer
  `tokio::time::timeout` at `team/mod.rs:346-348` drops `execute_inner`; the
  `tokio::spawn` tasks inside `execute_sub_tasks` continue). Framework
  `WorktreeHandle`/`DataWorkspaceHandle` have `finalize` but no removal
  contract (cleanup is the application's policy; EKO owns worktree lifecycle
  via `EkoWorktreeFactory` — A-TSK-05 scope).
- Fixture inventory: no test covers team timeout, team cancellation, a failing
  member (partial success), or member cleanup. Existing team tests cover
  routing (executor.rs:2988-3070), isolation (fork worktree/workspace,
  :3116-3671), checkpoints (manager_subagent.rs:538-728), timeout/cancel for
  Sync/Fork/Teammate only (:2659-2983).
- Dead/placeholder items: `_permit` token clone `:915`; module doc
  "Sync / Fork / Teammate modes" `:1` (Team omitted); `ObservedIsolation::Primary`
  / `PrimaryFallback` unproduced; `TeammateHandle` has no `Drop`
  (dropping detaches the spawned task); fork semaphore permit wait
  (`acquire_owned` at :1555-1560) is not cancel-aware.
- Cross-check of F-SUB-01 findings in scope: `invocation_disabled_tools` is
  called only in `dispatch_fork` (`:1744-1745`) from fork-only `allowed_tools`
  (`:1672-1676`) — confirms F-SUB-01-P1-01 (per-role tool_filter inert;
  sync/teammate/team carry no tool restriction). `compile_invocation` transfer
  policy is `Fresh` for non-Fork modes (`:1123-1132`), so `inherit_history`
  never applies to Team — confirms F-SUB-01-P2-02's Team arm.
- Log/artifact: none.

## Deviations

- Confirmed deviations vs expected invariants (recorded as findings):
  P1-01 (team cancel gap), P1-02 (team timeout/abort detaches members),
  P2-01 (timeout authority split + 60 s floor), P2-02 (missing required
  fixtures), P2-03 (dead team coordinator/runner/mailbox), P3-01 (dead
  isolation variants), P3-02 (placeholder + stale docs), P3-03 (Teammate
  handle drop / non-cancel-aware permit wait).

## Conclusion

Supports: the unified lifecycle claim holds for Sync/Fork/Teammate and for the
event/hook shell of Team, but Team execution is a detached, non-cancellable
island with its own timeout authority; the task card's required fixtures for
team partial failure and cleanup do not exist. Does not prove the 60 s floor or
member-detach behavior dynamically (no fixtures exist to run; static guarantee
from `max(60)` and `tokio::spawn` drop semantics).

## Follow-Up

Dynamic proof of P1-01/P1-02 belongs to Q-FLT-02 (fault injection) once the
fixtures from P2-02 are written. None for this attempt.
