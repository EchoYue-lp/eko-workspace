# F-SUB-02 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

There is exactly one subagent execution lifecycle authority (the
`SubagentExecutor` dispatch loop plus its mode routers) with no parallel
mode/manager/team lifecycle implementation and no legacy `worker` terminology
in either repository; every timeout/cancel/isolation authority is enumerated.

## Method

Inspection procedure (working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

1. `grep -rn "ExecutionMode" echo-agent/src` — enum definition + all production
   uses (excluded test modules).
2. `grep -rn -i "worker" --include="*.rs" echo-agent/src echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src` — worker-term scan; then a filtered scan excluding `NetworkError`.
3. `grep -rn -i "\bworker\b" --include="*.rs" echo-agent/src/agent/subagent echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime` — focused subagent/task-runtime scan; `grep -rn '"worker"'` for tool/mode string values.
4. `grep -rn "TeamRunner\|TeamCoordinator\|\.fan_out\|\.coordinator"` across both repos excluding the defining modules and `#[cfg(test)]` — production-caller inventory of team machinery.
5. `grep -rn "CancellationToken\|child_token\|cancelled()" echo-agent/src/agent/subagent/team` — cancellation-token inventory in the team module.
6. `grep -rn "ObservedIsolation::Primary\|PrimaryFallback"` across both repos — dead-variant search.
7. `grep -rn "dispatch_teammate\|dispatch_team\|TeamSpec\|ExecutionMode::Team\|ExecutionMode::Teammate"` outside `executor.rs`/`types.rs`/`agent_dispatch.rs` — other callers of the mode routers.
8. `grep -rn "SubagentExecutorConfig\|SubagentExecutor::new"` in `react/builder.rs`, `react/mod.rs` — executor construction sites and config threading.
9. Timeout-authority inventory: `grep -n "timeout_secs\|default_timeout_secs"` in `executor.rs`, `team/mod.rs`, `team/runner.rs`, `agent/config.rs`, `react/mod.rs`.

## Expected Result

- `ExecutionMode` has exactly 4 variants (Sync/Fork/Teammate/Team) consumed only by the unified dispatch loop; no second mode dispatcher.
- Zero `worker` matches that are not `NetworkError`-family.
- Zero production callers for any parallel team lifecycle machinery; if TeamCoordinator/TeamRunner/Mailbox have none, they are dead public APIs.
- Zero `CancellationToken` occurrences in the team module (would confirm the team path has no cancellation wiring).
- Timeout sources per mode are identified and compared.

## Result

- Exit code: not-applicable (static grep inspection)
- Duration: n/a
- Summary:
  1. `ExecutionMode` defined once at `echo-agent/src/agent/subagent/types.rs:24-54` (Sync/Fork/Teammate/Team); routed only by `dispatch()` at `executor.rs:552-565`. No parallel dispatcher found in either repo.
  2. `worker` scan: zero matches in code of either repo other than `LlmError::NetworkError` / `ChannelError::NetworkError` strings (verified: all matches contain "Network").
  3. `TeamRunner` / `TeamCoordinator` / `fan_out` / `.coordinator`: zero production callers in either repo (only the defining modules, re-export `team/mod.rs:16`, and `#[cfg(test)]` uses). `Team::coordinator` field (`team/mod.rs:104`) is constructed (`:128`) but never invoked.
  4. Team module `CancellationToken`: zero occurrences in `echo-agent/src/agent/subagent/team/` (all 8 files). `dispatch_team` (`executor.rs:992-1113`) never references `req.cancel`.
  5. `ObservedIsolation::Primary` / `PrimaryFallback` (`types.rs:57-58`): zero producers in either repo (only the enum declaration and `as_str` mapping at `:71`).
  6. Mode routers are called only from `dispatch()` (`executor.rs:553-564`); `dispatch_teammate` additionally reachable from `dispatch()`'s Teammate arm; `dispatch_background` (`:854-862`) spawns `dispatch`.
  7. Executor constructed once in `react/mod.rs:413-436`; `default_timeout_secs: config.subagent_timeout_secs` threaded from `AgentConfig` (`config.rs:222, 402-403, 524-525`, default 600). `TeamAgent::builder()` in `dispatch_team` never calls `.timeout_secs(...)`, so `SubagentExecutorConfig.default_timeout_secs` and `SubagentDefinition.timeout_secs` are unreachable for Team mode; the team timeout is `TeamConfig.default_timeout_secs` (`team/mod.rs:45-73`, EKO always `TeamConfig::default()` = 600, `subagent_loader.rs:471`).
  8. `TeamAgent::execute_with_usage` (`team/mod.rs:343-353`) uses `Duration::from_secs(self.team.config.default_timeout_secs.max(60))` — a 60 s floor contradicting the field doc "0 = no timeout" (`team/mod.rs:49`).
  9. `dispatch_teammate` holds `let _permit = child_token.clone();` (`executor.rs:915`) — a cloned token, not a semaphore permit; the executor semaphore (`executor.rs:363,369`) is acquired only in `dispatch_fork` (`:1555-1560`).
- Log/artifact: none (grep outputs inline above).

## Deviations

- `ObservedIsolation::Primary`/`PrimaryFallback` dead variants (P3 finding).
- `TeamRunner`/`TeamCoordinator`/mailbox machinery dead in production (P2 finding).
- Team module has zero cancellation wiring (basis of P1 findings).

## Conclusion

Supports: single unified lifecycle authority for Sync/Fork/Teammate/Team; zero
legacy worker terminology; team path has no cancellation token anywhere; team
timeout authority is disconnected from the executor/definition knobs; the
semaphore bounds Fork only. Does not prove runtime behavior (covered by V03/V04).

## Follow-Up

None. Cross-checked with F-SUB-01 V01-01 (worker scan, `tool_filter` readers)
and F-RCT-04 V01-01 (no batch/executor duplication).
