# B-BASE-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-BASE-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Target inventory: `echo_agent` has one lib, 68 examples, 1 bench
(`harness = false`), 2 integration tests, no bins; the CLI root has one lib
(`src/lib.rs`), one default bin `echo-agent-cli`, one `gui`-required bin
`echo-agent-tauri`, one build script; app-core has one lib and one test.
`required-features` gates exist exactly where the manifests declare them.

## Method

1. `cargo metadata --locked --format-version 1 --no-deps` in both repos;
   parsed `targets[]` (kind, `required-features`) per package.
2. Cross-checked the CLI target list against `src/`, `src-tauri/src/main.rs`,
   `build.rs` file existence.
3. Counted examples declared in the manifest `[[example]]` sections versus
   auto-discovered examples in `examples/`.
4. Verified `[[bench]] harness = false` for `agent_bench`.

## Expected Result

- Framework: lib `echo_agent`; 68 examples; bench `agent_bench`
  (harness=false); tests `cache_user_id_test`, `react_smoke`; zero bins.
- CLI: lib `echo_agent_cli`; bins `echo-agent-cli` (default-run) and
  `echo-agent-tauri` (required-features `gui`); custom-build target.
- app-core: lib `echo_agent_app_core`; test `runtime_state_e2e`.

## Result

- Framework targets (from metadata): lib `echo_agent`; 68 examples including
  `demo00_quickstart`…`demo70_scheduler` and `smoke_usage_passthrough`; bench
  `agent_bench`; tests `cache_user_id_test` and `react_smoke`; no bins.
  - The manifest declares ~50 `[[example]]` sections (`echo-agent/Cargo.toml:163-372`);
    the remaining examples (e.g. `demo00_quickstart`, `demo01_tools`,
    `demo02_tasks`, `demo07_skills`, `demo09_file_shell`, `demo10_streaming`,
    `demo11_callbacks`, `demo13_tool_execution`, `demo15_structured_output`,
    `demo17_chat`, `demo19_guard`, `demo32_token_budget`, `demo33_retry_policy`,
    `demo40_snapshot`, `demo52_loop_detection`, `demo53_adaptive_compression`,
    `demo54_headless`, `demo56_plugin_system`, `demo62_prompt_templates`,
    `demo64_tool_pipeline`, `demo65_context_assembler`,
    `demo66_context_selector`, `demo70_scheduler`) are auto-discovered
    `examples/*.rs` without feature gates.
  - Bench `agent_bench` is `harness = false` (`echo-agent/Cargo.toml:378-380`).
- CLI targets (from metadata): lib `echo_agent_cli` (auto-discovered from
  `src/lib.rs`); bin `echo-agent-cli` (path `src/main.rs`, `default-run`
  at `Cargo.toml:12`); bin `echo-agent-tauri` (path `src-tauri/src/main.rs`,
  `required-features = ["gui"]` at `Cargo.toml:43-46`); custom-build
  (from `build.rs`). `echo-agent-tauri` is absent from target lists unless
  `gui` is enabled — verified via metadata `required-features` and by the
  manifest `[[bin]]` gate.
- app-core targets: lib `echo_agent_app_core`; test `runtime_state_e2e`.
- `build.rs` behavior verified: calls `tauri_build::build()` only when
  `CARGO_FEATURE_GUI` is set, and only if `tauri.conf.json` exists
  (`echo-agent-cli/build.rs:3-11`).
- No bin target exists in the framework workspace (metadata: zero bins).

## Deviations

None.

## Conclusion

Supports: the exact target inventory, the `gui`-gated Tauri bin, the default
TUI/CLI bin, the harness=false bench, and both integration tests.

Does not prove: that the gates match CI execution (V04), that every example
compiles (Q-FW-02), or runtime composition of entry points (B-PATH-01).

## Follow-Up

Hand the feature-gated example list to `Q-FW-02` (examples grouped by
identical required features). Rerun as `V03-02` if targets change.
