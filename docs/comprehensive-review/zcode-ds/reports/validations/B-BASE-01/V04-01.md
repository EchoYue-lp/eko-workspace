# B-BASE-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-BASE-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

CI in both repositories enforces the submission and conditional gates declared
in root `AGENTS.md` (framework: fmt, all-feature Clippy, panic-safety Clippy,
`cargo test --workspace --all-targets --all-features --locked`, no-default
lib check; CLI: the same set plus `cargo check -p echo-agent-app-core
--no-default-features`, plus the frontend conditional gate when
`web-frontend/` changes).

## Method

1. Read both `.github/workflows/rust-ci.yml` files in full.
2. Compared each workflow command against the gate commands in
   `AGENTS.md:247-283` (submission gates) and `AGENTS.md:301-305`
   (frontend conditional gate).
3. Checked for Node/setup-node/npm steps in the CLI workflow.
4. Checked the framework test job's target semantics: `--lib --tests` vs
   `--all-targets`, and whether examples/benches/doctests are executed.
5. Checked whether the CLI workflow pins the framework revision used to
   satisfy the `../echo-agent` path dependencies.

## Expected Result

- Framework CI executes exactly the AGENTS.md framework gate: fmt check,
  two Clippy configs, `cargo test --workspace --all-targets --all-features
  --locked`, no-default check.
- CLI CI executes exactly the AGENTS.md CLI gate plus a frontend job
  (Prettier check, `npm test`, `npm run build`) because the repository
  ships a React frontend with committed lockfile.

## Result

- Exit code: not-applicable (static comparison; no CI run executed).
- Framework CI (`echo-agent/.github/workflows/rust-ci.yml`):
  - lint job `:27-30`: fmt check, all-feature Clippy `-D warnings`,
    panic-safety Clippy — matches.
  - test job `:62`: `cargo test -p "${{ matrix.package }}" --lib --tests
    --all-features --locked`. **Does not match** the gate at
    `AGENTS.md:259` (`cargo test --workspace --all-targets --all-features
    --locked`). The comment at `:61` ("The lint job already compiles examples
    and benches with --all-targets") confirms the intent: examples and benches
    are only *compiled* by Clippy, never executed as test harnesses. Doctests
    are not executed either (`--lib --tests` excludes `--doc`).
  - minimal job `:76`: `cargo check --workspace --lib
    --no-default-features --locked` — matches.
- CLI CI (`echo-agent-cli/.github/workflows/rust-ci.yml`):
  - Rust gate `:37-45`: fmt, both Clippy configs, `cargo test --workspace
    --all-features --locked`, `cargo check -p echo-agent-app-core
    --no-default-features` — matches the CLI submission gate
    (`AGENTS.md:266-277`).
  - **No frontend steps anywhere in the workflow**: no `setup-node`, no
    `npm`, no `prettier`, no `web-frontend` reference. The frontend
    conditional gate (`AGENTS.md:301-305`: `npx prettier --check`,
    `npm test`, `npm run build`) is not enforced by CI. The repo contains
    `web-frontend/package.json` with test/build scripts
    (`package.json:6-14`) and a committed `package-lock.json`.
  - **Unpinned framework input**: steps `:15-21` check out
    `EchoYue-lp/echo-agent` with `repository:` only — no `ref` — into the
    sibling path the manifests require (`echo-agent-cli/Cargo.toml:50`,
    `echo-agent-app-core/Cargo.toml:10`). The framework revision therefore
    floats with its default branch between runs of the same CLI commit;
    `--locked` cannot stabilize it because path dependencies carry no
    checksum, and a framework-main dependency change can additionally turn
    `--locked` red.

## Deviations

None.

## Conclusion

This validation FAILS. Supports: (a) framework CI does not execute the
declared all-target test gate (examples/benches/doctests excluded);
(b) CLI CI never runs any frontend validation despite a shipped React
frontend; (c) CLI CI's framework input is unpinned and non-reproducible.
These three gaps become findings `B-BASE-01-P2-01/02/03` in the task report.

Does not prove: whether current code happens to pass the gates (Q-FW-01 /
Q-CLI-01), or whether GitHub Actions runs would behave differently than the
file text (workflow syntax is standard and self-consistent).

## Follow-Up

`B-DOC-01` should classify the `AGENTS.md` "CI depends on the listed
mandatory gates" claim as `regressed`. A CI re-run after a workflow fix would
be a new attempt `V04-02`; the workflow files themselves must not be edited
during review (read-only).
