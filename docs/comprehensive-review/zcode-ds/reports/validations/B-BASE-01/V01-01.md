# B-BASE-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (B-BASE-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

`echo-agent` is one resolver-v3 workspace of 8 packages (root `echo_agent` +
7 sub-crates), and `echo-agent-cli` is one resolver-v3 workspace of 2 packages
(root `echo-agent-cli` + `echo-agent-app-core`), with exactly the cross-repository
path dependencies declared in the manifests. Both lockfiles resolve with
`--locked`.

## Method

1. Read every `Cargo.toml` in both repositories in full (8 framework
   manifests, 2 CLI manifests).
2. `cargo metadata --locked --format-version 1 --no-deps` in both repository
   roots; exit codes and member counts recorded.
3. Listed each package's workspace path dependencies from manifest `[dependencies]`.
4. Grepped all manifests for absolute/worktree paths to detect machine-local
   dependency leaks.

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent` and
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli`.

## Expected Result

- 8 framework workspace members; 2 CLI workspace members; `resolver = "3"` in
  both; `cargo metadata --locked` exits 0 in both.
- Framework sub-crates depend only on `echo_core`/`echo_macros`; root facade
  depends on all 7.
- CLI depends on framework via relative paths `../echo-agent` and
  `../echo-agent/echo-core`; app-core via `../../echo-agent`.
- No absolute or worktree paths in committed manifests.

## Result

- Exit codes: `cargo metadata --locked` = 0 in both repositories.
- Workspace members: framework = 8 (`echo_core`, `echo_macros`,
  `echo_execution`, `echo_integration`, `echo_tools`, `echo_state`,
  `echo_orchestration`, root `echo_agent`); CLI = 2 (root `echo-agent-cli`,
  `echo-agent-app-core`). Both `resolver = "3"`
  (`echo-agent/Cargo.toml:21`, `echo-agent-cli/Cargo.toml:2`).
- Framework workspace path deps (from manifests):
  `echo_core` none; `echo_macros` none; `echo_tools` → echo_core,
  echo_macros; `echo_integration` → echo_core; `echo_state` → echo_core;
  `echo_orchestration` → echo_core; `echo_execution` → echo_core, echo_tools;
  root → all 7 (`echo-agent/Cargo.toml:106-112`).
- CLI workspace path deps:
  - `echo-agent-cli/Cargo.toml:49` → `./echo-agent-app-core`;
    `:50` → `../echo-agent` (echo_agent, default-features=false, features
    mcp/lsp/human-loop/subagent/tasks); `:51` → `../echo-agent/echo-core`.
  - `echo-agent-app-core/Cargo.toml:10` → `../../echo-agent` (echo_agent,
    default-features=false, features mcp/lsp/human-loop/subagent/git/tasks/
    shell/files/web/data/statistics/chart/research/media/rag);
    `:16` → `../../echo-agent/echo-core`; `:59` dev-dep on echo_agent
    with `testing` feature.
- Grep for `worktrees` and `/Users/` in all manifests: zero hits. No
  machine-local paths.
- Lockfiles present: `echo-agent/Cargo.lock` (160,355 B),
  `echo-agent-cli/Cargo.lock` (240,188 B); both accepted by `--locked`.
- No `echo-agent-eval` directory exists at `b3b2e81f2b2d` (AGENTS.md:139
  claim is stale); an empty `echo-agent-cli/evals/` directory exists instead.

## Deviations

None. (The stale `echo-agent-eval` doc claim is recorded as a finding, not a
deviation of this validation.)

## Conclusion

Supports: exact member inventories, resolver version, relative-path-only
cross-repository dependencies, and lockfile validity of both workspaces.

Does not prove: feature semantics (V02), target/required-feature inventory
(V03), or CI enforcement (V04). `--no-deps` means the third-party resolved
graph was not audited here (deferred to Q-DEP-01).

## Follow-Up

None. Rerun as `V01-02` only if a manifest tree or lockfile changes.
