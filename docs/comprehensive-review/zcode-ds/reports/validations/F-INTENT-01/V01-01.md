# F-INTENT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Intent classification and trigger supervision have exactly one definition site
(`echo-agent/src/intent`) and no parallel implementation in either repository.

## Method

Repository-wide definition and duplicate search. Exact commands:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
grep -rn "intent" --include="*.rs" -i -l src/ | grep -v "/intent/"
grep -rn "TriggerSupervisor\|hook_activation_slot\|activate_skill" --include="*.rs" src/ | grep -v "/intent/"
grep -rn "ChainedClassifier" --include="*.rs" src/ echo-agent-cli-app-src   # zero production consumers
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
grep -rn -i "intent\|IntentRouter\|TriggerSupervisor\|KeywordClassifier" --include="*.rs" \
  echo-agent-app-core/src/ | grep -vi "intentional\|intention"
```

Search terms: `intent`, `IntentRouter`, `TriggerSupervisor`, `KeywordClassifier`,
`LlmIntentClassifier`, `ChainedClassifier`, `IntentClassifier`, `Intent::`,
`classify`, `activate_skill`, `hook_activation_cache`, `trigger`.

## Expected Result

One intent authority in the framework; EKO consumes it as a product wiring;
no second classification engine; the word "trigger" may appear in unrelated
subsystems (to be classified).

## Result

- Exit code: 0 (grep commands)
- Duration: < 5 s
- Summary:
  - Definitions: `echo-agent/src/intent/mod.rs` (`Intent`, `IntentClassifier`,
    `IntentRouter`, `IntentRouterConfig`), `classifier.rs`
    (`KeywordClassifier`, `KeywordClassifierConfig`, `LlmIntentClassifier`,
    `SkillDescription`, `ChainedClassifier`), `trigger_supervisor.rs`
    (`TriggerSupervisor`). Single definition site; re-exported via
    `echo-agent/src/lib.rs:46` (`pub mod intent`).
  - EKO consumers: `echo-agent-cli/echo-agent-app-core/src/runtime.rs:24-25,294-356`
    (wiring) and tests at :597-670. No other EKO file uses the intent API.
  - `ChainedClassifier` has zero production consumers (only re-export at
    `src/intent/mod.rs:159` and a stale comment in EKO `runtime.rs:294`).
  - Term overlaps classified as unrelated: evolution `src/evolution/triggers.rs`
    `TriggerDetector`/`memory_trigger_state` (memory-evolution triggers, F-EVO-01
    scope); `tools/builtin/human_in_loop.rs:32,41` and `subagent/context.rs:23`
    (prose uses of the word "intent"); `subagent/executor.rs:1601-1621`
    (worktree "intent", unrelated); EKO `tasks/task_runtime/types.rs:26`
    (DomainProfile doc, see V05).
  - No parallel intent classification engine, no second router, no second
    fusion supervisor found in either repository.

## Deviations

The word "trigger" is overloaded (intent `TriggerSupervisor` vs evolution
`TriggerDetector`); both are distinct live mechanisms with separate scopes, not
duplicates. Classified, not a finding.

## Conclusion

Supports: one generic intent authority in the framework, consumed by EKO as
product wiring; no duplicate implementation. Does not prove runtime
reachability (V02) or behavior (V03).

## Follow-Up

V02-01.
