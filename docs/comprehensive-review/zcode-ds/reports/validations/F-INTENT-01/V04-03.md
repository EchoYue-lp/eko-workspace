# F-INTENT-01 / V04 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The intent-router integration test in the framework agent test suite passes on
the reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --locked intent_router
```

Environment: default features, `--locked`.

## Expected Result

The `intent_router_*` integration test passes; exit code 0.

## Result

- Exit code: 0
- Duration: < 60 s (compile cached)
- Summary: `test result: ok. 1 passed; 0 failed; 0 ignored` (446 filtered
  out). Covered:
  - `intent_router_skill_activation_survives_compression_markers` — an
    activated skill's context projection survives force-compression; this
    pins the projection marker behavior relied on by `activate_skill`
    (`capabilities.rs:964-997`) after the intent router activates a skill.
- Log/artifact: none persisted.

## Deviations

None.

## Conclusion

Supports: skill activation through the router path is compatible with
compression (projection markers survive). It does not test the
`TriggerSupervisor` hook-slot interaction (no hook fixture in the framework
suite).

## Follow-Up

V04-04.
