# F-INTENT-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The routing fixtures that exercise the DirectAnswer shortcut through the
streaming and non-streaming run entries pass on the reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --locked direct_answer
```

Environment: default features, `--locked`.

## Expected Result

All direct-answer routing tests pass; exit code 0.

## Result

- Exit code: 0
- Duration: < 60 s (compile cached)
- Summary: `test result: ok. 3 passed; 0 failed; 0 ignored` (444 filtered
  out). Covered:
  - `value_scoped_direct_answer_records_usage_in_child_trace` — DirectAnswer
    shortcut records usage in a child trace run and does not mutate the agent
    run id;
  - `non_streaming_direct_answer_routes_through_projection_boundary` /
    `streaming_direct_answer_routes_through_projection_boundary` — with a
    pre-model projector installed, the "DirectAnswer" intent is routed through
    the projection boundary (i.e. NOT the shortcut), proving
    `allows_direct_answer_shortcut() == false` when a projector exists, on
    both entry paths.
- Log/artifact: none persisted.

## Deviations

None.

## Conclusion

Supports: the shortcut/non-shortcut gate behaves as `mod.rs:624-629`
documents on both run entries; the projector gate is empirically exercised.
Supports the EKO claim in F-INTENT-01-P2-02 (projector present -> shortcut
impossible) with an executable fixture.

## Follow-Up

V04-03.
