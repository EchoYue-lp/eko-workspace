# Q-DEP-01 / V05 / Attempt 01 — Cross-reference with existing findings (B-BASE-01, Q-STA-01)

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The dependency findings already on file (Q-STA-01-P3-03 duplicates; B-BASE-01
CI findings) are re-verified against the current lockfiles, manifests, and
workflows, and classified as current / fixed / stale / regressed per the
reporting protocol.

## Method

```text
# 1. Duplicate counts vs Q-STA-01 V07:
python3 Cargo.lock parse (both)          # Q-STA-01 claimed 38 (fw) / 76 (cli)
# 2. reqwest/crossterm splits vs Q-STA-01-P3-03:
cargo tree -i reqwest@0.13.3 / @0.12.28 / crossterm@0.29.0 / @0.28.1  (CLI)
grep tauri-2.11.2/Cargo.toml [target.'cfg(...)'.dependencies.reqwest]
# 3. B-BASE-01 CI claims:
grep -n "node\|npm\|frontend" echo-agent-cli/.github/workflows/rust-ci.yml   # expect zero
grep -n -A3 "repository: EchoYue-lp/echo-agent" echo-agent-cli/.github/workflows/rust-ci.yml  # expect no ref
```

## Expected Result

Q-STA-01-P3-03 holds in essence with possibly refined mechanics; the two
B-BASE-01 CI findings (P2-01 frontend never validated, P2-02 unpinned
framework input) remain current; no other cross-repo dependency claim drifts.

## Result

- Exit code: 0
- Summary:
  - **Q-STA-01-P3-03 (reqwest 0.12/0.13 + crossterm 0.28/0.29) —
    current, with a factual refinement.** Lockfile counts re-verified:
    exactly 38 multi-version names (framework) and 76 (CLI), matching
    Q-STA-01 V07. The crossterm split is real and both versions are linked in
    the shipped binary (0.28: TUI stack direct + ratatui + reedline; 0.29:
    comfy-table → polars-core → polars, pulled by EKO's `data`/`statistics`
    features). The reqwest half of the finding is **refined**: 0.13.3 is
    referenced only by `tauri 2.11.2` under
    `[target.'cfg(any(target_os = "android", all(target_vendor = "apple",
    not(target_os = "macos"))))'.dependencies.reqwest]` — a mobile-only,
    never-activated dependency. No default/`--all-features`/`--target all`
    resolution of the CLI workspace links it. The "two HTTP clients in the
    shipped app" wording of P3-03 overstates the case: the second HTTP client
    never ships on desktop; the finding's severity therefore drops toward
    "stale lockfile entry + hygiene" while the crossterm half stands.
  - **B-BASE-01-P2-01 (CLI CI never validates the shipped frontend) —
    current.** The workflow still contains no node/npm/frontend step (grep
    zero hits); the AGENTS.md conditional gate for `web-frontend/` changes
    remains unenforced in CI.
  - **B-BASE-01-P2-02 (CLI CI's framework input is unpinned and
    non-reproducible) — current.** The CLI workflow still checks out
    `EchoYue-lp/echo-agent` with `repository` + `path` only and no `ref`
    (line 18), so every CLI CI run compiles against the framework's current
    default branch — an unpinned supply-chain input that can flip a CLI
    commit green→red and is not reproducible from the CLI commit alone.
  - **No other drift**: workspace crate licenses (MIT), the single
    workspace build.rs, and the relative-path manifests (`../echo-agent`,
    `../../echo-agent`) all match B-BASE-01's inventory; the framework
    `data`-feature exclusion note (`echo-agent/Cargo.toml:59-63`) was
    verified present and is the anchor for the polars build-script
    observation in V04-01.

## Deviations

- The refinement of P3-03 is based on lockfile + tauri registry-source
  inspection; the mobile target is not buildable here, so "never links" is a
  static-graph conclusion, not an executed mobile build.

## Conclusion

Both previously filed dependency findings are current; Q-STA-01-P3-03 needs a
wording/severity refinement (reqwest 0.13.3 is lockfile-universe-only,
mobile-target-gated; crossterm duplicate is the real, shipped split). The
unpinned framework input in CLI CI (B-BASE-01-P2-02) is the main
supply-chain exposure already on file; it is referenced, not re-filed.

## Follow-Up

Task report Q-DEP-01 (write-up with findings), S-RDM-01 (roadmap: CI pin,
frontend CI job, npm updates, lockfile hygiene).
