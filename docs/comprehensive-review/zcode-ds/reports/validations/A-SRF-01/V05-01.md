# A-SRF-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents' TUI claims about the shared chat driver, reducer
coverage, HITL, cron, task controls, and surface parity either match current
code or are classified as stale/regressed with an anchor.

## Method

Grep of the TUI-relevant claims in:

```text
docs/MASTER-PLAN.md (root)             — lines 93, 107, 339, 454, 488-489, 733, 770-771
echo-agent-cli/docs/2026-07-17-surface-parity-closeout.md — lines 63, 72, 76, 106-117, 125
echo-agent-cli/docs/2026-07-11-tui-parity-design.md       — line 75
echo-agent-cli/docs/gui-status.md      — lines 56, 80
```

Each claim was checked against the current source anchors.

## Expected Result

Each documented claim classifies as current / fixed / stale / regressed
with a source anchor.

## Result

- Exit code: 0 (grep inspection)
- Summary:
  - MASTER-PLAN:93 "GUI/TUI/channel 已通过 drive_chat 统一驱动,差异主要在
    ChatSink" → **current** (TUI events.rs:2226, one TuiChatSink, V02-01).
  - MASTER-PLAN:107 "TUI 已接任务、plan、Subagent、HITL、记忆、附件、
    Browser/MCP 和会话恢复基础" → **current** (slash commands
    /tasks,/task-*,/plan,/steer,/memory,/attach,/browser,/mcp,/resume,
    /sessions; subagent bus subscription; TUI HITL provider — V02-01).
  - MASTER-PLAN:339 "新增字段或 terminal 状态时同一提交更新
    GUI/TUI/CLI/channel reducer" → **current with exception**: the TUI
    reducer keeps a `TurnStatus` arm (events.rs:880-886) and an
    `Interrupt` sink arm (events.rs:2045-2053) that no producer fires on
    the TUI path (P3-02; A-CHAT-01-P2-01).
  - MASTER-PLAN:488 "GUI 提供暂停/取消/恢复阻塞/retry/skip;TUI 提供
    /task-recovery、/task-retry、/task-skip" → **current** (TUI also has
    /task-cancel,/task-pause,/task-resume — events.rs:4305-4507).
  - MASTER-PLAN:489 "GUI task pause/cancel 清理 pending HITL;TUI approval
    future 被取消时只清理同一 request" → **current** (tui_provider.rs
    PendingCleanup).
  - MASTER-PLAN:733/770-771 "删除剩余单入口权威状态和无理由功能缺口;
    interrupt 不再静默丢失" → **regressed on interrupt** (dead wire
    variant, A-CHAT-01-P2-01 cross-check) and **open gap on workspace**
    (TUI has no workspace surface — P1-01).
  - surface-parity-closeout:63 "TUI free-form Input auto-approved" →
    **fixed** (tui_provider input_mode + approval keys; A-HITL-01 already
    classed fixed).
  - surface-parity-closeout:72 "reducers do not all cover budget/guard/
    memory recall" → **fixed** (TUI sink maps BudgetDecision/GuardTriggered/
    MemoryRecalled to notices, events.rs:2170-2183).
  - surface-parity-closeout:76 + :117 "TUI /cron sends prose instead of
    controlling the scheduler" → **fixed** (handle_tui_cron controls
    SchedulerRunner directly, events.rs:2232-2298).
  - 2026-07-11-tui-parity-design:75 "TUI 不并行维护第二份 session JSON" →
    **current** (single conversation projection via conversation_store).
  - gui-status.md:56 "Unified right workspace" → GUI-only panel claim,
    no TUI claim; the **absence** of any TUI workspace surface is the
    drift signal for P1-01 (AGENTS.md parity invariant).

## Deviations

None.

## Conclusion

Supports: MASTER-PLAN surface-parity claims for TUI are current except the
documented interrupt convergence (regressed at the wire level) and the
workspace capability (never documented as TUI, but required by the
AGENTS.md parity invariant — P1-01).

## Follow-Up

None.
