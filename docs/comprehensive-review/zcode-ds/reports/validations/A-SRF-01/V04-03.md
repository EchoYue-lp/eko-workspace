# A-SRF-01 / V04 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

The shared surface wire-contract tests (`surface_contract`, which covers the
`ChatDriverEvent` variants the `TuiChatSink` maps — including `TurnStatus`
and `Interrupt`) pass at the reviewed commit.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked surface_contract
```

## Expected Result

All surface_contract tests pass, exit code 0.

## Result

- Exit code: 0
- Summary: `test surface_contract::shared_driver_wire_contract_preserves_product_facts ... ok` — 3 passed, 0 failed, 646 filtered out; finished in 0.02s.
- Artifacts: none.

## Deviations

None. Note the contract test serializes `ChatDriverEvent::TurnStatus` and
`Interrupt` (surface_contract.rs:191-203) even though neither variant has a
production producer outside the GUI wrapper — consistent with
A-CHAT-01-P2-01 and this task's P3-02 (dead TUI arms).

## Conclusion

Supports: the shared wire contract compiles and serializes at the reviewed
commits; TUI sink mapping arms are contract-consistent. Does not prove the
variants are produced on the TUI path (they are not — see V01-01, V03-01).

## Follow-Up

None.
