# F-MEM-02 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

`SqliteStore` and `SqliteConversationStore` are valid independent framework
capability-menu options: pub types with docs, cfg-gated re-exports, and
demo-example usage with valid `required-features`; the echo-agent-cli's
non-use of SQLite is an application decision that does not justify deletion
(AGENTS.md deletion rules).

## Method

Static inspection procedure:

```text
1. Read echo-state/src/memory/sqlite_store.rs (pub struct, pub fn new/with_embedder,
   module-level rustdoc with Quick Start example) and sqlite_conversation.rs
   (pub struct, pub fn new).
2. Verify re-export chain: echo-state/src/memory/mod.rs:23-26 (cfg-gated modules),
   :47-50 (cfg-gated pub use); echo-agent/src/lib.rs:197-199 (prelude re-export
   with #[cfg_attr(docsrs, doc(cfg(feature = "sqlite")))]); root src/memory.rs:47
   (pub use echo_state::memory::*).
3. Check trait multi-implementation status: impl Store for SqliteStore
   (sqlite_store.rs:464), impl ConversationStore for SqliteConversationStore
   (sqlite_conversation.rs:97); sibling impls FileStore/InMemoryStore (echo-state
   src/memory/store.rs), EmbeddingStore (embedding_store.rs), FileConversationStore
   (file_conversation.rs).
4. Enumerate example usage: grep SqliteStore in echo-agent/examples/; verify each
   example's required-features entry in echo-agent/Cargo.toml.
5. Grep echo-agent-cli for SqliteStore/SqliteConversationStore/sqlite feature:
   echo-agent-app-core/Cargo.toml, Cargo.toml, src/**.
6. Verify docs.rs metadata contains "sqlite" (echo-agent/Cargo.toml
   [package.metadata.docs.rs]).
```

## Expected Result

All four checks pass: pub types documented and exported under the `sqlite`
feature; examples gated with valid feature names; CLI declares
`default-features = false` without `sqlite`; docs.rs lists `sqlite`.

## Result

- Exit code: not-applicable (read-only inspection)
- Duration: ~5 min
- Summary:
  - Pub API + docs: `pub struct SqliteStore` with `new`/`with_embedder`
    (sqlite_store.rs:60-79) and module-level Quick Start rustdoc
    (sqlite_store.rs:1-43); `pub struct SqliteConversationStore` + `new`
    (sqlite_conversation.rs:19-26). Both re-exported at echo-state
    memory/mod.rs:48,50 under `#[cfg(feature = "sqlite")]`; `SqliteStore`
    additionally re-exported in the root prelude (echo-agent/src/lib.rs:197-199)
    with a `doc(cfg)` badge; root docs reference it (src/memory.rs:6,37).
  - Trait menu status: `Store` has 4 implementations (InMemoryStore, FileStore,
    EmbeddingStore, SqliteStore); `ConversationStore` has 2 (FileConversationStore,
    SqliteConversationStore) — sqlite is one option of several, not a dead or
    shadowed implementation.
  - Example usage: demo27_sqlite_memory (required-features `["sqlite"]`,
    Cargo.toml:164-165), demo45_customer_service (`["sqlite","human-loop"]`,
    :168-169), demo46_data_analyst (`["sqlite"]`, :176-177),
    demo48_personal_assistant (`["sqlite","tasks","subagent"]`, :172-173).
    All feature names exist in the root manifest; demo27 exercises CRUD + FTS5 +
    namespace isolation + optional semantic search + agent integration, and
    demo46 exercises `with_embedder`.
  - CLI non-use: echo-agent-app-core/Cargo.toml:10-15 and echo-agent-cli
    Cargo.toml:50 declare `echo_agent` with `default-features = false` and a
    feature list that does not include `sqlite`; no `SqliteStore`/
    `SqliteConversationStore` usage exists anywhere in CLI source (grep, 0 hits).
  - docs.rs metadata: `[package.metadata.docs.rs]` features list includes
    `sqlite` (echo-agent/Cargo.toml).
- Log/artifact: none created.

## Deviations

None.

## Conclusion

Supports: sqlite backends are legitimate, documented, example-exercised
framework options; the CLI's non-enablement is an application decision
consistent with AGENTS.md ("echo-agent-cli 不启用 SQLite 是应用决策"), which
explicitly forbids deleting framework capability on that ground. Does not prove
behavioral correctness — covered by V02-V05.

## Follow-Up

None.
