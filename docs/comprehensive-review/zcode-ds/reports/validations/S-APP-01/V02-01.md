# S-APP-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every contradiction or duplicate among the A-phase task reports (and between
A-phase and Q-phase reports) is either reconciled to one canonical
ID/priority with the smallest relevant evidence re-opened, or preserved as an
explicit open question in the synthesis; no contradictory conclusion is
silently carried forward.

## Method

Identified from a full read of all 29 A-* reports plus Q-CLI-01, Q-GUI-01,
Q-WEB-01, Q-E2E-01, X-SRF-01, and the zcode-ds index README. For each
discrepancy, re-opened the smallest relevant source anchor at the reviewed
commits:

1. Priority conflict 1 — mid-wave store fault: A-TSK-03-P2-01 (filed P2) vs
   A-TSK-04-P1-02 (filed P1). Re-opened anchor:
   `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs`
   (Err branch, ~:570-582): `sed -n '560,585p'` — confirmed the Err branch
   emits `RunFailed`, notes the error, and `transition_run(..., Failed)`
   with **no** sibling Running-task cleanup (contrast: cancel path
   `finalize_cancelled_run_state`).
2. Priority conflict 2 — channels-only services: A-BOOT-01-P2-02 (filed P2)
   vs A-SRF-04-P1-02 (filed P1). Re-opened anchor:
   `echo-agent-cli/src/main.rs:355-405`: `sed -n '355,405p'` — confirmed the
   channels-only branch (`else` arm) awaits `run_channels_mode` and returns
   without calling `start_headless_services` (no scheduler, no background
   task service).
3. Priority conflict 3 — scheduler/background tokens never cancelled:
   A-BOOT-01-P3-02 (filed P3) vs A-SRF-04-P2-02 (filed P2). Re-opened
   anchors: `grep -n "scheduler.cancel_token\|tasks.cancel_token"`
   `echo-agent-app-core/src/state.rs` — only constructor clones at :662/:692/
   :699; `grep -rn "scheduler.cancel_token.cancel()\|tasks.cancel_token.cancel()"`
   over `src/` + `echo-agent-app-core/src/` — zero hits (no `.cancel()`
   producers).
4. Duplicate merge — live tool-event ingest keyed by producer `detail_ref`:
   A-SRF-03-P2-01 vs A-FE-02-P2-01. Re-opened anchor:
   `echo-agent-cli/web-frontend/src/stores/toolExecutionStore.ts:200-222`
   (`sed -n '200,222p'`) — confirmed live `ingest` stores by `tool.id` and
   appends to `idsByOwner`, while `executionIdentity` (owner, call_id) is
   used only at :112/:193/:195/:248 (hydration/merge paths). A-FE-02-P2-01
   additionally documents the missing status-rank guard; selected as
   canonical.
5. Count inconsistency — zcode-ds/README.md A-phase summary claims "20 P1
   findings across 12 tasks" while naming 14 task IDs summing to 21, and
   omits A-TSK-03-P1-01 and all three A-TSK-04 P1s. Cross-checked the task
   reports directly (see V01 census: 28 filed P1 records, 25 canonical).
6. ID slip — A-SRF-04-P2-02 header cites "A-BOOT-01-P3-03" as its
   cross-check target; A-BOOT-01-P3-03 is the PTY `close_all` finding, the
   correct ID for the never-cancelled tokens is A-BOOT-01-P3-02 (verified in
   the A-BOOT-01 report's own P3-02 body and A-SRF-02's cross-verified
   table).

## Expected Result

Each discrepancy resolves to one defensible canonical outcome backed by the
re-opened anchor: priorities reconciled per REPORTING.md definitions,
duplicates merged with backlinks, and the README count noted as a superseded
preliminary summary (README is not modified by this review).

## Result

- Exit code: not-applicable (static re-inspection; commands recorded above)
- Summary of outcomes:
  1. Re-rated canonical priority **P1** for the mid-wave fault (recovery
     error per REPORTING.md P1 definition: "concurrency/recovery error");
     A-TSK-03-P2-01's P2 was a likelihood-weighted rating, preserved as a
     note (trigger probability medium).
  2. Re-rated canonical priority **P1** for the channels-only services gap
     (violates the AGENTS.md surface-parity product invariant: "surface
     behavior that violates a product invariant"); A-BOOT-01-P2-02 retained
     as backlink.
  3. Re-rated canonical priority **P2** for the never-cancelled tokens
     (material consequence: process-exit-only stop produces the
     Paused-at-boot cron/background orphans of A-SRF-04-P2-01); A-BOOT-01-
     P3-02 retained as backlink.
  4. Merged A-SRF-03-P2-01 into canonical **A-FE-02-P2-01** (backlink
     retained in both directions).
  5. zcode-ds README's "20 P1 across 12 tasks" classified as a stale
     preliminary summary (predates the A-TSK-03/A-TSK-04 completions and is
     internally inconsistent); the synthesis's census-derived count (25
     canonical P1s) supersedes it. README left untouched per task rules.
  6. A-SRF-04-P2-02's cross-reference corrected to A-BOOT-01-P3-02 in the
     synthesis; recorded as a factual metadata correction, no re-run needed.
- Open questions preserved (minority/uncertain conclusions, not erased):
  A-INT-01-P2-03 (SSRF-style URL rejection: over-gating vs deliberate
  choice), A-TOOL-01-P2-01 (tool toggle: gate vs display-only), A-SUB-01-
  P2-04 / A-PLG-01-P1-01 (refresh vs document-boot-only scope decisions),
  A-TSK-06-P2-01 (wire vs delete the artifact projection), A-CHAT-01-P2-01
  (route interrupt through shared sink vs delete the variant), A-SRF-01-
  P2-02 (research workbench placement), channels concurrent-turn
  interleaving residual (X-SRF-01).

## Deviations

None.

## Conclusion

All identified contradictions are resolved to canonical outcomes with the
smallest relevant evidence re-opened at the reviewed commits; duplicates are
merged with backlinks; uncertain decisions are preserved as open questions.
This validation supports the "contradiction reconciliation" requirement.

## Follow-Up

None. S-X-01 should consume the canonical merge table when it reconciles
cross-repository duplicates.
