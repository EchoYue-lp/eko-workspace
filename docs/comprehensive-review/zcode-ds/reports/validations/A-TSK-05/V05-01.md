# A-TSK-05 / V05 / Attempt 01

> Status: passed (drift found and classified)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: same as V01-01

## Claim

Historical worktree claims in `echo-agent-cli/docs/MASTER-PLAN.md` and the two worktree design docs point at current code; any drift is classified.

## Method

Compare each claim to current code:

```text
grep -n "worktree\|work_tree" echo-agent-cli/docs/MASTER-PLAN.md
sed -n '1,90p' echo-agent-cli/docs/2026-07-25-logical-task-worktree-reuse.md
sed -n '1,140p' echo-agent-cli/docs/2026-07-22-unattended-worktree-lifecycle.md
grep -rn "repair\|stale" echo-agent-cli/docs/*.md
grep -n "run unisolated" echo-agent-cli/echo-agent-app-core/src/infra.rs   # comment-vs-code drift probe
```

## Expected Result

Each milestone claim either matches current code or is explicitly classified as current/fixed/stale/regressed with evidence.

## Result

- Exit code: not applicable (static comparison)
- Summary:
  - MASTER-PLAN "Unattended worktree lifecycle and review parity | Complete" (doc:59) — current: cleanup/merge/discard/list implemented in `worktree.rs:1093-1276`, surfaced via TUI (events.rs:2300-2410) and GUI (panels.rs:2049-2163, tauri/mod.rs:298-301).
  - MASTER-PLAN "Logical-task worktree reuse and content-aware cleanup | Complete" (doc:60) — current for reuse (V04-01); incomplete for "content-aware cleanup": no sweep exists for `eko-fork-*` resources (P2-02) and no stale-lock repair after crash (P2-01).
  - MASTER-PLAN:465 "worktree repair ... complete" — current only for the legacy `eko-unattended-*` surface (2026-07-22 repair doc); the fork namespace lacks the same repair (P2-01).
  - 2026-07-22 doc "unlocks inactive stale worktrees" — current for `eko-unattended-*` via `cleanup_unattended_worktrees` (worktree.rs:1238-1276) with `is_run_active` (store.rs:554-559); caveat: the force-unlock/`--force` remove path is cross-process-unsafe (recorded in P2-02 direction).
  - 2026-07-25 doc (at-most-one worktree per logical task, lock = active ownership, retry relock/reuse, clean removal, dirty retention) — current, verified by tests (V04-01).
  - Drift found: infra.rs:396-400 comment "if it's not a git repo, no factory is injected (subagents declaring isolation log a warning and run unisolated — the framework's default)" contradicts the framework hard-fail at `echo-agent/src/agent/subagent/executor.rs:1609-1619` ("refusing to run without isolation") and integration's git-root requirement (executor.rs:916-919). Classification: regressed (comment) / behavior is hard-fail (finding A-TSK-05-P3-01).
- Log/artifact: none.

## Deviations

None beyond the classified drift.

## Conclusion

Supports: historical claims are largely current; two "complete" claims are incomplete for the fork namespace (P2-01/P2-02), and one comment-vs-behavior drift exists (P3-01).

## Follow-Up

None.
