# A-TSK-05 / V01 / Attempt 01

> Status: passed (duplicate authority found and documented)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: `echo-agent` has pre-existing untracked `check_*` scratch files; `echo-agent-cli` has modified `web-frontend/src/generated/*.ts` (ts-rs codegen side effect of cargo test; pre-existing pattern)

## Claim

There is exactly one live EKO worktree/file-shadow/ownership authority in `echo-agent-cli` plus one generic framework trait seam, with no second live implementation of the same semantics; any parallel definition is classified as duplicate or distinct-purpose.

## Method

Repository-wide grep over both repositories (excluding `target/`), for concept names and behavior names:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
grep -rln "worktree\|work_tree\|work-tree" echo-agent-cli/echo-agent-app-core/src
grep -rn "fn default_worktree_path|fn parse_worktree_list|fn validate_worktree_target|repo_merge_lock|fork_branch_name|FORK_BRANCH_PREFIX|BRANCH_PREFIX" echo-agent echo-agent-cli --include="*.rs"
grep -rn "impl.*WorktreeFactory for|impl.*DataWorkspaceFactory for" echo-agent echo-agent-cli --include="*.rs"
grep -rn "FileOwnership|analyze_file_ownership|has_writer_file_overlap" echo-agent-cli/echo-agent-app-core/src --include="*.rs"
grep -rn "FileTaskShadow|shadow" echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime --include="*.rs"
grep -rn "isolation_id|isolate_worktree|isolate_workspace" echo-agent echo-agent-cli --include="*.rs"
grep -rn "cleanup_unattended|merge_unattended|discard_unattended|UnattendedWriteMode|enter_worktree|exit_worktree" echo-agent echo-agent-cli --include="*.rs"
```

## Expected Result

One EKO authority (`tasks/task_runtime/worktree.rs` for git worktrees, `planner.rs` for ownership, `file_shadow.rs` for file persistence), one framework trait (`echo-agent/src/agent/subagent/worktree.rs` + `workspace.rs`), and no parallel live implementation of the same lifecycle semantics.

## Result

- Exit code: not applicable (static grep)
- Summary:
  - EKO git-worktree authority: `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/worktree.rs` (RunWorktree lifecycle, integration, unattended management, EkoWorktreeFactory). Live consumers: `executor.rs`, `src/tui/events.rs`, `src/tauri/commands/panels.rs` (unattended commands delegate to it).
  - Duplicate found: `echo-agent-cli/src/tauri/commands/panels.rs` re-implements `parse_worktree_list` (:1828), `default_worktree_path` (:1908), `validate_worktree_target` (:1929), `validate_branch_name` (:1883) plus commands `list_worktrees`/`create_worktree`/`remove_worktree` (:1957-2047), all registered (`src/tauri/mod.rs:295-297`) and consumed by the frontend (`web-frontend/src/api/endpoints.ts:1972-1984`). The panels parser does not parse `locked` lines that the shared parser handles — already divergent.
  - Framework trait seam: `echo-agent/src/agent/subagent/worktree.rs` (WorktreeFactory, NoWorktreeFactory) and `workspace.rs` (DataWorkspaceFactory); single production impl each: `EkoWorktreeFactory` (worktree.rs:1297), `EkoDataWorkspaceFactory` (worktree.rs:1391).
  - `echo-tools/src/git_worktree.rs` + `worktree_tool.rs` (framework `enter_worktree`/`exit_worktree` model-facing tools, F-EXT-02 scope) are a separate surface not used by the TaskRuntime pipeline — not a duplicate authority in the task path.
  - Ownership: single `planner.rs` `FileOwnership`/`analyze_file_ownership`; consumers: `executor.rs` (wave filter, per-file locks, integration), `subagent_prompt.rs` (boundary prompt), `worktree.rs` (merge validation). `has_writer_file_overlap` has no production caller beyond the module re-export (`mod.rs:61`).
  - File shadow: single `FileTaskShadow` (`file_shadow.rs`), consumed by `file_store.rs` and `store.rs`; no second shadow/persistence authority.
  - Run/merge locking: `repo_merge_lock` (worktree.rs:39-57) used by `executor.rs:925`, `tui/events.rs:2335/2362/2393`, `panels.rs:2097/2130/2156`; legacy `panels.rs` create/remove commands do NOT take it.
- Log/artifact: none (grep output inline above).

## Deviations

- `default_worktree_path` exists twice (app-core `worktree.rs:218` and `panels.rs:1908`); the module doc at `worktree.rs:20-22` claims centralisation that has not removed the panels.rs copies (finding A-TSK-05-P2-04).
- No cleanup sweep exists for the `eko-fork-*` namespace (only `eko-unattended-*` via `BRANCH_PREFIX`) — the grep for `FORK_BRANCH_PREFIX` outside `worktree.rs` yields only `executor.rs:937` (event payload) (finding A-TSK-05-P2-02).

## Conclusion

Supports: one live authority per concept in the task pipeline; the panels.rs GUI helpers are a live duplicate with observed divergence. Does not prove: whether the divergence has user-visible effect beyond the lock-state field (checked in V03/V04 contexts).

## Follow-Up

None; feeds task report layering decision and findings A-TSK-05-P2-02/P2-04.
