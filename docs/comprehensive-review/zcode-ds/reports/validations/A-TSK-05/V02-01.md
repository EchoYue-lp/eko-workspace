# A-TSK-05 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: same as V01-01 (pre-existing generated/scratch dirt, no review writes)

## Claim

The full worktree lifecycle is registered and runtime-reachable from live entry points: factory injection → framework dispatch → finalize; integration after review; unattended management from TUI/GUI.

## Method

Static trace of definition → registration → live caller for each surface:

```text
1. Factory injection: echo-agent-cli/echo-agent-app-core/src/infra.rs:401-416 (git_repo_root best-effort -> EkoWorktreeFactory::new -> builder.subagent_worktree_factory) and :423-426 (EkoDataWorkspaceFactory) -> echo-agent/src/agent/config.rs:80-81,413-417 -> echo-agent/src/agent/react/mod.rs:428 (SubagentExecutorConfig.worktree_factory) -> echo-agent/src/agent/subagent/executor.rs:1603-1618 (isolate check + hard-fail) -> :1706-1717 (factory.create) -> :1836-1855 (finalize on every exit path).
2. Dispatch: execute_task (executor.rs:1843) routes Implementation|Debugging to run_writer_subagent (:2204-2227) -> framework delegate_to_agent_with_prompt_payload with ExternalRunContext.isolation_id = {run_id}:{task_id} (executor.rs:2894,2933) -> framework worktree_label = "{role}-{isolation_id}" (executor.rs:1658-1670) matches EKO integration label task_worktree_label (executor.rs:196-198).
3. Integration: resolve_dispatch (executor.rs:1518-1559) -> integrate_reviewed_task (:1686-1752) -> RealTaskDispatcher::integrate (:889-1029): repo root from primary agent working_dir, repo_merge_lock, spawn_blocking(integrate_fork_worktree) -> merge outcome events MergeStarted/MergeCompleted/MergeFailed.
4. Unattended surface: TUI src/tui/events.rs:2300-2410 (list/cleanup/merge/discard + repo_merge_lock), GUI src/tauri/mod.rs:298-301 + panels.rs:2049-2163, frontend endpoints.ts:1987-2001.
5. Legacy GUI panel: tauri/mod.rs:295-297 + panels.rs:1957-2047, frontend endpoints.ts:1972-1984.
```

## Expected Result

Every claimed surface resolves to the app-core authority and the framework seam; label identity at dispatch equals the label used at integration.

## Result

- Exit code: not applicable (static trace)
- Summary:
  - Injection chain verified end-to-end; `AgentConfig.subagent_worktree_factory` reaches `SubagentExecutorConfig.worktree_factory` (react/mod.rs:428) and `dispatch_fork` uses `self.config.worktree_factory` (executor.rs:1604-1605).
  - Isolation decision is made by the framework from `SubagentDefinition.isolate_worktree` (executor.rs:1603); EKO's writer routing (`run_writer_subagent`) passes no isolation flag and performs no role-isolation check — a writer role without `worktree: true` runs in the main checkout (finding A-TSK-05-P2-03). Builtin `general-purpose` role declares `readonly: false, worktree: false`.
  - Label identity: EKO passes `isolation_id = "{run_id}:{task_id}"` (task_isolation_id, executor.rs:192-194); framework label = "{agent_name}-{run_id}:{task_id}" (executor.rs:1658-1670); integration label = task_worktree_label("{role}-{run_id}:{task_id}", executor.rs:196-198). Match when the dispatched role equals task.agent_role (always true in this path).
  - Finalize runs on every exit path of the spawned dispatch task incl. cancel/timeout (executor.rs:1836-1855 after result assignment); cancellation does NOT leak locks; only process death (crash) leaves `git worktree lock` files (finding A-TSK-05-P2-01).
  - Unattended and legacy GUI surfaces are registered and consumed by the frontend.
- Log/artifact: none.

## Deviations

- infra.rs:396-400 comment claims non-git projects run unisolated with a warning; the framework hard-fails instead (executor.rs:1609-1619) (finding A-TSK-05-P3-01).
- The factory `create` and finalize execute blocking git subprocesses inline in the async dispatch task (not spawn_blocking, unlike integration) (finding A-TSK-05-P3-02).

## Conclusion

Supports: lifecycle wiring is complete and reachable from all four surfaces; label identity is consistent; isolation is definition-driven with no EKO enforcement (P2-03); crash-stale locks have no repair path (P2-01).

## Follow-Up

None.
