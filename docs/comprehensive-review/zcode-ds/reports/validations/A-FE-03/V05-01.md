# A-FE-03 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: dirty — 79 generated TS files only (pre-existing); generated-dir md5 unchanged.

## Claim

Historical documents that make frontend architecture/performance/state-ownership claims are classifiable as current, fixed, stale, or regressed against the reviewed code, and none silently carry forward.

## Method

Claim-to-code comparison over the documents declared in the A-FE-03 inputs:

```text
1. docs/MASTER-PLAN.md:124 (frontend only changes policy/rendering, not state semantics), :312 (attachment data-URL persistence is a frontend-owned contract), :789 (frontend gate claim "56 Vitest").
2. echo-agent-cli/docs/2026-07-25-gui-tool-execution-lazy-loading.md:32 (lazy tool payloads, no full render), :101 (collapsed InlineToolCall single-line summary).
3. echo-agent-cli/docs/gui-status.md (connected-surface matrix; no rendering/a11y claims).
4. echo-agent-cli/docs/2026-07-16-agent-lifecycle-audit.md:161 (P0-4 crash recovery — unrelated to chatStore's "P0-4" comment, checked for numbering collision).
5. chatStore.ts:104 "P0-4" comment referencing a message-retention cap — searched MASTER-PLANs/audits for any claim about a 500-message cap or virtualization.
```

## Expected Result

Each claim maps to current code or is marked stale/regressed with evidence; no doc claims a message-cap/virtualization design that code contradicts.

## Result

- Exit code: not-applicable (static)
- Summary:
  - MASTER-PLAN:124 "前端只改变策略或渲染，不改变状态语义" — **regressed** (already established in A-SRF-03 V05-01 via P1-01/P1-02): the frontend reducer re-produces terminal status ('completed' on error, chatStore.ts:354-362) and the hook owns a turn lifecycle with no backend counterpart for the interrupt path. Confirmed unchanged at the reviewed commit.
  - MASTER-PLAN:312 data-URL attachment contract — **current**: conversationStore persists `data:` URLs, MessageBubble.tsx:241 renders `img.url`; no change.
  - MASTER-PLAN:789 "56 个 Vitest" — **stale** (count drift): the suite is now 26 files / 101 tests (V04-01); the claim's gate-passing substance still holds (tsc/build/vitest pass at the reviewed commit).
  - lazy-loading doc:32/101 — **current** for lazy tool output (InlineToolCall collapses to a single summary line, expands on demand, 256 KiB live cap, toolExecutionStore.ts:23, InlineToolCall.tsx:23/91-105); the doc's single-producer assumption is broken by the double producer (A-SRF-02-P2-01) — already classified regressed in A-FE-02 V05-01; not re-filed.
  - gui-status.md — **current** with caveats: the connected-surface matrix concerns backend connectivity, not rendering; "Chat and streaming: Connected" holds at the surface level with the A-SRF-03 P1-01/P1-02 caveats (cross-referenced, not re-filed). No a11y/perf claims exist to drift.
  - lifecycle-audit P0-4 (crash recovery, audit:161) — **current but unrelated**: chatStore's comment "P0-4" (chatStore.ts:104) references a different numbering (a message-retention cap); no historical doc describes a 500-message cap or list virtualization, so the cap is an undocumented implementation detail and no doc is contradicted by it.
  - No historical claim exists about chat-list virtualization, per-token render cost, icon-button a11y, or modal semantics — the P2-01/P3-01/P3-02 findings are new observations, not regressions of documented designs.

## Deviations

None.

## Conclusion

Supports: one regressed claim (MASTER-PLAN:124, cross-owned by A-SRF-03), one stale count (MASTER-PLAN:789), everything else current or not-applicable; no drift claim was silently carried forward. Does not prove anything about documents outside the A-FE-03 input list.

## Follow-Up

None.
