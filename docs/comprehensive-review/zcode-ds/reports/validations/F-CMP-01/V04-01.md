# F-CMP-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The `echo_state` compression test suite passes on the reviewed commits:
`cargo test -p echo_state --lib --locked compression::` exits 0 with zero
failures.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_state --lib --locked compression::
```

Toolchain: rustc/cargo as configured in the workspace (`--locked`). No feature
flags (echo_state default features cover the compression module).

## Expected Result

Exit 0, zero failures. The run covers the ContextManager, sanitizer,
projections, memory promotion, verifier, horizon, and adaptive invariant tests
(the `compression::` namespace).

## Result

- Exit code: 0
- Duration: 0.02s test phase (after compile)
- Summary: 69 passed, 0 failed, 0 ignored, 62 filtered out (includes
  `compression::tests::*`, `compression::verifier::tests::*`,
  `compression::horizon::tests::*`, `compression::invariants::tests::*`).
- Log/artifact: full output captured in
  `/Users/ls/.zcode/cli/exec/sess_subagent_agent_eea677b2-3897-4832-9f21-74549d3f0571/call_00_NHKsBfL1uom8maPd2CWN5317-stdout.log`.

## Deviations

The existing tests cover boundary splits (sanitizer), protected markers,
projection replacement, token reduction (>30%), and idempotency by message
count. They do NOT cover: post-compression tokens <= token_limit with a fixed
window and few large messages (P1-01 stall); summary accumulation across
repeated passes (P1-02); adaptive L1 fold tool-result contiguity after
sanitize (P1-03); verifier false-fail fallback fact loss (P2-01). These remain
static findings, not test failures.

## Conclusion

Supports: the shipped suites are green at the reviewed commits; the gaps above
are coverage gaps, not regressions — they become findings via V03-01.

## Follow-Up

None (no rerun needed at these commits).
