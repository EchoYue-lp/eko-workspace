# F-CMP-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical design/operator documents about compression map to current code as
current, fixed, stale, or regressed (no unclassified reuse of old claims).

## Method

Grep and read the compression-related claims in:
`docs/MASTER-PLAN.md` (Phase C lines 271-277, L3 line 895, M9 line 379),
`echo-agent-cli/docs/configuration.md` (lines 61-63), `echo-agent/docs/zh/04-compression.md`,
`echo-agent/docs/{en,zh}/28-config-reference.md`; compare each claim to the code
anchors verified in V03-01.

## Expected Result

Each claim classifiable with a code anchor or validation link.

## Result

- Exit code: not applicable (document inspection)
- Summary of classifications (details in the task report's Historical Claim
  Status table):
  1. MASTER-PLAN Phase C "压缩后校验 tool_call/tool_result 配对" — current
     (`sanitize_tool_call_pairing` runs on every prepare, mod.rs:1451).
  2. MASTER-PLAN Phase C "run/task identity、已完成副作用和 pending
     interaction" verification — stale: the runtime verifier checks only
     tokens/summary/last-query/file-paths/TODO/errors/preferences
     (verifier.rs:40-96), and only when a summary exists; no run/task identity
     or side-effect check exists.
  3. MASTER-PLAN Phase C "限制 protected message 数量和体积" — stale: no
     size/count cap on protected content (only the 2000-char rules cap in
     `to_reinjection_messages`, echo-core/src/compression.rs:385).
  4. MASTER-PLAN:895 L3 dual-writer risk — current with mitigation: both
     `StoreMemoryPromoter` and `pre_compaction_flush` write via the same
     content-hash key `l3_{hash}` (`memory_promoter.rs` content_key /
     `durable_memory_content_key`), and `pre_compaction_flush` skips when
     `locate(key)` matches content (context.rs:790-794) — dedup works; the
     plan's own "二选一或加内容去重" option was implemented.
  5. MASTER-PLAN M9 durable compression timeline — current
     (`RunEvent::ContextCompression` emission, compact.rs:74-84;
     CompressionCheckpoint fields; EKO /context command).
  6. `echo-agent-cli/docs/configuration.md:61` "token_limit: 0 … 0 = 禁用" —
     regressed: with `token_limit: 0` EKO still passes
     `DEFAULT_CONTEXT_WINDOW` (infra.rs:258-262), `new_inner` installs
     SlidingWindow(40) (react/mod.rs:346-353), and `apply_compressor` installs
     the strategy (default "summary", config.rs:482) — compression cannot be
     disabled through this documented knob.
  7. `echo-agent-cli/docs/configuration.md:62` / 28-config-reference.md:
     strategy list "sliding / summary / hybrid" — stale: code also supports
     "adaptive" (config.rs:256-271).
  8. `echo-agent/docs/zh/04-compression.md` "长对话支持…不因上下文过长而崩溃"
     — regressed for long sessions by the count-based windows and summary
     accumulation (P1-01/P1-02); IncrementalSummaryCompressor is documented as
     a usable strategy but has no production construction site (dormant).

## Deviations

None.

## Conclusion

Supports: the doc-drift findings P3-02 (EKO "0 = 禁用" claim) and the
classification rows in the task report; confirms no other historical claim was
carried forward unclassified.

## Follow-Up

None.
