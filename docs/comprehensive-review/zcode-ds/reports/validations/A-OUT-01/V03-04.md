# A-OUT-01 / V03 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories); scratch files under /tmp/eko-out-check only

## Claim

Rust `format!` width/padding is character-based while `str::len()` is
byte-based, so the TUI table renderer's byte-derived column widths
(`tui/markdown.rs:388,414-415,419`) produce misaligned output and inconsistent
truncation thresholds for CJK/emoji cells. This is a dynamic confirmation of
the V03-01 static finding — executed OUTSIDE the repository (repo source is
read-only).

## Method

Standalone Rust program at `/tmp/eko-out-check/main.rs`, compiled with
`rustc -O main.rs -o check`, printing for `cell = "数据分析结果"` (6 chars /
18 bytes) and `emoji = "🚀🚀🚀"` (3 chars / 12 bytes):

1. `width = cell.len().min(40)` (the renderer's width formula).
2. `format!("{:<width$}│", cell, width = width)` (the renderer's padding).
3. `format!("{:.3}...", cell)` (the renderer's precision truncation).
4. `cell.len() > width` (the renderer's truncation condition).
5. Char-based width alternative for comparison.

Command:

```text
cd /tmp/eko-out-check && rustc -O main.rs -o check && ./check
```

## Expected Result

If the claim holds: byte-width is larger than char-width for CJK/emoji cells,
padding consumes the byte-derived width as if it were characters, and the
truncation condition uses bytes while the truncation itself cuts characters.

## Result

- Exit code: 0
- Duration: < 1 s
- Output:

```text
bytes=18 chars=6 width=18
padded byte len=33 (chars=19)
truncate-precision-3=数据分... chars=6
is_truncated(4-chinese-cell, width=12) = false
correct padded byte len=21
emoji bytes=12 chars=3
```

Interpretation: a 6-char CJK cell gets `width=18`; the renderer then pads to
18 characters (`{:<18}`), i.e., 12 extra display columns versus the char-based
width (correct padding would produce 21 bytes vs the observed 33 bytes);
`{:.3}` truncates by characters, so the byte-based threshold `cell.len() >
width` decides truncation in a different unit than the truncation applies.

## Deviations

None.

## Conclusion

Supports (dynamically) the byte/char confusion in the live TUI table renderer
— finding A-OUT-01-P2-02. The mismatch is a display defect (misaligned
columns, wrong truncation thresholds for CJK/emoji), not a panic.

## Follow-Up

None.
