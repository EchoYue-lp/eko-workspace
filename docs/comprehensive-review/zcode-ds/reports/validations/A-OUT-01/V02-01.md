# A-OUT-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every output/export surface's definition -> registration -> live-caller chain
is either fully wired or provably unreachable, and the surface availability
matrix (GUI / TUI / CLI / agent tool) is known.

## Method

Static call-graph tracing (no process launched; read-only review):

1. `grep -n "invoke_handler\|export_conversation\|export_systematic_review\|run_analysis" echo-agent-cli/src/tauri/mod.rs` — Tauri command registration.
2. `grep -n "register_all" echo-agent-cli/src/cli/repl.rs` — CLI registry assembly; `cmd_impls/advanced.rs` / `research.rs` `cmd!` registrations.
3. `grep -n "ResearchLibraryTool" echo-agent-cli/echo-agent-app-core/src/runtime.rs:287` — agent tool registration.
4. `sed -n '53,140p' echo-agent-cli/src/tui/commands.rs` + `sed -n '2622,2626p;4683,4688p' echo-agent-cli/src/tui/events.rs` — TUI slash-command enum and unknown-command fallback.
5. `grep -rn "conversationApi.export\|systematicReviewsApi.export\|ReviewWorkbench" echo-agent-cli/web-frontend/src` — frontend consumers.
6. `sed -n '905,960p' echo-agent-cli/web-frontend/src/api/endpoints.ts` — `analysisRequiresDesktop` / `researchRequiresDesktop` web-mode rejection.
7. `grep -rn "RightWorkspace\|PaperPanel" echo-agent-cli/web-frontend/src/components/layout/RightWorkspace.tsx:113-119` — GUI panel mounting.

## Expected Result

A complete, per-surface table of which export/output capability is reachable
where.

## Result

- Exit code: not applicable (static)
- Summary — reachability table:

| Capability | GUI (Tauri) | TUI (default surface) | CLI REPL | Agent tool |
|---|---|---|---|---|
| Systematic-review export (md/pdf/docx/json/csv/bib/ris) | live — `export_systematic_review` registered (mod.rs:284), `systematicReviewsApi.export` -> `ReviewWorkbench.tsx:155-169` exportReview | **absent** — `SlashCommand` enum (`tui/commands.rs:53-140`) has no Papers/Research/Export; unknown commands rejected `events.rs:4683-4688` | live — `/papers export` (`cmd_impls/research.rs:265-277`, registered repl.rs:174) | live — `research_library` tool `export_review` (`research_tool.rs:164-182`, registered runtime.rs:287) |
| Session export | **dead endpoint** — `export_conversation` registered (mod.rs:217) but `conversationApi.export` (endpoints.ts:437-441) has zero component callers; HTTP fallback route has no server | absent | live — `/export` (`cmd_impls/advanced.rs:166-217`, registered repl.rs:168) | n/a |
| Output-format switching | none | none | **no-op stub** — `/output` (`advanced.rs:265-279`) prints and returns; no state, no renderer change | n/a |
| Analysis workbench | live — `analysisApi` -> `commands/analysis.rs` (`run_analysis` mod.rs:92) | live — `/analysis` in enum (`tui/commands.rs` Coding section); `cmd_impls/analysis.rs` shares `run_analysis_with_agent` | live — `/analysis` | n/a (task-driven) |
| Review renderer availability | `document_renderer_available` / `pdf_renderer_available` (`research.rs:1194-1204`) drive GUI format list and `export_all_review_formats` (`:1171-1191`) | n/a | same authority | same authority |

Registration facts: `export_systematic_review` and `export_conversation` are
both in the Tauri `invoke_handler`; `OutputCommand`/`ExportCommand`/
`PapersCommand` are all registered into the CLI registry (repl.rs:161-174,
advanced.rs:215,230, research.rs:341-355); `ResearchLibraryTool` added at
runtime.rs:287. The TUI does not route unknown slash commands to the CLI
registry — `None => "Unknown command: {command}"` (events.rs:4683-4688).

## Deviations

- The GUI conversation-export endpoint (`export_conversation`) is registered
  but has no UI consumer — dead registration.
- The TUI (the default entry surface) has no research-library or session-export
  command at all; the capability is GUI+CLI only.

## Conclusion

Supports: the availability matrix and the finding that session export has two
divergent implementations (`cmd_export` over the live runtime context vs
`export_conversation` over the persisted store) with no canonical consumer;
the TUI research-surface gap; the `/output` no-op stub. Does not prove what
the exported content looks like — V03 covers content fidelity.

## Follow-Up

Next: V03-01 (UTF-8/large content), V03-02 (missing converter), V03-03
(artifact identity/delivery), V03-04 (dynamic format-semantics check).
