# A-OUT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every historical claim about EKO output/export used by this task is
classified against current code as current / fixed / stale / regressed.

## Method

Inspection of `echo-agent-cli/docs/MASTER-PLAN.md`,
`echo-agent-cli/docs/architecture.md`, `echo-agent-cli/README.md`, and
`echo-agent-cli/echo-agent-app-core/src/output/mod.rs` module docs, then
verification against current code (grep + read at the cited anchors).

## Expected Result

A drift table with per-claim classification and current-code anchors.

## Result

- Exit code: not applicable (static)
- Summary:

| Source claim | Classification | Current evidence |
|---|---|---|
| MASTER-PLAN.md:47 — "Citation audit and systematic-review report export Complete — file-backed Markdown, JSON, CSV, BibTeX, RIS under each review's `reports/`" | current | `research.rs:1123-1169` exports to `{ws}/research/reviews/<id>/reports/`; test V04-04 |
| MASTER-PLAN.md:52 — "PDF/DOCX systematic-review rendering Complete — Pandoc/Quarto discovery with selectable PDF engine and portable-format fallback" | current (discovery/engine) / partially stale ("portable-format fallback": no automatic fallback — Pdf/Docx error when the renderer is missing, by design, with clear causes) | `research.rs:1215-1233,1285-1290`; V03-02 |
| MASTER-PLAN.md:54 — "conversation export remains canonical" | stale | `export_conversation` (`src/tauri/commands/conversations.rs:642-662`) is registered but has zero frontend consumers; the live session export is CLI `/export` (`cmd_impls/advanced.rs:166-217`) which reads the raw runtime context — there is no canonical, consumed conversation-export path (A-OUT-01-P2-01/P3-01) |
| MASTER-PLAN.md:273-274 — "Pandoc 3.9.0.2 plus Typst produced real DOCX/PDF artifacts through the application export path" | current-shaped | pandoc fixture test `pandoc_renderer_produces_pdf_and_docx_bytes` (research.rs, unix-gated); V04-04 |
| `docs/architecture.md:120` — `Research { topic, max_papers, output_format: ResearchOutputFormat }` | stale (field documented but never read) | `tasks/background.rs:26` field; `to_prompt` ignores it (`:161-174`); all creation sites hardcode Markdown (A-OUT-01-P2-03) |
| README.md:387 — "`/output` 切换输出格式" | stale (command is a no-op stub) | `cmd_impls/advanced.rs:265-279` prints "Output format: {f}" and changes nothing (A-OUT-01-P2-03) |
| README.md:386 — "`/export` 导出会话" | current (exists) with fidelity defect | `cmd_impls/advanced.rs:166-217`; content = unfiltered runtime context (A-OUT-01-P2-01) |
| `output/mod.rs:6-7` — "`OutputRenderer` is the single facade; REPL and TUI both output through it" | stale | TUI uses `tui/markdown.rs`; REPL uses a 13-method subset; markdown/code-block/table methods uncalled (A-OUT-01-P3-01) |
| `output/format.rs:8` — "OutputFormat 用于 --output / -o 标志" | stale (no such flag exists) | `src/cli/args.rs` has no output flag; `OutputFormat` has zero production callers (A-OUT-01-P2-03/P3-01) |

## Deviations

None.

## Conclusion

Supports: the historical-claim table in the task report. The export machinery
that MASTER-PLAN declares "Complete" (review export) is genuinely complete and
tested; the conversation-export and output-format claims are stale relative to
current code.

## Follow-Up

None.
