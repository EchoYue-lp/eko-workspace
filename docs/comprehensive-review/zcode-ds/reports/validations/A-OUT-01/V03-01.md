# A-OUT-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Output/export render paths are UTF-8 safe (no byte/char confusion) and handle
large content without truncating or corrupting it, per AGENTS.md UTF-8 rules.

## Method

Static scan of every live and dead render/truncate site in the output/export
paths:

1. `src/tui/markdown.rs` `render_table` (`:373-450`) — column-width computation
   `cell.len().min(40)` (`:388`), truncation condition `cell.len() > width`
   (`:414`), precision truncation `format!("{:.width$}...", cell, ...)` (`:415`),
   padding `format!(" {:<width$} │", truncated, width = width)` (`:419`).
2. app-core `output/markdown.rs` `render_table` (`:298-325`) — `header.len()`
   (`:311`) vs `cell.chars().count()` (`:303`).
3. app-core `output/mod.rs` `print_tool_call` (`:207-212`) and
   `print_tool_result` (`:224-226`) — `chars().take(200/300)`.
4. `research.rs` stderr truncation `String::from_utf8_lossy(...).chars().take(2_000)` (`:1313-1316`); `to_string_lossy` for export paths (`:1160`).
5. Large-content behavior: `export_review` writes unbounded markdown via
   `atomic_write` (`research.rs:1123-1169`); `MAX_JSON_BYTES = 4 MiB` read/write
   cap (`:24,1968-1985`); TUI streaming re-render per chunk
   (`tui/mod.rs:1207` `render_markdown(streaming_text, &theme)` on every
   stream event — full re-parse + syntect re-highlight per token chunk).
6. `cmd_export` (`cmd_impls/advanced.rs:166-217`) — content selection for the
   exported file: `ctx.messages()` (runtime context, `react/mod.rs:1260-1263`),
   markdown branch uses `as_text_ref()` (Text variant only), JSON branch
   `as_text()` joins text parts; system-prompt messages are included
   (`Role::System` confirmed in `echo-core/src/llm/types.rs:152-172`).

## Expected Result

All truncation is char-based; table geometry uses one width unit; exported
content matches the canonical user-visible transcript projection
(`filter_user_visible_transcript`, `echo-agent/src/agent/snapshot.rs:65-71`).

## Result

- Exit code: not applicable (static; dynamic confirmation in V03-04)
- Summary:

| Site | Behavior | Verdict |
|---|---|---|
| `tui/markdown.rs:388` | `cell.len().min(40)` — BYTES for the column width | FAIL (byte/char confusion) |
| `tui/markdown.rs:414-415` | truncation decided on bytes, executed on chars (`{:.width$}` precision is char-based) — inconsistent thresholds | FAIL |
| `tui/markdown.rs:419` | `{:<width$}` pads to `width` CHARS while width was computed in bytes — CJK/emoji cells over-padded, columns misaligned | FAIL |
| `output/markdown.rs:311` | `header.len().min(40)` bytes mixed with char-count cells | FAIL (dead code — only reachable via unused `render_table`) |
| `output/mod.rs:207-212,224-226` | `chars().take()` | PASS |
| `research.rs:1313-1316,1160` | char truncation + lossy path conversion | PASS |
| `export_review` large content | no cap, no silent truncation; atomic write; 4 MiB JSON guard on records | PASS |
| TUI streaming (`tui/mod.rs:1207`) | full re-render of the accumulated message per chunk — O(n^2) for long outputs | WARN (performance, bounded by message size) |
| `cmd_export` content | unfiltered runtime context incl. `### system` (system prompt) + tool-role messages; `Parts`-only content silently dropped (`as_text_ref` -> None) | FAIL (fidelity invariant) |

## Deviations

The TUI table renderer (the live, default-surface markdown path) mixes byte
and char units; the `/export` path exports the unfiltered runtime transcript
rather than the canonical user-visible projection and silently drops
non-Text content.

## Conclusion

Supports findings A-OUT-01-P2-02 (TUI table byte/char mismatch, live surface)
and A-OUT-01-P2-01 (`/export` fidelity). The dead app-core renderer has the
same bug class but is unreachable (folded into P3-01). Large-content handling
of the review-export authority itself is sound.

## Follow-Up

V03-04 dynamically confirms the Rust formatting semantics claimed here.
