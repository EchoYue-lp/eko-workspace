# A-OUT-01 / V04 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The app-core `research` module (file-backed library, export formats,
missing-converter behavior) compiles and its tests pass at the current commit;
live-network provider tests are opt-in.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli && cargo test -p echo-agent-app-core --lib --locked research
```

## Expected Result

Exit code 0; the export-format test
(`citation_audit_and_all_report_formats_are_file_backed`) passes and the
pandoc fixture test (`pandoc_renderer_produces_pdf_and_docx_bytes`, unix-gated)
passes; live provider smoke tests are `#[ignore]`d.

## Result

- Exit code: 0
- Duration: ~2 s (incremental)
- Summary: `test result: ok. 6 passed; 0 failed; 2 ignored; 0 measured; 641
  filtered out`. The 2 ignored tests are the live-provider smokes in
  `research_connectors.rs:616,649` (`EKO_PROVIDER_SMOKE=1` / `ZOTERO_API_KEY`
  opt-in — same `not_run` class as F-EXT-03). The export test asserts the five
  renderer-independent artifacts (Markdown/Json/Csv/Bibtex/Ris) are written
  under the workspace and the paths resolve to files; the pandoc fixture test
  exercises the PDF/DOCX renderer happy path with a stub binary.

## Deviations

None.

## Conclusion

Supports: the review-export authority (single `export_review` path) is
exercised by tests covering content completeness (file existence, format set)
and the external-converter happy path; the missing-converter error behavior
(V03-02) is static-only (no fixture runs pandoc-absent, but the code path is a
straightforward `ok_or_else`).

## Follow-Up

None.
