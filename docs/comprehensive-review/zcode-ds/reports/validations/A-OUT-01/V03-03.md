# A-OUT-01 / V03 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Exported artifacts carry a stable identity (review_id/format/path/bytes +
citation audit) and are delivered consistently across all surfaces; the
research-export authority is single and lossless.

## Method

Static inspection:

1. `ReviewExportArtifact` (`research.rs:518-523`) and `export_review`
   (`:1123-1169`): artifact written via `atomic_write` (temp + rename + fsync,
   `:1988-1997`) under `{ws}/research/reviews/<id>/reports/systematic-review.<ext>`;
   `path` returned relative to the workspace root; `bytes` = written length.
2. Consumers: CLI `/papers export` prints the artifact JSON
   (`cmd_impls/research.rs:265-277`); Tauri `export_systematic_review` returns
   `Vec<ReviewExportArtifact>` (`src/tauri/commands/research.rs:240-255`);
   frontend `ReviewWorkbench.tsx:155-169` displays `artifact.path` and updates
   the audit from `artifacts[0].citation_audit`; agent tool `export_review`
   (`research_tool.rs:164-182`) serializes the same artifact.
3. Workspace-root resolution across surfaces: Tauri `workspace_root`
   (`src/tauri/commands/research.rs:18-26`) uses the active workspace or
   `workspace_root_for_agent` (`analysis.rs:228-232`); the agent tool uses
   `context.working_dir` with `current_dir()` fallback (`research_tool.rs:235-242`).
4. `export_all_review_formats` (`:1171-1191`) — `map(...).collect()` over
   `ResearchResult` short-circuits at the first error.
5. Surface availability (from V02-01): review export reachable on GUI, CLI
   REPL, and the agent tool — absent on the TUI (default surface).

## Expected Result

One artifact identity across surfaces; all surfaces reach the same export
authority; the export set is either fully delivered or reported with what was
delivered.

## Result

- Exit code: not applicable (static)
- Summary:

| Aspect | Behavior | Verdict |
|---|---|---|
| Artifact identity | `ReviewExportArtifact` {review_id, format, path, bytes, citation_audit} — same struct on GUI IPC, CLI JSON, agent tool | PASS |
| File location | one layout `{ws}/research/reviews/<id>/reports/`; atomic writes; path relative to workspace | PASS |
| Single export authority | all three surfaces call `research::export_review` / `export_all_review_formats` | PASS |
| Root consistency | Tauri resolves the active workspace; the agent tool resolves `working_dir` — divergent only when the agent's working_dir is not the workspace root (fallback `current_dir()`) | WARN (edge) |
| Partial failure | `export_all_review_formats` returns the first `Err`; Markdown/Json/Csv/Bibtex/Ris already written to disk are dropped from the response — user sees only the error while files exist | FAIL (P3-02) |
| Surface availability | GUI + CLI REPL + agent tool yes; TUI no research/export commands at all | FAIL (P2-04) |
| Session-export identity | `/export` (`~/.eko/exports/`) and `export_conversation` (IPC string) are two different identities/sources for one semantic | FAIL (P2-01) |

## Deviations

The availability and partial-failure expectations do not hold.

## Conclusion

Supports findings A-OUT-01-P2-01 (session-export divergence), A-OUT-01-P2-04
(TUI research gap), A-OUT-01-P3-02 (export_all partial-failure reporting). The
review-export identity itself is sound and consistent across the surfaces that
expose it.

## Follow-Up

None.
