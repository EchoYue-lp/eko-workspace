# A-OUT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

EKO output/export concepts have exactly one definition each per semantic, and
no parallel renderer/export machinery duplicates the same capability without
an owning layer decision. Definition search covers both repositories.

## Method

Inspection procedure (all from the repo root
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

1. `grep -rn "OutputFormat" echo-agent/src echo-agent/echo-core/src` — framework
   has no output-format concept.
2. `grep -rn "OutputFormat\|format_response\|FormattedOutput"` across
   `echo-agent-cli/src` and `echo-agent-cli/echo-agent-app-core/src`, excluding
   the `src/output/` directory.
3. `grep -rn "OutputRenderer\|render_markdown_to_terminal"` across both CLI
   trees; enumerate production method calls via
   `grep -oE "output\.[a-z_]+" src/cli/repl.rs | sort | uniq -c`.
4. `grep -rn "LatexExporter\|latex"` across both CLI trees.
5. `grep -rn "ResearchOutputFormat\|output_format"` across both CLI trees.
6. `grep -rn "ProfileManager\|profile_manager\|get_active"` across both CLI
   trees, excluding `src/profiles/`.
7. `grep -rn "comrak\|markdown::\|render_markdown"` across both trees; identify
   every markdown renderer implementation.
8. `grep -rn "export_review\|export_conversation\|cmd_export\|/export"` to list
   every session/review export implementation.
9. `grep -rn "axum\|Router::new\|TcpListener"` to check for an HTTP server
   behind the frontend non-Tauri API branches.

## Expected Result

Either one definition per concept with an owning layer, or explicitly
documented duplicates with a defined relationship.

## Result

- Exit code: not applicable (static inspection)
- Summary:

| Concept | Definitions found | Owners | Verdict |
|---|---|---|---|
| Output format enum | `OutputFormat` (Text/Json/Markdown/Table) app-core `output/format.rs:9-24` — zero production callers; no `--output`/`-o` CLI flag exists (grep of `src/cli/args.rs` empty) | application, dead | duplicate-vs-doc mismatch |
| Terminal markdown renderer | (a) app-core `output/markdown.rs` `render_markdown_to_terminal` (zero callers); (b) TUI `src/tui/markdown.rs` `render_markdown` (live: `tui/mod.rs:1207,1426`, `tui/widgets/chat.rs:209`) | (a) dead, (b) live | two renderers, one live |
| Terminal output facade | `OutputRenderer` app-core `output/mod.rs` — only REPL uses it, 13 of ~28 methods (`repl.rs:98,152,240,243,247,485,488,521,665,668,747,787,815,819,847,856`); `render_markdown/render_code_block/render_table/render_kv_table/print_info_box/set_*` unused | application | partial facade |
| LaTeX export | `LatexExporter` app-core `export/latex.rs` — zero production callers (only module re-export `export/mod.rs:7` + unit tests) | application, dead | duplicate/aspirational |
| Research output format | `ResearchOutputFormat` (Markdown/Latex) `tasks/background.rs:115-122`; field `output_format` in `BackgroundTaskKind::Research` (`:26`) is never read (`to_prompt` matches `..` at `:162-174`); all creation sites hardcode Markdown (`cmd_impls/research.rs:45`, `cmd_impls/coding.rs:235`, `cmd_impls/pipeline.rs:116`, `tasks/service.rs:899`) | application | inert field |
| Config profiles | `profiles/manager.rs` `ProfileManager` + `Profile.output_format` (`types.rs:22-23`) — zero production callers outside the module (lib.rs:16 re-export only) | application, dead | dead module |
| Session export | (a) CLI/TUI `/export` `cmd_impls/advanced.rs:166-217` (writes `~/.eko/exports/`); (b) Tauri `export_conversation` `src/tauri/commands/conversations.rs:642-662` (returns markdown string; registered `src/tauri/mod.rs:217`; zero frontend consumers — `conversationApi.export` `web-frontend/src/api/endpoints.ts:437-441` unreferenced); (c) HTTP fallback route `/conversations/${id}/export` — no server implements it (no axum Router/TcpListener anywhere in `echo-agent-cli/src`; `client.ts` HTTP mode is dev-only; app-core `error.rs` `ApiError`/`StreamingEvent` have zero callers) | application/adapter | two divergent implementations, no canonical consumer |
| Review export | single authority `research.rs::export_review` (`:1123`) consumed by CLI `/papers export` (`cmd_impls/research.rs:265-277`), Tauri `export_systematic_review` (`src/tauri/commands/research.rs:240-255`), agent tool `research_library.export_review` (`research_tool.rs:164-182`) | application | single authority, consistent |
| HTTP API types | `ApiError`/`StreamingEvent` (app-core `error.rs`) with axum dep (`echo-agent-app-core/Cargo.toml:44`) — zero callers | application, dead | aspirational web mode |

Framework side: no `OutputFormat`, no export module, no markdown renderer
(`grep` of `echo-agent/src`, `echo-core/src`, `echo-tools/src` finds only
JSON-extraction helpers and subagent section parsing). All output/export
machinery is EKO-application-owned; nothing needs moving to the framework.

## Deviations

None.

## Conclusion

Supports: definition/duplicate inventory of every output/export concept in
both repositories; the layering conclusion that all output/export logic is
application-side with no framework duplicate; identifies one single-authority
path (review export) and several dead/divergent surfaces. Does not prove
runtime behavior — V02 covers reachability.

## Follow-Up

Used by task report findings A-OUT-01-P2-01 (session export divergence),
P2-03 (decorative format machinery), P3-01 (dead code cluster). Next: V02.
