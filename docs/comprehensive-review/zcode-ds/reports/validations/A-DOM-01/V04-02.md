# A-DOM-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories); target/ build cache present

## Claim

The research-library and connector tests pass on the current commit (research.rs, research_connectors.rs, runtime routing).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked "research"
```

## Expected Result

Exit code 0; non-live tests pass (source dedup, medical review + PRISMA, citation audit + all export formats, pandoc renderer fixture, idempotent tool-result ingestion, research classifier routing); the two live-network tests are ignored.

## Result

- Exit code: 0
- Duration: 1.58 s
- Summary: 6 passed; 0 failed; 2 ignored (live provider smoke and live Zotero import — opt-in `#[ignore]`, require `EKO_PROVIDER_SMOKE=1` / `ZOTERO_API_KEY` + `ZOTERO_LIBRARY_ID`).
- Log/artifact: none retained (terminal output).

## Deviations

None.

## Conclusion

Supports: source/evidence file-backed persistence, dedup, PRISMA derivation, citation audit, all export formats (including the pandoc pdf/docx fixture), idempotent tool-result ingestion, and research classifier routing are green. Live connector behavior remains unverified by design (opt-in).

## Follow-Up

V04-03 (frontend tests), V05-01 (document drift).
