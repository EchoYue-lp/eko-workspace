# A-DOM-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

There is exactly one definition per analysis/research/provenance/connector concept across both repositories; no second EKO research store, connector authority, or analysis workbench duplicates a framework mechanism.

## Method

Repository-wide grep for definition and duplicate search terms in `echo-agent-cli/echo-agent-app-core/src`, `echo-agent-cli/src`, `echo-agent/src`, `echo-agent/echo-core/src`, `echo-agent/echo-tools/src` (read-only):

```text
1. grep -rn "research_library" across both repos
2. grep -rln "provenance" across both repos (definition sites)
3. grep -rn "fn run_analysis|fn research_|fn connector" in app-core analysis/research modules
4. grep -rn "install_auto_ingest_tools|ResearchLibraryTool|run_analysis|create_analysis|save_analysis" callers
5. grep -rn "analysis|research|connector" -l in both repos (surface inventory)
6. Confirm no second research store / connector authority: names "research_remember|research_recall" in CLI
```

## Expected Result

One definition per concept; `research_library` defined once; no duplicate connectors; no EKO use of the framework `research_remember`/`research_recall` stubs.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:
  - `research_library` tool: single definition `echo-agent-cli/echo-agent-app-core/src/research_tool.rs:24-28`; referenced by `runtime.rs:287` (registration) and by prompt text only (`tasks/background.rs:166,170,183`). No duplicate.
  - Analysis workbench: single implementation in `echo-agent-cli/echo-agent-app-core/src/analysis.rs` (create/save/run/list/load); consumers are the Tauri commands (`src/tauri/commands/analysis.rs`), CLI (`src/cli/cmd_impls/analysis.rs`), and frontend `analysisApi` (web-frontend/src/api/endpoints.ts:919). No second analysis engine in EKO; execution reuses the framework `run_code` tool via the agent's `ToolManager` (`analysis.rs:386-483`).
  - Research connectors: single module `research_connectors.rs` (OpenAlex/Crossref/Europe PMC/Zotero + auto-ingest); providers come from the framework `echo_agent::tools::research` clients (CrossrefClient/EuropePmcClient/OpenAlexClient/ZoteroClient) — reused, not duplicated.
  - Provenance: `SourceProvenance` defined only in `research.rs:59-65`; no provenance model exists in `echo-core`/`echo-tools` (grep empty); the framework `ScholarlyWork` carries wire-level `provider`/`provider_id` (F-EXT-03 reviewed), converted losslessly at the adapter boundary (`research_connectors.rs:397-428`).
  - `research_remember`/`research_recall` (framework stubs from F-EXT-03-P1-01): zero hits in the CLI repository — EKO does not use them.
  - Connector/analysis naming collisions: none found; `research` grep hits in `tasks/` are background-task kinds and prompts, not a second store.

## Deviations

None. `echo-tools` lives under `echo-agent/echo-tools` (workspace member), not at repo root; initial grep paths were corrected.

## Conclusion

Supports: single-authority claim for analysis/research/provenance/connectors; the framework memory stubs do not affect EKO's research flow (EKO persistence is the file-backed library). Does not prove runtime reachability (covered by V02-01) or behavior (V03-*).

## Follow-Up

V02-01 (registration and reachability).
