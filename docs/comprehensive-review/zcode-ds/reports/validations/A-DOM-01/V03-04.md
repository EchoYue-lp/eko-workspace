# A-DOM-01 / V03 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Artifact export and rendering are reliable: review exports (md/json/csv/bibtex/ris/pdf/docx) are written atomically under `research/reviews/<id>/reports/`, embed the citation audit, and escape their payloads consistently; the frontend renders backend facts (lineage, outputs, audit, PRISMA) without inventing state.

## Method

Static inspection of `research.rs` renderers, `analysis.rs` artifacts, frontend components, and fixture tests:

```text
1. research.rs:1123-1169 export_review; :1171-1192 export_all_review_formats
2. research.rs:1361-1472 markdown renderer; :1474-1527 CSV; :1529-1546 bibtex; :1548-1576 ris
3. research.rs:1594-1600 markdown_cell / csv_cell escaping; :1578-1592 citation_key
4. research.rs:1988-1999 atomic_write
5. analysis.rs:772-843 collect_outputs / output_kind; :904-906 bounded_text; :934-957 atomic_write
6. web-frontend/src/components/analysis/AnalysisPanel.tsx (lineage/results tabs); ReviewWorkbench.tsx (audit/export UI); ReviewMatrix.tsx (evidence matrix); PaperDetail.tsx
7. web-frontend/src/components/papers/ReviewWorkbench.test.ts (computePrismaFlow fixture); analysis/AnalysisPanel.test.ts
```

## Expected Result

Export payloads are complete and correctly escaped; artifacts land inside the workspace with hashes; frontend mirrors backend facts.

## Result

- Exit code: not-applicable (static inspection); fixture tests executed separately in V04-03
- Duration: not-applicable
- Summary:
  - Export writes: `export_review` writes atomically to `research/reviews/<id>/reports/systematic-review.<ext>` and returns path/bytes plus the full `citation_audit` (`research.rs:1153-1168`); all formats verified in the app-core test `citation_audit_and_all_report_formats_are_file_backed` (V04-02).
  - Escaping: CSV correctly quotes/escapes (`csv_cell`, `research.rs:1598-1600`); Markdown table cells escape `|` and newlines (`:1594-1596`); **BibTeX/RIS do not escape** titles/authors/venues containing `%`, `{`, `}`, `&` (`render_bibtex`/`render_ris`, `research.rs:1529-1576`), and `citation_key` (first-author last name + year, ASCII-filtered, `:1578-1592`) yields empty stems for non-ASCII authors and duplicate keys for same-author+same-year sources (A-DOM-01-P3-02).
  - Analysis artifacts: collected with kind classification (chart/table/report/result/file), sizes, and SHA-256 (`analysis.rs:772-843`); output capture bounded at 200 000 chars UTF-8-safely (`:904-906`); writes atomic (`:934-957`).
  - Frontend: AnalysisPanel renders script/input/parameter/seed/environment/sandbox lineage rows from the run record, plus outputs with sizes and open-artifact action; ReviewWorkbench renders audit issues, export artifacts, and its own `computePrismaFlow` (duplicates the backend derivation — A-DOM-01-P3-03); ReviewMatrix renders evidence per dimension; PaperDetail renders provenance-less paper metadata with notes/tags. No lifecycle state invented by the frontend beyond drafts.

## Deviations

Escaping/key gaps in BibTeX/RIS and the frontend PRISMA duplication are recorded as findings (P3-02, P3-03). `export_review` silently drops missing sources from the exported list, but the embedded citation audit flags them (`missing_source`), so the artifact remains honest.

## Conclusion

Supports: artifact export is file-backed, atomic, hashed, and audit-embedded; rendering fixtures cover the main formats; residual defects are localized to BibTeX/RIS escaping and citation-key derivation, and a duplicated PRISMA derivation in the frontend.

## Follow-Up

V03-05; V04-01..03 executable checks; V05-01 drift check.
