# A-DOM-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

`ResearchLibraryTool` and the auto-ingest wrappers are registered on the EKO primary agent only; analysis commands are reachable from GUI (Tauri), CLI/TUI, and channels; neither research tool is part of the framework read-only Subagent surface; the analysis run path reaches the framework `run_code` tool with correct status/cancel semantics.

## Method

Static call-graph tracing (read-only):

```text
1. runtime.rs:283-289 — install_auto_ingest_tools + add_tool(ResearchLibraryTool) on the primary agent
2. infra.rs create_agent / build_readonly_subagent_agent / build_writer_subagent_agent — tool surface construction; confirm research_library absent
3. src/tauri/mod.rs:88-92 — analysis command registration; src/tauri/commands/analysis.rs:39-120 — list/create/get/save/run/cancel
4. src/cli/cmd_impls/analysis.rs — /analysis list|create|show|run; register_all
5. analysis.rs:376-483 — run_analysis_with_agent -> run_analysis -> ToolManager::execute_tool_with_context("run_code", ...)
6. echo-tools/src/code.rs:415-485 + echo-core/src/sandbox.rs:277-288 — run_code result semantics (success = exit_code==0 && !timed_out && !cancelled)
7. echo-tools/src/code.rs:332-358 — ctx.cancel honored before/inside sandbox execution
8. echo-execution/src/tools.rs:606-625 — execute_tool_with_context returns ToolError::NotFound when run_code is absent (fail-closed)
9. tasks/service.rs + tasks/background.rs:160-183 — background Research task prompt routes to research_library on the primary agent
10. web-frontend/src/components/layout/RightWorkspace.tsx — AnalysisPanel/PaperPanel mounted in GUI
```

## Expected Result

Primary agent only; GUI/CLI/TUI reachable; readonly surface free of research_library; analysis status mapping consistent with run_code semantics; fail-closed when run_code is missing.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:
  - Registration: primary agent only (`runtime.rs:283-289`); `install_auto_ingest_tools` wraps `arxiv_search/semantic_scholar_search/pubmed_search/clinical_trials_search` present on that agent (research feature is CLI-enabled per `echo-agent-app-core/Cargo.toml:10-14`, verified in F-EXT-03).
  - Read-only surface: `ResearchLibraryTool` and the wrappers are added post-build to the primary agent; `register_readonly_tools` (framework registry, `echo-agent/src/agent/react/mod.rs:741-747`) never sees them — the F-EXT-03-P1-02 readonly-write defect does not extend to EKO's research tools.
  - GUI: `analysisApi`/`papersApi` (endpoints.ts:919,1278) -> Tauri commands (`commands/analysis.rs`, `commands/research.rs`) -> app-core; `run_analysis` guarded against duplicate concurrent runs via the session cancel-token map (`commands/analysis.rs:96-121`); cancel propagated through `ToolContext.cancel` (`analysis.rs:413-419`; run_code honors it at code.rs:332-358).
  - CLI/TUI: `/analysis list|create|show|run` shares the same app-core functions (`cmd_impls/analysis.rs`); CLI `/analysis run` passes no cancel token (no running-guard, unlike GUI).
  - Status mapping: `run_status` (`analysis.rs:866-875`) matches run_code semantics verified at `echo-core/src/sandbox.rs:277-288` (exit_code==0 → success; cancelled/timeout categories map to Cancelled/TimedOut).
  - Fail-closed: missing `run_code` (sandbox unavailable) yields `ToolError::NotFound` -> run record `Failed` with the error text (`analysis.rs:445-453`).
  - Frontend: `AnalysisPanel`/`PaperPanel`/`ReviewWorkbench`/`ReviewMatrix` mounted in `RightWorkspace.tsx`; all data flows through the API layer; no direct filesystem authority in the frontend.

## Deviations

None.

## Conclusion

Supports: reachability of analysis/research surfaces from GUI/CLI/TUI/channels; primary-agent-only registration; consistency of the analysis run-status mapping with the framework tool contract; fail-closed execution when `run_code` is absent. The background Research-task path relies on the primary agent's toolset (verified registration; not end-to-end executed).

## Follow-Up

V03-01..05 (invariants) and V04-01..03 (executable checks).
