# A-DOM-01 / V03 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Connector failures are classified and surfaced: provider search errors propagate as `ResearchError::External` to the caller; Europe PMC enrichment degrades per-endpoint into warnings; Zotero sync errors propagate; auto-ingest failures are logged without altering the original tool result; live-network behavior is explicitly opt-in and not required for the reviewed invariants.

## Method

Static inspection of `research_connectors.rs`, `research_tool.rs`, `research.rs`, and the framework clients' error classification (F-EXT-03 reviewed the clients):

```text
1. research_connectors.rs:83-112 search_and_ingest — provider errors via map_err(external)
2. research_connectors.rs:188-255 enrich_from_europe_pmc — per-endpoint try/catch into warnings, full-text failure tolerated
3. research_connectors.rs:114-152, 154-186 — zotero import/export error propagation
4. research_connectors.rs:257-281, 306-337 — ingest_tool_output / AutoIngestResearchTool: ingest errors logged, original tool result returned unchanged
5. research.rs:539-541 external() mapping to ResearchError::External
6. research_tool.rs:73-191 — tool actions map errors to ToolResult::error strings (visible to the agent)
7. research_connectors.rs:615-687 — live provider/zotero tests #[ignore] + EKO_PROVIDER_SMOKE=1 gate
```

## Expected Result

No swallowed errors on user-initiated paths; partial enrichment reports warnings; the agent-facing tool surfaces errors in the result.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:
  - `search_and_ingest`: network/provider failures -> `ResearchError::External` -> GUI error banner (PaperPanel error state) or agent-visible tool error. Verified.
  - `enrich_from_europe_pmc`: each endpoint (citations/references/text-mined terms/full text) fails independently into a `warnings` list; the source is still saved with the partial supplement; GUI surfaces "Enriched with N warning(s)". Good partial-failure design.
  - `import_zotero`/`export_zotero`: errors propagate; the agent-facing `export_zotero` additionally requires non-empty `source_ids` (`research_connectors.rs:158-162`).
  - Auto-ingest: ingest failures (including a per-record conflict aborting the batch — `ingest_tool_output` collects into `Result<Vec<_>>` which fails on the first Err) are downgraded to a `tracing::warn`; the original search result is returned unchanged — the caller cannot know persistence failed (see A-DOM-01-P2-01).
  - Renderer failures: PDF/DOCX export without pandoc/quarto returns `ResearchError::External` with an actionable message (`research.rs:1251-1323`); `export_all_review_formats` probes availability and skips missing formats (`:1171-1192`).
  - Live-network tests: two opt-in `#[ignore]` tests exist (`research_connectors.rs:615-687`); not run in this read-only review; recorded `not_run` with environmental reason (keys / EKO_PROVIDER_SMOKE=1).

## Deviations

None.

## Conclusion

Supports: connector failure handling is explicit and classified on all user-initiated paths; partial enrichment is warning-based; the only silent degradation is the auto-ingest side channel (finding P2-01). Live behavior not exercised — static classification only.

## Follow-Up

V03-04 (artifact/rendering fixtures), V03-05 (auto-ingest side-effect contract).
