# A-DOM-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical design claims about EKO analysis/research are classifiable against current code (current/fixed/stale/regressed): the M12 statistics-split boundary, the M13 file-backed analysis workbench, and the framework `run_code(script_path)` contract.

## Method

Compare document claims against current code (read-only):

```text
1. docs/MASTER-PLAN.md:109/382/840 — statistics split: exploratory only; formal inference via reviewable scripts through run_code
2. echo-agent-cli/docs/2026-07-18-file-backed-analysis-workbench.md — directory contract, run-record contract, rebuild-of-outputs semantics, UI/entry points, non-goals
3. echo-agent-cli/docs/2026-07-18-statistical-inference-correctness.md — exploratory_statistics inference=false contract; formal script artifact contract; EKO boundary
4. Current code anchors: analysis.rs, research.rs, research_connectors.rs, subagents/data/analyst.md, statistics.rs (framework, F-EXT-03 verified), registry.rs:467-495
```

## Expected Result

Each claim maps to a current code anchor or is explicitly stale.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:
  - MASTER-PLAN statistics split: `current` — `analyst.md` enforces the boundary; `ExploratoryStatisticsTool` descriptive-only; the workbench executes scripts via `run_code` (analysis.rs:420-422); framework registry test pins the split (echo-tools/src/registry.rs:467-495).
  - MASTER-PLAN M13 file-backed workbench: `current` — `analysis/<id>/` directory contract (manifest.json, analysis.py/analysis.R, latest-run.json, environment.json, result.json, outputs/, runs/<run-id>.json) matches analysis.rs constants (lines 25-33) and run-record construction (457-481); GUI + CLI entry points exist (V02-01).
  - M13 "每次运行会重建 environment.json/result.json/outputs/" (rebuild outputs each run): `current` — analysis.rs:403 clear_generated_outputs; note that this designed rebuild is the root of finding P2-02 (prior artifacts not retained; runs/ records then reference deleted files).
  - M13 acceptance "TUI、CLI、channel 能列出、查看、创建和重跑同一文件化分析": `current` — shared app-core functions and `/analysis` CLI command registered for CLI/TUI/channels.
  - M12 formal-script artifact contract (input sha, package versions, seed, diagnostics): `current` at capability level — run records capture fingerprints, environment, parameters, seed (analysis.rs:457-477); the analyst role mandates the rest.
  - M12/M13 non-goals (no SQLite, no second Python process manager, no frontend localStorage authority): `current` — no SQLite in the analysis/research paths (grep verified in V01-01); single execution authority (framework ToolManager/run_code).
  - F-EXT-03-P1-01 cross-check: the framework `research_remember`/`research_recall` stubs are unused by EKO (zero CLI references, V01-01) — EKO research persistence is the file-backed library, so the framework stub defect does not directly affect the EKO research flow.
  - F-EXT-03-P1-02 cross-check: `ResearchLibraryTool`/auto-ingest are primary-agent-only (V02-01) — the framework readonly-surface write-tool defect does not extend to EKO research tools; `research_library` declares `[Read, Write]` permissions (`research_tool.rs:69-71`) consistent with its write capability, unlike the framework registry misclassification.

## Deviations

None. All sampled claims classify as `current`; no stale or regressed claims found in the sampled documents.

## Conclusion

Supports: the design docs are accurate descriptions of the current implementation; the documented "rebuild outputs each run" semantics is the root of the P2-02 retention gap (design intent vs lineage completeness); the framework research-memory stubs and readonly-surface write tools do not reach the EKO research flow.

## Follow-Up

Final task report.
