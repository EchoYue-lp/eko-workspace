# A-DOM-01 / V03 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Provenance/lineage invariants hold for the research library and the analysis workbench: every source carries provenance; evidence links to sources; analysis runs record script/input/parameter fingerprints and detect staleness; deletion cascades remove dependent records; run records are immutable.

## Method

Static inspection of `research.rs` and `analysis.rs` (read-only):

```text
1. research.rs:59-65 SourceProvenance; :1694-1702 merge dedup (provider+record_url)
2. research.rs:397-428, 430-483 — provenance construction from providers and tool records
3. research.rs:747-810 — evidence upsert validates source_id and review_id exist
4. research.rs:696-730 — delete_source cascade (evidence, review source_ids/screening/risk_of_bias)
5. research.rs:1010-1121 — citation audit (missing_source, orphan_evidence, empty_claim, missing_locator, included_without_evidence, exclusion_without_reason)
6. analysis.rs:178-190, 394-399, 668-708 — run record fingerprints + stale detection
7. analysis.rs:342-374 — save with expected_script_revision optimistic concurrency
8. analysis.rs:478-481 — immutable runs/<run-id>.json + latest-run.json
9. research.rs:950-963 — delete_review cascade to evidence
10. research.rs:651-669 — write_full_text_xml (no size limit)
```

## Expected Result

Lineage complete and cascade-complete; run records immutable and self-contained.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:
  - Source provenance: created for connector search (`research_connectors.rs:420-425`), tool-record ingestion (`:475-480`), and Zotero import (`:135`); merged and deduped by (provider, record_url) on ingest (`research.rs:1694-1702`). Verified present in the persisted `SourceRecord`.
  - Evidence/review linkage: `upsert_evidence` requires an existing source (and review if given) — no orphan creation; `audit_review` flags orphan evidence and missing sources.
  - Analysis lineage: run records capture script/input/parameter/seed fingerprints (`analysis.rs:394-399, 457-477`); stale detection compares current fingerprints vs the last run and reports specific reasons (`:668-708`); save uses revision compare to reject lost updates (`:352-355`). All tested green (V04-01).
  - Gaps found (become findings):
    - `delete_source` (`research.rs:696-730`) does not remove `medical.guideline_source_ids` referencing the deleted source from other reviews, and does not delete the orphan `research/fulltext/<source_id>.xml` file; `audit_review` does not check `guideline_source_ids` (A-DOM-01-P3-01).
    - `runs/<run-id>.json` records are immutable, but the artifacts they reference are deleted by the next run (`analysis.rs:403` clear_generated_outputs before execution); only fingerprints are retained, so the previous run's outputs (and the executed script content) are unrecoverable after a rerun (A-DOM-01-P2-02).
    - `write_full_text_xml` accepts unbounded XML (no size cap; MAX_JSON_BYTES covers JSON reads only) (A-DOM-01-P3-04).

## Deviations

None beyond the recorded gaps.

## Conclusion

Supports: strong provenance/lineage design for sources/evidence/analysis with real tests; two lineage-integrity gaps (deletion cascade for guideline refs/fulltext; artifact retention across reruns) that are documented as findings; full-text XML write is unbounded.

## Follow-Up

V03-03 (connector failure), V03-04 (artifacts/rendering).
