# A-DOM-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The exploratory/formal-analysis boundary is coherent: quick descriptive statistics (framework `exploratory_statistics`, no inference claims) are distinct from formal inference (reviewable scripts executed through `run_code(script_path)` under the EKO analysis workbench), and EKO policy documents this boundary consistently with the code.

## Method

Cross-check the M12 design doc, the analyst Subagent, the framework statistics tool, and the analysis workbench (read-only):

```text
1. echo-agent-cli/docs/2026-07-18-statistical-inference-correctness.md:29-50 — framework/app boundary claims
2. echo-agent-cli/echo-agent-app-core/src/subagents/data/analyst.md — exploratory vs formal boundary policy
3. echo-tools/src/statistics.rs — ExploratoryStatisticsTool: inference=false marker, no p-value/significant/confidence_interval (spot-checked; F-EXT-03 reviewed in depth)
4. echo-tools/src/code.rs:190-220,253-320 — run_code script_path mode, oneOf code|script_path, path-escape rejection
5. echo-agent-cli/echo-agent-app-core/src/analysis.rs:376-483 — workbench run executes the persisted script via run_code with the analysis dir as working_dir
6. echo-tools/src/registry.rs:467-495 — registry test pinning the statistics split (verified in F-EXT-03)
```

## Expected Result

No inference-capable statistics tool in the framework; the formal path is script-based and sandboxed; the analyst role instructs the boundary; the workbench runs the same `run_code` contract.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not-applicable
- Summary:
  - Framework side: `ExploratoryStatisticsTool` is descriptive-only and its result is marked `inference=false`; no p-value/CI outputs (consistent with F-EXT-03's verified claim and `registry.rs` pin test).
  - `run_code` supports `script_path` with a strict oneOf contract and rejects absolute paths, parent traversal, symlink escape, and missing files (`code.rs:100-153`), enforcing the sandbox floor (A-TOOL-01 verified).
  - EKO workbench: `run_analysis` (`analysis.rs:386-483`) executes the manifest's `script_path` inside `analysis/<id>/` via the agent's ToolManager; script, inputs, parameters, seed are fingerprinted before the run (`analysis.rs:394-399`) so the run record captures the exact lineage; stale detection (`analysis.rs:668-708`) re-fingerprints after the run.
  - Analyst role (`analyst.md`): "Use exploratory_statistics only for descriptive distribution summaries; it is not an inference engine. For hypothesis tests ... first write a reviewable .py/.R script ... execute that same saved file through run_code with script_path"; mandates recording input path + SHA-256, package versions, seed, parameters, missing-data handling, diagnostics. Matches M12 doc claims.
  - MASTER-PLAN M12/M13 claims (statistics split; workbench is file-backed with run_code) classify as current.

## Deviations

None.

## Conclusion

Supports: boundary is correctly placed — descriptive tools stay generic framework capability; formal inference is an EKO product policy enforced by role prompt + file-backed workbench + framework script execution contract. No second inference engine, no in-memory notebook, no SQLite (doc non-goals verified: no SQLite dependency introduced in the analysis path).

## Follow-Up

V03-02 (provenance/lineage), V03-05 (hidden side-effect boundary).
