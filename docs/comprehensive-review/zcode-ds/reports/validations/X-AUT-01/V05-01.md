# X-AUT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Every canonical finding from the declared dependencies that bears on the
X-AUT-01 question ("automated Agent actions controlled while direct user
interactions remain usable; local data-loss/secret protections in place") is
re-verified at the current commits and folded into the task's matrix under
its canonical ID, without duplication into new X-AUT-01 findings.

## Method

For each canonical finding, the evidence anchor was re-checked at the current
commits (definition -> reachability) and classified against the task's two
axes: (A) automation control, (B) user-interaction usability / data-loss /
secret hygiene.

## Expected Result

All listed canonical findings still hold at commits 9b0e0fa / b3b2e81; none
is stale or regressed relative to its report.

## Result

- Exit code: not-applicable (static re-verification)
- Summary (matrix):

| Canonical ID | Anchor re-verified at current commit | Axis | Classification for X-AUT-01 |
|---|---|---|---|
| F-HITL-01-P1-01 | `snapshot.rs:841-852` (RequireApproval/Ask -> opaque `Err`, no ask); dead `run/approval.rs` + `process_steps` | A | Control gap on the ask path: Ask rules / Bubble / denial-escalation degrade to hard tool errors; Default+handler (EKO main path) unaffected |
| F-HITL-01-P1-02 | `service.rs:729-731` writes `last_modified_args`; sole reader in dead `run/approval.rs:143`; live `snapshot.rs:834` returns `Ok(None)` | A/B | User-modified (safer) args never execute; original args run — data-loss-adjacent |
| F-HITL-01-P1-03 | `service.rs:891-908` `DynProviderHandler` maps `SessionAllTools` -> `add_session_rule("*")`; `RuleMatcher::Pattern("*")` matches all (echo-core permission.rs:253-255) | A | One session-allow widens the boundary to ALL tools (framework side) |
| A-HITL-01-P1-02 | `hitl/repl_provider.rs:74-77` (`""` -> Approved); blocking `read_line` at :69-72 defeats the dispatcher deadline | A | Scripted/piped/EOF CLI auto-approves approvals; GUI default-provider contexts affected |
| A-HITL-01-P1-03 | TUI `a` events.rs:242-244, REPL :77-79, channel :158-160, GUI ChatPanel.tsx:321-322 -> chat.rs:335-342 -> `SessionAllTools` | A | Every surface's "approve all" = all-tools unlock for the session (EKO is the sole producer) |
| A-HITL-01-P2-03 | denial tracker `service.rs:575-585` + `snapshot.rs:841-846`; pool empty dispatcher `agent_pool.rs:960-975` | A | After 3 rejections (or 3 background auto-rejections) the 4th tool errors opaquely instead of asking |
| A-HITL-01-P1-01 | `panels.rs:102-151` stores rules in `config.permission_rules`; zero readers apply them | A | GUI rule management is behaviorally dead — user-configured deny/ask rules never reach the pipeline |
| A-INT-01-P2-05 | `browser/mod.rs:978-1018` -> dispatcher incl. REPL (runtime.rs:130-131,144-146) | A | Browser consequential confirmations inherit the REPL EOF auto-approve and the session-wide "*" |
| A-OBS-01-P1-02 | `chat_driver.rs:124,148-157` + `webhook/emitter.rs:138-149` raw payloads | B | Webhook sends raw tool args/error text to external endpoints — P1 secret exposure |
| F-OPS-01-P2-01 | `react/mod.rs:1934`, `snapshot.rs:548-559`, `trace/mod.rs:349-359` raw in JSONL | B | Trace persistence stores unredacted user input/output/error |
| F-OPS-01-P2-04 | `pipeline.rs:439-450` + `echo-state/src/audit/mod.rs:117-181` raw | B | Audit path unredacted (opt-in; EKO does not enable) |
| F-SEC-01-P3-11 | `echo-tools/src/web/fetch.rs:203` raw URL log | B | Credentials embedded in fetched URLs land verbatim in local logs |
| F-SEC-01-P3-04 / A-HITL-01-P2-04 | `src/tauri/error.rs:5-12,45-56`; zero callers | B | Dead `IpcAuth::require_full_auto` gate API + misleading doc (over-gating residue) |
| A-INT-01-P2-03 | `mcp.rs:169-208` `validate_ipc_mcp_url` | B | GUI MCP dialog blocks loopback/private + requires HTTPS — over-validation vs local model (NOT a permission-mode gate) |
| F-SEC-01-P3-03 | `echo-tools/src/security.rs:529-570,615-663`; `fetch.rs:199-201` unconditional | B | `web_fetch` cannot reach localhost/LAN; SSRF-style over-gating with no opt-out |
| F-SEC-01-P3-01 | `eval/runner.rs:226-237,680-704` | B | Eval `TestPass` executes unvalidated `sh -c`; SweBench list misses newline |
| A-TOOL-01-P1-01 / F-EXT-01-P1-01 | `infra.rs:963` `set_plan_mode(true)` vs `snapshot.rs:227-236` filter + `pipeline.rs:1004-1018` | A | Writer subagents silently read-only — plan mode blocks write tools/shell on the same subagent path whose permission service is absent (related to X-AUT-01-P1-01) |
| A-HITL-01-P2-01 | `chat.rs:261-435` `TauriHumanLoopHandler` + `:570-589` per-conversation install | A | GUI approval transport bypasses the app-core dispatcher (related to X-AUT-01-P2-01, which adds the shared-service handler mechanism) |
| F-HITL-01-P2-01..03, F-SEC-01-P3-02/04-10, A-INT-01-P1-01/P2-01..04, A-HITL-01-P2-02 | anchors re-checked in source where touched | B | Not central to the permission boundary; cross-referenced only |

Positive verifications (held, per dependency reports, re-anchored here):
- No `require_full_auto` gate on terminal/MCP (V04-01; A-HITL-01 V03-02,
  A-INT-01 V03-03).
- `run_code` OsSandbox floor holds on the EKO path, fail-closed (code.rs:313-345;
  F-SEC-01 V03-01).
- Worktree dirty-tree protection (worktree.rs:705-724; A-TSK-05).
- Redaction at the 5 live framework sinks (F-SEC-01 V02-01; V03-01 this task).

## Deviations

None. All canonical anchors hold at the reviewed commits; the two new
X-AUT-01 findings (P1-01 subagent boundary, P2-01 shared handler slot) and
one new P3 (CLI mode propagation) are the only additions beyond the
dependency inventory.

## Conclusion

Supports: the cross-boundary matrix for the X-AUT-01 question — automation
is controlled on primary/pool paths with indexed control-channel defects;
direct user interactions are gate-free with indexed over-validation items;
data-loss protections hold while secret hygiene has four indexed gaps.

## Follow-Up

None. Input to the task report's Findings and Historical Claim Status
sections.
