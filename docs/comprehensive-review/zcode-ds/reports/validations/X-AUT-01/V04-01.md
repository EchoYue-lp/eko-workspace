# X-AUT-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

No `require_full_auto`-style or `permission_mode`-style gate is attached to
any direct user interaction path (terminal, file picker, MCP configuration,
browser entry); all remaining gate-like residue is either dead code or
already-indexed over-validation.

## Method

Repository-wide grep + call-site inspection at the current commits:

1. `grep -rn "require_full_auto"` across `echo-agent-cli` and `echo-agent`
   (excluding `target/`).
2. `grep -rn "IpcAuth\|IpcPermission"` across both repositories.
3. `grep -rn "permission_mode"` across both repositories; every hit
   classified as agent-path propagation vs user-interaction gate.
4. Call-site check of every `permission_mode` reader: does any direct user
   command (Tauri command, TUI/CLI command, channel handler) gate on it?
5. Inventory of remaining validation-only guards on user paths:
   `validate_ipc_mcp_url` / `validate_ipc_mcp_stdio` (`mcp.rs:169-208,229-247`),
   `create_terminal` / `write_terminal` (terminal.rs:281-369), file dialog
   plugin (default.json:10-14, tauri-bridge.ts:85-95), framework
   `web_fetch` SSRF validation (`echo-tools/src/security.rs:529-570`),
   eval runner (`echo-agent/src/eval/runner.rs:226-237`).

## Expected Result

- `require_full_auto` appears only in the dead `IpcAuth` API
  (`src/tauri/error.rs`), zero callers.
- No `permission_mode` read exists on any user-interaction command.
- Any behavioral restriction on user paths is pure input validation or an
  already-filed over-gating item.

## Result

- Exit code: 0 for the greps (grep exit codes not applicable as validation);
  inspection complete.
- Summary:
  - `require_full_auto`: 2 hits, both inside `src/tauri/error.rs` (module
    doc :6 and the dead helper :45). `IpcAuth`/`IpcPermission`: definitions
    only (error.rs:17-69), zero callers in either repository. Matches
    A-HITL-01-P2-04 / F-SEC-01-P3-04 (dead API with a stale doc claiming
    gates that do not exist).
  - `permission_mode`: every hit is an agent-path write or read
    (`agent_pool.rs:196,249,467-483,931-932`, `state.rs:342,481`,
    `panels.rs:34,59-76`, `tui/events.rs:3580-3605`, `tui/mod.rs:327,827,
    1947-1948`, `cli/cmd_impls/coding.rs:645-675`). Zero hits inside
    `terminal.rs`, `commands/mcp.rs` (connect paths), file-dialog code, or
    channel handlers. The only `permission_mode`-adjacent strings on user
    paths are dead code.
  - Direct user commands verified gate-free with explicit local-model
    comments: `create_terminal` (terminal.rs:281-295),
    `connect_mcp_server` (mcp.rs:211-216). File picker is a native OS
    dialog. Browser entry points are agent tool calls (permission pipeline)
    — the only gate on browser behavior is the risk-based
    `confirm_action` consent gate for consequential actions, which is
    automation gating, not a user-interaction gate.
  - Remaining over-gating/over-validation residue (all already indexed,
    none new):
    - GUI MCP dialog SSRF-style private-range/https-only rejection
      (`validate_ipc_mcp_url`, mcp.rs:169-208) — A-INT-01-P2-03 (contradicts
      the config-file path and the local threat model).
    - Framework `web_fetch` unconditional private-range rejection,
      no opt-out — F-SEC-01-P3-03.
    - Eval runner `TestPass` unvalidated `sh -c` and newline gap —
      F-SEC-01-P3-01.
    - `IpcAuth` dead gate API + misleading doc — A-HITL-01-P2-04 /
      F-SEC-01-P3-04.
  - Also confirmed: no `require_full_auto`-style gate anywhere in
    `echo-agent` (framework side clean, matching F-HITL-01 V03-01).

## Deviations

None. No new over-gating site found; the grep matched the dependency-report
inventory exactly.

## Conclusion

Supports: the AGENTS.md hard rule "permission modes apply only to agent
automated paths; direct user interactions stay usable" holds — no
`permission_mode`/`require_full_auto` gate exists on terminal, file picker,
MCP configuration, or browser user paths; residue is dead API plus four
indexed over-validation items.

## Follow-Up

None.
