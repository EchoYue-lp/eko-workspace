# X-AUT-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The AGENTS.md "when to add protection" rules are honored at the
cross-repository boundary: (1) protection against unintentional user data
loss, (2) protection against framework bugs causing damage, and (3)
local-universal protections (secrets not written into logs) — each with at
least the mechanisms the dependency reports verified, and every gap already
indexed as a canonical finding.

## Method

Targeted re-verification at the current commits of each protection class,
with dependency reports F-SEC-01 / A-TSK-05 / A-STATE-01 / A-OBS-01 /
F-OPS-01 as the deep audit and fresh anchor checks here:

1. Unintentional data loss:
   - Worktree dirty-tree protection:
     `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/worktree.rs:705-724`
     (integration rejects when writer-owned paths overlap user uncommitted
     changes; `main_dirty_paths` :873-881) — A-TSK-05 verified.
   - Conversation persistence atomicity: framework
     `echo-agent/echo-state/src/memory/file_conversation.rs` (atomic write
     protocol) — verified by F-MEM-01/A-STATE-01 (cited, not re-audited).
   - Terminal write bound: per-session consent + 64 KiB payload cap +
     audit preview (`src/tauri/terminal.rs:301-369`).
2. Framework-bug containment:
   - `run_code` minimum-isolation floor `OsSandbox` + availability pre-check,
     fail-closed (`echo-tools/src/code.rs:305-345`; EKO `infra.rs:265-271`)
     — F-SEC-01 V03 verified.
   - Protected-path denial (`.git/.ssh/.env`) inside `PermissionService`
     (service.rs:505-521) on primary/pool agents.
3. Secret hygiene:
   - Live redaction sinks: `snapshot.rs:885` (output guard),
     `trace/mod.rs:434` (tool-call args), `pipeline.rs:943-953` (result
     preview), `src/tools/builtin/spawn_task.rs:171,175` (subagent output)
     — F-SEC-01 V02 verified (5 live sinks, 1 dead duplicate at
     `execution.rs:221-260`).
   - Known gaps re-verified at current commits:
     - `echo-agent-cli/echo-agent-app-core/src/chat_driver.rs:124,148-157`
       + `webhook/emitter.rs:138-149` — raw tool args/error text posted to
       external webhook endpoints, no redaction (A-OBS-01-P1-02).
     - `echo-agent/src/agent/react/mod.rs:1934`,
       `src/agent/snapshot.rs:548-559`, `trace/mod.rs:349-359` — trace JSONL
       persists raw user input/final output/error (F-OPS-01-P2-01).
     - `echo-agent/src/agent/react/run/pipeline.rs:439-450` +
       `echo-state/src/audit/*` — audit path raw (F-OPS-01-P2-04).
     - `echo-tools/src/web/fetch.rs:203` — raw URL logged (F-SEC-01-P3-11).

## Expected Result

All three protection classes have at least one enforced mechanism on the live
path; any gap is already indexed under a canonical finding ID.

## Result

- Exit code: not-applicable (static trace + grep)
- Summary:
  - Data-loss protections: present and enforced (worktree dirty-path
    rejection, atomic conversation persistence, terminal write consent/bound,
    protected-path denials). No new gap found beyond indexed findings.
  - Framework-bug containment: `run_code` sandbox floor holds on the EKO
    path; fallback disabled (`infra.rs:265-271`, F-SEC-01 V03).
  - Secret hygiene: enforced at the 5 framework sinks; NOT enforced at the
    webhook outbound channel (P1, A-OBS-01-P1-02), the trace JSONL
    input/output/error fields (P2, F-OPS-01-P2-01), the audit path (P2,
    F-OPS-01-P2-04), and the `web_fetch` URL log line (P3, F-SEC-01-P3-11).
    All four gaps are existing canonical findings at the current commits;
    none is new to this task.
  - Boundary note (new in X-AUT-01): on TaskRuntime subagents the
    protected-path check itself does not run (no `PermissionService`), but
    plan mode blocks the write tools/shell that would need it; `run_code`
    remains sandboxed there. Recorded as part of X-AUT-01-P1-01.

## Deviations

None beyond the indexed gaps. "不把密钥打进日志" is therefore only partially
upheld end to end (framework sinks yes; EKO outbound/persistence boundaries
no) — carried as canonical findings, not re-filed here.

## Conclusion

Supports: the three "when to add protection" rules are each implemented on
the primary live paths (worktree data-loss protection, sandbox fail-closed,
redaction at framework sinks). Does NOT support: that secret hygiene covers
all EKO boundaries — the webhook/trace/audit/fetch-log gaps (A-OBS-01-P1-02,
F-OPS-01-P2-01, F-OPS-01-P2-04, F-SEC-01-P3-11) remain open at these commits.

## Follow-Up

None.
