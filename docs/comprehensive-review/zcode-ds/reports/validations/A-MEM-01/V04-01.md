# A-MEM-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The instruction/memory/Dreaming related test suites pass and the app-core
crate compiles (known exit codes), and the existing tests cover the
workspace-switch fixture for the instruction projection.

## Method

Executed in the two workspace roots (read-only; no source modifications):

```text
cd echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked instruction_provider
cargo test -p echo-agent-app-core --lib --locked unified_memory
cargo test -p echo-agent-app-core --lib --locked rule_promoter
cargo test -p echo-agent-app-core --lib --locked review_integration
cargo test -p echo-agent-app-core --lib --locked agent_pool
cargo check -p echo-agent-app-core --locked

cd echo-agent
cargo test -p echo_agent --lib --locked dreaming
cargo test -p echo_agent --lib --locked evolution::layer
cargo test -p echo_state --lib --locked compression
```

## Expected Result

All commands exit 0; unified_memory's
`instruction_projection_replaces_previous_workspace` passes (instruction
projection replacement fixture exists); the compression suite covers
projection survival.

## Result

- Exit code: 0 for every command (see summary)
- Duration: seconds each (incremental builds)
- Summary:
  - `echo-agent-app-core instruction_provider`: 8 passed; 0 failed (exit 0)
  - `echo-agent-app-core unified_memory`: 4 passed; 0 failed (exit 0) —
    incl. `instruction_projection_replaces_previous_workspace` and
    `instruction_and_hot_memory_use_distinct_projections`
  - `echo-agent-app-core rule_promoter`: 3 passed; 0 failed (exit 0)
  - `echo-agent-app-core review_integration`: 7 passed; 0 failed (exit 0)
  - `echo-agent-app-core agent_pool`: 27 passed; 0 failed (exit 0)
  - `cargo check -p echo-agent-app-core --locked`: exit 0
  - `echo_agent dreaming`: 4 passed; 0 failed (exit 0) — incl.
    `dreaming_revives_and_promotes_high_recall_archived`
  - `echo_agent evolution::layer`: 19 passed; 0 failed (exit 0)
  - `echo_state compression`: 69 passed; 0 failed (exit 0) — incl.
    `context_projection_*` projection tests (mod.rs:1848-2075)
- Log/artifact: none (test output inspected inline; no large logs generated)

## Deviations

No test covers hot-memory projection replacement across workspaces or the
refresh-after-hot-mutation invariant; `state.rs` has no test module
(`switch_workspace`/`exit_workspace` are untested — matches A-CFG-01 V04
note). The absence is a coverage gap for the P1-01 regression fixture, not a
test failure.

## Conclusion

Supports: all reviewed suites green at baseline commits; instruction
projection replacement fixture exists and passes; projections survive
compression (echo_state suite). Does not prove the hot-memory refresh
invariant — the failing behavior (P1-01) is unexercised by any test.

## Follow-Up

Q-TST-01 (coverage map), iteration roadmap regression fixtures for P1-01.
