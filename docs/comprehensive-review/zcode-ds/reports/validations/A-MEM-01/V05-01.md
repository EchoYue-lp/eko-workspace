# A-MEM-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documents describing the instruction/memory/Dreaming protocol
match current code, or the drift is classified and recorded.

## Method

Diff the documented claims against the traced code:

```text
echo-agent-cli/docs/MASTER-PLAN.md:68, :245-261, :466-467
echo-agent-cli/docs/2026-07-23-memory-self-evolution-closure.md:10-21, :45-52
echo-agent-cli/docs/system-deep-dive/04-memory.md (projection/refresh rows)
echo-agent/docs/zh/25-self-improvement.md:258 (+en) — Dreaming semantics
echo-agent/docs/zh/03-memory.md — layer contract rows (sampled)
```

## Expected Result

Every claim classifies as current/fixed/stale/regressed with evidence.

## Result

- Exit code: not-applicable (document diff)
- Summary:
  - `MASTER-PLAN.md:251-256` — "Bootstrap, workspace switch/exit, Dreaming
    promotion, explicit hot memory mutation, and rule promotion refresh the
    primary Agent immediately; pooled Agents refresh too" | **regressed** |
    boot/switch/exit refresh both projections (state.rs:883/:1072,
    infra.rs:444), but Dreaming promotion and explicit hot mutation refresh
    only the instruction projection or nothing — hot memory stays stale
    (P1-01).
  - `MASTER-PLAN.md:256` — "GUI, TUI, and CLI run the same Dreaming
    schedule" | **stale** | channels mode (a CLI sub-mode) never spawns
    Dreaming (channels.rs has no spawn; only desktop.rs:247, tui/mod.rs:1999,
    repl.rs:106) — P3-01.
  - `2026-07-23-memory-self-evolution-closure.md:11` — "Dreaming 或人工
    hot-layer 变更后，当前 Agent 和池化 Agent 立即刷新投影" | **regressed** |
    refresh fires the wrong projection (P1-01).
  - `2026-07-23-memory-self-evolution-closure.md:19` — "user/project/local/
    AGENTS/hot memory 改为 eko:instruction-context 可替换投影" | **stale** |
    implementation uses two projections (unified_memory.rs:28-29) — the
    split is deliberate and documented in code, but the refresh sites were
    not updated for it (root cause of P1-01).
  - `2026-07-23-memory-self-evolution-closure.md:20` — refresh trigger list
    ("boot、工作区切换、退出工作区、Dreaming hot promotion、人工 hot memory
    新增/删除、规则晋升都会刷新当前 Agent；池内已有 Agent 同步刷新") |
    **regressed** | see P1-01 (hot promotion and manual hot add/delete
    refresh the wrong projection; remember tool / task memory bridge / budget
    demotion refresh nothing).
  - `2026-07-23-memory-self-evolution-closure.md:21` — "Dreaming 在启动稳定
    60 秒后先执行一次，再按日执行" | **current** | infra.rs:1156-1158
    (60s initial delay + 86400s interval).
  - `2026-07-23-memory-self-evolution-closure.md:50-52` — observation-driven
    budget work deferred | **current** | no entry-level/size caps added;
    hot layer stays at the 2000-token budget (layer.rs:94-95) enforced at
    write time only.
  - `MASTER-PLAN.md:68` — "instruction and hot-memory content use distinct
    replaceable projections" | **current** | unified_memory.rs:28-29,
    instruction_provider.rs:118-120.
  - `echo-agent/docs/zh/25-self-improvement.md:258` — "Dreaming 独立执行基于
    recall/inactivity 的确定性晋升、复活和归档" | **current** |
    dreaming.rs:126-176 (deterministic, no semantic merge).
  - `MASTER-PLAN.md:466-467` — sample workspace switching + long-lived
    sessions to verify instruction projection replacement, Dreaming's first
    pass, hot-memory budget | **current as a plan**; the hot-memory
    projection replacement arm is precisely the unverified/regressed path
    (P1-01).
- Log/artifact: none

## Deviations

No documentation claim was found that is contradicted beyond the recorded
rows; the two regressed rows are the P1-01 evidence anchors.

## Conclusion

Supports: the immediate-projection invariant is a documented contract that
current code violates (regressed); the channels-mode Dreaming claim is stale;
the remaining memory/Dreaming claims are current.

## Follow-Up

P1-01 regression fixture; Q-DOC-01 for the closure-doc wording fix.
