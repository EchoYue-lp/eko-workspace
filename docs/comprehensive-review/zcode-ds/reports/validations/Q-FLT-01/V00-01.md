# Q-FLT-01 / V00 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories (probe crate `/tmp/qflt01-probe` outside both repos; probe source modified, repos untouched)

## Claim

The fault-injection-relevant authorities (chunk processing, tool-args parse,
spill/truncation, retry, batch timeout/cancel, checkpoint/resume) exist as a
single implementation each in the framework, are reachable from the EKO main
path, and are covered by no existing per-scenario dynamic fixtures (definition
and duplicate search for Q-FLT-01).

## Method

1. Grep across both repositories for parallel implementations of: `process_stream_chunk`,
   `parse_tool_args`, `build_tool_calls_from_map`, `truncate_tool_output`,
   `process_tool_output_for_call`, `retry_llm_call`, `compute_concurrent_tool_batch_timeout`,
   `save_runtime_checkpoint`, `resume_from_state_store`, `envelope_event_stream`,
   `AnthropicStreamEvent`, `parse_sse_chunk`.
2. Reachability trace from EKO entry (`chat_driver.rs:513,538` / `executor.rs:3119-3130`)
   through `run_stream_entry` → `run_stream_channel` → `run_core_loop` →
   `phases::{think,tools,finalize}` and `snapshot.rs::process_tool_output_for_call`.
3. Fixture inventory: existing tests that exercise each fault shape (malformed
   args, empty content, truncated stream, mid-stream error, mid-stream cancel,
   batch timeout, spill, corrupt checkpoint).

## Expected Result

One live authority per semantic; all reachable; the fault-shape fixtures are
mostly absent (consistent with F-RCT-04-P2-01 / F-RCT-05-P2-02 / X-TOL-01 V04-01).

## Result

- Exit code: not-applicable (inspection)
- Summary:
  - No parallel authorities: `process_stream_chunk` (processor.rs:16), `parse_tool_args`
    (processor.rs:104), `build_tool_calls_from_map` (processor.rs:138),
    `retry_llm_call` (run/retry.rs:13), `process_tool_output_for_call`
    (snapshot.rs:926) each have exactly one definition and one live caller chain.
    `truncate_tool_output` exists twice (snapshot.rs:1068 live; execution.rs:215
    consumed only by the dead `process_steps`, F-RCT-02-P3-01) — not a live
    parallel authority.
  - Reachability: EKO drives all turns through the streaming entry
    (chat_driver.rs:513, :538 envelope consumption; executor.rs:3119-3130);
    the loop always uses `process_stream_chunk`; tool output always flows through
    `process_tool_output_for_call` (via `ExecuteStage`/`TruncationStage` in the
    15-stage pipeline, pipeline.rs:716-749).
  - Fixture inventory: no test feeds malformed SSE chunks through the loop;
    no test exercises a truncated stream (EOF without finish) or a mid-stream
    error item; `then_tool_calls` (parallel batch) has zero usages
    (F-RCT-04-P2-01); no corrupt-checkpoint test (F-RCT-05-P2-02); spill tested
    only at unit level (F-EXT-01 V03).
- Artifacts: probe crate `/tmp/qflt01-probe` (lib + 8 scenario binaries).

## Deviations

None.

## Conclusion

Supports: the fault-injection suite runs against the single live authority set;
each scenario validation below (V01-V08) can attribute observed behavior to a
specific source location without ambiguity from parallel implementations.
Does not prove: runtime behavior itself (that is the scenario validations).

## Follow-Up

None (evidence for the task report's Current Path / Layering sections).
