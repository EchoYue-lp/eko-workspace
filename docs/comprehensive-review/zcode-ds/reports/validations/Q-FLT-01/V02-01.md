# Q-FLT-01 / V02 / Attempt 01

> Status: passed (with canonical caveats)
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories; probe crate `/tmp/qflt01-probe`

## Claim

Fault scenario 2 (Unicode — CJK/emoji in tool args and messages): the
Agent/tool loop preserves CJK/emoji content and tool arguments byte-for-byte
and never panics on them; UTF-8 truncation is char-boundary safe. The known
byte-slice panics live in peripheral (non-core-loop) paths and are cited as
canonical evidence, not re-filed.

## Method

Real code path:

```text
cd /tmp/qflt01-probe && cargo build
./target/debug/scn2_unicode
# (a) one content chunk "结果: 中文回答 ✅ 完成测试。Emoji 🎉 和中文混排。"
# (b) tool_call("echo_tool", r#"{"path":"/tmp/测试目录/文件😀.txt","note":"你好世界"}"#)
#     → EchoArgsTool (real Tool) → ToolResult → next-request context check
# (c) mirror of snapshot.rs:1025-1040 head/tail truncation on 2,000,000 CJK chars
```

## Expected Result

Final answer byte-identical; CJK args parsed, executed, and present in the
next request's context; truncation produces valid UTF-8 with head+tail kept.

## Result

- Exit code: 0
- Summary (probe stdout):
  ```
  (a) Token("结果: 中文回答 ✅ 完成测试。Emoji 🎉 和中文混排。")
      FinalAnswer("结果: 中文回答 ✅ 完成测试。Emoji 🎉 和中文混排。")
  (b) ToolCall(call-cjk,echo_tool,{"note":"你好世界","path":"/tmp/测试目录/文件😀.txt"})
      ToolResult(call-cjk,echo_tool,len=65,truncated=false)
      model-visible message contains CJK tool result: true   (3rd recorded call)
  (c) preview len=500 (chars) valid_utf8=true
      truncated len=1622 chars, valid_utf8=true, head_tail_preserved=true
  ```

## Deviations

- Canonical byte-slice panics remain live at these commits (scenario
  evidence, not new): X-INV-01-P1-01 (`parse_pdf_date`, pdf.rs:225-227),
  X-INV-01-P1-02 (`extract_number_near_key`, eval/runner.rs:728),
  Q-STA-01-P1-01 (`percent_decode`, web/providers/utils.rs:32), Q-STA-01-P2-01
  (`parse_guard_response`, guard/llm.rs:101). All are non-core-loop paths
  (pdf tool, eval runner, web-search fallback, opt-in guard). The core loop
  itself (chunk accumulation processor.rs:16-88, tool-args parse
  processor.rs:104-135, spill preview snapshot.rs:968, truncation
  snapshot.rs:1033-1035) uses char iteration exclusively.
- The JSON key order in the echoed args changed (serde_json map order) —
  semantic content identical, expected.

## Conclusion

Supports: the core Agent/tool path survives Unicode fault injection
(content, args, results, truncation all preserved; no panic on 2M CJK chars).
Does not prove: the four canonical peripheral panics fixed (they are not);
they remain open canonical P1/P2 defects reachable from registered tools.

## Follow-Up

None (references canonical IDs; no new finding from this scenario).
