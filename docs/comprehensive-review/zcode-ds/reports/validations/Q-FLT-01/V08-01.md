# Q-FLT-01 / V08 / Attempt 01

> Status: failed (invariant violation; canonical F-RCT-04-P2-02 / X-TOL-01-P2-01 reconfirmed)
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories; probe crate `/tmp/qflt01-probe`

## Claim

Fault scenario 8 (partial side effects — tool executed or may have partially
executed while the turn/stream failed): the model is always told about an
interrupted tool; a possibly-side-effecting tool never silently disappears.

## Method

Real code path:

```text
cd /tmp/qflt01-probe && cargo build
./target/debug/scn8_partial
# (a) SleepTool (30 s, default risk_level = Standard = possibly side-effecting)
#     + tool_execution timeout_ms=300, retry_on_fail=true, max_retries=1 →
#     per-tool timeout → inspect ToolError failure classification and the
#     next request's model-visible messages
# (b) cancel mid-batch of the same tool → count ToolError events and terminals
# (c) static: sanitize_tool_call_pairing placeholder strings
```

## Expected Result

(a) a classified failure (Timeout + PartialSideEffect semantics) reaches the
model; (b) the in-flight tool gets a `ToolError` (or an explicit
partial-side-effect note) on batch kill.

## Result

- Exit code: 0
- Summary (probe stdout):
  ```
  (a) ToolError(call-write,write_tool,...,cat=Timeout,side=Possible)
      ToolBatchEnd
      model-visible: "[Error] Tool Error: Tool 'write_tool' execution failed:
        Tool Error: Tool 'write_tool' execution timed out"
  (b) cancel mid-batch: tool_error_count=0 tool_batch_end=true terminal_count=0
  (c) sanitize placeholders (source facts, compression/mod.rs:1619/1642/1697):
      all-orphaned → DanglingCallCleared (silent)
      partial → "[Result unavailable — tool result was removed during context
        compression]" with name "unknown" — attributes the loss to COMPRESSION
  ```

## Deviations

- (a) SURVIVES: the per-tool timeout path produces a classified
  `ToolError{category: Timeout, side_effect: Possible}` and the model-visible
  context carries "[Error] ... timed out" (tools.rs:252-256); per
  `allows_automatic_retry` (echo-core/src/tools/mod.rs:154-162) a possibly
  side-effecting tool is NOT auto-retried. The failure taxonomy reaches the
  EKO boundary on this path (X-TOL-01 V02 conformant).
- (b) FAILS: on batch kill (cancel/timeout), in-flight tools receive NO
  `ToolError` and no partial-side-effect note; the only later context repair
  is `sanitize_tool_call_pairing`, which either silently clears the orphaned
  call or inserts a placeholder that blames compression and never mentions a
  possible partial write — canonical F-RCT-04-P2-02, reconfirmed; the EKO
  boundary can only record a bare "cancelled" — canonical X-TOL-01-P2-01.

## Conclusion

Supports: the per-tool timeout path preserves the partial-side-effect
classification; the batch-kill path loses it entirely (canonical defects,
dynamically confirmed). The "may have partially executed" warning never
reaches the model on the kill path — the local-assistant correctness hazard
(AGENTS.md data-loss protections) remains.
Does not prove: shell/git process-level partial effects (F-EXT-02 scope).

## Follow-Up

None (canonical references only; no new finding from this scenario).
