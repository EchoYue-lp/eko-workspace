# Q-FLT-01 / V04 / Attempt 01

> Status: failed (1 invariant violation → finding Q-FLT-01-P1-02)
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories; probe crate `/tmp/qflt01-probe`

## Claim

Fault scenario 4 (timeouts): (a1) LLM timeouts are retried per the documented
retry policy (`llm_max_retries`); (a2) the most common timeout shapes
(first-chunk / idle / overall, client.rs:240-301) are yielded as stream items
and either retried or surfaced as typed terminals; (b) per-tool timeouts are
classified with failure taxonomy and the batch continues; (c) the batch-timeout
arm is statically known (canonical F-RCT-04-P1-02).

## Method

Real code path:

```text
cd /tmp/qflt01-probe && cargo build
./target/debug/scn4_timeout
# (a1) chat_stream returns Err(NetworkError "first-chunk timeout") 4×
#      (ScriptedLlmClient::with_call_start_err) — call-start failure shape
# (a2) stream yields Content("partial thinking…") then
#      Err(NetworkError "idle timeout") — mid-stream timeout shape
# (b) SleepTool (30 s) + tool_execution timeout_ms=300, retry_on_fail=true
#     max_retries=1 → per-tool timeout
# InMemoryRunStore attached in (a1) for trace status.
```

## Expected Result

(a1) 4 LLM attempts with backoff, then a typed error; (a2) the idle timeout is
also covered by retry (or at least the error is retried); (b) `ToolError`
with `failure.category=Timeout`, `side_effect=Possible`, no auto-retry for the
write-risk tool; trace finalized truthfully in both (a) cases.

## Result

- Exit code: 0
- Summary (probe stdout):
  ```
  (a1) ErrItem(LLM Error: Network error: LLM stream first-chunk timeout after 30000ms)
       ended_with_err=true terminal_count=0 calls=4 elapsed_ms=4934
       [trace a1] status=Running final_output_len=0
  (a2) ErrItem(LLM Error: Network error: LLM stream idle timeout after 60000ms)
       ended_with_err=true terminal_count=0 calls=1
  (b) ToolError(call-slow,slow_tool,...,cat=Timeout,side=Possible)
      ToolBatchEnd  (batch continued)
  ```

## Deviations

- (a2) FAILS: the mid-stream idle timeout is **never retried** (calls=1) —
  `retry_llm_call` (run/retry.rs:13) wraps only the `chat_stream()` creation;
  all three configured stream timeouts (first-chunk, idle, overall) fire
  *inside* the returned stream (client.rs:261-302) and arrive at
  `run_think` as a stream item error, which `try_send_or!`
  (stream_macros.rs:65-75) forwards and then abandons the turn — new finding
  Q-FLT-01-P1-02. The turn ends with no terminal event and the trace stays
  `Running` (trace finalization gap, canonical F-RCT-03-P2-04 / F-RCT-04-P1-02
  class).
- (a1) call-start failures ARE retried (calls=4, ~4.9 s backoff) but still
  end with no terminal event and trace `Running` (same canonical class).
- (b) SURVIVES: per-tool timeout is classified `Timeout` + `side_effect
  Possible` and — per `ToolFailure::allows_automatic_retry`
  (echo-core/src/tools/mod.rs:154-162) — is NOT auto-retried for
  possibly-side-effecting tools; the batch continues (ToolBatchEnd).
- (c) static: the batch-timeout arm (phases/tools.rs:284-292) uses
  `try_send_or!` — the error can be dropped on a full buffer and no per-call
  `ToolError` is emitted; canonical F-RCT-04-P1-02 (not re-filed).

## Conclusion

Supports: the per-tool timeout path survives with full classification; the
LLM retry policy covers only call-start transport errors, not the configured
stream timeouts (new P1-02); timeout/error abandons leave the trace `Running`
with no typed terminal (canonical, dynamically confirmed here).
Does not prove: real-network timeout timing (env-var configurable timeouts are
transport-internal; the error shapes were injected at the exact points the
transport yields them).

## Follow-Up

Finding Q-FLT-01-P1-02 in the task report.
