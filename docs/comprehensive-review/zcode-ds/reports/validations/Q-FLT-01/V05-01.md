# Q-FLT-01 / V05 / Attempt 01

> Status: failed (invariant violation; canonical F-RCT-03-P1-02 / F-RCT-05-P1-01 reconfirmed)
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories; probe crate `/tmp/qflt01-probe`

## Claim

Fault scenario 5 (cancellation mid-stream): per the trait contract
(echo-core/src/agent/mod.rs:552-553, 617-618), a cancelled turn terminates
with exactly one `AgentEvent::Cancelled`; cancel mid-tool-batch leaves no
poisoned recovery state.

## Method

Real code path:

```text
cd /tmp/qflt01-probe && cargo build
./target/debug/scn5_cancel
# (a) MockLlmClient::with_delay(30 s) + execute_stream_with_cancel; cancel
#     after 200 ms (mid-LLM-call)
# (b) tool_call(slow_tool) + SleepTool(10 s); cancel on ToolCall event;
#     inspect saved AgentCheckpoint pairing + restore_messages()
# plus rerun of the F-RCT-05 probe:
cd /tmp/frct05-probe && ./target/debug/frct05-probe
```

## Expected Result

(a) a `Cancelled` terminal event; (b) either no checkpoint or a restorable one
(paired messages).

## Result

- Exit code: 0 (probes; the system behavior under test FAILED the invariants)
- Summary (scn5 stdout):
  ```
  (a) Err(mock LLM stream cancelled)
      saw_cancelled=false saw_final=false saw_err=true items=1
  (b) ToolBatchStart(1) ToolCall(write-1,slow_tool) ToolBatchEnd
      saw_tool_call=true saw_tool_error=false saw_tool_batch_end=true terminal_count=0
      checkpoint: blocked_reason=Some("Tool batch cancelled")
      unpaired_tool_calls=["write-1"]
      restore_messages=Err("...checkpoint has tool calls without results: write-1")
  ```
- frct05-probe rerun (same commits): identical result —
  `blocked_reason=Some("Tool batch cancelled")`, unpaired `["write-1"]`,
  `restore_messages() = Err(...)`.

## Deviations

- (a) FAILS the documented contract: cancel mid-LLM surfaces as an error item
  (mock) or a silent stream end (real transport, F-LLM-01-P3-01), never a
  `Cancelled` event — canonical F-RCT-03-P1-02 reconfirmed dynamically.
- (b) FAILS recovery safety: the cancel arm checkpoints an assistant tool call
  with no result; the next resume rejects the checkpoint and the restore
  fallback wipes the conversation context to the system prompt — canonical
  F-RCT-05-P1-01 reconfirmed dynamically at these commits. Also no typed
  terminal on cancel (F-RCT-04-P1-02 class; X-EVT-01-P1-01).

## Conclusion

Supports: cancellation is the worst-surviving scenario — no typed terminal,
and the recovery path is actively poisoned (context wipe on next turn).
All three defects are canonical (F-RCT-03-P1-02, F-RCT-05-P1-01, F-RCT-04-P1-02
/ X-EVT-01-P1-01); this validation adds the at-commit dynamic reconfirmation.
Does not prove: EKO-surface rendering of the cancelled turn (A-CHAT-01 scope).

## Follow-Up

None (canonical references only; no new finding from this scenario).
