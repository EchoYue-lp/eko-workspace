# Q-FLT-01 / V07 / Attempt 01

> Status: failed (invariant violations; canonical F-RCT-05-P1-01/P2-01/P3-01 reconfirmed)
> Validation date: 2026-08-12
> Executor: ZCode-ds (subagent review)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean in both repositories; probe crate `/tmp/qflt01-probe`

## Claim

Fault scenario 7 (process crash and recovery): (a) checkpoint saves are
crash-safe (atomic tmp+fsync+rename, state/file.rs:206-230) and round-trip;
(b) a corrupt checkpoint file surfaces explicitly and the only recovery copy
is never silently destroyed; (c) recovery after an interrupted batch preserves
conversation context (canonical F-RCT-05-P1-01).

## Method

```text
cd /tmp/qflt01-probe && cargo build
./target/debug/scn7_crash
# (a) FileRuntimeStateStore::new(tmp) → save_checkpoint → get_checkpoint;
#     scan for *.tmp residue
# (b) truncate the checkpoint file mid-JSON → get_checkpoint → then a fresh
#     save_checkpoint → re-read the file to see whether the corrupt copy was
#     preserved or overwritten
# plus rerun: cd /tmp/frct05-probe && ./target/debug/frct05-probe
# (cancel mid-batch → checkpoint → restore_messages) — crash-recovery shape
```

## Expected Result

(a) round-trip Ok, zero tmp residue; (b) explicit error AND the corrupt file
preserved (renamed or untouched) before the next save; (c) recovery preserves
pre-interrupt context.

## Result

- Exit code: 0 (probes; the system behavior under test FAILED (b) and (c))
- Summary (scn7 stdout):
  ```
  (a) round_trip_ok=true tmp_residue=0
  (b) after truncation get_checkpoint =
        Err("parse .../checkpoint.json: EOF while parsing a string at line 3 column 98")
      after next save: file replaced (corrupt copy preserved=false)
  ```
- frct05-probe rerun (same commits): `blocked_reason=Some("Tool batch
  cancelled")`, unpaired `["write-1"]`, `restore_messages()=Err(...)` — the
  interrupted-batch checkpoint is rejected and the restore fallback
  (`run/context.rs:245-248` → `reset_messages()`) wipes the conversation
  context (canonical F-RCT-05-P1-01).

## Deviations

- (a) SURVIVES: atomic writes work as designed; no torn file can be left by a
  crash during save (rename-based).
- (b) FAILS: a corrupt/truncated checkpoint yields an explicit `Err` on read
  (good), but the very next save silently overwrites it — the only recovery
  copy is destroyed with no backup/rename; the restore path then wipes the
  in-process context (warn + `reset_messages`). Canonical F-RCT-05-P3-01,
  dynamically confirmed.
- (c) FAILS: canonical F-RCT-05-P1-01 reconfirmed at these commits (context
  wipe after cancel-shaped checkpoint); additionally, a process kill
  mid-batch leaves only the PRE-batch checkpoint, so completed side effects
  before it can be replayed (canonical F-RCT-05-P2-01, static).

## Conclusion

Supports: crash-safe writing survives; corrupt-file handling and interrupted-
batch recovery do not (all canonical — P1-01/P2-01/P3-01). The two dynamic
probes plus the F-RCT-05 rerun fix the evidence at the reviewed commits.
Does not prove: EKO conversation-store (`load_messages`) interplay after the
wipe (A-STATE-01 scope).

## Follow-Up

None (canonical references only; no new finding from this scenario).
