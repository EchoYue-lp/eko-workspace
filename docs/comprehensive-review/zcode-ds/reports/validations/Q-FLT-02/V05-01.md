# Q-FLT-02 / V05 / Attempt 01 — Scenario 5: crash — torn final line in events.jsonl

> Status: failed (bricking reproduced; canonical A-TSK-01-P1-01 / A-TSK-04-P1-03)
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The append-only ledger must tolerate a torn final line (the project's own
stated intent, `file_shadow.rs:114-117`): reads succeed with the partial event
dropped and seq continues from the last valid event. Current code hard-errors:
`read_events` fails on any malformed line (`file_shadow.rs:362-379`),
`next_seq` seeds from the same read (`file_shadow.rs:289-299`) so appends fail
too, and `recover_incomplete` can never touch the run (`store.rs:1631-1639`).

## Method

Standalone probe (`/tmp/qflt02-probe/src/bin/s5_torn_tail.rs`): build a valid
run + plan event stream via the real store, then append a truncated JSON line
(cut mid-payload, no trailing newline — exactly what a kill -9 mid-`append_line`
leaves), then call the REAL `read_events`, `get_run`, `claim_task`,
`append_event_line`, and `recover_incomplete`, then manually truncate the file
and confirm the run heals.

## Expected Result

Per the documented intent, reads/writes should survive. Given the code under
test, they are expected to brick.

## Result

- Exit code: 0
- Summary (log: `/tmp/qflt02-probe/out/s5_torn_tail.log`):

```
BEFORE: events.jsonl = 1112 bytes, 2 lines
CRASH SIMULATED: torn tail appended, file now 1248 bytes (last line truncated)
read_events = Err(shadow decode: line 3: EOF while parsing an object at line 1 column 136) — read side BRICKED
get_run = Some("invalid plan: file read: shadow decode: line 3: EOF while parsing ...")
claim_task (post-crash) = Some("invalid plan: file read: shadow decode: line 3: EOF ...")
append_event_line (post-crash) = Some("shadow decode: line 3: EOF while parsing ...")
recover_incomplete = 0 runs (cannot heal the torn-tail run)
get_run after recovery = Some("invalid plan: file read: shadow decode: line 3: EOF ...")
after MANUAL truncation: read_events = Ok(2 events), next seq = 3
```

Persisted before/after snapshots: before = 2 valid lines (1112 bytes); after =
3 lines with a torn tail (1248 bytes) and every read/write/recovery path
failing; after manual truncation the run is readable again (next seq = 3).

## Deviations

The torn line's serde error is reported as "line 3 ... at line 1 column 136"
(the outer label uses the file line index, serde's inner position restarts at
1) — cosmetic only, no behavior impact. `recover_incomplete` returns 0 with a
warn rather than an error, so the zombie run stays Running across every
restart, re-attempting recovery forever.

## Conclusion

This execution supports: one crash during any event append can permanently
brick the run — no claim, replay, or recovery path can execute; only manual
file editing recovers it. Scenario 5 survival verdict: **NOT SURVIVED** —
canonical A-TSK-01-P1-01 / A-TSK-04-P1-03, referenced, not re-filed.

## Follow-Up

None (canonical findings already carry the fix direction: truncate to the last
valid line on a final-line decode error, or length-prefix the records).
