# A-BOOT-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (A-BOOT-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories clean

## Claim

Historical documents' claims about entry-point composition, service parity,
startup recovery, and shutdown are classifiable against current code as
current / fixed / stale / regressed.

## Method

Spot-check of `docs/MASTER-PLAN.md`, `docs/2026-07-17-surface-parity-closeout.md`,
`docs/2026-07-16-agent-lifecycle-audit.md`, and `docs/2026-07-28-app-core-full-audit.md`
against the code anchors verified in V01-V03.

```text
1. MASTER-PLAN.md:17 "echo-agent-cli does not enable SQLite"
2. MASTER-PLAN.md:69 Iteration 2 "one Arc<WebhookEmitter> shared by chat,
   scheduler, and the active surface ... config reload ... hot-reloads both
   hooks and webhook endpoints; Model/MCP/runtime topology still requires
   restart"
3. MASTER-PLAN.md:73-74 Plugin wiring + Hook execution closure claims
4. surface-parity-closeout.md scope: "Foreground, background, and cron runs"
   across GUI/TUI/CLI/IM channels/cron-triggered runs
5. agent-lifecycle-audit.md:163 "recover_incomplete() 在启动时把所有 Running
   改为 Failed" and :133 "task_runtime.db / canonical SQLite 过时注释 ...
   P2 文档清理"
6. app-core-full-audit.md (spot grep for startup/shutdown claims)
```

## Expected Result

Each claim maps to a current code anchor or is explicitly classified.

## Result

- Exit code: not-applicable (document comparison)
- Summary (classification table in task report):
  - MASTER-PLAN.md:17 (no SQLite in CLI) — **current**: cargo tree confirms
    (V04-03).
  - MASTER-PLAN.md:69 (shared WebhookEmitter; config reload scope) —
    **current**: desktop.rs:196/modes.rs:58 override from_shared's emitter;
    watcher reloads hooks + webhook endpoints only (config_watcher.rs:1-14);
    both entries spawn the watcher.
  - MASTER-PLAN.md:73-74 (plugin/scheduler binding; hook reload) — **current**
    for the boot binding paths (main.rs:267-274, desktop.rs:233-240 bind the
    scheduler into the plugin runtime; watcher reloads hooks).
  - surface-parity-closeout.md (foreground/background/cron on all surfaces) —
    **regressed (three gaps)**: channels-only mode starts neither
    BackgroundTaskService nor Scheduler (main.rs:391-403 vs
    start_headless_services callers main.rs:258 / modes.rs:83); GUI never
    runs session-end memory review (on_session_end callers only main.rs:309,
    repl.rs:419/454); channels mode has no Dreaming spawn (call sites
    tui/mod.rs:1999, desktop.rs:247, repl.rs:106 only). B-PATH-01 already
    classified the GUI-only MCP health loop the same way.
  - agent-lifecycle-audit.md:163 (recover_incomplete sets Running -> Failed)
    — **fixed**: current `recover_incomplete` transitions interrupted runs to
    Paused ("recover_incomplete: failed to transition run to Paused",
    store.rs:1771; boot call sites main.rs:52, state.rs:557 with P1-8 comment
    "into resumable Paused runs", state.rs:555-556).
  - agent-lifecycle-audit.md:133 (stale `task_runtime.db`/SQLite mentions) —
    **current (known-stale)**: state.rs:931 still logs
    `runtime_state.db` while the store is file-backed (infra.rs:1250-1254);
    infra.rs:125 still says "sqlite-backed" for `state_store`. The audit
    itself classified these as P2 doc cleanup; they remain unfixed — recorded
    as A-BOOT-01-P3-05.
  - app-core-full-audit.md: no boot-specific claim contradicting current
    code found in the sampled sections (claim about generic chat cleanup
    ownership belongs to A-CHAT-01).

## Deviations

Only sampled the four documents; full B-DOC-01-style link validation is out
of scope for this task.

## Conclusion

Supports: the historical parity claims are regressed on the same three gaps
this task independently found (channels services, GUI session-end review,
channels Dreaming); the recovery-behavior claim is fixed; the SQLite/
runtime_state.db doc drift is still present. Feeds the Historical Claim Status
section of the task report.

## Follow-Up

None.
