# A-EVO-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The app-core evolution module (EvidenceStore Review Inbox accept/undo/merge/
stale handling, RulePromoter scan gates, ReviewIntegration rebind/observer/
review-gate, Dashboard diagnostics) passes its unit test suite.

## Method

Run the app-core evolution unit tests in
`/Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli`:

```text
cargo test -p echo-agent-app-core --lib evolution::
```

## Expected Result

All `evolution::*` tests pass; exit code 0.

## Result

- Exit code: 0
- Duration: 0.20 s (after compile)
- Summary:
  - 19 passed, 0 failed, 0 ignored (630 filtered out). Covers the
    review-gate invariants exercised by tests:
    `accept_and_undo_are_backed_by_layered_memory`,
    `memory_merge_candidate_accepts_and_restores_exact_metadata`,
    `stale_memory_merge_candidate_fails_before_mutation`,
    `memory_conflicts_are_queued_without_mutating_the_store`,
    `evolution_observer_survives_rebind_and_manager_rebuild`,
    `scan_hits_memories_written_to_warm_namespace`,
    `test_review_integration_session_end_disabled`.
- Log/artifact: none (tail output only).

## Deviations

None.

## Conclusion

Supports: the EKO Review Inbox / rule-promotion / review-integration /
dashboard behavior claimed in V03 (fail-before-mutation on stale merges,
rollback on accept/undo, observer survival across rebind, session-end
disabled by default) is exercised by green tests. Does not prove the
automatic-path behaviors (Dreaming, reflection-on-exit, L3 promoter) — those
have no unit tests on the EKO side. Compile status of the workspace is
covered by V04-02.

## Follow-Up

V04-02 (all-feature compile check), V05-01 (document drift).
