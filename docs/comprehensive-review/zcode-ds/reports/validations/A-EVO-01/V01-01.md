# A-EVO-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every evolution-surface concept on the EKO side (rule promotion, evidence
inbox, review integration, dashboard, curator, dreaming, auto-memory,
reflection, skill lifecycle) has exactly one authoritative definition across
both repositories — the framework defines the generic mechanism and EKO only
adds product adapters — with no parallel implementation of the same semantics
in `echo-agent-cli`.

## Method

Repository-wide grep (definition search) across `echo-agent` and
`echo-agent-cli` for the evolution surface terms and struct names:

```text
grep -rn "RulePromoter\|EvidenceStore\|ReviewIntegration\|Dashboard\|Curator\|Dreaming\|MemoryLayerManager\|MemoryReviewer\|BackgroundReviewer\|SkillPatcher\|SkillMerger\|SkillHealthMonitor\|SkillCandidateDetector\|SkillDraftGenerator\|ChangeLog" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src --include="*.rs"
grep -rn "pub struct <Name>" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src --include="*.rs"   # duplicate-definition probe
grep -rn "evolution\|进化\|curator\|dreaming\|evidence\|promot" echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src --include="*.rs" -l  # surface inventory
```

Working directory: `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`. No
feature flags; static source inspection only.

## Expected Result

EKO defines only product-level types (`RulePromoter`, `EvidenceStore`,
`ReviewIntegration`, `Dashboard`, `DashboardMetrics`) and reuses framework
types (`Curator`, `Dreaming`, `MemoryLayerManager`, `MemoryReviewer`,
`BackgroundReviewer`, `Skill*`, `ChangeLog`, `HookEvolutionObserver`,
`MemoryTriggerSink`) without redefining them.

## Result

- Exit code: not-applicable (static inspection)
- Summary:
  - Framework `src/evolution/` contains the single authority for each generic
    mechanism: `audit.rs`, `auto_memory.rs`, `background_review.rs`,
    `candidate.rs`, `curator.rs`, `draft.rs`, `dreaming.rs`, `health.rs`,
    `layer.rs`, `merge.rs`, `patch.rs`, `recall.rs`, `review.rs`,
    `runtime_integration.rs`, `security.rs`, `triggers.rs`.
  - EKO-only product types found exactly once each:
    `echo-agent-cli/echo-agent-app-core/src/evolution/rule_promoter.rs:63`
    (`RulePromoter`), `review_integration.rs:37` (`ReviewIntegration`),
    `evidence.rs:224` (`EvidenceStore`), `dashboard.rs:21/70`
    (`DashboardMetrics`/`Dashboard`). No duplicate `pub struct` for any
    framework evolution type exists anywhere in `echo-agent-cli`.
  - EKO surface inventory (definition + registration):
    - app-core `evolution/` module (mod.rs:7-24 re-exports) used by
      `infra.rs`, `state.rs`, `agent_pool.rs`, `runtime.rs`,
      `instruction_provider.rs`, `auto_memory/mod.rs`,
      `tasks/task_runtime/memory_bridge.rs` (13 files reference the module).
    - CLI `src/cli/cmd_impls/evolution.rs` (17 commands registered in
      `register_all` :1831-1850, REPL registration at
      `src/cli/repl.rs:172`).
    - TUI `src/tui/events.rs` slash commands AutoMemory/RunReview/
      EvidenceInbox/MemoryReview/SkillCandidates/EvolutionDashboard
      (enum `src/tui/commands.rs:100-131`).
    - Tauri `src/tauri/commands/panels.rs` review_run/list_evidence_candidates/
      evidence_candidate_action/curator_action/get_evolution_dashboard/
      scan_rule_proposals/promote_rule/scan_skill_candidates/
      generate_skill_draft/activate_skill_draft; `src/tauri/mod.rs:236-238`
      registration; frontend `web-frontend/src/components/evolution/
      EvolutionPanel.tsx` + `api/endpoints.ts:1440-1521` (`evolutionApi`).
  - The only EKO-defined near-framework concept is `EvidenceStore`/Review
    Inbox (JSONL candidate protocol) — a documented EKO product policy
    (roadmap `docs/2026-07-15-self-evolution-review-and-roadmap.md:97`), not
    a duplicate of a framework type.

## Deviations

None.

## Conclusion

Supports: single-authority definitions for every evolution concept across
both repositories; EKO evolution surface is an adapter layer over framework
mechanisms with product policy (Review Inbox, learned-rules.md rule
promotion, workspace curator binding). Does not prove runtime reachability of
each surface (V02) or the invariants (V03).

## Follow-Up

V02-01 (registration and reachability), V03-01 (invariants).
