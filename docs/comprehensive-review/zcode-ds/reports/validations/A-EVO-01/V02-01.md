# A-EVO-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Every mutation-capable evolution surface in EKO is registered and reachable
from a live entry point, and the inventory of reachable mutation triggers can
be enumerated and classified as automatic vs user-authorized.

## Method

Static call-graph tracing with grep, starting from the entry points and
following each evolution trigger to its registration point and runtime
caller. Working directory `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`.

```text
grep -rn "spawn_dreaming_task" echo-agent-cli/src --include="*.rs"        # Dreaming spawn sites
grep -rn "detect_and_write_memory_triggers" echo-agent/src/agent/react --include="*.rs"  # per-turn trigger capture
grep -rn "set_memory_trigger_sink" echo-agent-cli --include="*.rs"        # sink registration
grep -rn "run_reflection_on_exit\|run_auto_memory_on_exit\|run_memory_review_on_exit" echo-agent-cli/src/cli/repl.rs
grep -rn "on_session_end" echo-agent-cli/src --include="*.rs"
grep -rn "apply_transitions" echo-agent/src --include="*.rs" | grep -v curator.rs   # curator auto-run search
grep -rn "AUTO_MEMORY_ENABLED" echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src --include="*.rs"
```

## Expected Result

(a) The trigger-detection path is reachable every turn on the main agent;
(b) Dreaming is spawned on GUI/TUI/CLI-REPL but not channels; (c) curator
auto-transitions are user-triggered only; (d) auto-memory flag gates explicit
commands only; (e) session-end review defaults off; (f) an exhaustive
reachable-mutation-trigger list can be produced.

## Result

- Exit code: not-applicable (static inspection)
- Summary — reachable mutation triggers inventory:

  **Automatic (no user action):**
  1. L3 `StoreMemoryPromoter` — every compression pass
     (`echo-agent/echo-state/src/compression/mod.rs:836-838` →
     `echo-agent/src/memory_promoter.rs:70-94`); EKO enables the compressor on
     the primary agent and pool (`agent_pool.rs:947-949`).
  2. Dreaming pass — `spawn_dreaming_task` (app-core `infra.rs:1143`; spawn
     sites `src/tauri/desktop.rs:247`, `src/tui/mod.rs:1999`,
     `src/cli/repl.rs:106`; none in `src/cli/channels.rs`) — first pass 60 s
     after boot (`infra.rs:1156`), then daily (`infra.rs:1158`);
     promote/revive/archive via layer manager (`infra.rs:1203-1212`).
  3. TriggerDetector capture — per turn, main path
     (`echo-agent/src/agent/react/run/react_loop.rs:558`, context.rs:509/579
     → `detect_and_write_memory_triggers` context.rs:34) → EKO
     `ReviewIntegration::on_trigger` (review_integration.rs:277-321) captures
     to the Review Inbox; returns `Captured`, no durable write; fail-closed on
     inbox error (:314-318). Sink registered on primary agent
     (`runtime.rs:256`) and pool agents (`agent_pool.rs:924`).
  4. Curator `last_used_at` telemetry per tool batch
     (`echo-agent/src/agent/react/run/execution.rs:136-152`) — metadata only.
  5. CLI REPL exit auto-reflection — `run_reflection_on_exit` (`repl.rs:256`,
     def :340-411) — LLM call + append to `.eko/memory/PROJECT.md`.

  **User-authorized (explicit command/button/tool call):**
  6. `/remember` `/forget` CLI (`cmd_impls/all.rs`), TUI (`tui/events.rs:2826,
     2870`), GUI `add_memory`/`delete_memory` (`tauri/commands/memory.rs:86,192`).
  7. `remember` tool (framework builtin, agent-visible tool call).
  8. Review Inbox accept/undo — CLI `/evidence-inbox` (`evolution.rs:1781-1811`),
     TUI (`tui/events.rs:3832`), GUI `evidence_candidate_action`
     (`panels.rs:1329-1368`) → `EvidenceStore::accept/undo` (evidence.rs:478,
     638) with rollback.
  9. Rule promotion — CLI `/rule-promote` (`evolution.rs:1415`), GUI
     `promote_rule` (`panels.rs:1515`) → `RulePromoter::promote_rule`
     (rule_promoter.rs:178) writes learned-rules.md + PROMOTED_TO_RULE marker.
  10. Curator run — CLI `/curator run` (`evolution.rs:185`), GUI
      `curator_action "run"` (`panels.rs:1392`) → `Curator::apply_transitions`
      (framework curator.rs:504). No automatic caller found.
  11. Skill lifecycle — CLI `/skill-create`, `/skill-promote`,
      `/skill-register`, `/skill-pin`, `/skill-unpin`, `/skill-merge`,
      `/skill-patch <n> apply <i>` (`evolution.rs`); GUI
      `generate_skill_draft`/`activate_skill_draft` (`panels.rs:1664,1711`).
  12. Review runs — CLI `/review` (`evolution.rs:36`), `/memory-review`
      (`:588`); TUI `/run-review` (`tui/events.rs:3697`), `/memory-review`
      (`:3999`); GUI `review_run` (`panels.rs:1199`). BackgroundReviewer with
      `BackgroundReviewConfig::default()` (`auto_persist_user_preferences:
      false` — proposal-only); MemoryReviewer analysis → conflict proposals to
      inbox.
  13. Auto-memory — CLI/TUI `/auto-memory on|off|extract`, GUI
      `extract_auto_memory` (`panels.rs:283`), CLI REPL exit
      `run_auto_memory_on_exit` (`repl.rs:253` — queues to inbox only).
      `AUTO_MEMORY_ENABLED` static (`all.rs:462`) read only inside these
      handlers; no background loop.
  14. `/profile set|reset|refresh` (CLI, `evolution.rs:413`).
  15. Dashboard — CLI `/evolution-dashboard` (`evolution.rs:1526`), TUI
      (`tui/events.rs:3972`), GUI `get_evolution_dashboard`
      (`panels.rs:1462`) — read-only, computed on demand.

  **Registration facts:** all 17 CLI evolution commands registered
  (`evolution.rs:1831-1850`) and registered in REPL only (`repl.rs:172`);
  Tauri commands registered (`src/tauri/mod.rs:236-238`); frontend
  `evolutionApi` calls them (`web-frontend/src/api/endpoints.ts:1440-1521`).

- Deviations from expectation: `run_reflection_on_exit` (trigger 5) is an
  automatic LLM write found outside the review-gate model — recorded as
  A-EVO-01-P1-01. Channels mode has no Dreaming spawn (cross-reference
  A-MEM-01-P3-01, confirmed by grep: zero `spawn_dreaming_task` hits in
  `src/cli/channels.rs`).

## Deviations

None beyond those recorded above (they became findings, not measurement
failures).

## Conclusion

Supports: every evolution surface is registered and reachable; the
reachable-mutation-trigger inventory above is complete for the surfaces this
task covers. Does not prove authorization-boundary correctness (V03) or
behavioral soundness of each trigger.

## Follow-Up

V03-01 (user authorization boundaries and dead/aspirational classification).
