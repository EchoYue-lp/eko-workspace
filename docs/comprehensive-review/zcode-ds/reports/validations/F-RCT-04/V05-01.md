# F-RCT-04 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical claims about tool batch execution in the shared planning documents
can be classified as current, fixed, stale, or regressed against the reviewed
code, and the classification is anchored to source lines.

## Method

Search `docs/MASTER-PLAN.md` and `docs/PROJECT-ANALYSIS.md` for batch/tool
execution claims and verify each against current code anchors.

```text
grep -rn "工具.*批次|tool batch|batch timeout|并发.*工具|串行.*工具|工具结果|cancel|取消" \
     docs/MASTER-PLAN.md docs/PROJECT-ANALYSIS.md
then per-claim code anchor check (phases/tools.rs, echo-state/src/compression,
snapshot.rs, echo-execution/src/tools.rs, run/retry.rs).
```

## Expected Result

Each claim resolves to a verifiable anchor or is explicitly unverifiable.

## Result

- Exit code: not-applicable (static)
- Summary of classifications (full table in the task report):
  - MASTER-PLAN:96 and :147 — batch completion checkpoint +
    completed-work-skip on resume: current for the checkpoint site
    (`run_tools` `save_runtime_checkpoint` after all results, phases/tools.rs:429,
    and per-error checkpoints at :258-262,:414); resume/skip semantics deferred
    to F-RCT-05.
  - MASTER-PLAN:98 — "明确成功/失败/取消终态" for tool execution: regressed
    (partial) for the batch path — a batch cancelled by the user ends with
    `ToolBatchEnd` and channel close, with no cancelled terminal event
    (phases/tools.rs:295-300,418-423; driver stream_channel.rs:686-687).
  - MASTER-PLAN:149 — "取消必须…最终只产生一个 cancelled terminal":
    regressed for the main-loop batch path — no `AgentEvent::Cancelled` is
    produced on batch cancel or batch timeout (consistent with
    F-RCT-02-P3-03; `AgentEvent::Cancelled` producers are subagent dispatch
    only, `src/agent/subagent/executor.rs:2013`).
  - MASTER-PLAN:472 (M4) — unified failure classification with limited retry:
    current at the manager level (`ToolFailure` categories and
    `allows_automatic_retry`, echo-core/src/tools/mod.rs:20-198); the
    batch-level kill path bypasses the classification entirely (no failure
    record for in-flight tools) — see F-RCT-04-P2-02.
  - MASTER-PLAN:115, :275, :602 — oversized tool results as artifact + bounded
    preview: current (`process_tool_output_for_call`, snapshot.rs:926-1060;
    TruncationStage pipeline.rs:716-749).
  - PROJECT-ANALYSIS:245 — EKO writer subagent concurrency note: not
    revalidated (owned by A-TSK-05/F-EXT-02 scope).

## Deviations

None.

## Conclusion

Supports the Historical Claim Status section of the task report: the batch
checkpoint and artifact claims are current; the cancelled-terminal claims are
regressed for the batch path.

## Follow-Up

None.
