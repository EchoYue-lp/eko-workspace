# F-NBK-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

`echo_agent` compiles with `--no-default-features` even though `pub mod
notebook;` (`src/lib.rs:50`) is unconditional — the notebook module adds no
hidden default-feature dependency, so the minimal-posture build is not
distorted by it.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo check -p echo_agent --no-default-features --locked
```

Environment: macOS arm64, stable Rust toolchain, `--locked`.

## Expected Result

Exit code 0 with a clean `Finished` line. If the notebook module had required
default-only dependencies, this check would fail — the result therefore also
characterizes the module's feature posture.

## Result

- Exit code: 0
- Duration: 3 m 07 s (first no-default build)
- Summary: `Finished dev profile` with no errors. The notebook module
  compiles under the minimal feature posture.

## Deviations

None.

## Conclusion

Supports: the notebook module is feature-neutral (compiles with zero default
features) — the aspirational surface does not break minimal builds. Does not
support: any reachability claim (V02). Note: because the module is
unconditional, its code is always compiled even under `--no-default-features`;
per F-API-01-P2-02's pattern this is a (small) unconditional facade module,
which is consistent with the P2-01 cleanup/wiring decision.

## Follow-Up

V04-03 (notebook-targeted test run).
