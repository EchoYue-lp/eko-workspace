# F-NBK-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documents that mention the notebook capability either match current
code or are explicitly classified (fixed/stale), and no live product doc
claims a working notebook feature that does not exist.

## Method

Grep and targeted reads (working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

```text
1. grep -rni "notebook" docs/ echo-agent/docs/ echo-agent-cli/docs/ echo-agent/AUDIT_REPORT.md
2. sed -n '335,365p' echo-agent/AUDIT_REPORT.md          (2.1 finding text)
3. sed -n '75,90p;840,855p' docs/MASTER-PLAN.md          (layering claims)
4. sed -n '80,95p' echo-agent-cli/docs/MASTER-PLAN.md    (no-second-kernel claim)
5. sed -n '1,60p' echo-agent-cli/docs/2026-07-18-file-backed-analysis-workbench.md
6. sed -n '205,225p' echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/profiles.rs
7. sed -n '1,20p' echo-agent/docs/en/03-memory.md        (Notebook as analogy)
8. grep -rni "notebook" echo-agent/README.md echo-agent-cli/README.md
```

## Expected Result

Every historical claim is classifiable as current, fixed, stale, or regressed,
with no contradiction between live product docs and code.

## Result

- Exit code: not-applicable (static doc check)
- Duration: < 5 minutes
- Summary of classifications:
  - `echo-agent/AUDIT_REPORT.md:340-349` (2.1 "RwLock Poison Panics in
    Notebook Module", `unwrap()` at lines 50, 64, 69, 83, 89): **fixed** —
    current code recovers from poison at `src/notebook/mod.rs:52,67,73,88,95`
    (`unwrap_or_else(|e| e.into_inner())`); consistent with zcode-ds
    B-DOC-01 V01-01 classification.
  - Root `docs/MASTER-PLAN.md:81` ("Notebook/报告 UI" belongs to
    `echo-agent-cli` product layer) and `:848` (framework provides generic
    exploration/sandbox primitives only; "没有新增运行状态、数据库、Notebook
    kernel 或统计 DSL"): **current** — EKO's structured-artifact path is the
    file-backed `analysis.rs` contract; no notebook kernel exists in either
    repo. Note: MASTER-PLAN never claims a framework notebook tracker, so the
    dead `src/notebook` module is simply undocumented by the plan — that gap
    is part of F-NBK-01-P2-01.
  - `echo-agent-cli/docs/MASTER-PLAN.md:88` ("EKO does not add a second
    notebook kernel, a statistical DSL, or framework-authored inference
    algorithms"): **current** — verified: no kernel, no DSL.
  - `echo-agent-cli/docs/2026-07-18-file-backed-analysis-workbench.md:28,31`
    (audit section: "`NotebookPanel` 只写浏览器 `localStorage`，未接入主界面"
    and "数据工作区模板创建 `notebooks/analysis.md`，但没有真实执行和
    lineage 合同"): **stale as historical audit** — neither a `NotebookPanel`
    component nor a `notebooks/analysis.md` template exists in the current
    frontend/backend (grep: only generated `ArtifactKind.ts`), and the doc's
    forward contract (file-backed `manifest.json` + script + runs) is
    **current** (`echo-agent-cli/echo-agent-app-core/src/analysis.rs`).
    Because this is a dated design doc (2026-07-18), the audit section
    describes the pre-M13 state and is not a live misclaim; classified rather
    than filed.
  - `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/profiles.rs:218`
    and `subagents/data/analyst.md:16` (live analyst prompt: "do not make an
    in-memory-only notebook the source of truth"): **current** — the product
    direction is file-backed analysis; this corroborates that an in-memory
    notebook recorder is not the EKO mechanism of record.
  - `echo-agent/docs/en/03-memory.md:11` ("**Long-term knowledge** | `Store` |
    Notebook | ..."): **not drift** — "Notebook" is used as an analogy for the
    long-term `Store`, not a reference to `src/notebook`.
  - `echo-agent/README.md` and `echo-agent-cli/README.md`: no notebook
    mention at all — the notebook API is absent from the primary docs (zero
    documentation of the feature anywhere).

## Deviations

None material. The workbench design doc's audit section is stale but
historical; recorded here rather than as a finding to avoid manufacturing
drift from a dated archive document.

## Conclusion

Supports: no live document claims a working notebook feature; the one
documented notebook defect (AUDIT 2.1) is fixed; the product's notebook-adjacent
direction (file-backed analysis, no kernel) matches code. The notebook
capability itself is entirely undocumented, which reinforces the P2-01
"aspirational API surface" classification.

## Follow-Up

None.
