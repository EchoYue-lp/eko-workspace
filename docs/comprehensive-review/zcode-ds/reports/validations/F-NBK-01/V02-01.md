# F-NBK-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The notebook capability is registered and runtime-reachable: a consumer (or the
framework itself) can construct a `NotebookTracker`, recording happens when the
documented `enable_notebook` flag is set, and at least one framework or EKO
path exercises the API.

## Method

Trace definition -> registration -> live caller for every notebook symbol
(working dir `/Users/ls/MyWork/code/ylp_agent_learn/lp-agent`):

```text
1. grep -rn "enable_notebook" echo-agent/src echo-agent-cli --include="*.rs"
2. grep -rn "NotebookTracker::" echo-agent/src echo-agent/examples echo-agent-cli --include="*.rs"
3. grep -rn "record_cell" echo-agent/src echo-agent-cli --include="*.rs" | grep -v notebook/mod.rs
4. git log --oneline -S "NotebookTracker" -- src/   (echo-agent history)
5. git log --oneline -S "record_cell" -- src/
6. git log --oneline -S "enable_notebook" -- src/
7. sed -n '44,60p' echo-agent/src/lib.rs            (module registration)
8. sed -n '160,180p;800,825p' echo-agent/src/agent/config.rs  (flag + builder)
9. grep -rn "ArtifactKind::" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src
10. grep -rni "notebook" echo-agent-cli/web-frontend/src (NotebookPanel search)
```

## Expected Result

At least one production call site constructs `NotebookTracker`, or one reader
of `enable_notebook`, or one tool registration under a notebook name — making
the capability reachable and exercised.

## Result

- Exit code: not-applicable (static trace)
- Duration: < 1 minute
- Summary:
  - `enable_notebook`: defined at `src/agent/config.rs:174` (field,
    `pub(crate)`), set by public builder `src/agent/config.rs:815`
    (`pub fn enable_notebook`) on public `AgentConfig` (`config.rs:44`).
    Zero readers in `echo-agent` (root + all sub-crates) and zero in
    `echo-agent-cli`. The documented contract "each tool invocation is
    recorded as a NotebookCell" (`config.rs:171-173, 807-813`) is honored by
    no code.
  - `NotebookTracker::new()` / `::default()`: zero construction sites outside
    `src/notebook/mod.rs` itself.
  - `record_cell`: zero callers anywhere.
  - Module registration exists: `src/lib.rs:50` `pub mod notebook;` —
    unconditional, no feature gate, not re-exported in `prelude`
    (`src/lib.rs:137+`) or `advanced`; no doctests in the module.
  - No tool named notebook* in any registry (`echo-agent/src/tools`,
    `echo-tools/src`, CLI tool dirs: zero hits).
  - Git history: `NotebookTracker`, `record_cell`, and `enable_notebook` were
    introduced together in `a5feccf` (v0.2.1-dev) and never referenced by any
    later commit; the module's last touch is `c1ae71f` (2026-07-09,
    time-serialization change only).
  - EKO side: `ArtifactKind::Notebook` is constructed nowhere; the only
    matches are the `as_str`/`from_str` round-trip in
    `types.rs:579,591` and a JSON-parse default in
    `file_store.rs:239-242` (which can only ever see the variant if a file
    written by another producer contains the string). `store.rs:2254`
    constructs `ArtifactKind::Trace`, never `Notebook`.
  - Frontend: `web-frontend/src/generated/ArtifactKind.ts:7` contains
    `'notebook'` (generated from the Rust enum); no `NotebookPanel` or other
    notebook component exists anywhere in the frontend.

## Deviations

The reachability claim fails at every hop. Definition and facade registration
exist; runtime registration (tool), construction, recording, flag consumption,
and EKO artifact production do not.

## Conclusion

This result does NOT support the task's required claim ("reachable framework
API"). It supports the opposite: the notebook capability is an
isolated/aspirational API surface — definition present, facade registration
present, runtime reachability absent, exercised behavior absent. This is the
evidence base for F-NBK-01-P2-01 and F-NBK-01-P3-02.

## Follow-Up

V03-01 (invariant inspection on the unexercised surface), V04-01..03
(compile/test evidence), V05-01 (doc drift).
