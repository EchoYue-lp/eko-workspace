# F-PLG-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean (log artifact in /tmp)

## Claim

The EKO plugin runtime integration tests pass, including real plugin load/disable/
enable/uninstall, rollback on failed reload, rollback on lifecycle activation failure,
native lifecycle bracketing, and scheduler lock ordering.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked plugin
# log: /tmp/fplg-v04-appcore.log
```

Environment: macOS arm64, locked dependencies. Tests use `MockLlmClient`, a real
scaffolded plugin fixture directory, a fake LSP server script (unix), and a real
`SchedulerRunner`.

## Expected Result

All `plugin_runtime::tests` and `plugin_components::tests` pass; exit code 0.

## Result

- Exit code: 0
- Duration: ~0.9 s
- Summary: `test result: ok. 10 passed; 0 failed; 0 ignored; 0 measured; 639 filtered out`:
  - `real_plugin_load_disable_and_unload_are_live` — load -> subagent registered, LSP
    running, monitor scheduled, theme/output style projected; disable -> all removed from
    agent/scheduler/LSP/projection; enable -> restored; uninstall -> directory removed,
    registry empty, no stale registrations.
  - `failed_real_reload_restores_previous_live_components` — malformed hooks YAML fails
    reload and restores the previous subagent, themes, output style, monitor, and LSP
    server.
  - `activation_failure_restores_previous_components_and_lifecycle` — injected lifecycle
    activation failure rolls back the new theme and keeps the previous components live.
  - `failed_native_lifecycle_registration_shuts_down_and_can_retry` — failing activation
    during `register_lifecycle` triggers shutdown and allows re-registration.
  - `native_lifecycle_brackets_reload_configure_and_unregisters_on_uninstall` — init once,
    activate/deactivate counts, uninstall -> shutdown once, re-registration after
    reinstall.
  - `scheduler_binding_uses_the_same_lock_order_as_reload` — no deadlock between the
    plugin state lock and the scheduler lock.
  - `theme_and_output_style_preferences_survive_runtime_restart` — preferences persisted
    and re-projected after a fresh service instance.
  - `scaffold_and_validate_cover_application_components` — scaffold round-trip.
  - plugin_components: UTF-8 output-style parsing, variable expansion into themes/styles.
- Log/artifact: /tmp/fplg-v04-appcore.log

## Deviations

None.

## Conclusion

Supports: the EKO reload transaction (V03-02 invariants) is empirically green at the
baseline commit for load/disable/enable/uninstall/rollback/activation-failure paths with
real filesystem, scheduler, and lifecycle fixtures. Does not prove: behavior with a real
LLM/MCP endpoint or across process restarts of the registry state (framework-level
round-trip is unit-tested in V04-01).

## Follow-Up

None.
