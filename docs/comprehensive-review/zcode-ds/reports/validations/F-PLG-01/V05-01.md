# F-PLG-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical plugin-related claims in `docs/MASTER-PLAN.md` (section 二十三.5, lines
935-1010), `echo-agent/README.md`, `echo-agent-cli/README.md`, and
`echo-agent/docs/en/32-plugin-system.md` can be classified against current code as
current / fixed / stale / regressed.

## Method

Claim-to-symbol cross-check by grep and targeted reads. No source modifications.

```text
grep "set_plugin_data_base_dir_name" echo-agent-cli/src/main.rs          -> :70 (".eko")
grep "register_plugin_hooks" echo-agent/src/plugin.rs                    -> :294, :424
grep "PluginIntegrator" echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs -> :818
grep "wire_all" echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs -> :818-819
grep "unregister_by_source|unregister_skills_by_source" echo-agent echo-agent-cli
grep "build_registry" echo-agent-cli/src/tauri/commands/plugins.rs       -> none
grep "list_hooks|reload_hooks" echo-agent-cli/src/tauri/commands/hooks.rs -> :54, :123
grep "PluginScope" docs/en/32-plugin-system.md                           -> :174-194
grep -n "plugin" echo-agent/README.md                                    -> :920, :1114, :1188
```

## Expected Result

MASTER-PLAN milestones P0-1..P0-5 and P1-reload claim states that match current code;
README plugin claims resolve to existing APIs/artifacts; the plugin-system doc's scope
table matches `PluginScope::resolve_dir`.

## Result

- Exit code: 0 (grep/read only)
- Summary of classifications (detailed table in the task report):
  - MASTER-PLAN P0-3 plugin base-dir unification (line 960): **current** —
    `plugin_data_base_dir` + `set_plugin_data_base_dir_name` in
    `echo-core/src/plugin/mod.rs:102-136`; EKO calls `set_plugin_data_base_dir_name(".eko")`
    at `echo-agent-cli/src/main.rs:70`; Project/Local stay `.echo-agent`
    (`scope.rs:38-50`).
  - MASTER-PLAN P0-2 plugin hook source identity (line 961): **current** —
    `HookSource::Plugin` constructed in `register_plugin_hooks` (hooks.rs:610-645), used
    by facade `plugin.rs:294`.
  - MASTER-PLAN P0-2b thin adapter (line 982/1002): **current** — `wire_all` called from
    `plugin_runtime.rs:818-819`.
  - MASTER-PLAN P0-4 shared PluginRuntimeService (lines 983/988): **current** — single
    instance at `runtime.rs:279-280`; TUI/CLI/Tauri all consume it (V02-01); the noted
    limitation "disable/uninstall 不卸载已注册组件" is **fixed** by P1-reload.
  - MASTER-PLAN P1-reload real unload (lines 991/997): **current** — SkillDescriptor
    `source` + `unregister_skills_by_source` (capabilities.rs:915-958) + unload wiring
    (plugin_runtime.rs:1157-1176); the sub-claim "MCP 暂不 disconnect (惰性化)" is
    **superseded** by the newer `disconnect_mcp` call at plugin_runtime.rs:1172-1174.
  - MASTER-PLAN "tauri commands each build their own registry" (line 1008): **stale** —
    current commands use the shared service (`tauri/commands/plugins.rs:3,83`); no
    `build_registry` remains.
  - MASTER-PLAN `wire_skills`/`wire_mcp` retained (line 1009): **current** — both exist
    (plugin.rs:398-444) with zero in-repo callers.
  - MASTER-PLAN P1-frontend (line 998): **current** — `list_hooks`/`reload_hooks` exist
    (tauri/commands/hooks.rs:54,123); CLI `/plugins reload` exists (cmd_impls/plugins.rs:241).
  - `echo-agent/README.md`: plugin example/API claims (**current**) — demo56 entry
    (:1114), plugin docs links (:1188), ChannelPlugin as a channels concept (:920, not the
    plugin framework).
  - `echo-agent-cli/README.md`: plugin system mention (**current**) — `/plugins` command
    exists (cmd_impls/plugins.rs register_all:449).
  - `echo-agent/docs/en/32-plugin-system.md`: scope table and `~/.echo-agent` defaults
    (**current for the framework default** — the EKO `.eko` override is documented app
    policy in MASTER-PLAN P0-3).

## Deviations

None. (The only doc-level oddity: `echo-execution/src/skills/hooks.rs:52-53` documents
PluginLoaded/PluginDisabled in its event table while the authoritative enum lives in
`echo-core/src/hooks/types.rs:190-192` — a doc-location lag, not a claim about behavior;
both events exist.)

## Conclusion

Supports: MASTER-PLAN 二十三.5 claims are overwhelmingly current or fixed; the only
stale claim (per-command registries) was resolved; no regressed plugin claim was found.
Does not prove: behavior claims that require runtime execution (covered by V04).

## Follow-Up

None.
