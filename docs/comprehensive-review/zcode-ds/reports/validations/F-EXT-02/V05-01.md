# F-EXT-02 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

`cargo test -p echo_tools --lib --locked --features "shell,files,git" shell`
passes with a known exit code, exercising shell safety, streaming, timeout,
UTF-8 split, output cap, artifact spill, and child-kill-on-drop behavior.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_tools --lib --locked --features "shell,files,git" shell \
  > /tmp/f-ext-02-shell.log 2>&1; echo "EXIT: $?"
```

Note: echo_tools has `default = []`; the `shell` module and its tests are
feature-gated, so the shell feature (plus files/git for a shared build) must
be enabled. Output logged to /tmp/f-ext-02-shell.log.

## Expected Result

Exit 0; 20+ shell-related tests pass including
`shell_stream_preserves_unicode_split_across_pipe_reads`,
`shell_stream_caps_retained_output_and_reports_total_bytes`,
`shell_stream_spills_complete_ten_megabyte_artifact`,
`dropping_shell_stream_kills_and_reaps_child`, timeout tests.

## Result

- Exit code: 0
- Duration: 1.41 s (after a shared incremental build; build included in the
  run)
- Summary: `test result: ok. 22 passed; 0 failed; 0 ignored; 0 measured;
  96 filtered out`

## Deviations

- None. (Workspace file lock contention expected with parallel review
  builds; none observed beyond normal.)

## Conclusion

Supports: shell tool behavior claims — direct-argv execution, injection
rejection, streaming, incremental UTF-8 decoding across pipe reads, retained
output cap with total-byte metadata, artifact spill, timeout categories, and
process-group kill on stream drop. Does not prove: sandbox-path behavior in
a real sandbox (RecordingSandbox-style tests live in code.rs, run in V05-02).

## Follow-Up

None.
