# F-EXT-02 / V06 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean

## Claim

Cross-references between F-EXT-02 and F-EXT-01 / F-SEC-01 hold: domain
tools honor the ToolFailure/cancel/artifact-writer contract where they own a
process or output; the WRITE_TOOLS drift is consistent; validators and
fallback-client claims from F-SEC-01 are not contradicted by tool-level
evidence.

## Method

1. Re-read F-EXT-01 findings (P1-01 plan-mode, P2-01 WRITE_TOOLS, P3-03
   spawn_task truncation, V03 artifact writer usage list) and F-SEC-01
   findings (P3-05 fallback clients, P3-06 validate_output_file,
   P3-07 dead output guard) and check each against the tools read here.
2. Grep for `catch_unwind`, `with_base_dir` in echo-agent-cli, ToolContext
   cancel field, `register_all_tools` consumers.

## Expected Result

- shell.rs uses the artifact writer (ArtifactCapture) on the live path.
- run_code passes ctx.cancel into the sandbox and maps Cancelled/Timeout
  categories.
- F-SEC-01's "validate_within_base zero callers" and "validate_output_file
  lexical-only" claims unchanged; file tools use resolve_path, not
  PathValidator.
- No new fallback-Client sites beyond F-SEC-01's five.
- WRITE_TOOLS drift (F-EXT-01-P2-01) matches the registry (V03-01).

## Result

- Exit code: not-applicable (cross-reference inspection)
- Summary:
  - Artifact writer: shell.rs ArtifactCapture (871-917) pushes channel
    content and finalizes; spill metadata (output_handling/original_bytes/
    output_truncated) applied (919-949) — consistent with F-EXT-01 V03
    writer-usage list for shell. run_code bounds output via
    `execution.enforce_output_limit` (code.rs:414) without the writer —
    F-EXT-01 V03 recorded run_code as writer-absent; consistent.
  - Cancel contract: run_code pre-checks and passes `ctx.cancel`
    (code.rs:331-339, 358); shell cancels via stream drop (V02-01); git
    family ignores cancel (F-EXT-02-P3-01). F-EXT-01 V01 note (cancel
    cooperative-only at manager level) unchanged.
  - PathValidator: `validate_within_base` (security.rs:272-343) still has
    zero callers (grep both repos); `validate_output_file` (352-398) gates
    data tools only (excel.rs per F-SEC-01); file/git tools use
    `resolve_path` (files/mod.rs:35-132) — F-SEC-01 P3-06 handoff
    ("converge on validate_within_base") confirmed, no contradiction.
  - Fallback clients: no new `Client::new()` fallback sites in the tools
    read here (F-SEC-01 P3-05's five sites unchanged; fetch.rs fallback
    field still dead).
  - WRITE_TOOLS drift: re-confirmed at registry level (V03-01): run_code,
    git_commit/git_branch, enter/exit_worktree are registered mutating tools
    absent from the WRITE_TOOLS list (echo-agent/src/tools/mod.rs:118-139).
  - EKO wiring: `register_all_tools` is the default for non-readonly agents
    (echo-agent-cli/echo-agent-app-core/src/infra.rs:901 comment); no
    `with_base_dir` usage on file tools in echo-agent-cli (all hits are
    Persistence/WorkspaceRegistry types) — file-tool confinement in EKO
    comes from `ctx.working_dir` per call (pipeline.rs:495-512).

## Deviations

- None.

## Conclusion

Supports: F-EXT-01/F-SEC-01 cross-task claims remain consistent with the
tool layer; the writer/cancel/validator contracts are honored by shell and
run_code; git-family cancellation gap (P3-01) and WRITE_TOOLS drift (P2-01
upstream) are the only contract deviations, both already captured as
findings.

## Follow-Up

None.
