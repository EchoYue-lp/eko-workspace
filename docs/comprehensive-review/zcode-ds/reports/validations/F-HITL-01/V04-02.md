# F-HITL-01 / V04 / Attempt 02 — echo_agent + echo_core permission/approval tests

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-HITL-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The `echo_agent` and `echo_core` permission-related tests compile and pass;
the React approval integration module has no tests of its own.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
cargo test -p echo_agent --lib --locked "react::run::approval"
cargo test -p echo_agent --lib --locked "permission"
cargo test -p echo_core --lib --locked "permission"
```

(Default feature set; run 2026-08-12.)

## Expected Result

Permission tests pass; `react::run::approval` may have zero tests (module is
`#[allow(dead_code)]`).

## Result

- Exit code: 0 (all three commands)
- Duration: ~1 s (compile cached)
- Summary:
  - `cargo test -p echo_agent --lib --locked "react::run::approval"`:
    0 tests matched (447 filtered) — `run/approval.rs` contains no tests.
  - `cargo test -p echo_agent --lib --locked "permission"`: 7 passed
    (tools::permission tests + evolution::patch::tests::test_patch_for_permission_denied).
  - `cargo test -p echo_core --lib --locked "permission"`: 23 passed
    (PermissionMode, RuleMatcher, RuleRegistry deny-first, DefaultPermissionPolicy,
    PermissionDecision).
- Log/artifact: none (terminal only).

## Deviations

None.

## Conclusion

Supports: the core permission model (echo-core) and its re-export are
tested and green; the live React approval integration (snapshot.rs,
pipeline.rs PermissionStage) has no dedicated tests, and the dead
`run/approval.rs` module has none either — the framework's RequireApproval
consumer mapping, modified-args round trip, and scope inference are all
untested, which explains why the P1 findings shipped.

Does not prove: behavior under a live provider run (not available in this
read-only review).

## Follow-Up

None.
