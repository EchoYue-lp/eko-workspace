# F-HITL-01 / V05 / Attempt 01 — Historical-document drift check

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (F-HITL-01)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Historical claims about the permission/HITL subsystem still point at current
code: the AGENTS.md "old HumanGate vs new approval path" cleanup promise is
fulfilled; framework doc `docs/zh/05-human-loop.md` and CLI MASTER-PLAN
permission claims match current behavior.

## Method

```text
1. grep -rn "HumanGate|human_gate" echo-agent echo-agent-cli (both repos)
2. grep -rn "permission|审批|HumanGate|Bubble|缓存" echo-agent/docs/zh/05-human-loop.md
   and echo-agent-cli/docs/MASTER-PLAN.md
3. git log --all -S "HumanGate" -- src  (removal history)
4. Cross-check each doc claim against the V01-V03 evidence
```

## Expected Result

Claims classify cleanly as current / fixed / stale / regressed with anchors.

## Result

- Exit code: 0
- Summary of classifications (full table in the task report):
  - AGENTS.md "旧 HumanGate 路径 vs 新审批路径" cleanup promise —
    **current (cleanup done in code)**: zero `HumanGate` symbols in both
    repos; only `examples/demo68_human_gate.rs` keeps the legacy name in a
    doc comment ("replaces the legacy HumanGate"); Selection kind introduced
    by the long-running-task merge (`cc17aca`).
  - `docs/zh/05-human-loop.md:56` "Bubble — 子 Agent 权限上溯到父级处理" —
    **stale/unimplemented**: Bubble → `RequireApproval` → hard tool error on
    the live path (snapshot.rs:841-846, service.rs:643); no bubbling code
    exists (V01).
  - `docs/zh/05-human-loop.md:375` + `:378-390` "demo05 validates the
    `add_need_appeal_tool()` approval chain; rules are flushed at the first
    async permission check (`tool_needs_approval`/`check_tool_approval`)" —
    **regressed**: the live snapshot.rs entry points never call
    `flush_pending_permission_rules` (V01/V02); demo05 (which runs the live
    `agent.execute` path) can therefore pass without the approval ever
    firing.
  - `docs/zh/05-human-loop.md:92` / `:107-108` "Session cache = same tool +
    args; SessionAllTools = tool name" — **regressed on the service path**:
    `infer_scope_from_updates` collapses session approvals to per-tool
    global cache entries (V03-03).
  - `docs/zh/05-human-loop.md:363` cache-TTL comparison row — **current**
    (cache TTL exists and is configurable via `PermissionServiceConfig.cache_ttl`,
    default 30 min, service.rs:115-129, 191-193).
  - `echo-agent-cli/docs/MASTER-PLAN.md:13` "canonical call-scoped
    `permission_mode_override` participates in approval without mutating
    global mode" — **current**: `check_with_permissions_in_mode` override
    (service.rs:484-493) + dedicated test
    `call_scoped_mode_override_does_not_mutate_service_mode` (service.rs:1070-1085).
  - Root `AGENTS.md` over-gating lesson (create_terminal/connect_mcp_server
    `require_full_auto` removed) — **current, not regressed in echo-agent**:
    no such gate in the framework (V01); the only `IpcAuth::require_full_auto`
    remains in EKO's Tauri layer (echo-agent-cli/src/tauri/error.rs) — EKO
    scope (X-AUT-01/A-INT-01).

## Deviations

None.

## Conclusion

Supports the Historical Claim Status table in the task report: the HumanGate
cleanup is done, the permission-mode override claim is current, and three
framework-doc claims (Bubble, add_need_appeal_tool flush, session cache
granularity) are stale/regressed relative to current code.

Does not prove: EKO-side doc claims about its own HITL arbitration
(A-HITL-01/A-DOC scope).

## Follow-Up

None.
