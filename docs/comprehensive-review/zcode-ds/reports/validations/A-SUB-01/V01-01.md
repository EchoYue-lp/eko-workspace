# A-SUB-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

EKO has exactly one Subagent definition source (`.md` loader with project >
user > builtin precedence), exactly one prompt compiler implementation, no
second Subagent lifecycle/catalog authority, and zero `worker` terminology in
either repository.

## Method

Grep-based definition/duplicate search across both repositories (working
directory `lp-agent`), with inspection of every hit:

```text
# worker terminology (both repos, excluding target dirs)
grep -rniw "worker" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src echo-agent-cli/src-tauri
grep -rniw "worker" echo-agent/src echo-agent/echo-*/src

# catalog/snapshot/lifecycle authority candidates in app-core
grep -rn "SubagentCatalogSnapshot|from_registered|from_definitions|validate_default_subagent_routes|discover_subagents|add_subagent_catalog" --include="*.rs" echo-agent-cli/echo-agent-app-core/src

# prompt compiler implementations
grep -rn "impl SubagentPromptCompiler|compile_system|compile_invocation" echo-agent-cli/echo-agent-app-core/src --include="*.rs"

# framework SubagentKind readers
sed -n '/pub enum SubagentKind/,/^}/p' echo-agent/src/agent/subagent/types.rs
grep -rn "SubagentKind" echo-agent/src/agent/subagent/ echo-agent/src/tools/builtin/agent_dispatch.rs echo-agent/src/agent/react/capabilities.rs
```

Each hit was read in full context (loader precedence map, catalog builder
pair, compiler impl pair, registration call sites).

## Expected Result

- Zero `worker` matches.
- One loader (`subagent_loader.rs`), precedence builtin < user < project with
  last-write-wins on name collision.
- One EKO compiler (`EkoSubagentPromptCompiler`); the framework default
  compiler is the only other impl.
- `SubagentKind::Custom`/`Plugin` written but never read at dispatch.
- No second registry/dispatch-catalog authority in app-core.

## Result

- Exit code: 0 (all greps completed; counts are the result)
- Duration: ~2 min including context reads
- Summary:
  - `worker`: zero matches in either repository.
  - Loader precedence confirmed: builtins inserted with `or_insert`
    (`subagent_loader.rs:271-284`), user scope merges by overwrite
    (`:287-290`, `merge_scope` `:337` `by_name.insert`), project scope merged
    last (`:293-296`) — project > user > builtin, order-preserving output for
    builtin names (`:300-316`).
  - Catalog builders: exactly two, both on `SubagentCatalogSnapshot` —
    `from_definitions` (reads `readonly`/`isolate_worktree`/`isolate_workspace`
    fields directly, `subagent_loader.rs:170-183`) and `from_registered`
    (re-decodes readonly/isolation from tags, `:185-212`).
  - `from_definitions` call sites: `infra.rs:247` (primary + every pooled
    agent build), `task_tools.rs:369/:769` (tests), `profiles.rs:304/:324`
    (tests), `subagent_prompt.rs:469/:634` (tests). `from_registered` call
    sites: `tasks/task_runtime/register.rs:37/:55` (post-hoc task-tool
    registration on primary agent, both entry points).
  - Prompt compilers: one EKO impl (`EkoSubagentPromptCompiler`,
    `subagent_prompt.rs:46`); one framework default impl
    (`DefaultSubagentPromptCompiler`, `echo-agent/src/agent/subagent/prompt.rs`)
    used by the plugin path (`plugin_components.rs:541`).
  - `SubagentKind::Custom { path }` written at `infra.rs:695` and
    `SubagentKind::Plugin { source }` at `plugin_components.rs:485`; zero
    production readers in either repo (only builder tests and an unrelated
    `TeamMessage.kind` in team/coordinator.rs/mailbox.rs).
  - No app-core registry/dispatch-catalog authority: app-core only reads the
    framework `SubagentRegistry` (`list_available`) and the framework
    `sync_subagent_dispatch_catalog`/`update_dispatch_catalog`; no parallel
    map/registry implementation exists.
  - Plugin subagent registration exists as a second registration *path*
    (`register_plugin_agents`, plugin_components.rs:444-476) into the same
    framework registry — not a second lifecycle.

## Deviations

None. The plugin path surfaced two non-duplicate divergences that became
findings P2-02/P2-03/P3-01 (see task report) — they are behavioral
divergences, not duplicate authorities.

## Conclusion

Supports: single loader with correct precedence, single EKO compiler, single
framework registry/lifecycle authority, zero worker terminology. Does not
prove: runtime behavior of reload/refresh (V03), or the full registration
chain (V02).

## Follow-Up

None (single attempt).
