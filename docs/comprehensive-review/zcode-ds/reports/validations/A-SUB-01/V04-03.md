# A-SUB-01 / V04 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Default-route startup validation and the prompt-contract audit hold: every
`(domain profile, task kind)` default Subagent route resolves in the
effective catalog, and the builtin role prompts leave compiler-owned
sections to the compiler (no duplication, no demo phrasing).

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked 'profiles::'
cargo test -p echo-agent-app-core --lib --locked prompt_contract
```

Environment: same as V04-01.

## Expected Result

- profiles: 7 passed, including
  `every_default_route_resolves_in_the_effective_catalog` and
  `every_profile_uses_registered_subagent_roles`.
- prompt_contract: 5 passed, including
  `builtin_subagents_leave_shared_sections_to_the_compiler`.

## Result

- Exit code: 0 (both commands)
- Duration: 0.03 s + 0.02 s test runs (warm compile)
- Summary: profiles `ok. 7 passed; 0 failed; 642 filtered out`; prompt_contract
  `ok. 5 passed; 0 failed; 644 filtered out`.

## Deviations

None. The route test validates the parsed-definition catalog, not the live
registry — a registration skip would not be caught (A-SUB-01-P3-02, static).

## Conclusion

Supports: default-route startup validation holds at the catalog level and is
regression-tested; compiler-owned prompt sections are not duplicated in role
markdown (prompt cardinality claim). Does not prove: registry-level route
validity (P3-02).

## Follow-Up

None.
