# A-SUB-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

Historical documents describing the EKO Subagent catalog/loader/pool are
classified correctly against current code: `echo-agent-cli/docs/MASTER-PLAN.md`
("Subagent Prompt Compilation" section), `docs/subagent-unification-plan.md`,
`docs/2026-07-17-domain-subagent-orchestration.md`, and the root
`docs/MASTER-PLAN.md` (M11).

## Method

Document-to-code link check (read-only):

```text
sed -n '130,240p' echo-agent-cli/docs/MASTER-PLAN.md   # loader/catalog/prompt claims
grep -n "subagent|Subagent" echo-agent-cli/docs/subagent-unification-plan.md
grep -n "loader|catalog|pool|reload|immutable" echo-agent-cli/docs/2026-07-17-domain-subagent-orchestration.md
grep -n "M11|subagent" docs/MASTER-PLAN.md
```

Each claim was anchored to current code (subagent_loader.rs, subagent_prompt.rs,
infra.rs, plugin_components.rs, register.rs, agent_pool.rs).

## Expected Result

Claims classified as current/fixed/stale/regressed with code anchors; no
claim reused without classification.

## Result

- Exit code: not applicable (static document diff)
- Duration: ~20 min
- Summary of classifications (full table in task report):
  - "The effective Subagent catalog is an immutable snapshot derived from
    the same definitions used for registration, including project and user
    roles, and startup validates every default route against it"
    (MASTER-PLAN.md:169-171) — `current` for the `.md` surface
    (infra.rs:246-253, 479-496), `regressed in part` for plugin roles
    (missing from the snapshot; P2-02) and for scope refresh on workspace
    switch (P2-04).
  - "EKO compiles a cache-stable system prompt from role Markdown, common
    orchestration rules, ..., one language anchor, ..., and the canonical
    result contract" (MASTER-PLAN.md:158-163) — `current` for `.md`
    subagents, `regressed in part` for plugin subagents which compile with
    the framework default compiler (P2-03).
  - "one product-injectable SubagentPromptCompiler for both registration-time
    system prompts and dispatch-time invocations" (MASTER-PLAN.md:154-157) —
    `current` (EKO compiler is the single product compiler); plugin path
    uses the framework default, which contradicts the "EKO compiles..."
    arm (P2-03).
  - "`agent_tool` remains the single ad-hoc Subagent mechanism in Chat mode.
    Auto and Task mode physically hide it" (MASTER-PLAN.md:139-141) —
    `current` (tool_exposure.rs:155-164; agent_tool disabled in Task/Auto).
  - subagent-unification-plan.md:79 "不存在第二套执行角色层；并发许可、
    pool entry 和执行实例都只是 Subagent runtime 的实现细节" — `current`
    (V01: single registry; pool `__task__:` keys are pool entries, not a
    second lifecycle).
  - domain-subagent-orchestration.md:49 "`task_create` 新增可选字符串参数
    `subagent` ... 不使用封闭 enum，因为项目级和用户级 `.eko/subagents/**/*.md`
    可以扩展角色" — `current` (validate_task_spec against the snapshot,
    task_tools.rs:49-80; open role names).
  - Root MASTER-PLAN.md:815 (M11 layering: DomainProfile/领域 prompt/默认角色
    路由留在 `echo-agent-cli`) — `current` (profiles.rs, subagent_prompt.rs,
    subagent_loader.rs are app-layer).
  - Loader module doc "Subagent `.md` hot-loader" (subagent_loader.rs:1) —
    `stale` in naming: no hot reload exists; edits apply only on the next
    agent build (documented at infra.rs:564-568); the module title
    overpromises (P2-04 arm).

## Deviations

Two document claims are only partially true today; both map to findings
(P2-02, P2-03, P2-04).

## Conclusion

Supports: the historical claims above classify correctly; the central
"immutable catalog from the same definitions" claim holds on the `.md`
surface and breaks at the plugin/switch edges. Does not prove: any claim
about reload timing (none documented as live).

## Follow-Up

None (single attempt).
