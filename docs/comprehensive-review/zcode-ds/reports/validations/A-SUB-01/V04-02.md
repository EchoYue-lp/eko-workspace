# A-SUB-01 / V04 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds (deepseek-v4-flash)
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: clean (both repositories)

## Claim

The EKO prompt compiler cardinality/language invariants hold for every
builtin subagent and every invocation kind: each shared section appears
exactly once, the environment section is conditional, the language anchor
and result contract are present, and history inheritance matches the
transfer policy.

## Method

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent-cli
cargo test -p echo-agent-app-core --lib --locked subagent_prompt
```

Environment: same as V04-01.

## Expected Result

All 7 `subagent_prompt::tests::*` pass (exit 0), including
`builtin_system_prompts_have_single_owned_sections`,
`environment_section_rendered_once_when_provided`,
`direct_planned_and_fork_invocations_have_structured_cardinality`,
`primary_planned_invocation_owns_missing_system_sections_once`.

## Result

- Exit code: 0
- Duration: 0.02 s test run (warm compile)
- Summary: `test result: ok. 7 passed; 0 failed; 0 ignored; 642 filtered
  out`.

## Deviations

None. Note: these tests exercise only the EKO compiler on builtin
definitions; the plugin path (`DefaultSubagentPromptCompiler`) is not
covered here (finding A-SUB-01-P2-03).

## Conclusion

Supports: prompt cardinality/language invariants on the `.md` surface
(prompt cardinality/language claim of the task card). Does not prove:
plugin-surface prompt envelope equivalence (fails — P2-03).

## Follow-Up

None.
