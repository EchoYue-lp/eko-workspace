# F-API-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable (framework-only task)
> Worktree state: clean

## Claim

Concepts that share a name across the 8 workspace crates are re-exports of
one authoritative definition, not parallel implementations. Specifically
checked pairs: Agent/ReactAgent/StructuredAgent, Error/Result,
Store implementations, Task/TaskSpec, Tool/ToolResult, SandboxExecutor,
Skill/SkillRegistry, Guard, WorkflowDefinition.

## Method

Definition search across all crates for the named concepts:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent/echo-agent
grep -rn "pub trait Agent" --include="*.rs" src echo-core echo-execution \
  echo-orchestration echo-state echo-integration echo-tools
grep -rn "pub struct ReactAgent\|pub struct StructuredAgent" --include="*.rs" src echo-*
grep -rn "pub enum Error\|type Result" --include="*.rs" src echo-*
grep -rn "pub struct FileStore\|pub struct SqliteStore\|pub struct InMemoryStore\|pub trait Store" ...
grep -rn "pub struct Task\b\|pub struct TaskSpec\|pub struct TaskNode\|pub struct TaskManager" ...
grep -rn "pub trait SandboxExecutor\|pub trait Skill\|pub struct SkillRegistry" ...
```

Followed by reading the definition files found: `echo-core/src/agent/mod.rs`,
`echo-core/src/error.rs` (head), `echo-core/src/memory/store.rs`,
`echo-state/src/memory/store.rs`, `echo-execution/src/tools.rs` (head),
`echo-core/src/sandbox.rs`, `echo-execution/src/sandbox/mod.rs` (head),
`echo-core/src/guard/` (mod/llm/rule), `echo-orchestration/src/tasks/*`.

## Expected Result

At most one definition per concept; other occurrences are `pub use`
re-exports or `impl` blocks.

## Result

- Exit code: not-applicable (grep + read, all greps completed)
- `Agent` trait: single definition `echo-core/src/agent/mod.rs:474`;
  `AgentCallback` :920; `AgentBuilder` trait `echo-core/src/agent/builder.rs:23`.
  No parallel trait anywhere.
- `ReactAgent`/`StructuredAgent`/`ReactAgentBuilder`: single definitions,
  root-owned (`src/agent/react/mod.rs:104`, `structured.rs:33`,
  `builder.rs:26`).
- `Error`/`Result`: single authority `ReactError` enum
  (`echo-core/src/error.rs:30`) with `pub type Result<T> =
  std::result::Result<T, ReactError>` (:415). No `Error`-named enum and no
  `pub use ... as Error` alias exists in any crate. Root `error.rs` is a pure
  re-export.
- `Store`: trait defined once at `echo-core/src/memory/store.rs:182`;
  `echo-state/src/memory/store.rs:8-9` re-exports it
  (`pub use echo_core::memory::store::{Store, StoreItem}`) and implements
  `InMemoryStore` (:20) / `FileStore` (:220); `SqliteStore` implements the
  same trait (`echo-state/src/memory/sqlite_store.rs:65`). Single trait,
  three impls — intended.
- `Task`/`TaskSpec`/`TaskManager`: single authority
  `echo-orchestration/src/tasks/` (`runtime.rs:179` TaskSpec, `runtime.rs:251`
  Task, `manager.rs:13` TaskManager). `src/state/mod.rs:57` `TaskNode` is a
  distinct runtime-state-machine concept, not a duplicate of `Task`.
- `Tool`/`ToolResult`: single authority `echo_core::tools`;
  `echo-execution/src/tools.rs:20-24` re-exports them; root
  `tools/mod.rs:109-113` re-exports from echo_execution. Chain is
  facade → echo_execution → echo_core. No second `Tool` trait.
- `SandboxExecutor`: trait defined once at `echo-core/src/sandbox.rs:20`;
  `echo-execution/src/sandbox/mod.rs:60` re-exports core types and provides
  `LocalSandbox`/`DockerSandbox`/`K8sSandbox`/`SandboxManager` impls
  (:53-57). No parallel trait.
- `Skill`/`SkillRegistry`: `Skill`/`SkillInfo` defined in
  `echo_core::tools::skill` (re-exported `src/skills/mod.rs:34-36`);
  `SkillRegistry` defined in `echo_execution::skills` (re-exported
  `skills/mod.rs:39`). No parallel definitions.
- `Guard`: `echo_core::guard` owns `Guard`/`GuardResult`/`GuardDirection`
  and (feature-gated) `LlmGuard`/`RuleGuard`/`RuleGuardBuilder`; root
  `guard/llm.rs` and `guard/rule.rs` are thin facades. Single authority.
- `WorkflowDefinition`: defined once at `src/workflow/loader.rs:60`
  (root-owned); `echo_orchestration::workflow` has no such type.
- `ToolRunner` (referenced by echo-macros docs): defined only at
  `echo-core/src/tools/mod.rs:733`, not re-exported anywhere in the root
  facade (see F-API-01-P3-01).

## Deviations

Two public names exist for the same type only as deliberate compat aliases:
`echo_agent::llm::LlmMessage` (llm.rs:109) vs `echo_agent::llm::types::Message`;
`AgentBuilder` alias declared in both `agent/mod.rs:85` and `prelude:151`.
No functional duplication found.

## Conclusion

The V02 claim holds: every checked concept has a single authoritative
definition and all other occurrences are re-exports. The root facade does
not leak parallel implementations. What the search did surface is
name-level documentation drift (no `Error` alias while docs say `Error`;
`ToolRunner` used in docs but not exported), captured in findings
`F-API-01-P3-04` and `F-API-01-P3-01`.

## Follow-Up

`F-TSK-01`/`F-TSK-03` still own the deeper question whether
`src/state` (RuntimeStateStore/TaskNode) and `echo_orchestration::tasks`
state machines duplicate authority (behavioral, not name-level; out of this
validation's scope).
