# F-API-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: ZCode-ds
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: not-applicable (framework-only task)
> Worktree state: clean

## Claim

Public APIs named in the historical documents (`echo-agent/AUDIT_REPORT.md`,
root `docs/MASTER-PLAN.md`) still exist in the code at the reviewed commit.
B-DOC-01 already produced the comprehensive anchor index; this validation
independently samples API-name claims.

## Method

1. Extracted API type/tool names from the two documents:

```text
cd /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
grep -oE "\b(ToolManager|SqliteStore|FileStore|HybridCompressor|ReactAgentBuilder|...)\b" \
  echo-agent/AUDIT_REPORT.md
grep -oE "\b(ToolManager|SqliteStore|FileStore|HybridCompressor|ReactAgent|...)\b" \
  docs/MASTER-PLAN.md
```

2. Verified each name against the current code by grep and read:
   `TaskCreateTool`/`TaskUpdateTool`/`TaskListTool`
   (echo-orchestration/src/tasks/task_tools.rs:15/82/143), `PlanSpec`
   (echo-orchestration/src/planning/plan_spec.rs:18), `HookRegistry`
   (echo_execution::skills::hooks), `SkillRegistry`, `ToolManager`
   (echo-execution/src/tools.rs:55), `TaskManager`
   (echo-orchestration/src/tasks/manager.rs:13), `FileStore`
   (echo-state/src/memory/store.rs:220).
3. Cross-checked the MASTER-PLAN M13 claim (docs/MASTER-PLAN.md:387) about
   deletion of `plan_create/plan_patch/plan_execute` against the README's
   plan-execute section and current code.

## Expected Result

Every sampled historical API name resolves to a live symbol; any document
still describing deleted APIs is classified stale/regressed.

## Result

- Exit code: not-applicable (static)
- MASTER-PLAN API names — all current:
  `task_create/task_update/task_list` family exists as
  `TaskCreateTool`/`TaskUpdateTool`/`TaskListTool` (tool names unchanged);
  `PlanSpec` exists; `HookRegistry`/`SkillRegistry`/`ToolManager`/
  `TaskManager`/`FileStore` all exist and are re-exported by the facade
  (prelude, V01 map).
- MASTER-PLAN:387 (M13: `plan_create/plan_patch/plan_execute` deleted;
  `task_execute` unified API) — current: no `plan_create`/`plan_patch`/
  `plan_execute` symbols exist in code.
- AUDIT_REPORT.md: contains no public-API type names among the sampled
  terms (the report is behavior/anchor-oriented; its file:line anchors were
  indexed by B-DOC-01 V01/V04 — 19/25 findings fixed, 6 current).
- README remnants of the deleted plan API: `README.md:121-147` documents
  phantom features `plan-execute`/`self-reflection`, and `README.md:711-732`
  (§12 Task Planning) shows `agent.execute_with_planning(...)` which does
  not exist in code — classified stale vs MASTER-PLAN:387 (feeds
  F-API-01-P2-01).
- `echo-core/README.md` quickstart (`use echo_core::prelude::*`,
  `ReActAgent`, `SubAgent`, `TypedTool`, `TokenUsage`) — stale, no
  corresponding module/type exists (feeds F-API-01-P2-01).

## Deviations

README (root and echo-core) retains pre-M13/pre-split API claims while the
MASTER-PLAN documents the deletion correctly — i.e., the historical record
(MASTER-PLAN) is current and the user-facing docs (READMEs) are stale.

## Conclusion

The V05 claim holds for the sampled names: all MASTER-PLAN-referenced
public APIs exist, and the deleted plan APIs are indeed gone from code.
The drift found is in the READMEs, not in the historical plans; it is
consolidated under F-API-01-P2-01 with F-API-01-P3-02/P3-04.

## Follow-Up

Q-DOC-01 owns README content drift. B-DOC-01's classification stays valid;
this validation found no contradiction with it.
