# A-SRF-02 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent-cli` clean; unrelated concurrent `echo-agent` dirty
> paths excluded (V00)

## Claim

Browser and Subagent event bridges are both installed when the desktop builder
runs.

## Method

```text
1. Inspect both `.setup(|app| ...)` calls in src/tauri/mod.rs.
2. Inspect the exact installed tauri 2.11.2 Builder::setup implementation.
3. Trace BrowserRuntime subscribe -> `browser://event` -> useBrowserEvents ->
   browserStore, and Subagent bus -> `execution://event`.
4. Search for another browser bridge, setup composition helper, or snapshot API.
```

## Expected Result

Each setup concern executes once; adding a later setup hook must not erase an
earlier bridge.

## Result

- Exit code: not-applicable
- Duration: static call-chain inspection
- Summary: `mod.rs:40-68` installs DevTools and the sole browser bridge, then
  `mod.rs:311-772` calls `setup` again for shortcut/Subagent bridging. Installed
  Tauri `app.rs:1765-1770` assigns `self.setup = Box::new(setup)`, so the second
  hook replaces the first. `useBrowserEvents.ts:5-25` has no alternate source;
  `browserStore.ts:105-128,232-324` invokes commands but learns sessions,
  navigation, screenshots, tabs, and failures through the missing event stream.
- Log/artifact: installed source
  `/Users/ls/.cargo/registry/src/rsproxy.cn-e3de039b2554c837/tauri-2.11.2/src/app.rs`

## Deviations

This is source-conclusive despite no desktop launch: the builder setter's
replacement semantics are explicit. The second Subagent bridge remains active;
only the first setup body is unreachable.

## Conclusion

The claim fails and supports A-SRF-02-P1-02. It does not assess frontend
reducer behavior after correctly delivered browser events.

## Follow-Up

After composing setup logic into one hook, launch the GUI and assert one event
of every BrowserEvent variant plus one Subagent terminal event.
