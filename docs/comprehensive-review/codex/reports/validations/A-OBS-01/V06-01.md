# A-OBS-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Webhook configuration reload preserves the live last-known-good endpoint set
when the watched YAML is temporarily invalid.

## Method

```text
Trace GUI and non-GUI bootstrap emitter identity into spawn_config_watcher and
AppState/chat/scheduler. Inspect handle_config_change's parser and replacement
ordering. Compare committed config::load_config_file and load_config error
semantics. Verify whether hook and webhook reload failures use the same
last-known-good rule.
```

## Expected Result

One shared live emitter is updated only after the exact watched configuration
parses successfully; malformed edits retain existing endpoints and report the
rejection.

## Result

- Exit code: 0
- Duration: static bootstrap/reload trace; not timed
- Summary: bootstrap does share one live emitter in each mode; the temporary
  `AppState` emitter in GUI is replaced before publication. However, the watcher
  calls `load_config`, whose committed implementation logs an explicit-file
  parse error and returns `AppConfig::default`. It then unconditionally calls
  `reload_from_config`, replacing live endpoints with the default empty set.
  Hook reload correctly keeps last-known-good definitions on errors. The
  committed config module explicitly says reloaders should use the fallible
  `load_config_file`, but this watcher does not.
- Log/artifact: this report

## Deviations

Framework config source was read only through `git show HEAD:src/config.rs`.

## Conclusion

The configuration identity/last-known-good invariant fails and supports
A-OBS-01-P1-04.

## Follow-Up

Parse the exact resolved path fallibly, apply all live-reloadable domains from
one accepted config snapshot, and add malformed-edit/recovery coverage.
