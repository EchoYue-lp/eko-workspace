# A-OBS-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Webhook and diagnostics definitions have one identifiable application authority
and are not duplicated by a second EKO registry or emitter API.

## Method

```text
Search both repositories at the pinned commits for WebhookEvent,
WebhookPayload, WebhookEmitter, WebhookEndpoint, RunDiagnostics,
DiagnosticRunSummary, list_diagnostic_runs, load_run_diagnostics,
EventIdentity, RunStore, and prompt_assembly. Classify definitions, exports,
state owners, constructors, public mutators, and display adapters.
```

## Expected Result

One application webhook event/emitter authority and one diagnostic projection
should adapt the framework's reusable EventIdentity and RunStore facts.

## Result

- Exit code: 0
- Duration: static symbol/behavior search; not timed
- Summary: EKO has one webhook schema and emitter under `app-core/webhook`, and
  one diagnostic projection under `app-core/observability`. Framework RunStore
  and EventIdentity are upstream facts rather than duplicate application
  implementations. `AppState::from_shared` briefly constructs an emitter, but
  GUI bootstrap replaces it with the same instance passed to the config watcher
  before state becomes live; this is not a second runtime authority. Public
  `add_endpoint`, `remove_endpoint`, and `list_endpoints` have no production
  caller outside the emitter module.
- Log/artifact: this report

## Deviations

No dynamic registration or feature compilation was run by policy.

## Conclusion

Definition ownership is singular enough to review call paths directly. The
finding set should correct existing authorities rather than add parallel ones.

## Follow-Up

V02 traces live registration and entry points.
