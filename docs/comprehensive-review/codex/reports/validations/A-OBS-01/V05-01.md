# A-OBS-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Webhook enqueue, retry, shutdown, and final delivery outcome are bounded and
observable to the application lifecycle owner.

## Method

```text
Inspect WebhookEmitter::emit and deliver for task ownership, queue/backpressure,
endpoint snapshot locking, retry identity/backoff, result propagation, shutdown
drain/cancellation, and success/failure reporting. Search all callers for a
returned handle/status or lifecycle join.
```

## Expected Result

The live owner can distinguish queued/delivered/failed/cancelled, bound pending
work, retry with stable delivery identity, and drain or cancel work at shutdown.

## Result

- Exit code: 0
- Duration: static lifecycle inspection; not timed
- Summary: `emit` returns `()`, spawns one outer task plus an unbounded task per
  matching endpoint, and keeps the endpoint read guard across dispatch. Delivery
  failures are log-only, followed by one fixed two-second retry; no delivery ID,
  result, queue bound, join handle, cancellation token, drain, or persisted retry
  exists. GUI/main shutdown cancels other services but cannot wait for these
  tasks, so accepted events can disappear on process exit. No success fact is
  recorded, while terminal failure is only an uncorrelated URL warning.
- Log/artifact: this report

## Deviations

The module documents best-effort behavior. This result does not demand durable
exactly-once delivery, but shows the application cannot even observe or bound
its declared best-effort lifecycle.

## Conclusion

The delivery visibility/lifecycle invariant fails and supports
A-OBS-01-P1-03. A-TSK-04-P2-01 remains owner of durable lifecycle-hook replay;
this is the distinct HTTP webhook adapter.

## Follow-Up

Specify best-effort versus durable mode, then use one bounded dispatcher with
stable delivery IDs, typed results, cancellation, and shutdown drain.
