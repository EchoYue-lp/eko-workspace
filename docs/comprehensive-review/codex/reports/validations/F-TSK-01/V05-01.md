# F-TSK-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated TypeScript changes in CLI

## Claim

Inputs accepted by `task_create` obey the JSON Schema advertised to model/tool consumers.

## Method

```text
1. Inspect task_create/task_update/task item schemas.
2. Trace ReAct execution and ToolManager for generic JSON Schema validation.
3. Create /private/tmp/f_tsk_schema_fixture.rs and link existing rlibs with rustc.
4. Invoke TaskCreateTool with:
   - depends_on: [17, true, null]
   - max_retries: "not-an-integer"
   - execution_mode: "not-a-mode"
   - unknown task field while additionalProperties=false
5. Load the committed graph and print normalized values.
```

Environment: existing current-source default-feature rlibs; `rustc 1.97.1`; no Cargo build.

## Expected Result

The tool rejects schema-invalid arguments and commits no graph.

## Result

- Exit code: 1 (fixture intentionally fails when invalid input is committed)
- Duration: compile 1.7 seconds, execution under 1 millisecond
- Summary: `ToolResult.success` was true; a graph was committed with empty dependencies, `max_retries=3`, and Parallel mode. The unknown field was ignored. ToolManager's normal execution path does not call the optional `validate_parameters`, and task tools do not override it.
- Log/artifact: `/private/tmp/f_tsk_schema_fixture.rs`

## Deviations

The fixture initially had two failed rustc linking attempts while matching already-built crate hashes; they changed no source and are not behavioral evidence. This final execution linked the compatible artifacts.

## Conclusion

Supports F-TSK-01-P1-02. The advertised schema and executed semantics are not round-trip equivalent; malformed graph intent can be silently transformed into a valid but different graph.

## Follow-Up

F-EXT-01 should decide whether all tools receive centralized schema validation or each tool must implement strict typed parsing.
