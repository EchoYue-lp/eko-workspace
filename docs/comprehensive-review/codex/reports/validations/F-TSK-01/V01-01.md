# F-TSK-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated TypeScript changes in CLI

## Claim

`TaskSpec + TaskExecution + TaskStatus` is the sole generic dynamic task authority exported by the framework.

## Method

```text
1. Search both repositories for public Task, TaskSpec, TaskExecution, TaskStatus,
   ManagedTask, TaskState, TaskPlan, TodoItem, PlanTask, TaskManager, TaskStore,
   RevisionedTaskStore, and TaskPatch definitions.
2. Inspect echo-orchestration tasks/mod.rs, runtime.rs, task.rs, manager.rs,
   store.rs, executor.rs, planning/plan_spec.rs, root src/tasks.rs and src/lib.rs.
3. Trace each apparent duplicate through exports, constructors, production
   callers, examples, persistence, and conversion into canonical Task.
```

Environment: static source inspection at recorded commits; no feature flags or build.

## Expected Result

Authoring artifacts and rich per-task records may exist, but they must not own a second graph CRUD/store/state authority. Any adapter to `Task` must remain thin and lossless for scheduling semantics.

## Result

- Exit code: 0 (inspection completed); claim failed
- Duration: approximately 5 minutes
- Summary: canonical `Task` is public, revisioned, and used by task tools. A second public system remains: `ManagedTask` stores graph relations and mutable status, `TaskManager` owns CRUD/readiness/claims/run operations, `TaskStore` persists those nodes independently, and `TaskExecutor` reloads that graph at synthetic revision 0 before projecting each node with `ManagedTask::to_task`. Both systems are re-exported from `echo_orchestration`, the root facade, and the prelude. Framework examples construct the legacy authority.
- Log/artifact: source anchors `echo-orchestration/src/tasks/mod.rs:36`, `:42`, `:45`, `:53`, `:65`, `:66`; `task.rs:292`, `:606`; `manager.rs:11`; `store.rs:13`; `executor.rs:1562`, `:1608`; `src/lib.rs:321`

## Deviations

`SqliteTaskStore` accepts any framework `Store`; the finding is not that a reusable Store option exists or that EKO does not use SQLite. The defect is the separate non-revisioned task graph authority it persists.

## Conclusion

Supports F-TSK-01-P2-01. It does not assert that authoring `PlanTaskSpec` or a rich per-task execution record must be deleted; it asserts their graph/status/store authority must converge.

## Follow-Up

F-TSK-02 should separately assess dependency-analysis duplication and F-TSK-03 the runtime claim/execution implications.
