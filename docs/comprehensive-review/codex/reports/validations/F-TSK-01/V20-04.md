# F-TSK-01 / V20-04 - Primary relation-tool terminal contract

> Status: failed
> Executor: Codex primary reviewer
> Date: 2026-08-12
> Source commit: `echo-agent` `9b0e0faf74d35c9a432370b923acabfbb5f32d63`

## Claim

The default framework relation tools must not instruct the model to invoke an unavailable next step.

## Execution

Read-only comparison of `TaskCreateTool` success output, `build_task_tools`, `ReActAgentBuilder` registration, and the builder registration test.

## Result

The claim failed. Initial creation returns `Call task_execute with revision=1` (`task_tools.rs:63`), while `build_task_tools` constructs only `task_create`, `task_update`, and `task_list` (`task_tools.rs:205-210`). The default-agent test explicitly asserts only those three names (`builder.rs:1171-1182`). `task_execute` is an EKO application extension, not a framework tool.

## Impact

An independent framework consumer receives a successful graph creation followed by guidance to call a tool that is absent. The fix belongs at the framework/application boundary: generic output must describe the committed artifact without assuming an EKO command; the EKO adapter may add application-specific execution guidance.
