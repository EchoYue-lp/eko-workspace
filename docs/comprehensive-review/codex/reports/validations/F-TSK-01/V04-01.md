# F-TSK-01 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated TypeScript changes in CLI

## Claim

Two concurrent in-memory updates based on revision 1 cannot both commit revision 2.

## Method

```text
1. Create /private/tmp/f_tsk_cas_fixture.rs.
2. Compile it with rustc only, linking the existing current-source default-feature
   echo_orchestration rlib and its already-built dependencies; do not invoke Cargo.
3. Create a Unicode task at revision 1.
4. tokio::join! two Skip patches with base_revision=1.
5. Require exactly one Ok(revision=2) and one RevisionConflict(current=2).
```

Environment: `rustc 1.97.1`; existing `target/debug/deps` artifacts newer than inspected sources; no target write/build.

## Expected Result

One update commits and the other receives a typed `RevisionConflict`.

## Result

- Exit code: 0
- Duration: compile 1.5 seconds, execution under 1 millisecond
- Summary: left committed revision 2 with status Skipped; right returned `RevisionConflict { expected: Some(1), current: Some(2) }`.
- Log/artifact: `/private/tmp/f_tsk_cas_fixture.rs`

## Deviations

This exercises only the in-memory store. Application/durable store CAS belongs to adapter tasks.

## Conclusion

The default store implements atomic compare-and-commit for the tested conflict. It does not prove every third-party `RevisionedTaskStore` implementation is correct.

## Follow-Up

A-TSK-02/X-TSK-01 should replay the same scenario through the EKO store adapter.
