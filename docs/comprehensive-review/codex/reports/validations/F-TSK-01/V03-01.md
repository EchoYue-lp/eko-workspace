# F-TSK-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated TypeScript changes in CLI

## Claim

The canonical transition table and patch engine reject illegal lifecycle/spec mutations and preserve immutable-spec/runtime-state separation.

## Method

```text
1. Enumerate TaskStatus::can_transition_to for every source state.
2. Map Insert/Update/Skip/Reorder/SetStatus guards in TaskPatchEngine.
3. Inspect task identity validation and claim invalidation.
4. Compare current tests with uncovered transitions.
```

Environment: static inspection; no executable command.

## Expected Result

Pending is the only normal pre-dispatch state; terminal states cannot reopen; only Pending/Blocked specs can be edited/skipped; status changes use `transition_to`; spec mutations clear claims.

## Result

- Exit code: 0
- Duration: approximately 3 minutes
- Summary: SetStatus delegates to the canonical transition table, Update/Skip are limited to Pending/Blocked, blocked spec edits reset to Pending, and claim state is cleared. The validator checks `execution.task_id == spec.id`, nonempty identity/fields, retries, dependencies, and cycles before commit. Revision overflow uses `checked_add`.
- Log/artifact: `runtime.rs:87`, `:127`, `:226`; `revisioned.rs:472`, `:521`, `:547`, `:590`, `:934`; `planning/validator.rs:209`

## Deviations

This validation found a separate public helper defect in `TaskManager::pause_run`; see V07. It also does not cover retry/cancellation dispatch policy owned by F-TSK-03.

## Conclusion

The canonical patch/status implementation is coherent for inspected operations. This does not make the separate `TaskManager` mutation APIs revision-safe.

## Follow-Up

F-TSK-03 for full runtime lifecycle and claims.
