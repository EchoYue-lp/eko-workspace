# A-SRF-04 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent-cli` clean; unrelated `echo-agent` dirty paths excluded as recorded in V00

## Claim

CLI, IM channel, cron, and background definitions can be mapped to one EKO
TaskRuntime/chat authority without a duplicate Agent execution kernel in these
adapters.

## Method

```text
1. Search both repositories for Args, run_cli_mode, run_channels_mode,
   start_headless_services, drive_chat, ChatResources, BackgroundTaskService,
   SchedulerRunner, launch_cron_run, TaskRuntimeStore, task_execute, ready-loop,
   retry-loop, and dependency-loop concepts.
2. Classify each adapter as input/transport, EKO product policy, or generic
   framework mechanism.
3. Distinguish definitions from registrations and live callers.
```

## Expected Result

Every trigger either enters the shared chat driver or the canonical TaskRuntime
executor; adapters do not implement another ReAct or PlanTask DAG loop.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: CLI REPL and IM channels build `PreparedUserTurn`/`ChatResources` and
  call `drive_chat`. Background and cron paths create/launch canonical
  TaskRuntime runs. No second ReAct or PlanTask ready-frontier was found in
  these surface adapters. The previously accepted cross-TaskRun dependency
  poll remains canonical under A-TSK-03-P2-01 rather than a new finding here.
- Log/artifact: this report

## Deviations

Trigger wiring is incomplete for channel-only startup and several adapter
contracts, covered by V03-V08.

## Conclusion

This supports retaining the shared runtime architecture and fixing thin
application/adapter boundaries. It does not prove feature parity or lifecycle
correctness.

## Follow-Up

Use V02-V08 for live reachability and deviations.
