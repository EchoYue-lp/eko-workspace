# A-SRF-04 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent-cli` clean; unrelated `echo-agent` dirty paths excluded as recorded in V00

## Claim

Existing static tests cover the trigger matrix's group identity/routing,
production attachment reachability, channel-only automation, active-turn
cancellation, typed noninteractive output, and orderly shutdown cases.

## Method

```text
Inventory #[cfg(test)] and test modules adjacent to CLI args/REPL/channels,
channel session/QQ/Feishu transports, BackgroundTaskService, scheduler adapter,
and main dispatch. Map each test to the A-SRF-04 scenario table without running it.
```

## Expected Result

Each high-risk adapter invariant has at least one focused existing regression.

## Result

- Exit code: not-applicable
- Duration: static test inventory
- Summary: existing tests cover argument defaults, channel sentence aggregation
  including Unicode/cancel/error, channel attachment data conversion, HITL
  input/selection parsing, TaskRuntime cron routing, and background cancellation
  through the store. No test covers one sender in two group chats, group reply
  target selection, real QQ/Feishu attachment payloads, outbound artifacts,
  channel-only service startup/recovery, ordinary active-turn cancel, combined
  CLI/channel shutdown, or a typed noninteractive mode (which does not exist).
- Log/artifact: this report

## Deviations

Tests were inspected but not executed under the explicit pure-static review
policy.

## Conclusion

The claim fails. Existing unit coverage does not invalidate findings
A-SRF-04-P1-01 through P1-06 and lacks the required regression boundaries.

## Follow-Up

Use the missing cases as acceptance tests during implementation.
