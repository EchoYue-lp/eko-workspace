# A-SRF-04 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent-cli` clean; unrelated `echo-agent` dirty paths excluded as recorded in V00

## Claim

Production QQ and Feishu transports populate inbound attachments and can deliver
full output attachments/artifacts through the app's declared channel contract.

## Method

```text
1. Inspect InboundMessage/OutboundMessage attachment fields and constructors.
2. Trace every production QQ/Feishu ingress construction to app staging.
3. Trace every app channel output constructor to platform send functions.
4. Inventory attachment tests and distinguish conversion-unit tests from
   production transport reachability tests.
```

## Expected Result

Media/file inputs reach `PreparedUserTurn` as durable typed refs, and file/chart/
task artifacts are delivered as complete attachments or stable references.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: the framework types and app staging helper exist. QQ direct/group
  ingress constructs text-only `InboundMessage::new` at
  `qq/gateway.rs:271-278,307-314`. Feishu webhook rejects every non-text message
  at `feishu/webhook.rs:162-172`; webhook and long-poll both construct
  text-only messages at `:198-199` and `long_poll.rs:576-577`. No production
  path calls `with_attachments`. On output, `aggregate_by_sentence` constructs
  only `OutboundMessage::new` text chunks (`src/cli/channels.rs:528-648`) and
  never calls `with_attachments`; task/chart facts are textual/truncated
  previews rather than full artifacts.
- Log/artifact: this report

## Deviations

`channel_attachment_data` has unit tests for filename/MIME conversion, but no
test starts from a real QQ/Feishu payload or asserts output attachment delivery.
Generic export/file delivery details remain owned by A-OUT-01.

## Conclusion

The claim fails and supports A-SRF-04-P1-04: the declared channel media/artifact
contract is not reachable end to end in either production channel.

## Follow-Up

Add transport payload fixtures and outbound artifact delivery regressions;
coordinate complete artifact policy with A-OUT-01.
