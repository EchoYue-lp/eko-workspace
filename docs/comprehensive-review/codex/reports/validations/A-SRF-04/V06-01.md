# A-SRF-04 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent-cli` clean; unrelated `echo-agent` dirty paths excluded as recorded in V00

## Claim

EKO has a noninteractive CLI trigger that accepts one prepared turn and emits
the canonical typed, identity-bearing event stream with a meaningful exit code.

## Method

```text
1. Enumerate clap Args and main dispatch branches.
2. Search for prompt/stdin/exec/one-shot/json/jsonl/event-output flags and
   noninteractive callers of drive_chat.
3. Compare available ChatDriverEvent serialization with CLI rendering.
```

## Expected Result

Scripts can invoke one Agent turn without a REPL, receive ordered typed events
including exactly one terminal, and distinguish success/failure/cancel by exit
status.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: `src/cli/args.rs:12-68` exposes TUI, hidden interactive REPL,
  channels, config/model/project, and resume flags only. Default is TUI;
  `--cli` enters the blocking Reedline loop. No prompt/stdin/exec/JSON event
  mode or one-shot caller exists. Although `ChatDriverEvent` derives
  Serialize/Deserialize (`echo-agent-app-core/src/chat_driver.rs:27-45`), the
  REPL consumes it into human-oriented terminal output rather than a stable
  machine protocol.
- Log/artifact: this report

## Deviations

Webhooks can project selected events but are not a local one-shot CLI contract
and do not replace stdin/stdout/exit semantics.

## Conclusion

The claim fails and supports A-SRF-04-P1-06. Noninteractive Agent execution is
absent rather than merely missing one event variant.

## Follow-Up

Add a one-shot adapter over `PreparedUserTurn` and `drive_chat` with JSONL
canonical events and typed exit mapping; do not add another driver.
