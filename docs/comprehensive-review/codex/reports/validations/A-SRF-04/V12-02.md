# A-SRF-04 / V12 / Attempt 02

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent-cli` clean; unrelated `echo-agent` dirty paths excluded as recorded in V00

## Claim

The task report and validation set pass exact-ID, header, link-target,
executor, isolation-path, and clean-source integrity checks.

## Method

```text
Working directory: /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
1. Exact-match the A-SRF-04 catalog heading.
2. Require task report, authorized dependency task reports, and V00-V12 attempt
   files.
3. Count exact `Executor: Codex review subagent` validation headers.
4. Reject invalid IDs A-TASK-01/A-CHAN-01, zcode paths, and shared report paths.
5. Require `git -C echo-agent-cli status --short` to be empty.
```

## Expected Result

Every check exits zero and prints the success marker.

## Result

- Exit code: 0
- Duration: under one second
- Summary: exact catalog ID matched line 586; all expected reports and
  dependency targets existed; 13 validation files had the required executor;
  forbidden IDs/paths had no match; CLI source remained clean. The command
  printed `A-SRF-04 integrity checks passed`.
- Log/artifact: this report

## Deviations

The `echo-agent` worktree is intentionally not required clean because V00
records unrelated dirty paths and committed-blob reconstruction.

## Conclusion

The corrected integrity gate passes. It supports formal handoff at
`needs_evidence`; it does not replace primary source sampling or dynamic future
fixtures.

## Follow-Up

Codex primary acceptance should sample the six source-conclusive findings and
may then change status without editing this immutable validation.
