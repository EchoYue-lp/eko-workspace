# Q-DEP-01 / V02 / Attempt 02

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: committed frontend manifests only

## Claim

The frontend dependency, license-field, Git-source, and install-script inventory
is reproducible from the committed npm lock.

## Method

Correct the read-only Node expression, parse the committed lock as JSON, and
inspect the committed direct manifest separately.

## Result

- Exit code: 0
- 349 non-root package entries; zero Git-resolved packages.
- Every non-root entry has a license field.
- Exactly two install-script packages: required `esbuild 0.25.12` and optional
  `fsevents 2.3.3`; both declare MIT.
- Direct manifest declares `@tailwindcss/vite` twice with `^4.1.4` and `^4.1.8`;
  the lock currently resolves `4.2.2`.

## Conclusion

Frontend inventory is pinned and inspectable, with one duplicate direct
declaration recorded as Q-DEP-01-P3-03.
