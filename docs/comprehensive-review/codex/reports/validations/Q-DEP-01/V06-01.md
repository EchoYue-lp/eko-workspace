# Q-DEP-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> Worktree state: committed framework source only

## Claim

The public A2A JWT algorithm configuration constructs compatible verification
key material for every advertised algorithm family.

## Method

Trace `JwtConfig::hs256`, `JwtConfig::rs256`, `with_algorithms`, and the only
`validate_token` implementation from the committed A2A auth source.

## Result

- Exit code: 1 (contract mismatch).
- `rs256` accepts a PEM RSA/EC public key and sets `[RS256]` at lines 70-82.
- `with_algorithms` accepts any vector at lines 97-100.
- validation selects those algorithms at lines 220-245 but unconditionally
  constructs `DecodingKey::from_secret` at line 247.
- No typed association exists between algorithm family and key representation.

## Conclusion

Q-DEP-01-P1-01 is confirmed. Dynamic JWT fixtures are unnecessary to establish
the static key-constructor mismatch, but should become the regression suite.
