# Q-DEP-01 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: committed CI/manifests only

## Claim

Both repositories enforce an explicit advisory, source, and dependency-risk
policy in CI.

## Method

Inventory committed policy files; inspect both actual `rust-ci.yml` workflows;
search manifests and CI for `cargo audit`, `cargo deny`, npm audit, source and
advisory policy. Trace framework `a2a` and `full` to `jsonwebtoken`.

## Result

- Exit code: 1 (required policy absent).
- Neither repository contains `deny.toml`, cargo-audit policy, cargo-about
  policy, Dependabot/Renovate policy, or an advisory/license CI step.
- Framework CI runs formatting, Clippy, tests, and no-default check. EKO CI runs
  formatting, Clippy, tests, and app-core no-default check.
- Framework lines 146-150 explicitly label `jsonwebtoken` v9 as affected by an
  algorithm-confusion/JWT-forgery advisory and unsuitable for production before
  migration. Feature `a2a` enables it and `full` includes `a2a`.

## Conclusion

Q-DEP-01-P2-02 is confirmed as a source-conclusive policy contradiction. The
actual current advisory status remains reserved for V08.
