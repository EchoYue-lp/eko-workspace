# Q-DEP-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> Worktree state: committed manifests, locks, CI, and frontend lock only

## Claim

Native and build/install-script boundaries are statically identifiable and
feature/platform placement is understood.

## Method

Inventory resolved `*-sys`/`_sys` packages, root build-dependencies, optional
native features, CI operating-system packages, npm install scripts, and comments
documenting upstream build-script constraints.

## Result

- Exit code: 0
- Framework closure includes bundled SQLite, compression, TLS and platform sys
  crates; SQLite/data are optional reusable framework capabilities.
- EKO adds Tauri/WebKit/GTK/platform notification, PTY/oniguruma, compression,
  TLS and platform sys crates. CI explicitly installs the Linux Tauri system
  packages before the Rust gate.
- Root `tauri-build` is the explicit EKO build dependency. Frontend install
  scripts are limited to required esbuild and optional fsevents.
- Framework documents an upstream Polars nightly build-script incompatibility
  and excludes `data` from docs.rs, while the pinned project toolchain is stable.

## Conclusion

The native surface is large but attributable. EKO not enabling SQLite and the
framework retaining optional SQLite are both consistent with project policy.
Dynamic portability remains a future matrix, not a static finding here.
