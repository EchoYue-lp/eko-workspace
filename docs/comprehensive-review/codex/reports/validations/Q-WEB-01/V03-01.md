# Q-WEB-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> Commits: `echo-agent@3aa7929928442aab91e4dce9c426d909a5f0a1ab`; `echo-agent-cli@b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

The repository declares one Node/npm runtime contract compatible with every locked frontend gate tool.

## Method

Compared README prerequisites, package-level `engines`/`packageManager`, repository runtime-version files, workflow Node setup, and locked Vitest/Vite engine declarations.

## Expected Result

A repository-pinned Node version satisfies all tools and matches the documented minimum.

## Result

- Exit code: 0 for inspection; claim failed
- Duration: not recorded
- Summary: README advertises Node.js >=18. The locked Vitest 4.1.10 requires Node 20, 22, or 24+, while `package.json` has no `engines` or `packageManager`; no `.nvmrc`, `.node-version`, `.tool-versions`, or Node CI setup exists. Vite still permits Node 18, so the incompatibility specifically breaks the declared test gate.
- Evidence: `echo-agent-cli/README.md:71-86`; `web-frontend/package.json:1-43`; `package-lock.json:5010-5043`; `.github/workflows/rust-ci.yml:10-45`

## Deviations

Node 18 execution was prohibited and is not claimed. The package engine declaration is sufficient static compatibility evidence.

## Conclusion

Supports Q-WEB-01-P2-01.

## Follow-Up

Pin one supported Node/npm version and use it for install and all three gate attempts.
