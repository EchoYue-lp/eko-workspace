# F-TSK-03 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source tracked/staged state clean; external generated CLI files were not read, modified, or reverted

## Claim

One claim/dispatch/join failure does not discard already completed sibling
results or leave their claims unresolved.

## Method

```text
Enumerate every early return in wave JoinSet collection and resolve loop.
Model sibling A completed successfully while sibling B returns claim error,
panics/JoinError, or resolve_dispatch error.
```

## Expected Result

The whole wave drains and each obtained claim reaches exactly one resolve or
abandon callback before the kernel returns an infrastructure error.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not measured
- Summary: wave collection immediately returns on child `Err` or non-cancelled
  JoinError at `runtime_executor.rs:379-385`; Rust drops `join_set` and the local
  `wave_results` without resolving them. During the later resolution loop, one
  `resolve_dispatch(...).await?` error likewise returns before remaining
  siblings are resolved.
- Log/artifact: this report

## Deviations

Ordinary dispatch errors are values inside successful JoinSet outputs and are
resolved; the defect concerns controller/JoinSet infrastructure errors.

## Conclusion

The claim fails. A completed sibling can be replayed or remain Running because
its result never crosses the durable safe point. Supports F-TSK-03-P1-03.

## Follow-Up

Future three-sibling fixture for claim error, panic, and resolve persistence
error; all claims must reach deterministic terminal/unknown states.
