# F-TSK-03 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source tracked/staged state clean; external generated CLI files were not read, modified, or reverted

## Claim

Cancellation grace expiry durably releases or terminalizes every claim before
the kernel returns interruption outcome.

## Method

```text
Trace active wave cancellation: token propagation, grace timer, abort_all,
JoinSet drain, wave_results, resolve_dispatch, and interruption_outcome for the
generic controller contract and both live controllers.
```

## Expected Result

Aborted dispatches receive a controller cleanup callback keyed by claim, so no
task remains Running with a live durable claim.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not measured
- Summary: after grace, `abort_all` cancels tasks; cancelled join errors are
  discarded and produce no `wave_results`. Only wave_results call
  `resolve_dispatch`. The controller trait has no `abandon_claim` callback.
  Framework interruption cancels manager tasks broadly, but EKO
  `interruption_outcome` only classifies run outcome and does not clear each
  claim. Boot recovery repairs later, not before current return.
- Log/artifact: this report

## Deviations

Cooperative dispatches completing within grace are resolved correctly; the gap
is specifically non-cooperative dispatches aborted after the deadline.

## Conclusion

The claim fails. Cancellation can return while durable tasks remain Running,
requiring crash-style recovery and leaving current state misleading. Supports
F-TSK-03-P1-02.

## Follow-Up

Future non-cooperative controller fixture must assert per-claim cleanup before
Cancelled/Paused outcome returns.
