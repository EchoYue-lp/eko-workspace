# X-MEM-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

A reported promotion success/failure exactly matches the durable hot/warm/rule,
audit, observer, projection, and retry state.

## Method

```text
1. Order warm->hot promotion and hot-budget demotion writes/notifications.
2. Order rule file write, memory marker write, change-log write, hook, projection refresh.
3. Identify errors possible after the first durable commit.
4. Inspect retry/dedup identity and surface success branches.
```

## Expected Result

Mutation returns a durable typed receipt identifying committed generation and
projection work, or rolls back; retry is idempotent.

## Result

- Exit code: 1
- Duration: static state-table inspection
- Summary: warm->hot writes hot, deletes warm, records/notifies, then calls
  budget enforcement; a late error returns `Err`, so surfaces skip refresh even
  though durable state changed. Rule promotion writes the rule file before Store
  marker and change log; either later failure reports error after a partial
  commit, and retry can append the same rule again because the marker was not
  committed. No typed receipt exposes partial success.
- Log/artifact: this report

## Deviations

No injected error fixture was run.

## Conclusion

Supports `X-MEM-01-P1-02` as the cross-framework/application commit ambiguity.

## Follow-Up

Make promotion idempotent and return a receipt consumed by one projection
reconciler; preserve framework/application layering.
