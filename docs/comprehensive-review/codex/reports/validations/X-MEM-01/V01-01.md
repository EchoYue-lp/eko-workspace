# X-MEM-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Instruction, hot memory, warm Store, compression summary, and runtime projection
sources have an explicit deterministic precedence and one scope identity.

## Method

```text
1. Map InstructionProvider user/repository/project/learned/local tiers.
2. Map hot MEMORY.md and warm Store path resolution for project/global modes.
3. Map projection markers and compression summary role/placement.
4. Compare explicit root semantics with absence/error fallbacks.
```

## Expected Result

Explicit project scope never imports global hot memory implicitly, Store and hot
paths share one scope, and instruction/memory projections retain distinct IDs.

## Result

- Exit code: 1
- Duration: static inspection
- Summary: instruction order is explicit and hot memory has a distinct marker.
  Warm Store is project-local for explicit workspace. However, project hot
  memory falls back to global when the project file is absent, so hot and warm
  scope disagree. Read errors also collapse to absence. These atomic issues
  remain owned by `A-MEM-01-P1-04/P2-06`.
- Log/artifact: this report

## Deviations

No filesystem fixture was run.

## Conclusion

The source/precedence map is complete but fails project isolation; no duplicate
X finding is created for the dependency-owned fallback.

## Follow-Up

Make global memory an explicit tier or use it only in explicit global mode.
