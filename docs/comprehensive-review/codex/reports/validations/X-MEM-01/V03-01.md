# X-MEM-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Every committed hot-memory or rule mutation is visible to primary, existing
pooled, and future Agents before their next request.

## Method

```text
Build a mutation matrix for GUI/TUI/CLI remember/forget, framework tools,
Dreaming, evidence accept/undo, rule promotion, compression promotion, primary,
existing pool, and future pool consumers. Compare refresh helper actually called.
```

## Expected Result

Hot changes refresh the hot marker; instruction changes refresh the instruction
marker; every consumer receives the same mutation generation.

## Result

- Exit code: 1
- Duration: static matrix inspection
- Summary: current hot promotion/deletion/Dreaming paths call the instruction
  helper; the pool helper documented as refreshing both also refreshes only
  instructions. CLI rule promotion refreshes primary only. Evidence and tool
  mutations have no projection receipt. Future Agents reload files, causing
  age-dependent behavior. These atomic omissions remain `A-MEM-01-P1-02/P1-05`.
- Log/artifact: this report

## Deviations

No next-request capture was executed.

## Conclusion

Immediate refresh does not conform; X-MEM uses this matrix to support the
cross-layer generation findings rather than duplicate every surface defect.

## Follow-Up

One receipt-producing EKO reconciler must replace surface-specific refreshes.
