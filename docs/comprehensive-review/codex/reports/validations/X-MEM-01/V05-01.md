# X-MEM-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Workspace switch publishes one memory generation so no request can combine new
instructions/projections with old Store, promoter, or LayerManager state.

## Method

```text
Order AppState::switch_workspace and exit_workspace mutations:
workspace.current/CWD -> primary/pool working_dir + projections -> persistence ->
primary Store/LayerManager -> ReviewIntegration -> pool Store/LayerManager.
Search for active-turn fence, generation token, rollback, or atomic binding.
```

## Expected Result

All components stage under one generation and become request-visible once, or
the switch is rejected/rolled back without changing the prior generation.

## Result

- Exit code: 1
- Duration: static state-transition inspection
- Summary: primary and pooled Agents refresh new-workspace instruction/hot
  projections before the new Store and promoter are installed. Primary and pool
  are then rebound sequentially; no generation, active-turn fence, or rollback
  prevents a request from observing a cross-workspace pair. Pool also reopens a
  separate FileStore and ReviewIntegration reads path/Store from separate locks
  (`A-MEM-01-P0-01/P1-03`).
- Log/artifact: this report

## Deviations

No concurrency fixture was run.

## Conclusion

Supports `X-MEM-01-P1-01`, which remains after merely fixing duplicate Store
construction.

## Follow-Up

Stage and atomically publish one application MemoryBinding generation.
