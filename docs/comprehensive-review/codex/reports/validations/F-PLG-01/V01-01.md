# F-PLG-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories returned empty `git status --short` output

## Claim

Plugin definitions and runtime owners can be mapped without introducing a
duplicate framework or application authority.

## Method

Search both repositories for PluginManifest, PluginRegistry, PluginIntegrator,
PluginLifecycleManager, scopes, install/scan/enable/disable, component wiring,
reload callers and Skill/Hook/MCP registration. Inspect root exports and the
live EKO PluginRuntimeService entry points.

## Expected Result

Generic identity, manifests, dependency resolution, component ownership and
lifecycle remain framework mechanisms; EKO owns only product component
construction/policy and uses thin framework adapters.

## Result

- Exit code: 0 for static search and source reads.
- `echo-core` owns manifest, scope, registry, variables and lifecycle contracts;
  the root facade owns cross-crate PluginIntegrator assembly.
- EKO's PluginRuntimeService is the live caller for enable/disable/reload and
  constructs application-owned agent/LSP/monitor/theme/output-style components.
- Skill, Hook and MCP registrations already expose framework source ownership;
  there is no justification for another registry or EKO dependency resolver.
- Whether integration and rollback actually preserve this ownership remains to
  be checked by later validations.

## Deviations

Examples and tests were counted as coverage only, not as production reachability.

## Conclusion

Passed. Supports the layering and duplicate-authority baseline; it does not yet
claim transaction correctness.

## Follow-Up

V02 traces dependency/manifest/path acceptance. V03 traces live wiring and
rollback. V04 traces disable/unload/lifecycle terminal behavior.
