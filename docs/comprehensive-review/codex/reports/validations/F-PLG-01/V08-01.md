# F-PLG-01 / V08 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Existing static test coverage can be mapped precisely without treating unrun
tests as behavioral proof.

## Method

Inventory plugin manifest, registry, lifecycle and EKO runtime test names and
asserted branches. Compare them to each finding's missing boundary cases.

## Expected Result

Happy paths and prior regressions are identified as coverage; uncovered
collision/partial-failure cases remain explicit future validations.

## Result

- Exit code: 0.
- Existing tests cover manifest/name/path validation, dependency order/cycles/
  version/disabled dependency, basic install/toggle/uninstall, strict manual
  validation, source-tag unload, successful lifecycle transitions, failed
  unregister cleanup, and several EKO replacement/activation rollback paths.
- Missing: duplicate name across scopes, corrupt registry state, arrays for
  singular components, install-time missing component, partial copy, state-save
  failure after delete, MCP ownership collision, direct wire_all partial failure,
  lossy targeted helpers and partial lifecycle callback side effects.
- No test command was executed, so this report claims inventory only.

## Deviations

None.

## Conclusion

Passed as coverage inventory. It neither confirms nor refutes runtime behavior.

## Follow-Up

Use the matrix in V09 after implementation changes are authorized.
