# F-EXT-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; review reports are outside the source repositories

## Claim

Existing source tests guard the generic contract at its live boundaries:
schema enforcement, collision handling, cancellation, model-visible cursors,
typed blocked outcomes, and every result payload kind.

## Method

Static test inventory across `echo_core::tools`, `echo_execution::tools`, the
ReAct pipeline/snapshot/tool phases, the registry, pagination, artifacts, and
`read_artifact`. Test names and assertions were compared with the live call
graphs from V02-V08. No test was executed.

## Expected Result

At least one regression test reaches each invariant through the same public or
ReAct path used in production.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: tests cover deterministic schema ordering, context forwarding,
  timeouts/retry, streaming terminal order, classified failures returned by an
  executed tool, pagination cursor integrity/Unicode, complete text artifact
  recovery, spill fallback, and UTF-8. No test was found for a
  `validate_parameters` call on the live manager/pipeline, ordinary registration
  collision, generic manager cancellation, PageInfo through model projection,
  binary ToolResult through artifact/model projection, blocked validation as a
  typed error, or an implementation using both execute defaults.
- Log/artifact: this report

## Deviations

This reports assertion coverage, not current test pass/fail status. Executing
tests was explicitly prohibited.

## Conclusion

The claim failed. Existing positive tests explain why local mechanics are
strong while the cross-layer gaps in F-EXT-01-P1-01 through P2-07 remain
unguarded.

## Follow-Up

Use the missing cases as the fix acceptance matrix; do not add tests that call
only disconnected helper methods.
