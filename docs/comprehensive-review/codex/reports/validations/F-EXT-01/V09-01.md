# F-EXT-01 / V09 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; review reports are outside the source repositories

## Claim

The generic pagination/artifact/output-budget paths avoid unchecked UTF-8 byte
slicing, production panic APIs, and arithmetic overflow on externally derived
sizes/cursors.

## Method

Static forbidden-pattern and safe-pattern inspection of:

```text
echo-core/src/tools/{mod,artifact,pagination}.rs
echo-execution/src/tools.rs
echo-tools/src/registry.rs
echo-tools/src/files/artifact.rs
src/agent/snapshot.rs (output projection)
```

Searched `unwrap`, `expect`, `panic!`, `unreachable!`, `todo!`, direct indexes,
string slices, conversion, UTF-8 boundaries, and unchecked arithmetic;
test-only modules were separated from production code.

## Expected Result

External text/cursor handling uses character iteration or checked UTF-8
boundaries, conversions are fallible/saturating, and poison/errors do not panic
the process.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: output previews/head/tail use `chars`; cursor hex parsing uses
  `first`/`get` and checked arithmetic; artifact pages decode a valid UTF-8
  prefix and advance by the encoded byte length; size counters use
  `try_from` plus saturating operations; mutex poison is recovered. No
  production `unwrap`/`expect`/panic macro or unchecked string slice was found
  in this scoped authority.
- Log/artifact: this report

## Deviations

Test modules contain unwrap/expect/unreachable constructs. This validation is
limited to the generic F-EXT-01 production authority and does not certify every
individual domain tool; those belong to F-EXT-02/F-EXT-03.

## Conclusion

The scoped static safety claim passed. It supports retaining the current
UTF-8/cursor/artifact mechanics while fixing projection and lifecycle gaps.

## Follow-Up

Individual tool scans remain required in F-EXT-02 and F-EXT-03.
