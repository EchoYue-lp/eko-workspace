# Q-FW-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework externally dirty and inspected only through committed HEAD; CLI lockfile dirty excluded

## Claim

Committed CI executes the same all-target/all-feature test surface required by
the repository submission gate.

## Method

```text
Read committed Cargo.toml workspace members/default-members and target
declarations; enumerate committed example/bench paths; compare with the package
matrix and exact test command in .github/workflows/rust-ci.yml. Distinguish
compilation under Clippy from test-harness execution.
```

## Expected Result

All eight packages and every applicable all-target harness should execute under
the mandatory all-feature test gate.

## Result

- Exit code: 0
- Duration: static manifest/CI coverage inspection; not timed
- Summary: the CI package matrix exactly lists the root plus seven workspace
  members. However its test job runs only `--lib --tests` for each package.
  Committed source includes 88 example files, 44 explicit example targets and
  one benchmark target. The lint job compiles all targets under Clippy but does
  not execute example/benchmark harnesses as `cargo test --all-targets` would.
- Log/artifact: this report

## Deviations

This validation does not assert that any omitted harness contains tests or
currently fails; it proves only that mandatory target execution is absent.

## Conclusion

The claim failed. Supports Q-FW-01-P1-01.

## Follow-Up

Make CI execute the exact mandatory command or revise the authoritative gate in
one place with evidence that the reduced surface is intentional.

## Correction (2026-08-13)

The initial count of 88 included non-Rust files under `examples/`. A committed-
tree recount found 68 `.rs` example sources, 55 explicit `[[example]]` entries,
and one `[[bench]]`. V07-02 supersedes this attempt for target counts; the
target-execution conclusion is unchanged.
