# Q-FW-01 / V07 / Attempt 02

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework externally dirty and inspected only through committed HEAD; CLI lockfile dirty excluded

## Claim

Committed CI executes the same all-target/all-feature test surface required by
the repository submission gate.

## Method

```text
Read workspace/default-members and CI matrix from committed HEAD. Count Rust
example sources with git ls-tree plus an .rs suffix filter; count explicit
example and bench tables from Cargo.toml; compare exact CI test flags with the
mandatory all-target command.
```

## Expected Result

All eight packages and every applicable target harness execute under the
mandatory all-feature test gate.

## Result

- Exit code: 0
- Duration: static committed-tree/manifest/CI recount; not timed
- Summary: CI lists all eight packages but runs only per-package `--lib --tests`.
  Committed HEAD contains 68 Rust example sources, 55 explicit example targets,
  and one benchmark target. All-target Clippy compiles target code but does not
  execute the harnesses required by `cargo test --all-targets`.
- Log/artifact: this report

## Deviations

Cargo metadata was not run. Counts describe sources/declarations, not a claim
that every example has tests or an omitted target currently fails.

## Conclusion

The claim failed. Supports Q-FW-01-P1-01 with corrected counts.

## Follow-Up

Execute the exact mandatory test command in CI or converge policy on a proven-
equivalent matrix, deleting the weaker parallel command.
