# A-SRF-03 / V07 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Conversation reload preserves stable message association, attachments, Tool
summaries, and the latest TaskRuntime artifact snapshot without cross-load races.

## Method

```text
Trace conversation load generation -> record/tool fetch -> backend restore ->
message/attachment/step mapping -> tool hydration; trace App TaskRuntime load,
snapshot Promise.all, generation guards and polling terminal stop.
```

## Expected Result

Stale loads cannot overwrite a newer conversation; stable message IDs reconnect
Tools/Subagents, and the latest TaskRun snapshot restores artifacts and status.

## Result

- Exit code: 0 for inspection.
- Duration: static inspection.
- Summary: conversation and TaskRuntime stores use independent generations to
  reject stale loads. Persisted message IDs, attachments, execution steps and
  Tool summaries are restored. TaskRuntime loads plan/todos/artifacts/blockers,
  replays Subagent/Tool boundaries, and polls active runs until terminal. Tests
  cover stable IDs, attachment projection, stale load/refresh, Tool and Subagent
  restoration.
- Log/artifact: `conversationStore.ts:137-199`, `:296-387`;
  `taskRuntimeStore.ts:165-283`; corresponding store tests.

## Deviations

Ordinary in-flight chat is not covered by these snapshot APIs; that is V03.
Tool live ingestion has the monotonicity deviation in V05.

## Conclusion

Supports the positive reload matrix with those explicit limitations.

## Follow-Up

Retain generation guards and stable message IDs.
