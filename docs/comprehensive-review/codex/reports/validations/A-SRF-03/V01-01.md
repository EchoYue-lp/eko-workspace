# A-SRF-03 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

The GUI fact paths and duplicate state authorities can be completely mapped
from definitions through listeners, stores, restore queries, and components.

## Method

```text
Search both repositories by ChatEvent/chat://event, ExecEvent/execution://event,
message/conversation/run/task/subagent/tool/artifact identities, Zustand stores,
Tauri emit/listen, restore/load/hydrate, and component consumers. Trace every
production import/caller; distinguish defined-only components from live paths.
```

## Expected Result

One documented topology identifies every live fact source and any parallel UI
authority without treating a definition alone as reachable.

## Result

- Exit code: 0 for static searches/reads.
- Duration: static inspection.
- Summary: `chat://event` feeds `handleChatEvent`/`chatStore` through local hook
  refs. `execution://event` feeds global Tool/Subagent stores and triggers
  TaskRuntime query restoration. Conversation reload independently restores
  messages/tools; App restores the active conversation's latest TaskRun. The
  Subagent stores are global but message/run filters select visible records.
  `SubagentPanel`/its detail-selection writer are defined but have no production
  import/render caller, so no finding uses that dormant path.
- Log/artifact: `useTauriChat.ts`, `chatEventHandler.ts`, all four stores,
  `conversationStore.ts`, `App.tsx`, `ChatPanel.tsx`, `MessageBubble.tsx`.

## Deviations

ChatEvent remains a hand-written Rust/TypeScript pair; field-generation drift is
owned by A-FE-01 and is not promoted here.

## Conclusion

Supports the current-path and duplicate-authority map.

## Follow-Up

V02-V07 inspect behavior.
