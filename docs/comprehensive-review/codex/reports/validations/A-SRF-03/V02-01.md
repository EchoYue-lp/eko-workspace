# A-SRF-03 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

GUI turn identity remains owned until backend `run_status` plus `done`, so every
terminal advances queued input exactly once.

## Method

```text
Trace Tauri sink Agent FinalAnswer/Error/Cancelled -> postlude TurnStatus ->
Done, then useTauriChat isCurrentRunEvent -> handleChatEvent ref mutation ->
dispatchNextQueued and send admission.
```

## Expected Result

Agent payload events may render early, but only `done` releases message identity
and advances one queued input after backend ownership/postlude completes.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: `final_answer`, `error`, and `cancelled` clear
  `currentMessageKeyRef`. The subsequent same-key `run_status` and `done` fail
  `isCurrentRunEvent` before reducer entry. Therefore `dispatchNextQueued` never
  runs, the authoritative backend terminal status is dropped, and new sends are
  admitted locally before the backend releases its conversation turn.
- Log/artifact: `chat.rs:681-711`, `:1341-1387`;
  `useTauriChat.ts:40-70`; `chatEventHandler.ts:97-108`, `:140-157`, `:206-218`.

## Deviations

A-CHAT-01 owns the producer's Error/completed collapse. This result isolates the
frontend's earlier identity release and queue consequence.

## Conclusion

Supports A-SRF-03-P1-01.

## Follow-Up

Replay all terminal sequences after defining one frontend turn reducer.
