# A-SRF-03 / V06 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

TaskRuntime and Subagent projections preserve run/revision/attempt identity and
can reconstruct terminal result/artifact facts from durable events.

## Method

```text
Trace ExecEvent run_id/task_id/subagent_run_id through the Tauri flattening,
Subagent store key/status guard, TaskRuntime event reconstruction, latest-attempt
selection, artifact normalization, and associated static tests.
```

## Expected Result

Retries remain separate, same execution across runs cannot collide, terminal
state cannot reopen, and reload reconstructs result/artifact/verification facts.

## Result

- Exit code: 0 for inspection.
- Duration: static inspection.
- Summary: store key is `(run_id, subagent_run_id)`, where execution IDs include
  plan revision and attempt. Terminal records reject subsequent events. Durable
  assigned/released events reconstruct the same identity, complete result,
  artifact, verification, touched-file and remaining-work projection. Tests
  cover terminal duplicate start, retries, cross-run IDs and restore.
- Log/artifact: `executor.rs:85-159`; `chat.rs:1419-1445`;
  `subagentRunStore.ts:157-176`, `:300-440`, `:443-533`;
  `subagentRunStore.test.ts:15-210`.

## Deviations

Framework terminal correctness remains dependency-owned. Tests were read, not
executed.

## Conclusion

Supports the positive identity/recovery conclusion, not overall GUI parity.

## Follow-Up

Preserve this key and monotonic guard during store scoping fixes.
