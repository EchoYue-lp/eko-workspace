# A-SRF-03 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Live Tool summary ingestion is monotonic under duplicate or late started and
terminal events, matching history hydration.

## Method

```text
Trace started/finished/cancelled summary identity from ToolExecutionRepository
through execution://event into toolExecutionStore.ingest. Compare live ingest
with mergeHydratedToolExecutions and TaskRuntime boundary merge; inspect tests.
```

## Expected Result

For one execution identity, terminal state cannot return to running and older
terminal facts cannot replace a newer terminal fact.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: repository start and finish reuse the same detail ID. Live `ingest`
  unconditionally overwrites `tools[tool.id]`, so a late/duplicate started
  summary reopens a succeeded/failed/cancelled card. History hydration has a
  terminal-rank/timestamp merge and TaskRuntime has an authoritative boundary
  merge, creating different reducer semantics for the same facts.
- Log/artifact: `tool_execution.rs:191-223`; `chat.rs:185-206`;
  `useTauriChat.ts:132-137`; `toolExecutionStore.ts:46-116`, `:202-217`,
  `:223-253`.

## Deviations

Tauri delivery usually preserves same-producer order, but multiple live,
persisted, and TaskRuntime sources exist and the task explicitly requires
duplicate/late safety. No dynamic reordering was executed.

## Conclusion

Supports A-SRF-03-P2-04.

## Follow-Up

Use one identity-aware monotonic merge for every ingestion source.
