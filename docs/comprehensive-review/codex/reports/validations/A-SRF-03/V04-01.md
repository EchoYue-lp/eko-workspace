# A-SRF-03 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

A `run_started` event for a background/inactive conversation cannot replace the
TaskRuntime projection of the conversation currently displayed by the GUI.

## Method

```text
Trace TaskRuntime RunStarted payload -> Tauri execution://event -> global
listener -> loadByConversation; compare App activeId effect and RightRail /
MessageBubble consumers of one global activeRun.
```

## Expected Result

Background events update conversation-keyed state only. The focused projection
changes solely when `conversationStore.activeId` changes.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: every `run_started` directly calls `loadByConversation` with the
  event payload's conversation ID, without comparing activeId. That method
  replaces the one global activeRun/plan/todos/artifacts and starts its polling.
  App only reloads the visible conversation when activeId changes, so a later
  background event can leave the right rail and message execution projection
  focused on another conversation.
- Log/artifact: `executor.rs:350-360`; `chat.rs:1419-1445`;
  `useTauriChat.ts:138-147`; `taskRuntimeStore.ts:219-265`;
  `App.tsx:51-56`; `RightRail.tsx:5-12`; `MessageBubble.tsx:185-216`.

## Deviations

No concurrent GUI was launched. Reachability follows EKO's explicit pooled,
multi-conversation background execution path.

## Conclusion

Supports A-SRF-03-P1-03.

## Follow-Up

Use conversation-keyed snapshots and focused selectors; replay A/B ordering.
