# A-SRF-03 / V00 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; framework concurrently dirty and excluded

## Claim

A-SRF-03 can use one clean application revision while excluding unrelated
concurrent framework edits.

## Method

```text
git -C echo-agent rev-parse HEAD; git -C echo-agent status --short;
git -C echo-agent-cli rev-parse HEAD; git -C echo-agent-cli status --short.
```

## Expected Result

Both commits are known, CLI is clean, and framework dirty paths are disclosed
without reading their current contents or diffs.

## Result

- Exit code: 0 for both repository snapshots.
- Duration: under one second.
- Summary: CLI remained clean. Framework had concurrent edits under core utils,
  Anthropic integration, human-loop/scheduler/state/tool paths, React run,
  snapshot/eval/evolution/trace, plus one untracked core utils file. They were
  neither read for current-content evidence nor modified/reverted.
- Log/artifact: this report.

## Deviations

Framework was not clean; this application/frontend task did not require its
current dirty contents.

## Conclusion

Supports the source boundary only.

## Follow-Up

Primary rechecks both statuses at acceptance.
