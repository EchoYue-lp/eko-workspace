# A-SRF-03 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

ChatPanel remount or WebView reload can reconnect to an active ordinary chat
turn and resume its canonical event stream.

## Method

```text
Trace listener setup/cleanup and hook-local identity initialization; search all
IPC/store APIs for active-chat-turn query, event cursor/replay, and in-progress
message rebind. Compare TaskRuntime and conversation restore paths.
```

## Expected Result

The frontend obtains the active `{conversation_id, message_key, cursor}` and a
snapshot/replay before consuming new events, or explicitly cancels the orphaned
turn and projects that terminal.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: listener cleanup is race-safe, but every mount initializes
  `currentMessageKeyRef`/conversation refs to null. Backend turns continue in
  detached Tokio tasks and emit keyed events globally. A new listener rejects
  every keyed event, and no active-turn/snapshot/cursor API exists. TaskRuntime
  runs can reload from persisted events; ordinary chat tokens, HITL, final
  answer, queue and terminal state cannot reattach through that path.
- Log/artifact: `useTauriChat.ts:23-33`, `:50-61`, `:74-167`;
  `chat.rs:536-566`, `:681-725`; `taskRuntimeStore.ts:219-265`;
  `App.tsx:51-56`.

## Deviations

No WebView was launched. Exact persistence of an interrupted partial assistant
message depends on A-STATE-01; this validation claims only missing live rebind.

## Conclusion

Supports A-SRF-03-P1-02.

## Follow-Up

Add an active-turn snapshot/replay contract and remount/reload regression.
