# F-RCT-05 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source review only

## Claim

Corrupt-load and checkpoint-save failures fail closed before recovery state can
be overwritten or completed side effects can be replayed.

## Method

Trace FileRuntimeStateStore corrupt reads, restore_thread_context error handling,
all `save_runtime_checkpoint` callers, its return type/error handling, and the
post-tool safe-point comment/flow.

## Expected Result

Corruption preserves recoverable bytes and stops execution; a required safe-point
save failure prevents the next model step/terminal until explicitly handled.

## Result

- Exit code: 0
- Duration: under one second
- Summary: file store correctly returns parse error, but restore catches it,
  resets context, and continues; later checkpoints can overwrite the corrupt
  authority. `save_runtime_checkpoint` returns unit, warns on serialization/store
  error, and every caller continues, including the tool-batch point claimed to
  prevent replay of completed writes.
- Log/artifact: source anchors in F-RCT-05-P0-04 and P1-05

## Deviations

No fault injection was run. The claimed absence of propagation is source-
conclusive; crash timing remains a future regression.

## Conclusion

Both fail-closed invariants failed.

## Follow-Up

Return typed save/restore outcomes and make required safe points transactional.
