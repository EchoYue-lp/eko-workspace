# F-RCT-05 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source review only

## Claim

Public snapshot rollback restores a safe execution state without replaying
already completed external effects.

## Method

Trace SnapshotManager capture/rollback, automatic capture after tool batches,
ReactAgent rollback/rollback_to, facade/docs/examples, and EKO session commands.

## Expected Result

Either rollback is explicitly transcript-only, or it restores/invalidates every
state and side-effect fact needed for safe continuation.

## Result

- Exit code: 0
- Duration: under one second
- Summary: StateSnapshot stores messages plus free metadata only; rollback
  replaces only ContextManager messages and truncates later in-memory snapshots.
  It does not restore plan, skills, working_dir, nodes, durable checkpoint,
  memory, files/artifacts, tool observations, or cancellation state. It is
  exposed/documented as agent state rollback and is reachable from GUI.
- Log/artifact: source anchors in F-RCT-05-P1-03 and P2-06

## Deviations

A message-only branch/fork capability can be valid if renamed and prevented from
being interpreted as external-state rollback.

## Conclusion

Execution-safe rollback invariant failed.

## Follow-Up

Separate transcript branching from durable execution recovery.
