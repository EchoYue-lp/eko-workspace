# F-RCT-05 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source review only

## Claim

Restoring AgentCheckpoint replaces every checkpoint-owned field exactly.

## Method

Map every AgentCheckpoint field to `resume_from_state_store` writes and inspect
clear/deactivate APIs and the order of message, plan, skill, node, working-dir,
and blocked-state restoration.

## Expected Result

None/empty values clear prior state and any failure leaves the agent unchanged.

## Result

- Exit code: 0
- Duration: under one second
- Summary: messages replace first; plan writes only when Some; skills are only
  marked active and never clear prior activations/projections; working_dir writes
  only when Some; blocked_reason is only logged; later node/working-dir failures
  are warning-only. The operation is neither exact replacement nor atomic.
- Log/artifact: source anchors in F-RCT-05-P1-02

## Deviations

The checkpoint schema itself serializes all declared fields; the defect is live
hydration semantics.

## Conclusion

Invariant failed; supports F-RCT-05-P1-02.

## Follow-Up

Define a validated replacement snapshot and commit it atomically.
