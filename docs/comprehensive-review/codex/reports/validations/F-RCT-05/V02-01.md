# F-RCT-05 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source review only

## Claim

A steer accepted for the active turn is drained by the same stable identity.

## Method

Trace invocation runtime `turn_id` and `run_id` through
`run_stream_channel`, `AgentRunSnapshot::from_agent_with_invocation`,
`TurnSteerMailbox::steer`, and `drain_steer_into_context`; confirm EKO creates
an active turn with turn_id and may independently carry a Task run_id.

## Expected Result

Mailbox begin, public acceptance, and drain use one canonical turn ID.

## Result

- Exit code: 0
- Duration: under one second
- Summary: mailbox begins with `turn_id.or(run_id)`, while snapshot drain uses
  only `current_run_id`. When both IDs exist and differ, EKO can receive
  `accepted` for turn_id but every drain asks for run_id and returns empty.
- Log/artifact: source anchors in F-RCT-05-P1-01

## Deviations

Legacy/non-Task chat where run_id is None is not affected.

## Conclusion

Invariant failed; supports F-RCT-05-P1-01.

## Follow-Up

Use one explicit turn identity field end to end.
