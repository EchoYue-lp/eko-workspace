# A-HITL-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

The EKO `HitlDispatcher` is the actual multi-surface authority, and losing/disconnected providers leave no stale pending state.

## Method

Search all register/unregister and provider-replacement sites; trace BrowserRuntime default/conversation provider lifecycle.

## Expected Result

Connected surfaces register source-scoped providers in one dispatcher; first-response cancellation removes losing requests; conversation providers are removed at turn/disconnect completion.

## Result

- Exit code: 0 for source inspections.
- Bootstrap registers only REPL (`runtime.rs:127-145`); TUI unregisters it and registers TUI (`main.rs:247-257`). GUI and channel do not register with the dispatcher: each overwrites the selected agent's provider directly (`chat.rs:568-582`; `cli/channels.rs:145-152`).
- BrowserRuntime keeps bootstrap dispatcher as default and separately stores GUI conversation providers (`browser/mod.rs:145-167,978-1007`). Chat registers a provider at `chat.rs:583-589` but no production caller invokes `remove_conversation_approval_provider`.
- Dispatcher first-response dropping can trigger TUI's Drop cleanup, but channel/Tauri futures have no general drop guard for their pending entries; stale entries survive until a response, fixed timeout, targeted cancel or replacement.

## Deviations

This finding does not demand broadcasting every approval to every surface. The application may route to one eligible provider, but ownership and lifecycle must be canonical and source-scoped.

## Conclusion

Failed. Supports A-HITL-01-P2-07 and the stale browser-provider portion of P1-02/P1-04.
