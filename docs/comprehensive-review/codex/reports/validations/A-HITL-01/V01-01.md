# A-HITL-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

Each concurrently executing conversation has a call-scoped approval transport that cannot be replaced by another conversation.

## Method

Trace primary PermissionService construction through `SharedResources::extract_from`, pool Agent construction, GUI/channel per-use provider injection, and the framework's provider replacement storage.

## Expected Result

Policy/rules may be shared, but the selected HumanLoop provider is scoped by request/session/conversation and immutable for the duration of a check.

## Result

- Exit code: 0 for source inspections.
- AgentPool explicitly supports parallel conversations (`agent_pool.rs:183-212`) and extracts one `Arc<PermissionService>` from the primary agent (`:120-159`). Every pool agent receives that same Arc (`:928-930`).
- Each GUI turn calls `set_human_loop_provider_preserving_approvals` with its Tauri handler (`chat.rs:568-582`); each channel sender does the same with its channel provider (`cli/channels.rs:131-152`).
- That method invokes `PermissionService::replace_provider_preserving_cache`, which overwrites one service-wide `RwLock<Arc<dyn PermissionRequestHandler>>` (`echo-agent/src/agent/react/mod.rs:1617-1628`; `service.rs:271-281`). The later check clones whichever handler is current at `service.rs:720-726`.

## Deviations

F-HITL-01 already owns the service's missing session cache identity. This validation concerns application sharing/routing of the provider, not cache scope.

## Conclusion

Failed. Supports A-HITL-01-P1-01.
