# A-HITL-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

GUI/TUI/channel/dispatcher obey one request deadline and return the same typed cancel, timeout, input-drop and default-deny semantics.

## Method

Compare every provider's `HumanLoopRequest.timeout` handling, pending cleanup, dropped response and caller cancellation branches.

## Expected Result

One absolute deadline enters the request and all surfaces honor it; cancellation is distinct from timeout/rejection; input timeout does not fabricate valid empty text.

## Result

- Exit code: 0 for source inspections.
- TUI and channel honor `req.timeout` with a 300-second default (`tui_provider.rs:200-223`; `channel_provider.rs:110-128`). Dispatcher wraps all providers in a separate fixed shared 300-second deadline (`dispatcher.rs:96-124`).
- Tauri ignores `req.timeout` and sleeps a fixed 300 seconds for every kind (`chat.rs:330-430`). Approval/selection timeout maps to Timeout, but input drop/timeout maps to `Text(String::new())` (`:378-388`), whereas TUI/channel empty/drop paths reject or time out.
- GUI cancel sends a synthetic `PendingResponse::Approval` to every kind (`chat.rs:236-255`): approval becomes Rejected, input falls into empty Text, selection becomes Timeout. TUI future cancellation cleans asynchronously; channel has no explicit external cancellation method.

## Deviations

F-HITL-01 owns the framework adapter's loss of timeout fields. This report records additional EKO provider behavior after a complete request is supplied.

## Conclusion

Failed. Supports A-HITL-01-P1-04.
