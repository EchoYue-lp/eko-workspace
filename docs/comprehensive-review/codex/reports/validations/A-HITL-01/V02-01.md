# A-HITL-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

GUI supports every pending approval/input/selection for concurrent active conversations without hiding or overwriting requests.

## Method

Trace Tauri pending map/event identity to frontend listener filtering and store/card cardinality.

## Expected Result

Backend and UI use a conversation/message/request-keyed queue; switching conversations reveals pending requests and cancellation/timeout removes the exact item.

## Result

- Exit code: 0 for source inspections.
- Backend `PENDING_RESPONSES` is a request-id HashMap and Tauri events carry request ID, message key and conversation ID (`chat.rs:210-257,283-430`; `emit_chat_event:114-142`).
- Frontend drops any event not matching the hook's current message/conversation refs (`useTauriChat.ts:50-68`). The global chat store retains only one approval, one input and one selection value (`chatStore.ts:42-44,418-423`), and ChatPanel renders only those singleton cards.
- Backend timeout/cancel removes pending entries, but no corresponding timeout/cancel event clears a card by request ID; ordinary response callbacks clear only the current singleton.

## Deviations

The backend map itself can retain multiple requests; the failure is the application projection/cardinality and visibility across active conversations.

## Conclusion

Failed. Supports A-HITL-01-P1-02.
