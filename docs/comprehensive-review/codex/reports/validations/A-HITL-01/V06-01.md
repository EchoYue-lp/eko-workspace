# A-HITL-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

All interactive surfaces expose and apply the same automated Agent permission-mode choices to primary, existing pooled and future pooled Agents.

## Method

Compare GUI settings, TUI command, CLI command and channel command surfaces with `AgentPool::apply_permission_mode` and future-agent initialization.

## Expected Result

GUI/TUI/CLI/channel offer default/auto-edit/full-auto/strict through one application service that updates primary, pool state, existing pool Agents and future Agents.

## Result

- Exit code: 0 for source inspections.
- GUI and TUI update primary plus `pool.apply_permission_mode` (`panels.rs:38-79`; `tui/events.rs:3576-3606`). Pool correctly applies to current Agents and stores the value for new Agents (`agent_pool.rs:465-491,928-932`).
- CLI `/permission` updates only `CommandContext.agent` (`cmd_impls/coding.rs:638-690`). CommandContext contains no pool handle (`cli/command.rs:83-108`), even though REPL ChatResources later uses a pool.
- Channel supports `/mode` only and exposes no permission-mode command or shared application policy update. Its pooled Agents therefore retain whatever pool default was set at process construction.

## Deviations

This concerns Agent automation policy parity. It does not authorize applying permission mode to direct user terminal/file/MCP commands.

## Conclusion

Failed. Supports A-HITL-01-P1-05.
