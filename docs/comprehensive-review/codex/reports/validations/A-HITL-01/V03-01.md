# A-HITL-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

TUI and channel providers preserve multiple concurrent request identities instead of superseding or overwriting them.

## Method

Inspect provider pending storage, publication, response and cleanup paths; connect them to pooled/background concurrency and shared dispatcher cancellation.

## Expected Result

Pending state is keyed by stable request/call identity; cancellation/removal affects only that request; multiple Subagent/tool requests remain independently answerable.

## Result

- Exit code: 0 for source inspections.
- `TuiHumanLoopProvider` stores one `Option<PendingApproval>` and unconditionally overwrites it for every request (`tui_provider.rs:78-80,170-198`). The older response sender is dropped, causing that request to reject while only the newest card remains.
- `ChannelHumanLoopProvider` also stores one Option; a newer request explicitly rejects and replaces the previous request (`channel_provider.rs:24-35,90-108`). One provider is shared by all per-sender pooled agents owned by a channel handler (`cli/channels.rs:35-68,145-152`).
- Request-id-aware cleanup prevents an old future clearing a newer item, which is positive, but it does not supply multi-request capacity.

## Deviations

No scheduler fixture ran; source establishes the singleton overwrite behavior. Exact arrival interleavings remain future validation.

## Conclusion

Failed. Supports A-HITL-01-P1-03.
