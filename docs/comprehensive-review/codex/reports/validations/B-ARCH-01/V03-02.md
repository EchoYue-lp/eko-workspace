# B-ARCH-01 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

A minimal consumer depending only on `echo_macros` and `echo_core` can compile
the split-crate usage advertised by `echo-macros/README.md`.

## Method

The exact V03-01 temporary crate was compiled outside the filesystem sandbox so
Cargo could populate its source cache. Its dependencies are `echo_macros`,
`echo_core`, and only the generic serde/schema/futures dependencies required by
generated code; it deliberately does not depend on `echo_agent`.

```text
cargo check --offline --manifest-path /tmp/echo-macro-attr.jqUlCl/Cargo.toml
```

## Expected Result

If the macro package is independently usable, `#[tool]` resolves the core
contracts and the probe compiles without the facade package.

## Result

- Exit code: 101
- Duration: about 15 seconds
- Summary: compilation reached `macro_attr_probe` and failed at `#[tool]` with
  `Could not find echo_agent in dependencies or dev-dependencies`. Source
  inspection shows `echo_agent_crate_path()` is used by all seven attribute
  macros, whereas only `derive(Tool)` implements a core-first resolver.
- Log/artifact: `/tmp/echo-macro-attr.jqUlCl`

## Deviations

The README sample has additional return-type/API issues, but this probe uses a
valid core `Result<ToolResult>` signature to isolate dependency resolution.

## Conclusion

The split `echo_macros` crate is not independently usable for its advertised
attribute macros. This supports `B-ARCH-01-P1-01`.

## Follow-Up

`F-MAC-01` must test every macro in both facade-only and core-plus-macros
consumer crates, including dependency renames.
