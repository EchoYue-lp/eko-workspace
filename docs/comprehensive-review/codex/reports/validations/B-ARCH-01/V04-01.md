# B-ARCH-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

Current root and split-crate READMEs accurately describe the eight-package
architecture, public import paths, features, and target counts.

## Method

Every split-crate README, root README architecture/feature sections, crate
roots, manifest feature tables, and the corrected Cargo target inventory from
`B-BASE-01/V03-02` were compared.

```text
for c in echo-core echo-macros echo-execution echo-tools echo-integration echo-state echo-orchestration; do sed -n '1,220p' "$c/README.md"; done
nl -ba README.md | sed -n '88,130p;179,205p;281,300p;386,397p'
rg -n 'plan-execute|self-reflection|full.*everything|runnable examples|echo-agents' README.md Cargo.toml
```

## Expected Result

Documents name only current packages/features, demonstrate imports that exist,
and do not promise counts or meta-feature coverage contradicted by metadata.

## Result

- Exit code: 0 for inspection commands; claim failed
- Duration: under one second
- Summary: the root architecture tree lists nonexistent `echo-agents`, says 64
  examples while metadata has 68, and another highlight says 66. It claims
  `full` enables everything and lists nonexistent `plan-execute` and
  `self-reflection` features. `echo-core/README.md` imports nonexistent
  `echo_core::prelude` and names nonexistent `ReActAgent`/`SubAgent` core types.
  `echo-tools/README.md` labels files/shell dependencies as default although its
  default feature set is empty. `echo-integration/README.md` imports MCP and
  channels without showing their required features. `echo-macros/README.md`
  advertises a split-crate attribute macro path that fails V03-02.
- Log/artifact: line anchors are in `B-ARCH-01.md`

## Deviations

This task checked architecture and import-contract statements, not every
feature description or runnable snippet in the documentation set.

## Conclusion

Architecture documentation is materially stale and cannot be used as a reliable
consumer contract. This supports `B-ARCH-01-P2-04`.

## Follow-Up

`B-DOC-01` should index broader drift; `F-API-01` should compile representative
public examples before documentation is rewritten.
