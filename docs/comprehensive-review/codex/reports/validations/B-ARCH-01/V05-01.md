# B-ARCH-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The facade's `workspace` escape hatch exposes `echo_tools`, one of the seven
split crates it claims to provide direct access to during migration.

## Method

A minimal facade-only consumer at `/tmp/echo-workspace-facade.uCau6N` imported
`echo_agent::workspace::tools` with default features disabled. It was compiled
outside the filesystem sandbox after dependency cache preparation.

```text
cargo check --offline --manifest-path /tmp/echo-workspace-facade.uCau6N/Cargo.toml
```

## Expected Result

The import succeeds if direct workspace access covers every split runtime
member.

## Result

- Exit code: 101
- Duration: about 12 seconds on the final cached run
- Summary: rustc emitted E0432: no `tools` in `echo_agent::workspace`.
- Log/artifact: `/tmp/echo-workspace-facade.uCau6N`

## Deviations

None. The failure is the exact public import under test.

## Conclusion

The workspace escape hatch is incomplete and contradicts its general direct
access documentation. This supports `B-ARCH-01-P2-02`.

## Follow-Up

`F-API-01` should decide the intended stable public contract before changing
this migration API.
