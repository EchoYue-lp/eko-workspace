# X-PLG-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework external edits excluded through committed-HEAD inspection; CLI `Cargo.lock` excluded

## Claim

Existing tests should detect stale Tool/Hook/Skill/Subagent state in primary,
existing-pool and future-pool Agents after plugin lifecycle transitions.

## Method

```text
Inventory plugin_runtime.rs, plugin_components.rs and agent_pool.rs test names
and assertion bodies. Classify coverage of initial load, disable/uninstall,
malformed reload rollback, lifecycle callbacks, scheduler/LSP, source cleanup,
shared resources, per-Agent propagation, removal and intent classifier refresh.
Do not execute tests.
```

## Expected Result

At least one integrated fixture should mutate a plugin after pool construction
and compare complete component identity/generation across primary, an existing
pooled Agent and a future pooled Agent, including routing after removal.

## Result

- Exit code: 0
- Duration: static test inventory; not timed
- Summary: tests cover real primary-Agent load/disable/uninstall, malformed
  reload restoration, native lifecycle bracketing/failure, monitor/LSP recovery,
  preferences, and generic pool sharing/creation. No test constructs
  PluginRuntimeService with AgentPool, mutates a plugin after pool creation, or
  asserts Skill/Subagent removal/refresh in existing and future pool Agents. No
  test rebuilds or probes IntentRouter after plugin Skill change.
- Log/artifact: this report

## Deviations

Tests were inspected only; the review instruction prohibited execution.

## Conclusion

The existing suite would remain green with both X-PLG findings present. This
supports the regression requirements but does not itself establish runtime
failure beyond the source traces in V05 and V06.

## Follow-Up

Add the integrated generation fixture described in V09 after implementation.
