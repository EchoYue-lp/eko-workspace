# X-PLG-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework external edits excluded through committed-HEAD inspection; CLI `Cargo.lock` excluded

## Claim

EKO's primary-Agent plugin transition has an explicit prepare, deactivate,
replace, activate, commit and unload sequence with source-scoped cleanup where
framework primitives expose it.

## Method

```text
Trace reload/enable/disable/configure/install/uninstall through
apply_candidate and replace_agent_components. Record the ordering for dependency
resolution, component parsing, collision checks, replacement LSP preparation,
lifecycle deactivation, monitor replacement, primary-Agent unload/wire,
Subagent registration, LSP swap, lifecycle activation, state commit, event
emission and uninstall. Compare cleanup with WiredPluginComponents and
HookSource::Plugin.
```

## Expected Result

Parsing and collision failures should precede live mutation; successful
transitions should replace one coherent primary generation; unload should use
source ownership rather than global name guesses.

## Result

- Exit code: 0
- Duration: static state-transition inspection; not timed
- Summary: EKO prepares application components and replacement LSP before live
  mutation, deactivates lifecycle callbacks, replaces monitors, unloads prior
  primary components, wires candidate framework components, registers candidate
  Subagents, swaps LSP, activates callbacks, then commits registry/prepared
  state. Skill cleanup uses `plugin:{name}`, Hook cleanup uses
  `HookSource::Plugin`, and Subagents are explicitly removed by prepared name.
  Disable/uninstall reach that path for enabled plugins. These are material
  positive boundaries to retain.
- Log/artifact: this report

## Deviations

Framework MCP receipts are server-name based and generic `wire_all` is not
transactional; those established defects remain owned by F-PLG-01.

## Conclusion

The primary-Agent adapter has a recognizable lifecycle transaction skeleton.
The cross-Agent defects are not evidence that this entire sequence should be
replaced.

## Follow-Up

V04 inspects failure rollback; V05 compares the sequence with AgentPool state.
