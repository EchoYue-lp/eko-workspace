# A-FE-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework dirty paths excluded

## Claim

Generated TypeScript optional/null and numeric types describe the values Rust actually serializes across HTTP, WebSocket, and Tauri JSON boundaries.

## Method

```text
Compare serde attributes and scalar Rust types with generated ApiError.ts,
ServerMessage.ts, SubagentArtifactResult.ts, and the dynamic execution-event wire adapter.
```

## Expected Result

Omitted values are optional in TypeScript, explicit null values are nullable, and 64-bit integers have a wire representation that survives JavaScript parsing.

## Result

- Exit code: not-applicable
- Duration: static serialization/type comparison
- Summary: ApiError.details/request_id and every non-Pong ServerMessage.id use `skip_serializing_if = Option::is_none`, but generated TypeScript requires each property and represents absence only as `| null`. The Subagent artifact binding declares `bytes: bigint | null`, while `execution://event` is assembled through `serde_json::Value`; the handwritten wire type consequently accepts number/string/bigint and converts after receipt. The latter documents a contract split but no realistic current precision-loss impact was established.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/error.rs:23`; `echo-agent-cli/echo-agent-app-core/src/types/response.rs:218`; `echo-agent-cli/web-frontend/src/generated/ApiError.ts:22`; `echo-agent-cli/web-frontend/src/generated/ServerMessage.ts:8`; `echo-agent-cli/web-frontend/src/generated/SubagentArtifactResult.ts:3`; `echo-agent-cli/web-frontend/src/stores/subagentRunStore.ts:42`

## Deviations

No runtime serializer was executed. The `serde` omission behavior is source-conclusive; the Tauri u64 transport behavior remains a future fixture concern.

## Conclusion

Supports A-FE-01-P2-02 for optional/null semantics. The numeric observation is retained as coverage risk, not promoted to a separate finding.

## Follow-Up

Export optional fields as `field?: T` when serde omits them and serialize IPC u64 values as decimal strings before TypeScript bigint conversion.
