# A-FE-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; only clean framework `src/agent/subagent/events.rs` inspected

## Claim

Every user-visible Subagent stream variant in the canonical framework event bus is represented on the GUI execution channel and in its TypeScript discriminated union.

## Method

```text
1. Enumerate current SubagentEvent variants from clean framework events.rs.
2. Follow each execution-flow variant through the Tauri event bridge match.
3. Compare emitted `event` strings with SubagentRunEventKind and store ingestion.
4. Inspect existing store tests for the omitted variants.
```

## Expected Result

Lifecycle, thinking start/delta/end, final-answer token delta, usage, tool, and terminal events either reach a typed GUI consumer or have a documented lossless alternate projection.

## Result

- Exit code: not-applicable
- Duration: static variant matrix
- Summary: Lifecycle, usage, isolation, terminal, and tool summaries are forwarded. The bridge explicitly executes `continue` for DispatchThinkingStarted, DispatchThinkingDelta, DispatchThinkingEnded, and DispatchTokenDelta. `SubagentRunEventKind` has no corresponding variants, so no lossless alternate consumer exists on the GUI channel. Existing Subagent store tests cover lifecycle/result projection, not these omitted stream variants.
- Log/artifact: `echo-agent/src/agent/subagent/events.rs:114`; `echo-agent/src/agent/subagent/events.rs:153`; `echo-agent-cli/src/tauri/mod.rs:383`; `echo-agent-cli/src/tauri/mod.rs:649`; `echo-agent-cli/web-frontend/src/stores/subagentRunStore.ts:31`

## Deviations

Framework source was clean at inspection but its repository contained unrelated external dirty files. Those paths were excluded.

## Conclusion

Supports A-FE-01-P1-01: the GUI event contract deliberately omits live Subagent reasoning/output stream information, violating the required surface parity.

## Follow-Up

Add Rust-to-TypeScript event fixtures for every execution-flow variant and GUI reducer/component tests for thinking/token streaming.
