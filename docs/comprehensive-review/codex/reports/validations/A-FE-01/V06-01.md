# A-FE-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework dirty paths excluded

## Claim

The machine-readable Tauri error kind survives the generic frontend invoke adapter.

## Method

```text
1. Inspect IpcError variants and Serialize implementation.
2. Trace a representative validation/not-found command failure through
   apiInvoke and errorMessage to component/store callers.
3. Search frontend callers for structured kind/error matching.
```

## Expected Result

Frontend code receives a typed error carrying `kind`, `message`, and any compatibility field, so it can distinguish validation, not-found, and internal failures.

## Result

- Exit code: not-applicable
- Duration: static error-path trace
- Summary: Rust serializes `{kind,message,error}`. `apiInvoke` catches it and throws a new native Error containing only `errorMessage(error)`, irreversibly discarding `kind` and `error`. No frontend `IpcError` type or structured matching path was found. TaskRuntime recovery conflicts, file revision validation, and missing resources therefore all collapse to display strings at the shared adapter.
- Log/artifact: `echo-agent-cli/src/tauri/error.rs:72`; `echo-agent-cli/src/tauri/error.rs:102`; `echo-agent-cli/web-frontend/src/lib/tauri-bridge.ts:44`; `echo-agent-cli/web-frontend/src/lib/tauri-bridge.ts:164`

## Deviations

Direct raw `invoke` users may observe the original object; this finding is limited to the exported shared `apiInvoke` path used by endpoints.

## Conclusion

Supports A-FE-01-P2-03. The Rust typed error contract exists but is defeated at the adapter boundary.

## Follow-Up

Define a generated/shared IpcError DTO and preserve it in an Error subclass or typed Result decoder.
