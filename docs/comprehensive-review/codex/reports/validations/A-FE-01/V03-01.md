# A-FE-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework external dirty paths excluded

## Claim

Handwritten frontend command response assertions match the registered Rust Tauri responses field-for-field.

## Method

```text
Compare exported frontend return types and live component reads with Rust JSON construction for:
- workflow list/get/create/delete/execute;
- terminal create/list/close.
```

## Expected Result

Every required frontend field is emitted with the declared type and every command result is asserted as its actual shape.

## Result

- Exit code: not-applicable
- Duration: static field matrix
- Summary: The claim failed. Workflow list emits id/name/node_count/edge_count/created_at/updated_at while WorkflowInfo requires definition/status/created_at; WorkflowPanel renders missing `status`. Workflow create emits `{success,id}` but is asserted as WorkflowInfo. Terminal create emits `{id,pid}` but TerminalSession requires `{id,cwd,created_at}`; close emits `{success}` but the endpoint asserts `{closed:string}`. TerminalDrawer currently bypasses the exported terminal endpoint in Tauri mode, making those two terminal lies latent rather than the active drawer's immediate data source.
- Log/artifact: `echo-agent-cli/src/tauri/commands/panels.rs:677`; `echo-agent-cli/src/tauri/commands/panels.rs:710`; `echo-agent-cli/web-frontend/src/types/api.ts:303`; `echo-agent-cli/web-frontend/src/components/workflow/WorkflowPanel.tsx:72`; `echo-agent-cli/src/tauri/terminal.rs:278`; `echo-agent-cli/src/tauri/terminal.rs:402`; `echo-agent-cli/web-frontend/src/api/endpoints.ts:956`

## Deviations

This field matrix sampled the known high-risk handwritten workflow/terminal surfaces from dependency evidence; it is not an exhaustive matrix of every dynamic `serde_json::Value` command.

## Conclusion

Supports A-FE-01-P2-01. It also independently confirms the DTO aspect of A-SRF-02's workflow/terminal observation without duplicating that task's lifecycle/layering findings.

## Follow-Up

Generate or explicitly share response DTOs for all Tauri commands, then fixture-test the complete command inventory.
