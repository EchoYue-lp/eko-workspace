# F-CTX-01 / V03 / Attempt 05

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Framework-owned projection identity cannot be forged by ordinary user message content.

## Method

Trace `is_context_projection_message`, marker parsing, protected classification, multimodal/plain user insertion, transcript filtering, and all production marker-registration call sites.

## Expected Result

Internal projection identity is typed/out-of-band or validates an unforgeable ownership field; arbitrary user text remains visible and compressible.

## Result

- Exit code: 0
- Duration: not measured
- Summary: identity is only `starts_with("<echo-agent-context-projection-v1>")` on text or first text part. User messages enter ContextManager without sanitization and can carry the same prefix. Such content becomes permanently protected and is omitted by transcript persistence filtering, even without a valid marker envelope.
- Log/artifact: `echo-state/src/compression/mod.rs:30`, `:36`, `:678`; `src/agent/snapshot.rs:35`; `src/agent/react/run/context.rs:607`

## Deviations

The prefix constant is private, but obscurity does not make a content prefix typed identity; copied/restored content can contain it accidentally.

## Conclusion

The claim fails and supports F-CTX-01-P2-08.

## Follow-Up

Move projection ownership to typed metadata/envelope state and persist user content independently of presentation markers.
