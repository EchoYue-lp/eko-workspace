# F-CTX-01 / V02 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Resolved model context windows and runtime context-window updates reach the active ContextManager.

## Method

Trace `infer_context_window`, `ModelProfileResolver`, `AgentConfig.model_profile`, config-file resolution, `new_inner`, and all `set_token_limit` callers/readers in both repositories.

## Expected Result

Construction uses the resolved profile/config window once, and a runtime update atomically rewires the ContextManager limit/budget/compressor before the next request.

## Result

- Exit code: 0
- Duration: not measured
- Summary: `ModelProfile.context_window` has no production consumer outside profile construction. Generic builders keep their separate 396K default. `AppConfig` has a second resolution path and, when neither token_limit nor explicit context_window is set, writes `usize::MAX` despite computing an inferred/default window. `set_token_limit` changes only `AgentConfig`; the already-built ContextManager has no corresponding setter call. EKO model switch paths call this inert runtime method.
- Log/artifact: `echo-core/src/llm/capabilities.rs:182`, `src/config.rs:97`, `:120`, `src/agent/react/mod.rs:336`, `src/agent/react/capabilities.rs:484`; bounded EKO callers recorded in task report

## Deviations

The config-file path does use an explicit configured context window during initial construction; the failure concerns inferred/profile facts and runtime mutation.

## Conclusion

The claim fails and supports F-CTX-01-P1-02 and P1-03.

## Follow-Up

Future model-profile/config/runtime-switch table in V04-01.
