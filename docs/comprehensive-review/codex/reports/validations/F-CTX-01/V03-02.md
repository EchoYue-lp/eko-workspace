# F-CTX-01 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Automatic compression guarantees its returned message set is within the effective token limit while preserving protected content.

## Method

Inspect the default compressor installed by `new_inner`, `prepare` effective-limit calculation, protected split/merge, SlidingWindow implementation, canonical reinjection, and any post-compression token assertion/retry/error.

## Expected Result

Compression either returns a request within budget or a typed cannot-fit error; protected and canonical content are included in that final check.

## Result

- Exit code: 0
- Duration: not measured
- Summary: the default SlidingWindow keeps 40 messages by count and never reads `CompressionInput.token_limit`. Protected messages are removed before compression and merged afterward; canonical content is then reinserted. `prepare` returns without a final total-token check. A single large recent/protected message or 40 large messages can remain above the limit.
- Log/artifact: `src/agent/react/mod.rs:346`, `echo-state/src/compression/compressor/sliding_window.rs:29`, `echo-state/src/compression/mod.rs:1335`, `:1525`

## Deviations

Other optional compressors may reduce tokens more aggressively; the finding concerns the framework default and the missing universal postcondition.

## Conclusion

The claim fails and supports F-CTX-01-P1-05.

## Follow-Up

F-CMP-01 must review compressor-specific semantic preservation; V04-01 defines limit regressions.
