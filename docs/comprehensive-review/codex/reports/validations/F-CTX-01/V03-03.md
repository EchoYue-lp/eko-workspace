# F-CTX-01 / V03 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Public ContextAssembler enforces the total and per-source token budget it exposes.

## Method

Map every `ContextBudget` field to `assemble`, inspect its arithmetic/unit semantics, source ordering, and all repository consumers/tests.

## Expected Result

`total_tokens` and `user_reserve` constrain the final list; all sources are charged in one unit; overflow is handled; protected high-priority content has an explicit cannot-fit policy.

## Result

- Exit code: 0
- Duration: not measured
- Summary: `total_tokens` and `user_reserve` are never read. Only memory/history/tool-result caps are partially applied; memory cap counts characters while history/tools use UTF-8 bytes divided by four. System, developer/project/task/hook, subagent reports, and final user input are unbounded. Accumulation uses unchecked `+=`. Repository use is examples/tests only, but this remains a documented public custom-loop building block.
- Log/artifact: `src/context/mod.rs:31`, `:111`, `:183`, `:200`, `:223`

## Deviations

No claim is made that the default ReAct loop uses this helper; it explicitly does not.

## Conclusion

The claim fails and supports F-CTX-01-P2-06.

## Follow-Up

Choose a single shared budget/accounting engine before wiring this helper into ReAct.
