# F-CTX-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Multimodal attachments are accounted for and all public multimodal paths invoke context preparation.

## Method

Inspect `MessageContent::Parts/as_text`, ContextManager token estimation, multimodal streaming initialization/preparation, and direct `chat_multimodal` request construction.

## Expected Result

Provider-visible images/files contribute a conservative cost or trigger a typed unsupported/unbounded decision, and every path prepares/compresses before the request.

## Result

- Exit code: 0
- Duration: not measured
- Summary: `as_text` drops ImageUrl and File bodies, so ContextManager counts only text parts. Streaming preserves attachments but prepares against that text-only estimate. `chat_multimodal` copies raw context without `prepare`, sends `max_tokens: None`, and ignores configured temperature/tool/output context policy.
- Log/artifact: `echo-core/src/llm/types.rs:90`, `echo-state/src/compression/mod.rs:1541`, `src/agent/react/run/context.rs:561`, `src/agent/react/mod.rs:3170`

## Deviations

Provider-native image tokenization is model-specific; the framework still needs a conservative or explicit admission contract.

## Conclusion

The claim fails and supports F-CTX-01-P1-04.

## Follow-Up

Future large inline file/image tests in V04-01.
