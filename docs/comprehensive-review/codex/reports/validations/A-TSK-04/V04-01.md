# A-TSK-04 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; relevant framework files match HEAD

## Claim

Every state write derived from a loaded plan snapshot is fenced by that snapshot's revision or claim.

## Method

```text
Trace the failed-upstream branch in RuntimeDagExecutor::execute.
Compare the loaded RuntimePlanSnapshot identity with RuntimeDagController::block_task,
EkoRuntimeDagController::block_task, and TaskRuntimeStore::set_task_status parameters.
Construct the static interleaving: load revision N -> commit patch N+1 that removes
the failed dependency but retains the task ID -> execute the old block callback.
```

## Expected Result

The old callback is rejected as Superseded/ReloadSnapshot and cannot mutate revision N+1.

## Result

- Exit code: 0
- Duration: static interleaving inspection
- Summary: The controller callback receives only run/task/reason (`runtime_executor.rs:124,239-244`). EKO forwards only task ID to broad `set_task_status` (`executor.rs:1564-1580`), which validates existence but no revision, spec, prior status, or claim (`store.rs:953-983`). The old callback can therefore mark the retained N+1 task Blocked and clear its claim.
- Log/artifact: source anchors above

## Deviations

No test covers a plan patch winning immediately before dependency blocking.

## Conclusion

The claim fails and supports A-TSK-04-P1-01.

## Follow-Up

Add a revision-fenced block outcome and an interleaving regression test.
