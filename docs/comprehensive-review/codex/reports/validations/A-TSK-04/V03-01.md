# A-TSK-04 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; unrelated framework dirty paths excluded

## Claim

On the normal EKO dispatch path, claim identity is persisted and an obsolete claim cannot overwrite a task whose current status or claim differs.

## Method

```text
Inspect store.rs:985-1121 and event_rebuild.rs:207-247 field by field.
Confirm claim generation inputs, TaskStarted payload persistence, projection folding,
and the exact comparison used by terminal/requeue writes. Inventory claim race tests.
```

## Expected Result

Claim acquisition checks revision/spec/Pending under the run lock, persists the claim, and later writes require Running plus exact claim equality.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: `claim_task` rejects revision/spec/status drift, persists `(revision, retry_count+1, spec_hash)`, and `set_claimed_task_status`/`requeue_claimed_task` return Superseded unless status and claim match. Replay restores the serialized claim. Existing tests cover a revision race, stale completion after cancellation, and a changed spec hash.
- Log/artifact: `store.rs:985-1121`, `event_rebuild.rs:207-247`, `store.rs:3039-3246`

## Deviations

The tuple can repeat across physical restart/reclaim; that structural defect is F-TSK-03-P1-01 and is not reissued here.

## Conclusion

Normal changed-tuple stale-write rejection passes. It does not prove global uniqueness of a physical lease.

## Follow-Up

Consume F-TSK-03-P1-01 when implementing a durable claim generation.
