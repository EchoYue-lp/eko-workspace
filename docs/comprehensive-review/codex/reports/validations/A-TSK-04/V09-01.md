# A-TSK-04 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; unrelated framework dirty paths excluded

## Claim

Every persisted task/Subagent lifecycle event eventually reaches configured hooks in event order across bootstrap and process restart.

## Method

```text
Trace append_event_line -> event_hook -> HookEventDispatcher queue/fire and
register_agent_tools -> attach_hook_event_dispatcher. Search these files for a
durable delivery cursor, replay scan, stable delivery identity, and deduplication.
Inventory queue/order/shutdown tests.
```

## Expected Result

Events appended before attachment or process death are replayed from the next undelivered seq without reordering or silent omission.

## Result

- Exit code: 0
- Duration: static call-graph and state search
- Summary: The durable append precedes a live callback (`file_shadow.rs:153-176`), attachment explicitly says earlier events are not dispatched (`store.rs:215-243`), and registration only attaches a fresh in-memory dispatcher (`register.rs:106-129`). The queue has backpressure/order/flush for the current process but no persisted cursor or startup replay (`hook_event_dispatcher.rs:69-185`). A stop after append or before consumer fire loses hook delivery permanently.
- Log/artifact: source anchors above

## Deviations

The code guarantees order only for events successfully enqueued during the current process.

## Conclusion

The claim fails and supports A-TSK-04-P2-01.

## Follow-Up

Define the hook durability contract; if durable, add ordered replay with stable delivery IDs and idempotent consumers.
