# A-TSK-04 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; unrelated framework dirty paths excluded

## Claim

Every user-confirmed retry advances the same durable retry budget and produces a distinct next attempt.

## Method

```text
Compare normal retry_blocked_task (store.rs:1211-1366) with recovery
resolve_recovery_task Retry (store.rs:2132-2179), then follow claim_task's
attempt derivation at store.rs:1010-1017. Inventory recovery tests.
```

## Expected Result

Both retry paths check max_retries and durably advance retry_count before Pending/reclaim.

## Result

- Exit code: 0
- Duration: static field/path comparison
- Summary: Normal retry checks task/run state and budget and increments `retry_count`. Recovery Retry appends RecoveryResolved and calls broad `set_task_status(Pending)` without a budget check or retry-count update. A restarted physical attempt can therefore reclaim the same logical attempt number; repeated recovery decisions do not consume max_retries. The recovery test asserts only Pending/resume.
- Log/artifact: `store.rs:1211-1366`, `store.rs:2132-2179`, `store.rs:2810-2854`

## Deviations

The resulting repeatable claim tuple activates canonical F-TSK-03-P1-01; this validation reports only the EKO recovery-policy cause.

## Conclusion

The claim fails and supports A-TSK-04-P1-03.

## Follow-Up

Use one atomic retry-budget primitive for normal and recovery retries.
