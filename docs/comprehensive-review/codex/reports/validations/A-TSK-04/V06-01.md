# A-TSK-04 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; unrelated framework dirty paths excluded

## Claim

Boot recovery is restart-idempotent: interruption or write failure at any recovery step is completed by the next boot.

## Method

```text
Inspect recover_incomplete store.rs:1620-1775 in durable-write order.
For every possible stop/failure after transition_run(Paused), derive the next
recover_incomplete selection and the background resume behavior at service.rs:556-586.
```

## Expected Result

An incomplete recovery remains selected or is atomically rolled back; resume cannot start while a task retains an orphaned Running claim.

## Result

- Exit code: 0
- Duration: static crash-point trace
- Summary: Recovery selects only Running runs, persists Paused first, then resets each Running task. Task reset failures are logged and skipped. A crash/failure after Paused leaves Paused plus Running task; the next boot excludes it. With no synthesized recovery blocker, background resume transitions the run to Running and starts the executor, whose snapshot still classifies that task as in flight without a live dispatch.
- Log/artifact: `store.rs:1631-1775`, `service.rs:556-586`

## Deviations

The existing boot tests exercise one uninterrupted pass only.

## Conclusion

The claim fails and supports A-TSK-04-P1-02.

## Follow-Up

Make recovery one durable operation or rescan Paused runs with orphaned Running claims until cleanup settles.
