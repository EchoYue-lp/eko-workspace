# Q-TST-01 / V00 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework HEAD matched but had 67 external modified/deleted/untracked paths; CLI HEAD matched with external `Cargo.lock` modification

## Claim

All Q-TST-01 source conclusions can be reconstructed from the two assigned
committed snapshots without reading either live dirty body.

## Method

```text
1. `git rev-parse HEAD` and `git status --short` in each repository.
2. Use only `git show <assigned-sha>:<path>`, `git grep <assigned-sha>`, and
   `git ls-tree <assigned-sha>` for source/test/CI inspection.
3. Verify no Q-TST-01 report existed before creation.
```

## Expected Result

Both HEADs equal the assigned revisions; all live changes remain excluded.

## Result

- Exit code: 0
- Duration: under one second
- Summary: both HEADs matched. The framework worktree was heavily externally
  dirty and the CLI had only dirty `Cargo.lock`; no live dirty source or diff
  was used. No prior Q-TST-01 task/validation file existed.
- Log/artifact: none

## Deviations

The worktrees were not clean, so ordinary filesystem reads of source were not
admissible. Committed-blob reads supplied every source anchor.

## Conclusion

This supports the stated source-isolation boundary. It does not establish that
either repository currently builds or tests.

## Follow-Up

Repeat against new explicit commits if either reviewed SHA changes.
