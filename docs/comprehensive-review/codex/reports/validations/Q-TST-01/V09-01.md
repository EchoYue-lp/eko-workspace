# Q-TST-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only; no mutation executed

## Claim

Selected named contract tests would fail under a source-level negative control
that removes the production behavior they claim to guard.

## Method

```text
Static counterfactual inspection only (no edits/builds/tests):
A. Remove `cache_user_id` assignment from both ReAct ChatRequest builders.
B. Remove pure-channel scheduler/background composition.
C. Strip identity/terminal fields in the Tauri/reducer boundary.
Trace whether each existing named test reads/calls the altered production line.
```

## Expected Result

Each named propagation/parity/contract suite depends on the changed production
line and therefore would fail.

## Result

- Exit code: not-applicable (static negative control)
- Duration: under one second
- Summary: A would leave all `cache_user_id_test.rs` assertions unchanged
  because none invokes either builder assignment. B leaves
  `capability_matrix_has_evidence_for_every_surface` unchanged because it checks
  non-empty prose. C leaves producer serialization tests unchanged because they
  stop before the actual adapter/reducer. B/C are existing canonical findings;
  A supports Q-TST-01-P2-04.
- Log/artifact: none

## Deviations

No actual mutant was compiled or executed by explicit instruction. This proves
static independence of test and production line, not the result of a mutation
framework.

## Conclusion

The selected negative controls fail the credibility criterion. A future
mutation run is still required after fixtures are rewired across each seam.

## Follow-Up

See V10 for the deferred executable mutation gate.
