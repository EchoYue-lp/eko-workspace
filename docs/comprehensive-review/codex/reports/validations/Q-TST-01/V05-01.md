# Q-TST-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only; live dirty paths excluded per V00

## Claim

The mandatory CI topology compiles/tests target-specific macOS and Windows
production branches represented in both repositories.

## Method

```text
Inspect both committed workflows for runner matrices/targets. Search committed
Rust blobs for macOS/Windows cfg branches and sample sandbox, command, Tauri,
IPC and TUI paths.
```

## Expected Result

Each supported desktop target has a compile lane, with behavioral fixtures for
its process/path/sandbox/IPC adapters where feasible.

## Result

- Exit code: 0
- Duration: under one second
- Summary: both workflows use only `ubuntu-latest`. Static search finds 42
  framework and 14 EKO macOS/Windows cfg occurrences, including framework local
  sandbox/script execution and EKO browser commands, desktop lifecycle, IPC and
  TUI platform actions. Linux all-features does not compile `#[cfg(target_os =
  "macos")]` or Windows-only bodies.
- Log/artifact: none

## Deviations

Raw cfg occurrence count includes both production and test declarations and is
not itself defect count. The absence of target lanes is exact.

## Conclusion

The claim fails and supports Q-TST-01-P2-03. It does not prove a particular
platform branch is broken.

## Follow-Up

Add pinned macOS/Windows compile lanes and focused platform adapter fixtures;
keep live UI automation in a separate, appropriately scheduled tier.
