# Q-TST-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only; live dirty paths excluded per V00

## Claim

Tests named as integration/contract evidence actually traverse the production
seam stated in their comments or names.

## Method

```text
Sample all three external Rust test files plus app-core `surface_contract.rs`.
For each claimed invariant, trace whether the test calls the production
producer, adapter and consumer rather than constructing the postcondition.
```

## Expected Result

Propagation/parity/contract claims fail if the responsible production
assignment, registration, adapter or reducer is removed.

## Result

- Exit code: 0
- Duration: under one second
- Summary: EKO `runtime_state_e2e.rs` meaningfully crosses the application Agent
  constructor into framework state/prompt fields. Framework `react_smoke.rs`
  honestly limits itself to two pure paths. In contrast,
  `cache_user_id_test.rs` says it verifies every ReAct LLM path but tests only
  config/DTO acceptance, tracker arithmetic, hashing and cache layout; it never
  calls the assignments in `react_loop.rs:61` or `think.rs:322`.
  `surface_contract.rs` constructs its own prose matrix and producer DTOs; it
  never traverses composition roots, Tauri wire adapters or frontend reducers.
- Log/artifact: none

## Deviations

The surface-contract weakness is already canonically owned by B-PATH-01,
A-SRF-01 and X-EVT-01. This task does not duplicate it.

## Conclusion

The blanket claim fails. Q-TST-01-P2-04 covers the additional cache propagation
overclaim; existing canonical reports retain ownership of parity/event seams.

## Follow-Up

Rename tests to their actual local contract or replace them with injected
recorders/recorded fixtures that cross the production seam.
