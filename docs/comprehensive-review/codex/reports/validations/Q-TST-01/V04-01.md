# Q-TST-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only; live dirty paths excluded per V00

## Claim

Ignored tests are limited to legitimate opt-in live integrations and no known
red core regression is removed from the mandatory test gate.

## Method

```text
`git grep` both committed revisions for `#[ignore]`; inspect every matched test
and compare its category with the mandatory CI command.
```

## Expected Result

Network/provider/LSP tests may be opt-in; deterministic core regressions must
remain active or the owning defect must block the gate.

## Result

- Exit code: 0
- Duration: under one second
- Summary: five ignored tests are legitimate environment-backed LSP/provider/
  Zotero smoke tests. A sixth deterministic ReAct test,
  `truncated_stream_is_not_accepted_as_complete`, scripts a partial delta plus
  mid-stream error and requires no external service, but is explicitly ignored
  as red until a future milestone. The CI test command does not use `--ignored`.
- Log/artifact: none

## Deviations

The test comment calls this a clean-disconnect fixture, while its actual script
contains an explicit network error. The narrower partial-output/error terminal
invariant is still deterministic and mandatory.

## Conclusion

The claim fails and supports Q-TST-01-P1-02. This report owns the misleading
gate behavior, not the underlying stream implementation defect.

## Follow-Up

Fix the runtime invariant, remove `#[ignore]`, and add a separate true EOF/
missing-terminal fixture if that behavior is also claimed.
