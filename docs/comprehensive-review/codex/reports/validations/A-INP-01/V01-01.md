# A-INP-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

EKO has one application-layer prepared-turn authority, and any remaining
attachment helper has a distinct reachable role rather than a second entry
normalizer.

## Method

```text
1. Inspect PreparedUserTurn, UserTurnInput, InputResourceRef, AttachmentRef,
   build_message_from_refs, and ChatResources definitions.
2. Search both repositories for constructors and behaviorally equivalent
   text-plus-attachment merge helpers.
3. Trace the remaining build_message_from_refs caller.
```

## Expected Result

All product entries use PreparedUserTurn; the only parallel helper is a thin
reconstruction adapter with a unique downstream role.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: `PreparedUserTurn::build` is the sole entry normalization API.
  `build_message_from_refs` remains reachable only for TaskRuntime Subagent
  reconstruction and does not own entry admission or long-input spilling.
- Log/artifact: this report

## Deviations

`AttachmentRef` is a lossy projection of `InputResourceRef`; that is recorded
by A-INP-01-P1-02 rather than treated as harmless duplication.

## Conclusion

The application/framework placement is appropriate and no new framework input
authority is needed. This supports the layering conclusion and P1-02.

## Follow-Up

Primary reviewer should independently sample the constructor/caller search.
