# A-INP-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Attachment identity fixes the exact bytes seen by preparation, the primary
agent, persisted TaskRun data, and later Subagent reconstruction.

## Method

```text
Compare AttachmentData, AttachmentRef, InputResourceRef, inline_attachment_refs,
PreparedUserTurn::build/to_message, and build_message_from_refs fields and reads.
Trace refs into ChatResources/TaskRun.
```

## Expected Result

A stable ID and verified hash/size survive every projection, and bytes are
captured once or verified before each use.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: AttachmentRef carries path/name/MIME/source only. Inline
  InputResourceRef records no sha256. Preparation reads the file, to_message
  reads it again, and Subagent reconstruction can read it again later. The
  projection back to AttachmentRef drops bytes/kind/delivery/hash.
- Log/artifact: this report

## Deviations

Spilled long text does compute a hash; the failure is specific to inline
attachment identity and its projection.

## Conclusion

The same logical attachment can resolve to changed or missing bytes across
consumers. Supports A-INP-01-P1-02.

## Follow-Up

Test mutation/deletion between prepare, message construction, persistence,
restart, and Subagent execution.
