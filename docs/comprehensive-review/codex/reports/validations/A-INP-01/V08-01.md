# A-INP-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Deleting a conversation removes all input artifacts owned by it and reports
cleanup completion truthfully.

## Method

```text
Compare uploads layout/AttachmentRef ownership with GUI delete_conversation and
TUI /delete-session cleanup paths; inventory scoped cleanup tests.
```

## Expected Result

Every input artifact has a conversation owner or shared reference policy, and
delete success covers its cleanup or returns typed pending/failure state.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: long-input artifacts are conversation scoped and cleaned. Uploads
  are flat UUID-prefixed files with no conversation/turn owner in AttachmentRef.
  GUI and TUI deletion clean tool and user-input artifact scopes but cannot
  locate uploads. GUI additionally detaches best-effort cleanup and returns
  success before its result.
- Log/artifact: this report

## Deviations

Existing cleanup tests cover only user-input artifacts, not uploaded files.

## Conclusion

Conversation deletion leaves original uploaded content indefinitely. Supports
A-INP-01-P1-05.

## Follow-Up

Add per-conversation upload ownership and deletion/restart/partial-failure tests;
coordinate broader delete semantics with the state-lifecycle review.
