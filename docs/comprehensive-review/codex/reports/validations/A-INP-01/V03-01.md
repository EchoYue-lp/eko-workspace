# A-INP-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Prepared-turn long and Unicode text handling is path-confined, recoverable by
the model, and free of byte-slice truncation in the reviewed path.

## Method

```text
Inspect spill thresholds, preview construction, path-component normalization,
.partial write/rename flow, artifact root composition, read_artifact/grep
registration, and existing CJK/emoji/boundary tests.
```

## Expected Result

Long content is preserved in a full artifact with hash and typed resource
metadata; only a Unicode-safe preview/reference reaches the model.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: long original messages and long/pasted text resources are stored
  below the shared tool artifact root, referenced with sha256/size/path, and
  recoverable by registered artifact tools. Preview truncation uses
  `chars().take`; scoped path components are ASCII-normalized. Existing unit
  tests cover CJK, emoji, thresholds, projection round-trip, and cleanup.
- Log/artifact: this report

## Deviations

Tests were inventoried but not executed under the pure-static review rule.

## Conclusion

This is a positive control. It supports the current long-text design but does
not prove attachment byte identity across multiple reads.

## Follow-Up

Future dynamic regression should execute the existing tests and add entry-level
long/Unicode/empty cases.
