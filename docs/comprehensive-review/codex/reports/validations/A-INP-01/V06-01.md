# A-INP-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

TUI steer preserves long input, pending attachments, and queue fallback through
the same prepared-turn path as GUI steer.

## Method

```text
Trace key Enter -> handle_enter -> early /steer branch and compare with
handle_slash_command(Steer), including NotSteerable queue behavior.
```

## Expected Result

The dispatcher reaches the prepared branch and queues the same text/resources
when steering is not currently accepted.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: the early branch strips `/steer ` and calls `Message::user` with no
  attachment take/spill. On NotSteerable it queues an empty attachment vector.
  The later correct PreparedUserTurn branch is shadowed for normal typed input.
- Log/artifact: this report

## Deviations

None.

## Conclusion

TUI steer violates surface parity and loses attachment association. Supports
A-INP-01-P1-03.

## Follow-Up

Delete the early authority after routing all steer input through one handler;
test typed command, palette insertion, long Unicode text, attachments, and each
steer terminal outcome.
