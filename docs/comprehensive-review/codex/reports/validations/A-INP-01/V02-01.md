# A-INP-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The five user-input surfaces (GUI, TUI, CLI REPL, IM channel, and steer) all
reach the same prepared-turn merge exactly once with equivalent fields.

## Method

```text
Trace each production constructor into PreparedUserTurn::build and then
drive_chat/to_message or steer_input. Compare text, attachments, mode_hint,
spill_dir, conversation_id, and turn_id field by field.
```

## Expected Result

Every surface supplies the fields it owns and no earlier branch bypasses its
canonical constructor.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: GUI regular, GUI steer, TUI regular, CLI REPL, and channel paths
  construct PreparedUserTurn. Normal typed TUI `/steer <text>` is intercepted
  at `events.rs:1304` and calls `Message::user` directly, before the later
  canonical slash-command steer branch can run.
- Log/artifact: this report

## Deviations

Scheduler/background prompts are system-generated work, not user attachment
entries, and therefore were not counted as missing constructors.

## Conclusion

The five-entry parity claim fails for the live TUI steer route. Supports
A-INP-01-P1-03.

## Follow-Up

Add a surface matrix regression that exercises the actual key/command
dispatcher rather than directly calling the later slash handler.
