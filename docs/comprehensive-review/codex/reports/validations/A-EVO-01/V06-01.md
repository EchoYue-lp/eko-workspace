# A-EVO-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; framework current dirty files excluded.

## Claim

Evidence persistence can recover prior valid records after a torn final append
and remains bounded for long-lived workspaces.

## Method

Inspect append ordering/sync, both full-log readers, locking, record framing,
sequence/checksum, tail recovery, quarantine, snapshot, and compaction paths.

## Expected Result

Only a proven incomplete tail is recoverable; interior corruption fails with a
repair path; current/undo state remains readable and history growth is bounded.

## Result

- Exit code: 1
- Duration: static persistence analysis
- Summary: write/sync can leave a partial line; every read parses every nonempty
  line and one error aborts the entire store. No framing, sequence, checksum,
  recovery, snapshot, quarantine, or compaction exists.
- Log/artifact: task finding A-EVO-01-P1-04.

## Deviations

The exclusive sidecar lock protects cooperating writers while alive; it does not
make a multi-byte append crash-atomic or repair an already torn log.

## Conclusion

Supports A-EVO-01-P1-04.

## Follow-Up

Add framed durable state and tested tail recovery before expanding the log.
