# A-EVO-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; framework dependency evidence accepted.

## Claim

Evidence accept/undo remains crash-consistent across candidate state, target
memory, and merge before-state.

## Method

Enumerate each persistence/mutation boundary in SaveMemory accept, merge accept,
memory undo, merge undo, returned-error compensation, and restart reconciliation.

## Expected Result

An operation phase and all recovery data are durable before mutation; restart
converges incomplete work idempotently.

## Result

- Exit code: 1
- Duration: static crash-window analysis
- Summary: mutations precede Applied/Pending snapshots. Compensation runs only
  after returned errors. `AcceptAttempt`/`UndoAttempt` lack phase/target/before
  authority, and no startup reconciler consumes incomplete attempts.
- Log/artifact: task finding A-EVO-01-P1-03.

## Deviations

F-EVO-01-P1-09 separately owns the framework merge's member-level atomicity.
This result covers EKO's candidate/mutation boundary even after a framework
mutator returns success.

## Conclusion

Supports A-EVO-01-P1-03.

## Follow-Up

Introduce one durable operation journal and crash recovery protocol.
