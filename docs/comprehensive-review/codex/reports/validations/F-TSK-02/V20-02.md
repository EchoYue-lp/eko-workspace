# F-TSK-02 / V20 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Failure propagation computes the transitive blocked closure before selecting a
run disposition.

## Method

Inspect `blocked_by_failures`, `all_unfinished_failed_or_blocked`, and the first
failed-task branch in `RuntimeDagExecutor`.

## Expected Result

For `a(Failed) -> b(Pending) -> c(Pending)`, both descendants are normalized
before the controller receives the exhaustion boolean.

## Result

- Exit code: 0 for inspection; invariant failed.
- Duration: under one second.
- Summary: both predicates check only whether a direct dependency ID is in the
  original `failed` set. The executor persists direct blocks, then immediately
  computes disposition from the same pre-propagation snapshot and returns.
  Multi-level descendants remain Pending and can make exhaustion false.
- Log/artifact: `tasks/runtime.rs:460-499`,
  `runtime_executor.rs:232-264`.

## Deviations

None. Delegated V04-11 provides a three-level executable probe.

## Conclusion

F-TSK-02-P1-03 is accepted.

## Follow-Up

Return normalized transitive status effects and exhaustion from one pure graph
operation.
