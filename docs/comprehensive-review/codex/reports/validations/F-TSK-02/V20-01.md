# F-TSK-02 / V20 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Every persisted `TaskStatus` has one coherent dependency-satisfaction and
restart classification in the canonical DAG state.

## Method

Inspect `TaskStatus::{is_terminal,is_running}`, `DagExecutionState::from_tasks`,
`ready_task_ids`, `all_completed`, and the runtime executor's empty-frontier
branches; trace revision `Skip` and persistent `resume_from_store` into them.

## Expected Result

Skipped prerequisites have one explicit propagation meaning; Paused returns a
paused outcome; orphan Retrying is normalized or has a durable owner.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: `all_completed` counts skipped nodes as resolved, but readiness
  requires dependencies only in `completed`, stranding dependents. `Paused` is
  absent from every state set and reaches generic stall. `Retrying` is classified
  as in-flight and polled indefinitely after restart, while recovery resets only
  `Running` to `Pending`.
- Log/artifact: `tasks/runtime.rs:109-125,350-499`,
  `runtime_executor.rs:206-313`, `revisioned.rs:547-567`,
  `executor.rs:1562-1604`.

## Deviations

No new build; delegated V04-10/V04-12/V04-17 retain executable boundary probes.

## Conclusion

F-TSK-02-P1-01 and P1-02 are accepted.

## Follow-Up

Centralize restart/status normalization and define skip propagation in the sole
dependency analyzer.
