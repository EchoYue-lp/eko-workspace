# F-TSK-02 / V20 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

All public dependency queries are cycle-safe and all execution shapes consume a
single deterministic readiness authority.

## Method

Inspect public `get_dependency_chain`, its recursive helper and task admission;
compare `TaskManager::get_ready_tasks`/one-wave executor paths with
`DagExecutionState::ready_task_ids`/full-DAG paths.

## Expected Result

Queries use guarded traversal with typed cycle outcomes; one-wave and full-DAG
APIs delegate to the same analyzer.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: `TaskManager::add_task` admits arbitrary dependencies, while public
  dependency-chain recursion has no visited/active-path set and repeats a cycle
  until stack overflow. Separately, public one-wave execution uses DashMap-based
  manager predicates; full DAG execution uses `DagExecutionState`, with distinct
  ready/block/skip/order semantics.
- Log/artifact: `tasks/manager.rs:69,240-305,384-403`, `tasks/dag.rs:44-49`,
  `tasks/executor.rs:561-596,1409-1467`, `tasks/runtime.rs:437-478`.

## Deviations

The primary deliberately did not execute an unbounded stack-overflow probe.
Static recursion is conclusive; delegated safe compile probes cover adjacent
graph boundaries.

## Conclusion

F-TSK-02-P1-04 and P2-01 are accepted.

## Follow-Up

Replace the recursive helper and duplicated manager predicates with one guarded,
deterministic analyzer.
