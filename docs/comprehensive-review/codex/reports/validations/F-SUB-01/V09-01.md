# F-SUB-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; review reports are outside the source repositories

## Claim

`SubagentOutcome` artifact availability, hash, and producer identity are based
on runtime observation rather than a model-reported path alone.

## Method

Trace `render_result_contract`, `ReportedSubagentOutcome`,
`parse_subagent_outcome`, path resolution/hydration, observed tool artifacts,
evidence merge, and EKO `assess_task_execution` required-artifact acceptance.
Inspect relative, absolute, pre-existing, and missing-path branches. No files
were created and no test was run.

## Expected Result

A model-reported path remains reported/unverified unless the current execution
observed its creation or an authoritative artifact subsystem supplied its hash
and producer.

## Result

- Exit code: 0
- Duration: static provenance trace
- Summary: parser hydration accepts any readable regular absolute path, or any
  relative file under the current working directory. It hashes the file, marks
  it available, and overwrites `producer_execution_id` with the current
  execution even when no tool event observed that file. EKO TaskRuntime treats
  availability + 64-character hash + non-empty producer as sufficient for a
  required artifact, so a pre-existing unrelated file can satisfy acceptance.
  Observed tool-log artifacts are stronger but do not constrain reported ones.
- Log/artifact: this report

## Deviations

This is correctness/provenance, not an online threat-model claim. Hashing and
UTF-8-safe bounds themselves are implemented defensively.

## Conclusion

The claim failed. Runtime-generated integrity fields overstate production
provenance. Supports F-SUB-01-P1-06.

## Follow-Up

Separate `reported`, `exists`, and `observed-produced` provenance; acceptance
must require the last category or an explicit pre-existing-input rule.
