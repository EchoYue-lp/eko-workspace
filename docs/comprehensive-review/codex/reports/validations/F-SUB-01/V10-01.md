# F-SUB-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; review reports are outside the source repositories

## Claim

Existing source tests guard registration coherence, catalog revision/readiness,
definition semantics, role capability selection, attachments, typed terminals,
and result provenance through live paths.

## Method

Static inventory of tests in Subagent types, builder, registry, context, prompt,
executor, builtin dispatch, ReactAgent catalog, EKO prompt/infra/plugin runtime,
and TaskRuntime result assessment. Assertions were compared with V03-V09 live
call graphs. No test was executed.

## Expected Result

At least one regression test reaches each invariant through the same public or
model-facing path used in production.

## Result

- Exit code: 0
- Duration: static test inventory
- Summary: positive tests cover uncontended registration, definition promotion,
  sorted local catalog update, filtered history, attachment-preserving task
  replacement, runtime-owned result status, bounded UTF-8 result fields,
  cancellation propagation, and timeouts. No test covers partial sync
  registration, same-name identity mismatch, catalog clear/reload/readiness,
  omitted mode through real `agent_tool`, definition tool/memory enforcement,
  builtin attachment forwarding, error-category projection, or a pre-existing
  model-reported artifact reaching TaskRuntime acceptance.
- Log/artifact: this report

## Deviations

This reports assertion coverage, not test pass/fail status. Executable
validation was prohibited.

## Conclusion

The claim failed. Existing tests explain strong local mechanics but do not
guard the cross-layer findings F-SUB-01-P1-01 through P2-05.

## Follow-Up

Use the missing cases as the fix acceptance matrix; avoid tests of disconnected
helpers only.

## Correction (2026-08-12)

The Conclusion used the draft range suffix `P2-05`. The final task report has
eight findings, `F-SUB-01-P1-01` through `F-SUB-01-P1-06` and
`F-SUB-01-P2-01` through `F-SUB-01-P2-02`. The coverage conclusion and status
are unchanged.
