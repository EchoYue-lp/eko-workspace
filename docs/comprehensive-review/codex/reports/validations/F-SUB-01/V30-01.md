# F-SUB-01 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at acceptance

## Claim

The eight delegated F-SUB-01 findings are independently supported by current
framework definitions and live registration/dispatch/result paths.

## Method

Static primary sampling of registry write/read order, per-Agent dispatch catalog,
`AgentDispatchTool` mode/history/message construction, executor cancellation and
terminal conversion, result artifact hydration, EKO required-artifact acceptance,
and production callers of `ContextBuilder`/`SubagentOutput`. No build, test,
Cargo, rustc, or fixture was run.

## Expected Result

One role identity should bind definition and executable atomically; model-visible
roles should be executable; declared mode/context/capabilities should control the
dispatch; attachments and terminal categories should survive; artifact producer
facts should come from runtime observation.

## Result

- Exit code: 0
- Duration: under one second per bounded static inspection
- Summary: instances/definitions/factories are independently locked and
  replaceable; catalog copies are upsert-only and may advertise definition-only
  roles; builtin dispatch defaults/forces mode and snapshots history twice;
  `DispatchRequest.message` is always None; executor typed errors reach a generic
  permanent ToolResult adapter; readable model-reported files are assigned the
  current producer ID and satisfy EKO's artifact predicate; multiple definition
  fields and the alternate context/output API lack production consumers.
- Log/artifact: this report and task-report anchors

## Deviations

External framework consumers are not enumerable from this workspace, so public
API cleanup remains a synthesis/implementation decision. The defect is the
current misleading/disconnected contract, not automatic authorization to delete
a reasonable reusable API.

## Conclusion

F-SUB-01 is source-conclusive and complete with six P1 and two P2 findings.

## Follow-Up

F-SUB-02, F-MAG-01, A-SUB-01, and Subagent/task acceptance synthesis.
