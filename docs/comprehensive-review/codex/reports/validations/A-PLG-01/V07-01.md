# A-PLG-01 V07-01: EKO Skill enable/disable persistence

> Status: failed - finding
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Expected

An EKO command that claims a Skill was enabled or disabled for the next session
must surface failure to save `enabled-skills.json`. Runtime and durable outcome
must be distinguishable.

## Procedure

```text
nl -ba echo-agent-cli/src/tauri/commands/panels.rs | sed -n '350,680p'
nl -ba echo-agent-cli/echo-agent-app-core/src/skills_hub/enabled_skills.rs | sed -n '1,330p'
nl -ba echo-agent-cli/echo-agent-app-core/src/skills_hub/registry.rs | sed -n '1,240p'
rg -n 'clear.*skill|unregister.*skill|replace.*skill|register_descriptor|skill_descriptors' echo-agent/src echo-agent/echo-* --glob '*.rs'
```

All commands exited 0.

## Observed

`persist_skill_enabled` returns `()`, replaces load errors with a default
configuration, and logs save errors. `enable_skill` loads the Skill live before
this best-effort persistence and always returns success. `disable_skill` cannot
unload the current runtime, claims `requires_restart: true`, and returns success
even if the disable intent was not saved, so restart can re-enable it.

This supports `A-PLG-01-P1-04`. The framework's two activation authorities and
generic unload reachability remain `F-SKL-01-P1-01`/`P2-05`; the new issue is
EKO's false durable command outcome.
