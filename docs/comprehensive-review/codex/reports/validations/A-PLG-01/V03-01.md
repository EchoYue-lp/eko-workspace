# A-PLG-01 V03-01: Pool and mode projection

> Status: failed - finding
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Expected

A plugin capability advertised as active should affect primary, existing pool,
and future pool Agents consistently. Shared registries may provide that property
without per-Agent replay; per-Agent prompt projections require explicit pool
propagation.

## Procedure

```text
rg -n 'from_runtime|skill_registry|hook_registry|subagent_registry|mcp_manager|plugin|create_agent' echo-agent-cli/echo-agent-app-core/src/agent_pool.rs
nl -ba echo-agent-cli/echo-agent-app-core/src/agent_pool.rs | sed -n '214,285p'
nl -ba echo-agent-cli/echo-agent-app-core/src/agent_pool.rs | sed -n '824,980p'
rg -n 'replace_system_context_projection|OUTPUT_STYLE_PROJECTION' echo-agent echo-agent-cli --glob '*.rs'
```

All commands exited 0.

## Observed

- Pool Agents share the primary ToolManager and HookRegistry, so framework Tool,
  MCP tool, and Hook registration is live across those Agents. This prevents an
  overly broad stale-pool conclusion.
- Skill descriptors are a pool snapshot with an explicit refresh operation.
- The plugin output-style projection is not shared. `activate_output_style` and
  reload reconciliation call `replace_system_context_projection` only through
  `PluginRuntimeService.agent_handle`, the primary Agent.
- `AgentPool::create_agent` has no plugin preference/projection input, and no
  plugin runtime method iterates existing pool Agents.
- GUI conversations, channels, scheduled work, and background tasks acquire
  pooled Agents on real paths.

## Deviation

The service and GUI can report an active persisted output style while existing
and newly created pooled conversations omit its instructions. This supports
`A-PLG-01-P1-02`. It does not imply themes require Agent projection; themes are
surface rendering policy.
