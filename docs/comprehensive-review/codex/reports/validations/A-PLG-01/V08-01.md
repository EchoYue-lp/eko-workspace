# A-PLG-01 V08-01: Surface registration and command convergence

> Status: passed with product deviations
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Procedure

```text
nl -ba echo-agent-cli/src/tauri/commands/plugins.rs | sed -n '1,340p'
nl -ba echo-agent-cli/src/cli/cmd_impls/plugins.rs | sed -n '1,470p'
rg -n 'list_plugins|get_plugin|install_plugin|uninstall_plugin|enable_plugin|disable_plugin|configure_plugin|reload_plugins|list_plugin_themes|activate_plugin_theme|list_plugin_output_styles|activate_plugin_output_style' echo-agent-cli/web-frontend/src echo-agent-cli/src/tauri/mod.rs --glob '*.ts' --glob '*.tsx' --glob '*.rs'
```

All commands exited 0.

## Result

Tauri commands are registered, frontend endpoints call their exact names, and
CLI/TUI paths receive the same runtime service rather than constructing a new
registry. Install, uninstall, enable, disable, configure, reload, theme, output
style, scaffold, and validate converge on `PluginRuntimeService`.

The local personal-assistant boundary is respected: user-selected local plugin
paths receive existence/manifest validation without an automated-action
permission gate. Findings V03/V04/V07 concern propagation and truthful terminal
outcomes, not missing command registration or local extension authorization.
