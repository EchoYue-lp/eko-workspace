# A-PLG-01 V04-01: Failed mutation compensation

> Status: failed - finding
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Expected

Enable, disable, configure, and install should either commit durable plus live
state together or return an explicit indeterminate/rollback-failed outcome.

## Procedure

```text
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs | sed -n '201,410p'
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs | sed -n '551,910p'
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs | sed -n '993,1046p'
```

All commands exited 0.

## Observed

Framework registry mutation persists before `apply_candidate`. On apply failure,
EKO invokes `restore_enabled_state`, `restore_plugin_config`, or
`rollback_install`. Each helper starts with a new scan, ignores scan failure,
logs mutation/cleanup failure, returns no receipt, and is not appended to the
command error.

Therefore the live generation can remain old while durable state retains the
failed candidate. The user sees only the activation error and cannot know a
restart may retry that enabled/configured plugin or that an install artifact
remains. This supports `A-PLG-01-P1-01`.

The framework's staged-install and reversible-wiring deficits are already
`F-PLG-01-P1-03`/`P1-04`; this application finding is specifically the lossy EKO
compensation terminal contract.
