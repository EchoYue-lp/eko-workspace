# A-PLG-01 V09-01: Existing test coverage inventory

> Status: passed with gaps
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Procedure

```text
rg -n '#\[(tokio::test|test)\]|async fn [a-zA-Z0-9_]+\(|fn [a-zA-Z0-9_]+\(' echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs echo-agent-cli/echo-agent-app-core/src/config_watcher.rs echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/hook_event_dispatcher.rs echo-agent-cli/src/tauri/commands/plugins.rs echo-agent-cli/src/cli/cmd_impls/plugins.rs
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs | sed -n '1475,1884p'
```

Both commands exited 0. Tests were inspected, not executed.

## Covered By Existing Tests

- Real primary-Agent component load/disable/enable/uninstall.
- Malformed candidate rollback to previous primary components.
- Native lifecycle activation failure and uninstall shutdown.
- Scheduler/reload lock order.
- Preference restart restoration on the primary Agent.
- Dispatcher FIFO/backpressure/flush/shutdown in an unchanged registry.
- Watch target and filesystem event classification.

## Missing

- Compensation failure after persisted enable/config/install mutation.
- Existing and future pool Agents after output-style activate/reload/disable.
- A queued task event across user-hook or plugin-hook registry replacement.
- Read-only/corrupt `enabled-skills.json` enable and disable outcomes.
- Full surface mutation assertions and graceful process teardown.

This inventory supports regression-test recommendations only; it does not claim
the unexecuted suite passes at the reviewed commits.
