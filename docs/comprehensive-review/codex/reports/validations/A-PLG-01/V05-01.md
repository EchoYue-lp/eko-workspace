# A-PLG-01 V05-01: Reload, unload, and lifecycle ownership

> Status: passed with inherited deviations
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Procedure

```text
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs | sed -n '551,1285p'
nl -ba echo-agent/echo-core/src/plugin/lifecycle.rs | sed -n '1,260p'
rg -n 'shutdown_all|deactivate_all|PluginLifecycleManager' echo-agent echo-agent-cli --glob '*.rs'
```

All commands exited 0.

## Result

- Full reload is serialized by the service state mutex and uses one lock order
  with scheduler binding.
- Product monitors have explicit remove/add compensation.
- Candidate and previous LSP managers are explicitly shut down on replacement
  and most failure branches.
- Subagents, source-tagged Skills, plugin Hooks, and MCP receipts have explicit
  unload calls before replacement.
- Native lifecycle callbacks deactivate before rewire, reactivate after commit,
  unregister on uninstall, and synchronously shut down from manager Drop.
- Stdio LSP children use `kill_on_drop(true)`, so last-owner drop is a process
  safety fallback even though graceful application teardown is not explicit.

Uninstall persistence/cleanup ambiguity and callback partial-effect state are
canonical `F-PLG-01-P1-07` and `F-PLG-01-P2-08`. No additional EKO finding is
created here. Exact graceful shutdown timing remains a future validation.
