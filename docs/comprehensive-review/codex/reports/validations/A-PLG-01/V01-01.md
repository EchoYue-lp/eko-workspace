# A-PLG-01 V01-01: Definition, authority, and layering map

> Status: passed with inherited deviations
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Expected

EKO should have one application owner for product plugin policy while generic
manifest, dependency, source ownership, component wiring, and lifecycle
mechanics remain in the framework. SkillsHub may index EKO-installed content,
but must not become a second runtime Skill registry.

## Procedure

```text
rg --files echo-agent-cli | rg '(^|/)(plugin|plugins|skill|skills|hook|hooks|config_watcher|extension)'
rg -n --glob '*.rs' --glob '*.ts' --glob '*.tsx' 'PluginRuntimeService|plugin_runtime|SkillsHub|skills_hub|HookEventDispatcher|hook_event_dispatcher|HookConfigLoader|config_watcher|activate_plugin|deactivate_plugin|reload_plugin|unload_plugin|install_plugin|uninstall_plugin' echo-agent-cli
rg -n '^\s*(pub\s+)?(async\s+)?fn |^\s*pub\s+(struct|enum|trait) |^impl ' echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs
```

All commands exited 0.

## Result

- `PluginRuntimeService` is the single live EKO plugin mutation owner.
- `PluginRegistry`, `PluginIntegrator`, `PluginLifecycleManager`, framework
  Skill/Hook/MCP ownership, and dependency resolution remain framework-owned.
- EKO owns Subagent definitions, LSP process policy, scheduled monitors, themes,
  output styles, local skill catalog/install state, GUI/TUI projection, and
  product command responses.
- Tauri and CLI plugin commands delegate to the shared service; no second live
  EKO plugin registry mutation path was found.
- Framework identity/transaction/lifecycle defects remain canonical under
  `F-PLG-01`; Skill activation authority defects remain canonical under
  `F-SKL-01`.

## Boundary Gate

| Classification | Result |
|---|---|
| Generic mechanism | Framework manifest/dependency/source receipt/reversible registration/lifecycle. |
| EKO policy | Enabled local roots, Subagent/LSP/monitor/theme/style construction, UI and persistence responses. |
| Adapter | Prepare product files, bracket framework transaction, project results to surfaces. |
| Duplicate search | Names, traits, stores, commands, registration and live callers were searched across both repositories. |
| Migration deletion | Do not add another host; strengthen the existing service and delete branch-local compensation once one transaction owns it. |

This validation supports the layering map, not correctness of each lifecycle
transition.
