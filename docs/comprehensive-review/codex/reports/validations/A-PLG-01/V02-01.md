# A-PLG-01 V02-01: Prepare, activation, and live component reachability

> Status: passed with framework dependencies
> Executor: Codex review subagent
> Date: 2026-08-13
> Commits: `echo-agent@3aa79299`, `echo-agent-cli@b3b2e81f`

## Procedure

Static line trace:

```text
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs | sed -n '1,1050p'
nl -ba echo-agent-cli/echo-agent-app-core/src/plugin_components.rs | sed -n '1,520p'
nl -ba echo-agent-cli/echo-agent-app-core/src/runtime.rs | sed -n '240,410p'
```

All commands exited 0.

## Result

The production path is:

```text
AgentRuntime::bootstrap
  -> PluginRuntimeService::new -> reload
  -> scan Registry -> resolve dependencies
  -> prepare_application_components (no live mutation)
  -> collision check + replacement LSP preparation
  -> lifecycle deactivate + monitor replace
  -> one AgentHandle write: old framework/EKO components out, candidate in
  -> LSP swap -> lifecycle activate -> preference projection -> events
```

Subagents are parsed into framework definitions/factories and registered in the
live Subagent registry. Monitors reach the shared SchedulerRunner. LSP configs
reach a replacement LspManager. Themes and output styles remain application
projections. Skills/hooks/MCP go through the framework integrator.

Preparation rejects duplicate EKO component names before live mutation. The
remaining manifest cardinality, source identity, partial framework wiring, and
MCP ownership defects are inherited from `F-PLG-01-P1-01` through `P1-05`, not
renumbered here.

This validation supports definition-to-live reachability for the primary
runtime. Pool projection is assessed separately in V03.
