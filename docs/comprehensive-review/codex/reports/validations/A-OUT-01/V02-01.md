# A-OUT-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

Each output/export implementation has a registered live caller and equivalent user-facing entry on GUI, TUI, CLI and channel surfaces.

## Method

```text
Trace:
- conversation export definition -> Tauri registration -> frontend endpoint -> component caller;
- systematic-review export -> CLI/channel command, Tauri command and React component;
- analysis output collection -> shared CLI/TUI/channel command and Tauri/React panel;
- generic OutputRenderer/OutputFormat -> production caller.
Inventory the TUI SlashCommand enum and concrete handlers rather than inferring parity.
```

## Expected Result

Every declared capability reaches its intended surfaces through thin presentation adapters.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: analysis has real GUI and shared CLI/TUI/channel paths. Systematic-review export has GUI, CLI and channel paths, while TUI has no `/papers` command; its generic Agent can only reach the registered research Tool indirectly. The GUI reports review paths as inert text. Conversation export is registered in Tauri and wrapped in `endpoints.ts` but has no frontend consumer and no TUI/CLI/channel entry. Generic `OutputFormat::format_response` has no production caller.
- Log/artifact: this report

## Deviations

This validation establishes registered workflow reachability, not whether a model will choose the indirect research Tool from natural language.

## Conclusion

The claim fails. It supports `A-OUT-01-P1-01`, `A-OUT-01-P1-07` and `A-OUT-01-P2-01`.

## Follow-Up

Add a common export scenario to the future multi-surface parity suite after the adapters are unified.
