# A-OUT-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

Immutable analysis run records continue to resolve every recorded artifact after later runs.

## Method

```text
Trace run_analysis ordering from run ID creation through clear_generated_outputs,
Tool execution, collect_outputs and runs/<run-id>.json persistence. Compare each
AnalysisOutputArtifact path with the directory removed by the next run.
```

## Expected Result

Artifacts are stored under a run/revision identity or copied into immutable storage before a later run mutates the latest-output workspace.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: each run record is written under a unique run ID, but its output entries point to shared `environment.json`, `result.json` and `outputs/` paths. The next run deletes those paths before execution and recreates them. Old records retain hashes and metadata but no bytes, so their paths become missing or identify newer bytes.
- Log/artifact: this report

## Deviations

The existing failed-rerun test treats removal of previous output as preventing inheritance; it does not verify historical run retrieval.

## Conclusion

The claim fails and supports `A-OUT-01-P1-05`.

## Follow-Up

Run two successful and one failed rerun, then resolve and hash every artifact through each historical run record.
