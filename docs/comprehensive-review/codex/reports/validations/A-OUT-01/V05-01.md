# A-OUT-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

Missing, failing or stalled PDF/DOCX converters produce bounded, typed and complete batch outcomes without hidden partial effects.

## Method

```text
Inspect renderer discovery, --version probing, PDF-engine probing, command execution,
stderr capture, export_all_review_formats sequencing and Tauri/CLI/channel call sites.
Derive the state table for: missing renderer, missing PDF engine, spawn failure,
non-zero exit, missing output, hang, and a late failure during "all".
```

## Expected Result

Each subprocess has timeout/cancellation and cleanup. A batch result records every requested format as produced, skipped or failed, including all paths already written.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: individual missing-renderer/non-zero errors preserve useful causes and stderr. However, discovery and conversion use synchronous `status()`/`output()` with no timeout or cancellation. `all` silently omits DOCX/PDF when discovery says unavailable. It writes formats sequentially, so a late conversion failure returns one error after earlier artifacts were already overwritten, with no partial-result manifest.
- Log/artifact: this report

## Deviations

No converter process was launched per review instruction. Exact hang duration and executor starvation remain dynamic uncertainty.

## Conclusion

The claim fails and supports `A-OUT-01-P1-03`.

## Follow-Up

Run separate missing, non-zero, no-output, hanging, cancellation and late-batch-failure fixtures after implementation.
