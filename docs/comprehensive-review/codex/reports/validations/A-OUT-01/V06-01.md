# A-OUT-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

Human-readable review and bibliography renderers retain evidence/reference content and generate stable, unique Unicode-safe identities.

## Method

```text
Compare ReviewRecord/ReviewDocument, SourceRecord and EvidenceRecord fields with
render_review_markdown, render_evidence_csv, render_bibtex and render_ris.
Inspect citation_key normalization and field escaping for duplicate author/year,
non-ASCII names, newlines, braces and backslashes. Inventory matching tests.
```

## Expected Result

Human-readable reports include the cited source bibliography and material evidence facts; bibliography keys are non-empty, unique and stable, with syntax-safe values.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: Markdown/PDF/DOCX use an evidence table that omits excerpt, effect, limitations and medical evidence fields present in CSV and includes no reference list. Source loading silently drops unreadable linked source records while only the audit reports the discrepancy. BibTeX keys are only ASCII-filtered `last-name + year`, so same-author/year records collide and non-ASCII-only names can collapse to a year-only or empty key; values are interpolated without BibTeX escaping. RIS likewise permits embedded newlines to become control-looking lines.
- Log/artifact: this report

## Deviations

No parser/render fixture ran. The Unicode and duplicate-key failure is source-conclusive; parser-specific failure messages are not claimed.

## Conclusion

The claim fails and supports `A-OUT-01-P1-04`.

## Follow-Up

Use multilingual duplicate-author/year fixtures and parse every generated bibliography/report with independent consumers.
