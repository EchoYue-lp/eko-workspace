# A-OUT-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

Every systematic-review export artifact has stable identity and enough lineage to bind its bytes to the exact review revision and renderer.

## Method

```text
Trace ReviewDocument.revision through export_review, ReviewExportArtifact and the
fixed output path. Compare the artifact contract with the analysis artifact's hash
fields and inspect repeated-export overwrite behavior.
```

## Expected Result

An artifact identifies its source review revision, byte hash, generation time and converter identity/version; repeated exports do not silently change the bytes behind an old identity.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: `ReviewExportArtifact` contains only review ID, format, relative path, byte count and audit. `export_review` always overwrites `reports/systematic-review.<ext>` and does not retain the review revision, output hash, generation time or external renderer provenance. A prior response can therefore name the same path after its bytes have changed.
- Log/artifact: this report

## Deviations

No filesystem mutation was performed; overwrite follows directly from the deterministic path and `atomic_write` call.

## Conclusion

The claim fails and supports `A-OUT-01-P1-02`.

## Follow-Up

Validate immutable revision-addressed exports and SHA-256 equality across Markdown and derived PDF/DOCX artifacts.
