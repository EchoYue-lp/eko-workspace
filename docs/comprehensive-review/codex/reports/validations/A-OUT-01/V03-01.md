# A-OUT-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

Conversation Markdown export is a lossless projection of the canonical stored transcript facts.

## Method

```text
Compare StoredMessage fields consumed by conversation persistence/restore with fields
read by the registered Tauri export_conversation command. Trace the returned value to
the TypeScript endpoint and search for a frontend caller.
```

## Expected Result

The export retains message identity/order/time, multimodal attachment metadata, Tool calls and Tool results, or explicitly links a complete artifact containing those facts.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: the exporter emits only each message's `role` and optional text `content`. It ignores `tool_calls_json`, `tool_result_json`, `attachments_json`, message ID and timestamp. Tool-only assistant messages therefore become empty headings and structured attachment/artifact facts disappear. The command returns content in memory and has no frontend consumer that writes or downloads it.
- Log/artifact: this report

## Deviations

Canonical transcript persistence correctness is owned by A-STATE-01; this validation only inspects the downstream export projection.

## Conclusion

The claim fails and supports `A-OUT-01-P1-01`. It does not duplicate A-STATE-01's persistence findings.

## Follow-Up

Regression should export a canonical multimodal turn with Tool call/result, Unicode content, identity and timestamp, then import/inspect field equality.
