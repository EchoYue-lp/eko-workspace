# A-OUT-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty and excluded; application clean

## Claim

EKO has one reachable typed output-format authority whose selected value controls the response or export renderer.

## Method

```text
Search both repositories by definition and behavior for OutputFormat, OutputConfig,
output_format, ResearchOutputFormat, format_response, LatexExporter, --output,
and renderer/export call sites. Classify each definition as constructed, read and
runtime-reachable rather than accepting its declaration as evidence.
```

## Expected Result

One typed format selection reaches a registered renderer from every supported entry path, or unsupported representations are absent.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: the application contains an `OutputFormat`/`OutputConfig` facade whose `format_response` is test-only, a free-form `Profile.output_format` with no production read, and `ResearchOutputFormat::Latex` whose value is ignored by `BackgroundTaskKind::to_prompt`. The CLI has no matching `--output` flag. The standalone `LatexExporter` is not joined to the research format enum.
- Log/artifact: this report

## Deviations

No dynamic command was run. The absence conclusion is based on repository-wide symbol and behavior searches at the recorded commits.

## Conclusion

The claim fails and supports `A-OUT-01-P2-01`: EKO exposes multiple inert or disconnected format contracts rather than one live registry.

## Follow-Up

After convergence, statically retrace the selected format from configuration/command to bytes, then run a separate format matrix.
