# F-HITL-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> Source commits: `echo-agent` `9b0e0faf74d35c9a432370b923acabfbb5f32d63`; `echo-agent-cli` `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Once, Session, and SessionAllTools retain their documented identity and
argument scope.

## Method

Traced HumanLoopResponse scopes through both provider adapters,
PermissionResponse rule updates, `infer_scope_from_updates`, RuleRegistry, and
SessionApprovalCache lookup/write keys.

## Result

- Exit code: 0
- `ApprovalScope::Session` is documented as same tool plus same arguments.
  DynProviderHandler instead creates an allow rule for only the tool name.
- `infer_scope_from_updates` converts any session allow rule to
  SessionAllTools, so the cache also ignores arguments. ModifiedArgs with scope
  Session records the original input before execution.
- SessionApprovalCache has no session/conversation/agent identity; sharing one
  PermissionService or preserving cache across transport swaps can reuse an
  approval in a different logical session.

## Conclusion

Supports F-HITL-01-P1-02 and P1-03. Scope and session identity must be explicit
parts of one typed approval key; a modified call must never authorize the
unmodified input.
