# F-HITL-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> Source commits: `echo-agent` `9b0e0faf74d35c9a432370b923acabfbb5f32d63`; `echo-agent-cli` `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

User-modified approved arguments are the arguments actually validated and
executed on the canonical tool path.

## Method

Compared PermissionResponse -> PermissionService -> snapshot
`check_tool_approval` -> PermissionStage -> ExecuteStage field flow, then
searched every `take_modified_args` consumer.

## Result

- Exit code: 0
- PermissionService stores updated input in one shared `last_modified_args`
  slot and returns plain `PermissionDecision::Allow`.
- The canonical snapshot helper maps Allow to `Ok(None)` and never consumes the
  slot. PermissionStage therefore leaves the parsed original params in place and
  ExecuteStage executes them.
- Only the older ReactAgent helper calls `take_modified_args`. Even on that path,
  modification occurs after ParseValidate and is not schema/protected-path
  revalidated; the general post-mutation validation gap is already owned by
  F-EXT-01.

## Conclusion

Supports F-HITL-01-P0-01. Approval must return a call-scoped typed decision with
the final arguments, followed by the same validation/protection stages.
