# A-DOM-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded; CLI clean.

## Claim

An immutable analysis run record must continue to resolve the exact artifact bytes and hashes that it records after later reruns.

## Method

```text
Inspect AnalysisRunRecord/AnalysisOutputArtifact at analysis.rs:160-190.
Trace run_analysis at analysis.rs:392-481 into clear_generated_outputs at 750-769 and collect_outputs at 772-804.
rg -n "AnalysisRunRecord|list_analysis_runs|get_analysis_run|runs/|latest_run" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/web-frontend/src
Compare echo-agent-cli/docs/MASTER-PLAN.md:79-86.
```

## Expected Result

Each run owns immutable artifact bytes (or a content-addressed reference with retention); history is queryable and later execution cannot invalidate it.

## Result

- Exit code: 0 for inspection; behavioral expectation failed
- Duration: static inspection
- Summary: each `runs/<run-id>.json` stores paths and hashes for shared `analysis/<id>/outputs`, `result.json`, and `environment.json`. Before every new run, those shared paths are deleted. Old JSON remains but its artifact references become missing or point to new bytes. Only `latest-run.json` is loaded/exposed; no analysis-run history API was found.
- Log/artifact: source anchors above

## Deviations

No dynamic two-run fixture was executed. Existing tests that clear previous outputs do not assert preservation of the previous run's evidence.

## Conclusion

Supports `A-DOM-01-P0-01`: metadata immutability is presented as evidence immutability, but later reruns delete the historical evidence bytes.

## Follow-Up

Use per-run or content-addressed artifact promotion and add list/get/open history fixtures.
