# A-DOM-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded; CLI clean.

## Claim

A partial Europe PMC refresh must preserve previously persisted enrichment dimensions whose provider calls failed.

## Method

```text
Inspect research_connectors.rs:188-255.
Trace citations/references/entities/full-text endpoint failures into the newly constructed EuropePmcSupplement and research.rs::save_europe_pmc_supplement.
Search connector tests for prior supplement + one endpoint failure and for retained full_text_path.
```

## Expected Result

Successful dimensions update; failed dimensions retain prior values and receive per-field status/warnings, unless the user explicitly requests reset.

## Result

- Exit code: 0 for inspection; behavioral expectation failed
- Duration: static inspection
- Summary: every provider failure is converted to an empty vector or `None`; a fresh supplement is then saved over the existing one and the operation returns success with warnings. A transient failure can erase prior citation IDs, references, entities, and the recorded full-text path.
- Log/artifact: source anchors above

## Deviations

The full-text file may remain on disk, but its record link is still lost. No partial-failure preservation test was found.

## Conclusion

Supports `A-DOM-01-P0-02`: connector retry can erase previously persisted local enrichment metadata.

## Follow-Up

Merge endpoint results dimension-by-dimension and test every one-fails/others-succeed case.
