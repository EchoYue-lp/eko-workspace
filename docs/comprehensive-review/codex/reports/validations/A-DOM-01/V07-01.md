# A-DOM-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded; CLI clean.

## Claim

When the installed auto-ingest adapter cannot persist a successful provider result, the Tool outcome must expose that partial failure to the Agent.

## Method

```text
Trace runtime.rs:283-289 -> install_auto_ingest_tools -> AutoIngestResearchTool::execute_with_context at research_connectors.rs:306-336 -> ingest_tool_output.
Inspect ResearchLibraryTool::search_sources at research_tool.rs:125-131 as the explicit alternative.
Inspect research_connectors tests at 593 onward for persistence-failure/malformed-output coverage.
```

## Expected Result

The wrapper returns typed persistence status/warnings or fails a contract that promises ingestion; it never returns indistinguishable search success after local persistence failure.

## Result

- Exit code: 0 for inspection; behavioral expectation failed
- Duration: static inspection
- Summary: the wrapper logs ingestion errors and returns the original successful ToolResult unchanged. The Agent cannot observe malformed provider JSON, local I/O failure, or record conflict. Only the explicit `research_library.search_sources` path couples provider and persistence errors. Existing tests cover idempotent success and ignored live smoke tests, not adapter failure.
- Log/artifact: source anchors above

## Deviations

This is EKO adapter behavior, not the generic framework Tool error contract already reviewed by F-EXT-03.

## Conclusion

Supports `A-DOM-01-P1-04`: a live automatic provenance path can silently lose evidence while reporting success.

## Follow-Up

Choose one authoritative search-and-ingest contract. Remove the best-effort wrapper if the explicit transactional Tool replaces it.
