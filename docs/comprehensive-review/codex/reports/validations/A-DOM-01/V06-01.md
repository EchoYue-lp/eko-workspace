# A-DOM-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded; CLI clean.

## Claim

A formal systematic-review export must reject or clearly downgrade structurally contradictory review state.

## Method

```text
Inspect research.rs save_review/get_review/validate_review at 892-947 and 1748-1773.
Inspect prisma_flow at 965-1007, audit_review at 1010-1120, and export_review at 1123-1168.
Inspect ReviewWorkbench.tsx:35-48 for frontend PRISMA derivation.
Search tests for duplicate stage decisions, unknown assessment IDs, impossible PRISMA counts, and export with audit errors.
```

## Expected Result

One canonical semantic validator enforces source membership, unique/staged screening decisions, assessment referential integrity, and internally possible PRISMA values; formal export cannot silently bless errors.

## Result

- Exit code: 0 for inspection; behavioral expectation failed
- Duration: static inspection
- Summary: validation checks identity/title and medical PICO/PECO only, then verifies top-level source IDs and guideline kind. It does not reject duplicate decisions, decisions or bias assessments outside the review/source library, invalid stage progressions, duplicate/empty GRADE outcomes, or impossible PRISMA counts. `prisma_flow` counts raw rows, and the frontend duplicates the same calculation. `audit_review` checks only a narrower citation subset. `export_review` writes every format regardless of audit errors.
- Log/artifact: source anchors above

## Deviations

The workbench UI normally replaces one decision per `(source, stage)`, but Agent-facing `research_library.save_review` accepts an arbitrary full record and local files are user-editable. The invalid path is therefore reachable without treating the local user as an attacker.

## Conclusion

Supports `A-DOM-01-P1-03`: formal artifacts can present contradictory evidence-accounting state as a systematic review.

## Follow-Up

Create one backend semantic authority; frontend must render its computed flow rather than recalculate it.
