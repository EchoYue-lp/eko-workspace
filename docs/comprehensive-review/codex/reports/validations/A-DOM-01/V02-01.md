# A-DOM-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded through `git show HEAD`; CLI clean.

## Claim

The exploratory/formal-analysis boundary is explicit and the formal path is registered and reachable through persisted scripts.

## Method

```text
git -C echo-agent show HEAD:echo-tools/src/statistics.rs
git -C echo-agent show HEAD:echo-tools/src/registry.rs
rg -n "exploratory_statistics|formal inference|SciPy|statsmodels|run_code|script_path" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/docs
Trace AnalysisPanel/Tauri and CLI/channel -> analysis.rs::run_analysis_with_agent -> ToolManager::execute_tool_with_context("run_code").
```

## Expected Result

Exploratory statistics never claim inference; formal work requires a saved Python/R script and the canonical Tool execution path.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: framework output declares `analysis_kind=exploratory_descriptive`, `inference=false`, and excludes p-values/confidence intervals. Registry tests assert obsolete inference tools are absent. EKO workspace prompts and analyst definition require saved SciPy/statsmodels/R scripts through `run_code.script_path`. Analysis GUI, CLI, and channel reach the same application service and canonical ToolManager.
- Log/artifact: source anchors in task report

## Deviations

The inherited `A-TOOL-01-P1-01` finding currently prevents writer Subagents, including analyst, from obtaining the required write/shell capabilities. That impact is not renumbered here. Primary/workbench execution remains separately reachable.

## Conclusion

The conceptual and primary-path formal/exploratory boundary passes. It does not establish that input lineage or artifact retention is reliable.

## Follow-Up

Fix `A-TOOL-01-P1-01` without reintroducing framework inference approximations.
