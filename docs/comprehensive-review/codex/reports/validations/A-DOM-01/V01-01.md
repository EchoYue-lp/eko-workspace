# A-DOM-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded through `git show HEAD`; CLI clean.

## Claim

Generic data/research mechanisms remain in the framework while EKO-specific persistence, formal-analysis policy, workbench state, and exports remain in the application, without a second generic execution authority.

## Method

```text
rg -n "analysis|research|provenance|exploratory_statistics|run_code|ResearchLibraryTool|AnalysisRunRecord" echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src echo-agent-cli/web-frontend/src
git -C echo-agent grep -n "exploratory_statistics\|run_code\|Scholarly" HEAD -- echo-tools src
Inspect echo-agent-cli/echo-agent-app-core/src/{analysis.rs,research.rs,research_connectors.rs,research_tool.rs,runtime.rs,workspace_routing.rs}.
Inspect framework HEAD echo-tools/src/{statistics.rs,registry.rs} and generic research tools.
```

## Expected Result

The framework owns reusable clients/tools and EKO owns file layout, review policy, and UI adapters; formal execution reuses the canonical `run_code` Tool.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: framework HEAD exposes provider-neutral scholarly/search clients and `exploratory_statistics`; EKO owns `analysis/`, `research/`, review protocol, citation audit, GUI/CLI/channel projections, and ResearchLibraryTool. `run_analysis_with_agent` obtains the primary ToolManager and invokes its registered `run_code`; no application-side code runner was found.
- Log/artifact: source anchors summarized in the task report

## Deviations

Framework dirty worktree contents were not reviewed. Framework API absence from this CLI was not treated as dead-code evidence.

## Conclusion

The repository boundary is appropriate: generic mechanism in framework, EKO policy/state in application, thin adapters at Tool and UI edges. This does not prove correctness of the application contracts, which later validations challenge.

## Follow-Up

Retain this layering when fixing findings; do not move EKO review schemas or file layout into `echo-agent`.
