# A-DOM-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty excluded through `git show HEAD`; CLI clean.

## Claim

TaskRuntime data Subagent outputs must map to a durable EKO analysis/workbench artifact with a resolvable identity and cleanup lifecycle.

## Method

```text
Inspect analyst.md and data-shaper.md frontmatter/prompts.
Inspect profiles.rs:97-135 and background.rs:191 onward for live routing.
Inspect worktree.rs:1369-1467 and infra.rs:418-426.
git -C echo-agent show HEAD:src/agent/subagent/executor.rs | inspect lines 1620-1636, 1719-1735, 1857-1880
git -C echo-agent show HEAD:src/agent/subagent/workspace.rs
```

## Expected Result

An isolated Subagent result identifies durable artifact paths or promotes them into the canonical workspace workbench; downstream Subagents can resolve them and temporary workspaces have bounded cleanup.

## Result

- Exit code: 0 for inspection; behavioral expectation failed
- Duration: static inspection
- Summary: data implementations route to `analyst`, whose `workspace:true` receives a new OS temp directory. EKO calls `TempDir::keep`, so it is never automatically cleaned. Finalization emits only non-recursive basenames, not the temp-root path or structured artifact identities; the framework merely appends that text to the Subagent output. A later Subagent receives a different temp root, and `analysis/<id>/` created there is invisible to the main workspace workbench.
- Log/artifact: source anchors above

## Deviations

The inherited `A-TOOL-01-P1-01` currently blocks the analyst's required writer tools. This validation addresses the independent artifact-mapping defect that remains after that blocker is fixed.

## Conclusion

Supports `A-DOM-01-P1-02`: isolated output avoids collisions but does not create a reliable handoff or durable artifact authority.

## Follow-Up

Add an application promotion/manifest boundary and bounded cleanup; do not move EKO workbench policy into the framework.
