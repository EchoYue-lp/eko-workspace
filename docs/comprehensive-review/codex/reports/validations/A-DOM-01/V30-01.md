# A-DOM-01 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer

## Claim

All A-DOM-01 finding families can be independently reconstructed from the
current clean CLI source after excluding the delegated review's disclosed
non-dependency report exposure.

## Method

Do not use the accidentally opened reports. Starting from the A-DOM task card,
sample analysis run/output identity, missing-input fingerprints, Europe PMC
partial failure, automatic ingestion, review save/validation/audit/export,
provenance merge/projection, data workspace creation/finalization, and app
registration. Recount delegated validations/findings and verify CLI status.

## Expected Result

The two P0, four P1, and one P2 claims follow directly from current source;
delegated evidence is structurally complete and the CLI remains clean.

## Result

- Exit code: 0
- Duration: static primary reconstruction
- P0-01: every analysis run clears shared outputs before execution while old run
  records retain hashes/paths to those bytes.
- P0-02: each Europe PMC endpoint failure becomes an empty/None dimension and a
  complete replacement supplement is saved with only warnings.
- P1-01: missing declared inputs are fingerprinted unavailable but do not stop
  execution; unchanged unavailable fingerprints produce no stale reason.
- P1-02: EkoDataWorkspaceFactory calls `TempDir::keep`, finalizes only sorted
  top-level basenames as text, and has no promotion/cleanup owner.
- P1-03: `validate_review` checks identity/title and narrow medical fields;
  frontend independently recomputes PRISMA and export proceeds with audit errors.
- P1-04: auto-ingest logs persistence errors and returns the original successful
  ToolResult unchanged.
- P2-01: provenance carries query/time but de-duplicates by provider+URL and is
  absent from ordinary paper/review projections sampled.
- Seven finding IDs and all delegated validation links are unique/present; CLI
  remains clean at `b3b2e81f...`.

## Deviations

No provider request, execution, test, build, fixture, or browser render ran.
Framework evidence was limited to committed blobs where needed.

## Conclusion

A-DOM-01 is independently primary-accepted despite the disclosed delegated
isolation incident.

## Follow-Up

Carry durable artifact identity into X-STA/X-TOL and formal-review provenance
into the application roadmap; retain framework public capability primitives.
