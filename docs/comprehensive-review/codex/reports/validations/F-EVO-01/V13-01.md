# F-EVO-01 / V13 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

Public eval/improve/evolution inputs cannot trigger panic, UTF-8 boundary failure,
or unchecked overflow on reachable production paths.

## Method

```text
Static focused search of production Rust in src/eval, src/improve and
src/evolution for unwrap/expect/panic/unreachable/todo, direct indexing/slicing,
lowercase-offset reuse, unchecked aggregates and duration casts. Classify tests
separately and inspect preconditions around every production hit.
```

## Expected Result

External/deserialized text uses character-safe operations; collection sizes and
numeric aggregates are validated/checked; dataset shape returns typed errors.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: invariant failed. RegressionSuite slices `run_id` by bytes
  (`eval/regression.rs:80`). Eval numeric fallback finds an offset in a lowercased
  string then slices the original string (`eval/runner.rs:724-729`). A singleton
  criteria group calls `usize::clamp(1, 0)` (`improve/loop.rs:92-96`). Memory
  merge revision counts, trajectory totals/tool counts/durations, and elapsed
  milliseconds use unchecked sums/casts (`evolution/review.rs:417-426`,
  `improve/trajectory.rs:264-285`, `eval/runner.rs:74-84`). Auto-memory truncation
  uses `floor_char_boundary`; PromptGenerator/trigger previews use `.chars()` and
  are not affected.
- Evidence: paths and anchors above.

## Deviations

Potentially safe fixed-ASCII delimiter slices and test-only unwrap/index uses were
not promoted to findings. Overflow reachability for duration/token aggregates is
lower than the direct panic paths but still violates the repository invariant.

## Conclusion

Supports F-EVO-01-P1-04 and the aggregate portions of P1-09/P2-03.

## Follow-Up

Add Unicode/case-folding/singleton/extreme-aggregate regressions with typed failure.
