# F-EVO-01 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The fourteen delegated eval/improve/evolution findings are independently
supported by the current clean committed source and preserve the framework/EKO
layering boundary.

## Method

Primary reviewer statically sampled:

- EvalCase fixture destination/cleanup, Agent invocation context and SWE-bench
  setup ordering;
- grader assertion identity/cardinality and trigger confusion metrics;
- ImprovementLoop split/iteration/factory/run-store behavior and report facade;
- Unicode/panic paths in split, regression run ID and ValueMatch extraction;
- ChangeLog lock/fsync/cache/record shape and mutation/audit ordering;
- candidate grouping/identity, draft path/write/Curator/audit sequencing;
- Skill patch source identity/concurrency and Skill merger Store/Curator/live CLI;
- memory merge/undo sequencing and Curator corrupt-state load/save;
- PromptGenerator error fallback and TrajectorySaver append/read/lineage fields;
- EKO evidence/review adapters versus reusable framework ownership.

The framework commit transition `9b0e0faf..3aa79299` was checked with a scoped
quiet diff over `src/eval`, `src/improve` and `src/evolution`; it returned empty.
No Cargo, rustc, test, build, fixture or network command was run.

## Expected Result

Every accepted defect is reachable through a public framework API or live EKO
adapter; destructive paths are prioritized by local data-loss impact, and EKO
product review/workspace policy is not moved into the framework.

## Result

- Exit code: 0 for static reads, diff and integrity checks.
- Eval fixture cleanup joins unchecked public case IDs before remove_dir_all;
  direct draft generation joins unchecked candidate names before SKILL.md write.
- The Eval Agent is invoked without the computed cwd, while SWE-bench repository
  preparation occurs after Agent execution. Grader/trigger facts lack exact
  identity/cardinality joins.
- The “improvement” facade repeats unchanged factories, ignores its own maximum
  and RunStore wiring, while singleton split and two Unicode paths can panic.
- ChangeLog has no rollback operation; its lock fallback and discarded sync
  error contradict complete durability. Candidate/draft, patch/merge, memory
  merge/undo and Curator corruption each expose the reported partial terminal
  states or false success.
- Prompt generation maps provider/malformed-output failure to unchanged success;
  trajectory appends have no shared lock/sync, malformed rows vanish and records
  omit model/provider/revision/attempt lineage needed for authoritative training.
- Proposal/review separation is directionally sound. EKO correctly owns
  workspace evidence and user interactions; framework mutation correctness
  remains framework responsibility.
- Fourteen finding IDs are exact and unique. Delegated validation links resolve.

## Deviations

Runtime failure injection and filesystem fixtures remain future regressions by
explicit user direction. P0 here means immediate local recursive delete/file
overwrite through public inputs, not a remote multi-tenant threat model.

## Conclusion

Passed. F-EVO-01-P0-01 through P0-02, P1-01 through P1-10 and P2-01 through
P2-02 are accepted. The task is promoted to `complete` at current commit.

## Follow-Up

A-EVO-01 should consume the typed proposal/source/revision/undo requirements
while keeping workspace/UI policy in EKO. Dynamic regression cases remain
deferred until implementation or explicit authorization.
