# F-EVO-01 / V07 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

The current evolution API clearly separates analysis/review proposals from
mutation entry points, allowing EKO to own product approval policy.

## Method

```text
Build a static authority matrix for BackgroundReviewer, MemoryReviewer,
ConflictDetector, SkillCandidateDetector, SkillDraftGenerator, MemoryMerger,
SkillPatcher, SkillMerger, Curator, Dreaming, and MemoryLayerManager. Trace EKO
proposal capture/accept/undo separately from framework mutation.
```

## Expected Result

Analysis APIs do not silently mutate semantic state; explicit mutators are named
and callable without embedding EKO UI/workspace policy.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: the principal separation is present. `MemoryReviewer::review` is
  analysis-only and returns deterministic proposals (`review.rs:503-670`).
  Background review is proposal-only by default and only optionally persists a
  high-confidence user preference through a supplied layer manager
  (`background_review.rs:84-113`, `:486-531`). EKO captures proposals in its
  workspace evidence inbox and owns accept/undo interactions. Draft generation,
  patching, merging, dreaming and layer methods are explicit mutators.
- Evidence: `echo-agent/src/evolution/review.rs:503-670`,
  `echo-agent/src/evolution/background_review.rs:1-6`, `:486-531`,
  `echo-agent-cli/echo-agent-app-core/src/evolution/evidence.rs:101-142`,
  `:500-769`

## Deviations

Passing this architectural claim does not establish transaction, audit, path, or
source-identity correctness. V08-V12 document those failures.

## Conclusion

The generic-framework versus EKO-product boundary is directionally correct and
should be preserved while fixing mutation primitives.

## Follow-Up

None.
