# F-EVO-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

LLM grading and trigger/regression metrics validate input cardinality and identity,
produce bounded deterministic scores, and remain UTF-8 safe.

## Method

```text
Inspect LlmGrader structured/fallback parsing, TriggerAccuracy single/multi-run
confusion matrices, RegressionSuite identity construction, numeric extraction,
and their existing tests. Evaluate duplicate/unknown/missing grader IDs, mismatched
case/result lengths, empty multi-run rows, wrong routing target, and Unicode IDs.
```

## Expected Result

Every expected assertion/case has exactly one matched result; invalid envelopes
fail closed; metrics remain finite/in-range; Unicode input cannot panic.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: invariant failed. Grader pass count accepts duplicate and unknown IDs,
  so pass rate can exceed 1.0 (`grader.rs:138-165`). Trigger evaluation zips and
  silently omits missing rows; routing to the wrong Subagent increments both TP
  and FP, preserving recall (`trigger.rs:61-105`). Multi-run scoring divides by
  an empty actual run vector and uses only the first run for the confusion matrix
  (`:116-145`). RegressionSuite slices a public UTF-8 run ID by byte offset
  (`regression.rs:79-87`). Numeric value extraction similarly maps offsets from a
  lowercased copy back into the original string by byte slice (`runner.rs:724-729`).
- Evidence: paths and anchors above.

## Deviations

No malformed dynamic envelopes were executed. Existing tests cover only nominal
grading and selected trigger cases, not these boundaries.

## Conclusion

Supports F-EVO-01-P1-02 and F-EVO-01-P1-04.

## Follow-Up

Add field/cardinality/finite-number validation and Unicode/mismatched-length tests.
