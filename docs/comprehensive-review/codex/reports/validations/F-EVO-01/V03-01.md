# F-EVO-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

Eval fixture setup is contained within the configured workspace and the evaluated
Agent executes against the prepared fixture before criteria are checked.

## Method

```text
Trace EvalCase.project_fixture and id through EvalRunner::setup_fixture, Agent
execution, criteria cwd, cleanup, and SweBench clone/checkout/patch/test order.
Check PathBuf::join behavior for absolute and parent-containing public IDs.
```

## Expected Result

Untrusted/malformed case identity cannot escape the workspace; setup failure is
typed; the Agent and criteria observe the same initialized repository.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: invariant failed. `workspace_root.join(case.id)` accepts absolute and
  parent-traversing IDs, then an existing destination is passed to
  `remove_dir_all` with its error discarded (`runner.rs:177-190`). The computed
  fixture cwd is never passed to `agent.execute(&case.task)` (`:58-67`) and is
  used only by criteria. SweBench clones/checks out the repository only inside
  criteria after Agent execution (`:333-438`). Constructor/setup failures also
  degrade to a usable-looking runner or root cwd.
- Evidence: `echo-agent/src/eval/mod.rs:66-89`,
  `echo-agent/src/eval/runner.rs:25-35`, `:51-108`, `:177-192`, `:333-438`

## Deviations

No filesystem fixture was executed by instruction. Path semantics and call order
are source-conclusive; platform-specific cleanup failure behavior was not tested.

## Conclusion

Supports F-EVO-01-P0-01 and F-EVO-01-P1-01. It does not establish cancellation
of already-started Agent/tool work at timeout.

## Follow-Up

Add contained-path and recording-Agent regression fixtures in the implementation
iteration.
