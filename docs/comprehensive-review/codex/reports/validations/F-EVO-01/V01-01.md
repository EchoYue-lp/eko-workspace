# F-EVO-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

Eval, improvement, and evolution have explicit public/export boundaries, and their
framework option value does not depend on EKO constructing each API.

## Method

```text
Statically inspect root Cargo features/docs metadata, lib.rs cfg exports, eval/
improve/evolution module exports, required-feature example targets, and symbol
references across echo-agent and echo-agent-cli. Reconcile with accepted
F-FEAT-01 rather than rerunning compilation.
```

No Cargo, rustc, test, build, fixture, or network operation was started.

## Expected Result

Feature gates match imports; reasonable public framework capabilities are retained
even when EKO has no current caller.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: `eval` and `improve` are empty root features at `Cargo.toml:95-96`,
  are in `full`, and gate `lib.rs:36-45`. `improve` alone exports trajectory
  support while eval-dependent analysis is internally gated. `demo50_eval` and
  `demo51_self_improvement` declare the matching required features at
  `Cargo.toml:304-309`. Evolution is an unconditional framework module and has
  substantial EKO callers, but EKO non-use of eval/improve does not make their
  offline framework APIs dead.
- Log/artifact: this report

## Deviations

Evolution is compile-unconditional rather than a named Cargo feature. This is a
packaging choice, not proof that its reusable API is invalid.

## Conclusion

The feature/export and independent framework-value claim passed. Behavioral
correctness is evaluated separately.

## Follow-Up

None.
