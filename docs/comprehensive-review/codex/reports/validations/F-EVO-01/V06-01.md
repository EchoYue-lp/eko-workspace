# F-EVO-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

Trajectory export is append-safe, reports corrupt data, retains enough source
identity for downstream review, and computes aggregates without overflow.

## Method

```text
Inspect Run -> ShareGPT conversion, TrajectoryEntry fields, JSONL append/list,
terminal-state selection, stats arithmetic, error handling, and all trajectory tests.
```

## Expected Result

Concurrent saves cannot interleave, corrupt rows are surfaced, lineage needed to
audit training records is retained, and public aggregate arithmetic is checked or
saturating.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: invariant failed. Save opens and appends each JSON line without a
  cross-handle/process lock (`trajectory.rs:218-231`); list silently drops every
  malformed row (`:235-261`). Export retains run/session/model but drops existing
  parent run, agent, provider, turn and execution IDs (`:25-48`, `:206-216`). Stats
  use unchecked `sum` for token/tool/duration totals (`:264-285`). Tool results use
  only preview text and pending calls are emitted without a typed missing-result
  marker (`:102-181`).
- Evidence: `echo-agent/src/improve/trajectory.rs:25-48`, `:85-233`, `:235-285`

## Deviations

This does not claim trajectory export should include secret or complete raw Tool
artifacts by default; bounded redacted artifact references can preserve lineage.

## Conclusion

Supports F-EVO-01-P2-03.

## Follow-Up

Add concurrent append/corrupt-line/lineage/maximum aggregate regression cases.
