# F-EVO-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

Improvement configuration affects execution, trace-backed criteria are wired, and
iterations evaluate an explicitly changed candidate without panicking on small
datasets.

## Method

```text
Trace EvalDrivenImprovement builder fields into ImprovementLoop, stratified split,
EvalRunner construction, run-store use, suggestion generation, factory calls,
holdout evaluation, early exit, report persistence, and cleanup.
```

## Expected Result

Every public option has observable value; analysis-only behavior is named as such;
trace criteria see the configured store; iteration N+1 evaluates an applied change;
all dataset sizes return a typed result.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: invariant failed. `EvalDrivenImprovement.max_iterations` is stored but
  `run` constructs `ImprovementLoop::new()` without applying it
  (`eval_improvement.rs:30-65`, `:81-93`). Each loop iteration generates
  suggestions but invokes the same factory again without applying them
  (`loop.rs:126-171`), so best-iteration selection measures repeated unchanged
  Agents. The optional RunStore is loaded only after eval results; it is never
  attached to EvalRunner, so ToolUsed/ToolNotUsed can fail without trace. A
  criteria group of one computes `clamp(1, 0)`, which panics (`:78-99`). Early
  threshold exit skips workspace cleanup (`:176-183`).
- Evidence: paths and anchors above.

## Deviations

The current `ImprovementLoop` type-level comment correctly says suggestions are
human-reviewed and not applied. The higher facade/module still advertises
iterative prompt improvement, so the defect is API composition and naming, not a
requirement for unsafe automatic mutation.

## Conclusion

Supports F-EVO-01-P1-03 and F-EVO-01-P1-04.

## Follow-Up

Separate an analysis report API from an explicit caller-supplied candidate-update
callback, or remove iteration claims/options until real candidate changes exist.
