# F-EVO-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at inspection

## Claim

The evolution change log is a complete, durable mutation record with sufficient
before/after and source identity to support its documented rollback capability.

## Method

```text
Inspect ChangeEntry/ChangeLog shape, all builder fields, JsonlChangeLog load/
append/cache/locking/fsync behavior, and mutation call sites for before/after,
actor/source/proposal identity and rollback entry points.
```

## Expected Result

Successful mutation returns only after durable audit; lock failure fails closed;
every reversible mutation records an authoritative undo payload and causal source;
the advertised rollback operation exists.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: invariant failed. ChangeLog exposes record/query/latest/len but no
  rollback (`audit.rs:82-102`), while module docs promise undo of any change
  (`:1-8`). ChangeEntry has free-text trigger but no run/session/turn/execution,
  proposal revision, actor/reviewer, or approval identity (`:56-80`). Many skill
  mutations record neither before nor after. After up to ten minutes of lock retry,
  append explicitly proceeds without the lock (`:262-288`), and `sync_all` error
  is discarded despite the documented durability guarantee (`:290-305`). Each
  handle's in-memory query cache also never observes another handle's later writes.
- Evidence: `echo-agent/src/evolution/audit.rs:1-8`, `:56-102`, `:184-346`,
  `echo-agent/src/evolution/draft.rs:128-137`,
  `echo-agent/src/evolution/patch.rs:315-325`,
  `echo-agent/src/evolution/merge.rs:270-279`

## Deviations

Memory merge exposes a separate explicit snapshot restore API; that localized
positive path does not satisfy the global ChangeLog rollback claim.

## Conclusion

Supports F-EVO-01-P1-05.

## Follow-Up

Make audit failure abort or compensate mutation, record typed causal/revision and
undo facts, and either implement rollback or remove the global claim.
