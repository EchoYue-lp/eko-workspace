# S-QA-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source changes were external and excluded; synthesis reads Codex Q reports only.

## Claim

Immutable validation filenames and headers encode one consistent retry lineage.

## Method

For each catalog validation filename, compare its validation ID/attempt suffix
with the first heading and inspect corrected attempts named in task matrices.

## Expected Result

`VNN-AA.md` always means validation `VNN`, attempt `AA`; the heading agrees and
different checks use different validation IDs.

## Result

- Exit code: 0 for the inventory; the invariant comparison failed.
- Duration: about four seconds.
- Summary: Q reports use at least two incompatible conventions. Many reports
  follow `V01-02.md` = `V01 / Attempt 02`; Q-STA and Q-GUI also use suffixes as
  separate validation IDs (`V00-02 / Attempt 01`, `V03-05 / Attempt 02`). A
  filename alone therefore cannot reliably distinguish a new validation from a
  retry or reconstruct attempt order.
- Log/artifact: none.

## Deviations

Existing reports are immutable and were not renamed or rewritten. Task matrices
and headings still make human reconstruction possible.

## Conclusion

This supports `S-QA-01-P2-01`: machine-readable retry lineage is ambiguous even
though report content remains available.

## Follow-Up

Adopt separate immutable validation ID and attempt metadata for future reports;
do not mutate historical evidence.
