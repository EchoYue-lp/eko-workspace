# F-MEM-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

Existing source tests cover the required non-SQLite durability, corruption,
namespace, path, round-trip, search, pagination, and concurrency invariants.

## Method

Inventory every `#[test]` and `#[tokio::test]` in non-SQLite memory modules and
map its source body to the F-MEM-01 invariant matrix. Do not execute tests.

```text
rg -n '^\s*#\[(tokio::)?test\]|async fn test_|fn test_' \
  echo-state/src/memory/{store,file_conversation,embedding_store,typed_store}.rs
```

Environment: static existing-test coverage inventory; no test binary executed.

## Expected Result

Each data-loss, concurrency, namespace-encoding, reserved-ID, ordering, and
pagination boundary has an explicit regression test.

## Result

- Exit code: 0 for the inventory; claim failed
- Duration: not recorded
- Summary: InMemoryStore has CRUD/search/simple namespace tests, but FileStore
  has no constructor, reopen, corrupt/truncated, atomic, multi-instance,
  maintenance, or flush-failure test. FileConversationStore has CRUD/search,
  corrupt-record, traversal, projection round-trip, stale-meta reconciliation,
  and ID-counter tests, but no `_meta` reserved-ID, pagination offset, message
  ordering, or multiple-instance test. Existing namespace tests use only
  separator-free components. Typed/embedding happy-path tests do not cover
  maximum limit or namespace collisions.
- Log/artifact: this report

## Deviations

This is a coverage inspection, not evidence that existing tests currently pass.
The review-stage protocol explicitly forbids new test/build executions.

## Conclusion

The required edge cases are materially under-tested. This supports the
regression requirements of all findings but is not a separate implementation
defect.

## Follow-Up

Add the named cases during implementation and run them in the relevant Q task.
