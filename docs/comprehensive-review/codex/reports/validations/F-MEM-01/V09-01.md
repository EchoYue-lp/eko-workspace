# F-MEM-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

Current memory documentation accurately describes available non-SQLite
conversation implementations, namespace isolation, and supported shared-file
behavior.

## Method

Compare memory/subagent docs and EKO MASTER-PLAN claims with current exports,
constructors, and implementation details. Classify each claim as current,
fixed, stale, or regressed.

```text
Inspect echo-agent/docs/en/{03-memory,06-subagent,12-mock}.md,
echo-core/src/memory/conversation.rs, echo-state/src/memory/mod.rs, and
echo-agent-cli/docs/MASTER-PLAN.md:70.
```

Environment: static document-to-source comparison only.

## Expected Result

Documentation names live options and does not promise isolation/concurrency that
the implementation cannot provide.

## Result

- Exit code: 0 for the comparison; claim failed
- Duration: not recorded
- Summary: the EKO MASTER-PLAN description of FileConversationStore unique-temp,
  fsync, path validation, corruption errors, and atomic ensure is current except
  for the unmentioned `_meta` collision. `docs/en/03-memory.md:74` and the core
  module comment still say the concrete ConversationStore is SQLite, which is
  stale because FileConversationStore is public/live. The namespace isolation
  guarantee at `03-memory.md:104-117` is regressed by slash flattening. The
  shared physical FileStore scenario at `06-subagent.md:152-161` is regressed by
  per-instance snapshots and fixed temp paths.
- Log/artifact: this report

## Deviations

Historical documents were used only as hypotheses, not copied as findings.

## Conclusion

This supports runtime reachability/contract interpretation for findings
F-MEM-01-P0-02 and F-MEM-01-P1-01 and identifies localized stale docs. It does
not establish when the drift was introduced.

## Follow-Up

Update docs only with the implementation fix so they do not become a second
authority.
