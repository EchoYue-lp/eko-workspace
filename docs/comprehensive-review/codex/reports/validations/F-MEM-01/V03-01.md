# F-MEM-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

`FileStore` fails closed on corrupt/truncated JSON and does not convert a parse
failure into a later successful overwrite of previously durable memory.

## Method

Perform a branch-complete static trace of `FileStore::new`, `put`, `delete`, and
`flush`, including the live EKO constructor's handling of `FileStore::new`.

```text
1. Inspect echo-state/src/memory/store.rs:227-278 and :309-397.
2. Follow parse success and parse error values into Self.data.
3. Follow the next successful mutation into serialize -> temp -> rename.
4. Follow FileStore::new's Result through EKO create_memory_store_at and the
   framework setup_memory_store fallback.
```

Environment: static control/data-flow inspection; a corrupt fixture was not
executed under the review-stage no-test rule.

## Expected Result

Malformed persisted JSON returns a typed error or preserves/quarantines the
original bytes; no subsequent normal write may silently replace all prior data.

## Result

- Exit code: 0 for the inspection; claim failed
- Duration: not recorded
- Summary: `FileStore::new` catches every JSON parse error with
  `unwrap_or_else`, logs a warning, and returns `Ok(Self)` with an empty map
  (`store.rs:232-250`). The live caller therefore treats memory as enabled. The
  next `put`, `delete`, `put_batch`, or public flush serializes that empty/current
  map and renames it over the malformed original (`store.rs:254-278`), making
  recovery of any still-valid previous content impossible through this store.
- Log/artifact: this report

## Deviations

No dynamic crash/corruption fixture was run. The failure is source-conclusive
because both parse-error and overwrite branches are unconditional.

## Conclusion

This supports finding F-MEM-01-P0-01. It does not establish which backup or
recovery format should replace the fall-open behavior.

## Follow-Up

Future fix regression: malformed and truncated files must fail closed, preserve
the original bytes, and remain recoverable after a rejected mutation.
