# F-MEM-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

The reviewed File/InMemory conversation and memory implementations are reachable
through real framework and EKO construction paths, not definition-only code.

## Method

Trace constructors from public exports through framework Agent construction and
EKO composition roots, then trace terminal read/write operations.

```text
1. Trace ReactAgent::new -> setup_memory_store -> FileStore -> four memory tools.
2. Trace ReactAgentBuilder::with_memory_tools/store custom injection.
3. Trace EKO app-core create_memory_store_at/create_conversation_store and the
   CLI/Tauri scheduler constructors.
4. Trace run finalization -> save_transcript_projection -> ensure_conversation
   -> save_messages.
5. Trace GUI and TUI search commands -> ConversationStore::search_conversations.
```

Environment: static source/call-site inspection only; no build or test executed.

## Expected Result

At least one production path constructs each relevant persistent implementation,
and persistence/search trait methods have live terminal consumers.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: default framework Agent construction reaches `FileStore` at
  `src/agent/react/mod.rs:750` and registers remember/recall/search/forget tools.
  EKO injects workspace-scoped `FileStore` at
  `echo-agent-app-core/src/infra.rs:341` and constructs scheduler FileStores in
  both CLI and Tauri. EKO constructs `FileConversationStore` at
  `infra.rs:1214`; framework finalization writes it through
  `src/agent/snapshot.rs:648`; GUI and TUI invoke conversation search. EKO uses
  `InMemoryStore` as the scheduler fallback, and framework public APIs and
  evolution code also construct it.
- Log/artifact: this report

## Deviations

Repository-wide call search found no production caller for `Store::prune_expired`
or `Store::dedup_by_content`. Those remain public framework capabilities and are
not considered dead merely because EKO currently does not invoke them.

## Conclusion

The core persistence defects supported by later reports are runtime relevant.
This validation does not prove every mode uses the same physical store instance
or that maintenance methods complete.

## Follow-Up

V03-V08 inspect behavior and edge cases; A-MEM-01 owns EKO refresh/parity.
