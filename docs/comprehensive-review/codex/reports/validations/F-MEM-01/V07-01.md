# F-MEM-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

Non-SQLite production memory paths are UTF-8/panic/overflow safe for external
IDs, text, limits, and persisted input.

## Method

Search production scope for panic macros, unwrap/expect, direct byte slicing,
unchecked arithmetic, byte-length text decisions, and numeric casts; classify
test-only matches separately and manually inspect all production hits.

```text
rg -n 'unwrap\(|expect\(|panic!|unreachable!|todo!|\[[^]]+\.\.[^]]*\]|\* 3|\.len\(\)' \
  echo-core/src/memory echo-state/src/memory --glob '*.rs' --glob '!sqlite*'
```

Environment: static source scan only; no compile/Clippy/test command executed.

## Expected Result

Production external-input paths contain no panicking parse/index/slice and all
limit arithmetic is checked or saturating.

## Result

- Exit code: 0 for the search; claim failed
- Duration: not recorded
- Summary: no production `panic!`, `expect`, direct byte slice, or unsafe direct
  indexing was found in the primary store/conversation implementations;
  `safe_segment` iterates chars. `TypedMemoryStore::search_typed`, however,
  forwards `limit * 3` without checked/saturating multiplication
  (`typed_store.rs:238-246`), which can panic in checked builds or wrap in
  release. Tokenization uses byte `len() > 1`, producing inconsistent one-character
  treatment for ASCII versus multibyte scripts but not a panic.
- Log/artifact: this report

## Deviations

Test modules contain unwrap/expect/direct indexing and were not treated as
production external-input paths. Snapshot and SQLite modules were outside this
task's primary scope.

## Conclusion

This supports finding F-MEM-01-P3-01 and a positive UTF-8-slicing conclusion for
the inspected production paths. It does not replace panic-safety Clippy or
exhaustive fuzzing.

## Follow-Up

Future regression: `limit = usize::MAX`, multilingual one-scalar queries, and
fuzzed persisted JSON/IDs.
