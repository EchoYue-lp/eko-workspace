# F-MEM-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

ConversationStore defaults and FileConversationStore implement the documented
full-search, atomic ensure, pagination, and message-order contracts.

## Method

Compare every non-SQLite trait method with FileConversationStore and inspect
default implementations, ordering, limits, and lock boundaries.

```text
1. Read echo-core/src/memory/conversation.rs:93-205.
2. Read echo-state/src/memory/file_conversation.rs:248-451.
3. Trace list offset/limit, search candidate selection, ensure get/create, and
   get_messages order.
```

Environment: static contract/implementation matrix; no fixture executed.

## Expected Result

The default search examines all eligible conversations, ensure is accurately
documented or atomic, pagination applies after deterministic ordering, and
messages are returned by ascending ID as promised.

## Result

- Exit code: 0 for the inspection; claim failed
- Duration: not recorded
- Summary: FileConversationStore correctly applies offset then limit after
  updated-time ordering, overrides search across all records, and overrides
  ensure under one instance lock. The public default search nevertheless asks
  `list_conversations` for only 100 records while claiming to scan all, so
  matches outside the latest 100 are invisible. The public default ensure is a
  non-atomic get-then-create. FileConversationStore `get_messages` returns the
  saved vector unchanged, even though the trait promises ID-ascending order; a
  caller may save supplied IDs in any order.
- Log/artifact: this report

## Deviations

The live FileConversationStore overrides both defective defaults, so the
100-record cap and ensure race affect third-party/default implementations, not
this EKO backend. The ordering mismatch remains in FileConversationStore.

## Conclusion

This supports findings F-MEM-01-P2-01 and F-MEM-01-P2-02. It does not evaluate
SQLite overrides, which belong to F-MEM-02.

## Follow-Up

Future regression: >100 conversations with an old-only match, concurrent ensure
for a backend using the selected contract, offset/limit boundaries, and
out-of-order supplied message IDs.
