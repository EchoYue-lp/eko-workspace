# F-MEM-01 / V03 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

`FileStore::prune_expired` and `dedup_by_content` terminate and report durable
success whenever they remove at least one item.

## Method

Inspect lock lifetimes and nested lock acquisition for both Store trait method
implementations and `FileStore::flush`.

```text
1. Inspect FileStore::flush at echo-state/src/memory/store.rs:254-278.
2. Inspect prune_expired at :430-446 and dedup_by_content at :448-482.
3. Record the held tokio::sync::RwLock guard at each await.
4. Search both repositories for production callers and existing tests.
```

Environment: static async lock-order inspection only; no timeout fixture was run.

## Expected Result

Mutation releases the write guard before flush takes a read snapshot, and a
flush error is propagated rather than converted to successful removal.

## Result

- Exit code: 0 for the inspection; claim failed
- Duration: not recorded
- Summary: both methods retain `let mut data = self.data.write().await` while
  awaiting `self.flush()`. `flush` immediately awaits `self.data.read()`, which
  cannot be granted while the same task holds the write guard. Therefore the
  `removed > 0` branch waits indefinitely. Both methods also discard the flush
  result with `let _ =`, so merely releasing the guard would still permit an
  in-memory-only removal to be reported as durable success. No production
  caller or FileStore-specific maintenance test was found.
- Log/artifact: this report

## Deviations

The zero-removal branch returns normally. Lack of an in-repository caller does
not make the public framework implementation obsolete.

## Conclusion

This supports finding F-MEM-01-P1-02. It does not determine scheduler policy for
when pruning/deduplication should run.

## Follow-Up

Future fix regression: force one removal under a short timeout, reopen the store,
and inject a flush failure to verify error propagation and in-memory rollback.
