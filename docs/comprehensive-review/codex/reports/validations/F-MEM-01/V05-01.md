# F-MEM-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

Every `safe_segment`-accepted conversation ID maps to a record path distinct
from internal metadata, and corrupt/truncated records fail closed while normal
writes use durable atomic replacement.

## Method

Trace `FileConversationStore::new`, `conv_path`, `meta_path`, record/meta
read-write paths, `safe_segment`, and `atomic_write`; symbolically execute
`create_conversation(NewConversation { conversation_id: "_meta", ... })`.

```text
Inspect echo-state/src/memory/file_conversation.rs:73-215, :248-451,
:457-533, and the existing tests at :536-779.
```

Environment: static branch/data-flow inspection only; no fixture executed.

## Expected Result

Accepted IDs are an injective disjoint record namespace; corruption returns an
error; temp files are unique, synced, renamed, cleaned, and parent-synced.

## Result

- Exit code: 0 for the inspection; claim failed
- Duration: not recorded
- Summary: corrupt metadata and records correctly return typed errors, and
  `atomic_write` uses UUID temp names, file fsync, rename, cleanup, and Unix
  parent-directory fsync (`file_conversation.rs:494-532`). However `_meta` is
  accepted by `safe_segment` and maps to `_meta.json`, exactly the internal
  metadata path. On a fresh store, create writes a conversation record there,
  then `persist_meta` atomically overwrites it with `StoreMeta` and returns
  success; the conversation is immediately unreadable as a record.
- Log/artifact: this report

## Deviations

The traversal test covers `../escape` but not internal reserved names or
case-folding filesystem collisions.

## Conclusion

This supports finding F-MEM-01-P0-03 while also confirming the normal
FileConversationStore atomic-write/corruption path is substantially stronger
than FileStore. It does not prove multi-process safety.

## Follow-Up

Future regression: reject/reserve `_meta` before any write and test all internal
filenames plus platform case/reserved-name behavior.
