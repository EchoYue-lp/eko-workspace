# F-MEM-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated modified generated TypeScript files; source repositories were not edited

## Claim

The non-SQLite memory surface has one canonical trait authority and a coherent
implementation/decorator matrix rather than duplicate framework and EKO stores.

## Method

From the workspace root, inspect `echo-core/src/memory`,
`echo-state/src/memory`, the root memory facade, and both repositories for trait,
implementation, decorator, and re-export definitions. Classify SQLite matches
as F-MEM-02 scope and test-only implementations separately.

```text
1. Search both repositories for trait Store, trait ConversationStore,
   impl Store for, impl ConversationStore for, FileStore, InMemoryStore,
   FileConversationStore, EmbeddingStore, and TypedMemoryStore.
2. Read echo-core/src/memory/{store,conversation}.rs.
3. Read echo-state/src/memory/mod.rs and root src/memory.rs re-exports.
4. Inspect EKO matches for an independent implementation rather than a caller.
```

Environment: macOS workspace checkout; static source inspection only; no
features enabled and no build or test executed.

## Expected Result

Traits are owned once by `echo_core`; `echo_state` supplies reusable concrete
implementations/decorators; the root crate re-exports them; EKO consumes rather
than reimplements the contracts.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: `Store` exists only at `echo-core/src/memory/store.rs:182` and
  `ConversationStore` only at `echo-core/src/memory/conversation.rs:98`.
  Non-SQLite concrete implementations are `InMemoryStore`, `FileStore`, and
  `FileConversationStore`; `EmbeddingStore` is a `Store` decorator and
  `TypedMemoryStore` is a typed serialization/filter facade. `echo_state` and
  root `echo_agent::memory` re-export this authority. No EKO-owned duplicate
  trait or store implementation was found. SQLite implementations were found
  but deliberately deferred to F-MEM-02.
- Log/artifact: this report

## Deviations

`echo-core/src/memory/conversation.rs:4` and current English memory documentation
still name only the SQLite conversation implementation even though
`FileConversationStore` is public and live. That drift is evaluated in V09.

## Conclusion

This supports the framework/application layering and duplicate-authority claim.
It does not prove implementation correctness or runtime construction.

## Follow-Up

V02 traces registration and live callers; F-MEM-02 owns SQLite behavior.
