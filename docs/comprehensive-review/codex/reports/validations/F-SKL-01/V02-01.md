# F-SKL-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; only Codex reports changed outside them

## Claim

File-skill discovery applies deterministic scope precedence and deterministic
same-name collision/reload behavior.

## Method

Trace `SkillLoader::discover`, `scope_to_dirs`, recursive `scan_directory`,
descriptor insertion and `ReactAgent::discover_skills_inner`; compare with
existing nested discovery/additive refresh tests.

## Expected Result

Scope order and same-scope tie breaking are stable, and rediscovery either
atomically replaces a named source or reports a typed collision.

## Result

- Exit code: 0
- Duration: not separately timed
- Summary: earlier `DiscoveryScope` entries win, and Project orders `skills/`
  before `.agents/skills/`. Within a scanned directory, however, async
  `read_dir` entries are consumed without sorting and the first same-name
  descriptor wins. A new Agent discovery creates a fresh loader, but then skips
  any descriptor already installed in the catalog registry; changed metadata,
  hooks and an existing activation projection are not refreshed. Tests cover
  nested discovery and additive different-name refresh, not collisions or
  same-name replacement.
- Log/artifact: this report

## Deviations

The framework documents only scope-order precedence, not a same-scope tie
breaker or repeated-discovery replacement policy.

## Conclusion

Fails deterministic collision/reload behavior and supports
`F-SKL-01-P2-04`.

## Follow-Up

Add ordered path candidates and an explicit source-scoped reconcile contract.
