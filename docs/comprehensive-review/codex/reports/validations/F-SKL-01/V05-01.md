# F-SKL-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; only Codex reports changed outside them

## Claim

Catalog, baseline, activation, hook, tool-visibility and permission injection are
connected to the live Agent with correct path/cardinality semantics.

## Method

Trace discovery through catalog projection/tool replacement/hook registration;
trace programmatic/model activation to context/tool visibility; inspect
`inject_methodology_baseline` and its EKO bootstrap caller.

## Expected Result

Tier 1 is one replaceable catalog, Tier 2 is one replaceable Skill projection or
one model-visible result, active allowed-tools reaches schema and execution
filters, and baseline bodies are read from each descriptor's actual file.

## Result

- Exit code: 0
- Duration: not separately timed
- Summary: catalog uses one named projection; skill tools are replaced with a
  current shared registry; hooks register; invocation snapshots enforce active
  allowed-tools at schema and pipeline stages. Activation projections use stable
  markers. However `SkillDescriptor.location` is already the absolute
  `SKILL.md` path, while `inject_methodology_baseline` appends another
  `SKILL.md`; reads therefore target `.../SKILL.md/SKILL.md` and silently skip.
  EKO calls this helper for its enabled methodology baseline and then logs that
  injection succeeded.
- Log/artifact: this report

## Deviations

F-RCT-01 separately owns the subsequent system-prompt mutation defect. This
validation stops at the independently broken baseline file lookup.

## Conclusion

Passes ordinary catalog/tool/hook injection but fails baseline injection,
supporting `F-SKL-01-P1-03`.

## Follow-Up

Read `descriptor.location` directly and make missing/bad baseline content a
typed result rather than silent success.
