# F-LLM-01 / V12 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

A malformed OpenAI-compatible SSE `data:` event becomes a typed stream error.

## Method

Run `cargo run --offline --quiet --bin stream_malformed` with approved localhost
bind. The loopback response contains invalid JSON followed by stream EOF.

## Expected Result

The stream should yield an error, not normal completion.

## Result

- Exit code: 0
- Duration: 1.54 seconds
- Summary: the fixture confirmed the malformed event was silently dropped and
  the stream completed without an error item.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/stream_malformed.rs`

## Deviations

The binary exits zero when the defect is reproduced; validation status reflects
the false invariant.

## Conclusion

This supports F-LLM-01-P1-01.

## Follow-Up

Add permanent malformed, truncated, and invalid-UTF-8 stream fixtures.
