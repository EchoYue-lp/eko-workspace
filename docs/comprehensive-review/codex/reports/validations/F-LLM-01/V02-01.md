# F-LLM-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

Every public provider-neutral abstraction is registered in a real factory and
reaches the framework/EKO runtime without a parallel route.

## Method

Trace `LlmConfig::build_client` and `ProviderFactory` into `OpenAiClient` and
`AnthropicClient`; search both repositories for every `ProviderAdapter`
implementation and `AdapterClient` construction; trace EKO
`resolve_runtime_model -> build_llm_config -> ReactAgentBuilder::llm_config ->
ReactAgent::set_llm_config`; trace the canonical ReAct streaming call through
`create_llm_stream`.

## Expected Result

The exported generic adapter participates in provider construction, and every
configured field reaches each request-producing path.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: live factory construction bypasses `AdapterClient`; repository-wide
  search found no `ProviderAdapter` implementation or `AdapterClient::new`
  call. EKO and the ReAct loop reach the concrete clients, but thinking and
  response-format facts are lost on live construction/streaming paths.
- Log/artifact: none

## Deviations

An independent downstream consumer could implement `ProviderAdapter`; absence
of an in-repository implementation alone is not a deletion criterion for a
public framework API. The finding is the mismatch between its advertised
generic behavior and its own implementation, plus duplicate built-in authority.

## Conclusion

The reachability claim fails. This supports F-LLM-01-P1-06,
F-LLM-01-P1-07, and F-LLM-01-P2-10.

## Follow-Up

F-LLM-02 and F-LLM-03 should revalidate the selected adapter authority after
provider-specific fixes.
