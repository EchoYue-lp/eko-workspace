# F-LLM-01 / V30 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

The provider-neutral streaming path preserves UTF-8 and candidate identity, and
cancellation can interrupt a pending transport read.

## Method

Trace `OpenAiClient` through shared `stream_post`, compare the wire
`ChatCompletionChunk` with neutral `ChatChunk`, then follow ReAct's reconstruction.
Inspect cancellation placement around send/byte-read futures.

## Expected Result

Byte framing is incremental, cancellation participates in every pending await,
and response ID plus candidate index/cardinality remain representable.

## Result

- Exit code: 0 for inspection; three invariants failed.
- Duration: under one second.
- Summary: `client.rs:315` independently converts every network chunk with
  `from_utf8_lossy`; malformed parsed events can disappear through nested
  `Option`; cancellation is checked before, but not selected against, the next
  byte future. Neutral `ChatChunk` has no ID/index/candidate collection, and
  ReAct fabricates empty ID/index zero after adapters select one choice.
- Log/artifact: `providers/client.rs:174-350`, `echo-core/src/llm/mod.rs:232-240`,
  `react/run/phases/think.rs:329-343`.

## Deviations

No new build under disk pressure. Delegated V11-V13 and V25 provide fresh
executable timing/byte-split confirmation.

## Conclusion

F-LLM-01-P1-01, P1-02, and P2-08 are accepted.

## Follow-Up

Use one error-bearing incremental SSE decoder and a cancellation-aware neutral
stream operation that preserves or explicitly rejects candidate identity.
