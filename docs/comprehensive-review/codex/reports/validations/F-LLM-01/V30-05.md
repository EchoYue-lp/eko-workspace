# F-LLM-01 / V30 / Attempt 05

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

The typed tool-choice API is connected to requests and the documented generic
provider adapter is the live, internally coherent provider-extension authority.

## Method

Compare `ToolChoice` output with `ChatRequest.tool_choice`; inventory
`ProviderAdapter` implementations/constructions and compare declared hooks with
`AdapterClient` behavior and provider factories.

## Expected Result

All typed choices fit the neutral request field; built-ins or external adapters
flow through a client that honors every declared hook.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: named `ToolChoice` produces a JSON object while the neutral request
  can store only `Option<String>`. Repository-wide production search finds no
  `ProviderAdapter` implementation or factory construction; built-ins use
  concrete clients, and `AdapterClient` duplicates/hardcodes behavior instead of
  consuming several declared hooks.
- Log/artifact: `echo-core/src/llm/mod.rs:109-182`,
  `providers/traits.rs`, `providers/adapter_client.rs`, and provider factory
  search.

## Deviations

None.

## Conclusion

F-LLM-01-P2-09 and P2-10 are accepted.

## Follow-Up

Make tool choice typed end to end and migrate to one provider authority before
deleting the parallel abstraction.
