# F-LLM-01 / V13 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

Cancelling while the stream awaits its next byte chunk ends the stream promptly
and does not emit that delayed chunk.

## Method

Correct V13-01 to inspect the next item and elapsed time, then rerun:

```text
cargo run --offline --quiet --bin stream_cancel
```

The loopback server delays the next chunk by 500 ms and cancellation occurs
after 50 ms.

## Expected Result

No delayed content should be produced and completion should occur well before
the 500 ms transport delay.

## Result

- Exit code: 0
- Duration: 2.46 seconds
- Summary: the cancelled stream waited 502.57 ms and still yielded content
  `late` from the next byte chunk.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/stream_cancel.rs`

## Deviations

The binary exits zero when the defect is reproduced; validation status reflects
the false invariant.

## Conclusion

This supports F-LLM-01-P1-02 and supersedes V13-01.

## Follow-Up

Conformance tests should cover cancellation before send, during first-byte
wait, during idle wait, and after a completed event.
