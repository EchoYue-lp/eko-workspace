# F-LLM-01 / V09 / Attempt 01

> Status: inconclusive
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

A mixed-provider usage object exposes the expected effective prompt total while
cache fingerprints distinguish every provider-visible prompt fact.

## Method

Run `cargo run --offline --quiet --bin usage_cache` from the temporary fixture.
The first fixture expected `effective_prompt_tokens == 200` for contradictory
OpenAI and Anthropic cache fields.

## Expected Result

The assertion should characterize current first-field/provider selection.

## Result

- Exit code: 101
- Duration: 0.73 seconds
- Summary: the assertion failed with left `110`, right `200`.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/usage_cache.rs`

## Deviations

The expected value was wrong. Current logic selects the OpenAI cached value 10,
then uses Anthropic-field presence to add it to prompt 100, producing 110.
This is a fixture error, not a source pass/fail result.

## Conclusion

Inconclusive. The failed attempt is retained and corrected in V09-02.

## Follow-Up

V09-02.
