# F-LLM-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

EKO's provider/model adapter is thin and losslessly supplies the selected
model's connection, generation, and thinking settings to all created agents.

## Method

Trace `ConfiguredModel -> resolve_runtime_model -> build_llm_config ->
ReactAgentBuilder -> ReactAgent`, plus pooled-agent creation and later GUI/TUI
model-update paths. Compare selected-model and legacy-mirror fields.

## Expected Result

Initial construction and later model changes apply the same selected model
facts; application code adds policy but no second provider translation
authority.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: provider/model/auth/base URL/temperature/max/context reach startup.
  `resolve_runtime_model` reads thinking from the legacy model mirror rather
  than the selected record; `build_llm_config` never receives it; initial
  `create_agent` never calls `set_thinking`. Later GUI/TUI/pool update paths do,
  so startup and update semantics differ.
- Log/artifact: none

## Deviations

When `set_default_model` has already mirrored a selection into legacy config,
the first loss can be masked; the generic `resolve_runtime_model(Some(id))`
path remains field-incomplete.

## Conclusion

The EKO adapter claim fails for thinking configuration and supports
F-LLM-01-P1-06.

## Follow-Up

A-CFG-01 should verify every entry point after the framework contract is fixed.
