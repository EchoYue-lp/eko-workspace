# F-LLM-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

Usage and cache diagnostics have one validated authority and produce safe,
content-complete values for every neutral message/tool variant.

## Method

Trace all `Usage` normalization helpers and provider assignments; inspect
`PromptCacheLayout`, `hash_message`, and `hash_tool`; then trace cache hints and
fingerprints into ReAct requests and EKO observability consumers.

## Expected Result

Contradictory usage families are rejected or explicitly tagged, ratios remain
within their documented range, arithmetic cannot overflow, layout accepts all
messages without panic, and hashes include every provider-visible prompt fact.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: provider identity is inferred from optional-field presence and first
  non-None priority; impossible ratios can be emitted; Anthropic uses unchecked
  token addition; layout can construct an inverted slice; message hashes ignore
  image/file/tool/reasoning/name facts and tool hashes ignore type/description.
- Log/artifact: none

## Deviations

The fingerprint is currently diagnostics/cache-hint metadata, not evidence of
an actual provider cache key. Impact is therefore limited to false cache
observability and adapter hints.

## Conclusion

Usage/cache authority is incomplete and can panic. This supports
F-LLM-01-P1-08 and F-LLM-01-P2-11.

## Follow-Up

V09, V16, and V17 exercise the edge cases.
