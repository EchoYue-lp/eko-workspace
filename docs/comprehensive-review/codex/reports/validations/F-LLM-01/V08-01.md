# F-LLM-01 / V08 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

Malformed message content shapes and a missing role are rejected instead of
being accepted as valid empty/custom messages.

## Method

From `/private/tmp/f-llm-01-probe.u1G2zt`, run:

```text
cargo run --offline --quiet --bin malformed
```

The fixture deserializes object and numeric `content`, plus a message with no
role, and asserts the current lossy results.

## Expected Result

The probe should demonstrate the inspected lossy acceptance behavior.

## Result

- Exit code: 0
- Duration: approximately 43 seconds including first compilation
- Summary: malformed content objects/numbers and a missing role were accepted
  lossily.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/malformed.rs`

## Deviations

The temporary fixture target was later removed for disk pressure; source
fixture remains.

## Conclusion

This supports F-LLM-01-P2-12. It does not prove how every provider validates
all other response fields.

## Follow-Up

Add permanent rejection fixtures when message wire policy is selected.
