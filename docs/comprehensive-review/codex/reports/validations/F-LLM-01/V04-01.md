# F-LLM-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The concrete provider request/response conversions preserve every neutral
field they advertise or return an explicit unsupported/error result.

## Method

Compare `OpenAiClient` and `AnthropicClient` request/response mapping against
the V03 matrix. Inspect all `ChatRequest` reads, wire structs, response block
matches, capability overrides, and raw-response construction.

## Expected Result

No accepted request fact or provider response block is silently discarded;
capabilities accurately predict supported fields.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: OpenAI-compatible mapping preserves most modeled fields after
  documented file normalization. Anthropic keeps only the last system message,
  omits `tool_choice` and `response_format`, cannot represent thinking blocks,
  and returns an empty fabricated `raw` response. It also inherits OpenAI
  capabilities instead of returning `ProviderCapabilities::anthropic()`.
- Log/artifact: none

## Deviations

Provider-specific attachment normalization was treated as an explicit
capability adaptation, not automatically as a defect.

## Conclusion

The provider conversion claim fails. This supports F-LLM-01-P1-03 and
F-LLM-01-P1-05.

## Follow-Up

F-LLM-02 and F-LLM-03 should own exhaustive wire fixtures for each provider.
