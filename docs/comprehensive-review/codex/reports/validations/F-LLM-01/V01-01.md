# F-LLM-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The provider-neutral LLM concepts have one authoritative definition and no
parallel provider/config abstraction with the same responsibility.

## Method

Inspect `echo-core/src/llm/{mod,types,capabilities,thinking,cache}` and search
both repositories for `LlmClient`, `ChatRequest`, `ChatResponse`, `ChatChunk`,
`ToolChoice`, `ProviderAdapter`, `ThinkingProtocol`, `LlmConfig`, and EKO chat
DTO definitions. Distinguish product transport DTOs from framework protocol
authority, then search implementations and constructors.

## Expected Result

One neutral contract owns each semantic fact; provider and EKO types are thin,
lossless adapters rather than parallel authorities.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: `echo_core::llm` is the live neutral authority and EKO chat DTOs are
  projections, but `ProviderAdapter` duplicates thinking/capability policy and
  has no implementation or factory construction. `ToolChoice` is a typed API
  disconnected from the request's `Option<String>` field.
- Log/artifact: none

## Deviations

The EKO request/response DTOs were not classified as duplicates because they
serve a distinct application command boundary.

## Conclusion

The uniqueness claim fails for provider configuration/translation and typed
tool choice. This supports F-LLM-01-P2-09 and F-LLM-01-P2-10.

## Follow-Up

V02 traces whether either duplicate is live.
