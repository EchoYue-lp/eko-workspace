# F-LLM-01 / V14 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

Anthropic stream tool calls retain the provider content-block index when text
blocks precede tool blocks.

## Method

Run `cargo run --offline --quiet --bin anthropic_tool` with approved localhost
bind. The fixture streams text block index 0 then tool-use block index 1 and a
valid argument delta/stop for index 1.

## Expected Result

One tool call should be emitted before `finish_reason=tool_calls`.

## Result

- Exit code: 0
- Duration: 2.87 seconds
- Summary: text `preface` and `finish_reason=tool_calls` were emitted, but tool
  call count was zero.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/anthropic_tool.rs`

## Deviations

The binary exits zero when the defect is reproduced.

## Conclusion

This supports F-LLM-01-P1-04.

## Follow-Up

F-LLM-03 should add interleaved text/thinking/multiple-tool block fixtures.
