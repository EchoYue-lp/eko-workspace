# F-LLM-01 / V30 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Anthropic conversion preserves accepted neutral request/response facts, keys
stream state by provider block identity, and reports concrete capabilities.

## Method

Inspect request/response conversion, content-block streaming, the `LlmClient`
implementation, and the neutral capability defaults.

## Expected Result

Accepted fields are preserved or rejected explicitly; stream start/delta/stop
share the wire block index; Anthropic overrides the OpenAI-compatible default.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: system assignment overwrites prior system messages; response/tool/
  thinking/raw facts are only partially mapped. Tool start stores at map length
  while delta/stop look up the provider `index`. `AnthropicClient` implements
  chat methods but no `capabilities`, inheriting OpenAI-compatible defaults even
  though `ProviderCapabilities::anthropic()` exists.
- Log/artifact: `providers/anthropic.rs:60-220,260-345,367-625` and
  `echo-core/src/llm/mod.rs:100-106`.

## Deviations

None. Delegated V14/V15 provide protocol fixture confirmation.

## Conclusion

F-LLM-01-P1-03, P1-04, and P1-05 are accepted.

## Follow-Up

Define a complete capability-gated conversion table and key all streaming state
by the exact provider content-block identity.
