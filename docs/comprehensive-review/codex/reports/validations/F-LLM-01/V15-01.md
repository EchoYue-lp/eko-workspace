# F-LLM-01 / V15 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

Anthropic request conversion preserves multiple system messages and either maps
or rejects structured-output and tool-choice controls.

## Method

Run `cargo run --offline --quiet --bin anthropic_request` with a loopback server
that captures the serialized request and returns a minimal valid response. The
request has two system messages, `tool_choice=none`, and JSON-object output.

## Expected Result

Both system instructions and both controls should be present or the client
should return a typed unsupported error before sending.

## Result

- Exit code: 0
- Duration: 3.66 seconds
- Summary: request capture contained only `second system`; `first system`,
  `tool_choice`, and `response_format` were absent, and the call succeeded.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/anthropic_request.rs`

## Deviations

The fixture uses a local protocol stub rather than an external provider.

## Conclusion

This supports F-LLM-01-P1-03 and F-LLM-01-P1-05.

## Follow-Up

F-LLM-03 should decide supported translations and freeze request JSON fixtures.
