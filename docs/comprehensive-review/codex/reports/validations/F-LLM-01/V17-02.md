# F-LLM-01 / V17 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

`PromptCacheLayout::from_messages` accepts a system-role message whose content
starts with the public runtime-context marker without panicking.

## Method

Pass an empty tool slice to the V17-01 fixture and rerun:

```text
cargo run --offline --quiet --bin cache_layout_panic
```

The panic is caught so the fixture can report it deterministically.

## Expected Result

Layout construction should return segments without a panic.

## Result

- Exit code: 0
- Duration: 0.97 seconds
- Summary: source panicked at `layout.rs:126` because the slice started at 1
  and ended at 0; the fixture caught and confirmed it.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/cache_layout_panic.rs`

## Deviations

The binary exits zero after proving the panic.

## Conclusion

This supports F-LLM-01-P1-08 and corrects V17-01.

## Follow-Up

Add role/marker permutation tests and eliminate unchecked range construction.
