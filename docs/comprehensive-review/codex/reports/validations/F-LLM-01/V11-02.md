# F-LLM-01 / V11 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

An OpenAI-compatible SSE event remains valid when one UTF-8 scalar is split
between HTTP byte chunks.

## Method

With approved localhost bind, rerun from the temporary fixture:

```text
cargo run --offline --quiet --bin stream_utf8
```

## Expected Result

The emitted content should equal the original Chinese character.

## Result

- Exit code: 0
- Duration: 0.78 seconds
- Summary: the probe asserted and printed that the split scalar decoded as
  `"���"` rather than the original character.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/stream_utf8.rs`

## Deviations

The binary is intentionally written to succeed when the defect is reproduced;
validation status is failed because the claimed invariant is false.

## Conclusion

This supports F-LLM-01-P1-01 and supersedes the environmental V11-01 attempt.

## Follow-Up

Repeat the fixture for the Anthropic transport in F-LLM-03.
