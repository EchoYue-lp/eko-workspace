# F-LLM-01 / V30 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Thinking and structured-output configuration reach initial and streaming Agent
requests through one lossless path.

## Method

Trace framework `LlmConfig.thinking`, config conversion, `set_llm_config`, EKO
selected model resolution and initial builder construction; compare non-stream
and canonical stream request assembly.

## Expected Result

Selected-model thinking is parsed/applied at initial construction, and streaming
forwards the same configured `ResponseFormat` as non-streaming.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: framework YAML entries cannot carry thinking and both config
  conversion directions set it to `None`; `set_llm_config` does not apply the
  field. EKO selected-model resolution reads the legacy mirror's thinking, the
  initial builder never calls `set_thinking`, while hot-update paths do.
  Non-streaming forwards `response_format`; canonical streaming hardcodes
  `None` in both trait and legacy branches.
- Log/artifact: `providers/config.rs:160-185,221-235,301-327,528-542,842-850`,
  `echo-agent-cli/.../model_config.rs:237-317`, `infra.rs:303-320,440-452`,
  `react/run/react_loop.rs:25-63`, `react/run/phases/think.rs:312-376`.

## Deviations

None. Application-specific ownership will be resolved in A-CFG/A-BOOT roadmap
items; this validation accepts only the cross-boundary semantic loss.

## Conclusion

F-LLM-01-P1-06 and P1-07 are accepted.

## Follow-Up

Build one neutral request from Agent state and one EKO model-apply adapter shared
by startup and hot updates.
