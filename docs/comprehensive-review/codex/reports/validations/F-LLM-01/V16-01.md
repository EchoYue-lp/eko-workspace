# F-LLM-01 / V16 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture is under `/private/tmp`

## Claim

Normalizing provider-supplied `u32` usage counters cannot panic.

## Method

Run `cargo run --offline --quiet --bin anthropic_overflow` with approved
localhost bind. The response reports `input_tokens=u32::MAX` and
`output_tokens=1`; the client future runs in a task so the fixture can inspect
the join result.

## Expected Result

The adapter should return a value using checked/saturating arithmetic or a typed
invalid-response error.

## Result

- Exit code: 0
- Duration: 2.34 seconds
- Summary: the adapter panicked at `anthropic.rs:345` with `attempt to add with
  overflow`; the fixture observed a panic join result.
- Log/artifact: `/private/tmp/f-llm-01-probe.u1G2zt/src/bin/anthropic_overflow.rs`

## Deviations

The binary exits zero after proving the panic.

## Conclusion

This supports F-LLM-01-P1-08. F-CORE-01-P2-06 covers related public token
arithmetic, but this attempt independently proves a live provider adapter path.

## Follow-Up

Exercise the same boundary in streaming usage and all normalization helpers.
