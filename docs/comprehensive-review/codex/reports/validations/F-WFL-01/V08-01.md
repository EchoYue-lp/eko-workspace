# F-WFL-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Cancellation, timeout, loop bounds, and concurrent partial failure are coherent across Graph, SequentialWorkflow, ConcurrentWorkflow, and DagWorkflow.

## Method

```text
1. Search workflow code for CancellationToken, timeout/deadline, max_steps,
   tokio::spawn, JoinHandle abort/drain, and error paths.
2. Trace each executor's child task ownership after a join or agent error.
3. Compare public APIs and existing tests.
```

## Expected Result

Every workflow has caller-controlled cancellation/deadline; spawned siblings are aborted or drained deterministically; bounds count every execution safely.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: only Graph accepts a cancellation token, checked between nodes; it cannot interrupt a hung node and exposes no timeout. Sequential/Concurrent/DAG expose neither. Concurrent/DAG spawn all children, then await handles in registration order; returning early on one JoinHandle/agent error drops remaining handles without aborting their tasks, so siblings continue detached and their outputs/side effects are unobserved. Graph max_steps does not recheck inside fan-out.
- Log/artifact: source anchors in finding F-WFL-01-P1-06

## Deviations

Dropping a Tokio JoinHandle detaches rather than aborts by contract; no runtime timing fixture was executed.

## Conclusion

Supports F-WFL-01-P1-06. It does not assign generic ToolExecution cancellation defects owned by F-RCT/F-EXT to workflow.

## Follow-Up

Future cancel/deadline/error matrix with observable sibling completion.
