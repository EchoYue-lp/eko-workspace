# F-WFL-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Public workflow production paths are panic-free, UTF-8 safe, and use checked/saturating arithmetic for externally configurable bounds.

## Method

```text
1. Static scan workflow production modules for unwrap, expect, panic, unreachable,
   direct indexing, byte slicing, unchecked casts, additions, and counter increments.
2. Separate doctest/test-only occurrences from production reachability.
3. Inspect identified production anchors in context.
```

## Expected Result

No public configuration/input can panic or wrap counters; documentation examples do not teach invalid UTF-8 slicing.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: DAG production uses direct HashMap indexing at run/topological/cycle paths, relying on builder invariants that duplicate IDs already weaken. in-degree and workflow step/revision counters use unchecked addition. Writing pipeline performs unchecked usize-to-i64 conversions and i64 increment. SequentialWorkflow documentation slices output by byte boundary and can panic for Unicode. Most unwrap/panic occurrences are tests/doctests.
- Log/artifact: source anchors in finding F-WFL-01-P2-05

## Deviations

Practical overflow needs extreme input and is lower priority; the UTF-8 defect is in public documentation rather than runtime code.

## Conclusion

Supports F-WFL-01-P2-05 as a combined hard-rule cleanup finding. Does not claim observed panic execution.

## Follow-Up

Future invalid/large topology and Unicode doctest checks.

## Correction - 2026-08-12

The finalized task report assigns the panic/UTF-8/arithmetic finding ID
`F-WFL-01-P3-01`; the earlier reference to `F-WFL-01-P2-05` in this
immutable attempt is superseded. Evidence and conclusion are unchanged.
