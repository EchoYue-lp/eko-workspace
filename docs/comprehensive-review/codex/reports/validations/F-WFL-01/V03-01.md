# F-WFL-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Graph and DAG builders reject ambiguous identities and invalid node/edge/interrupt topology before execution.

## Method

```text
1. Inspect every GraphBuilder and DagWorkflowBuilder insertion and build validation.
2. Trace duplicate node IDs, duplicate fan-out targets, empty fan-out, finish IDs,
   interrupt IDs, conditional targets, and max-step accounting.
3. Compare with existing builder tests.
```

## Expected Result

Node identity is unique, all static references validate, runtime routes cannot escape the compiled graph, and every executed node counts against the bound.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: ordinary edge endpoints, entry, cycle, and multiple outgoing edges receive checks. Graph node insertion silently overwrites duplicate names. DAG keeps duplicate IDs in node_order but collapses agents into a HashMap, permitting repeated execution of the last agent. Finish/interrupt IDs, empty/duplicate parallel targets are not rejected. Conditional targets are checked only by a later node lookup. Parallel branch steps are added inside fan-out without rechecking max_steps.
- Log/artifact: source anchors in finding F-WFL-01-P1-03

## Deviations

No dynamic malformed-graph fixture was run under the static-only directive.

## Conclusion

Supports F-WFL-01-P1-03 and the missing-test inventory. It does not establish every malformed topology's exact runtime output.

## Follow-Up

Future table-driven builder regressions.
