# F-WFL-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

A checkpoint has stable graph/run/attempt identity and can be consumed only by its owning graph at its persisted continuation.

## Method

```text
1. Inventory Checkpoint identity, graph_name, current_node, path, step_count,
   parent_checkpoint_id, branch, pending_action, and interrupt_type.
2. Trace resume, resume_with_state, restore_to_checkpoint, and branch_from.
3. Search for graph-name/version checks, persisted existence/claim checks, attempt IDs,
   lineage propagation, and stale/replayed checkpoint rejection.
```

## Expected Result

Resume validates graph definition/identity, atomically claims one continuation attempt, rejects stale/replayed snapshots, and preserves lineage.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: checkpoint ID is a UUID and branch_from records a parent, but no graph definition version/run/attempt identity exists. `resume` accepts any public cloned/deserialized Checkpoint value, never checks graph_name or store presence, and deletes by ID only after completion. The same snapshot can therefore execute repeatedly or on another graph containing the named node. Successive interrupt checkpoints are created with `Checkpoint::new`, dropping parent lineage.
- Log/artifact: source anchors in finding F-WFL-01-P1-01

## Deviations

This is static proof of missing guards and reachable execution; an external-effect replay fixture remains future validation.

## Conclusion

Supports F-WFL-01-P1-01. It aligns with B-REF stable run/attempt recovery consensus without duplicating F-RCT-05's ReAct-specific checkpoint findings.

## Follow-Up

Future stale/replay/cross-graph resume regression.
