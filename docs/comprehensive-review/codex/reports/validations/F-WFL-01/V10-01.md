# F-WFL-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Existing tests cover identity, topology, concurrency, checkpoint corruption, resume/replay, interrupts, cancellation, and partial failure sufficiently to support the advertised workflow contract.

## Method

```text
1. Inventory every #[cfg(test)] test in workflow graph/state/node/checkpoint/DAG/
   sequential/concurrent/pipeline files and root DSL/loader.
2. Map tests to the F-WFL-01 requested invariant matrix.
3. Do not execute tests under the review-stage directive.
```

## Expected Result

Each critical recovery/concurrency edge has a focused regression; absence is recorded rather than inferred as passing.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: tests cover happy linear/conditional/loop/parallel execution, basic max steps, some unknown edges, cycle/topological helpers, state happy round-trip/merge, memory/file happy checkpoint operations, one before-interrupt resume_with_state, loader parsing, and pipelines. Missing: duplicate identities, invalid finish/interrupt/fan-out, scalar merge conflicts, sibling failure/cancel, all resume execution variants, after/fan-out/tool/user interrupt, cross-graph/replayed/stale checkpoints, lineage, corrupt/truncated/path IDs, concurrent saves, cleanup failure, timeouts, and Sequential/Concurrent/DAG error behavior.
- Log/artifact: this report

## Deviations

Static inventory only; passing source tests were not claimed.

## Conclusion

Supports `needs_evidence` status and every future regression listed in the task report.

## Follow-Up

Primary should schedule the smallest focused regressions after review synthesis.
