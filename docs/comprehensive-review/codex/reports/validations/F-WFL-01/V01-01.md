# F-WFL-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Fixed workflow orchestration is a generic framework capability distinct from the dynamic TaskRun/PlanTask/SubagentRun authority, while its internal public surfaces can be inventoried without adding another task model.

## Method

```text
1. Search both repositories for Graph, StateGraph, DagWorkflow, SequentialWorkflow,
   ConcurrentWorkflow, TaskRun, PlanTask, TaskPlan, TaskManager, RuntimeDagExecutor,
   approval-state names, checkpoint, resume, and pipeline builders.
2. Compare ownership, mutable state, execution inputs, and exported APIs.
3. Read F-TSK-01 and B-REF-01 dependency boundaries.
```

## Expected Result

Workflow represents reusable, caller-defined fixed topology; it neither owns EKO Plan artifacts nor creates plan-approval runtime states.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: workflow APIs live in `echo-orchestration/src/workflow` with thin root DSL/loader adapters. Dynamic task authorities live separately under task modules/EKO. No workflow code references TaskRun, PlanTask, TodoItem, or plan approval states. Graph/StateGraph and pipeline variants are multiple fixed-topology APIs, not an EKO Task authority.
- Log/artifact: source anchors in the task report

## Deviations

Graph and DagWorkflow duplicate some topology/execution mechanics and expose materially different concurrency/recovery contracts; later validations assess those defects rather than proposing a new authority.

## Conclusion

Supports retaining workflow as a framework capability and forbids solving its recovery defects by introducing Plan approval states. Supports the layering section, not behavioral correctness.

## Follow-Up

V02-01.
