# F-WFL-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Graph parallel fan-out executes branches concurrently with isolated input snapshots, deterministic merge, and atomic failure semantics.

## Method

```text
1. Trace NextStep::Parallel through run, run_until_interrupt, resume, and run_stream.
2. Compare SharedState::fork/deep_merge behavior and branch input creation order.
3. Inspect failure/cancellation exits and parallel tests.
```

## Expected Result

All branches start from one immutable pre-fan-out snapshot; merge conflicts have an explicit deterministic policy; failure does not publish a partial combined state as successful or silently retain an order-dependent prefix.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: Graph fan-out is a sequential `for` loop. Each later fork is created after earlier results were merged, so branches observe prior sibling writes. Deep merge overwrites scalar conflicts in target order. A later branch error returns after earlier branch state/side effects were already merged. Resume uses the shared state directly rather than fork/deep_merge, diverging further from run/run_stream.
- Log/artifact: source anchors in finding F-WFL-01-P1-04

## Deviations

The source comment admits sequential execution, but module/API descriptions still advertise simultaneous parallel fan-out and the order-dependent state contract remains material.

## Conclusion

Supports F-WFL-01-P1-04. It does not measure timing or external side effects dynamically.

## Follow-Up

Future conflict/partial-failure fixture across all four execution methods.
