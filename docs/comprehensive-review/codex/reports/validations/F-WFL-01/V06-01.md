# F-WFL-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Before/after/parallel/tool interrupts persist an exact continuation and approval outcome without skipping work or inventing a plan lifecycle.

## Method

```text
1. Trace run_until_interrupt checkpoint construction for BeforeNode and AfterNode.
2. Trace resume decisions and next-step reconstruction for Single, Parallel, and End.
3. Search all repositories for ToolApproval/UserRequest interrupt construction,
   pending_action writes, and live HumanLoop integration.
```

## Expected Result

A checkpoint encodes the exact next continuation, including pending fan-out; rejection is a distinct terminal/aborted outcome; advertised tool/user interrupts have a live producer/consumer path.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: after a node whose next step is Parallel, checkpoint construction stores only `then`, discarding every target; resume jumps directly to convergence. Rejected and Deferred return `Completed` and leave the checkpoint stored. ToolApproval/UserRequest/pending_action are public shapes but no graph execution path constructs or consumes them; ToolApproval exists only as a standalone InterruptState constructor. Node Before/After is the only live interrupt path.
- Log/artifact: source anchors in findings F-WFL-01-P1-02 and F-WFL-01-P2-03

## Deviations

F-HITL-01 separately owns the canonical tool permission pipeline. This validation only establishes that workflow's advertised ToolApproval checkpoint is not integrated; it does not duplicate HITL decision defects.

## Conclusion

Supports F-WFL-01-P1-02 and P2-03. No Plan approval state is recommended.

## Follow-Up

Future fan-out resume and reject/defer cleanup regressions; F-HITL-01 owns tool policy integration.

## Correction - 2026-08-12

The finalized task report assigns the inert interrupt finding ID
`F-WFL-01-P2-01`; earlier references to `F-WFL-01-P2-03` in this immutable
attempt are superseded. Evidence and conclusion are unchanged.
