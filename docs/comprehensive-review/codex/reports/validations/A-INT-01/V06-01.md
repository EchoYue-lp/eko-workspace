# A-INT-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Deleting a conversation closes and removes its conversation-scoped Browser session metadata and provider state.

## Method

```text
Trace BrowserSessionManager create/restore/close APIs and every close/remove call.
Trace registered delete_conversation cleanup for messages, tool executions,
artifacts, Browser session, and Browser approval provider.
```

## Expected Result

Conversation deletion removes all conversation-scoped Browser state or reports cleanup failure.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: Browser sessions are keyed/persisted by conversation, but only process shutdown calls `close_all`; no per-conversation close/remove API exists. Deletion cleans store/tool/artifact data only. Provider cleanup absence is already canonical A-HITL-01-P2-07.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/browser/session.rs:168-218`, `:495-521`; `browser/mod.rs:149-166`, `:194-203`; `src/tauri/commands/conversations.rs:585-639`.

## Deviations

Closed metadata is intentionally restored for continuity; the finding is deletion semantics, not normal restart persistence.

## Conclusion

Supports A-INT-01-P2-05 and defers provider ownership to A-HITL-01.

## Follow-Up

Expose one idempotent conversation Browser cleanup operation and call it from canonical conversation deletion.
