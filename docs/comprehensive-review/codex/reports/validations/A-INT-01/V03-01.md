# A-INT-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Fetching a redacted MCP config and saving an unrelated edit preserves existing credentials.

## Method

```text
Trace get_mcp_config -> redact_mcp_config_secrets -> frontend JSON editor ->
update_mcp_config -> serde_json::from_value -> in-memory replacement/reconnect.
Search for placeholder merge/preserve handling for `<redacted>`.
```

## Expected Result

Redacted placeholders are presentation-only and cannot replace stored secrets.

## Result

- Exit code: 0 for inspections; claim failed.
- Duration: static inspection.
- Summary: every env value and credential header/query value is replaced by `<redacted>` in the returned object. The frontend edits that object and update parses/replaces it wholesale. No merge with the secret-bearing source exists.
- Log/artifact: `echo-agent-cli/src/tauri/commands/mcp.rs:377`, `:402`, `:477`; `web-frontend/src/components/mcp/McpPanel.tsx:24`, `:36`, `:50`.

## Deviations

Secret exfiltration is correctly prevented; the defect is destructive round-trip semantics.

## Conclusion

Supports A-INT-01-P1-02.

## Follow-Up

Use masked-field patch semantics or an opaque secret reference; add a round-trip regression.
