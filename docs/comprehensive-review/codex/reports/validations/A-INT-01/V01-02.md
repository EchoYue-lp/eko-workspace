# A-INT-01 / V01 / Attempt 02

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Browser, MCP, and LSP application authorities and their real registration paths can be identified without inventing a second integration layer.

## Method

```text
Search both repositories for BrowserRuntime, BrowserSessionManager, McpConfigFile,
load_mcp_config, connect/disconnect/toggle/update_mcp_config, LspManager,
register_lsp_tools, LspStatusTool, plugin LSP merge, and registered IPC callers.
Read exact definitions and registrations in runtime.rs, infra.rs, state.rs,
browser/*, tauri/commands/{browser,mcp}.rs, tauri/mod.rs, and frontend endpoints.
```

## Expected Result

One EKO Browser runtime, one Agent MCP client registry, and one shared LSP manager feed live tools/surfaces.

## Result

- Exit code: 0 for corrected searches and reads.
- Duration: static inspection.
- Summary: Browser has one process runtime; MCP live clients belong to the Agent while GUI also keeps an application config mirror; LSP tools and plugin reload share one manager handle. No dedicated Tauri LSP command surface exists.
- Log/artifact: task report Current Path.

## Deviations

MCP has two configuration representations, and LSP root/config are snapshot fields. These are assessed in V03-V05.

## Conclusion

Supports the layering and duplicate-authority map. It does not validate protocol behavior owned by F-INT-01/F-INT-02.

## Follow-Up

V02 through V06.
