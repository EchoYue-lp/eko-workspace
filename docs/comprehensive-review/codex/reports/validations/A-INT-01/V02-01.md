# A-INT-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

The MCP configuration shown and saved by GUI is the same durable configuration loaded into the Agent at bootstrap.

## Method

```text
Trace AgentRuntime::bootstrap -> infra::load_mcp_config -> Agent clients.
Trace AppState::from_shared -> PluginState::mcp_config.
Trace get_mcp_config/update_mcp_config -> frontend McpPanel load/save.
Search every application assignment/read of PluginState::mcp_config and every
load_mcp_config caller.
```

## Expected Result

The editor reads the configured source of truth and a successful save durably updates it.

## Result

- Exit code: 0 for inspections; claim failed.
- Duration: static inspection.
- Summary: bootstrap loads a selected file directly into the Agent, while AppState initializes `mcp_config` to default and never copies the loaded file. GUI update replaces only the in-memory mirror and reconnects clients; it performs no file write despite saying “Persist”.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/runtime.rs:109`, `infra.rs:1069`, `state.rs:489`, `src/tauri/commands/mcp.rs:377`, `:477`.

## Deviations

Explicit `connect_mcp_server` is intentionally live-only; the failure concerns the full config editor's durable-save contract.

## Conclusion

Supports A-INT-01-P1-01.

## Follow-Up

Add one application-owned durable MCP config authority and delete the mirror.
