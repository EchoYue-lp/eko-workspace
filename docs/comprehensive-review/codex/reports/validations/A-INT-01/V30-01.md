# A-INT-01 / V30-01: Primary acceptance sample

> Status: passed
> Executor: Codex primary reviewer
> Execution date: 2026-08-13
> Method: static source inspection only; no build, test, fixture, network, or source mutation

## Claim

The delegated A-INT-01 findings are supported by current production call paths,
respect the local-product threat model, and do not duplicate the framework
protocol findings.

## Evidence Sampled

- `echo-agent-cli/echo-agent-app-core/src/runtime.rs`: bootstrap loads MCP
  directly into the Agent, constructs the LSP manager from a bootstrap root,
  and registers the shared LSP tools.
- `echo-agent-cli/src/tauri/commands/mcp.rs`: GUI reads and replaces the separate
  `PluginState` config, masks credentials in the same document later accepted
  for replacement, and launches untracked disconnect/reconnect tasks without a
  generation check.
- `echo-agent-cli/echo-agent-app-core/src/plugin_runtime.rs`: plugin LSP rebuild
  clones the captured base config and uses the captured `lsp.project_root` even
  though a separate helper can read the current Agent working directory.
- `echo-agent-cli/echo-agent-app-core/src/state.rs`: workspace switch/exit update
  working directory, memory, and other resources but do not replace or rebind
  the shared LSP manager.
- `echo-agent-cli/src/tauri/commands/conversations.rs` and
  `echo-agent-cli/echo-agent-app-core/src/browser/session.rs`: deletion removes
  conversation/tool/artifact records while Browser sessions remain keyed by
  conversation until broader runtime shutdown.

## Result

All five findings were independently reproduced from source anchors. Direct
user Browser/MCP/LSP actions are not treated as requiring Agent automation
permission, so the conclusions remain aligned with EKO's local trusted-user
boundary. A-HITL owns launcher/provider over-gating, and F-INT owns protocol
semantics; no duplicate finding was accepted here.

No other reviewer directory was read. Concurrent framework worktree changes
were excluded; A-INT-01 conclusions rely on clean CLI source at commit
`b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`.
