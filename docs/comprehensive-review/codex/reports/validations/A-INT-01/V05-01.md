# A-INT-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

LSP configuration and project root follow EKO workspace switch/exit transitions.

## Method

```text
Trace register_lsp_tools root/config snapshot -> PluginLspRuntime fields ->
prepare_lsp/reload. Compare AppState::switch_workspace and exit_workspace updates
for Agent, Pool, memory, plugin runtime, and LSP manager.
```

## Expected Result

After a workspace transition, LSP processes and tools use the new root and nearest `.lsp.yaml`.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: bootstrap captures `base_config` and `project_root`. Plugin discovery computes a dynamic root, but `prepare_lsp` always rebuilds with the captured base config/root. Workspace switch/exit updates Agent and Pool working directories and memory but never rebinds LSP.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/runtime.rs:499-591`, `plugin_runtime.rs:25-42`, `:946-990`, `state.rs:844-1031`, `:1064-1181`.

## Deviations

Framework request/URI semantics remain delegated to F-INT-01/F-INT-02.

## Conclusion

Supports A-INT-01-P1-04.

## Follow-Up

Add one workspace-generation transition that atomically re-discovers and swaps LSP state.
