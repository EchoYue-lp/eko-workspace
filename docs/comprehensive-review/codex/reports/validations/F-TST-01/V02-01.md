# F-TST-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex reports only

## Claim

Public testing documentation tells external consumers how to enable the gated API and links to a real runnable example.

## Method

```text
Compare Cargo.toml testing feature and example required-features with src/lib.rs
gates and bilingual docs/12-mock.md; search examples and git history for
demo16_testing.rs and for --features testing instructions.
```

## Expected Result

The guide declares `features=["testing"]`/`--features testing`, and every referenced example target exists.

## Result

- Exit code: 0 for source/history searches; no-match searches for the referenced file returned 1.
- Duration: under one second.
- Summary: the module is absent without `testing`, but both guides directly import it without dependency/feature instructions. Both end with `cargo run --example demo16_testing`; the file and Cargo target do not exist. Other mock examples correctly declare the feature.
- Log/artifact: this report.

## Deviations

F-API-01-P2-05 separately owns a failing testing rustdoc caused by `CompressionInput`; this validation does not duplicate it.

## Conclusion

Supports F-TST-01-P3-05: an external consumer following the dedicated guide cannot use the advertised entry unchanged. This attempt remains excluded because it read a non-dependency report; V02-02 independently supports the same source conclusion.

## Follow-Up

Document feature enablement and point to a current required-feature example or restore one maintained example.
