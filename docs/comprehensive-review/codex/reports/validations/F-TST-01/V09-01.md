# F-TST-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex reports only

## Claim

`MockTool` can faithfully script the public Tool schema, result, context, cancellation, error and stream contracts advertised by the Mock guide.

## Method

```text
Compare MockTool fields/methods with Tool, ToolContext, ToolResult,
ToolResultKind, ToolFailure and ToolStreamEvent; inventory current MockTool
tests/callers and bilingual guide claims for parameter parsing/error handling.
```

## Expected Result

The mock can validate its configured schema, observe context/cancel, return either trait-level error or arbitrary typed result, and script streaming progress/output/terminal order.

## Result

- Exit code: not-applicable; static contract matrix.
- Duration: under one second per inspection.
- Summary: `with_parameters` only changes published JSON; MockTool inherits always-Ok validation. Responses are limited to `ToolResult::success(text)` and `ToolResult::error(text)`. It cannot return `Err`, structured failure/category, JSON/binary/kind/truncation/metadata, observe ToolContext/cancel, opt into serial/timeout traits, or emit stream progress/output. Existing shared-mock tests mostly exercise registration or successful text cycles; pipeline uses dedicated local streaming tools for stream cases.
- Log/artifact: this report.

## Deviations

Generic production schema-validation and cancellation defects remain owned by F-EXT-01. This validation concerns whether the public mock can verify a fixed contract.

## Conclusion

Supports F-TST-01-P2-04: the guide's parameter/error coverage claim exceeds MockTool's observable script surface.

## Follow-Up

Add arbitrary `Result<ToolResult>` and `ToolStreamEvent` scripts, context capture, validation hooks and explicit policy flags; keep one canonical Tool contract.
