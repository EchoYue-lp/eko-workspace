# F-TST-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex reports only

## Claim

`MockLlmClient` can script the provider-neutral streaming, usage, error, cancellation and ordering contract consumed by ReAct.

## Method

```text
Field/variant compare MockLlmResponse/PopResult/chat_stream with LlmClient,
ChatChunk and live run_think/direct_answer loops; enumerate constructible stream
sequences for content, reasoning, tool deltas, usage, errors and delays.
```

## Expected Result

Tests can enqueue an ordered stream of arbitrary `Result<ChatChunk>` items, including split deltas, standalone final usage, mid-stream errors, idle delays and cancellation.

## Result

- Exit code: not-applicable; static contract matrix.
- Duration: under one second per inspection.
- Summary: one queued response becomes exactly one immediately ready `ChatChunk`; finish reason and optional usage share that chunk. Errors occur only before a stream is returned. Delay occurs only before construction. No public script expresses multi-chunk content/reasoning/tool argument fragments, usage-only final chunks, delayed inter-chunk idle, empty successful streams, or an error after visible deltas. Cancellation is returned as untyped `ReactError::Other` only during pre-stream delay.
- Log/artifact: this report.

## Deviations

Pure `process_stream_chunk` tests cover one reasoning chunk and argument repair; they do not exercise run-loop order/error/usage across a stream. Provider transport fixtures in F-LLM-01 cover upstream adapters but do not replace a reusable neutral mock.

## Conclusion

Supports F-TST-01-P2-01. Existing happy-path ReAct tests cannot establish shape-independent stream lifecycle behavior.

## Follow-Up

Use a script of timed `Result<ChatChunk>` items and a typed cancellation outcome.
