# F-TST-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex reports only

## Claim

Production-module tests using shared mocks cover the real neutral contracts and do not overstate what a mock proves.

## Method

```text
Inventory MockLlmClient/MockAgent/MockTool callers in React stream/compact,
Subagent executor/team and React tests; inspect assertions and comments for
stream ordering, usage, cancellation, terminal events and exact call counts;
identify dedicated local stubs used where shared mocks are insufficient.
```

## Expected Result

Tests claiming streaming/cancellation fidelity use a mock capable of the relevant timing/event shape, and strict call counts catch unexpected retries/calls.

## Result

- Exit code: 0 for searches with matches.
- Duration: under one second per bounded inventory.
- Summary: MockLlmClient is heavily used by the live ReAct stream loop, including usage, tool cycles, cancellation and steer. The cancellation test says it proves propagation to the LLM layer, but its delay explicitly selects cancellation unlike real provider pending I/O (F-LLM-01-P1-02). Usage tests only cover usage co-located with content/tool terminal. Shared MockAgent drives broad orchestration paths but cannot emit the event matrix consumed by `execute_agent_streaming`; dedicated local agents correctly cover cancellation and usage in a few tests. Exact LLM call assertions exist, while shared Agent/Tool mocks remain permissive on surplus calls.
- Log/artifact: this report.

## Deviations

This validation does not claim all tests are invalid; many message/config/tool-cycle assertions are appropriately supported.

## Conclusion

Confirms concrete false-confidence impact for P2-01 through P2-03 and bounds it to unsupported shapes.

## Follow-Up

Relabel existing cancellation claims and migrate order/error/usage/cancel tests to strict event scripts.
