# F-TST-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex reports only

## Claim

Unexpected extra MockAgent/MockTool calls and malformed public helper construction fail explicitly rather than fabricate success or panic.

## Method

```text
Inspect response-queue exhaustion in mock_agent.rs/mock_tool.rs and constructor
validation/panic/UTF-8/index/arithmetic operations in all src/testing modules;
compare MockLlmClient exhaustion and public error contracts.
```

## Expected Result

Strict scripted mocks fail on exhaustion by default; permissive repetition/default success is explicit opt-in; public invalid configuration returns a typed result.

## Result

- Exit code: not-applicable; static branch inspection.
- Duration: under one second.
- Summary: MockAgent exhaustion fabricates `"mock agent response"`; MockTool exhaustion fabricates successful `"mock response"`. Neither exposes remaining count or strict mode. MockLlmClient correctly returns EmptyResponse. Public `MockEmbedder::new(0)` calls `assert!`, violating repository no-panic policy. Other inspected string/collection operations are UTF-8/index safe and lock poison is recovered.
- Log/artifact: this report.

## Deviations

Some structural registry tests intentionally need an unscripted placeholder and do not execute it. Fail-closed default should retain an explicit permissive constructor for those uses.

## Conclusion

Supports F-TST-01-P2-03 and F-TST-01-P3-06.

## Follow-Up

Make strict exhaustion default with explicit repeat/default helpers; make embedder construction fallible or clamp to a documented valid dimension.
