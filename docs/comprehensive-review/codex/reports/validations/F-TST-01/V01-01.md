# F-TST-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex reports only

## Claim

Framework mock definitions, exports, feature gates, duplicates, and real test consumers are mapped.

## Method

```text
Search both repositories for MockLlmClient, MockAgent, FailingMockAgent,
MockTool, MockEmbedder, feature="testing", pub mod testing, and testing examples;
separate definitions, cfg exports, docs/examples, inline tests and production-module tests.
```

## Expected Result

One coherent public test utility surface exists under an explicit feature and duplicate helpers are bounded to crate-private tests.

## Result

- Exit code: 0 for searches with matches.
- Duration: under one second per bounded search.
- Summary: root `testing` and prelude exports are correctly gated by `cfg(any(test, feature="testing"))`; five public mocks exist. Examples using them declare `required-features=["testing"]`. `echo_state` has a separate cfg(test)-private MockEmbedder with the same algorithm, a localized duplicate rather than a public competing API. Root mocks drive ReAct, compression, Subagent and Team tests.
- Log/artifact: this report.

## Deviations

CLI non-use was not treated as dead-code evidence.

## Conclusion

Public test utilities are reasonable independent framework APIs. Supports layering and reachability; semantic fidelity is tested separately.

## Follow-Up

Consider sharing the embedding helper internally, without moving EKO policy into the framework.
