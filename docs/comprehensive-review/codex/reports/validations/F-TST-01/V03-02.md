# F-TST-01 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The corrected MockLlm stream script models both neutral chunk structure and the
timing/cancellation lifecycle consumed by ReAct.

## Method

Compare commit `9b0e0faf..3aa79299`, then statically trace StreamChunk,
with_stream_script, chat_stream request cancellation and the returned stream.
Inspect the new whole-loop fixtures without running them.

## Expected Result

Scripts express arbitrary ordered chunk/error/terminal steps with optional
inter-step waits; request cancellation remains live while the consumer is
waiting for any later step.

## Result

- Exit code: 0 for static reads; full lifecycle invariant still failed.
- The new commit fixes the old structural defects: StreamChunk expresses
  multiple Delta values, a separate Terminal with finish reason/usage and an Err
  after visible deltas. Plain response/tool builders also emit separate delta and
  terminal chunks. The old V03 statements that every response was one chunk and
  that mid-stream errors were impossible are now obsolete.
- Script chunks are converted to immediately-ready `futures::stream::iter`
  values. No step carries a delay/pending gate, and the request cancellation
  token is consumed only by the optional delay before the stream is returned.
  Once a partial delta is visible, later waiting/cancellation cannot be modeled.
- Cancellation still returns untyped ReactError::Other. The new truncated-stream
  test is present but explicitly ignored for a later runtime fix; it is coverage
  inventory, not executed evidence.

## Deviations

No test/build command was run. This attempt supersedes V03-01 only for current
source semantics; the first attempt remains immutable repair-before evidence.

## Conclusion

Failed in narrowed form. Supports the revised F-TST-01-P2-01 only; confirms the
structural half of the old finding is fixed.

## Follow-Up

Use timed/cancel-aware stream steps and typed cancellation; keep the new neutral
delta/terminal/error representation.
