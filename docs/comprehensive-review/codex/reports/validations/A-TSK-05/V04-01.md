# A-TSK-05 / V04 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

Ordinary EKO integration and cleanup protect user-staged, overlapping dirty, conflicting, and late-arriving isolated changes.

## Method

```text
Inspect integrate_existing_worktree and cleanup_managed_worktree in order:
staging isolated changes, ownership validation, active-Git-operation guard,
main index guard, unstaged/untracked overlap guard, merge-tree preflight,
merge abort/preserve behavior, and clean-only checkout/branch deletion.
Compare the exact in-module tests for each guard.
```

## Expected Result

No automatic merge may capture user-staged work, overwrite dirty overlapping paths, leave a failed merge operation active, or delete a dirty/unique isolated checkout.

## Result

- Exit code: 0
- Duration: under one second per static inspection batch
- Summary: any staged main path blocks integration (`worktree.rs:705-713`); unstaged/untracked overlap with writer changes blocks (`:714-725`); merge-tree preflight precedes merge (`:728-754`); owned merge failure is aborted and the isolated checkout preserved (`:756-783`). Cleanup relies on non-forced Git removal and branch deletion only after no unique work. Existing tests exercise ownership violation, merge conflict, dirty overlap, staged user changes, late dirty content, and late unique commits (`:1631-1668`, `:1767-1901`).
- Log/artifact: none

## Deviations

Disjoint unstaged/untracked user changes are permitted to remain while Git merges unrelated paths. The current merge command does not stage those paths, so this is compatible with the stated exact-overlap policy.

## Conclusion

The inspected ordinary dirty-tree/data-loss guards pass. This does not cover branch drift, crash-held locks, or cancellation after merge begins.

## Follow-Up

V05-V07 cover those gaps.
