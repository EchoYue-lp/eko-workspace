# A-TSK-05 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

EKO data workspaces retain a durable owner and a complete locator/lineage handoff until downstream collection, then have a deterministic cleanup path.

## Method

```text
Trace EkoDataWorkspaceFactory create/finalize through committed framework
DataWorkspaceHandle and SubagentExecutor. Inspect TempDir ownership, retained
path, listing recursion, result envelope, downstream workspace creation, and
repository-wide sweep/cleanup references. Compare EKO data-workspace tests.
```

## Expected Result

A retained workspace should be keyed to TaskRun/PlanTask/SubagentRun, expose an unambiguous locator plus complete manifest/lineage to authorized downstream collection, and be removed only after explicit terminal ownership transfer.

## Result

- Exit code: 0
- Duration: under one second per static inspection batch
- Summary: EKO creates a random temp directory then consumes `TempDir` with `keep()` (`worktree.rs:1414-1429`). The returned handle owns only `PathBuf`; finalize lists lossy top-level names without recursion, full paths, content hashes, sizes, or source execution identity (`:1437-1464`). The committed framework appends that listing, not the directory path, to ordinary model output (`SubagentExecutor:1857-1879`). Each later isolated Subagent receives a different random workspace, and no `eko-data-*` sweep/ledger exists. Tests verify only creation, one filename listing, and empty output (`worktree.rs:2128-2170`).
- Log/artifact: none

## Deviations

The absolute path is injected into the producing Subagent prompt and logged, but neither is a durable structured handoff to downstream collection. The module comment says handle drop cleans the directory, contradicting `keep()` and its own later “future sweep” comment.

## Conclusion

The claim fails and supports A-TSK-05-P1-04. This finding is limited to workspace ownership/locator/cleanup; A-TSK-06 owns review acceptance and parent artifact projection semantics.

## Follow-Up

Define one TaskRuntime-owned workspace manifest/lease and cleanup lifecycle, then let A-TSK-06 consume its structured artifact references.
