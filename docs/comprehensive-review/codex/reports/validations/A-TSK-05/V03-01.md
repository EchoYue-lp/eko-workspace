# A-TSK-05 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

Concurrent writer selection and final integration enforce declared exact-file ownership without silently accepting out-of-scope changes.

## Method

```text
Inspect PlanTask file normalization and FileOwnership classification; trace
ready-wave conflict selection and per-file locks; inspect integration-time
changed-file enumeration and Known-ownership validation; compare existing
ownership/conflict tests.
```

## Expected Result

Known writers may share a wave only when normalized file sets are disjoint, unknown writers serialize, and the actual staged diff must remain within declared ownership before main checkout mutation.

## Result

- Exit code: 0
- Duration: under one second per static inspection batch
- Summary: planner normalization rejects absolute/parent/glob-like paths and distinguishes ReadOnly/Known/Unknown ownership. The selected ready wave serializes unknown writers and conflicting exact owners; executor per-file mutexes are an additional physical guard. Integration stages the isolated checkout, computes NUL-delimited changed files, and rejects any path outside a Known set before merge (`worktree.rs:644-653`, `:809-826`). Tests cover stable identity, disjoint integration, declared-ownership violation, and conflict preservation (`:1544-1554`, `:1727-1841`).
- Log/artifact: none

## Deviations

Unknown ownership intentionally permits any diff but is serialized against other writers. That is an explicit conservative policy, not a bypass of concurrent exact ownership.

## Conclusion

The static ownership-conflict invariant passes. This validation does not establish target-branch, cancellation, or restart ownership.

## Follow-Up

V04-V07 inspect those remaining boundaries.
