# A-TSK-05 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

Cancellation or claim supersession cannot permit an obsolete integration to mutate the authoritative checkout.

## Method

```text
Trace resolve_dispatch's claim checks, integrate_reviewed_task, dispatcher
cancel checks/merge-lock wait, spawn_blocking integration, merge command, and
post-integration claimed completion CAS. Compare task/run cancellation behavior
and search tests for a barrier during blocking merge.
```

## Expected Result

An integration side effect should be claim-fenced at its durable commit point; once cancelled/superseded, either no authoritative merge occurs or settlement durably records that exact merge before returning.

## Result

- Exit code: 0
- Duration: under one second per static inspection batch
- Summary: EKO checks the claim at `executor.rs:1518-1519`, then calls integration. The dispatcher checks cancellation only before root lookup, while waiting for the process-local merge lock, and immediately before `spawn_blocking` (`:912-933`). Once `integrate_fork_worktree` starts at `:961-969`, no cancel or claim fence is consulted before its `git merge`. Only afterward does `resolve_dispatch` perform `set_claimed_status(Completed)` (`:1534-1543`), which may return Superseded. Thus cancellation or another valid state transition can win while the obsolete attempt still commits changes.
- Log/artifact: none

## Deviations

A process-local repository mutex serializes concurrent EKO merges, but it is not a TaskClaim fence and cannot retract an already-started blocking merge.

## Conclusion

The claim fails and supports A-TSK-05-P1-01. The general completion/cancellation race remains canonical in A-TSK-03; this validation owns the additional irreversible Git side effect before final claim CAS.

## Follow-Up

Add a claim-bound integration transaction/idempotent settlement and a barrier regression at merge start.
