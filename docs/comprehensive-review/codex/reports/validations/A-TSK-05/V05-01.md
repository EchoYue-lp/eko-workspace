# A-TSK-05 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

A logical PlanTask retains one recoverable fork-worktree identity across retry, role edits, process interruption, and cleanup.

## Method

```text
Trace task_isolation_id/task_worktree_label into framework worktree_label;
inspect acquire_fork lock/reuse branches, lock reason, boot recovery hooks,
FORK_BRANCH_PREFIX enumeration, and TaskPatchEngine role mutability. Compare
stable/reuse/cleanup tests with crash-held-lock and role-change cases.
```

## Expected Result

Identity should derive from immutable TaskRun/PlanTask identity, an active lock should be bound to a recoverable claim/lease, and restart should safely reclaim or surface every retained fork without deleting dirty content.

## Result

- Exit code: 0
- Duration: under one second per static inspection batch
- Summary: the dispatch isolation value is stable `run_id:task_id`, but framework prefixes it with the selected Subagent name and EKO integration reconstructs the same label from mutable `agent_role` (`executor.rs:192-198`, `:935-937`, `:2213-2223`; committed framework executor `:1655-1670`). A Blocked task may update `agent_role` (committed `revisioned.rs:521-545`, `:640-642`). `acquire_fork` rejects any existing locked checkout (`worktree.rs:381-390`); its Git lock reason contains only a label (`:911-925`), with no claim/lease/process identity. Cleanup enumeration scans only `eko-unattended-`, not `eko-fork-` (`:1093-1107`). No boot-recovery path reclaims the latter.
- Log/artifact: none

## Deviations

Normal finalization unlocks a dirty fork and the existing retry test proves same-label reuse. It does not cover process death before finalize or a role edit before retry.

## Conclusion

The claim fails and supports A-TSK-05-P1-03: a crash can leave a permanent “already active” lock, and a role edit can redirect retry/integration to a new branch while preserving an unreachable old fork.

## Follow-Up

Add crash/restart and Blocked-role-edit regression scenarios during implementation.
