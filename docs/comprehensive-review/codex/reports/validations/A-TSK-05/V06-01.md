# A-TSK-05 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

Fork creation and integration preserve one explicit target checkout identity and remain idempotent if the user changes branches between execution and settlement.

## Method

```text
Inspect RunWorktree persisted/in-memory fields and create/acquire base selection;
trace integration target, merge base, ancestry, preflight, merge command, commit
trailer lookup, cleanup, and repeated-integration test. Search TaskRuntime
events/summaries for target ref/base persistence and a branch-switch test.
```

## Expected Result

The target ref and base chosen at worktree creation should be durably associated with the claim; integration should verify that same target and idempotency should not depend on whichever branch is currently checked out.

## Result

- Exit code: 0
- Duration: under one second per static inspection batch
- Summary: `RunWorktree.base` is in-memory only (`worktree.rs:364-373`). Creation uses the then-current `HEAD` (`:431-456`), while integration recomputes merge base/ancestry and executes `git merge` against the later current `HEAD` (`:646-647`, `:681-686`, `:728-772`). No target symbolic ref/base is persisted with TaskRun, claim, branch, or integration record. Idempotency searches only `git log HEAD` for the execution trailer (`:928-947`). The repeated-integration test stays on one branch (`:1939-1960`); no branch-switch case exists.
- Log/artifact: none

## Deviations

Changed files and an execution trailer are recorded after a successful merge, but neither identifies nor verifies the originally intended target ref.

## Conclusion

The claim fails and supports A-TSK-05-P1-02: switching the authoritative checkout can merge into the wrong branch or make an already-integrated execution appear to have no changes on retry.

## Follow-Up

Persist/verify the target ref and creation base, then add branch-switch/restart idempotency regressions.
