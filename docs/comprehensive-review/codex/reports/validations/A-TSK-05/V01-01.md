# A-TSK-05 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; framework externally dirty, and every inspected framework path was read from `HEAD`

## Claim

Worktree, file-ownership, merge, and data-workspace authorities can be located without finding a second TaskRun/PlanTask/SubagentRun execution graph.

## Method

```text
Search both repositories for WorktreeFactory, DataWorkspaceFactory,
RunWorktree, eko-fork, integrate_fork_worktree, file_ownership,
validate_changed_files, repo_merge_lock, isolation_id, execution_id,
workspace finalize, branch cleanup, and merge events. Classify generic
framework contracts, EKO product policy, and live adapters. Read dirty
framework files only through `git show HEAD:<path>`.
```

## Expected Result

One framework isolation contract should be adapted by one EKO Git/workspace policy, while generic Tool worktree actions and task execution remain separate owners.

## Result

- Exit code: 0
- Duration: under one second per static search batch
- Summary: framework `SubagentExecutor` owns invocation-scoped isolation and calls injected factories; EKO owns concrete Git fork acquisition/integration, file conflict policy, and temporary data-workspace policy. `RuntimeDagExecutor` remains the only PlanTask relation loop. Framework `echo-tools` worktree actions are user-invoked generic Tools and are canonical in F-EXT-02, not a second EKO TaskRuntime merge adapter.
- Log/artifact: source anchors in V02-V08

## Deviations

The application intentionally has separate Git-backed writer isolation and non-Git data workspaces. They serve different mutation classes and do not duplicate one TaskRun relation authority.

## Conclusion

The ownership map is sufficient for A-TSK-05. It supports application ownership of the findings without proposing another executor/store.

## Follow-Up

V02 traces the live registration and dispatch path.
