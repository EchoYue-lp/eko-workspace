# F-EXT-03 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Representative data-quality and statistics paths reject edge cardinalities
without panic or non-finite output.

## Method

```text
Inspect all direct indexing, denominators, finite filtering, percentile math,
integer casts and existing tests in data.rs, data_quality.rs and statistics.rs;
derive array indices for the minimum accepted IQR cardinality.
```

## Expected Result

The minimum accepted four-value IQR input accesses only indices 0..3 and all
small-cardinality paths return typed results.

## Result

- Exit code: not-applicable
- Duration: static proof
- Summary: execute accepts four finite values, then
  `sorted[3 * n / 4.min(n - 1)]` evaluates to `sorted[4]` for `n=4`, an
  out-of-bounds panic. Existing data-quality tests cover larger fixtures and do
  not exercise this boundary. The newer exploratory-statistics helpers do use
  finite filtering and checked access.
- Log/artifact: this report

## Deviations

Invariant failed; see `F-EXT-03-P1-04`.

## Conclusion

A deterministic public-tool panic exists for a valid minimum input. It was not
executed because dynamic fixtures were prohibited.

## Follow-Up

Future 0..5 value, NaN/infinity, constant series, and extreme-f64 matrix.
