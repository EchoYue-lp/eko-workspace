# F-EXT-03 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories returned empty `git status --short` output

## Claim

The ten delegated domain-tool findings are supported by current source, are
distinct from the generic Tool-contract review, and honor the local-product and
independent-framework boundaries in the repository instructions.

## Method

Primary reviewer independently sampled:

- readonly and normal feature registries plus actual permissions/side effects of
  the disputed RAG, Excel, and text tools;
- research remember/recall bodies and their live registration;
- RAG global embedder/store construction, all repository setter callers,
  chunk-size/overlap logic, and registration readiness;
- four-value IQR index arithmetic and direct vector access;
- SQL execution, pagination order, row type conversion, and result artifacts;
- core bounded `web_fetch` as a positive control and specialized image,
  enhanced-fetch, PDF, and scholarly response readers;
- root-vs-split tool facades and scholarly page/status contracts;
- delegated integrity evidence, links, and final source status.

No Cargo, rustc, test, build, network, or dynamic fixture command was run.

## Expected Result

Each finding must be source-conclusive and reachable, must not duplicate the
generic F-EXT-01 contract, and must not infer that a valid framework option is
dead merely because EKO does not use it.

## Result

- Exit code: 0 for all static read and integrity commands.
- Duration: not measured.
- The readonly registry includes two file writers and `rag_index`, contradicting
  its explicit physical non-mutation contract.
- Registered research memory acknowledges writes it never performs and recall
  always returns empty; registered RAG has no repository wiring for its required
  global embedder.
- Four accepted values make the IQR expression index `sorted[4]` directly.
- SQL calls `fetch_all` before pagination and converts valid non-string cells to
  the literal `"?"`.
- Specialized fetchers consume complete bodies before limits and do not retain
  full truncated content; the bounded core web fetch confirms a viable existing
  pattern. This is a local resource/data-integrity issue, not an added online
  permission boundary.
- RAG overlap/Unicode/store ownership, root facade omissions, and scholarly
  continuation/failure inconsistencies were independently confirmed.
- Framework database and SQLite remain valid reusable options; CLI non-use was
  not treated as deletion evidence.

## Deviations

Provider payloads, platform-specific SQL behavior, and resource peaks remain
future dynamic regressions under the static review policy.

## Conclusion

Passed. F-EXT-03-P1-01 through P1-07 and P2-08 through P2-10 are accepted. The
task is promoted to `complete`.

## Follow-Up

Implementation synthesis should first fix deterministic registry/fake-memory/
RAG-readiness/IQR defects, then design shared bounded database and network
result primitives while keeping EKO policy outside the reusable framework.
