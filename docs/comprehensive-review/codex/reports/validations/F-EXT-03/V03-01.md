# F-EXT-03 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

`register_readonly_tools` contains no tool implementation that writes durable
or shared state.

## Method

```text
Compare every readonly registry entry with Tool::permissions and execute body;
trace output paths and shared-state mutations for Excel, text, RAG, chart,
database, research, and media entries.
```

## Expected Result

The registry's documented physical readonly boundary excludes every file write
and shared-index mutation.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: the readonly registry includes `excel_to_csv` and `text_export`,
  which validate output paths, create directories, and write files. It also
  includes `rag_index`, which mutates the process-global vector store. Several
  are declared with `ToolPermission::Write`.
- Log/artifact: this report

## Deviations

Invariant failed; see `F-EXT-03-P1-01`.

## Conclusion

Readonly Subagents are not physically readonly when media or RAG is enabled.
This static result does not execute a write fixture.

## Follow-Up

Future registry membership and denied-write side-effect tests.
