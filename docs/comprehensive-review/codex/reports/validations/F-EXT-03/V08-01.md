# F-EXT-03 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Network/media/PDF tools enforce response bounds while reading and make every
truncated successful result recoverable as a complete artifact.

## Method

```text
Inspect Content-Length checks, streaming/body APIs, integer size conversion,
timeout/redirect configuration, ToolContext artifact use, output projection,
and cancellation-token consumption in web, media, PDF-fetch and scholarly
clients.
```

## Expected Result

Chunked/unknown-length bodies stop at a hard cap, arithmetic is checked, and a
truncation includes a model-visible complete artifact reference.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: core `web_fetch` is a positive bounded/artifact path. In contrast,
  `image_fetch`, enhanced image fetch, enhanced text fetch, research PDF fetch,
  and scholarly `json_response`/`text_response` consume `.bytes()`/`.text()` in
  full. `image_fetch` multiplies model-controlled MB by 1 MiB without checked
  arithmetic. Image tools expose only a short data-URI prefix, enhanced/PDF
  text truncates, and none persists the complete payload through ToolContext.
  Domain implementations do not consume cancellation.
- Log/artifact: this report

## Deviations

See `F-EXT-03-P1-07`; generic executor cancellation remains owned by
`F-EXT-01-P1-03` and is not duplicated here.

## Conclusion

Specialized network tools bypass the bounded recoverable pattern already used
by core web fetch. No network request was performed.

## Follow-Up

Future chunked oversized, lying Content-Length, timeout/cancel, redirect,
artifact-failure, and Unicode payload fixtures.
