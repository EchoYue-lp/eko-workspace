# F-EXT-03 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Research/search results expose source identity and a continuation mechanism,
and HTTP failures have truthful terminal categories.

## Method

```text
Compare SearchResult and ScholarlyWork fields, each research tool output,
provider totals/offset/page tokens, status handling, error construction and
existing parser tests. Trace model-visible output rather than metadata only.
```

## Expected Result

Every result has stable provider/source identifiers and URL/DOI where supplied;
when total exceeds returned results the model can continue; 4xx/429/5xx are not
reported as successful empty research.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: web results preserve title/URL/snippet and reusable clients preserve
  provider/provider_id plus scholarly identifiers. Public search tools return
  at most 100 records but expose no offset/cursor/page token. ArXiv does not
  check status before parsing the body; other legacy tools include bounded
  result metadata but read unbounded error bodies and do not classify retry
  categories. Only web_search attaches typed transient failure facts.
- Log/artifact: this report

## Deviations

See `F-EXT-03-P2-10`. The generic invisible PageInfo cursor remains owned by
`F-EXT-01-P1-04`.

## Conclusion

Basic citation provenance is present, but exhaustive research and uniform
network failure semantics are not.

## Follow-Up

Future multi-page provider fixtures and 400/401/404/429/500/malformed-body
matrix.
