# A-TSK-02 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

All EKO PlanTask authoring and update entry points apply one current Subagent/tool capability policy before graph commit.

## Method

```text
Compare the policy/service construction for primary-agent registration, GUI update_tasks,
pooled-agent construction, BackgroundTaskService::submit_dag, and commit_eko_task_plan.
Then trace post-registration skill/MCP/plugin/tool mutation paths.
```

## Expected Result

One EKO policy owner validates the same current capability set at every commit, including background DAG creation.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: primary and pooled registrations construct immutable `TaskCapabilityCatalog` snapshots. The GUI update command reconstructs a fresh snapshot per request. `submit_dag` calls `commit_eko_task_plan`, which constructs `DefaultTaskToolPolicy`; its `validate_candidate` is a no-op. Skills, MCP, plugins, LSP, and direct tool mutation can change the live registry after the original snapshot.
- Log/artifact: this report

## Deviations

The shared framework validator still rejects structural DAG defects; the failure is specifically EKO Subagent/tool capability policy.

## Conclusion

Claim failed and supports `A-TSK-02-P1-02`.

## Follow-Up

Use a single live EKO capability provider/service factory and cover create, update, background, load, unload, and disconnect transitions.
