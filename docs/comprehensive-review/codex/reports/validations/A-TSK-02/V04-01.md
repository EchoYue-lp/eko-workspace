# A-TSK-02 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

A rejected initial `task_create` leaves no durable TaskRun or externally visible lifecycle event.

## Method

```text
Trace TaskRevisionService::create_from_tool in statement order, then trace
EkoTaskToolPolicy::ensure_scope and create_prepared/finalize_and_validate/compare_and_commit.
Search TaskRuntimeStore for rollback/delete/remove-run support and inspect existing bootstrap tests.
```

## Expected Result

Capability/DAG validation and commit succeed atomically with run creation, or every failure rolls back the run and its lifecycle event.

## Result

- Exit code: not-applicable
- Duration: not recorded
- Summary: `create_from_tool` calls `ensure_scope` before task preparation, product capability validation, structural DAG validation, and graph CAS. EKO `ensure_scope` durably creates the run, optionally loses attachment-binding errors, transitions it to Running, and emits RunStarted. No delete/rollback API exists. A later invalid Subagent/tool/dependency/metadata or store failure therefore leaves a started run without revision 1.
- Log/artifact: this report

## Deviations

No dynamic fault fixture was run under the review-only rule. The current code order is unconditional and source-conclusive.

## Conclusion

Claim failed and supports `A-TSK-02-P1-01`.

## Follow-Up

Add fault injection at every post-bootstrap boundary when implementing the fix.
