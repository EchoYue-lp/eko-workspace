# B-DOC-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Current architecture/plan documentation uses current paths and the mandatory
Subagent-only terminology.

## Method

Searched non-generated Markdown outside the comprehensive review and historical
`superpowers` execution plans, then checked named legacy paths on disk.

```text
rg -n --glob '*.md' --glob '!docs/comprehensive-review/**' --glob '!docs/superpowers/**' --glob '!echo-agent-cli/docs/superpowers/**' --glob '!**/node_modules/**' --glob '!**/.eko/**' --glob '!**/target/**' '(?i)\bworker(s)?\b' docs echo-agent echo-agent-cli
test -e echo-agent-cli/echo-agent-eval
test -e echo-agent-cli/echo-agent-app-core/src/{sensitive.rs,embedded_server.rs,server_pid.rs,config.rs}
```

## Expected Result

Current architecture guides use Subagent terminology and do not describe
deleted crates/modules as current project structure.

## Result

- Exit code: 0 for the search; legacy path checks reported absent
- Duration: under one second
- Summary: current-looking root `PROJECT-ANALYSIS.md` and
  `deep-iteration-plan.md` retain many product/internal `worker` names and old
  symbol anchors. `echo-agent-eval` and the four app-core modules are absent.
  `AGENTS.md` still lists `echo-agent-eval` as a current workspace component and
  gives a worktree path rule for its nonexistent manifest.
- Log/artifact: none

## Deviations

External fixed protocol names would be allowed, but the sampled occurrences are
internal product/implementation terminology. Historical design files were
excluded from the finding to avoid rewriting archival evidence.

## Conclusion

The claim failed. This supports `B-DOC-01-P3-02`; the absent app-core modules
also classify the corresponding audit cleanup items as fixed.

## Follow-Up

`Q-DOC-01` should scope a terminology/path check to documents explicitly marked
current, while preserving clearly labeled historical snapshots.
