# B-DOC-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The curated documents have no unresolved local Markdown targets.

## Method

The first link checker extracted Markdown destinations and excluded only
`http`, `https`, `mailto`, and empty targets.

```text
for doc in docs/MASTER-PLAN.md echo-agent-cli/docs/MASTER-PLAN.md echo-agent/AUDIT_REPORT.md echo-agent-cli/docs/2026-07-28-app-core-full-audit.md echo-agent-cli/docs/code-review-2026-07-03.md; do
  # extract ](...) targets and test non-http destinations relative to the doc
done
```

## Expected Result

Zero unresolved local targets.

## Result

- Exit code: 1
- Duration: under one second
- Summary: four targets were reported: three UUID-like targets in root
  `MASTER-PLAN.md` and a `javascript:` example inside the July code review.
- Log/artifact: none

## Deviations

The `javascript:` sample is a URI-scheme string, not a local link. The method
therefore contains one false positive and cannot support an exact missing-link
count.

## Conclusion

The zero-link claim failed, but this attempt is not the accepted count. V05-02
corrects URI-scheme classification without editing this result.

## Follow-Up

V05-02.
