# B-BASE-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The first target inventory accurately counted Cargo targets and required
features.

## Method

The first attempt mixed manual `[[example]]` counting and partial target output
instead of treating `packages[].targets[]` as the authoritative final target
set.

```text
rg -n '^\[\[example\]\]|^required-features' Cargo.toml
cargo metadata --no-deps --locked --format-version 1
```

## Expected Result

The summary and detailed target list report identical totals derived from one
authoritative structure.

## Result

- Exit code: 0 for the commands; validation failed on evidence review
- Duration: not recorded
- Summary: the first summary reported 28 feature-gated framework examples while
  its detailed body identified 44. A subsequent structured recount found 45,
  so neither first-attempt number is accepted.
- Log/artifact: not retained

## Deviations

The first attempt also failed to distinguish 55 explicit `[[example]]` tables
from Cargo's 68 final example targets after auto-discovery and the renamed
`demo42_browser_mcp` target.

## Conclusion

This attempt is internally contradictory and cannot support the target
inventory.

## Follow-Up

[V03-02](V03-02.md) uses only Cargo's normalized target array for final target
counts, with manifest/filesystem counts as cross-checks.
