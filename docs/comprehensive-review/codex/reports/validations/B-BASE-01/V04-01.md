# B-BASE-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The checked-in CI workflows enforce the mandatory repository gates documented
in root `AGENTS.md`, with deterministic cross-repository inputs.

## Method

The two workflow files were compared command by command with `AGENTS.md` lines
245-307. Checkout behavior, feature combinations, target selection, and
frontend scripts were included.

```text
nl -ba echo-agent/.github/workflows/rust-ci.yml
nl -ba echo-agent-cli/.github/workflows/rust-ci.yml
rg -n 'cargo (fmt|clippy|test|check)|prettier|npm (test|run build)' \
  echo-agent/.github/workflows echo-agent-cli/.github/workflows AGENTS.md \
  -g '*.yml' -g '*.yaml' -g '*.md'
```

Environment: read-only workflow comparison; no GitHub Actions run was
dispatched.

## Expected Result

CI should enforce the applicable formatting, Clippy, test, no-default, GUI, and
frontend gates, and CLI should select a stable compatible `echo-agent` revision.

## Result

- Exit code: 0 for inspection commands; validation claim failed
- Duration: under one second
- Summary: Rust fmt, both Clippy configurations, workspace tests, and the
  documented no-default check are substantially represented. Three deviations
  remain: CLI CI has no frontend Prettier/test/build job; it checks out the
  framework repository without `ref`, so the tested dependency floats with the
  framework default branch; framework CI runs package `--lib --tests` rather
  than the documented workspace `--all-targets` test gate.
- Log/artifact: source anchors and impact are in `B-BASE-01.md`

## Deviations

The CLI workflow's all-feature Rust test compiles GUI code, but it does not
replace the documented no-default `gui` isolation commands. The framework lint
job compiles examples and the benchmark, but Clippy compilation does not
execute any target-local tests.

## Conclusion

The claim is false. CI does not fully enforce the repository's declared gates
and the CLI's framework input is not revision-stable. This supports findings
`B-BASE-01-P2-01`, `B-BASE-01-P2-02`, and `B-BASE-01-P2-03`; it does not claim
that the omitted commands currently fail.

## Follow-Up

`Q-FW-01`, `Q-CLI-01`, `Q-GUI-01`, and `Q-WEB-01` must record the current
executable outcomes independently.
