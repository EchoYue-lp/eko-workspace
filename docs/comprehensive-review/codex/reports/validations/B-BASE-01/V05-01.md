# B-BASE-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary review
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The corrected topology attempts are internally reproducible, and the three CI
deviations reported by `B-BASE-01` are present in current source at the reviewed
commits.

## Method

The primary reviewer independently reran structured Cargo metadata reductions
for package, target, example, required-feature, test, bench, custom-build, and
optional-dependency counts. It then read both CI workflows and the mandatory
gate section of root `AGENTS.md` line by line.

```text
cd echo-agent
cargo metadata --no-deps --locked --format-version 1 | jq '<target reduction>'
cargo metadata --no-deps --locked --format-version 1 | jq '<optional dependency reduction>'
nl -ba .github/workflows/rust-ci.yml | sed -n '1,180p'

cd echo-agent-cli
cargo metadata --no-deps --locked --format-version 1 | jq '<target reduction>'
nl -ba .github/workflows/rust-ci.yml | sed -n '1,180p'
rg -n 'npm|prettier|web-frontend|checkout@|repository:|ref:' .github/workflows

cd ..
nl -ba AGENTS.md | sed -n '235,315p'
```

Environment: macOS, Cargo 1.97.1, Rust 1.97.1. This was a read-only metadata and
workflow validation; no build or test target was executed.

## Expected Result

Independent reductions match V01-02/V02-02/V03-02 exactly. Workflow source
anchors support each reported CI deviation without relying on historical prose.

## Result

- Exit code: 0 for every command
- Duration: under one second for metadata; workflow inspection was immediate
- Summary: framework recount returned 8 packages and 79 targets: 68 examples,
  2 integration tests, 1 benchmark, and 45 gated examples. Optional dependency
  records total 46 with per-package counts `3/0/0/22/2/1/1/17`. CLI recount
  returned 2 packages and 6 targets: 2 libraries, 2 binaries, 1 integration
  test, and 1 custom build. These match the corrected attempts exactly.
  CLI CI has no Node/frontend command and checks out `EchoYue-lp/echo-agent`
  without `ref`. Framework CI runs package tests with `--lib --tests`, while
  root `AGENTS.md` requires workspace `--all-targets` testing.
- Log/artifact: no large artifact; exact reductions and source anchors are
  summarized above and in V01-02 through V04-01

## Deviations

The frontend gate is conditional on frontend changes in `AGENTS.md`, so the
finding means CI fails to enforce that condition for affected changes; it does
not require every Rust-only change to run frontend jobs. The floating framework
checkout could be an intentional latest-main compatibility lane, but the
workflow provides no pinned reproducibility lane or comment establishing that
policy.

## Conclusion

The corrected topology evidence is accepted. The three P2 findings are supported
at the reviewed commits and are scoped to CI reproducibility/coverage rather
than claims that current code necessarily fails. This validation closes the
primary-acceptance requirement for `B-BASE-01`.

## Follow-Up

`Q-FW-01`, `Q-CLI-01`, `Q-GUI-01`, and `Q-WEB-01` execute the actual gates.
`B-DOC-01` classifies the mismatch between repository rules and CI comments.
