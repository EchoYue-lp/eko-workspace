# A-FE-03 / V02 / Attempt 01

> Status: failed/finding
> Validation date: 2026-08-13
> Executor: Codex primary reviewer

## Claim

A streamed token updates only the active message and bounded visible rows.

## Method

Trace `appendToken` reference changes through ChatPanel, every mounted
MessageBubble, its Zustand selectors/memoized derivations, nested
ParallelExecutionBlock, Subagent association filtering, and tool clocks. Check
the maximum retained history and virtualization inventory.

## Expected Result

Per-token work is independent of total retained history except for a single
indexed store update; off-screen/unchanged rows do not subscribe to whole maps.

## Result

- Exit code: 1 (invariant violated)
- Duration: static hot-path complexity trace
- `appendToken` clones the message array, notifying every MessageBubble that
  subscribes to the complete array despite `React.memo`.
- Each row searches for the last assistant, rebuilds all message IDs, converts
  and filters the complete Subagent map; its nested block repeats the same
  subscriptions and scans.
- ChatPanel maps all retained rows, up to the explicit 500-message cap, and no
  virtualization/windowing dependency or code path exists.
- Mounted running tools additionally schedule independent 250ms clocks.

## Deviations

No runtime profiler was used, so exact milliseconds are future evidence; the
nested collection operations and notification fanout are source-conclusive.

## Conclusion

A-FE-03-P1-01 is confirmed.

## Follow-Up

Index associations once, narrow row selectors, window the transcript, and add
render-count/frame-time regression budgets.
