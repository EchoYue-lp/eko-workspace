# A-FE-03 / V03 / Attempt 01

> Status: failed/finding
> Validation date: 2026-08-13
> Executor: Codex primary reviewer

## Claim

Rendering or updating a maximum-size Task graph performs one linear derivation
and constant-time work per visible task.

## Method

Trace Subagent store notification into TaskRuntimePanel; count collection-wide
filter/sort calls in completion counts, status/description helpers, and each
todo row. Confirm the canonical framework task bound from committed source and
search for task-list windowing.

## Expected Result

One `taskId -> latestTrace` and `taskId -> PlanTask` index is reused throughout
the render, and only a bounded visible set mounts.

## Result

- Exit code: 1 (invariant violated)
- Duration: static selector/render trace
- `traceRunForTodo` filters and sorts every trace list per call.
- Completion computation and every todo render call it repeatedly; each row
  also searches `plan.tasks` linearly.
- The framework validator admits up to 100 tasks. All todos render with no
  windowing, and the panel subscribes to the entire Subagent map.

## Deviations

No synthetic 100-task browser fixture was run per instruction.

## Conclusion

A-FE-03-P2-02 is confirmed.

## Follow-Up

Build keyed projections once per state change and virtualize long task lists.
