# F-OPS-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: external dirty paths excluded; source otherwise inspected read-only

## Claim

JsonlRunStore confines every run ID-derived file access beneath its configured
directory.

## Method

```text
1. Follow public Run.run_id and RunStore::load(run_id) into JsonlRunStore::run_path.
2. Apply PathBuf::join semantics to absolute and parent-containing IDs.
3. Trace resulting paths into OpenOptions/read_to_string and production EKO injection.
```

## Expected Result

Logical run identity is never filesystem authority; derived paths are validated
relative components and canonical containment is checked before read/write.

## Result

- Exit code: 0
- Duration: static path-containment proof
- Summary: `run_path` is `dir.join(format!("{run_id}.jsonl"))` with no validation.
  An absolute formatted path replaces `dir`; `../` components escape it. `save`
  opens that path with create+append and `load` reads it. Both methods are public
  trait operations, and EKO constructs this backend for every agent.
- Log/artifact: this report

## Deviations

No file mutation fixture was run. Standard path-join behavior plus direct sink
reachability is sufficient to reject the containment claim.

## Conclusion

The containment invariant fails and supports F-OPS-01-P0-01.

## Follow-Up

Absolute, parent, separator, empty, Unicode, symlink, and unchanged-outside-root
fixtures during implementation.
