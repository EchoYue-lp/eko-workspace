# F-OPS-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: external dirty paths excluded; source otherwise inspected read-only

## Claim

Once a scheduled occurrence starts, cancellation, completion, failure, and
persistent terminal state remain observable without blocking unrelated schedules.

## Method

```text
1. Trace tick -> fire_task -> FireFn -> update_last_run.
2. Inspect cancellation selection, execution ordering, deadline, occurrence ID,
   durable claim, result/error persistence, and persistence-error handling.
3. Exclude application background-task ownership already reported by B-PATH-01.
```

## Expected Result

Each occurrence has stable identity and a durable terminal; cancellation reaches
in-flight work; one blocked task cannot stop all schedules; persistence failure
is returned or made observable.

## Result

- Exit code: 0
- Duration: static lifecycle trace
- Summary: due tasks execute serially and each FireFn is awaited without timeout
  or selection on cancellation. Runner cancellation is checked only between
  30-second sleeps. `last_fired` is process-local and marked before execution;
  there is no durable occurrence claim/attempt ID, and `update_last_run` errors
  are discarded on both success and failure. Multiple runners can duplicate an
  occurrence, while one hung task blocks all subsequent tasks and shutdown.
- Log/artifact: this report

## Deviations

The already-owned absence of an application cancel/join owner is not restated.
This validation concerns the framework's in-flight FireFn and durable occurrence
contract.

## Conclusion

The execution lifecycle invariant fails and supports F-OPS-01-P1-02.

## Follow-Up

Use deterministic multi-occurrence, hung-fire, cancellation, two-runner, and
persistence-failure regressions during implementation.
