# F-OPS-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: external dirty paths excluded; source otherwise inspected read-only

## Claim

The advertised headless CI/scripting facade produces a validated, machine-readable
terminal contract and every public configuration option changes behavior.

## Method

```text
1. Inspect HeadlessConfig, HeadlessResult, run_headless, root exports and demo54.
2. Map build, success, execution error, empty prompt, JSON/text formatting,
   cancellation/timeout, identity, event, and exit-code fields.
3. Reuse F-RCT-02 for ReAct-core terminal defects rather than duplicate them.
```

## Expected Result

Configuration is typed and effective; machine output distinguishes terminal
kind and includes stable correlation; automation can supply cancellation/deadline.

## Result

- Exit code: 0
- Duration: static field/branch matrix
- Summary: `exit_on_error` is never read; `exit_code` ignores it. Arbitrary
  output-format strings silently become text. Every error is flattened into the
  output string and a boolean, with no typed error/cancel/limit terminal, run or
  session identity, usage, event stream, cancellation token, or deadline. The
  example simulates most behavior instead of invoking the facade.
- Log/artifact: this report

## Deviations

The raw `Agent::execute` terminal weakness is owned by F-RCT-02; this report
classifies only the additional public adapter contract.

## Conclusion

The headless surface is materially misleading for its documented automation
use and supports F-OPS-01-P2-02.

## Follow-Up

Future table-driven headless result/JSON/exit/cancel/deadline tests.
