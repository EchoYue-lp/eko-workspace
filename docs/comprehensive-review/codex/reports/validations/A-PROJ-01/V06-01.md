# A-PROJ-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Git workspace-change paths retain exact UTF-8/rename identity and command
failure is distinguishable from a clean repository.

## Method

```text
Inspect workspace_changes and parse_workspace_changes against Git porcelain v1
quoting/rename forms and nonzero exits. Trace parsed path to FileBrowser
loadDiff and backend path resolution. Inspect existing parser tests.
```

## Expected Result

Use an unambiguous byte-delimited Git format, decode paths losslessly, and
return a typed error when Git status fails.

## Result

- Exit code: 0
- Duration: static protocol inspection; not timed
- Summary: the command requests newline-delimited `--porcelain=v1`, converts
  output with `from_utf8_lossy`, splits renames on text ` -> `, and merely trims
  quotes (`files.rs:211-263`). Default Git quoting can encode non-ASCII or special
  paths with C/octal escapes; those bytes are displayed and later submitted as
  a different pathname. A nonzero Git exit returns `Ok(Vec::new())`, identical
  to a clean tree. The unit test supplies already-decoded Unicode rather than
  actual quoted porcelain and cannot cover this behavior (`files.rs:675-697`).
- Log/artifact: this report

## Deviations

No Git process/fixture was run by policy.

## Conclusion

The claim failed. Supports A-PROJ-01-P1-03.

## Follow-Up

Validate `--porcelain=v1 -z` bytes for spaces, quotes, tabs, newlines, Chinese,
emoji, rename/copy pairs, deletion, non-repository, and Git execution failure.
