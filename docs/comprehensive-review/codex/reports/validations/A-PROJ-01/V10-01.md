# A-PROJ-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Existing tests cover the reviewed lifecycle boundaries, UTF-8 paths, conflicts,
workspace switches, duplicate diff semantics, and large-input behavior.

## Method

```text
Inventory tests referencing ProjectIndex, GitIgnore, ProjectContext,
WorkspaceRegistry/switch, FileChangeTracker/CodingLoop, diff_file,
parse_workspace_changes, write_workspace_file, useFileStore, and workspaceStore.
Compare assertions with the task's required scenarios.
```

## Expected Result

Tests should cover live registration plus invalidation, real Git wire encoding,
workspace-keyed drafts/in-flight requests, equal diff/error distinctions, bounds
and cancellation.

## Result

- Exit code: 0
- Duration: static test inventory; not timed
- Summary: app-core diff tests cover equal/add/remove/replace; Tauri tests cover
  revision conflict and a hand-written decoded Unicode status string; fileStore
  tests cover dirty/save/conflict and in-flight typing. ProjectIndex tests cover
  only simple build/search/related files; GitIgnore tests are ASCII. No test
  covers ProjectIndex invalidation/reachability, globstar Unicode panic,
  CLI/TUI/GUI switch parity, cross-workspace equal revisions, stale async
  response, actual Git quoted bytes/nonzero status, live GUI unchanged diff,
  missing refs, large repositories/files, cancellation, inert tracker, or `.bak`
  production.
- Log/artifact: this report

## Deviations

This is a source inventory; tests were not executed.

## Conclusion

The claim failed. Existing focused tests confirm useful local invariants but do
not cover any of the cross-owner failures reported here.

## Follow-Up

Use the regression lists in each finding; do not add tests around the duplicate
Tauri diff implementation if it will be deleted.
