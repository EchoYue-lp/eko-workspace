# A-PROJ-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Workspace switch is a truthful runtime transition with equivalent reachability
across CLI, TUI, and GUI.

## Method

```text
Trace GUI switch_workspace to AppState; trace CLI /workspace switch/info/list/
link from CommandRegistry; inspect WorkspaceRegistry open/list ordering and the
REPL's project context/CodingLoop construction. Enumerate TUI SlashCommand and
event handlers for a workspace transition path.
```

## Expected Result

Each surface should invoke one application transition owner that rebinds live
project/coding/file state, and must not label recency metadata as current state.

## Result

- Exit code: 0
- Duration: static cross-surface trace; not timed
- Summary: GUI calls `AppState::switch_workspace` (`tauri/commands/workspace.rs:
  131-179`). CLI `/workspace switch` only calls `WorkspaceRegistry::open_by_name`,
  which touches `last_active`, then prints "Switched" (`cli/cmd_impls/workspace.rs:
  113-145`; `workspace/registry.rs:185-208`). It never rebinds the Agent,
  CodingLoop, prompt, or stores; those are frozen at REPL startup
  (`repl.rs:118-127,182-198`). CLI info/link choose `list()[0]` by recency and
  list labels hardcoded ID `default` active. TUI exposes no workspace command
  and source comments explicitly say it has no workspace concept.
- Log/artifact: this report

## Deviations

A-CFG-01-P1-02 already owns GUI transition atomicity; this validation does not
repeat it and covers only absent/fake surface transitions.

## Conclusion

The claim failed. Supports A-PROJ-01-P1-02.

## Follow-Up

Route all surfaces through one live application transition service and test
post-switch file, prompt, coding command, task, and conversation roots.
