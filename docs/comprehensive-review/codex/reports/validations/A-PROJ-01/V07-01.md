# A-PROJ-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

GUI diff derives from the canonical app-core diff authority and truthfully
distinguishes unchanged, missing-at-ref, untracked, and Git failures.

## Method

```text
Compare app-core diff.rs types/generate_unified_diff/tests with Tauri files.rs
DiffResult/DiffHunk/DiffLine and diff_file. Trace git show outcomes, hunk
construction, frontend empty-state logic, and registration/consumption.
```

## Expected Result

One diff implementation should produce no hunks for equal content, context-
bounded correct headers for changes, and typed provenance/errors for base lookup.

## Result

- Exit code: 0
- Duration: static algorithm comparison; not timed
- Summary: app-core has the tested `grouped_ops(context)` authority and asserts
  no hunks for equal content (`diff.rs:65-160,438-444`). The live GUI implements
  another algorithm over `iter_all_changes` (`files.rs:267-380`): every equal
  line is appended, so an unchanged nonempty file returns one whole-file hunk
  with counts 0/0 and the UI suppresses "No changes". Equal context is excluded
  from hunk counts. Any git-show spawn/nonzero/join failure becomes empty old
  content, indistinguishable from a genuinely untracked file and capable of
  rendering a false all-insert diff. The GUI duplicate does not call app-core.
- Log/artifact: this report

## Deviations

No executable diff fixture was run by policy; the unchanged branch is direct
from the iterator and terminal push conditions.

## Conclusion

The claim failed. Supports A-PROJ-01-P1-04.

## Follow-Up

Delete the Tauri diff algorithm after adapting the canonical result losslessly;
test equal, binary, deleted, untracked, missing ref/path, invalid repository,
Unicode, distant hunks, and context/count correctness.
