# A-PROJ-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

Every reviewed project service has a production registration and a live caller
that reaches the advertised behavior.

## Method

```text
Trace each definition through module exports and production references. For CLI,
trace register_all -> REPL CommandRegistry -> command handler. For GUI, trace
Tauri generate_handler -> endpoints.ts -> Zustand store -> mounted panel. Search
ProjectIndex, CodingLoop mutation methods, and GitIgnore for non-test callers.
```

## Expected Result

Public application services advertised as live should have a production owner;
registered commands should reach the same current workspace facts.

## Result

- Exit code: 0
- Duration: static call-graph inspection; not timed
- Summary: GUI file/tree/diff/change commands are registered at
  `src/tauri/mod.rs:119-124`, consumed by `filesApi`, `useFileStore`, and the
  mounted FileBrowser. CLI workspace and coding commands are registered at
  `src/cli/repl.rs:159-198`. ProjectIndex has no caller outside its own tests.
  CodingLoop's record-write/delete methods have no caller anywhere. GitIgnore is
  live through ProjectContext, but ProjectIndex never consults it.
- Log/artifact: this report

## Deviations

No dynamic surface was launched by task policy.

## Conclusion

The claim failed. GUI and CLI entry points are live, while ProjectIndex and the
CodingLoop change tracker are definition/test-only. Supports
A-PROJ-01-P2-05 and A-PROJ-01-P2-06.

## Follow-Up

V03 and V09 inspect the inert components' advertised contracts.
