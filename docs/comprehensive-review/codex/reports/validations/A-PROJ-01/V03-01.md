# A-PROJ-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; externally dirty framework worktree excluded

## Claim

ProjectIndex has a current-file lifecycle with invalidation, bounded traversal,
ignore-rule consistency, and cache identity validation.

## Method

```text
Inspect echo-agent-app-core/src/project/index.rs build/load/save/walk and every
caller; compare traversal with project/context.rs and project/gitignore.rs.
Check update/remove/refresh APIs, cache schema/root/fingerprint validation,
symlink handling, visited sets, traversal bounds, error propagation, atomic
save, and tests for changed/deleted files and large repositories.
```

## Expected Result

A persisted index must prove which root/revision it describes, invalidate or
refresh after file changes, honor one ignore policy, avoid symlink cycles and
bound large-tree work.

## Result

- Exit code: 0
- Duration: static invariant inspection; not timed
- Summary: ProjectIndex documents startup/on-demand refresh and persistence at
  `index.rs:43-46`, but has only build/load/save and lookup APIs. `load` trusts
  serialized root/files/built_at and merely rebuilds skipped maps. `walk`
  follows `path.is_dir()` recursively with no visited set, file/depth/count/time
  bound or cancellation, silently drops I/O errors, and uses a hardcoded skip
  list instead of ProjectContext's live GitIgnore. `save` is a plain overwrite.
  There is no production owner, so these defects are currently latent rather
  than proof of a stale runtime cache.
- Log/artifact: this report

## Deviations

No large repository fixture was executed. The runtime impact is therefore
limited to the misleading public API and future adoption risk.

## Conclusion

The claim failed. ProjectIndex's documented lifecycle is not implemented and
the service is unreachable. Supports A-PROJ-01-P2-05.

## Follow-Up

If the service becomes live, add fixture validation for invalidation, symlink
cycles, ignored files, corrupt cache, and cancellation before relying on it.
