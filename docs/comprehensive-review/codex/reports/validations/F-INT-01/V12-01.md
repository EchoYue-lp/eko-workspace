# F-INT-01 / V12 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

User-configured MCP extensions receive lightweight malformed-input validation at
the adapter boundary without framework-level policy that blocks legitimate local
commands.

## Method

```text
Compare config_loader conversion/validation, direct McpServerConfig construction,
stdio transport validation, URL handling, EKO connect paths, and the repository's
local personal-assistant threat model. Search all command/URL validators.
```

No command, process, network request, or fixture was started.

## Expected Result

Empty/malformed values fail clearly; valid user-selected executable/argv/URL
choices remain usable and product policy is not duplicated in a transport.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: config_loader and StdioTransport apply different validators. The
  transport rejects every argument beginning `-o`, independent of executable or
  actual option semantics, so legitimate extension arguments can be blocked.
  Direct McpServerConfig bypasses loader checks but still reaches transport checks.
  URL syntax/scheme is deferred to reqwest. This is inconsistent policy and too
  broad for trusted user-configured local extensions, while still not forming one
  coherent malformed-input contract.
- Evidence: `echo-agent/echo-integration/src/mcp/config_loader.rs:107`,
  `echo-agent/echo-integration/src/mcp/config_loader.rs:117`,
  `echo-agent/echo-integration/src/mcp/config_loader.rs:134`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:284`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:315`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:323`

## Deviations

This conclusion intentionally does not apply a multi-user/public-service threat
model. It concerns false rejection and incoherent input behavior.

## Conclusion

The boundary claim failed and supports F-INT-01-P2-03.
