# F-INT-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

A2A task identity, cancellation, and terminal state are owned by the actual Agent
execution rather than a disconnected projection.

## Method

```text
Trace request ID/task ID insertion, cancellation-token construction and use,
Agent sync/stream calls, cancel endpoint, state writes, duplicate caller IDs,
terminal event handling, and cleanup map removal.
```

No A2A server or fixture was started.

## Expected Result

Cancellation reaches Agent work, cannot be overwritten by late completion, each
task ID has one active owner, and Agent failure yields Failed exactly once.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: both paths create cancellation tokens, but sync uses `Agent::execute`
  and streaming uses `execute_stream`, so the token never enters the Agent call.
  Cancel marks the map entry Canceled, while a later sync success replaces it
  with Completed. Streaming checks cancellation only between events. A streamed
  `AgentEvent::Error` is an ordinary `Ok` stream item ignored by the accumulator,
  after which the task is marked Completed. Caller-supplied duplicate IDs replace
  task/token ownership while the displaced execution continues.
- Evidence: `echo-agent/src/a2a/server.rs:154`,
  `echo-agent/src/a2a/server.rs:175`, `echo-agent/src/a2a/server.rs:179`,
  `echo-agent/src/a2a/server.rs:207`, `echo-agent/src/a2a/server.rs:224`,
  `echo-agent/src/a2a/server.rs:279`, `echo-agent/src/a2a/server.rs:300`,
  `echo-agent/src/a2a/server.rs:379`, `echo-agent/src/a2a/server.rs:400`,
  `echo-agent/src/a2a/server.rs:414`, `echo-agent/src/a2a/server.rs:527`

## Deviations

A2A is a legitimate standalone framework feature despite no EKO caller.

## Conclusion

The lifecycle claim failed and supports F-INT-01-P1-08 and F-INT-01-P1-09.
