# F-INT-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

MCP server identity and adapted Tool names remain unique from configuration
through manager connection, React registration, invocation, and disconnect.

## Method

```text
Trace McpServerConfig -> McpManager::connect -> McpToolAdapter -> ToolManager,
then ReactAgent connect/disconnect and EKO Tauri/TUI/plugin callers. Compare raw
server/tool identity with the exposed-name sanitizer and replacement semantics.
```

No executable validation was started.

## Expected Result

Two distinct remote identities cannot collapse to one exposed Tool name, and
reconnection/disconnection consistently removes precisely the prior definitions.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: the path is live in ReactAgent and EKO. Reconnect removes old tools
  before replacing the client. However, `sanitize_tool_name_part` maps every
  non-ASCII/non-alphanumeric character to `_`, making names such as `a/b` and
  `a b` indistinguishable. The resulting `mcp__server__tool` key enters the
  replacement-based Tool registry identified by F-EXT-01.
- Evidence: `echo-agent/echo-integration/src/mcp/mod.rs:72`,
  `echo-agent/echo-integration/src/mcp/tool_adapter.rs:35`,
  `echo-agent/echo-integration/src/mcp/tool_adapter.rs:77`,
  `echo-agent/src/agent/react/capabilities.rs:1149`,
  `echo-agent/src/agent/react/capabilities.rs:1315`,
  `echo-agent-cli/src/tauri/commands/mcp.rs:211`

## Deviations

This is a local user-extension correctness issue, not a public-service threat.

## Conclusion

The identity claim failed and supports F-INT-01-P2-02.
