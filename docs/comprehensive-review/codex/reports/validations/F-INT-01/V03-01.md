# F-INT-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

Streamable HTTP routes synchronous and accepted-asynchronous responses, retains
session state, and closes or cancels outstanding work deterministically.

## Method

```text
Trace HttpTransport construction, send retry/pending insertion, 202 handling,
notification receiver, session header capture/reuse, notify, close, and every
write to pending/notification_tx across the repository.
```

No network server or fixture was started.

## Expected Result

Every registered pending request has a live response producer and every terminal
path removes it; close uses protocol session/cancellation semantics.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: synchronous JSON responses work structurally, but no task reads an
  HTTP GET/SSE response channel and no code sends into `pending` or
  `notification_tx`. A 202 response therefore waits 60 seconds and can only
  time out. Notifications discard HTTP status/errors. Close sends one generic
  cancellation notification and clears all waiters; it does not cancel a
  correlated request or terminate the negotiated session.
- Evidence: `echo-agent/echo-integration/src/mcp/transport/http.rs:25`,
  `echo-agent/echo-integration/src/mcp/transport/http.rs:39`,
  `echo-agent/echo-integration/src/mcp/transport/http.rs:69`,
  `echo-agent/echo-integration/src/mcp/transport/http.rs:160`,
  `echo-agent/echo-integration/src/mcp/transport/http.rs:204`,
  `echo-agent/echo-integration/src/mcp/transport/http.rs:230`

## Deviations

The local retry loop only sees request-send errors; HTTP 5xx statuses reach the
later error branch despite the comment claiming 5xx retry.

## Conclusion

The lifecycle claim failed and supports F-INT-01-P1-01.
