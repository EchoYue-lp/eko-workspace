# F-INT-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

LSP request/session lifecycle is bounded, cancellable, reconnectable, accurately
reported, and robust to malformed framing.

## Method

```text
Enumerate StdioLspClient spawn/read/write/request/shutdown paths and LspManager
start/stop/restart/status behavior. Check pending cleanup, timeouts, cancel input,
process state, restart fields, frame limits, and all error conversions.
```

No language server or fixture was started.

## Expected Result

Every request has timeout/cancel ownership; process exit updates state; advertised
restart policy runs; malformed frames cannot allocate without bound.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: only initialization has a manager-level timeout; all live capability
  requests wait on oneshot indefinitely and accept no cancellation. Writer-send
  failure leaves its pending entry. Reader EOF clears waiters but never flips the
  running/initialized flags. `max_restarts`, restart_count, and last_error are
  exposed but no automatic restart path updates them. Content-Length directly
  controls `vec![0; len]` without a maximum.
- Evidence: `echo-agent/echo-integration/src/lsp/client.rs:120`,
  `echo-agent/echo-integration/src/lsp/client.rs:125`,
  `echo-agent/echo-integration/src/lsp/client.rs:164`,
  `echo-agent/echo-integration/src/lsp/client.rs:202`,
  `echo-agent/echo-integration/src/lsp/client.rs:523`,
  `echo-agent/echo-integration/src/lsp/manager.rs:76`,
  `echo-agent/echo-integration/src/lsp/manager.rs:157`

## Deviations

None.

## Conclusion

The lifecycle claim failed and supports F-INT-01-P1-05 and F-INT-01-P1-06.
