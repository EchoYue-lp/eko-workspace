# F-INT-01 / V11 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

A2A client streaming preserves UTF-8 across network chunks, reports stream errors
to callers, bounds requests, and normalizes remote errors without data loss.

## Method

```text
Inspect A2AClient construction, request/status handling, SSE byte buffering,
event parsing/yield type, read/decode/parse failures, and send/get/cancel timeout
and typed-error behavior.
```

No network request or fixture was started.

## Expected Result

Byte framing precedes UTF-8 decode, every terminal error is observable through
the returned stream/result, and all operations have bounded lifetime.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: each reqwest byte chunk is decoded independently, so a valid Unicode
  scalar split across chunks is discarded. The returned stream item is
  `A2AStreamEvent`, not Result; read/decode/parse errors only log and continue or
  end, indistinguishable from normal EOF. The reqwest client has no configured
  timeout. Non-streaming send/get/cancel parse JSON without first checking HTTP
  status and flatten protocol/transport failures into `ReactError::Other` text.
- Evidence: `echo-agent/src/a2a/client.rs:37`,
  `echo-agent/src/a2a/client.rs:131`, `echo-agent/src/a2a/client.rs:139`,
  `echo-agent/src/a2a/client.rs:195`, `echo-agent/src/a2a/client.rs:242`,
  `echo-agent/src/a2a/client.rs:250`, `echo-agent/src/a2a/client.rs:298`,
  `echo-agent/src/a2a/client.rs:334`

## Deviations

None.

## Conclusion

The streaming/error claim failed and supports F-INT-01-P1-10.
