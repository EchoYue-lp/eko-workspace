# F-INT-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

MCP SSE and stdio reconnect, pending-request cleanup, notification delivery,
UTF-8 framing, shutdown, and no-panic behavior are complete on every exit path.

## Method

```text
Enumerate constructor/background task/send/timeout/error/EOF/close/Drop branches
for SseTransport and StdioTransport. Check every pending insertion/removal,
notification producer, reconnect transition, byte-to-text boundary, and integer
backoff operation.
```

No server, child process, or fixture was started.

## Expected Result

Transport loss either reconnects or terminates all callers with typed failure;
valid split UTF-8 survives; teardown is runtime-independent and bounded.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: normal SSE EOF returns `Ok` and stops reconnecting. Endpoint-missing,
  POST failure, and timeout paths leave pending entries; close does not drain or
  await the task. Each byte chunk is decoded independently, so a valid Unicode
  scalar split across chunks is rejected. Server `retry` is doubled unchecked.
  Stdio write/read-error paths can retain waiters until timeout, server
  notifications are discarded, and Drop unconditionally calls `tokio::spawn`,
  which can panic when dropped outside a Tokio runtime.
- Evidence: `echo-agent/echo-integration/src/mcp/transport/sse.rs:65`,
  `echo-agent/echo-integration/src/mcp/transport/sse.rs:98`,
  `echo-agent/echo-integration/src/mcp/transport/sse.rs:124`,
  `echo-agent/echo-integration/src/mcp/transport/sse.rs:202`,
  `echo-agent/echo-integration/src/mcp/transport/sse.rs:322`,
  `echo-agent/echo-integration/src/mcp/transport/sse.rs:415`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:144`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:188`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:265`,
  `echo-agent/echo-integration/src/mcp/transport/stdio.rs:270`

## Deviations

None.

## Conclusion

The claim failed and supports F-INT-01-P1-02 and F-INT-01-P2-01.
