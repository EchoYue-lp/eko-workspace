# F-INT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

MCP, LSP, and A2A have explicit feature/export boundaries, with reusable protocol
mechanisms in the framework and EKO product startup/configuration policy outside
those implementations.

## Method

```text
Inspect both repositories' manifests, cfg gates, module/prelude exports, protocol
traits, concrete adapters, EKO dependency features, and exact symbol callers.
Search definitions, exports, registration, and call paths independently.
```

No Cargo, rustc, test, build, network, or dynamic fixture was started.

## Expected Result

Each public integration is feature-gated and reusable; EKO policy is not required
for framework compilation or protocol operation.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: root features map `mcp` and `lsp` to `echo_integration`; `a2a` is a
  root feature. Root facades and integration modules use matching cfg gates.
  EKO enables MCP/LSP and owns discovery/startup/UI policy; it does not enable
  A2A. A2A remains a documented/example-backed public framework capability, so
  EKO non-use is not dead-code evidence.
- Evidence: `echo-agent/Cargo.toml:68`, `echo-agent/Cargo.toml:69`,
  `echo-agent/Cargo.toml:70`, `echo-agent/echo-integration/Cargo.toml:23`,
  `echo-agent/src/lib.rs:69`, `echo-agent/src/lib.rs:88`,
  `echo-agent/src/lib.rs:92`, `echo-agent-cli/Cargo.toml:50`

## Deviations

None for feature ownership. Behavioral adapter defects are evaluated separately.

## Conclusion

The feature/export/layering claim passed.
