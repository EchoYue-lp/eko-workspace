# F-INT-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

LSP wire values, document lifecycle, URIs, positions, and malformed responses map
losslessly to typed framework values or typed failure.

## Method

```text
Compare echo_core LSP serde types and concrete deserializers with emitted/received
JSON, then trace root LSP Tool argument parsing, URI construction, didOpen/change/
close, diagnostics cache, and output position conversion.
```

No protocol fixture was started.

## Expected Result

Numeric protocol enums deserialize, malformed capability responses fail visibly,
document versions advance, URIs are encoded, and numeric conversion cannot wrap.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: DiagnosticSeverity and CompletionItemKind use default enum serde names
  while LSP sends numeric values; surrounding `unwrap_or_default` converts decode
  failure into empty diagnostics/completions. Definition/reference parse failures
  are likewise empty successes. Project roots use `format!("file://{}")` rather
  than URL conversion. Tools send didOpen version 1 before each query without
  didClose; didChange is always version 2. Tool `u64 as u32` arguments can wrap,
  and position units are not explicitly converted to LSP UTF-16 code units.
- Evidence: `echo-agent/echo-core/src/lsp/types.rs:24`,
  `echo-agent/echo-core/src/lsp/types.rs:91`,
  `echo-agent/echo-integration/src/lsp/client.rs:190`,
  `echo-agent/echo-integration/src/lsp/client.rs:353`,
  `echo-agent/echo-integration/src/lsp/client.rs:444`,
  `echo-agent/echo-integration/src/lsp/client.rs:455`,
  `echo-agent/echo-integration/src/lsp/client.rs:475`,
  `echo-agent/echo-integration/src/lsp/manager.rs:58`,
  `echo-agent/src/tools/lsp.rs:459`

## Deviations

Position-unit impact depends on caller convention, but numeric enum mismatch and
silent empty-success behavior are source-conclusive.

## Conclusion

The mapping claim failed and supports F-INT-01-P1-07 and F-INT-01-P2-03.
