# F-INT-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at inspection

## Claim

MCP Tool adaptation preserves schema, structured/rich result facts, identity,
and Tool execution contract in both client and server directions.

## Method

```text
Compare McpTool fields with Tool parameters/metadata/risk/result, then compare
McpServer advertised tool_list with its execution map and handle_tools_call
conversion. Trace duplicate names and failed/structured/binary outputs.
```

No tool fixture was started.

## Expected Result

Advertised schema and execution resolve the same definition; inputs are checked;
all result variants and failure facts remain reconstructable.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: client input schemas and structured JSON are retained, but image/audio
  content becomes descriptive placeholders rather than complete bytes/artifacts.
  On the server, duplicate names remain duplicated in `tool_list` while the
  execution map keeps only the last definition. `handle_tools_call` requires an
  object but does not validate against the advertised schema. ToolResult is
  flattened to text and drops structured data, kind, metadata, and artifact facts.
- Evidence: `echo-agent/echo-integration/src/mcp/tool_adapter.rs:96`,
  `echo-agent/echo-integration/src/mcp/tool_adapter.rs:132`,
  `echo-agent/echo-integration/src/mcp/tool_adapter.rs:166`,
  `echo-agent/echo-integration/src/mcp/server.rs:96`,
  `echo-agent/echo-integration/src/mcp/server.rs:409`,
  `echo-agent/echo-integration/src/mcp/server.rs:456`,
  `echo-agent/echo-integration/src/mcp/server.rs:654`

## Deviations

F-EXT-01 owns the generic registry/schema/result contract; this validation records
the protocol-specific lossy adapter and divergent advertised/executed definitions.

## Conclusion

The claim failed and supports F-INT-01-P1-04.
