# A-TOOL-01 / V05 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

The GUI interactive PTY path is separate from Agent automation permission mode and remains directly user-operable.

## Method

Traced Tauri terminal registration, create/write/confirm/resize/close commands, frontend terminal creation and first-input flow, and searched those paths for `permission_mode`, `require_full_auto`, and `IpcAuth` use.

## Expected Result

No automation permission-mode gate; the first actual user input can establish the lightweight per-session consent and then write to the PTY.

## Result

- Exit code: 0 for all inspections.
- `create_terminal`, confirmation, write, resize, close and list do not read Agent permission mode.
- Frontend calls confirmation on the first xterm `onData` event, then sends the same input; default mode therefore does not block terminal use.
- Agent `shell`/`run_code` remain separate framework Tool calls under invocation, permission, sandbox, timeout and cancellation policy.

## Deviations

The terminal write audit logs an 80-byte preview. Secret-redaction assessment belongs to F-SEC-01 and is not re-numbered here. PTY process cleanup lifecycle belongs to A-BOOT-01.

## Conclusion

Passed for the product boundary required by root `AGENTS.md`. Does not endorse online-XSS rationales or prove PTY cleanup.
