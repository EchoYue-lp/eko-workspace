# A-TOOL-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

Channel users receive the same material tool lifecycle, typed failure, cancellation and artifact facts as GUI/TUI/CLI users.

## Method

Traced shared `drive_chat` events through `ChannelChatSink`, `ChannelRenderEvent`, `aggregate_by_sentence`, and outbound messages. Compared GUI durable `ToolExecutionRepository`, TUI/CLI Tool events/artifact handling, surface-contract claims, and channel tests.

## Expected Result

Channel rendering preserves bounded ToolCall/progress/output, ToolResult or typed ToolError, cancellation, and complete-output artifact references.

## Result

- Exit code: 0 for all inspections.
- Channel uses the shared driver and receives complete `AgentEvent` envelopes.
- Its renderer handles Token, final/cancel/agent-level error, budget/guard/safety/parameter/chart and attention TaskRuntime events.
- ToolCall, ToolOutput/ToolStream, ToolResult and ToolError match the wildcard branch and emit nothing. Tool failure can be recovered within ReAct without becoming an Agent-level error, so the user never sees its category/recovery.
- Channel has no durable ToolExecutionRepository projection or artifact-open/read command; its tests cover token flush, agent cancellation/error and UTF-8, not tool events.
- The static surface contract nevertheless claims a channel `driver event renderer` for tool stream/failure/retry/cancel and `artifact reference text`.

## Deviations

TaskRuntime attention events may expose a 500-character payload for some task failures, but they do not preserve ordinary chat tool lifecycle, complete output, or typed ToolFailure.

## Conclusion

Failed. Supports A-TOOL-01-P1-03.
