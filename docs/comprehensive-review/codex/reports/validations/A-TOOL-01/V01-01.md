# A-TOOL-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

EKO uses one framework registry per Agent and invocation-scoped mode exposure rather than constructing independent per-surface executors.

## Method

Inspected `tool_exposure.rs`, `chat_driver.rs`, primary/pool construction, TaskRuntime direct-run calls, framework `ToolRuntime`, `ToolVisibilityState`, `tool_search`, and execution-time visibility enforcement. Compared Chat/Task/Auto initial and disabled sets and searched all callers.

## Expected Result

Mode policy filters the live registered names, `tool_search` can activate only eligible non-disabled tools, and GUI/TUI/CLI/channel share the same `drive_chat` invocation policy.

## Result

- Exit code: 0 for all read-only inspections.
- `initial_visible_tools` intersects named product groups with the live registry; `disabled_tools_for_mode` removes legacy/parallel task authorities.
- `drive_chat` supplies both sets per invocation, and TaskRuntime supplies Task/Auto equivalents.
- Framework `ToolVisibilityStage` enforces the snapshot at execution, while `tool_search` activates within the same invocation and cannot activate disabled tools.
- AgentPool shares the primary `ToolManager`; it does not build a second application executor.

## Deviations

Role-specific Subagent capability validation is not represented by this mode policy and is covered by V03/V04.

## Conclusion

Passed for interaction-mode registry authority and reachability. Does not prove each Subagent role can execute every plan-declared tool.
