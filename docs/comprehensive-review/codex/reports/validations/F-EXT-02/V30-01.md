# F-EXT-02 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

F-EXT-02's nine specialized file/Git/worktree/sandbox findings are current,
source-conclusive, and distinct from the generic Tool contracts in F-EXT-01.

## Method

Primary review independently sampled current checkpoint creation/rollback,
worktree create/lookup/merge/removal, path confinement, file mutation, registered
edit authorities, Git branch metadata/process execution, Docker/Kubernetes
control phases, report links and clean source state.

No Cargo, rustc, test, build, destructive fixture, daemon, cluster or network
command was run.

## Expected Result

The two P0 findings directly demonstrate possible loss of ordinary local user
work. Remaining findings identify current reusable framework correctness gaps,
without recommending cloud-style permission gates.

## Result

- Exit code: 0 for static source, link and clean-state checks.
- Checkpoints tag only HEAD with second-resolution names; rollback checks out the
  tag over repository-wide `.`. Dirty/staged/untracked pre-mutation state is not
  captured, and unrelated tracked edits can be overwritten.
- Worktree suffixes are unconfined, unknown paths are synthesized as managed,
  removal unconditionally uses `--force`, branch deletion uses ignored `-D`, and
  failures can still return success; merge failure can leave partial state.
- Nonexistent nested descendants can bypass immediate-parent canonicalization
  through an existing symlink; mutators use check-then-direct-write/rename rather
  than one atomic revisioned primitive.
- Git/worktree execution uses blocking unbounded child processes inside async Tool
  paths. Docker/Kubernetes cancellation/deadlines cover only portions of resource
  lifecycle and cleanup results are discarded.
- GitBranch declares Read/ReadOnly while creating, switching and deleting branches.
- Both `update_file` and `edit_file` are registered replacement authorities with
  incompatible safety semantics; paging is applied after unbounded materialization.
- Static summary: `findings=9 unique=9 p0=2 p1=5 p2=2 current_anchors=1
  generic_tool_dedup=1 source_clean=1 links_ok=1`.

## Deviations

No destructive Git/filesystem/process fixture was run. The data-loss conclusions
follow directly from command construction and state scope; dynamic work remains
future regression evidence.

## Conclusion

Passed. F-EXT-02-P0-01 through P2-02 are accepted and the task is promoted to
`complete`.

## Follow-Up

First replace false checkpoint/worktree safety with exact owned state and
truthful partial outcomes. Then unify atomic file mutation, controlled child
execution and bounded query behavior. F-SEC-01 should backlink these concrete
implementation defects rather than duplicate them as policy findings.
