# F-EXT-02 / V16 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The corrected integrity gate proves that the F-EXT-02 task references every
existing validation attempt exactly once, uses canonical headers/paths, and did
not modify either source repository.

## Method

```text
1. Extract unique VNN-01.md tokens from the task report with:
   rg -o 'V[0-9]{2}-01\.md' .../tasks/F-EXT-02.md | sort -u
2. Enumerate actual attempts with find .../validations/F-EXT-02 -name
   'V??-01.md' -exec basename ... and sort.
3. Compare the two 15-entry outputs exactly (V01 through V15).
4. Inspect every validation's exact task heading, executor, commit, and source
   state headers; search task/finding prose for obsolete IDs and path leakage.
5. Recheck `git status --porcelain=v1` and HEAD in both source repositories.
```

## Expected Result

Referenced and actual attempt names are identical; headers are canonical; all
reports remain in the Codex tree; source states are clean at recorded commits.

## Result

- Exit code: 0 for corrected enumeration/search/source checks
- Duration: under 1 second
- Summary: task references and actual files were identical for V01-V15; every
  attempt uses task ID F-EXT-02, Attempt 01, and executor `Codex review
  subagent`; both source repositories were clean at the recorded commits. The
  only unified-terminology search token appeared inside V15's description of
  the integrity search itself, not as a project concept, finding, or direction.
- Log/artifact: no external artifact; compact outputs are reproducible from the
  commands above.

## Deviations

V15 remains immutable failed evidence. Its attempted use of BSD sed had an
undefined capture, and its `rg -L` interpretation was not relied upon here.
V16 is appended after checking V01-V15, as required for an immutable new attempt.

## Conclusion

The corrected integrity gate passes. F-EXT-02 is structurally ready for primary
evidence sampling with status `needs_evidence`; no source change or executable
validation was performed.

## Follow-Up

Primary acceptance should sample findings and may add its own validation
attempt without modifying V01-V16.
