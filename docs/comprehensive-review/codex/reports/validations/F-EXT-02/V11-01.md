# F-EXT-02 / V11 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The primary shell/code output and pagination code avoids byte-index UTF-8
truncation and unchecked arithmetic in the inspected paths.

## Method

```text
1. Inspect shell incremental decoder, chunk splitter, retained output, and
   output limit arithmetic.
2. Inspect run_code path/metadata conversion and result limits.
3. Search reviewed paths for string byte slices, direct vector indexing,
   unchecked offset/count arithmetic, unwrap/expect/panic/todo/unreachable.
4. Classify test-only assertions separately from production paths.
```

## Expected Result

Production paths split on character boundaries or decode incrementally, use
checked/saturating conversions, and return typed failures rather than panic.

## Result

- Exit code: 0 for targeted searches and inspection
- Duration: approximately 4 minutes
- Summary: ShellTool uses an incremental UTF-8 decoder and character-aware
  stream chunking; retained byte prefixes are converted lossily rather than
  sliced as str. RunCode uses checked path components and bounded numeric
  conversions. Reviewed production arithmetic is predominantly saturating or
  checked. No source-conclusive UTF-8 panic was found in these main paths.
- Log/artifact: `echo-tools/src/shell.rs:825`,
  `echo-tools/src/shell.rs:951`, `echo-tools/src/shell.rs:968`,
  `echo-tools/src/code.rs:304`, `echo-tools/src/code.rs:489`.

## Deviations

This positive static result does not cover every dependency parser, every OS
path encoding, or runtime chunk boundary.

## Conclusion

No additional UTF-8/panic/overflow finding is raised for the inspected primary
paths. Existing positive tests are inventoried separately; none were executed.

## Follow-Up

Retain split multibyte/emoji output and non-UTF-8 OS path regressions in the
future dynamic matrix.
