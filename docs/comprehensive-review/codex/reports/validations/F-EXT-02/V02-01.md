# F-EXT-02 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The reviewed tools are feature-exported, registered, and reachable from the
normal ReactAgent composition path.

## Method

```text
1. Inspect root Cargo feature forwarding for shell/files/git.
2. Inspect echo-tools::register_all_tools and register_readonly_tools.
3. Trace ReactAgent construction to the selected registrar and sandbox
   injection path.
4. Compare read-only and full registration sets.
```

## Expected Result

Each reported implementation is reachable through an enabled feature and the
full registry; mutation tools must not enter the read-only subset.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: the full registry includes ShellTool, RunCodeTool, all inspected file
  mutators/readers, GitBranchTool, and worktree tools. ReactAgent chooses the
  full or read-only registrar and later applies its sandbox. The read-only set
  excludes shell, file mutators, branch/commit, and worktree tools.
- Log/artifact: `echo-tools/src/registry.rs:197`,
  `echo-tools/src/registry.rs:224`, `echo-tools/src/registry.rs:260`,
  `src/agent/react/mod.rs:743`, `src/agent/react/mod.rs:1422`.

## Deviations

None.

## Conclusion

Findings in this task affect live framework paths, not unregistered helpers.
Public checkpoint rollback is additionally a documented external framework API.

## Follow-Up

None.
