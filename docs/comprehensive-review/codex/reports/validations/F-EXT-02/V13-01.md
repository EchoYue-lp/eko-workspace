# F-EXT-02 / V13 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Current framework documentation accurately states what Git checkpoint and
worktree cleanup preserve.

## Method

```text
1. Search current English documentation for checkpoint, rollback, automatic
   safety net, worktree merge, and cleanup claims.
2. Compare each behavioral claim with current source.
3. Inspect scoped Git history to distinguish mechanics from later fixes.
```

## Expected Result

Documentation does not promise exact repository-state capture, safe scoped
rollback, or successful merge/cleanup unless code provides those semantics.

## Result

- Exit code: 0 for static searches and git log
- Duration: approximately 3 minutes
- Summary: `docs/en/34-git-isolation.md` calls checkpoint an automatic safety
  net that captures the exact repository state, says rollback restores that
  state, and presents merge/cleanup as a natural successful flow. Current code
  tags only HEAD, rolls back all tracked paths, and force-removes/deletes while
  swallowing failures. Scoped history shows the checkpoint feature's original
  introduction but no evidence that the documented exact-state invariant was
  subsequently implemented.
- Log/artifact: `docs/en/34-git-isolation.md:175`,
  `docs/en/34-git-isolation.md:217`, `docs/en/34-git-isolation.md:351`,
  `echo-tools/src/git_checkpoint.rs:6`, `echo-tools/src/git_worktree.rs:115`.

## Deviations

Historical documents were treated as hypotheses, not copied as findings.

## Conclusion

The mechanics documentation is current, while the exact-state safety and
successful cleanup claims are stale/misleading. Supports `F-EXT-02-P0-01` and
`F-EXT-02-P0-02`.

## Follow-Up

Documentation must be corrected with the implementation; documentation-only
changes cannot resolve the data-loss findings.
