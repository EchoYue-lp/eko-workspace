# F-EXT-02 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Agent-callable worktree exit/merge identifies only owned worktrees, preserves
uncommitted/unmerged work by default, and reports every partial side effect.

## Method

```text
1. Trace enter_worktree path derivation and ManagedWorktree ownership facts.
2. Trace exit_worktree lookup fallback, optional merge, remove, and result text.
3. Inspect remove_worktree failure branches and branch deletion.
4. Inspect merge_worktree checkout/merge failure state and rollback behavior.
```

## Expected Result

Unknown paths are rejected; destructive cleanup requires verified ownership and
clean/merged state; each subprocess failure stops the sequence with precise
side-effect facts.

## Result

- Exit code: not-applicable (static side-effect trace)
- Duration: approximately 5 minutes
- Summary: a caller-controlled path suffix can escape `.worktrees`; an unknown
  exit path is synthesized as `managed=true` with a branch inferred from its
  basename. Removal always uses `git worktree remove --force`; failure is
  swallowed after prune; branch deletion uses `git branch -D` and its result is
  ignored; the helper always returns Ok and the Tool reports success. Merge
  checkout/merge can leave the main repository switched or conflicted with no
  restoration.
- Log/artifact: `echo-tools/src/git_worktree.rs:43`,
  `echo-tools/src/git_worktree.rs:115`, `echo-tools/src/git_worktree.rs:119`,
  `echo-tools/src/git_worktree.rs:130`, `echo-tools/src/git_worktree.rs:138`,
  `echo-tools/src/git_worktree.rs:193`, `echo-tools/src/worktree_tool.rs:187`,
  `echo-tools/src/worktree_tool.rs:225`.

## Deviations

No destructive worktree fixture was executed.

## Conclusion

The worktree lifecycle can discard local work, delete an unmerged branch, leave
partial repository state, and still report success. Supports
`F-EXT-02-P0-02`.

## Follow-Up

Future regression must cover unknown/external paths, dirty/untracked worktrees,
unmerged commits, remove failure, branch-delete failure, merge conflict, current
branch restoration, and exact partial-side-effect reporting.
