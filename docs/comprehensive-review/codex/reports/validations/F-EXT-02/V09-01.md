# F-EXT-02 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Git Tool permission and risk declarations truthfully describe all actions
reachable through each advertised schema.

## Method

```text
1. Compare GitBranchTool permissions/risk with schema action variants.
2. Trace each variant to concrete git arguments.
3. Compare full/read-only registry placement without relying on registry
   placement to repair metadata.
```

## Expected Result

Any branch creation, checkout, or deletion action declares write/execute risk;
read-only metadata is reserved for listing.

## Result

- Exit code: 0
- Duration: under 1 minute
- Summary: GitBranchTool declares only Read and ReadOnly, but the same Tool can
  create a branch, checkout another branch, or delete a branch. It is correctly
  excluded from the read-only registrar, yet callers enforcing the Tool's own
  permission/risk metadata receive a false contract.
- Log/artifact: `echo-tools/src/git.rs:380`, `echo-tools/src/git.rs:389`,
  `echo-tools/src/git.rs:396`, `echo-tools/src/git.rs:436`,
  `echo-tools/src/registry.rs:47`, `echo-tools/src/registry.rs:267`.

## Deviations

Generic permission pipeline semantics are outside this task; this validation
only proves the specialized Tool metadata/action mismatch.

## Conclusion

The Tool can perform repository mutations while claiming read-only behavior.
Supports `F-EXT-02-P1-05`.

## Follow-Up

Regression should exercise list/create/switch/delete through the real permission
pipeline and assert action-specific risk/approval facts.
