# F-EXT-02 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

RunCode's Docker and Kubernetes adapters apply cancellation and timeout to the
entire control-plane lifecycle, including availability, create, and cleanup.

## Method

```text
1. Trace RunCodeTool cancellation into SandboxExecutor.
2. Inspect Docker availability/create/start/remove/cleanup awaits.
3. Inspect Kubernetes availability/run/delete/global-cleanup awaits.
4. Identify exactly which awaits are inside timeout/cancel selection.
```

## Expected Result

The configured deadline/cancellation bounds every external control-plane
command and cleanup returns verified resource disposition.

## Result

- Exit code: not-applicable (static await-boundary trace)
- Duration: approximately 4 minutes
- Summary: RunCode forwards cancellation correctly. Docker applies the select
  only after an unbounded `docker info` and `docker create`; all `docker rm -f`
  calls are unbounded and ignored. Kubernetes bounds the attached `kubectl run`
  wait, but availability and `delete pod`/cleanup commands are unbounded and
  cleanup results are ignored. Cancellation/timeout terminal creation can wait
  indefinitely for cleanup and cannot prove that the resource was removed.
- Log/artifact: `echo-tools/src/code.rs:332`,
  `echo-tools/src/code.rs:358`, `echo-execution/src/sandbox/docker.rs:108`,
  `echo-execution/src/sandbox/docker.rs:318`,
  `echo-execution/src/sandbox/docker.rs:345`,
  `echo-execution/src/sandbox/docker.rs:403`,
  `echo-execution/src/sandbox/k8s.rs:93`,
  `echo-execution/src/sandbox/k8s.rs:136`,
  `echo-execution/src/sandbox/k8s.rs:305`.

## Deviations

No Docker or Kubernetes environment was contacted; network and dynamic fixtures
are prohibited.

## Conclusion

Execution body cancellation is implemented, but the whole sandbox resource
lifecycle is not bounded. Supports `F-EXT-02-P1-04`.

## Follow-Up

Future fault injection should hang each control-plane phase, cancel at each
phase, fail removal, and verify retryable orphan-resource facts plus later
cleanup.
