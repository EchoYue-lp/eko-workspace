# F-EXT-02 / V12 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Existing tests cover the principal positive shell/code behavior but do not
falsify the reported path, mutation, checkpoint, worktree, or remote sandbox
lifecycle defects.

## Method

```text
1. Inventory #[test]/#[tokio::test] under shell.rs, code.rs, files, git*,
   worktree_tool.rs, git_worktree.rs, and local/docker/k8s sandbox adapters.
2. Map tests to UTF-8, path confinement, cancellation, process cleanup, atomic
   write/conflict, checkpoint, worktree failure, and control-plane cleanup.
3. Do not execute tests.
```

## Expected Result

Coverage gaps are explicit and no unexecuted test is reported as passing.

## Result

- Exit code: 0
- Duration: approximately 3 minutes
- Summary: shell tests cover safety parsing, streaming, split Unicode, output
  retention/artifacts, timeout, and stream-drop cleanup; run_code tests cover
  language/path/symlink/isolation and result categories; local sandbox has a
  process-group cancellation test. File tests cover read pagination/UTF-8 and a
  stale hash case. Checkpoint/worktree tests are minimal; Docker/K8s tests focus
  on argument/spec construction. No test statically found for nested missing
  symlink ancestors, atomic replacement races/failures, exact dirty checkpoint
  rollback, destructive worktree partial failure, or hung control-plane cleanup.
- Log/artifact: test modules in the reviewed source paths.

## Deviations

All test outcomes remain unknown because execution is prohibited.

## Conclusion

The inventory explains why current tests do not refute this task's findings and
defines future regression ownership.

## Follow-Up

Execute the targeted matrix only during an authorized implementation/quality
phase.
