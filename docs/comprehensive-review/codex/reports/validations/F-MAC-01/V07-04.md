# F-MAC-01 / V07 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` concurrently dirty only under 38 generated frontend files

## Claim

The concurrent CLI change set can be recorded by path, Git status, and mtime
without reading file contents.

## Method

```text
set -euo pipefail
git -C echo-agent-cli status --short -- web-frontend/src/generated
for generated_path in $(git -C echo-agent-cli status --short -- web-frontend/src/generated | sed 's/^...//'); do
  stat -f '%m %N' "echo-agent-cli/$generated_path"
done
```

## Expected Result

Exit zero; exactly the disclosed generated paths are listed with status and
mtime, without a content read.

## Result

- Exit code: 0
- Duration: 0.02 seconds
- Summary: 38 paths had status `M`; mtimes were 1786521886 or 1786521887.
- Log/artifact: [generated-dirty-metadata.txt](artifacts/generated-dirty-metadata.txt)

## Deviations

No file content was read. Ownership is inferred from timing and concurrent task
activity, not from file contents.

## Conclusion

The dirty set is precisely recorded and outside F-MAC's write scope.

## Follow-Up

V07-05 enforces the exact count/path allowlist.
