# F-MAC-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Macro definitions are publicly exported and have identifiable production/example
consumers across both repositories.

## Method

```text
set -o pipefail
rg -n '#\[(tool|callback|guard|handler|compressor|permission_policy|audit_logger)|derive\([^]]*echo_macros::Tool|pub use echo_macros' echo-agent echo-agent-cli --glob '*.rs' --glob '!target/**' --glob '!**/.worktrees/**' | awk -F: '{print $1}' | sort | uniq -c | sort -nr
```

## Expected Result

The facade export, external examples, and any live split-crate consumers appear.

## Result

- Exit code: 0
- Duration: less than 0.01 seconds
- Summary: the facade re-exports all eight macros; attribute macros are used in
  framework examples, and derive is used by `echo-tools/src/git.rs` and
  `statistics.rs`. No EKO Rust consumer was found.
- Log/artifact: none

## Deviations

Counts include macro rustdoc source occurrences and are used only as an inventory,
not as runtime coverage.

## Conclusion

The public facade and real split-crate derive reachability are established.

## Follow-Up

V04-16 compiles the real Git derive consumer.
