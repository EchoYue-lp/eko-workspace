# F-MAC-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; review-only fixture outside source repositories

## Claim

A facade-only consumer can compile the facade-reexported `#[tool]` macro.

## Method

```text
cd docs/comprehensive-review/codex/reports/validations/F-MAC-01/artifacts/macro-probes
CARGO_TARGET_DIR=/private/tmp/f-mac-01-target cargo check --offline -p facade-minimal-attr
```

## Expected Result

Exit zero with `echo_agent` as the only direct dependency.

## Result

- Exit code: 101
- Duration: not separately measured across build-output yields
- Summary: expansion failed with four E0433 errors for undeclared `serde`,
  `schemars`, `serde_json`, and `futures` crates.
- Log/artifact: [fixture](artifacts/macro-probes/facade-minimal-attr/src/lib.rs)

## Deviations

The first shared dependency build required several harness yields; the final exit
code is known.

## Conclusion

Supports F-MAC-01-P2-02. It does not test derive or renamed dependencies.

## Follow-Up

V04-02 adds the four direct dependencies.
