# F-MAC-01 / V20 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Malformed macro metadata and user-controlled names produce deliberate syn
diagnostics rather than silent fallback or a procedural-macro panic.

## Method

Inspect `parse_tool_param_attrs` and its caller at
`derive_tool.rs:144-198,271-293`, plus guard name generation at
`echo-macros/src/lib.rs:397-405` and `to_pascal_case` at lines 777-789.

## Expected Result

Parser errors propagate as `syn::Result`; transformed names use fallible
identifier parsing with an input span.

## Result

- Exit code: 0 for source inspection; invariants failed.
- Duration: under one second.
- Summary: line 153 uses `if let Ok(parsed)` and silently treats every parser
  error as absent metadata. The guard path passes the transformed external name
  directly to `format_ident!`; there is no identifier validation before this
  panic-capable operation.
- Log/artifact: source anchors above.

## Deviations

No new compile fixture was built under disk pressure. Delegated V04-07 and
V04-08 preserve executable confirmation for both boundary inputs.

## Conclusion

F-MAC-01-P2-04 and P2-05 are accepted. Both violate the repository's explicit
diagnostic/no-panic contract for external input.

## Follow-Up

Return `syn::Result` from helper parsing and replace external-string
`format_ident!` with fallible `Ident` parsing.
