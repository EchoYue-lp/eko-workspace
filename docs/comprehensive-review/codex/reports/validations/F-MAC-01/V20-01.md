# F-MAC-01 / V20 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

The facade-reexported `derive(Tool)` emits only paths that the facade exports.

## Method

Independently inspect the derive expansion at
`echo-macros/src/derive_tool.rs:348-409`, the facade tool exports at
`src/tools/mod.rs:104-113`, and the sole `ToolRunner` definition at
`echo-core/src/tools/mod.rs:727-735`; search all root exports for `ToolRunner`.

## Expected Result

The generated `echo_agent::tools::ToolRunner` path resolves from a valid facade
consumer.

## Result

- Exit code: 0 for source inspection; invariant failed.
- Duration: under one second.
- Summary: derive line 387 unconditionally emits
  `<Self as #echo_crate::tools::ToolRunner<_>>::run`. Facade fallback resolves
  `#echo_crate` to `echo_agent`, but `src/tools/mod.rs` exports the execution
  `Tool` family without `ToolRunner`. Repository search finds `ToolRunner` only
  in `echo_core` and direct split consumers.
- Log/artifact: source anchors above.

## Deviations

The primary did not rebuild the already-isolated compile fixture because free
disk was below the repository's cleanup threshold. The emitted unresolved path
and missing export are deterministic compile-time facts; delegated V04-21 and
V04-22 retain executable confirmation.

## Conclusion

F-MAC-01-P1-01 is accepted. The public facade derive cannot resolve its own
generated helper trait.

## Follow-Up

Re-export the canonical core contract through one facade support surface and add
a facade-only compile-pass fixture.
