# F-MAC-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

One combined inventory command can establish macro definitions, consumers,
crate exports, and compile-test coverage.

## Method

```text
rg -n '^#\[proc_macro|^pub fn |^fn |^struct |^impl syn::parse::Parse|quote!|parse_macro_input|unwrap|expect|panic!|unreachable!|todo!|::serde|::schemars|::futures|echo_agent_crate_path|resolve_echo_crate_path' echo-agent/echo-macros/src/lib.rs echo-agent/echo-macros/src/derive_tool.rs
rg -n '#\[(tool|callback|guard|handler|compressor|permission_policy|audit_logger)|derive\([^]]*Tool' echo-agent echo-agent-cli --glob '*.rs' --glob '!target/**' --glob '!**/.worktrees/**'
rg -n 'echo_macros|pub use echo_macros|macro' echo-agent/Cargo.toml echo-agent/src echo-agent/echo-core echo-agent/echo-tools echo-agent/echo-execution echo-agent/echo-orchestration echo-agent/echo-state echo-agent/echo-integration --glob '*.toml' --glob '*.rs'
rg -n 'trybuild|compile_fail|ui_test|macrotest|cargo_expand|rustversion' echo-agent --glob '*.toml' --glob '*.rs' --glob '!target/**' --glob '!**/.worktrees/**'
```

## Expected Result

Every sub-search exits zero and the output remains separable by claim.

## Result

- Exit code: 1
- Duration: less than 0.01 seconds
- Summary: the first three searches returned results; the final test-framework
  search returned no match and made the combined command exit one.
- Log/artifact: none

## Deviations

Four separable claims were grouped, so this attempt is not acceptable evidence.

## Conclusion

This attempt supports no finding. V01-02, V02-01, V03-01, and V04-17 replace it
with atomic evidence.

## Follow-Up

Use the corrected atomic validations listed above.
