# F-MAC-01 / V01 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The macro entry points and shared expansion helpers have one enumerable source
authority.

## Method

```text
rg -n '^#\[proc_macro|^pub fn (derive_tool|tool|callback|guard|handler|compressor|permission_policy|audit_logger)|fn (echo_agent_crate_path|resolve_echo_crate_path|extract_fn_params|impl_block_to_boxfuture_methods|extract_boxfuture_methods_with_return|lifetimed_params|add_lifetime_a|to_pascal_case)' echo-agent/echo-macros/src/lib.rs echo-agent/echo-macros/src/derive_tool.rs
```

## Expected Result

Exactly one proc-macro crate defines one derive and seven attribute entry points,
with visible path, parameter, lifetime, and identifier helpers.

## Result

- Exit code: 0
- Duration: less than 0.01 seconds
- Summary: one `derive(Tool)` and seven attribute macros were found; derive and
  attribute paths use separate resolvers, while all attributes share the facade
  resolver and transformation helpers.
- Log/artifact: none

## Deviations

None.

## Conclusion

Definition ownership is established. This does not prove expansion correctness.

## Follow-Up

V02-01 and V03-01 trace consumers and boundary handling.
