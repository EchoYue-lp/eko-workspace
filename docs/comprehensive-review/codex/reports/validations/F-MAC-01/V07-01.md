# F-MAC-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` concurrently dirty under generated frontend files

## Claim

The final F-MAC-01 report set is internally consistent, uses real task IDs and
evidence links, preserves `needs_evidence`, and leaves reviewed source clean.

## Method

```text
zsh docs/comprehensive-review/codex/reports/validations/F-MAC-01/artifacts/final-gate.zsh
```

The complete gate is [final-gate.zsh](artifacts/final-gate.zsh). It checks report
count/executor, task status, matrix completion, placeholders, local links,
catalog task IDs, finding uniqueness/priority, de-duplication and primary handoff,
pinned commits, source cleanliness, and temp-target absence.

## Expected Result

Exit zero without output.

## Result

- Exit code: 1
- Duration: 0.13 seconds
- Summary: all report/link/ID/finding checks and `echo-agent` clean check passed;
  the final `echo-agent-cli` clean assertion failed because a parallel task had
  modified 38 `web-frontend/src/generated/*.ts` files after F-MAC began.
- Log/artifact: [final-gate.zsh](artifacts/final-gate.zsh)

## Deviations

The source tree changed concurrently. F-MAC did not read, create, or modify the
generated TypeScript files and does not revert them.

## Conclusion

This attempt does not establish full source-state hygiene.

## Follow-Up

V07-02 diagnoses the exact scope; V07-03 permits only the disclosed concurrent
generated-file set and rejects every other dirty path.
