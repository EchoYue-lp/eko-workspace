# F-MAC-01 / V20 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Generated macro code is hygienic with respect to helper crates and preserves or
explicitly rejects Rust generics and where clauses.

## Method

Inspect the attribute expansion at `echo-macros/src/lib.rs:203-265`, callback
method reconstruction at lines 650-671, and derive expansion at
`derive_tool.rs:226-410`; search the macro package for `split_for_impl`,
`where_clause`, and `Generics` handling.

## Expected Result

Generated paths resolve through the owning crate, and accepted generic syntax is
either preserved in generated items or rejected with a deliberate diagnostic.

## Result

- Exit code: 0 for source inspection; invariants failed.
- Duration: under one second.
- Summary: attribute and derive expansions directly name `::serde`,
  `::schemars`, `::serde_json`, and `::futures`, making them downstream direct
  dependency requirements. Both expansion families reconstruct types and impls
  from identifiers/fragments; the package contains no `split_for_impl`,
  where-clause, or generic-emission path despite accepting general syn inputs.
- Log/artifact: source anchors above.

## Deviations

No new Cargo build was started under current disk pressure. Delegated compile
fixtures independently show the exact missing-crate and absent-generic compiler
failures.

## Conclusion

F-MAC-01-P2-02 and P2-03 are accepted. The generated public contract leaks four
implementation crates and accepts syntax it cannot emit correctly.

## Follow-Up

Centralize support re-exports and add compile-pass/fail UI coverage for generic,
lifetime, const-generic, and where-clause inputs.
