# F-MAC-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Current project-owned macro documentation matches package names, dependency
requirements, and generated trait terminology.

## Method

```text
git -C echo-agent log --oneline --all -- echo-macros/src echo-macros/README.md echo-macros/Cargo.toml | head -n 80
rg -n 'macro|derive\(Tool\)|#\[tool\]|echo_macros' echo-agent/docs echo-agent/README.md echo-agent/echo-macros/README.md echo-agent/echo-tools/README.md --glob '*.md' | head -n 240
```

## Expected Result

Historical edits and current docs consistently describe the present package and
generated `Tool` contract.

## Result

- Exit code: 0
- Duration: 0.05 seconds
- Summary: current docs still reference nonexistent `echo_agent_macros`, call
  generated output `TypedTool`, and the split README declares only
  `echo_macros` while its sample imports `echo_core` and needs four helper crates.
- Log/artifact: none

## Deviations

Documentation defects already accepted by B-ARCH-01/F-API-01 are classified but
not duplicated as new F-MAC findings.

## Conclusion

The historical/document contract is stale/regressed. Macro implementation
findings rely on independent source and fixtures.

## Follow-Up

F-API-01 owns public guide corrections.
