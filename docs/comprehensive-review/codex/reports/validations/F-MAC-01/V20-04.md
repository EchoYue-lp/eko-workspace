# F-MAC-01 / V20 / Attempt 04

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

The macro crate contains executable package-owned contract tests for its public
derive and attribute APIs.

## Method

Inventory every file under `echo-agent/echo-macros`, inspect its manifest, and
search macro sources for test modules or a compile-test harness.

## Expected Result

At least one executable compile-pass/fail suite covers generated paths,
supported syntax, and diagnostics.

## Result

- Exit code: 0 for inventory; coverage invariant failed.
- Duration: under one second.
- Summary: the crate contains only manifest/README and two source modules. It
  has no `tests/` directory, trybuild/UI dependency, or unit-test module. Its
  examples are documentation blocks already observed as ignored by delegated
  package-test evidence.
- Log/artifact: `echo-agent/echo-macros/{Cargo.toml,src/lib.rs,src/derive_tool.rs}`.

## Deviations

The primary did not rerun the package Cargo command under disk pressure;
delegated V04-17 records its zero-test/ignored-doctest execution.

## Conclusion

F-MAC-01-P2-06 is accepted. A green package command currently exercises no
macro contract.

## Follow-Up

Add compile-pass/fail UI fixtures before changing expansion templates.
