# A-TSK-01 / V07 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; fixture only under `/private/tmp/a-tsk-01-fault-fixture`

## Claim

The authoritative JSONL reader tolerates or repairs a crash-truncated final event line without making all earlier committed events unavailable.

## Method

Use the successful direct-rustc fixture binary from V06. The `corrupt_tail` scenario creates a run, appends the bytes `{"seq":"` without a newline-complete event, then calls `TaskRuntimeStore::list_events`.

```text
/private/tmp/a-tsk-01-fault-fixture/fault-fixture corrupt_tail
```

## Expected Result

For the claim to pass, earlier valid events remain readable and the partial tail is ignored, quarantined, or repaired. The fixture exits 0 when current behavior instead rejects the whole read, so the validation status is determined from the printed behavior rather than the process code alone.

## Result

- Exit code: 0 (fixture observed the defect as designed)
- Duration: <1s
- Summary: `events_result=Err(InvalidPlan("file read: shadow decode: line 2: EOF while parsing a value at line 1 column 7"))`.
- Log/artifact: `/private/tmp/a-tsk-01-fault-fixture/src/main.rs`

## Deviations

None.

## Conclusion

The claim is false: one crash-truncated tail makes the entire authoritative event stream unreadable. Because `next_seq` also calls `read_events`, later appends cannot self-heal the file. Supports `A-TSK-01-P1-02`.

## Follow-Up

Add startup/read repair that only tolerates an invalid final non-newline record, preserves hard failure for interior corruption, and validates contiguous sequence numbers.
