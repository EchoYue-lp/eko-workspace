# A-TSK-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The EKO-to-framework task adapter preserves every current TaskSpec and TaskExecution field, while explicitly rejecting framework lifecycle states that the EKO DTO cannot losslessly represent.

## Method

Compare `echo-orchestration/src/tasks/runtime.rs:90-254` field by field with `types.rs:868-1200` and patch types at `types.rs:1218-1316`; inspect `load_revisioned_task_graph` and `compare_and_commit_revisioned_task_graph` at `store.rs:674-884`.

```text
TaskSpec: id, title, description, kind, agent_role, depends_on, files,
allowed_tools, required_artifacts, execution_checks, acceptance_criteria,
max_retries, metadata
TaskExecution: task_id, status, retry_count, failure_fingerprint, claim
Graph context: goal, assumptions, risks, execution_mode, metadata
```

## Expected Result

All framework fields round-trip or are carried in typed EKO metadata; semantic detail must not be silently flattened in the revision-store path.

## Result

- Exit code: not-applicable
- Duration: ~8 minutes inspection
- Summary: All listed fields have explicit mappings. EKO metadata carries domain profile, parallel group, sort order, plan ID, and plan domain. `TaskStatus` is persisted directly in `RunStateSnapshot`; `TodoStatus` is a UI/materialized projection. `PlanTask::try_from` rejects `Retrying` and `Paused` instead of claiming a lossless conversion. Patch fields map one-for-one for the current public patch contract.
- Log/artifact: None.

## Deviations

The executable round-trip test uses `Running` and does not enumerate all status variants or a non-null claim. The static mapping covers those fields, but an exhaustive property test is still desirable.

## Conclusion

No field-loss finding was established for the production revision adapter. This does not prove compatibility with future framework fields or malformed metadata.

## Follow-Up

V04 and primary exhaustive round-trip evidence.
