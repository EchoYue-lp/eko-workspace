# A-TSK-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; fixture only under `/private/tmp/a-tsk-01-fault-fixture`

## Claim

A syntactically valid `PlanRevisionCommitted` event with an invalid `plan` payload is rejected as malformed rather than silently reconstructed as an empty revision-0 plan.

## Method

The fixture creates a valid run, appends a fully valid JSON event whose `payload.plan` is `{"plan_id":7}`, reads the event stream, and calls the public `rebuild_plan_from_events`.

```text
/private/tmp/a-tsk-01-fault-fixture/fault-fixture invalid_commit
```

## Expected Result

Rebuild returns an explicit error identifying the invalid committed plan schema.

## Result

- Exit code: 0 (fixture observed the defect as designed)
- Duration: <1s
- Summary: `events=2 revision=0 plan_id="" tasks=0`. The invalid commit was silently ignored and `empty_plan_for` supplied a successful empty plan.
- Log/artifact: `/private/tmp/a-tsk-01-fault-fixture/src/main.rs`

## Deviations

None.

## Conclusion

The claim is false. `serde_json::from_value::<PlanRevision>(...).ok()` erases schema errors, so a corrupt authoritative commit can become an apparently successful empty plan. Supports `A-TSK-01-P1-03`.

## Follow-Up

Make invalid payloads and task references typed rebuild errors; add corrupt/interior/partial-state fixture coverage.
