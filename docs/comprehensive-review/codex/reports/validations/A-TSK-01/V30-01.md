# A-TSK-01 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

A-TSK-01's TaskRuntime authority and adapter findings are current; framework
dependencies F-TSK-01/F-TSK-02 are complete; the SubagentRun finding is narrowed
to the exact current definition/use evidence.

## Method

Primary review independently sampled current event append/projection rewrite and
read paths, JSONL parser/sequence recovery, authoritative event rebuild,
revisioned framework adapter/policy boundaries, SubagentAssigned/Released
persistence, Rust/TS SubagentRun definitions and all repository construction/use
sites. Completed F-TSK-01 and F-TSK-02 were checked for authority overlap.

No Cargo, rustc, test, build, fixture, failure injection or network command was
run.

## Expected Result

File authority failures are demonstrable from current control flow; the adapter
remains thin; no claim relies on the earlier mistaken statement that the
SubagentRun constructor itself is absent.

## Result

- Exit code: 0 for static source, dependency and clean-state checks.
- Every accepted mutation appends an authoritative event, then separately
  rewrites one or two projections. Normal reads directly trust `plan.json` and
  `run-state.json` without an applied sequence/generation or load-time repair.
- `read_events` hard-fails on any invalid non-empty line; `next_seq` depends on
  the same reader, so a crash-truncated tail disables history and later appends.
- `PlanRevisionCommitted` payload decoding uses `.ok()` and a missing decoded plan
  falls through to `empty_plan_for`, turning authoritative corruption into a
  successful revision-0 empty graph.
- `EkoRevisionedTaskStore` delegates load/CAS to TaskRuntimeStore and EKO policy
  adds product metadata/capability validation; it does not own a second patch,
  validator or ready-frontier authority.
- `SubagentRun::new` is publicly defined, correcting the delegated report's
  overbroad “no constructor” wording. Full-repository search finds no call or
  struct construction. Production persists assigned/released events and the
  frontend owns a separately shaped SubagentRunState projection, so the nominal
  aggregate remains disconnected.
- F-TSK-01 and F-TSK-02 are both complete and retain canonical ownership of the
  framework's parallel task authorities and DAG state defects.
- Static summary: `findings=4 unique=4 p1=3 p2=1 dependency_complete=2
  adapter_thin=1 source_clean=1`.

## Deviations

Delegated V04/V06/V10-V13 record historical executable attempts. Primary did not
rerun them after the user prohibited build/test work; current static evidence is
the acceptance basis.

## Conclusion

Passed. A-TSK-01-P1-01 through P1-03 and narrowed P2-01 are accepted, and the
task is promoted to `complete`.

## Follow-Up

Keep event/projection recovery entirely in the EKO application layer without
SQLite. Retain the framework revision service as the single graph authoring
authority and choose one typed SubagentRun projection for all surfaces.
