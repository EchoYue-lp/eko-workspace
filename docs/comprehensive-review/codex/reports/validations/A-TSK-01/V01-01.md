# A-TSK-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The TaskRun/PlanTask/SubagentRun and framework Task definitions can be enumerated across both repositories without relying on prior review conclusions.

## Method

From the workspace root, search Rust and generated/frontend TypeScript for definitions of TaskRun, TaskPlan, PlanTask, SubagentRun, TodoItem, Task, TaskSpec, TaskExecution, and their status enums; inspect the defining modules and conversion implementations.

```text
rg -n "pub (struct|enum) (TaskRun|TaskPlan|PlanTask|SubagentRun|TodoItem|Task|TaskSpec|TaskExecution|TaskStatus)|impl TryFrom<echo_agent::tasks::Task> for PlanTask" echo-agent/echo-orchestration/src/tasks echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src echo-agent-cli/web-frontend/src --glob '*.rs' --glob '*.ts' --glob '*.tsx'
```

## Expected Result

Framework-generic task types are owned by `echo-orchestration`; EKO-specific run, file, UI, and subagent projections are owned by app-core, with no second product runtime model hidden elsewhere.

## Result

- Exit code: 0
- Duration: <1s
- Summary: Framework `TaskSpec`/`TaskExecution`/`Task` are defined in `tasks/runtime.rs`; EKO `TaskRun`, `TaskPlan`, `PlanTask`, `TodoItem`, and `SubagentRun` are defined in `task_runtime/types.rs`. Additional TUI and Tauri types are presentation/query projections, not stores. `SubagentRun` is separately assessed in V14 because no live Rust construction appeared in this definition search.
- Log/artifact: None.

## Deviations

The repositories also contain older, generic framework task abstractions (`tasks/task.rs`, manager/executor types). This validation does not classify their framework-wide legitimacy; that belongs to F-TSK tasks.

## Conclusion

The core type locations and repository ownership are established. This supports the task's duplicate-model map but does not by itself prove runtime reachability or lossless conversion.

## Follow-Up

V02, V03, and V14.
