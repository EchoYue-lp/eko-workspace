# A-TSK-01 / V14 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

The declared durable Rust `SubagentRun` model is constructed and consumed by the runtime or UI as the single subagent-attempt projection.

## Method

Search constructors, struct literals, typed containers/returns, generated TypeScript imports, and actual assigned/released event consumers across both repositories.

```text
rg -n "SubagentRun::new|SubagentRun \{|Vec<SubagentRun>|Option<SubagentRun>|Result<.*SubagentRun|generated.*SubagentRun|: SubagentRun\b" echo-agent-cli echo-agent --glob '*.rs' --glob '*.ts' --glob '*.tsx'
rg -n "SubagentAssigned|SubagentReleased|subagent_assigned|subagent_released" echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src echo-agent-cli/web-frontend/src --glob '*.rs' --glob '*.ts' --glob '*.tsx'
```

## Expected Result

If `SubagentRun` is the durable model described by its docs, production code constructs/persists it and frontend code consumes the generated type, or a single explicit adapter materializes it from events.

## Result

- Exit code: first search 1 (no construction/typed-consumption matches), second search 0
- Duration: <1s
- Summary: Rust `SubagentRun`/`SubagentRun::new` and generated `SubagentRun.ts` have no live callers/importers. Production persists `SubagentAssigned`/`SubagentReleased` payloads and frontend `taskRuntimeSubagentExecutionEvents` independently builds `SubagentRunState`. The event path is live from executor to store to frontend hydration.
- Log/artifact: None.

## Deviations

Other result subtypes (artifact, verification, touched files) are live through event payloads. The finding is limited to the aggregate `SubagentRun` DTO and its constructor/generated contract.

## Conclusion

The claim is false. The source declares a durable aggregate that is not the actual authority, leaving two nominal shapes and misleading generated API surface. Supports `A-TSK-01-P2-01`.

## Follow-Up

Either delete the unused aggregate/generated type and document events as authority, or materialize exactly this type in one backend projection and have all UIs consume it; do not retain both shapes.
