# A-TSK-01 / V09 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean

## Claim

Framework `TaskPatchEngine` and `PlanValidator` are the only production structural patch/DAG authorities used by EKO; application duplicates are test-only adapters.

## Method

Search definitions and callers for patch application, DAG validation, CAS, and the legacy-looking app helpers; inspect `TaskRevisionService::finalize_and_validate` and app store cfg attributes.

```text
rg -n "PlanValidator|validate_task_snapshot|TaskPatchEngine|apply_operations|compare_and_commit|validate_runtime_plan|attach_plan_for_test|apply_task_patch_for_test" echo-agent/echo-orchestration/src echo-agent-cli/echo-agent-app-core/src --glob '*.rs'
```

## Expected Result

Production create/update routes use framework patch semantics and structural validation exactly once before the thin EKO CAS adapter. Any duplicate application helper is unreachable outside tests.

## Result

- Exit code: 0
- Duration: <1s search plus source inspection
- Summary: `TaskRevisionService` owns production patching and validation. `validate_runtime_plan`, `attach_plan_for_test`, and `apply_task_patch_for_test` are `#[cfg(test)]`; their callers are test modules. The store CAS validates revision and EKO metadata/identity but does not re-run the DAG validator. Runtime executors also validate loaded snapshots at execution boundaries, which is defensive consumption rather than a second authoring implementation.
- Log/artifact: None.

## Deviations

Framework-wide coexistence of older task APIs is outside A-TSK-01 and awaits F-TSK dependency reports.

## Conclusion

No production duplicate patch/DAG authority finding was established in EKO. This supports the intended framework/application split.

## Follow-Up

F-TSK-01 and F-TSK-02 must classify the framework-wide task families.
