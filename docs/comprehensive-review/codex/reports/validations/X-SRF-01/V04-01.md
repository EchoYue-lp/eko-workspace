# X-SRF-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty/HEAD-only; application clean

## Claim

Active-turn identity, cancellation, replay and terminal settlement are application-owned and survive adapter lifetime changes on every interactive surface.

## Method

```text
Compare GUI active-turn maps/events with frontend refs, TUI queued/active fields,
CLI local cancellation token and blocking REPL, and channel detached token/HITL
cancel. Inspect remount, disconnect, Ctrl+C, lag and resume paths.
```

## Expected Result

One conversation/turn lifecycle registry supports scoped cancel, rebind/replay and one terminal independent of renderer lifetime.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: GUI backend owns detached turns but the frontend loses correlation on remount; CLI/channel discard the cancellation handle; TUI keeps adapter-local state and cannot durably reconstruct Task/Subagent evidence after restart/lag. No common active-turn snapshot/replay/cancel registry is consumed by all surfaces.
- Log/artifact: this report

## Deviations

A-CHAT-01 owns shared terminal producer defects and is not an authorized X-SRF dependency; this validation confines itself to surface lifecycle ownership already established by authorized reports/current source.

## Conclusion

The claim fails and supports `X-SRF-01-P1-03`.

## Follow-Up

Add one application lifecycle service keyed by conversation + turn, then remove adapter-local ownership.
