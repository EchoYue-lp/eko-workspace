# X-SRF-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework dirty/HEAD-only; application clean

## Claim

One application capability manifest drives service startup and command availability for GUI, TUI, CLI, channel, cron and background hosts.

## Method

```text
Compare production composition roots, Tauri invoke registration, TUI SlashCommand,
CLI CommandRegistry, channel command interceptors and unattended service startup.
Classify trigger-specific N/A separately from missing complete-Agent capabilities.
```

## Expected Result

Every long-lived host starts common services once, while applicable attended capabilities map to the same application service.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: composition and command catalogs are handwritten per surface. Channel-only never calls `start_headless_services`; workflow CRUD/execution exists only inside Tauri; attended Task graph mutation is GUI-only; research export has CLI/channel/GUI but no TUI command; noninteractive CLI is absent. Cron legitimately lacks interactive input/HITL selectors, but these missing attended/host capabilities are not trigger differences.
- Log/artifact: this report

## Deviations

Natural-language Tool access can cover some workflows indirectly, but it does not provide the same attended control or typed delivery contract.

## Conclusion

The claim fails and supports `X-SRF-01-P1-01`.

## Follow-Up

Introduce one EKO capability/service manifest and derive thin per-surface registries.
