# X-INV-01 / V06 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: committed blobs only

## Claim

Cargo path dependencies are portable relative paths and framework worktrees
remain ignored.

## Method

```text
Search committed Cargo manifests for absolute, tilde, /Users, and .worktrees
path dependencies; inspect CLI root/app-core framework paths and framework
.gitignore. Classify documentation/example matches separately.
```

## Expected Result

No absolute/worktree Cargo dependency; exact sibling relative paths and
`.worktrees/` ignore are present.

## Result

- Exit code: 0
- Duration: static search
- Summary: zero absolute/tilde/worktree Cargo path matches. CLI root uses
  `../echo-agent` and app-core uses `../../echo-agent`; `.worktrees/` remains in
  framework `.gitignore`.
- Log/artifact: this report

## Deviations

Historical CLI planning prose contains developer-machine `cd /Users/...`
commands; Git-isolation docs intentionally show `.worktrees/`, and a test uses a
Unicode absolute literal. None is a manifest dependency.

## Conclusion

The relative Cargo/worktree path invariant passes at the pinned commits.

## Follow-Up

Keep the manifest-only absolute/worktree path check in merge gates.
