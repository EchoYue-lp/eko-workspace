# X-INV-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: committed blobs only; CLI lockfile excluded

## Claim

EKO does not enable or directly depend on SQLite while the generic framework
retains its legitimate optional SQLite implementations.

## Method

```text
Inspect committed CLI root/app-core Cargo manifests and grep committed CLI
manifests/Rust/TS for sqlite, SqliteStore, SqliteConversationStore, rusqlite,
and sqlx. Inspect the framework root feature/dependency declarations separately.
```

## Expected Result

No CLI dependency or feature activation; framework optional `sqlite` remains.

## Result

- Exit code: 0
- Duration: static search
- Summary: zero CLI manifest/source matches. Both CLI framework dependencies
  set `default-features=false` and list no SQLite feature. Framework exposes an
  optional `sqlite` feature and optional `rusqlite`, which is retained by design.
- Log/artifact: this report

## Deviations

CLI historical documentation mentions framework SQLite choices and explicitly
states EKO does not use them; these prose references are not activation.

## Conclusion

The EKO no-SQLite invariant passes without deleting framework capability.

## Follow-Up

Add a committed-manifest assertion that CLI never enables `echo-agent/sqlite`.
