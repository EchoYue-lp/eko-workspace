# X-INV-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: committed blobs only

## Claim

Production and public library paths contain no input-reachable unwrap, expect,
panic, unreachable, todo, direct-index, or overflow panic.

## Method

```text
Run committed-source candidate searches for unwrap/expect/panic/unreachable/todo
in both Rust repositories, then sample inline test modules, macro boundary,
public constructors/accounting, compression IDs, and eval inputs against their
canonical completed reports. Inspect both CI panic-Clippy commands statically.
```

## Expected Result

All matches are test-only, generated macro boundaries, or proven safe internal
invariants; reachable public/production inputs return typed errors.

## Result

- Exit code: 1
- Duration: static classification
- Summary: raw candidates total 1,452 framework and 383 CLI lines, dominated by
  inline tests in production files. CI does deny unwrap/expect/panic/unreachable
  for workspace lib/bins. Nonetheless canonical source samples remain current:
  zero-capacity EventBus construction, extreme token arithmetic, short
  checkpoint IDs, singleton eval split and related edge inputs can panic or
  overflow. Test assertions and Tauri generated glue are separately classified.
- Log/artifact: this report

## Deviations

Regex totals are candidate counts, not defect counts. Clippy was not executed.

## Conclusion

Invariant fails under existing canonical owners including
`F-CORE-01-P2-06/P3-07`, `F-CMP-01-P1-07`, and `F-EVO-01-P1-04`; no duplicate
finding is created here.

## Follow-Up

Fix the canonical owners and keep the mandatory panic-safety Clippy gate.
