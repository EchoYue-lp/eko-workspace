# X-INV-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: committed blobs only

## Claim

Strings that may contain user, model, filesystem, network, or generated Unicode
are never truncated or indexed by raw byte offsets.

## Method

```text
Search committed Rust for slice ranges, length/min truncation, as_bytes indexing,
and direct index patterns. Classify vector slices, byte protocols, delimiter
indices, char-boundary operations, tests, and human-text paths separately.
```

## Expected Result

Every string slice is ASCII/protocol proven or derived from a boundary in the
same string; arbitrary human text uses chars or an explicit char boundary.

## Result

- Exit code: 1
- Duration: static classification
- Summary: a narrow range pattern returned 56 framework and 14 CLI candidates.
  Most are vector slices, byte buffers, same-string delimiter boundaries,
  `floor_char_boundary`, tests, or false positives. Current sampled violations
  remain: Hybrid compression slices a public checkpoint ID to eight bytes,
  Eval regression slices a run ID by byte length, and EKO globstar iterates all
  byte offsets then slices arbitrary paths. A prior framework glob matcher has a
  positive multibyte regression and is classified fixed, not current.
- Log/artifact: this report

## Deviations

No runtime Unicode fixture ran by explicit review policy.

## Conclusion

Invariant fails under `F-CMP-01-P1-07`, `F-EVO-01-P1-04`, and
`A-PROJ-01-P2-08`; no duplicate finding is created here.

## Follow-Up

Repair canonical owners and add a focused forbidden human-text slice lint/search.
