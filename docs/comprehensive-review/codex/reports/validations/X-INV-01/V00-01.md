# X-INV-01 / V00 / Attempt 01

> Status: inconclusive
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both repositories externally dirty

## Claim

Initial repository inventory is isolated from external dirty files.

## Method

```text
Read git status and pinned commits. An initial rg command then explicitly named
echo-agent-cli/Cargo.lock while intending to exclude it with --glob '!Cargo.lock'.
```

## Expected Result

Only committed manifests and source are read; the externally dirty lockfile is
never inspected.

## Result

- Exit code: 2
- Duration: less than one second
- Summary: the explicit file argument overrode the exclusion intent and printed
  lockfile dependency-name lines; another named path did not exist. No file was
  modified. The execution is rejected in full and none of its dependency facts
  are used.
- Log/artifact: this report

## Deviations

This is an immutable isolation failure. All adopted evidence was rebuilt with
`git show HEAD:path`/`git grep HEAD`, without naming current `Cargo.lock`.

## Conclusion

Inconclusive and excluded from findings. It establishes the corrected evidence
boundary used by V01-V08.

## Follow-Up

Use committed blobs only, which the subsequent validations do.
