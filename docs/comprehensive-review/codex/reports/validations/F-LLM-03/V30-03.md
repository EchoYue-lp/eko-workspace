# F-LLM-03 / V30 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`

## Claim

Anthropic streaming errors and terminal reasons have an explicit, mode-consistent neutral mapping.

## Method

Static inspection of `AnthropicStreamEvent`, its catch-all match arm, and the separate non-stream/stream stop-reason tables.

## Result

The claim failed. `AnthropicStreamEvent` has no `error` variant and uses `#[serde(other)] Other`, which the stream match ignores. Separately, non-stream maps `max_tokens` to `length`, while stream passes the provider string through unchanged. Both are live trait paths.

## Conclusion

F-LLM-03-P1-03 and P2-06 are primary-accepted. Provider fixtures belong in the later fix regression suite.
