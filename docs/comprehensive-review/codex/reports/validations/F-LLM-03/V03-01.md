# F-LLM-03 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: framework clean; CLI had unrelated generated-frontend changes

## Claim

Anthropic request/response fields, block variants, cache facts, terminal
reasons, cancellation, and malformed envelopes are lossless or rejected
explicitly, with focused tests for each boundary.

## Method

Field/variant matrix over neutral `ChatRequest`, `ChatResponse`,
`ChatChunk`, Anthropic request/response/event structs, thinking translation,
cache-plan conversion, and all Anthropic/cache unit tests. Compared stream and
non-stream paths branch by branch.

## Expected Result

Every accepted neutral field and provider event has one explicit mapping or a
typed unsupported/invalid-response outcome; both methods are semantically
equivalent.

## Result

- Exit code: 0
- Duration: not separately measured
- Summary: happy-path text/tools/basic usage and automatic cache placement map,
  but standard hyphenated 4.6 names select the wrong thinking protocol;
  response enums omit thinking/redacted-thinking; stream events omit provider
  errors and thinking/signature deltas; stream and non-stream finish-reason
  normalization differ; non-stream cancellation is unused; malformed assistant
  tool arguments default to JSON null. Non-stream adds a prompt-cache beta
  header while stream does not.
- Log/artifact: source anchors in the task report

## Deviations

Multiple-system loss, omitted tool choice/response format/raw data,
capabilities, interleaved tool index, malformed generic SSE, split UTF-8,
stream-pending cancellation, and usage overflow remain under accepted
F-LLM-01 findings and are not renumbered here.

## Conclusion

The invariant and focused-test-completeness claims are false. This supports all
F-LLM-03 findings and defines the executable fixtures.

## Follow-Up

Primary reviewer should compare V12-V18 with these static branches.
