# F-LLM-03 / V15 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: source repositories not modified; localhost fixture only

## Claim

A well-formed Anthropic `error` SSE event terminates the neutral stream with
one error.

## Method

Returned HTTP 200 SSE containing only a named `error` event with type
`overloaded_error` and message `fixture overloaded`, then EOF.

## Expected Result

Exactly one error item retaining provider details and zero successful chunks.

## Result

- Exit code: 1
- Duration: below one second after compilation
- Summary: the returned stream contained zero items; the event was silently
  discarded.
- Log/artifact: private fixture `stream_error.rs`

## Deviations

`AnthropicStreamEvent` has no error variant and its catch-all has no payload.

## Conclusion

The claim is false and supports F-LLM-03-P1-03. This refines, rather than
duplicates, F-LLM-01's malformed-event finding: this envelope is modeled by
Anthropic as a named provider event, not malformed JSON.

## Follow-Up

Add explicit error/ping variants and terminal-error tests.
