# A-MEM-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Explicit project instruction/memory scope is deterministic, isolated, and
distinguishes file absence from read failure.

## Method

Inspect InstructionProvider load order and path selection for user, repository
AGENTS chain, project, learned rules, local, and hot memory. Follow each read
result into UnifiedMemory projection replacement.

## Expected Result

An explicit project root never implicitly reads global learned memory; only
NotFound removes a marker, while other read failures remain visible.

## Result

- Exit code: 1 (invariant failed)
- Duration: static branch inspection
- Summary: textual instruction ordering is deterministic and project context no
  longer duplicates AGENTS content. However, absent project MEMORY falls back to
  global MEMORY. All relevant `read_to_string` errors become `None`; refresh then
  removes the existing marker without returning an error.

## Deviations

No filesystem fault was injected. Branches and `.ok()` conversion are explicit
and do not depend on timing.

## Conclusion

Supports A-MEM-01-P1-04 and A-MEM-01-P2-06. It does not claim the intended
policy for an explicit user-global tier; no such tier currently exists.
