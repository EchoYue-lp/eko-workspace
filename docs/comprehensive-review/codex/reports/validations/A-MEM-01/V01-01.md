# A-MEM-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Framework mechanisms and EKO product policy can be separated without adding a
second Store, layer manager, compressor, or recall authority.

## Method

Search both repositories for Store, TypedMemoryStore, MemoryLayerManager,
InstructionProvider, projection markers, Dreaming, review integration, pool
overrides, and every memory construction/installation caller. Trace definitions
through registration and production use.

## Expected Result

Generic mechanisms remain in `echo-agent`; EKO owns only file protocol, scope,
surface scheduling, and projection reconciliation.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: `echo-agent` has one generic trait/implementation and layer/compression
  family. EKO adds `.eko` precedence, workspace binding, ReviewIntegration,
  commands, Dreaming runners, and AgentPool adaptation. No EKO SQLite feature is
  enabled. The relevant duplication is EKO reopening one Store path and
  duplicating refresh sequences, not a need for a new framework service.

## Deviations

Public framework alternatives with no EKO caller were not classified as dead;
they are valid reuse options under AGENTS.

## Conclusion

The layering table and deletion boundary in the task report are supported.
