# A-MEM-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Primary, ReviewIntegration, existing pool, and future pool Agents share exactly
one Store instance for a workspace generation at bootstrap, switch, and exit.

## Method

Trace each `create_memory_store_*`, Store clone, Agent install, ReviewIntegration
rebind, pool override, and future Agent constructor. Compare physical paths and
Arc provenance. Consume F-MEM-01-P0-02 for same-path multi-instance behavior.

## Expected Result

One constructor per generation; all consumers receive clones of that Arc.

## Result

- Exit code: 1 (invariant failed)
- Duration: static identity trace
- Summary: bootstrap shares one Arc. Switch creates Store A for primary/review,
  then passes a root to pool, which creates Store B for the same file. Exit
  repeats the split globally. Existing and future pool Agents share B, not A.
  F-MEM-01 proves independent same-path snapshots can overwrite one another.

## Deviations

This report does not duplicate the framework FileStore root cause. It establishes
the EKO live constructor topology that makes it a production data-loss path.

## Conclusion

Supports A-MEM-01-P0-01.
