# A-MEM-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

Every hot-layer create/promotion/delete path refreshes the hot marker on primary
and existing pooled Agents before another model request.

## Method

Build a static matrix for GUI/TUI/CLI remember and forget, framework callable
memory tools, Dreaming, evidence accept/undo/merge, TaskRuntime memory bridge,
and pool refresh. Compare mutation result with the exact refresh helper invoked.

## Expected Result

Hot changes call `refresh_hot_memory_projection` or the combined refresh for
primary and pool; non-hot changes do not produce a false hot refresh.

## Result

- Exit code: 1 (invariant failed)
- Duration: static caller matrix
- Summary: GUI/TUI/CLI promotion and hot deletion call the instruction-only
  helper; pool's method with a “hot-memory and instruction” comment also calls
  only that helper. Framework tool and several evidence/task mutation paths have
  no projection reconciliation callback. The combined helper exists but is not
  the shared mutation postcondition.

## Deviations

Some mutations remain warm and need no hot refresh; the finding is limited to
paths that return a promotion, locate a Hot entry, or can commit Hot changes.

## Conclusion

Supports A-MEM-01-P1-02 and the need for one typed mutation receipt.
