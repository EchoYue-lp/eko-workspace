# A-TSK-03 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; Codex report files only

## Claim

Existing tests cover the application controller's basic DAG path and the boundary-specific retry, completion-interruption, terminal-write, and background-dependency edge cases.

## Method

```text
Inventory #[test]/#[tokio::test] cases in:
- echo-orchestration/src/tasks/runtime_executor.rs
- echo-agent-cli/.../task_runtime/executor.rs
- echo-agent-cli/.../tasks/service.rs
- echo-agent-cli/.../task_runtime/task_execute_tool.rs
Map each test to the four A-TSK-03 findings without executing it.
```

## Expected Result

Basic dependency/revision/ownership/cancel behavior and all identified application boundary races/failure classes have targeted regression tests.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: framework tests cover dependency order, revision safe points, skip/cancel, claim reload, persisted error, and downstream block. EKO tests cover one-task completion, dependency order, inserted revision, result retry, pre-dispatch cancel/pause, ownership subsets, and cancellable background waiting. Gaps: no dispatch-error retry-budget parity; no pause/cancel injected after kernel Completed but before completion CAS; no terminal-store failure propagation; no cross-TaskRun dependency timeout/cycle/stall; no full `execute_run` completion-drain race test. Basic tests call private `execute_runtime_plan`, so they do not exercise outer settlement.
- Log/artifact: this report

## Deviations

Tests were read, not run, by explicit review policy.

## Conclusion

The claim is false. Existing tests support the normal single-kernel design but leave all four boundary defects without regression coverage.

## Follow-Up

Use the scenarios specified in P1/P2 regression-validation fields during implementation.
