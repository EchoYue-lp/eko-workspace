# A-TSK-03 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; Codex report files only

## Claim

Equivalent retryable execution failures consume the same `PlanTask.max_retries` budget and return to Pending at a framework safe point.

## Method

```text
1. Trace EkoRuntimeDagController::dispatch_task errors into resolve_dispatch.
2. Trace successful dispatch outputs classified by assess_task_execution.
3. Compare status writes, retry_count/claim.attempt arithmetic, requeue behavior, and returned RuntimeTaskResolution.
4. Inspect tests for both failure classes.
```

## Expected Result

Cancellation and non-retryable policy errors terminate immediately; typed retryable provider/timeout/execution failures requeue with a new claim until the common budget is exhausted.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: any `dispatch_task` error is converted directly to Cancelled/TimedOut/Failed status and a terminal resolution at `executor.rs:1356-1380`; `max_retries` is never inspected. Only `Ok(dispatch)` whose result is later classified `ExecutionFailed` uses `claim.attempt`, `task.max_retries`, `requeue_claimed_task`, and Pending (`:1396-1431`). The `runtime_plan_failure_propagates_and_blocks_downstream` test locks immediate dispatch-error failure (`:5638-5676`), while `real_execution_failure_retries_within_budget` covers only returned remaining-work (`:6125-6178`).
- Log/artifact: this report

## Deviations

Current `ReactError`/`TaskDispatchFailure` conversion does not preserve a typed retryability class, so some immediate failures may correctly be non-retryable. The defect is the inability to apply policy consistently, not that every error must retry.

## Conclusion

The claim is false and supports `A-TSK-03-P1-01`. EKO already requeues at framework safe points for one branch; remediation should unify classification there rather than add an inner retry loop.

## Follow-Up

Add typed recoverability and a shared resolution policy; retain one claim per physical attempt.
