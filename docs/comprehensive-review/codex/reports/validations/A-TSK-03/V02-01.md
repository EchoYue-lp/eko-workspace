# A-TSK-03 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: both source repositories clean; Codex report files only

## Claim

The EKO controller is registered on real task execution paths rather than existing only in tests.

## Method

```text
1. Trace task_execute registration and execution into execute_run.
2. Search all echo-agent-cli Rust callers of execute_run.
3. Trace background service, Tauri resume/retry, and TUI resume entry points.
4. Confirm the controller is constructed from execute_runtime_plan on each planned-run path.
```

## Expected Result

Registered task tools and all planned TaskRun resume/background entry points converge on `execute_run -> execute_runtime_plan -> RuntimeDagExecutor<EkoRuntimeDagController>`.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: `register_task_tools_on_agent` adds `ExecuteTaskTool`; its execute method validates the committed revision, registers run cancellation, and awaits `execute_run` (`task_execute_tool.rs:236-417`). Background service calls `execute_run` for materialized plans (`service.rs:359-430`). Tauri and TUI resume paths also call it. Each planned call reaches the sole EKO controller construction at `executor.rs:1623-1655`.
- Log/artifact: this report

## Deviations

Unplanned background runs use the Agent chat driver by design; they do not claim to execute a PlanTask DAG.

## Conclusion

The audited controller boundary is production reachable across application surfaces. This does not prove those entry points classify terminal outcomes consistently.

## Follow-Up

V04-V07.
