# F-CMP-01 / V08 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated EKO TypeScript changes present; review was source-read-only

## Claim

Existing test source can be mapped to each invariant, and uncovered scenarios
are explicit rather than inferred as passing.

## Method

```text
1. Inventory every #[test]/#[tokio::test] under echo-state/src/compression.
2. Read assertions for manager, strategies, invariants, verifier and horizon.
3. Map assertions to protocol, instructions, active task, recent evidence,
   repeated compression, fallback, attachment, cancellation and numeric cases.
```

No tests were executed per the review-stage prohibition.

## Expected Result

Static inventory distinguishes covered assertions from future executable
regressions needed for the discovered defects.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: source tests cover projection replacement/scope, canonical system
  dedup, protected relative placement, simple sanitizer cases, promoter
  invocation, SlidingWindow count idempotence, Adaptive CJK snip, Adaptive
  escalation, horizon parallel groups and 30/50-turn growth, and basic verifier
  rules. They do not cover Summary/Incremental actual LLM compression,
  second/third manager compression, empty/malformed summary, attachment eviction,
  cancellation, failed persistence acknowledgement, Adaptive fold plus sanitizer
  adjacency, short/non-ASCII checkpoint IDs, or maximum numeric configuration.
- Log/artifact: this report

## Deviations

The test named `invariant_token_target_met` asserts only token reduction, not
that `token_after <= token_limit`; this is consistent with F-CTX-01-P1-05.

## Conclusion

The inventory is complete for the assigned compression directory and explains
why dynamic regression work remains future validation. It proves no test passes.

## Follow-Up

Run the listed targeted cases only after the review phase, under the iteration
task that implements each fix.
