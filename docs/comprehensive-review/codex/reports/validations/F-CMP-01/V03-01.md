# F-CMP-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated EKO TypeScript changes present; review was source-read-only

## Claim

System instructions, protected projections, active task state, and the active
query survive compression through explicit lossless mechanisms.

## Method

```text
1. Inspect projection envelopes, protected split/merge, canonical reinjection,
   replaceable markers, and post-compression ordering.
2. Trace live skill projections and EKO TaskRuntime recovery projection.
3. Trace every read/write of CanonicalContext.skill_injections and
   CompressionInput.current_query.
```

## Expected Result

Every advertised canonical field is restored, protected values remain lossless,
and the current query influences every policy that claims to protect it.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: projection envelopes are always protected and EKO's run recovery
  capsule is refreshed at every model boundary; task briefs use a replaceable
  protected marker. Exact canonical system prompt and truncated project rules
  are reinjected. However, `merge_protected` restores by the old number of
  trailing compressible messages; after a large shrink that count exceeds the
  new output length and `saturating_sub` inserts a boundary projection at index
  zero, ahead of the base system prompt. `CanonicalContext.skill_injections` is
  considered by `has_any()` but never emitted, while the root adapter also puts
  project rules in both the base prompt and the supplemental canonical field.
  Live activated skills avoid the unused skill field because they now use
  projections. `current_query` is forwarded only through the manager and Hybrid
  metadata; no compressor uses it to select or protect a message, while canonical
  ReAct passes `None`.
- Log/artifact: this report

## Deviations

The lossless EKO task capsule and live skill projection pass; the generic public
canonical skill field and current-query promise fail.

## Conclusion

Supports findings F-CMP-01-P1-02, F-CMP-01-P2-01 and F-CMP-01-P2-02. It does
not claim task recovery is absent in EKO.

## Follow-Up

Retain the projection approach; align or remove the misleading unused fields
when implementing the fixes.
