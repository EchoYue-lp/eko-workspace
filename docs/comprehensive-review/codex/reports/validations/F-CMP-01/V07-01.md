# F-CMP-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated EKO TypeScript changes present; review was source-read-only

## Claim

Compression code is UTF-8 safe, panic-free for public/custom inputs, uses
checked arithmetic, and does not claim a token postcondition already owned by
F-CTX-01.

## Method

```text
1. Search compression production code for direct indexing/slicing,
   unwrap/expect/panic, and unchecked add/multiply.
2. Inspect every result in context rather than classifying by grep alone.
3. Compare token postcondition evidence with F-CTX-01-P1-05.
```

## Expected Result

User/provider/config/custom-compressor inputs cannot panic or overflow, and this
task links rather than duplicates the canonical budget finding.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: text truncation in Adaptive uses a proven char boundary and horizon
  error previews use `chars().take`, so those UTF-8 paths pass. Hybrid slices
  public/custom checkpoint IDs with `&checkpoint_id[..8]`, which can panic on a
  short or non-ASCII ID. Adaptive/Horizon perform unchecked public-config
  arithmetic including `target_tokens * 2`, `max_tokens * 4`, `keep * 2`, and
  percentage multiplications. Several count additions can also overflow at
  pathological limits. The missing within-budget postcondition is already
  established by F-CTX-01-P1-05 and was not duplicated here.
- Log/artifact: this report

## Deviations

Test-only unwrap/index occurrences were inventoried separately and were not
reported as production panics. `safe_truncate`'s final byte slice is backed by
an explicitly computed character boundary.

## Conclusion

Supports F-CMP-01-P1-07 and the cross-link to F-CTX-01-P1-05.

## Follow-Up

Use `chars().take(8)`, checked/saturating arithmetic, and invalid-config errors;
reuse F-CTX-01's token postcondition acceptance tests.
