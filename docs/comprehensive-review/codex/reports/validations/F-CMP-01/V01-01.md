# F-CMP-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` clean; `echo-agent-cli` had unrelated generated `ApiError.ts` and `StreamingEvent.ts` changes; source repositories were not edited

## Claim

Compression has one framework contract and a classifiable implementation/export
matrix, rather than a duplicate EKO-owned authority.

## Method

```text
1. Search both repositories for ContextCompressor, CompressionInput/Output,
   ContextManager, every `impl ContextCompressor`, and compressor constructors.
2. Read echo-core/src/compression.rs, echo-state/src/compression/**,
   root src/compression.rs and root prelude exports.
3. Classify concrete implementations and application adapters.
```

Static source inspection only; no feature, build, test, or fixture execution.

## Expected Result

`echo_core` owns portable contracts, `echo_state` owns reusable policy
implementations and the manager, the root crate re-exports them, and EKO only
selects policy or injects protected product context.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: one `ContextCompressor` trait was found in
  `echo-core/src/compression.rs:445`. Concrete implementations are
  SlidingWindow, Summary, IncrementalSummary, Hybrid, Adaptive, and
  VisibilityHorizon under `echo-state`; `echo-agent/src/compression.rs` is a
  facade. EKO owns no compressor or manager duplicate. Its
  `compact_context.rs` is a thin `PreModelContextProjector` for product task
  recovery, not a second compression engine.
- Log/artifact: this report

## Deviations

The top-level `echo-state` module comment enumerates only SlidingWindow and
Summary although four additional public strategies exist. This is documentation
drift, not duplicate authority.

## Conclusion

The ownership/export matrix passes. It does not establish behavioral correctness
or live selection; those are covered by V02-V07.

## Follow-Up

V02 traces construction and live ReAct reachability.
