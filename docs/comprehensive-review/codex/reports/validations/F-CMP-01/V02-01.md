# F-CMP-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated EKO TypeScript changes present; review was source-read-only

## Claim

Configured compressors are reachable from real ReAct requests, and the strategy
matrix distinguishes default/live application paths from optional framework APIs.

## Method

```text
1. Trace ReactAgent::new_inner -> ContextManager construction.
2. Trace AppConfig::apply_compressor and both EKO primary/pooled agent callers.
3. Trace stream run -> run_compact -> ContextManager::prepare -> compressor.
4. Search constructors of every public compressor and visibility horizon.
```

## Expected Result

Each claimed live strategy has a constructor, installation site, and model-call
path; public-only strategies remain valid framework options without being
misclassified as EKO dead code.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: `ReactAgent::new_inner` installs SlidingWindow(40) when a finite
  limit/budget exists. `AppConfig::apply_compressor` can replace it with Summary,
  Hybrid, SlidingWindow, or Adaptive; EKO runtime and pool both call this method.
  Normal streaming iterations call `run_compact`, apply projections, then await
  `ContextManager::prepare` before thinking. Current checked-in EKO YAML selects
  `sliding`; the framework config default is `summary`. VisibilityHorizon is a
  builder option. IncrementalSummary is public/exported and reasonably usable by
  independent consumers, but has no built-in EKO selector.
- Log/artifact: this report

## Deviations

The live selector documentation at `src/config.rs:449` omits the supported
`hybrid` value. The default installed in `new_inner` is later replaced when EKO
applies its configuration; these are sequential installation points, not two
simultaneous authorities.

## Conclusion

Compression is live on the canonical ReAct path and all public strategies have
a coherent use classification. It does not prove semantic preservation.

## Follow-Up

V03-V07 inspect preservation and failure behavior.
