# F-CMP-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; unrelated generated EKO TypeScript changes present; review was source-read-only

## Claim

Eviction promotion, compressor fallbacks, cancellation, and error paths either
complete durably or expose a typed failure without committing irreversible loss.

## Method

```text
1. Trace MemoryPromoter trait -> ContextManager callers -> StoreMemoryPromoter.
2. Trace primary failure, verifier failure, and SlidingWindow fallbacks.
3. Trace run cancellation into compression LLM ChatRequest fields.
4. Check checkpoint/metrics meaning on every branch.
```

## Expected Result

Evicted recovery facts are acknowledged only after successful persistence,
cancellation reaches compression LLM calls, and failed fallbacks restore the
original buffer with accurate diagnostics.

## Result

- Exit code: 0
- Duration: not recorded
- Summary: primary+fallback failure restores `original_messages`, which passes.
  But `MemoryPromoter::promote` returns `()`, `StoreMemoryPromoter` logs and
  swallows each store error, and ContextManager then commits eviction while its
  checkpoint counts all submitted messages as promoted. Summary/Incremental and
  Adaptive L4 construct LLM requests with `cancel_token: None`; `run_compact`
  awaits `prepare` while holding the context mutex and performs no cancellation
  selection. Verifier-fallback failure logs and continues with the lossy summary.
- Log/artifact: this report

## Deviations

A best-effort pre-compaction fact flush exists on automatic ReAct runs, but it is
also documented as non-blocking and does not cover every manual/public
compression path; it cannot serve as durable acknowledgement for evicted data.

## Conclusion

Supports F-CMP-01-P1-05 and P1-06. It does not assert every promoted message
contains an extractable fact; that is precisely why a message-count field cannot
represent successful fact persistence.

## Follow-Up

Return a typed promotion outcome, decide eviction policy on persistence failure,
and propagate the active cancellation token into every compression LLM request.
