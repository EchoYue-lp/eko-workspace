# A-BOOT-01 / V09 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The six reported startup/lifecycle gaps are present on real EKO entry paths,
properly layered in the application, and do not duplicate the accepted
B-PATH findings.

## Method

The primary reviewer independently traced both GUI binaries and every parsed
headless branch through `run_desktop_entry`, `AgentRuntime::bootstrap`,
`AgentCreateParams`, `start_headless_services`, `AppState::from_shared`,
`run_channels_mode`, and normal teardown. Definition/caller searches covered
`TaskRuntimeStore::new`, `AgentPool::shutdown`, `Agent::close`,
`ReactAgent::shutdown`, `ChannelManager::stop_all`, and BrowserRuntime start and
shutdown across both source repositories. The `B-PATH-01` task report was
compared only to enforce finding ownership.

## Expected Result

Each accepted finding has one reachable entry path and exact missing or
divergent behavior; framework close primitives already exist; pure-channel
service parity and scheduler/background token ownership remain with B-PATH.

## Result

- Exit code: 0 for all source searches and line inspections
- Duration: under one second per command batch
- Summary: explicit project selection reaches prompt/Subagent discovery while
  `working_dir` remains absent and TUI lists `.`; both GUI routes return an
  adapter that unconditionally returns `Ok(())`; Browser prewarm precedes the
  fallible agent constructor; primary/pool close APIs have no production EKO
  caller; combined channel-plus-CLI drops its spawned handle before
  `stop_all`; every headless TUI/CLI service start constructs a second
  `AppState` and runs TaskRuntime recovery before replacing that store. All six
  findings and their stated priorities are accepted. P1-03 remains medium
  confidence because the missing awaited rollback is certain but orphan
  duration depends on runtime scheduling.
- Log/artifact: none

## Deviations

No GUI, external MCP child, or real channel account was launched. This
validation proves control-flow and ownership gaps, not a deterministic orphan
process duration.

## Conclusion

The report's four P1 and two P2 findings are accepted without moving EKO
composition policy into the framework or duplicating B-PATH ownership.

## Follow-Up

V10 independently reruns the targeted watcher tests; V11 closes report
integrity.
