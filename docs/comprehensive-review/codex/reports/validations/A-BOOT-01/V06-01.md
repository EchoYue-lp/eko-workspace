# A-BOOT-01 / V06 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before report creation

## Claim

The live config watcher consistently watches app/global/project hook targets,
and current source plus tests support its deliberately limited hooks/webhooks
reload contract.

## Method

Inspect both entry registrations, watcher module documentation and reload body,
and pool resource extraction. Run the watcher unit tests from the
`echo-agent-cli` workspace without changing features.

```text
cargo test -p echo-agent-app-core config_watcher::tests --locked
```

## Expected Result

All watcher tests pass; GUI and headless pass a live primary agent plus shared
webhook emitter; pooled agents share the primary hook registry. Model/MCP/
runtime-limit changes remain explicitly restart-required rather than silently
partially reloaded.

## Result

- Exit code: `0`
- Duration: approximately 1.2 seconds
- Summary: 3 watcher tests passed, 0 failed. Headless and GUI each register the
  same watcher API (`src/main.rs:229-237`, `src/tauri/desktop.rs:163-171`). Its
  implementation reloads merged hooks and webhook endpoints, and explicitly
  documents model selection, MCP topology, and runtime limits as
  restart-required (`config_watcher.rs:249-277`). `AgentPool` extracts the
  primary agent's `Arc<RwLock<HookRegistry>>` and installs that same Arc in
  pooled agents (`agent_pool.rs:120-159`, `:882-887`), so hook replacement is
  shared across already-created and future pool agents.
- Log/artifact: none

## Deviations

The unit tests cover target/event classification, not an end-to-end filesystem
notification or webhook replacement. Full config/workspace lifecycle remains
assigned to `A-CFG-01`.

## Conclusion

The limited reload contract is consistently composed across GUI and headless
entry points. This does not claim full application config hot reload.

## Follow-Up

`A-CFG-01` should validate restart-required UI/API messaging and workspace
switch transactions; no A-BOOT finding is raised for the limited watcher.
