# X-AUT-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework evidence from committed blobs only; CLI `Cargo.lock` excluded

## Claim

Existing tests cover the local permission boundary's positive and negative
cases without encoding online over-gating or omitting data-loss/secret cases.

## Method

Statically inventory tests adjacent to `PermissionService`, AgentPool mode
propagation, MCP validators, path validation, revisioned file editing,
`native_write_file`, and webhook logging.

```text
rg -n '#\[test\]|#\[tokio::test\]|mod tests|native_write_file|validate_workspace_root|validate_ipc_path|Webhook delivery failed|write_workspace_file|set_permission_mode|connect_mcp_server' echo-agent-cli/src/tauri echo-agent-cli/echo-agent-app-core/src --glob '!Cargo.lock'
git -C echo-agent grep -n -e '#[test]' -e 'PermissionMode::' HEAD -- echo-orchestration/src/human_loop src/agent
```

## Expected Result

Tests cover default/full-auto decisions for automated Tools, direct-user
non-gating, arbitrary user-selected roots, stale writes, secret-redacted logs,
and malformed input. Tests do not assert fixed local-extension/public-network
blocks as product requirements.

## Result

- Exit code: 0
- Duration: below one second per inventory
- Summary: framework has mode-decision tests and AgentPool has existing/future Agent propagation coverage. MCP tests explicitly enshrine fixed launcher and localhost/private-network rejection. Path tests cover denied secret names, case folding, traversal and empty input but not user-selected external roots or benign paths containing `history`/`cookie`. The revisioned workspace editor has stale-write logic; `native_write_file` has no adjacent tests. Webhook emitter has no adjacent redacted-log assertion.
- Log/artifact: test anchors summarized in the task report

## Deviations

Tests were not executed by explicit review instruction. This is a coverage
inventory, not a pass/fail run.

## Conclusion

Failed on boundary coverage. Supports all three X-AUT findings and the future
regression matrix; does not assert that existing tests fail dynamically.

## Follow-Up

Run the listed regression cases during the fix phase, not this review phase.
