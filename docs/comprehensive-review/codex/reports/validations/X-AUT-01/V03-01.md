# X-AUT-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework evidence from committed blobs only; CLI `Cargo.lock` excluded

## Claim

Application logs do not disclose local secrets embedded in configured endpoint
URLs.

## Method

Inspect webhook endpoint representation, construction from canonical config,
live injection into GUI/CLI/channel chat resources, delivery failures/retries,
and all logging statements that mention endpoint/config/token/secret/headers.

```text
rg -n '(url|endpoint|headers|token|secret|env|command|args).*tracing::|tracing::.*(url|endpoint|headers|token|secret|env|command|args)' echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src --glob '!Cargo.lock'
rg -n 'WebhookEmitter|webhook_emitter|WebhookEvent::' echo-agent-cli/echo-agent-app-core/src echo-agent-cli/src --glob '!Cargo.lock'
```

## Expected Result

Failure logs identify a destination without printing userinfo, query tokens or
other credential-bearing URL components. HMAC secrets and event payloads are
not logged.

## Result

- Exit code: 0
- Duration: below one second per search
- Summary: canonical config URLs are cloned verbatim into the live emitter used by GUI, CLI, channel and scheduler. On initial and retry failure, the full `{url}` is interpolated into warning logs. URLs commonly carry webhook access tokens or signed query parameters; those values therefore enter local logs. The separate HMAC `secret` is not logged.
- Log/artifact: source anchors in X-AUT-01-P0-01

## Deviations

No network request was issued. The finding is conditional only on a configured
URL containing userinfo/query credentials, a supported ordinary webhook form.

## Conclusion

Failed. Supports X-AUT-01-P0-01. It is distinct from framework WebSocket bearer
logging and raw audit persistence owned by `F-SEC-01` because this leak is in
EKO's outgoing webhook adapter.

## Follow-Up

Redact to origin plus safe path label or endpoint ID and add a credentialed-URL
failure regression test.
