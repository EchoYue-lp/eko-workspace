# X-AUT-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework evidence from committed blobs only; CLI `Cargo.lock` excluded

## Claim

Automation permission policy is confined to Agent Tool execution, while direct
terminal, file, MCP and Browser paths do not consult that mode and retain only
local-product-appropriate validation.

## Method

1. Search committed framework permission mode, PermissionService and pipeline
   callers with `git grep ... HEAD`, then inspect `ReactAgent::set_permission_mode`,
   `PermissionService::check_with_permissions_in_mode`, and pipeline approval.
2. Search CLI `IpcAuth`, `permission_mode`, terminal/file/MCP/Browser commands and
   their registrations/callers, excluding `Cargo.lock`.
3. Inspect `path_validator.rs`, `ipc.rs`, `commands/workspace.rs`,
   `commands/mcp.rs`, `commands/browser.rs`, and frontend directory-selection
   to workspace-create flow.

```text
git -C echo-agent grep -n -e set_permission_mode -e PermissionMode -e permission_mode HEAD -- src/agent echo-orchestration echo-tools
rg -n 'IpcAuth|IpcPermission|require_not_strict|require_full_auto|permission_mode|set_permission_mode' echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src --glob '!Cargo.lock'
rg -n 'selectDirectory|create_workspace|native_read_file|native_write_file|native_open_path|connect_mcp_server|execute_main' echo-agent-cli/src echo-agent-cli/echo-agent-app-core/src echo-agent-cli/web-frontend/src --glob '!Cargo.lock'
```

## Expected Result

Agent-generated Tool actions pass one permission service with a default/full-auto
mode distinction. User-directed commands do not consult it. Directory selection
and explicit local MCP/browser/file operations remain usable except for empty,
malformed or clearly mistaken input.

## Result

- Exit code: 0 for all corrected searches/inspections
- Duration: below one second per command
- Summary: framework Tool execution consults the service; `full-auto` maps to `BypassPermissions`, while protected paths run first. EKO mode setters target primary/pool Agents. No live direct command calls `IpcAuth`; that type survives only in its own file. Direct Browser actions use `execute_main`, and direct file/MCP paths do not inspect Agent mode. However, every user-selected custom workspace root is passed through `validate_workspace_root`, which forces it under HOME and rejects broad credential-name substrings using an explicit XSS-exfiltration rationale. This rejects external drives and otherwise valid user-selected directories such as `~/projects/browser-history-analyzer`.
- Log/artifact: source anchors in X-AUT-01-P1-02

## Deviations

The already-owned GUI MCP fixed-launcher/private-host over-gate was observed but
is not renumbered; see `A-HITL-01-P1-06`. Framework protected-path false
positives are owned by `F-HITL-01-P1-09`.

## Conclusion

Failed. It passes mode separation but supports X-AUT-01-P1-02: direct directory
selection is still capability-gated by an online Web threat model after the
user explicitly selects the path.

## Follow-Up

Primary reviewer should reconstruct the frontend picker -> create IPC ->
validator chain from the fixed CLI commit.
