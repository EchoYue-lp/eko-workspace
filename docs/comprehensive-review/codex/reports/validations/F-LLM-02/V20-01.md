# F-LLM-02 / V20 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

OpenAI success and error/refusal envelopes are structurally distinguished before
neutral conversion.

## Method

Trace shared non-stream/SSE decoding into `ChatCompletionResponse`/
`ChatCompletionChunk`, then inspect response-message deserialization and the
concrete client's first-choice conversion.

## Expected Result

Top-level errors and refusal payloads remain typed facts and cannot become empty
success projections.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: both wire success structs default ID/choices and ignore unknown
  fields, so a top-level `error` can deserialize as success. Non-stream then
  reports unrelated `EmptyResponse`; streaming maps empty choices to a successful
  empty `ChatChunk`. `RawMessage` has no refusal field and converts null content
  to `Empty`, allowing refusal success with its only semantic payload erased.
- Log/artifact: `echo-core/src/llm/types.rs:282-313,617-637,787-829`,
  `providers/client.rs:109-170,317-349`, `providers/openai.rs:302-316,366-373`.

## Deviations

No new build under disk pressure. Delegated V08-V10 retain loopback protocol
proof for the exact envelopes.

## Conclusion

F-LLM-02-P1-01 and P1-02 are accepted.

## Follow-Up

Decode an explicit success/error union and preserve refusal in the neutral
response contract.
