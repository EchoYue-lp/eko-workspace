# F-LLM-02 / V06 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture under `/private/tmp`

## Claim

The compatible stream preserves two interleaved tool calls, their indices and
argument fragments, the `tool_calls` finish reason, and the usage-only terminal
chunk.

## Method

```text
env CARGO_TARGET_DIR=/private/tmp/f-llm-02-target cargo run --offline --quiet --bin stream_tool_usage
```

The local SSE fixture emits indices 0 and 1 in one chunk, completes both in a
second chunk, then emits choices-empty usage and `[DONE]`.

## Expected Result

Both calls assemble to valid, distinct argument JSON; finish reason and exact
usage survive; captured request has stream/include_usage true.

## Result

- Exit code: 0
- Duration: under 2 seconds
- Summary: calls assembled as `call_a/alpha/{"x":1}` and
  `call_b/beta/{"y":2}`; finish and usage 7/3/10 survived; request flags passed.
- Log/artifact: `/private/tmp/f-llm-02-probe/src/bin/stream_tool_usage.rs`

## Deviations

This validates the provider adapter's standard delta shape. It does not cover
the inherited loss of non-first choices/response identity.

## Conclusion

The normal parallel-tool and usage mapping passes.

## Follow-Up

Add this fixture to the repository test suite after fixes are implemented.
