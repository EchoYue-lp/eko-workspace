# F-LLM-02 / V20 / Attempt 04

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

`ChatResponse.raw` retains unmodeled provider facts and all public compatible
entry points delegate to one semantic authority.

## Method

Inspect serde attributes on the raw response, then inventory OpenAI concrete
client, default client, free helpers, exports, constructors and live callers.

## Expected Result

Unknown provider metadata is flattened/retained; compatibility routes are thin
delegates with identical defaults and field handling.

## Result

- Exit code: 0 for inspection; invariants failed.
- Duration: under one second.
- Summary: `extra` is a normal optional property rather than `serde(flatten)`,
  so unknown top-level and nested facts are discarded. Three public compatible
  routes remain; `DefaultLlmClient` and free helpers rebuild requests, drop
  thinking controls, and diverge on convenience defaults/empty responses.
- Log/artifact: `llm/types.rs:617-637`, `providers/openai.rs:139-207,215-472`,
  root `src/llm.rs` exports/helpers, factory search.

## Deviations

The disconnected generic `AdapterClient` remains owned by F-LLM-01 and is not
counted again here.

## Conclusion

F-LLM-02-P2-05 and P2-06 are accepted.

## Follow-Up

Expose honest raw JSON and migrate compatible callers to one concrete client.
