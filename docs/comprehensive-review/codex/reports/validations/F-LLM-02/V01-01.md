# F-LLM-02 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; review fixture/reports outside them

## Claim

The OpenAI-compatible provider has one request/response adaptation authority and
one public client contract.

## Method

Repository-wide symbol/behavior search across both repositories for
`OpenAiClient`, `DefaultLlmClient`, `AdapterClient`, `LlmClient` impls, public
`chat`/`stream_chat`, `ChatCompletionRequest` construction, re-exports, and all
constructors/callers. Definitions were then compared field by field.

## Expected Result

One live compatible-provider client builds the wire request, with compatibility
entry points delegating without changing defaults or accepted fields.

## Result

- Exit code: 0
- Duration: not separately measured
- Summary: found three public compatible-provider adaptation routes:
  `OpenAiClient`, `DefaultLlmClient` over free functions, and `AdapterClient`.
  Root facade re-exports `DefaultLlmClient` and adds another pair of public free
  wrappers. The concrete factory constructs `OpenAiClient`; no internal
  `DefaultLlmClient::new` call exists. `DefaultLlmClient` drops thinking, changes
  `chat_simple` temperature from 0.7 to 0.3, and changes empty-content behavior.
- Log/artifact: source anchors in the task report

## Deviations

`AdapterClient` duplication is inherited from F-LLM-01-P2-10 and is not
reported as a new F-LLM-02 finding.

## Conclusion

The single-authority invariant is false. This supports F-LLM-02-P2-06.

## Follow-Up

Primary reviewer should independently sample the constructors and re-exports.
