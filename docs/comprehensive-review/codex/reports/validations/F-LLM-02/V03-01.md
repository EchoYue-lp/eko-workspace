# F-LLM-02 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; review fixture/reports outside them

## Claim

Request, response, delta, tool, usage, error, cancellation, and attachment edge
cases are either preserved or rejected explicitly, and tests cover those
boundaries.

## Method

Field/variant matrix over `ChatRequest -> ChatCompletionRequest`,
`ChatCompletionResponse/Chunk -> ChatResponse/Chunk`, `Message`, tool deltas,
usage, shared transport, attachment normalization, and ReAct tool assembly.
Search current provider/core/ReAct tests for each boundary.

## Expected Result

No accepted field silently disappears; malformed/error envelopes become typed
errors; document bytes remain accessible; tests exercise all material branches.

## Result

- Exit code: 0
- Duration: not separately measured
- Summary: normal tools/format/thinking/user ID/usage map, but the non-stream
  token is unused; response structs ignore unknown top-level/choice/message
  facts; permissive defaults accept error-shaped JSON; non-image files become
  name-only placeholders. EKO marks short documents `Inline`, then supplies
  `ContentPart::File`, leaving no model-visible path after normalization.
  Existing OpenAI tests cover only four attachment normalization cases and
  shared client tests cover three basic SSE parser cases. There are no request
  capture, response metadata, error-envelope, cancellation, parallel-tool, or
  end-to-end usage fixtures.
- Log/artifact: source anchors in the task report

## Deviations

The inherited first-choice/stream-identity, typed tool-choice, split-UTF8,
malformed-SSE, and pending-stream-cancellation issues remain classified under
F-LLM-01 rather than duplicated here.

## Conclusion

The invariant and test-completeness claims are false. This supports all five
F-LLM-02 findings and defines the executable fixture matrix.

## Follow-Up

Primary reviewer should sample the attachment delivery trace and response serde
defaults.
