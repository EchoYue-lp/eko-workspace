# F-LLM-02 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean; fixture under `/private/tmp`

## Claim

A non-stream HTTP 200 body carrying an error envelope preserves the provider
error rather than turning it into an unrelated empty-response error.

## Method

```text
env CARGO_TARGET_DIR=/private/tmp/f-llm-02-target cargo run --offline --quiet --bin nonstream_error_envelope
```

## Expected Result

The call returns an error containing `provider overloaded`.

## Result

- Exit code: 1
- Duration: 0.90 seconds
- Summary: permissive `ChatCompletionResponse` decoding accepted the envelope
  with zero choices, then `OpenAiClient` returned `Empty response from LLM`; the
  provider message/type/code were lost.
- Log/artifact: `/private/tmp/f-llm-02-probe/src/bin/nonstream_error_envelope.rs`

## Deviations

HTTP non-success error handling is independently covered by V12 and passes.

## Conclusion

The error-normalization invariant is false. This supports F-LLM-02-P1-01.

## Follow-Up

Decode success/error envelopes as an explicit union before completion mapping.
