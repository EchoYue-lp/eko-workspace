# F-LLM-02 / V20 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

`ChatRequest.cancel_token` has the same accepted semantics for concrete
non-stream and stream calls.

## Method

Compare `OpenAiClient::chat` and `chat_stream` request consumption and the
shared `post`/`stream_post` signatures.

## Expected Result

Non-stream send/body waits race the token or the API explicitly rejects the
unsupported control.

## Result

- Exit code: 0 for inspection; invariant failed.
- Duration: under one second.
- Summary: non-stream request construction never reads `cancel_token`, and
  shared `post` accepts no token. Streaming alone passes the field to
  `stream_post`.
- Log/artifact: `providers/openai.rs:263-318,322-364` and
  `providers/client.rs:109-170,182-188`.

## Deviations

Delegated V11 supplies the timing proof: a 30-ms cancel returned the delayed
successful response after approximately 356 ms.

## Conclusion

F-LLM-02-P1-04 is accepted.

## Follow-Up

Race cancellation against non-stream send, headers/body and retry ownership.
