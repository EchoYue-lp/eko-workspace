# F-SUB-02 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean; review reports are outside them

## Claim

Checkpoint keys and payload validation bind resumed results to the same task,
plan item, Team definition, and assigned member deterministically.

## Method

```text
Inspect checkpoint IDs at manager_subagent.rs:18-28, synthesis fast path at
:93-102, stored-plan parsing at :112-132, member selection/reuse at :217-284,
and Team HashMap iteration at team/mod.rs:93-103 and :218-244. Compare checkpoint
payload contents with original task, Team revision, member name, and plan hash.
```

No executable test was run.

## Expected Result

Resume must reject stale or malformed checkpoints and must reproduce the exact
member assignment used by the original execution.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: IDs contain only run_id and phase/index. Stored synthesis is returned
  without binding to task or Team definition. Plan parsing silently drops
  malformed array entries and does not validate stored `idx`. Member assignment
  is index modulo a vector produced by randomized HashMap iteration, while member
  identity is absent from checkpoint nodes. A reused output is therefore not
  proven to belong to the current task/plan/member topology.
- Log/artifact: this report

## Deviations

The application normally intends run_id to identify one logical run; that does
not supply the missing plan/member revision or validate malformed store data.

## Conclusion

The claim failed and supports F-SUB-02-P1-07.

## Follow-Up

Future validation: resume across process seeds, reordered registration, modified
task/Team, partial malformed plan, mismatched index, and stale synthesis.
