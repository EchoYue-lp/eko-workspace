# F-SUB-02 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Method boundary: static source sampling only; no build, test, rustc, or dynamic fixture

## Claim

The delegated Subagent modes/Team lifecycle report is source-conclusive and can
be promoted from `needs_evidence` to `complete`.

## Method

Independently sampled the live background, Fork, and Team dispatch paths;
Teammate/background handle contracts; Team Agent adapter; TeamSpec/TeamConfig
conversion; manager fan-out and terminal construction; RuntimeStateStore
checkpoint load/save and identity; requested workspace-isolation fallback; and
the report's final mechanical integrity evidence. Cross-checked the now-complete
F-RCT-04 boundary to avoid duplicate batch-execution claims.

## Result

- Exit code: 0
- P1-01/P1-02 are supported: Fork unconditionally awaits the semaphore before
  constructing the execution cancellation race, and background spawn discards
  the JoinHandle while returning only execution/name strings.
- P1-03/P1-04 are supported: live Team resolves cached `Arc<dyn Agent>` values,
  invokes raw Agent methods, never applies `TeamSpec.config`, and therefore
  bypasses canonical invocation identity/cancellation plus declared bounds.
- P1-05 is supported: member failures become synthesis inputs or disappear on
  join failure, while a successful outer synthesis is unconditionally projected
  as `SubagentStatus::Completed`.
- P1-06/P1-07 are supported: checkpoint load/save errors are converted to empty
  or discarded outcomes; keys and payloads do not bind task/plan/Team/member
  revision, while member ordering is not a stable persisted authority.
- P1-08 is supported: requested worktree isolation fails closed, whereas a
  missing data-workspace factory only warns and continues in the ordinary path.
- The two P2 findings accurately distinguish the common joined Teammate route
  and public but disconnected TeamRunner/Coordinator/Mailbox lifecycle.
- V14 establishes fourteen delegated validation reports, ten unique finding
  headings, valid links/anchors, exact executor metadata, and clean source state.

## Conclusion

Primary acceptance passed. `F-SUB-02` is complete with eight P1 and two P2
findings. The future executable scenarios remain iteration regressions, not a
reason to weaken the static lifecycle conclusions.
