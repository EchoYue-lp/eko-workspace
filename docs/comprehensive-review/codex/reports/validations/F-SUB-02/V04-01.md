# F-SUB-02 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean; externally owned generated CLI paths not inspected

## Claim

Background mode returns a durable, controllable handle whose result can be
joined, cancelled, or recovered after a receiver lag/restart.

## Method

```text
Inspect BackgroundSubagentHandle and TeammateHandle at executor.rs:256-299;
dispatch_background at :830-868; SubagentEventBus at events.rs:334-380; and the
model-facing started response at agent_dispatch.rs:301-315. Search both
repositories for query/join/cancel APIs keyed by BackgroundSubagentHandle or its
execution_id, excluding UI projection code unrelated to framework ownership.
```

No executable test was run.

## Expected Result

Non-blocking dispatch must retain a recoverable result and cancellation/join
authority, or expose a durable query protocol rather than relying only on a
bounded ephemeral event subscription.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: the background task's join handle and request cancellation handle are
  dropped. The returned handle contains only two strings. Completion is exposed
  through a broadcast channel with capacity 128 and no replay/store/query API;
  send errors are discarded. The existing test subscribes before dispatch and
  proves only the timely happy path.
- Log/artifact: this report

## Deviations

None.

## Conclusion

The claim failed and supports F-SUB-02-P1-02. A caller that misses or lags the
event cannot recover the terminal result from the framework handle.

## Follow-Up

Future validation: delayed subscription, receiver lag beyond capacity, parent
completion, explicit cancellation, executor drop, and process-resume cases.
