# F-SUB-02 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean; review reports are outside them

## Claim

`TeamSpec.config` and strategy fields control the live Team runtime consistently,
including bounded fan-out and timeout ownership.

## Method

```text
Compare TeamSpec.config at types.rs:112-120 with dispatch_team builder calls at
executor.rs:1031-1070 and TeamAgentBuilder config methods at team/mod.rs:481-623.
Trace reads of TeamConfig.max_concurrent/default_timeout_secs/
allow_reassignment/cross_talk/mailbox_capacity and TeamStrategy::Swarm.batch_size.
Inspect ManagerSubagent fan-out at manager_subagent.rs:217-284.
```

No executable test was run.

## Expected Result

The definition's configuration should be applied to the built Team, concurrency
should be bounded in every parallel strategy, and every declared field should
either affect behavior or be rejected as unsupported.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: `dispatch_team` copies strategy but never applies `spec.config`; the
  builder therefore uses all TeamConfig defaults. ManagerSubagent spawns every
  planned line without a semaphore. `allow_reassignment` and `cross_talk` have
  no production reads; Mailbox/Coordinator are not used by TeamAgent execution;
  Swarm ignores `batch_size`. Only Swarm reads the default-built
  `max_concurrent`.
- Log/artifact: this report

## Deviations

The manager prompt requests 2-5 lines, but parser output is not structurally
bounded and model compliance is not a concurrency control.

## Conclusion

The claim failed and supports F-SUB-02-P1-04. Definition-level limits can be
silently ignored on the live path.

## Follow-Up

Future validation: configure max_concurrent=1, custom timeout, reassignment,
cross-talk, and Swarm batch sizes, then measure actual member starts.
