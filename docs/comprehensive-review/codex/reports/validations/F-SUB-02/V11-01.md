# F-SUB-02 / V11 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean; review reports are outside them

## Claim

Requested Fork filesystem isolation either establishes its declared boundary or
fails before execution, consistently for code and data workspaces.

## Method

```text
Inspect definition contract at types.rs:165-188, executor factory selection at
executor.rs:1601-1635, observed isolation at :1737-1766, and finalize behavior at
:1833-1882. Compare worktree.rs and workspace.rs factory contracts.
```

No executable test was run.

## Expected Result

A role declaring an isolated working directory must not run in the primary
directory when the corresponding factory is absent; observed isolation must
match the requested boundary.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: missing WorktreeFactory hard-fails, but missing
  DataWorkspaceFactory only warns and continues. The latter is then reported as
  Context isolation and runs in the ordinary directory despite the definition's
  disjoint-output intent. Create errors otherwise fail and finalize errors retain
  the original result.
- Log/artifact: this report

## Deviations

The type comment describes workspace creation failure, but does not explicitly
authorize missing-factory fallback; module documentation promises a fresh
workspace for the declared flag.

## Conclusion

The claim failed and supports F-SUB-02-P1-08.

## Follow-Up

Future validation: two concurrent data roles with identical output filenames,
with missing/failing factories, and assert no primary-directory writes.
