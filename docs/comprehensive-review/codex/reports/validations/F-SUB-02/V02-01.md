# F-SUB-02 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean at this inspection; externally owned generated CLI paths were not read or modified

## Claim

All four execution modes and background dispatch are reachable from the
model-facing framework Tool, while application definitions can reach Team mode.

## Method

```text
Trace agent_dispatch.rs:197-327 -> DispatchRequest -> executor.rs:407-565.
Trace Team definition loading in echo-agent-cli/echo-agent-app-core/src/
subagent_loader.rs:454-475 and registration in infra.rs:713-831.
Trace dispatch_team -> registry member resolution -> TeamAgent builder ->
execute_with_usage -> ManagerSubagentOrchestrator.
Search for production callers of TeamRunner and TeamCoordinator.
```

No executable command was run.

## Expected Result

Reachability evidence should distinguish the actual model/tool route from public
standalone helpers and tests.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: `agent_tool` parses Sync/Fork/Teammate/Team and background, constructs
  the request, and calls either `dispatch` or `dispatch_background`.
  `dispatch` selects all four mode branches. EKO parses `TeamSpec` frontmatter and
  registers it. Team member names are resolved from the registry at dispatch.
  `TeamRunner`/`TeamCoordinator` have documentation/tests but no production caller
  in either repository's inspected source.
- Log/artifact: this report

## Deviations

F-SUB-01 already owns the finding that the builtin Tool sends an explicit mode
override; this validation uses that as an input and does not duplicate it.

## Conclusion

The live-path claim passed. Findings in this task apply to actual framework/EKO
dispatch for the primary four-mode path; standalone Team APIs are labeled as
public but not claimed to be EKO runtime paths.

## Follow-Up

None.
