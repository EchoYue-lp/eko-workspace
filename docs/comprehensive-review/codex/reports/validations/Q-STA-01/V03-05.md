# Q-STA-01 / V03-05 / Attempt 02

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only.

## Claim

HybridCompressor may safely compose any public `ContextCompressor` implementation.

## Method

```text
Inspect echo-state/src/compression/compressor/hybrid.rs:36-128;
inspect public CompressionOutput/CompressionCheckpoint/ContextCompressor in
echo-core/src/compression.rs:46-145; search HybridCompressor exports/configuration.
```

## Expected Result

The pipeline does not assume a custom stage's public checkpoint ID is UUID-shaped.

## Result

- Exit code: 0.
- Summary: failed. `HybridCompressorBuilder::stage` accepts any public compressor, `CompressionCheckpoint.checkpoint_id` is a public deserializable String, and the pipeline formats `&cp.checkpoint_id[..8]` (`hybrid.rs:85-101`). Built-in constructor IDs are UUIDs, but a valid custom stage or persisted object can return a shorter/multibyte ID.
- Log/artifact: none.

## Deviations

The default EKO hybrid construction uses built-in stages; impact is on the framework's advertised composition API.

## Conclusion

Supports `Q-STA-01-P1-01` as a reusable-framework contract violation, not as evidence that EKO's built-in default currently emits malformed IDs.

## Follow-Up

Use `chars().take(8)` and add a custom-compressor checkpoint fixture.
