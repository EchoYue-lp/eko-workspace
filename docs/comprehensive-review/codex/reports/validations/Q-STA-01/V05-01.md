# Q-STA-01 / V05-01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only.

## Claim

Every production `unsafe` block has an enforceable safety contract at its API/call boundary.

## Method

```text
Search all committed production Rust before test modules for `unsafe`;
inspect echo-core/src/plugin/variables.rs:175-212 and CLI
echo-agent-app-core/src/infra.rs:1409-1501; trace GUI entry through
src/main.rs:59-76 and src-tauri/src/main.rs:3-5 to src/tauri/desktop.rs:67-130.
```

## Expected Result

Process-environment writes occur before any multi-threaded runtime exists, or are behind an unsafe API with a caller-enforced contract.

## Result

- Exit code: 0.
- Summary: failed. Framework `export_to_env` is a safe public function whose docs require callers to guarantee single-threaded initialization, but the type system/API cannot enforce that (`variables.rs:175-211`) and it has no internal caller. CLI `load_shell_env` uses `Once`, which prevents a second invocation but not concurrent environment access; both GUI entry binaries construct a Tokio runtime before `run_desktop` calls it (`src/main.rs:59`, `src-tauri/main.rs:4`, `desktop.rs:124-130`, `infra.rs:1478-1495`).
- Log/artifact: none.

## Deviations

Static review cannot observe actual libc environment readers on every target. The unsafe blocks and their stated precondition are nonetheless explicit.

## Conclusion

Supports `Q-STA-01-P1-02`: safe/public or live startup APIs do not uphold the documented single-threaded condition.

## Follow-Up

Prefer passing an explicit environment map into subprocess/provider configuration. If process mutation is retained, complete it before constructing Tokio/Tauri threads and make any residual unsafe precondition structurally enforceable.
