# Q-STA-01 / V03-01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only.

## Claim

Eval value extraction and regression-case construction never slice external strings at arbitrary byte offsets.

## Method

```text
Inspect src/eval/runner.rs:577-735 and src/eval/regression.rs:24-95;
trace SuccessCriteria::ValueMatch -> extract_number_near_key and public
RegressionSuite::from_traces -> build_case_from_run.
```

## Expected Result

Offsets are UTF-8 boundaries in the original string, and previews use characters.

## Result

- Exit code: 0.
- Summary: failed. `lower_text.find(lower_key)` returns an offset in a transformed lowercase allocation, then slices original `text` at that offset and an arbitrary `+ key.len() + 50` byte end (`runner.rs:725-729`). `RegressionSuite` slices public `Run.run_id` at `12.min(byte_len)` (`regression.rs:80`). Both APIs are exported at `eval/mod.rs:42,45` and `lib.rs:38`.
- Log/artifact: none.

## Deviations

No runtime fixture was permitted, so concrete Unicode strings were reasoned from Rust UTF-8 boundary semantics rather than executed.

## Conclusion

Supports `Q-STA-01-P1-01`: non-ASCII eval output/key or a short/multibyte persisted run ID can panic an eval/regression workflow.

## Follow-Up

Use regex match spans on original text or character-boundary-safe windows; add Unicode case-fold expansion and multibyte-ID tests.
