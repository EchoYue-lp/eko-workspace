# Q-STA-01 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only; external framework dirty source and CLI Cargo.lock excluded

## Claim

The Q-STA candidates are independently reproducible, exact duplicates are
removed from Q-level counts, and four remaining finding roots are acceptable.

## Method

Inspect committed PDF date and percent decoding, both GUI runtime/bootstrap
entry paths plus environment mutators, ImageFetch byte conversion and GUI token
threshold arithmetic, and exact CLI YAML manifest/source use. Compare Eval,
Hybrid, retry, compression, typed-memory and session sites with completed atomic
reports before accepting IDs.

## Result

- Exit code: 0
- PDF lossy text is sliced at byte offsets 4/6/8 after a byte-length check;
  percent decoding slices `input[i+1..i+3]`, which can split a multibyte code
  point after `%`. Both are registered/default Tool paths.
- Both GUI entry shapes create/enter Tokio before `load_shell_env` mutates the
  process environment. `Once` limits writers but does not enforce absence of
  concurrent readers; framework `export_to_env` exposes the same precondition
  through a safe public API.
- ImageFetch casts public JSON `u64` to `usize` and multiplies by MiB; GUI token
  threshold multiplies before division. Neither validates the extreme first.
- Exact committed search finds root `serde_yaml_ng` only in the CLI manifest;
  Rust YAML calls use `serde_yaml`.
- Eval/Hybrid/retry/compression/typed-memory/session matches are exact existing
  roots under F-EVO/F-CMP/F-REL/F-MEM/F-INT and were removed from new Q scope.
- Report links and source boundaries resolve; the unauthorized-report incident
  is disclosed and no exposed content was needed for these conclusions.

## Deviations

No dynamic panic, race, Clippy, build or extreme-value fixture was run. V10
remains the future implementation regression matrix.

## Conclusion

The narrowed Q-STA-01-P1-01, P1-02, P1-03 and P3-04 are accepted. Q-STA-01 is
complete for the authorized static scope.
