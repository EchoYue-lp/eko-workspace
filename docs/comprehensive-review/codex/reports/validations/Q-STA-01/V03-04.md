# Q-STA-01 / V03-04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only.

## Claim

EKO's gitignore globstar matcher is UTF-8 safe and production reachable.

## Method

```text
Inspect echo-agent-app-core/src/project/gitignore.rs:81-192 and tests :194-248;
search `GitIgnore`, `is_ignored`, `should_ignore_path`, and project-context callers
across committed CLI Rust source.
```

## Expected Result

Either character-boundary iteration is used, or the faulty helper is live and becomes a finding.

## Result

- Exit code: 0.
- Summary: the matcher is not UTF-8 safe: it iterates `j in 0..=remaining.len()` and slices `remaining[j..]`, then `j + part.len()` (`gitignore.rs:159-185`). Existing tests are ASCII only. However `should_ignore_path` is the sole production wrapper and has no caller; project tree generation bypasses the parsed gitignore entirely (`context.rs:124-179`).
- Log/artifact: none.

## Deviations

The claim combines behavior and reachability to prevent a false current-impact finding.

## Conclusion

Failed invariant but currently unreachable. It supports a future regression item, not a numbered finding. Enabling `should_ignore_path` without fixing globstar would expose the panic.

## Follow-Up

Replace custom matching with the existing/proven glob engine or character-safe matching, then wire only one ignore authority and add Unicode globstar tests.
