# Q-STA-01 / V03-02 / Attempt 02

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only.

## Claim

Registered PDF tools treat PDF metadata bytes as malformed input rather than panicking on UTF-8 boundaries.

## Method

```text
Inspect echo-tools/src/pdf.rs:19-159 and :169-230;
git grep 'PdfExtractTool\|PdfInfoTool' 3aa792... -- '*.rs'
```

## Expected Result

Malformed/non-ASCII PDF date values are parsed defensively.

## Result

- Exit code: 0.
- Summary: failed. Both registered `extract_pdf` and `pdf_info` reach `extract_pdf_metadata`; CreationDate/ModDate bytes pass through `String::from_utf8_lossy`, after which `rest.len() >= 8` is a byte-count guard and `rest[0..4]`, `[4..6]`, `[6..8]` can split a replacement or multibyte character (`pdf.rs:187-227`). Registrations are at `registry.rs:97-109` and `:312-322`.
- Log/artifact: none.

## Deviations

Feature activation was not compiled; reachability is definition plus registry under the existing domain-tool feature.

## Conclusion

Supports `Q-STA-01-P1-01`: a local malformed PDF can panic a registered read Tool when metadata is requested.

## Follow-Up

Validate ASCII digits on original bytes, return raw/lossy value on invalid format, and add malformed metadata fixtures.
