# Q-STA-01 / V03-03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only.

## Claim

DuckDuckGo redirect percent decoding accepts arbitrary response URL strings without slicing inside a Unicode scalar.

## Method

```text
Inspect echo-tools/src/web/providers/utils.rs:25-47;
trace DuckDuckGoProvider::extract_url at duckduckgo.rs:41-54 and the default
WebSearch provider construction at web/search.rs:42-74.
```

## Expected Result

Malformed `%` sequences adjacent to Unicode pass through or return an error, never panic.

## Result

- Exit code: 0.
- Summary: failed. The decoder walks every byte, and after `%` slices `input[i+1..i+3]` without ensuring both indices are character boundaries (`utils.rs:28-44`). `%` followed by a three/four-byte character can split it. DuckDuckGo applies the function to response `href` values and is the no-key default provider.
- Log/artifact: none.

## Deviations

No network or dynamic parser fixture was run.

## Conclusion

Supports `Q-STA-01-P1-01`: malformed search response content can panic the default web search path.

## Follow-Up

Decode from byte hex digits only; add `%€`, truncated percent, invalid hex, plus and encoded-UTF-8 cases.
