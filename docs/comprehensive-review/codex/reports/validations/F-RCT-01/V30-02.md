# F-RCT-01 / V30 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`

## Claim

`AgentConfig.allowed_tools` is enforced at the common schema/execution choke point regardless of registration API.

## Method

Static definition/reader search across config, initial registration, builder custom tools, `add_tool(s)`, trait registration, AgentRunSnapshot schema selection, and execution policy.

## Result

The claim failed. The root allowlist is read only by batch `add_tools`; builtins, initial tools, single-tool and trait registration bypass it. Snapshot/execution filtering covers other policies but not `config.allowed_tools`.

## Conclusion

F-RCT-01-P1-03 is primary-accepted. This is automated-agent capability correctness, consistent with the local application threat model rather than an interaction permission gate.
