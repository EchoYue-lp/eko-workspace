# F-RCT-01 / V02 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Core builder budgets, token limits, hooks, tool pipeline, intent router, and visibility-horizon options reach their intended runtime owners.

## Method

```text
Map builder.rs:872-1064 into AgentConfig and injected subsystems; trace
run_budget/max_iterations into stream/direct loops, token_limit/token budget
into ContextManager and request metadata, hook_registry into prepare/approval/
pipeline/finalize/subagent executor, pipeline into execution, intent_router into
direct and streaming loops, and visibility horizon into ContextManager.
```

## Expected Result

Each mapped option has a runtime reader on live ReAct paths and shared dependencies retain identity.

## Result

- Exit code: 0.
- Duration: under one second per search/slice.
- Summary: The inspected budget, token, hook, pipeline, router, and visibility options are consumed. The hook registry is allocated before subagent executor construction and the same Arc is stored in ToolExecutionSubsystem and run snapshots. `run_budget` is read in streaming iteration control; token settings build ContextManager and request limits.
- Log/artifact: this report.

## Deviations

This validation does not review budget arithmetic (F-REL-01) or ReAct terminal semantics (F-RCT-02).

## Conclusion

Supports the positive option-to-runtime map and narrows findings to the explicitly listed disconnected options.

## Follow-Up

Downstream F-RCT-02 should reuse only the ownership map, not infer behavioral correctness.
