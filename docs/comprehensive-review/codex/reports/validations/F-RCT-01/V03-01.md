# F-RCT-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Initial system-prompt assembly and canonical compression state represent each project-rule instruction exactly once.

## Method

```text
Inspect ReactAgent::build_system_prompt (src/agent/react/mod.rs:676-722),
new_inner ContextManager/canonical construction (:330-382),
CanonicalContext::to_reinjection_messages
(echo-core/src/compression.rs:345-401), and ContextManager canonical reinjection
(echo-state/src/compression/mod.rs:854-925). Compare exact stored strings and
dedup predicates.
```

## Expected Result

Project rules have one authority and one effective occurrence before and after compression.

## Result

- Exit code: 0.
- Duration: under one second per source slice.
- Summary: `build_system_prompt` injects raw project rules into the complete system prompt. `new_inner` stores that complete prompt as canonical `system_prompt` and separately stores the same rules in `canonical.project_rules`. After compression, the complete prompt already matches and survives, while `to_reinjection_messages` emits a differently wrapped project-rule system message. Exact-string dedup cannot recognize the duplicated payload, so a second copy is inserted.
- Log/artifact: this report.

## Deviations

No dynamic compression fixture was run under the user's review-only constraint; the branch and exact-string comparison are source-conclusive.

## Conclusion

Supports F-RCT-01-P1-02: project-rule cardinality is two after the first canonical reinjection.

## Follow-Up

Future fix validation should force compression with a real rules file and count the rule body across all system messages.
