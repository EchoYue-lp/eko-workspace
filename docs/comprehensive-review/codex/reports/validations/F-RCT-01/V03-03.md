# F-RCT-01 / V03 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Changing a ReAct agent working directory refreshes both the active and canonical project-rule sources.

## Method

```text
Trace ReactAgent::set_working_dir -> refresh_root_system_prompt
(src/agent/react/mod.rs:907-945) against new_inner canonical construction
(:358-382) and ContextManager::set_canonical_system_prompt
(echo-state/src/compression/mod.rs:862-867).
```

## Expected Result

The new directory's assembled prompt and separate canonical rule source replace the old directory's values atomically.

## Result

- Exit code: 0.
- Duration: under one second.
- Summary: Refresh recomputes the full system prompt for the new path and updates only `canonical.system_prompt`; the setter explicitly preserves canonical rules and skills. `canonical.project_rules` therefore remains from construction. On later compression, the old rule body is emitted as a supplemental system message. A failed `try_lock` also leaves prompt state unchanged even though `config.working_dir` already changed.
- Log/artifact: this report.

## Deviations

No concurrent lock fixture was run; the stale-rule branch is direct from the setters.

## Conclusion

Supports the stale-workspace portion of F-RCT-01-P1-02.

## Follow-Up

Future regression should switch between two directories with distinct rule files, compress, and assert only the second rule survives.
