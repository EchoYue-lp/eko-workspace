# F-RCT-01 / V03 / Attempt 08

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Existing tests cover prompt cardinality, mutation, feature absence, allowlist choke points, disconnected options, and injected-memory state strongly enough to catch the observed defects.

## Method

```text
Search and inspect tests under src/agent/react/tests.rs, builder.rs,
echo-state/src/compression/mod.rs, prompt-template tests/examples, loop-detector
tests, and config tests for canonical/project rules/system prompt/working dir/
allowed_tools/human-loop/subagent/prompt_template/notebook/loop detector/memory.
```

## Expected Result

Each public construction invariant has at least one assertion at the final runtime owner, including negative/disabled cases.

## Result

- Exit code: 0 for adopted bounded inspections.
- Duration: under one second per search/slice.
- Summary: Tests cover basic builder field mapping, enabled-feature registration, direct template manager rendering, detector internals, and canonical system-message dedup when project rules are separate. They do not count a real rule body embedded in both prompt and supplemental canonical state; do not test canonical update after prompt/cwd mutation; intentionally assert only trait override storage; do not test absent-feature successful no-ops; do not test allowlist across registration APIs; do not test agent consumption of template/notebook/detector; and do not assert the memory-enabled flag after external-store injection.
- Log/artifact: this report.

## Deviations

One mixed test-search invocation included a nonexistent `echo-state/src/context` path and exited 2; it is preserved separately as V90-02 and not used as the adopted evidence.

## Conclusion

Supports the stated regression gaps for all findings. Existing unit tests do not falsify the source-conclusive defects.

## Follow-Up

Add the future matrix in V04-01 after fixes are implemented.
