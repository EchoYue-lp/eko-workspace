# F-RCT-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Normal ReAct construction uses one authoritative ToolManager from registration through LLM schema exposure and execution.

## Method

```text
Trace ReactAgent::new_inner -> ToolManager::new_with_config -> core/feature/custom
registrations -> Arc<ToolManager> in ToolExecutionSubsystem ->
AgentRunSnapshot::from_agent -> tools_for_llm -> think::tools_for_request;
inspect ToolManager register/register_tools/list/schema ordering and all
set_tool_manager/register_tool/add_tool/add_tools paths.
```

## Expected Result

Registrations and request schemas share one manager; duplicate names have deterministic semantics; definitions are stable.

## Result

- Exit code: 0.
- Duration: under one second per bounded inspection.
- Summary: One ToolManager is created, all initial tools are registered before/through the same Arc, snapshots clone that Arc, and LLM schemas flow through `tools_for_llm`. DashMap registration replaces duplicate names, invalidates the schema cache, and list/schema definitions are sorted by name. Pool injection can replace the Arc explicitly but does not create a parallel per-request registry.
- Log/artifact: this report.

## Deviations

Policy filters are layered after registration; V03-05 shows that `AgentConfig.allowed_tools` is not among the snapshot filters.

## Conclusion

Supports a positive “one registry” conclusion while bounding the separate allowlist finding F-RCT-01-P1-03.

## Follow-Up

Add a future regression asserting identical manager identity after pool injection and schema cache invalidation.
