# F-RCT-01 / V30 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`

## Claim

Runtime prompt mutation and project-rule reinjection have one coherent authority.

## Method

Static source/reachability inspection only: compare both `set_system_prompt` APIs, prompt getters, ContextManager canonical setters/reinjection, initial project-rule composition, cwd refresh, and bounded EKO callers.

## Result

The claim failed. The trait setter writes only `mutable_system_prompt`, while no turn path reads it; the inherent setter updates config/current message without canonical recomposition. Initial rules are embedded in the complete system prompt and also retained in `canonical.project_rules`, so compression reinjects a differently wrapped second copy. Cwd refresh replaces only canonical system text and preserves the old separate rules.

## Conclusion

F-RCT-01-P1-01 and P1-02 are primary-accepted. Their remediation must be one prompt/canonical authority, with separate regressions for runtime mutation and workspace-rule cardinality.
