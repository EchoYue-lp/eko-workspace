# F-RCT-01 / V03 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Every public runtime system-prompt mutation updates the active context and canonical compression authority consistently.

## Method

```text
Inspect the Agent trait setter in src/agent/react/mod.rs:3017-3022, the inherent
async setter in capabilities.rs:1336-1355, current/system getters in
mod.rs:984-995 and :2750-2764, ContextManager canonical setter, and live CLI/TUI/
app-core callers in echo-agent-cli. Search all readers of mutable_system_prompt
and build_system_prompt.
```

## Expected Result

One mutation path recomposes COT/model/rules sections, updates the active system message, and updates canonical state before reporting success.

## Result

- Exit code: 0.
- Duration: under one second per search/slice.
- Summary: The trait setter writes only `mutable_system_prompt`; no turn-start reader applies it, despite the test comment. TUI `/system` calls this trait method through an immutable read guard and then reports success. The async inherent setter changes the config base and current message but neither recomposes COT/model-profile/project-rule sections nor updates `canonical_context.system_prompt`; compression can restore the old prompt. App-core bootstrap reads the raw base via `current_system_prompt`, adds methodology, then invokes this async setter, making the divergence reachable during normal startup.
- Log/artifact: this report.

## Deviations

Method resolution is established from the immutable TUI closure and imported trait signature; no compile fixture was run by instruction.

## Conclusion

Supports F-RCT-01-P1-01: runtime prompt mutation has split, inconsistent authorities and a live false-success TUI path.

## Follow-Up

Future regression must cover trait-object mutation, TUI command, async mutation, COT/profile/rules preservation, and compression after mutation.
