# F-RCT-01 / V03 / Attempt 06

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: framework clean; concurrent CLI generated changes excluded

## Claim

Requesting a Cargo-feature-dependent capability either constructs that capability or fails construction explicitly.

## Method

```text
Compare root Cargo default/features (Cargo.toml:65-92), ungated builder methods
enable_human_in_loop/enable_subagent/register_agent_dispatch_tool
(builder.rs:339-420), build validation (:854-870), and cfg-gated new_inner
registration/infrastructure (mod.rs:403-478). Inspect existing tests and cfgs.
```

## Expected Result

Feature-dependent builder methods are themselves cfg-gated or build returns a typed unsupported-capability error when the compiled feature is absent.

## Result

- Exit code: 0.
- Duration: under one second.
- Summary: Default features are empty. The three builder methods are always callable and their booleans are stored in AgentConfig. Construction validates only model presence and subagent/tools coupling. Without `human-loop`, enabling HITL has no runtime branch. Without `subagent`, enabling subagents or agent-tool registration is ignored and build still succeeds. Existing positive registration tests are compiled only when the corresponding feature is present; there are no absent-feature construction assertions.
- Log/artifact: this report.

## Deviations

No no-default executable construction test was run under the user's review-only instruction.

## Conclusion

Supports F-RCT-01-P2-04: the builder can return a successful agent that lacks explicitly requested capabilities.

## Follow-Up

Feature-gate the setters or reject unsupported requests; add no-default compile/runtime fixtures.
