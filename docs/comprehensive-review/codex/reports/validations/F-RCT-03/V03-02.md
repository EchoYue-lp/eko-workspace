# F-RCT-03 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source clean; concurrent generated files not inspected

## Claim

A partial provider stream followed by error cannot be finalized as successful DirectAnswer content.

## Method

Inspect error branches and caller interpretation in DirectAnswer and canonical think; compare envelope normalization.

```text
nl -ba src/agent/react/run/stream_channel.rs | sed -n '380,480p'
nl -ba src/agent/react/run/phases/think.rs | sed -n '99,125p'
nl -ba echo-core/src/agent/event_envelope.rs | sed -n '107,194p'
```

## Expected Result

Provider error terminates the run as failed exactly once; partial bytes may be retained as diagnostic draft but never become successful final output.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: DirectAnswer sends `AgentEvent::Error` on a chunk error, then returns `Ok(content)`. It subsequently sends LlmUsage and `FinalAnswer(content)` and its caller finalizes the trace Completed. Raw consumers can receive two terminal events; `envelope_event_stream` stops at the Error and hides the later success, while the durable trace remains Completed. Canonical think instead forwards an `Err` and abandons.
- Log/artifact: `src/agent/react/run/stream_channel.rs:385-395`, `:417-479`, `:235-242`; `echo-core/src/agent/event_envelope.rs:133-177`

## Deviations

F-CORE-01 owns typed error loss in the envelope; this validation concerns contradictory producer terminal/trace semantics.

## Conclusion

Supports F-RCT-03-P1-05.

## Follow-Up

Return the provider error with optional partial draft and route through one terminal commit; delete post-error usage/final success emission.
