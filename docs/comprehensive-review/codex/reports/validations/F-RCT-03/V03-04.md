# F-RCT-03 / V03 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source clean; concurrent generated files not inspected

## Claim

Streaming tool-call deltas assemble by provider index with UTF-8-safe malformed-JSON repair and stable execution ordering.

## Method

Inspect delta assembly, JSON repair, sorted map conversion, and ToolCall emission.

```text
nl -ba src/agent/react/run/processor.rs | sed -n '10,183p'
nl -ba src/agent/react/run/phases/tools.rs | sed -n '50,100p'
```

## Expected Result

Interleaved indices retain independent ID/name/arguments, conversion is stable by index, and malformed text handling does not byte-slice or panic.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: deltas merge into a map keyed by `u32` index; IDs/names replace only when non-empty; argument strings append; conversion sorts indices; repair removes Unicode scalar values using `String::pop` and has a bound of eight attempts. Valid calls are emitted in sorted provider index order. No byte slicing was found.
- Log/artifact: `src/agent/react/run/processor.rs:56-83`, `:104-183`; `run/phases/tools.rs:63-80`

## Deviations

Malformed calls are dropped and covered as a future behavioral regression; this validation does not prove all provider delta variants.

## Conclusion

No new tool-delta/UTF-8 finding is supported in this bounded path.

## Follow-Up

F-RCT-04 should independently validate call/result pairing and malformed-call user observability.
