# F-RCT-03 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source clean; concurrent generated files not inspected

## Claim

The DirectAnswer streaming shortcut returns the receiver before producing into its bounded channel.

## Method

Trace channel creation, DirectAnswer classification, `direct_answer_stream`, and the point where `ReceiverStream` is returned.

```text
nl -ba src/agent/react/run/stream_channel.rs | sed -n '35,315p'
nl -ba src/agent/react/run/stream_channel.rs | sed -n '350,480p'
```

## Expected Result

Producer work runs in a spawned task or otherwise concurrently with the consumer; no bounded send can wait before the receiver is returned.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: `run_stream_channel` creates capacity 256, then awaits the entire `direct_answer_stream` before returning `ReceiverStream`. Each content delta uses blocking `send().await`. Up to 256 events are buffered with no visible streaming; the next send waits forever because the caller cannot poll a receiver it has not received. The execution mutex and active-turn lease remain held during the wait.
- Log/artifact: `src/agent/react/run/stream_channel.rs:40-41`, `:185-245`, `:385-415`, `:465-479`

## Deviations

None.

## Conclusion

Supports F-RCT-03-P1-01.

## Follow-Up

Future regression should produce more than the buffer capacity in distinct deltas and assert first-event latency plus completion.
