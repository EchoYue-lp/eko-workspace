# F-RCT-03 / V03 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source clean; concurrent generated files not inspected

## Claim

Think phase events carry one stable usage value independent of provider chunk boundaries.

## Method

Trace `in_reasoning`, transition-time ThinkEnd events, final usage chunk collection, closing ThinkEnd, and consumers.

```text
nl -ba src/agent/react/run/processor.rs | sed -n '10,88p'
nl -ba src/agent/react/run/phases/think.rs | sed -n '110,245p'
nl -ba echo-core/src/agent/mod.rs | sed -n '312,375p'
```

## Expected Result

One reasoning phase has one start/end pair and its end reports the final known usage, regardless of whether content/tool calls arrive before a separate usage-only chunk.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: when content/tool calls first end reasoning, `process_stream_chunk` emits ThinkEnd with zeros before final usage is known. If the stream ends while still reasoning, `run_think` emits ThinkEnd with actual usage. Content plus tool calls can also create a synthetic second ThinkStart/ThinkEnd pair for the buffered content. Consumers therefore observe provider-shape-dependent phase counts and token totals; LlmUsage is the stable full-call event.
- Log/artifact: `src/agent/react/run/processor.rs:28-63`; `src/agent/react/run/phases/think.rs:127-245`; `echo-core/src/agent/mod.rs:329-375`

## Deviations

F-CORE-01-P2-06 owns overflow in `AgentEvent::total_tokens`; this finding is phase cardinality/semantic drift.

## Conclusion

Supports F-RCT-03-P2-06.

## Follow-Up

Make ThinkEnd structural and keep usage solely in LlmUsage, or delay exactly one ThinkEnd until usage is final.
