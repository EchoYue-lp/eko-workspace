# F-RCT-03 / V02 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source clean; concurrent generated files not inspected

## Claim

Dropping or stopping a streaming consumer promptly cancels upstream LLM/tool work and closes durable run state.

## Method

Trace ReceiverStream drop through producer send behavior; inspect the chat sink false branch, A2A/subagent loops, cancellation token checks, and terminal finalizers.

```text
nl -ba echo-agent-cli/echo-agent-app-core/src/chat_driver.rs | sed -n '512,566p'
nl -ba src/agent/react/run/phases/think.rs | sed -n '99,125p'
nl -ba src/agent/react/run/stream_macros.rs | sed -n '38,74p'
```

## Expected Result

Consumer disconnect cancels the invocation token and the core records a typed cancelled/abandoned terminal before releasing the execution lease.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: a sink returning false breaks and drops the stream but does not cancel the invocation token. The producer notices closure only at its next send. Canonical text content emits no content events until the provider stream ends, so a non-reasoning LLM may continue consuming the full response after disconnect. Closed-channel macros return `Abandoned`/`Ok(())`, which inherits F-RCT-02's incomplete terminal persistence rather than recording cancellation.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/chat_driver.rs:540-546`; `echo-agent/src/agent/react/run/phases/think.rs:110-125`, `:224-247`; `run/stream_macros.rs:38-74`

## Deviations

Whether each provider observes its cancellation token immediately belongs to provider tasks; the framework does not request cancellation at disconnect.

## Conclusion

Supports F-RCT-03-P1-04 and depends on F-RCT-02-P1-03 for durable terminal impact.

## Follow-Up

F-RCT-05 should validate resume behavior after disconnect once a typed cancellation terminal exists.
