# F-RCT-03 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source clean; concurrent generated files not inspected

## Claim

User-visible Token events contain only accepted response content and preserve provider chunk timing.

## Method

Trace content/reasoning accumulation through `process_stream_chunk`, post-stream emission, steer handling, verifier handling, and frontend token consumers.

```text
nl -ba src/agent/react/run/processor.rs | sed -n '10,88p'
nl -ba src/agent/react/run/phases/think.rs | sed -n '99,258p'
nl -ba src/agent/react/run/stream_channel.rs | sed -n '640,700p'
nl -ba src/agent/react/run/phases/verify.rs | sed -n '98,132p'
```

## Expected Result

Streaming content arrives incrementally, and drafts rejected by steer/verifier are either explicitly provisional/retractable or never emitted as accepted output.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: canonical `run_think` calls `process_stream_chunk(..., false)`, buffering all normal content. After the provider stream closes, it emits one Token containing the whole draft. The driver only afterwards checks steer and verifier; rejected drafts remain visible to append-only consumers such as TUI. Reasoning deltas are emitted live using the same Token variant, so Token has mixed reasoning/final-content semantics.
- Log/artifact: `src/agent/react/run/phases/think.rs:110-125`, `:224-247`; `run/stream_channel.rs:657-676`; `run/phases/verify.rs:107-131`; `echo-agent-cli/src/tui/events.rs:602-606`

## Deviations

The final accepted answer is later published as `FinalAnswer`, but no retract/replace event is attached to the earlier draft.

## Conclusion

Supports F-RCT-03-P1-03.

## Follow-Up

Choose an explicit draft protocol or delay content publication until acceptance; reasoning needs its own event payload.
