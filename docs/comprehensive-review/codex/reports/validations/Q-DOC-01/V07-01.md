# Q-DOC-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: not-applicable
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed CLI blobs; Cargo.lock excluded

## Claim

EKO configuration and data-root documentation names the canonical paths used by current source.

## Method

```text
Compare README.md, docs/configuration.md, docs/getting-started.md, and config/echo-agent.example.yaml with src/main.rs:64-70 and echo-agent-app-core/src/config_discovery.rs:8-17,184-280,396.
```

## Expected Result

All operator paths consistently use the EKO root and current project/global filenames.

## Result

- Exit code: 0 (inspection); invariant failed
- Duration: not recorded
- Summary: startup selects `~/.eko`; discovery uses `~/.eko/echo-agent.yaml`, project `echo-agent.yaml`, global `~/.eko/mcp.json`, project `.mcp.json`, and `.eko` instruction/LSP paths. Current docs repeatedly prescribe `~/.echo-agent/config.yaml`, `./.echo-agent/*`, `~/.echo-agent/workspaces`, and `~/.echo-agent/memory`.
- Log/artifact: this report

## Deviations

Runtime precedence defects remain canonical A-CFG-01-P2-01 and are not repeated here.

## Conclusion

Supports Q-DOC-01-P2-03: following current setup docs can write files the branded EKO discovery inventory does not treat as canonical.

## Follow-Up

Publish one generated path/precedence table after A-CFG-01 is resolved.
