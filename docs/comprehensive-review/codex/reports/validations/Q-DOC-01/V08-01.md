# Q-DOC-01 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed blobs only; CLI Cargo.lock excluded

## Claim

EKO documentation respects the product rule that the CLI uses file/in-memory storage and does not enable framework SQLite.

## Method

```text
Compare EKO README.md and docs/architecture.md SQLite claims with both EKO Cargo.toml files and committed source references.
```

## Expected Result

EKO docs describe file-backed conversation/memory/runtime storage and do not claim the CLI enables `sqlite`.

## Result

- Exit code: 0 (inspection); invariant failed
- Duration: not recorded
- Summary: neither EKO manifest enables `echo-agent/sqlite` and no EKO Sqlite store is constructed, but README says sessions use SQLite+FTS, lists `sqlite` as an enabled framework feature, and maps architecture/workspaces to SQLite; architecture.md additionally calls SQLite the default memory backend.
- Log/artifact: this report

## Deviations

Framework SQLite remains a valid optional capability and is not a deletion candidate.

## Conclusion

Supports Q-DOC-01-P2-03. This is EKO documentation drift, not a framework feature defect.

## Follow-Up

Align EKO docs to FileStore/FileConversationStore/FileRuntimeStateStore and retain framework SQLite documentation.
