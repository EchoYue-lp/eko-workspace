# Q-DOC-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: not-applicable
> Worktree state: committed framework blobs only

## Claim

Every command advertised in the framework README names an example with the required feature set.

## Method

```text
Compare README.md:92-100 with Cargo.toml [[example]] required-features entries.
```

## Expected Result

Each command either works with default features or explicitly enables every required feature.

## Result

- Exit code: 0 (inspection); invariant failed
- Duration: not recorded
- Summary: README commands omit `--features macros` for `demo25_macros`, `--features workflow` for `demo34_workflow_stream`, and `--features multimodal` for `demo36_multimodal`; the manifest requires those features at lines 198-200, 226-232.
- Log/artifact: this report

## Deviations

Commands were not executed by explicit task constraint.

## Conclusion

Supports Q-DOC-01-P2-02. It does not report compiler behavior.

## Follow-Up

After documentation correction, execute each published command in a future validation.
