# B-PATH-01 / V14 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary review
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The three entry/composition findings follow from current live branches and are
not inferred solely from feature existence or the hard-coded parity matrix.

## Method

The primary reviewer independently read the headless dispatch in `src/main.rs`,
`start_headless_services` and both mode functions, the `AppState` service token
construction/start methods, and the parity-matrix assertion. It compared those
sources with V01-02, V09, V10, V11, and the successful feature build reports,
then checked task references after V13.

```text
nl -ba src/main.rs | sed -n '130,390p'
nl -ba src/cli/modes.rs | sed -n '1,170p'
nl -ba echo-agent-app-core/src/state.rs | sed -n '500,735p'
nl -ba echo-agent-app-core/src/surface_contract.rs | sed -n '1,190p'
node <catalog task-id checker> codex/reports/tasks/B-PATH-01.md
```

## Expected Result

Pure channel mode is build-reachable and bypasses common headless service
startup; the existing parity test has no constructor/behavior dependency; and
started scheduler/background tokens lack an explicit cancel/join owner in the
reviewed shutdown paths.

## Result

- Exit code: 0 for source/reference inspections
- Duration: under one second after compile/test validations completed
- Summary: `main.rs` builds TaskRuntime/AgentPool for every headless surface,
  but the pure channel branch calls `run_channels_mode` directly. The only
  headless `start_headless_services` call is inside the CLI path; GUI and TUI
  start equivalent services separately. The parity test asserts only static
  array sizes and non-empty strings. `AppState` creates independent scheduler
  and task tokens and passes clones to spawned services; inspected shutdown
  paths cancel a separate watcher token and do not cancel/join these services.
  The corrected task report has no unknown exact task ID.
- Log/artifact: `B-PATH-01.md`, V01-02, V03 through V13

## Deviations

This validation does not claim spawned tasks outlive process/runtime teardown.
It accepts the narrower absence of an explicit orderly service stop point.
Dreaming policy for unattended channels may need product confirmation, but
cron/background/plugin monitor omission independently establishes the P1 parity
failure.

## Conclusion

The P1 channel composition failure and two P2 testing/lifecycle findings are
accepted at the reviewed commit. Detailed lifecycle design remains owned by
`A-BOOT-01`.

## Follow-Up

`A-BOOT-01`, `A-SRF-04`, `X-SRF-01`, and `Q-E2E-01` consume these facts.
