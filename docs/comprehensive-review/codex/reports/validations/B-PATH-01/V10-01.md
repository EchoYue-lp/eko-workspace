# B-PATH-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The existing product-surface capability-matrix test validates live mode
composition rather than merely documenting intended evidence.

## Method

```text
cd echo-agent-cli && cargo test --locked -p echo-agent-app-core surface_contract::capability_matrix_has_evidence_for_every_surface -- --exact
```

Then inspect the complete test body at
`echo-agent-app-core/src/surface_contract.rs:141-150` and compare it with the
mode trace in V09.

## Expected Result

Either the test fails for the pure-channel missing services, or its assertions
mechanically reference/execute current constructors and registration paths.

## Result

- Exit code: 0
- Duration: 4m 19s
- Summary: one test passed (`1 passed; 648 filtered out`). The test only asserts
  five surfaces, eleven rows, non-empty capability names, five evidence strings
  per row, and non-empty strings. It does not call an entry point or composition
  constructor and passed despite V09 proving the channel service omission.
- Log/artifact: none

## Deviations

The Cargo execution itself succeeded. The validation status is failed because
the falsifiable claim concerns test sensitivity, not command health.

## Conclusion

The test is a documentation-shape test, not an executable parity contract. It
cannot detect stale or false evidence strings. Supports B-PATH-01-P2-02.

## Follow-Up

Replace or supplement it with constructor-level mode contracts that assert
actual service handles, stores, provider kinds, and shutdown tokens.
