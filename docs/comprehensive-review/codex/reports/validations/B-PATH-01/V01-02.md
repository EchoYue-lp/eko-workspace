# B-PATH-01 / V01 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

Every live EKO process entry and its immediate mode dispatch can be enumerated
from the current manifests and source without relying on historical documents.

## Method

Read `echo-agent-cli/Cargo.toml`, `tauri.conf.json`, `src/main.rs`,
`src-tauri/src/main.rs`, `src/lib.rs`, `src/tauri/desktop.rs`, and
`src/cli/modes.rs`; then search both repositories for `fn main`,
`run_desktop_entry`, `run_tui`, `run_cli_mode`, `run_channels_mode`,
`start_headless_services`, and scheduler/background startup calls.

## Expected Result

All shipped binaries, mode flags, feature gates, and the fact that cron and
background work are in-process services rather than extra binaries are visible
in a closed call graph.

## Result

- Exit code: not-applicable
- Duration: not-applicable
- Summary: two shipped binary targets exist. `echo-agent-cli` enters the
  default TUI or hidden CLI/channel modes through `run_tui_or_cli_entry`;
  `echo-agent-tauri` enters GUI through `run_desktop_entry`. A GUI-only build of
  the package-name binary also redirects to `run_desktop_entry`. Cron and
  background work have no separate `main`; they are constructed by
  `start_headless_services` or the GUI composition root.
- Log/artifact: task report current-path graph

Key anchors: `echo-agent-cli/Cargo.toml:39`, `:43`;
`echo-agent-cli/tauri.conf.json:6`; `echo-agent-cli/src/main.rs:59`, `:75`,
`:78`, `:83`, `:95`, `:240`, `:357`; `echo-agent-cli/src-tauri/src/main.rs:3`;
`echo-agent-cli/src/tauri/desktop.rs:67`, `:124`.

## Deviations

Attempt 01 has a non-Codex executor and is retained immutably. This attempt
independently reproduced its entry graph. It also corrects the characterization
that all top-level dispatch branches are mutually exclusive: a build with both
`tui` and `gui` takes the TUI branch in the package-name binary, while the
dedicated Tauri binary remains available separately.

## Conclusion

The definition and duplicate-entry search is complete. It proves target and
source reachability, not service parity or successful execution.

## Follow-Up

V02 inventories the composition roots; V09 compares their services.
