# B-PATH-01 / V08 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

A package binary built without TUI, GUI, or channels is rejected at compile
time by the declared entry guard.

## Method

```text
cd echo-agent-cli && cargo check --locked --no-default-features --bin echo-agent-cli
```

## Expected Result

Nonzero exit containing `One of the tui, gui, or channels features must be enabled`.

## Result

- Exit code: 101
- Duration: 9.87s
- Summary: the expected `compile_error!` fired at `src/main.rs:90`.
- Log/artifact: none

## Deviations

Rust also reported two derivative return-type errors because the cfg-stripped
function body has no `Result` expression. These do not weaken the guard but
make its diagnostic noisier than necessary.

## Conclusion

The no-surface combination is deliberately unavailable. This validation is
passed because rejection was the expected terminal result.

## Follow-Up

The extra diagnostics are low-impact and deferred to feature-design review.
