# B-PATH-01 / V12 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

Historical product invariants used in this task can be classified against
current source rather than copied as conclusions.

## Method

Compare only the root `AGENTS.md` mode-parity and historical TUI TaskRuntime
claims with the entry/service trace; inspect the old Web mode flags in current
args and dispatch source. No historical audit report was loaded.

## Expected Result

Each used claim is classified current, fixed, stale, or regressed with a
current-code anchor.

## Result

- Exit code: not-applicable
- Duration: not-applicable
- Summary: mode parity remains the current product invariant. The historic TUI
  `task_runtime_store = None` gap is fixed by the store/tool/pool wiring at
  `src/main.rs:172-201`. The statement in `state.rs:1-5` that state supports Web
  and CLI modes is stale; `--web` exits as removed at `src/main.rs:351-354`.
  Pure channel service parity is currently regressed/missing per V09.
- Log/artifact: none

## Deviations

Broader historical document drift is intentionally deferred to B-DOC-01.

## Conclusion

The limited historical inputs required by this task are classified and do not
substitute for current-code evidence.

## Follow-Up

B-DOC-01.
