# Q-PERF-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> Commits: `echo-agent@3aa7929928442aab91e4dce9c426d909a5f0a1ab`; `echo-agent-cli@b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

External hook latency cannot block an authoritative TaskRuntime mutation while its same-run write lock is held.

## Method

Traced persisted event hook invocation into `HookEventDispatcher::dispatch`, the bounded synchronous queue, its single consumer, framework bridges, serial hook evaluation, validation, and action timeout branches.

## Expected Result

Durable state commit releases its write lock before potentially blocking extension work; shutdown and cancellation have finite deadlines.

## Result

- Exit code: 0 for inspection; behavioral claim failed
- Duration: not recorded
- Summary: `append_event_line` invokes the dispatcher under the same-run mutex. Queue capacity is 256 and `SyncSender::send` blocks when saturated. One consumer awaits all matching hooks serially while holding a registry read guard; configured MCP/subagent actions accept timeout zero and then await without a deadline. Slow or stuck hooks therefore fill the queue and stall subsequent authoritative writes, flush, shutdown, and same-run cancellation.
- Evidence: `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/file_shadow.rs:132-176`; `hook_event_dispatcher.rs:70-101,125-149,152-213`; `echo-agent/src/hooks_bridge.rs:90-100,116-126,158-171,226-239,273-286`; `echo-agent/echo-execution/src/skills/hooks.rs:131-200,217-307,786-836,995-1051`

## Deviations

The queue was not saturated dynamically. A-PLG-01-P1-03 remains owner of registry-generation reinterpretation; this validation owns the distinct persistence-lock/backpressure impact.

## Conclusion

Supports Q-PERF-01-P1-01 with high static confidence.

## Follow-Up

V11 defines the cancellation/shutdown scenario.
