# Q-PERF-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> Commits: `echo-agent@3aa7929928442aab91e4dce9c426d909a5f0a1ab`; `echo-agent-cli@b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

TaskRuntime in-memory per-run state and on-disk run history have explicit lifecycle bounds.

## Method

Inspected `FileTaskShadow` cache construction, run lock lookup, enumeration, and repository-wide removal/retention/compaction symbols at committed HEAD.

## Expected Result

Terminal/deleted runs evict `seq_cache` and `run_write_locks`, and disk history has a retention/compaction owner.

## Result

- Exit code: 0 for inspection; behavioral claim failed
- Duration: not recorded
- Summary: both maps are process-lifetime structures keyed by every touched run; comments explicitly say lock entries are never removed. Run directories are enumerated without a TaskRuntime retention or compaction API. Calling this “bounded by total runs ever written” is not a finite lifecycle bound.
- Evidence: `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/file_shadow.rs:19-50,55-64,180-194,301-329`

## Deviations

Object sizes and real historical run counts were not measured.

## Conclusion

Supports Q-PERF-01-P2-01's memory/disk lifecycle component.

## Follow-Up

Add lifecycle tests for terminal eviction, process-long run churn, and disk retention after authority design is fixed.
