# F-SEC-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

Guard, sandbox, audit/trace redaction, and path-security authorities and duplicates are mapped across both repositories.

## Method

Search definitions, exports, feature gates, constructors and same-purpose types for `GuardManager`, `GuardDirection`, `SandboxManager`, `SandboxPolicy`, audit/trace sinks, secret scanners and path validators; classify framework mechanism, EKO policy and adapter wiring.

## Expected Result

Reusable enforcement mechanisms have one framework authority; EKO only chooses product policy and wires it without redefining semantics.

## Result

- Exit code: 0 for bounded searches with matches.
- Guard authority is `echo-core/src/guard`; sandbox policy/executors are in `echo-execution/src/sandbox`; root exposes facades and integrates them into ReAct.
- EKO app-core injects `SandboxManager::local_sandbox()` and disables `run_code` when native local isolation is unavailable. It does not redefine sandbox semantics.
- `echo-tools::security::PathValidator` contains both the canonical ancestor-aware `validate_within_base` and a separate lexical `validate_output_file` implementation.
- No second EKO guard/sandbox authority was found. CLI non-use was not treated as dead framework code.

## Deviations

Two path-validation algorithms coexist inside the framework tool layer; V07 checks their behavioral difference.

## Conclusion

Definition/layering inventory passed. Semantic defects are isolated in later reports.
