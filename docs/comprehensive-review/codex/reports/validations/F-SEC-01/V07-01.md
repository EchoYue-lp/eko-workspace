# F-SEC-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: clean source repositories

## Claim

Malformed public guard configuration fails explicitly rather than silently removing an intended protection.

## Method

Inspect RuleGuard builder parsing, return types and tests for invalid regex input.

## Expected Result

Invalid regex makes construction fallible with a typed/configuration error, or is retained as a visible rejected entry.

## Result

- Exit code: 0 for source inspections.
- `RuleGuardBuilder::blocked_pattern` logs and ignores `Regex::new` failure (`echo-core/src/guard/rule.rs:102-109`), then infallible `build()` returns a guard without that rule (`:136-145`).
- No invalid-pattern regression test was found in the module.

## Deviations

Built-in constant regex lists also use filter-map, but those are developer-owned literals; this finding is limited to the public caller-supplied builder.

## Conclusion

Failed. Supports F-SEC-01-P2-09.
