# A-SRF-01 / V07 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before report writes

## Claim

Existing static test coverage meaningfully validates TUI capability and reducer
parity rather than only documenting intended surface names.

## Method

```text
Inventory #[test]/#[tokio::test] under src/tui and app-core surface_contract;
map coverage to sink event variants, command reachability, Task/Subagent/HITL
reducers, resume hydration, Unicode bounds, terminal events, and capability
matrix claims. Tests were inspected, not executed.
```

## Expected Result

The parity matrix should be tied to code or replay fixtures, and high-risk
reducers should have representative event sequences.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: useful unit tests cover Unicode-bounded tool output, compact tool
  display, Subagent result projection, command parsing, and selected view
  formatting. There is no direct TuiChatSink chart/terminal mapping fixture, no
  restart hydration or broadcast-lag fixture, and no attended task-edit parity
  fixture. `CAPABILITY_MATRIX` stores free-form strings and its only test checks
  row count/non-empty text (`app-core/src/surface_contract.rs:23-149`), so the
  test passes even if a referenced handler is deleted.
- Log/artifact: this report

## Deviations

The inventory passed, but it does not mean parity is proven; missing cases are
recorded above and in the task findings.

## Conclusion

Existing tests do not catch the three observed surface defects and support
A-SRF-01-P2-04.

## Follow-Up

Replace prose evidence with callable contract cases or generated mappings.
