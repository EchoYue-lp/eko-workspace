# A-SRF-01 / V10 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before report writes

## Claim

TaskRun, Subagent, tool stream, and HITL approval/input/selection each have a
definition-to-live-TUI-reducer path.

## Method

```text
Trace four independent production flows:
1. TaskRuntimeStore registration -> ChatResources/task_execute -> 250 ms view
   refresh and task commands.
2. Agent subagent_registry event bus -> TUI subscription -> update_subagent_runs
   -> sidebar and /tasks summary.
3. framework AgentEvent ToolCall/ToolStream/ToolResult/Error -> TuiChatSink ->
   tool execution message/reducer/artifact opener.
4. TuiHumanLoopProvider registration -> shared pending handle -> approval card
   -> approval/input/selection key response.
```

## Expected Result

All four capability families should be genuinely registered and consumed by
TUI, with known cross-task defects linked rather than restated.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: all four paths are live. TaskRuntime and Subagent projections reach
  the sidebar and `/tasks`; tool stream channels, metadata, failures, and
  artifacts reach TUI tool state; the TUI provider handles approval, free-text
  input, and selection before normal key dispatch. The flow does not erase the
  task-edit/replay gaps in V04/V05 or shared A-CHAT/A-HITL findings.
- Log/artifact: this report

## Deviations

HITL concurrency/identity/default-policy defects are already canonical in
A-HITL-01. Chat terminal/lifecycle defects are canonical in A-CHAT-01.

## Conclusion

The TUI is not missing these capability families wholesale. Surface fixes
should extend the current paths rather than introduce parallel implementations.

## Follow-Up

Consume V04/V05 and the dependency reports when planning fixes.
