# A-SRF-01 / V06 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before report writes

## Claim

Conversation resume, prepared attachments, browser selection, MCP management,
artifacts, and scheduler controls have real TUI command-to-service paths.

## Method

```text
Trace /sessions,/resume,/fork,/delete-session,/attach,/open-artifact,/browser,
/mcp,/plugins,/cron from SlashCommand parsing through handler, concrete service,
and state/result projection. Inspect normal dispatch into PreparedUserTurn and
ChatResources attachments.
```

## Expected Result

Each advertised command should invoke a production service and preserve the
shared input/runtime contract rather than stop at help text.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: all named commands have live handlers. Normal/queued turns build
  `PreparedUserTurn`, pass inline attachment refs to ChatResources, and reach
  `drive_chat` (`events.rs:1294-1435`). Resume restores framework history and
  conversation identity (`:4818-4874`); MCP load/disconnect calls the live Agent
  (`:3425-3477`); browser switches the shared BrowserRuntime (`:4551-4615`);
  cron uses the shared SchedulerRunner (`:2232-2296`, `:4624-4630`).
- Log/artifact: this report

## Deviations

Attachment lifecycle defects and the separate live `/steer` normalization path
are owned by A-INP-01; root/workspace semantics are owned by A-BOOT-01.

## Conclusion

These capability families are truly reachable in TUI. They should not be
described as GUI-only or removed because another surface differs.

## Follow-Up

Consume A-INP-01 and A-BOOT-01 when implementing fixes.
