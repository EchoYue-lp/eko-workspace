# A-SRF-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before report writes

## Claim

The TUI sink preserves the complete semantic payload of every canonical Agent
event even when it renders that payload textually.

## Method

```text
Field-by-field static diff of echo_core::agent::AgentEvent against
TuiChatSink::on_event/local AgentEvent/reducer, with Tauri's mapping as the
peer surface. Inspect Token, terminal, tool stream/result, budget/guard/memory,
Chart, safety, parameter error, and fallback variants.
```

## Expected Result

A terminal UI may choose textual rendering, but must retain the full payload or
an artifact/reference from which the user can recover it.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: tool stream/result fields are substantially preserved and Unicode
  truncation helpers are character-safe. `AgentEvent::Chart`, however, is
  serialized and truncated to 500 characters, then downgraded to an untyped
  Notice (`src/tui/events.rs:2184-2186`). Tauri preserves the full JSON spec as
  `ChatEvent::Chart` (`src/tauri/commands/chat.rs:1540`) and the frontend renders
  it. No TUI artifact or continuation reference is created.
- Log/artifact: this report

## Deviations

FinalAnswer and shared lifecycle defects are canonical A-CHAT-01 findings and
are intentionally not re-reported here.

## Conclusion

The claim fails for charts and supports A-SRF-01-P1-03. Textual rendering is
acceptable; irreversible truncation is not.

## Follow-Up

Add a full chart payload/artifact reducer fixture after the review phase.
