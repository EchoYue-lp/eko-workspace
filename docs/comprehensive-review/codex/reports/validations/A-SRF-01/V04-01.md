# A-SRF-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before report writes

## Claim

The TUI exposes the same user-directed Task graph inspection and editing
workflow as the GUI while reusing the canonical revision service.

## Method

```text
Compare TUI TaskRuntime commands/view/action branches with GUI
TaskRuntimePanel/taskRuntimeStore and Tauri update_tasks. Search TUI for task
insert/update/reorder/skip and revision patch callers; distinguish Agent tool
availability from direct surface controls.
```

## Expected Result

Both surfaces should let a user inspect and revise pending/blocked PlanTasks
through the one revisioned task API; the TUI need not copy GUI widgets.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: TUI has read projection plus run cancel/pause/resume/recovery/retry/
  skip actions (`src/tui/events.rs:4216-4507`), but no user command reaches the
  canonical `apply_eko_task_update` path. GUI exposes edit, insert, reorder, and
  skip through `taskRuntimeStore.updateTasks` and the same application revision
  service (`web-frontend/src/components/task/TaskRuntimePanel.tsx:745-828`,
  `src/tauri/commands/task_runtime.rs:399-413`). Natural-language Agent task
  tools are not an equivalent attended control while TUI rejects normal input
  during a running turn (`src/tui/events.rs:1308-1317`).
- Log/artifact: this report

## Deviations

A-TSK-03 has no Codex report yet; executor-loop correctness was not inferred.

## Conclusion

The claim fails and supports A-SRF-01-P1-01. The missing piece is a thin TUI
adapter to existing revision authority, not a new task model.

## Follow-Up

Regression should edit/insert/reorder a pending graph from TUI and GUI and
compare resulting revision/events field-for-field.
