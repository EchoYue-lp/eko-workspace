# A-TSK-06 / V07 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; committed framework blobs used

## Claim

Thinking protocol deltas do not enter the full result, durable Subagent result, review input, parent summary, or final task tool output.

## Method

```text
Trace ThinkStart/Token/ThinkEnd and FinalAnswer in the committed framework
streaming executor and EKO primary-task stream. Follow `output` into
SubagentReleased, review_output, TaskExecutionSummary, and task_execute output.
```

## Expected Result

Thinking remains a transient typed event; only user-facing tokens/final answer become output.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: both streaming paths maintain `in_thinking`; thinking tokens emit only thinking events, while non-thinking tokens populate output. `FinalAnswer` replaces output. Durable result and review paths consume that output, not thinking events. Summary formatters do not read thinking event kinds.
- Log/artifact: `echo-agent/src/agent/subagent/executor.rs:1183`, `:1200`, `:1224`, `:1395`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:3140`, `:3155`, `:3183`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/task_execute_tool.rs:581`

## Deviations

This proves current event routing, not provider behavior if a provider incorrectly sends reasoning as ordinary final content.

## Conclusion

The task-level thinking non-leak invariant is supported for correctly typed events.

## Follow-Up

Keep a future malformed event-order regression case.
