# A-TSK-06 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; committed framework blobs used

## Claim

Accepted artifact metadata retains a durable identity and valid locator after writer integration and is reachable through the TaskRuntime artifact projection.

## Method

```text
Trace reported/observed artifacts through framework hydration, EKO result
persistence, worktree merge and cleanup, integration summary rewrite,
ArtifactProduced/add_artifact, list_artifacts, and the Tauri list command.
Search all production callers of add_artifact.
```

## Expected Result

Integration rebases artifact locators to the authoritative checkout or a retained artifact store and emits a durable ArtifactProduced projection.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: framework hydration resolves relative reported paths against the isolated invocation directory and marks them available. Successful writer integration removes that worktree. EKO rewrites only `touched_files.written` and an integration decision, leaving result artifact paths/availability unchanged. Separately, `list_artifacts` derives exclusively from `ArtifactProduced`, while `add_artifact` has no production caller; the Tauri artifact list can therefore remain empty despite result artifacts.
- Log/artifact: `echo-agent/src/agent/subagent/types.rs:600`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:1685`, `:1734`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/worktree.rs:648`, `:786`, `:1001`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/store.rs:1432`, `:1800`; `echo-agent-cli/src/tauri/commands/task_runtime.rs:119`

## Deviations

Tool-output artifacts outside managed worktrees may remain valid; the failed claim concerns writer-produced files and the disconnected TaskRuntime projection.

## Conclusion

Artifact durability and projection fail on the production writer path. Supports A-TSK-06-P1-03.

## Follow-Up

Define one durable artifact authority and test post-cleanup resolution from the public list/read surface.
