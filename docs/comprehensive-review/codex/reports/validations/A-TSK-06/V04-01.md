# A-TSK-06 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; committed framework blobs used

## Claim

A `needs_fix` review preserves its issues in the next attempt of the same PlanTask.

## Method

```text
Trace review_task -> ReviewResult persistence -> circuit_breaker_action ->
build_fix_task -> ReviewGateOutcome::NeedsFix -> resolve_dispatch ->
retry_blocked_task -> next dispatch prompt.
```

## Expected Result

Review feedback is persisted and injected into the same canonical PlanTask's next attempt without creating a second task relation authority.

## Result

- Exit code: not-applicable
- Duration: static inspection
- Summary: `build_fix_task` correctly builds a same-ID, issue-enriched task, but `resolve_dispatch` binds it as `_fix_task` and discards it. The task is only marked Blocked. The explicit retry path increments `retry_count` while leaving title and description unchanged, so the next dispatch receives the original brief and no review issues.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/review.rs:122`, `:241`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:1475`, `:1822`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/store.rs:1211`, `:1261`

## Deviations

Review events retain issue text for UI/history, but no production prompt path reads those events for retry input.

## Conclusion

The invariant fails and supports A-TSK-06-P1-02.

## Follow-Up

Preserve review feedback on the same Task node/attempt contract and delete the unused fix-task return path.
