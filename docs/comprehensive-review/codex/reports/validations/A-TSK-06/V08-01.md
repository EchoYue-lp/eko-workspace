# A-TSK-06 / V08 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: CLI clean; committed framework blobs used

## Claim

After restart, the exact completed execution can feed the same complete output and structured result into review without redispatch or cross-spec reuse.

## Method

```text
Trace record_subagent_assigned/released identity -> events.jsonl ->
recoverable_subagent_result exact run/task/execution filter ->
dispatch_task recovery branch -> resolve_dispatch review_output.
Inspect existing restart and patched-spec tests.
```

## Expected Result

Only the matching claim-derived execution is reusable, and full output rather than only the 1,200-character summary reaches review.

## Result

- Exit code: 0
- Duration: static inspection
- Summary: release persists plan revision, attempt, execution ID, structured result, and full output. Recovery filters exact task and execution identity; a later assignment clears an older terminal fact. Dispatch returns the recovered full output to the same review branch. Tests cover no redispatch and changed-spec execution identity isolation.
- Log/artifact: `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/store.rs:1898`, `:1932`, `:2036`, `:2737`, `:3147`; `echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs:1284`, `:1457`, `:5458`

## Deviations

The existing restart execution test uses a read-only task without a reviewer LLM; the call graph still proves that reviewable tasks receive the recovered full output, while a combined crash-plus-review fixture remains future work.

## Conclusion

Restart-equivalent result routing is source-conclusive. A combined dynamic fixture is still desirable.

## Follow-Up

V12 records the prohibited dynamic fixture.
