# F-FEAT-01 / V77 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The corrected direct-use search and manifest comparison independently support
the ten-dependency ownership finding.

## Method

Search only existing facade target roots using crate-path patterns for
`scraper`, `html2text`, `url`, `pdf_extract`, `lopdf`, `calamine`, `docx_rs`,
`encoding_rs`, `polars`, and `sqlx`; then compare declarations in the root and
`echo-tools` manifests. The corrected scenario was run twice, once beside the
metadata recount and once alone.

## Expected Result

Zero facade crate-path uses and duplicate optional declarations in both
manifests.

## Result

- Exit code: 0 for both corrected executions
- Duration: under one second each
- Summary: zero direct crate-path matches in facade `src`, examples, tests, or
  benches; all ten optional dependencies are declared in both manifests and
  root features forward the matching `echo_tools` capability.
- Log/artifact: none

## Deviations

This establishes redundant ownership, not multiple compiled versions; Cargo
may unify identical versions.

## Conclusion

F-FEAT-01-P2-04 is accepted as a layering/maintenance defect.

## Follow-Up

None.

## Correction (2026-08-12)

This summary combined two corrected executions. V77-03 and V77-04 separately
record them; only those split reports are used for the final execution-granular
gate.
