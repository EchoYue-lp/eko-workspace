# F-FEAT-01 / V48 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The `full` feature satisfies the official `demo25_macros` target's required
features.

## Method

Working directory: `echo-agent/`.

```text
cargo check -p echo_agent --example demo25_macros --no-default-features --features full --locked --quiet
```

## Expected Result

The command exits 0 if `full` covers all framework examples.

## Result

- Exit code: 101
- Duration: 0.20s
- Summary: Cargo refused target selection because `macros` was required but is
  not enabled by `full`.
- Log/artifact: no separate log retained.

## Deviations

The flag itself is a no-op; the failure is caused entirely by target metadata.

## Conclusion

This confirms that a no-op compatibility flag can still make an official
example unavailable under `full`, supporting `F-FEAT-01-P2-02` and
`F-FEAT-01-P2-03`.

## Follow-Up

Delete the artificial target gate or implement a real feature boundary, then
rerun as a new attempt.
