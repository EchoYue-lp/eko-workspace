# F-FEAT-01 / V47 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The `full` feature satisfies the official `demo04_subagent` target's required
features.

## Method

Working directory: `echo-agent/`.

```text
cargo check -p echo_agent --example demo04_subagent --no-default-features --features full --locked --quiet
```

## Expected Result

The command exits 0 if `full` covers all framework examples.

## Result

- Exit code: 101
- Duration: 0.53s
- Summary: Cargo refused target selection because `testing` was required but
  is not enabled by `full`.
- Log/artifact: no separate log retained.

## Deviations

The example was not compiled because Cargo rejected the feature set first.

## Conclusion

This independently confirms a second class of `full` omission and supports
`F-FEAT-01-P2-03`.

## Follow-Up

Align `full` semantics and testing/example classification before rerunning.
