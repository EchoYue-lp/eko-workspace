# F-FEAT-01 / V75 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The facade `full` feature is a superset of every official example's required
features.

## Method

Use Cargo metadata to subtract the resolved `full` member list from each of the
68 example targets' `required-features`.

## Expected Result

Zero examples have a missing required feature.

## Result

- Exit code: 0 for the computation
- Duration: under one second
- Summary: the invariant failed for 17 examples. Missing flags are `testing`
  (six examples), `workflow` (three), `sandbox` (two), `macros` (two), and one
  each for `semantic-memory`, `provider-factory`, and `multimodal`.
- Log/artifact: none

## Deviations

The script succeeded; validation status is failed because the asserted product
contract is false.

## Conclusion

This independently accepts F-FEAT-01-P2-03 and its exact 17-example count.

## Follow-Up

None.
