# F-FEAT-01 / V74 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

A first `jq` projection independently totals package features and examples.

## Method

Pipe Cargo metadata through a projection that emitted a package array followed
by `map(.features)` and `map(.examples)` totals.

## Expected Result

Exit zero with package rows and integer totals.

## Result

- Exit code: 5
- Duration: under one second
- Summary: package rows printed, then `jq` failed with `Cannot index array with
  string "features"` because the comma expression changed the aggregation input.
- Log/artifact: none

## Deviations

This is a validation-script defect, not a Cargo feature defect.

## Conclusion

The attempted totals are invalid and were not accepted.

## Follow-Up

Bind projected rows to a variable before summing.

## Correction (2026-08-12)

This projection was the first segment of a shell invocation followed by a
successful per-package projection. The failing segment's exit was 5, while the
overall wrapper's last-command exit was 0. Because that invocation grouped
distinct outcomes, it is not used as accepted count evidence; V74-04 and
V74-05 record the repeated failure and corrected command separately.
