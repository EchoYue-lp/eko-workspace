# F-FEAT-01 / V74 / Attempt 03

> Status: passed after repeated failed command
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

The corrected Cargo-metadata projection independently reproduces the reported
package, feature and example counts.

## Method

The erroneous projection from attempt 02 was accidentally executed a second
time and again exited 5. It was then corrected by binding the projected rows:

```text
cargo metadata --format-version 1 --locked --no-deps |
  jq '[.packages[]|{name,features:(.features|keys|length),examples:...}] as $rows |
      {packages:($rows|length), features:([$rows[].features]|add),
       examples:([$rows[].examples]|add), rows:$rows}'
```

## Expected Result

The repeated old command must remain rejected; the corrected command exits zero
and matches the task report.

## Result

- Exit code: 5 for the repeated old command; 0 for the corrected command
- Duration: under one second per execution
- Summary: corrected totals are 8 packages, 65 feature entries and 68 examples.
  Per package: core 7, macros 0, execution 3, tools 13, integration 4, state 1,
  orchestration 2, facade 35. A second corrected execution reproduced the same
  values with exit 0.
- Log/artifact: none

## Deviations

The accidental repeat is retained here rather than hidden. Only the corrected
projection supplies accepted counts.

## Conclusion

Primary recount accepts the report's feature and example inventory.

## Follow-Up

None.

## Correction (2026-08-12)

This report combined the repeated failed command and later corrected commands,
so it is not used as the final per-execution evidence. V74-04, V74-05 and
V74-06 split those outcomes without changing the accepted numbers.
