# F-FEAT-01 / V76 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d3`
> `echo-agent-cli` commit: `b3b2e81f2b2d`
> Worktree state: both source repositories clean

## Claim

Facade `--no-default-features` excludes the optional execution files/shell
domains and parser dependencies.

## Method

```text
cargo tree -p echo_agent --no-default-features --locked -e features
```

Filter for execution/tools, files/shell, encoding and tree-sitter edges.

## Expected Result

No execution default/files/shell or parser edge is present.

## Result

- Exit code: 0 for the dependency-tree command
- Duration: under one second
- Summary: resolved tree contains `echo_execution feature "default"`, its
  `files` and `shell` features, `echo_tools/files`, encoding_rs, tree-sitter,
  and six language packages (seven parser packages including tree-sitter core).
- Log/artifact: none

## Deviations

None.

## Conclusion

The minimal-build invariant fails and F-FEAT-01-P2-01 is accepted.

## Follow-Up

None.
