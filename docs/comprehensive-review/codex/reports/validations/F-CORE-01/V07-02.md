# F-CORE-01 / V07 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Current repository policy requires no backward compatibility, while the event
envelope publicly exports a schema-version/persistence contract that did not
change when ToolError gained a required field.

## Method

Reran a source-only search from `echo-agent`, explicitly including only the root
instructions, framework README/docs, `echo-core/src/agent`, and facade `src`.

```text
rg -n 'backward|compatib|schema_version|AGENT_EVENT_SCHEMA_VERSION|向后兼容|兼容' ../AGENTS.md README.md docs echo-core/src/agent src --glob '*.md' --glob '*.rs' --glob '!**/target/**'
```

## Expected Result

The output identifies both the no-compatibility policy and the source's current
version/persistence claims, allowing priority to be calibrated without using
another reviewer's work.

## Result

- Exit code: 0
- Duration: under one second
- Summary: `AGENTS.md:341-343` explicitly removes backward-compatibility burden.
  `event_envelope.rs` still exports schema version 1, hashes it into event IDs,
  rejects other versions, and advertises resume/persistence behavior. Git/source
  sampling confirms ToolError acquired a required field while version stayed 1.
- Log/artifact: none

## Deviations

The policy does not forbid current wire-version correctness; it changes impact
from retained-user-data breakage to misleading/ambiguous public contract unless
a live retained event store is proven.

## Conclusion

Primary review downgrades schema finding P1-01 to P2-01 and rewrites its impact
and direction around selecting one honest version policy.

## Follow-Up

State/recovery tasks should determine whether any current durable store consumes
these envelopes before prescribing migration support.
