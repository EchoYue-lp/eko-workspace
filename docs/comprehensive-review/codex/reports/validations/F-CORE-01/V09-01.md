# F-CORE-01 / V09 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The current repository's targeted event-envelope unit tests pass independently
when rerun by the primary reviewer.

## Method

```text
cd echo-agent
cargo test -p echo_core agent::event_envelope --locked
```

## Expected Result

Exit 0 with all targeted tests passing. Passing current-behavior tests must not
be interpreted as covering the negative public probes.

## Result

- Exit code: 0
- Duration: 4 minutes 33 seconds, mostly waiting for parallel build locks
- Summary: 6 passed, 0 failed, 282 filtered out. Tests cover sequencing/tool
  parenting, current schema serialization, resume IDs, terminal normalization,
  and selected validator cases.
- Log/artifact: none

## Deviations

Parallel feature-matrix builds held the target lock. No timeout or command
failure occurred. Existing tests do not cover missing legacy ToolError fields,
message ID loss, divergent same-slot payloads, sequence exhaustion, typed error
round-trip, token overflow, or zero EventBus capacity.

## Conclusion

Current positive envelope tests are green and coexist with the accepted
negative edge probes; they do not invalidate the findings.

## Follow-Up

None.
