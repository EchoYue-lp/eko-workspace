# F-CORE-01 / V02 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

`EventEnvelope` has live framework and EKO producer/consumer paths rather than
being test-only.

## Method

Search all constructor, wrapper, and payload-consumer calls in both repositories
and inspect the Subagent, A2A, chat, and task-runtime paths.

```text
rg -n --glob '*.rs' 'EventEnvelope::new|envelope_event_stream_after|envelope_event_stream\(|validate_event_trajectory|EventEnvelope' echo-agent echo-agent-cli
nl -ba echo-agent/src/agent/subagent/executor.rs | sed -n '1147,1225p'
nl -ba echo-agent/src/a2a/server.rs | sed -n '205,250p'
nl -ba echo-agent-cli/echo-agent-app-core/src/chat_driver.rs | sed -n '477,560p'
nl -ba echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs | sed -n '3110,3160p'
```

## Expected Result

At least one non-test producer wraps a raw agent stream and at least one
non-test consumer reads the resulting envelope.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: Subagent execution, A2A, EKO chat, and EKO task runtime wrap raw
  streams. Chat forwards the complete envelope to its sink; Subagent, A2A, and
  task runtime immediately discard envelope metadata and consume only payload.
- Log/artifact: source anchors in the F-CORE-01 task report

## Deviations

This validates local reachability, not a unified cross-surface subscription
path; V02-02 examines that separate claim.

## Conclusion

The envelope adapter is live. The result supports the error-loss reachability
in `F-CORE-01-P1-03` and clarifies that independent-consumer sufficiency differs
by entry point.

## Follow-Up

`F-CORE-01` V02-02.
