# F-CORE-01 / V04 / Attempt 05

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; probe only under `/private/tmp`

## Claim

An external consumer can reproduce current round-trip success, old schema-v1
ToolError incompatibility, identity/error loss, collision/overflow behavior,
and token-accounting panic using only public APIs.

## Method

Extend the V04-04 probe with a schema-v1 ToolError fixture lacking `failure` and
`catch_unwind` around `ThinkEnd { usize::MAX, 1 }.total_tokens()`, then run:

```text
cd /private/tmp/f-core-01-probe.RliwB1
cargo run --offline --quiet
```

## Expected Result

Current envelope round-trip succeeds; legacy schema-v1 ToolError fails on the
missing field; all previously observed identity/collision/error behaviors recur;
the unchecked token sum panics in debug mode and is caught by the probe.

## Result

- Exit code: 0
- Duration: about 1 second
- Summary: all assertions passed. Rust's panic hook printed `attempt to add with
  overflow` at `echo-core/src/agent/mod.rs:366`; `catch_unwind` caught it, so the
  process completed successfully.
- Log/artifact: `/private/tmp/f-core-01-probe.RliwB1`

## Deviations

The probe uses debug overflow checks. In optimized builds, unchecked addition
may wrap instead; neither behavior is a valid exact usage total.

## Conclusion

Supports findings `F-CORE-01-P1-01`, `F-CORE-01-P1-03`,
`F-CORE-01-P2-04`, `F-CORE-01-P2-05`, and `F-CORE-01-P2-06`.

## Follow-Up

None.
