# F-CORE-01 / V04 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; probe only under `/private/tmp`

## Claim

At sequence exhaustion, the trajectory validator reports both duplicate event
ID and non-contiguous sequence.

## Method

Run the V04-02 public-consumer probe from cached dependencies:

```text
cd /private/tmp/f-core-01-probe.RliwB1
cargo run --offline --quiet
```

## Expected Result

Two events resumed after `u64::MAX` share neither a sequence nor an event ID, or
the validator reports both ordering and duplicate-ID violations.

## Result

- Exit code: 101
- Duration: about 11 seconds
- Summary: probe code ran. Both produced events had sequence `u64::MAX` and the
  same event ID. The validator reported duplicate ID but did not report
  non-contiguous order, so the probe's stronger assertion failed at line 73.
- Log/artifact: `/private/tmp/f-core-01-probe.RliwB1`

## Deviations

The failed assertion refined the observed behavior: validator expectation also
saturates, so repeated maximum sequence values look contiguous to that check.

## Conclusion

The claim fails and strengthens `F-CORE-01-P2-05`. This is a probe-expectation
failure that exposed product behavior, not a repository test regression.

## Follow-Up

Correct the expectation and rerun as V04-04.
