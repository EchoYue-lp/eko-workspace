# F-CORE-01 / V02 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The public unified `GLOBAL_EVENT_BUS` is registered on live framework producers
so a Webhook/Trace/UI/Audit consumer receives agent envelopes without modifying
each entry point.

## Method

Search Rust sources for the bus definition, all sends, subscriptions, imports,
and parallel task/Subagent buses.

```text
rg -n --glob '*.rs' 'GLOBAL_EVENT_BUS|event_bus::EventBus|crate::event_bus|echo_agent::event_bus|EventBus' echo-agent
rg -n --glob '*.rs' 'EventBus::send|GLOBAL_EVENT_BUS\.send|\.send\(.*EventEnvelope|send\(event.*EventEnvelope' echo-agent echo-agent-cli
nl -ba echo-agent/src/event_bus.rs | sed -n '1,80p'
```

## Expected Result

At least one live agent stream publishes every sequenced envelope, and one
representative consumer subscribes or the API clearly declares manual-only use.

## Result

- Exit code: 1 for the producer search (zero matches); other inspections exited 0
- Duration: under 1 second
- Summary: the public module and global static exist, but no source path calls
  `EventBus::send` or `GLOBAL_EVENT_BUS.send`. Live task and Subagent flows use
  separate buses. The module documentation nevertheless says it replaces
  scattered per-frontend mapping.
- Log/artifact: source anchors in task report finding `F-CORE-01-P1-02`

## Deviations

External consumers can manually publish their own envelopes, but that does not
make the framework's execution paths observable through the advertised global
bus.

## Conclusion

The claim fails and supports `F-CORE-01-P1-02`.

## Follow-Up

`F-API-01` should separately validate the stale public example.
