# F-CORE-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Run, turn, message, tool-call, execution, and event identities use one typed,
lossless framework contract rather than interchangeable strings.

## Method

From the workspace root, inventory identity definitions and call sites in both
repositories, then inspect `Message`, `ToolCall`, `ExternalRunContext`,
`ToolContext`, `EventIdentity`, and `EventEnvelope` field by field.

```text
rg -n --glob '*.rs' 'turn_id: Option<String>|turn_id: String|message_id: Option<String>|execution_id: Option<String>|run_id: Option<String>|conversation_id: Option<String>|call_id: String|event_id: String' echo-agent/echo-core/src echo-agent/src
rg -n --glob '*.rs' '^pub (struct|enum|type) .*Id|struct .*Id\(' echo-agent/echo-core/src echo-agent/src
rg -n --glob '*.rs' 'EventIdentity \{' echo-agent echo-agent-cli
nl -ba echo-agent/echo-core/src/llm/types.rs | sed -n '235,485p'
nl -ba echo-agent/echo-core/src/tools/mod.rs | sed -n '963,1030p'
nl -ba echo-agent/echo-core/src/agent/event_envelope.rs | sed -n '9,105p'
```

## Expected Result

Semantically distinct IDs cannot be swapped accidentally, and every identity
already carried by one invocation, including `message_id`, survives into the
envelope consumed by an independent observer.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: 93 raw identity-shaped fields were found in the framework search.
  `Message` has no message ID; `ToolCall.id`, all invocation IDs, and envelope
  IDs are `String`. `ExternalRunContext.message_id` exists, but
  `EventIdentity::from_invocation` does not copy it and `EventEnvelope` has no
  corresponding field. `EventIdentity` also publicly derives `Default`, which
  creates an empty mandatory `turn_id`.
- Log/artifact: source anchors in task report finding `F-CORE-01-P2-04`

## Deviations

The count includes repeated identity fields in domain-specific types; it is an
inventory signal, not proof that every occurrence should share one Rust type.

## Conclusion

The claim fails. This supports `F-CORE-01-P2-04`; it does not by itself prove a
runtime collision, which V03/V04 inspect separately.

## Follow-Up

`F-CORE-01` V03 and V04.
