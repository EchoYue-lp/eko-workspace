# F-CORE-01 / V04 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; probe only under `/private/tmp`

## Claim

The public API reproduces current JSON round-trip success and the statically
identified identity, collision, overflow, and error-erasure behaviors.

## Method

Correct only the V04-03 ordering expectation, then rerun:

```text
cd /private/tmp/f-core-01-probe.RliwB1
cargo run --offline --quiet
```

## Expected Result

Current JSON round-trip succeeds; two invocations differing only by message ID
produce equal event identity; payload/parent differences in the same slot
produce equal IDs; overflow duplicates are detected only by event ID; typed LLM
error becomes free text; default empty turn is accepted.

## Result

- Exit code: 0
- Duration: 0.94 seconds
- Summary: every expected current behavior was reproduced.
- Log/artifact: `/private/tmp/f-core-01-probe.RliwB1`

## Deviations

This attempt predates the legacy-v1 and token-overflow assertions added in
V04-05.

## Conclusion

Supports `F-CORE-01-P1-03`, `F-CORE-01-P2-04`, and
`F-CORE-01-P2-05`; also confirms current-format `EventEnvelope` JSON can round
trip.

## Follow-Up

Expanded V04-05.
