# F-CORE-01 / V03 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Wire-incompatible `AgentEvent` changes advance the envelope schema or provide
deserialization defaults so persisted events remain readable.

## Method

Inspect the commits that introduced schema v1, trajectory validation, and the
later structured `ToolError.failure` field; compare current serde attributes.

```text
git -C echo-agent show --stat --oneline dba349e
git -C echo-agent show dba349e -- echo-core/src/agent/event_envelope.rs src/event_bus.rs
git -C echo-agent show --stat --oneline 93fac5b
git -C echo-agent show 93fac5b -- echo-core/src/agent/event_envelope.rs
git -C echo-agent show --stat --oneline 55d7a25
git -C echo-agent show 55d7a25 -- echo-core/src/error.rs echo-core/src/tools/mod.rs echo-core/src/agent/mod.rs
```

## Expected Result

Adding a required serialized field either increments
`AGENT_EVENT_SCHEMA_VERSION` and supplies a migration/decoder, or marks the
field with a compatible default.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: commit `dba349e` introduced schema v1. Commit `55d7a25` later added
  required `failure: ToolFailure` to the serialized `AgentEvent::ToolError` but
  did not change schema v1 and did not add `#[serde(default)]`.
- Log/artifact: git history and current source anchors in
  `F-CORE-01-P1-01`

## Deviations

The repository does not contain a persisted EventEnvelope fixture catalog, so
the concrete old-wire decode is tested externally in V04-05.

## Conclusion

The claim fails and supports `F-CORE-01-P1-01`.

## Follow-Up

V04-05 legacy fixture decode.
