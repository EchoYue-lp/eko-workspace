# F-CORE-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Event IDs, ordering, lineage, and resume counters remain collision-safe for all
values accepted by the public constructors and validator.

## Method

Inspect the event ID hash inputs, sequence increment, identity validation,
parent checks, terminal checks, and all unit tests.

```text
nl -ba echo-agent/echo-core/src/agent/event_envelope.rs | sed -n '1,530p'
rg -n --glob '*.rs' 'validate_event_trajectory\(|envelope_event_stream_after\(' echo-agent echo-agent-cli
```

## Expected Result

Invalid empty identities and exhausted sequence spaces are rejected; event IDs
distinguish divergent payload/parent facts or consumers can verify equivalence;
the validator is used before persisted replay/recovery decisions.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: IDs hash schema version, invocation identity, and sequence only.
  Payload and parent are excluded. Sequence generation and expected-sequence
  validation both saturate at `u64::MAX`. Identity non-emptiness and the
  recomputed event ID are not validated. Outside unit tests, neither
  `validate_event_trajectory` nor `envelope_event_stream_after` has a caller.
- Log/artifact: source anchors in `F-CORE-01-P2-04` and `F-CORE-01-P2-05`

## Deviations

A deterministic ID per logical slot is useful for idempotency. The defect is
that the public contract neither rejects divergent facts in one slot nor gives
an independent consumer a content-equivalence check.

## Conclusion

The claim fails and supports findings `F-CORE-01-P2-04` and
`F-CORE-01-P2-05`.

## Follow-Up

Executable probe V04.
