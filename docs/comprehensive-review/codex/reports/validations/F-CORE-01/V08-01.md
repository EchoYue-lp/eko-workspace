# F-CORE-01 / V08 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The sub-review's definition/reachability evidence supports its findings, while
their priorities and compatibility impact conform to the review protocol and
current `AGENTS.md`.

## Method

Primary reviewer independently inspected:

1. `AgentEvent`, `EventIdentity`, `EventEnvelope`, sequence/hash/validator, and
   `ExternalRunContext` source.
2. All `GLOBAL_EVENT_BUS`/`EventBus::send` source calls and the live specialized
   Task/Subagent event producers.
3. Raw `ReactError` conversion and the live Subagent payload consumer/status
   mapper.
4. Git forms of ToolError before and after the required `failure` field.
5. The no-backward-compatibility rule and the source's explicit version/resume
   contract.
6. Public overflow and zero-capacity probe reports and their exact source lines.

```text
rg -n 'GLOBAL_EVENT_BUS|EventBus::' src echo-* --glob '*.rs'
nl -ba echo-core/src/agent/event_envelope.rs | sed -n '1,280p'
nl -ba src/agent/subagent/executor.rs | sed -n '120,165p;1140,1195p;1370,1420p'
git show dba349e:echo-core/src/agent/mod.rs
git show 55d7a25:echo-core/src/agent/mod.rs
```

## Expected Result

Every retained finding has a live or public-consumer reachability boundary;
claims of current user data loss are rejected without retained-store evidence;
localized defects are not inflated.

## Result

- Exit code: 0 for all source/Git inspections
- Duration: under one second per command batch
- Summary: no global EventBus producer exists; live envelope and Subagent
  string-erasure paths were reproduced; message identity loss, unchecked token
  addition, saturated sequence behavior, and zero-capacity panic source all
  match the probe reports. Primary review changed schema P1-01 to P2-01 because
  the version contract is dishonest but retained v1 data damage is unproven,
  and changed zero-capacity P2-07 to P3-07 because current internal construction
  is safe and the defect is localized.
- Log/artifact: none

## Deviations

An earlier broad compatibility search violated reviewer-directory isolation and
is separately retained as failed V07-01; none of that output was used.

## Conclusion

The corrected findings are accepted within scope: two P1, four P2, and one P3.
The report does not claim broadcast durability or backward migration burden.

## Follow-Up

V09 reruns the targeted repository tests; V10 checks report integrity.
