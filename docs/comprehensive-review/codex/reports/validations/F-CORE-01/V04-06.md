# F-CORE-01 / V04 / Attempt 06

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; probe only under `/private/tmp`

## Claim

The public `EventBus::new(0)` constructor panics rather than rejecting zero
capacity through its type/result contract.

## Method

Add a path dependency on the root facade to the existing external probe and
catch unwind around the public constructor:

```text
cd /private/tmp/f-core-01-probe.RliwB1
cargo run --offline --quiet
```

## Expected Result

`catch_unwind(|| echo_agent::event_bus::EventBus::new(0))` observes a panic at
the broadcast-channel construction site; prior probe assertions continue to
pass.

## Result

- Exit code: 0
- Duration: about 17 seconds (first root-facade probe compile)
- Summary: the panic hook printed `broadcast channel capacity cannot be zero`
  at `src/event_bus.rs:18`; the probe caught it and all assertions passed.
- Log/artifact: `/private/tmp/f-core-01-probe.RliwB1`

## Deviations

The same process also printed the expected/caught token-overflow panic from
V04-05.

## Conclusion

The claim is confirmed and supports `F-CORE-01-P2-07`.

## Follow-Up

None.
