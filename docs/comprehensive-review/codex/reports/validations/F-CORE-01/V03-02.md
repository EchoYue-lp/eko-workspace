# F-CORE-01 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Typed subsystem errors preserve machine-readable kind and recovery semantics
when they cross the agent event boundary.

## Method

Inventory `ReactError` and sub-errors, inspect raw stream error producers,
envelope normalization, typed tool failures, Subagent consumption, and EKO's
typed status adapter.

```text
nl -ba echo-agent/echo-core/src/error.rs | sed -n '1,435p'
nl -ba echo-agent/echo-core/src/agent/mod.rs | sed -n '190,310p'
nl -ba echo-agent/echo-core/src/agent/event_envelope.rs | sed -n '107,194p'
nl -ba echo-agent/src/agent/react/run/phases/finalize.rs | sed -n '210,270p'
nl -ba echo-agent/src/agent/subagent/executor.rs | sed -n '137,158p;1395,1406p'
nl -ba echo-agent-cli/echo-agent-app-core/src/tasks/task_runtime/executor.rs | sed -n '1039,1067p'
```

## Expected Result

An LLM 429, cancellation, timeout, parse failure, or other typed error remains
classifiable after serialization and through Subagent/application adapters.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: `ReactError` is a typed non-exhaustive hierarchy, and tool failures
  preserve typed recovery facts. All non-tool raw stream errors are converted
  to `AgentEvent::Error { source: "agent_stream", message: error.to_string() }`.
  The Subagent consumer converts that event back into `ReactError::Other`; its
  otherwise typed terminal mapper can then only report `Failed`.
- Log/artifact: source anchors in task report finding `F-CORE-01-P1-03`

## Deviations

Tool errors are a positive exception: `ToolFailure` carries category, recovery,
side-effect, retry, and idempotency facts.

## Conclusion

The claim fails for non-tool errors and supports `F-CORE-01-P1-03`.

## Follow-Up

Serialization probe V04-05.
