# F-RCT-04 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: no tracked/staged source diff; external generated CLI files were not read, modified, or reverted

## Claim

Existing tests cover the tool-batch cross-products required by the task card.

## Method

```text
Statically inventory tests referencing ToolBatchStart/End, checkpointed tool
results, cancellation drain, duplicate/unpaired IDs, streaming order,
allows_parallel_batch_execution, timeout calculation, retry, and oversized
output. Compare scenarios with F-RCT-04 invariants.
```

## Expected Result

Tests cover multi-call pairing, mixed serial/parallel ordering, cancel versus
timeout under load, duplicate/empty IDs, partial side effects and recovery, and
oversized UTF-8 output.

## Result

- Exit code: 0 for searches; coverage claim failed
- Duration: under one second per search
- Summary: current tests cover a fully completed single write checkpoint,
  checkpoint validator rejection of manually constructed unpaired/duplicate
  messages, one delayed-tool cancellation drain, a synthetic pipeline stream
  ordering case, timeout calculation defaults, retry behavior, and output
  artifacts. They do not drive the canonical multi-tool phase through partial
  checkpoint recovery, invalid IDs before side effects, biased control
  starvation, mixed ordering, timeout outcome pairing, or mixed/multiple
  final_answer calls.
- Log/artifact: this report

## Deviations

No tests were executed; this is a source inventory. F-EXT-01 owns general
schema/result-shape gaps, while F-RCT-03 owns primary-channel event loss.

## Conclusion

The claim fails. Unit coverage proves useful components in isolation but misses
the batch-level compositions where the reported defects arise.

## Follow-Up

Add the future regressions listed by each finding during remediation.
