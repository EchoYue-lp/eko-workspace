# F-RCT-04 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: no tracked/staged source diff; external generated CLI files were not read, modified, or reverted

## Claim

The live tool-batch authority and any competing implementation can be identified
without treating a definition as runtime reachability.

## Method

Working directory: repository root. Static search only.

```text
1. Search run_tools/process_steps definitions and all callers in echo-agent/src.
2. Trace public stream entry -> run_core_loop -> phases::tools::run_tools.
3. Search both repositories for batch execution, concurrency-policy, timeout,
   PostToolBatch, and tool-result insertion authorities.
```

## Expected Result

One canonical live batch executor exists; duplicates are classified by callers.

## Result

- Exit code: 0
- Duration: under one second per static search
- Summary: `run_tools` is called by the live phase driver at
  `src/agent/react/run/stream_channel.rs:655`. The several-hundred-line
  `process_steps` at `run/react_loop.rs:179` is `#[allow(dead_code)]` and has no
  caller. ToolManager is the lower-level per-tool timeout/retry/concurrency
  authority, not a second batch loop.
- Log/artifact: this report

## Deviations

The dead `process_steps` still contains divergent ordering, timeout-exempt, and
PostToolBatch logic, so source search alone would otherwise overstate coverage.

## Conclusion

Supports using `phases/tools.rs::run_tools` as the canonical behavior under
review and treating `process_steps` only as stale duplicate authority. Does not
prove dynamic behavior. The broader dead-loop finding remains F-RCT-02-P2-04;
this report also supports the PostToolBatch reachability gap in F-RCT-04-P2-07.

## Follow-Up

Delete the dead processor when F-RCT-02 is remediated; batch fixes must target
the phase implementation.
