# F-RCT-04 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: no tracked/staged source diff; external generated CLI files were not read, modified, or reverted

## Claim

Cancellation and the batch timeout remain observable under sustained tool
stream output and take precedence within a bounded delay.

## Method

```text
Inspect branch order and guards in each biased tokio::select in:
- concurrent run_tools loop;
- serial run_tools loop;
- ExecuteStage streaming forwarder.
Model simultaneous readiness of stream events, completions, cancel, and timeout.
```

## Expected Result

Ready control signals cannot be indefinitely postponed by data-plane events.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not measured
- Summary: concurrent selection at `tools.rs:194-293` is biased in order:
  grace, completed future, stream event, cancel, timeout. Before cancellation is
  observed, continuously ready completions/events can starve both controls.
  Serial selection at 326-359 orders result and stream before first cancel
  observation. ExecuteStage likewise prioritizes stream receive over result at
  `pipeline.rs:527-550`; after execution returns it drains until channel close.
- Log/artifact: this report

## Deviations

Once cancellation has been observed, its five-second grace timer is first; the
defect is initial observation fairness. Per-tool timeouts may eventually end
ordinary tools but do not repair prompt batch-cancel semantics or own-timeout
tools.

## Conclusion

The claim fails. A high-rate streaming tool can postpone cancel/outer timeout;
control responsiveness is coupled to stream readiness. Supports
F-RCT-04-P1-03.

## Follow-Up

Future regression: never-idle ToolStream plus ready cancel/timeout, asserting a
bounded terminal and no stream event after the terminal.
