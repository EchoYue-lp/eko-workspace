# F-RCT-04 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: no tracked/staged source diff; external generated CLI files were not read, modified, or reverted

## Claim

The canonical phase path invokes the declared PostToolBatch lifecycle with
accurate success/failure counts after each batch.

## Method

```text
Search HookEvent::PostToolBatch producers, the live run_tools implementation,
run context helpers, and the dead process_steps implementation. Classify each
producer by reachability.
```

## Expected Result

The live batch executor fires PostToolBatch once after a batch and before its
terminal transition, including failure/timeout.

## Result

- Exit code: 0 for searches; semantic result failed
- Duration: under one second per search
- Summary: live `phases/tools.rs::run_tools` contains no PostToolBatch call.
  `run/context.rs` can create a default context but has no batch caller. The only
  ReAct batch producers are inside the dead `process_steps` at
  `react_loop.rs:319-335` and `455-474`.
- Log/artifact: this report

## Deviations

Per-tool pre/post hooks remain in the live pipeline; this validation concerns
the separate documented aggregation lifecycle.

## Conclusion

The claim fails. Plugins configured for PostToolBatch are silently absent from
canonical execution, while dead code falsely suggests support. Supports
F-RCT-04-P2-07 and F-RCT-04-P2-04.

## Follow-Up

Future regression for success, partial failure, timeout, and cancellation batch
counts after the lifecycle is moved into the sole live authority.
