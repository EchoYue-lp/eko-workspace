# F-RCT-04 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean at acceptance

## Claim

The seven delegated tool-batch findings are independently supported by the live
phase scheduler, pipeline, checkpoint validator, and hook/retry paths.

## Method

Static primary sampling of `run_tools`, call-map assembly, ExecuteStage ID
repair, checkpoint pairing validation, timeout calculation, PostToolBatch
callers, and the dead `process_steps` duplicate. Compare with completed
F-RCT-02/03/05 boundaries. No executable validation.

## Expected Result

A batch validates canonical IDs before effects, preserves model order across
serial barriers, promptly observes control signals, emits one terminal for every
announced call, and persists only resumable state.

## Result

- Exit code: 0
- Duration: under one second per bounded static inspection
- Summary: partial error/cancel checkpoints can contain unmatched calls; empty
  IDs are repaired only after public/context projections and duplicates are not
  gated; biased data-first selects can postpone ready controls; partitioning runs
  all concurrent calls before every serial call; timeout abandons without paired
  outcomes/BatchEnd/checkpoint; retry-delay arithmetic is plain u64 before final
  saturation; PostToolBatch callers exist only in dead process_steps.
- Log/artifact: this report and task-report anchors

## Deviations

F-RCT-05 owns checkpoint-save failure and global restore semantics. F-RCT-04
retains batch shape, ordering, identity, and partial-result ownership.

## Conclusion

F-RCT-04 is source-conclusive and complete with five P1 and two P2 findings.

## Follow-Up

F-TSK-03, F-EXT-02, and batch/recovery synthesis.
