# Q-GUI-01 / V01-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed CLI blobs only; dirty `Cargo.lock` excluded.

## Claim

The committed CLI defines one root-package GUI feature and a dedicated GUI bin
whose manifest, entry point and Tauri runner agree.

## Method

```text
git show <cli-commit>:Cargo.toml
git show <cli-commit>:src/main.rs
git show <cli-commit>:src-tauri/src/main.rs
git show <cli-commit>:src/lib.rs
git show <cli-commit>:build.rs
git show <cli-commit>:tauri.conf.json
git grep -n -e 'feature = "gui"' -e 'echo-agent-tauri' <cli-commit> -- ':!Cargo.lock'
```

## Expected Result

`gui` activates all GUI dependencies, the dedicated bin requires it, and Tauri
selects that bin with the same feature.

## Result

- Exit code: 0.
- Duration: below one second per inspection.
- Summary: root `gui` enables Tauri/plugins/PTY/application channels;
  `echo-agent-tauri` points to `src-tauri/src/main.rs` and requires `gui`;
  `tauri.conf.json` selects that binary and `features=["gui"]`; `build.rs`
  invokes `tauri_build` only when `CARGO_FEATURE_GUI` is set.
- Log/artifact: none.

## Deviations

There is no standalone `src-tauri/Cargo.toml`; GUI is a target of the root package.

## Conclusion

This supports static feature/bin/runner alignment. It does not prove compilation,
linking, startup or packaging succeeds.

## Follow-Up

V02, V06 and V07.
