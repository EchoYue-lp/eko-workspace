# Q-GUI-01 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> Commits: `echo-agent@3aa7929928442aab91e4dce9c426d909a5f0a1ab`; `echo-agent-cli@b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`

## Claim

The delegated GUI matrix findings are independently supported by committed EKO source, without treating all-feature or static checks as GUI-only execution evidence.

## Method

Read committed `Cargo.toml`, `src/main.rs`, `.github/workflows/rust-ci.yml`, `build.rs`, and both `capabilities/default.json` files. Compared feature selection, cfg branches, CI commands, config root and capability identities/permissions.

## Result

- Exit code: 0.
- Default features select `tui`; the desktop route is specifically `gui && !tui`; the dedicated Tauri bin requires `gui`.
- CI installs native libraries and runs all-feature gates, but never runs either exact GUI-only command required by AGENTS. This supports P2-01 without claiming current compilation failure.
- The root build script resolves root `tauri.conf.json`; both root and nested capability files claim identifier `default`, while only the root copy includes event emit and filesystem defaults. This supports the P3 configuration-drift claim, not a runtime permission failure.
- V06 and V07 remain `not_run`.

## Conclusion

Primary static acceptance passed. Q-GUI-01 remains `needs_evidence` until the exact GUI-only check and test commands have immutable executable attempts.
