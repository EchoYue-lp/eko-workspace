# Q-GUI-01 / V04-01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed CLI blobs only; dirty `Cargo.lock` excluded.

## Claim

Repository-declared Linux GUI native prerequisites and current macOS host command
prerequisites are explicit before any future GUI matrix attempt.

## Method

```text
git show <cli-commit>:.github/workflows/rust-ci.yml
uname -s -m
xcode-select -p
command -v clang
command -v cargo
command -v npm
```

No compiler, package manager build, frontend command or network request ran.

## Expected Result

CI declares Linux Tauri packages and the current macOS host exposes Xcode command
line tools plus command paths needed for a later attempt.

## Result

- Exit code: 0.
- Duration: below one second.
- Summary: CI installs `libayatana-appindicator3-dev`, `librsvg2-dev`,
  `libssl-dev`, `libwebkit2gtk-4.1-dev`, and `libxdo-dev`. Host is Darwin arm64;
  Xcode CLT resolves to `/Library/Developer/CommandLineTools`; `clang`, `cargo`
  and `npm` command paths exist.
- Log/artifact: none.

## Deviations

Command presence does not establish SDK version compatibility, Rust targets,
package availability, linker success or frontend dependency installation.

## Conclusion

This records the environment prerequisite boundary without silently skipping it.
It does not upgrade V06 or V07 from `not_run`.

## Follow-Up

At execution time, record tool versions and any native linker/system dependency
failure in new immutable attempts.
