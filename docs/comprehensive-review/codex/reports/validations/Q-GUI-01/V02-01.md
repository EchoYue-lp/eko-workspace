# Q-GUI-01 / V02-01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed CLI blobs only; dirty `Cargo.lock` excluded.

## Claim

CI contains dedicated checks for the GUI-only conditional feature matrix.

## Method

```text
git show <cli-commit>:.github/workflows/rust-ci.yml
git show <cli-commit>:Cargo.toml
git show <cli-commit>:src/main.rs
git grep -n -e '--no-default-features' -e '--features gui' -e 'echo-agent-tauri' \
  <cli-commit> -- '.github/workflows/**' ':!Cargo.lock'
```

Compare CI commands with the required GUI-only commands:

```text
cargo check --no-default-features --features gui --bin echo-agent-tauri
cargo test --no-default-features --features gui
```

## Expected Result

CI separately checks and tests `gui && !tui`, in addition to any all-feature gate.

## Result

- Exit code: 0 for source inspection; the invariant comparison failed.
- Duration: below one second.
- Summary: CI installs Linux Tauri libraries and runs all-target/all-feature
  Clippy plus all-feature tests, but contains neither GUI-only command. With
  all features, `src/main.rs` selects its `#[cfg(feature="tui")]` branch rather
  than the `#[cfg(all(feature="gui", not(feature="tui")))]` desktop branch.
- Log/artifact: none.

## Deviations

The dedicated `echo-agent-tauri` target is compiled by all-target/all-feature
Clippy, so the gap is specifically feature isolation and GUI-only tests, not
total absence of GUI compilation in CI.

## Conclusion

This supports `Q-GUI-01-P2-01`. It does not show that current GUI-only source
fails; the required executable checks were prohibited and remain not run.

## Follow-Up

Add separate GUI-only CI steps, then run V06/V07 as new attempts.
