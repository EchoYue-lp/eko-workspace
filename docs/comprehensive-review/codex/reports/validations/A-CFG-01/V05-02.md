# A-CFG-01 / V05 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

An explicitly selected malformed YAML file causes startup loading to fail
instead of silently activating a different/default provider configuration.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  invalid-explicit /private/tmp/a-cfg-fixture-invalid-offline
```

The probe writes malformed YAML, calls `load_config_file` and then the real
startup API `load_config(Some(path))`.

## Expected Result

Strict parsing should fail and the startup API should make that failure
observable to its caller.

## Result

- Exit code: 0
- Duration: approximately 52 seconds including first offline compile
- Summary: `strict_error=true`, but startup returned provider `deepseek` and
  model `deepseek-v4-flash`.
- Log/artifact: `/private/tmp/a-cfg-fixture-invalid-offline/invalid.yaml`

## Deviations

The executable completed successfully because `load_config` converts the parse
error into `AppConfig::default`; the validation claim therefore failed.

## Conclusion

This directly supports `A-CFG-01-P1-01`.

## Follow-Up

Fix-time executable startup status test.
