# A-CFG-01 / V12 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

On Unix, config files persisted by `save_config` are owner-only, while normal
GUI config/provider responses do not serialize credential values.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  secret-permissions /private/tmp/a-cfg-fixture-secret-isolated-2

Static response check:
- full_config_response and configured_model_views fields
- provider/config tracing calls containing auth_token/api_key/secret
```

## Expected Result

File mode should be 0600; IPC/log projection should expose presence/source, not
the credential string.

## Result

- Exit code: 0
- Duration: 2.73 seconds
- Summary: `mode=600`. GUI responses use `has_auth_token` and `auth_source`;
  inspected tracing calls do not format token/key/secret values.
- Log/artifact: `/private/tmp/a-cfg-fixture-secret-isolated-2/user-data/config.yaml`

## Deviations

This did not inspect every provider-client dependency's internal HTTP error
format and does not test non-Unix ACLs.

## Conclusion

The reviewed EKO secret-at-rest and response/log boundary is proportionate to
the trusted local-product threat model. No security finding is raised.

## Follow-Up

None.
