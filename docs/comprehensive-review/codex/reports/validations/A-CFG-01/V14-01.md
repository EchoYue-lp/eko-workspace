# A-CFG-01 / V14 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before command; still clean after

## Claim

The existing app-core model-config unit-test subset compiles and passes at the
reviewed commit.

## Method

```text
cd echo-agent-cli
cargo test -p echo-agent-app-core model_config --locked
```

## Expected Result

All selected tests pass with exit code zero.

## Result

- Exit code: 0
- Duration: 1.25 seconds build plus 0.03 seconds tests
- Summary: 4 passed, 0 failed; 645 filtered out; integration target selected
  zero matching tests.
- Log/artifact: terminal output recorded in the review session

## Deviations

The four tests cover context-window inference and legacy projection only. They
do not cover configured/default model mutation, credential precedence,
deletion runtime propagation, or invalid config.

## Conclusion

The existing narrow model-config tests are green; findings outside their
coverage remain valid.

## Follow-Up

Add mutation/reachability tests when fixing P1-04.
