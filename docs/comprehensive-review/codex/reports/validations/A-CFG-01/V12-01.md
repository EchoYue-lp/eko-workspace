# A-CFG-01 / V12 / Attempt 01

> Status: inconclusive
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The secret-permission fixture checks the file created by `save_config`.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  secret-permissions /private/tmp/a-cfg-fixture-secret-isolated
```

## Expected Result

The probe should stat the selected persisted config and print its Unix mode.

## Result

- Exit code: 1
- Duration: 3.33 seconds
- Summary: the probe looked for fixture-root `echo-agent.yaml`, but
  `save_config` created user-data `config.yaml`; stat returned ENOENT.
- Log/artifact: `/private/tmp/a-cfg-fixture-secret-isolated/`

## Deviations

Probe assertion path was wrong; the saved file itself was created.

## Conclusion

This attempt supports no permission conclusion. Corrected attempt V12-02 is
immutable and separate.

## Follow-Up

V12-02.
