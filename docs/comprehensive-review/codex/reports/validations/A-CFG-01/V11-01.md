# A-CFG-01 / V11 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Workspace switch and exit replace current workspace, cwd, persistence,
conversation store, runtime state store, memory store, routing, and primary/
pool/future-pool identities atomically.

## Method

```text
1. Trace registered Tauri switch_workspace/delete_workspace callers.
2. Read AppState::{switch_workspace,exit_workspace} in mutation order.
3. Trace AgentPool SharedResources extraction and create_agent injection.
4. Search for conversation/state-store override or apply methods.
5. Verify AgentHandle::try_write lock semantics.
```

## Expected Result

Success should mean all consumers observe one canonical root and matching
stores; failure should roll back; exit should restore the pre-workspace root.

## Result

- Exit code: not applicable
- Duration: not applicable
- Summary: current workspace is committed first; missing roots and cwd errors
  only warn; primary store swaps use non-blocking `try_write`; pool shared
  conversation/state stores never change; future pool Agents inject bootstrap
  stores. Exit does not set process cwd back and does not inject the new global
  conversation store into the primary Agent. The function returns success.
- Log/artifact: none

## Deviations

No rollback or single retained canonical root/store generation exists.

## Conclusion

This supports `A-CFG-01-P1-02`. A-BOOT-01-P1-01 covers initial `--project`
normalization; this validation covers the distinct live workspace transition.

## Follow-Up

A-MEM-02 and Q-E2E-01 should consume the resource-identity facts.

## Correction (2026-08-12)

`A-MEM-02` is not a shared-catalog task ID. The intended downstream owner is
`A-MEM-01`; this correction changes only handoff metadata, not validation
evidence or outcome.
