# A-CFG-01 / V21 / Attempt 03

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

After adding V21-02, all reports retain exact executor metadata, the task stays
`needs_evidence`, source repositories are clean, and no review-owned Cargo
session remains active.

## Method

```text
1. Assert 25 validation files and 25 exact executor lines (before V21-03).
2. Assert both source `git status --short` outputs are empty.
3. Print task status and finding headings.
4. Attempt `pgrep -fl 'cargo|rustc|a-cfg-probe'`.
5. Reconcile every review-owned exec session with its returned exit code.
```

## Expected Result

Metadata/source assertions pass, task remains awaiting primary, and all
review-owned sessions are exited.

## Result

- Exit code: 0
- Duration: 0.05 seconds
- Summary: 25 pre-existing validation files and executor lines matched; source
  worktrees were clean; task status was `needs_evidence`; 1 P0, 5 P1, and 3 P2
  headings printed. Every Cargo session started by this review had already
  returned an exit code. Adding this immutable attempt makes the final count 26.
- Log/artifact: terminal output recorded in the review session

## Deviations

The environment denied global process enumeration (`pgrep: Cannot get process
list`). This does not affect the stronger ownership-scoped fact that each
session ID created by this review returned a terminal exit code.

## Conclusion

The review left no owned Cargo/exec session running, did not dirty source, and
is ready for primary V20 evidence.

## Follow-Up

Primary V20.
