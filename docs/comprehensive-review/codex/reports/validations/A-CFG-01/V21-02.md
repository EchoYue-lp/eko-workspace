# A-CFG-01 / V21 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The subagent report set has exact executor metadata, valid finding counts/IDs,
resolvable links, Codex-only paths, and clean source worktrees.

## Method

```text
set -eu
# Count A-CFG validation files and exact executor lines.
# Reject out-of-range A-CFG finding IDs.
# Resolve every ../validations/A-CFG-01/*.md task-report link.
# Assert 1 P0 + 5 P1 + 3 P2 findings.
# Assert both source `git status --short` outputs are empty.
```

The corrected link-loop variable was `report_link`, avoiding zsh's special
`path` array.

## Expected Result

All assertions pass and the command exits zero.

## Result

- Exit code: 0
- Duration: 0.57 seconds
- Summary: `validation_files=24 executor_exact=24 findings=9 links=ok
  source_worktrees=clean` before this attempt file was added. Adding V21-02
  raises the final validation-file/executor count to 25 without changing task
  finding/link assertions.
- Log/artifact: terminal output recorded in the review session

## Deviations

This is the subagent preflight only. Per protocol, Codex primary still performs
independent V20 before marking the task complete.

## Conclusion

Subagent output is internally coherent, isolated under the Codex report tree,
and did not dirty either source repository.

## Follow-Up

Primary V20.
