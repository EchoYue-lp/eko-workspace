# A-CFG-01 / V19 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

An invalid app-config edit preserves last-known-good values for every live
reload domain.

## Method

```text
Trace config watcher handle_config_change for malformed app YAML through:
- load_config(Some(path)) error branch
- HookConfigLoader error branch
- WebhookEmitter::reload_from_config and replace_endpoints
Use V05-02's executable proof that load_config(Some(invalid)) returns defaults.
```

## Expected Result

If parsing fails during an editor write/rename window, both hooks and webhook
endpoints should remain at their last-known-good state.

## Result

- Exit code: not applicable
- Duration: not applicable
- Summary: hook reload checks `loaded.errors` and preserves prior hooks, but
  webhook reload unconditionally consumes `new_config`. For invalid YAML that
  value is `AppConfig::default`, so `reload_from_config` atomically replaces
  endpoints with the default empty list.
- Log/artifact: V05-02 executable fixture

## Deviations

No existing watcher test invokes `handle_config_change` with malformed YAML or
asserts endpoint identity.

## Conclusion

This supports `A-CFG-01-P1-04`: a transient invalid save silently disables
live webhook delivery instead of preserving last-known-good configuration.

## Follow-Up

Q-E2E-01 should exercise invalid atomic-save and restoration behavior.
