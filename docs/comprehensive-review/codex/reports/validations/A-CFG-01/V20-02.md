# A-CFG-01 / V20 / Attempt 02

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Primary source sampling independently confirms the definitions and registered
call paths supporting all nine A-CFG findings.

## Method

From the correct repository roots, inspect framework config search/load/save
and serde shapes; EKO startup, provider/config commands, model mutation,
workspace transitions, AgentHandle locking, pool resource construction, watcher
reload, webhook replacement and frontend save handling. Search registrations
and callers across `src`, `echo-agent-app-core` and `web-frontend/src`.

## Expected Result

Every finding has a current definition plus real entry/caller path; no cited
branch has already been superseded.

## Result

- Exit code: 0
- Duration: under one second per read-only batch
- Summary: all nine findings' key branches remain at the reviewed commits.
  Explicit load discards source identity; save re-searches mutable cwd; explicit
  parse errors default; workspace mutation commits before best-effort resources;
  primary store swaps use unchecked `try_write`; future pool agents retain
  bootstrap conversation/state stores; model delete omits runtime apply; invalid
  watcher input supplies default config to webhook reload; save errors only warn
  while commands/front end report success; config serde has no unknown-field
  rejection.
- Log/artifact: none

## Deviations

This validates reachability and static semantics; executable behavior is
separately sampled in V20-05 and V20-07.

## Conclusion

The A-CFG findings are current at both reviewed commits.

## Follow-Up

V20-05 and V20-07 independently execute the two highest-risk public config
behaviors.
