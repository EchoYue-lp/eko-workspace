# A-CFG-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Unknown configuration keys, especially credential-key typos, are rejected.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  unknown-field /private/tmp/a-cfg-fixture-unknown
```

The fixture spells `auth_token` as `auth_tokne`.

## Expected Result

Parsing should report the unknown field rather than accept a configuration
which appears credentialed but is not.

## Result

- Exit code: 0
- Duration: 0.31 seconds
- Summary: `accepted=true`, model retained, `has_auth_token=false`.
- Log/artifact: `/private/tmp/a-cfg-fixture-unknown/unknown.yaml`

## Deviations

Top-level and nested config types use `serde(default)` without
`deny_unknown_fields` or a validation pass.

## Conclusion

This supports `A-CFG-01-P2-03`.

## Follow-Up

Add schema/unknown-key diagnostics without adding local permission gates.
