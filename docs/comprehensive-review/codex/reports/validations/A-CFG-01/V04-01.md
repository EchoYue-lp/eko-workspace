# A-CFG-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Deleting the active default configured model updates persisted config, primary
Agent, existing pooled Agents, and future pooled-Agent config as one operation.

## Method

```text
Trace Tauri delete_configured_model -> model_config::delete_configured_model ->
set_default_model and compare it with Tauri set_default_model ->
apply_runtime_model -> AgentPool::{update_app_config,apply_runtime_model}.
```

## Expected Result

When deletion selects a replacement default, all current and future runtime
Agents should switch before success is returned.

## Result

- Exit code: not applicable
- Duration: not applicable
- Summary: the pure config function selects a replacement, but the Tauri
  delete command saves and returns success without calling `apply_runtime_model`.
  Primary, existing pool Agents, `runtime_llm_config`, and pool `app_config`
  remain on the deleted model until another switch or restart.
- Log/artifact: none

## Deviations

The claim is false for a live, registered GUI command.

## Conclusion

This supports `A-CFG-01-P1-03`. It does not claim deletion loses the YAML
change when persistence succeeds.

## Follow-Up

Add a fix-time primary/pool identity test under A-SRF-01 or Q-E2E-01.
