# A-CFG-01 / V10 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

A project partial config overrides only specified values while retaining a
global provider and credential.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  layered-precedence /private/tmp/a-cfg-fixture-layered
```

The fixture writes a global `config.yaml` with provider/token and a project
`echo-agent.yaml` containing only `model.name`.

## Expected Result

The runtime model should combine project model name with global provider/token,
or the product should reject the unsupported partial layering explicitly.

## Result

- Exit code: 0
- Duration: 7.70 seconds
- Summary: provider became default `deepseek`, model was `project-model`, and
  `has_auth_token=false`.
- Log/artifact: `/private/tmp/a-cfg-fixture-layered/`

## Deviations

The project file replaced the global config wholesale; serde defaults masked
the missing fields.

## Conclusion

This supports `A-CFG-01-P2-01`. It is a precedence/coherence defect, not an
argument for SQLite or an authorization gate.

## Follow-Up

Define and test one explicit global/project policy.
