# A-CFG-01 / V08 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Saving config after loading an explicit path persists back to that source even
after the process cwd changes.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  cwd-save /private/tmp/a-cfg-fixture-cwd-save-isolated
```

The probe overrides framework user-data before path resolution, loads
`startup/explicit.yaml`, changes cwd to `workspace`, and calls `save_config`.

## Expected Result

The explicit source should contain the saved model; no unrelated config should
be created.

## Result

- Exit code: 0
- Duration: 3.40 seconds
- Summary: `explicit_contains_saved=false` and
  `workspace_config_exists=false`. This attempt did not print the selected
  global target.
- Log/artifact: `/private/tmp/a-cfg-fixture-cwd-save-isolated/`

## Deviations

The expected explicit-file write did not occur. The probe output was
insufficient to distinguish global-file creation from another target.

## Conclusion

The claim failed; V08-03 adds exact target evidence without changing this
attempt.

## Follow-Up

V08-03.
