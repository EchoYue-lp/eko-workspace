# A-CFG-01 / V20 / Attempt 05

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; fixture under `/private/tmp`

## Claim

After explicitly loading config A, changing cwd to workspace B and saving does
not overwrite B's existing project config.

## Method

Rerun the V20-03 fixture to a terminal exit. It writes distinct A/B YAML, loads
A explicitly, mutates the model, changes cwd to B, invokes public `save_config`,
then reads both files.

## Expected Result

`explicit_changed=true workspace_overwritten=false`.

## Result

- Exit code: 0
- Duration: 0.44 seconds
- Summary: `explicit_changed=false workspace_overwritten=true`.
- Log/artifact: `/private/tmp/a-cfg-primary-fixture`

## Deviations

The process exits zero because it reports behavior rather than asserting the
expected invariant.

## Conclusion

Primary executable evidence independently confirms A-CFG-01-P0-01.

## Follow-Up

Fix-time save-to-source and atomic-write tests.
