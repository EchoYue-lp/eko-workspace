# A-CFG-01 / V09 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

A config save cannot overwrite an unrelated project config merely because a
workspace switch changed the process cwd.

## Method

```text
env -u ECHO_AGENT_CONFIG CARGO_TARGET_DIR=/private/tmp/a-cfg-probe-target \
  cargo run --offline --quiet --manifest-path /private/tmp/a-cfg-probe/Cargo.toml -- \
  workspace-existing-save /private/tmp/a-cfg-fixture-workspace-existing
```

The fixture explicitly loads `startup/explicit.yaml`, pre-creates a different
`workspace/echo-agent.yaml`, changes cwd, then saves.

## Expected Result

Only the explicit startup file should receive the new model.

## Result

- Exit code: 0
- Duration: 3.06 seconds
- Summary: `explicit_contains_saved=false`,
  `workspace_contains_saved=true`.
- Log/artifact: `/private/tmp/a-cfg-fixture-workspace-existing/`

## Deviations

The unrelated workspace file was overwritten with the full in-memory config.

## Conclusion

This supports the highest-impact branch of `A-CFG-01-P0-01`: workspace
navigation can redirect later settings/provider writes.

## Follow-Up

Fix-time source-identity and atomic-write tests.
