# A-CFG-01 / V13 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Config changes are either applied live to every affected service or explicitly
reported as restart-required, and persistence failure is returned to the GUI.

## Method

```text
1. Trace ConfigPanel save -> configApi.updateFull -> Tauri update_full_config.
2. Field-map UpdateFullConfigRequest against in-memory cfg mutations, primary
   Agent setters, AgentPool methods, channel/MCP/server/logging service owners,
   and config watcher scope.
3. Inspect persistence error handling and GUI success messaging.
```

## Expected Result

The response should describe which fields are live and which require restart;
success should imply durable save. Live model/Agent changes should update both
primary and pool where applicable.

## Result

- Exit code: not applicable
- Duration: not applicable
- Summary: the endpoint mutates all requested in-memory fields, warns and
  continues on `save_config` error, hot-applies only primary temperature,
  max-tokens, and system prompt, and returns the ordinary full config. GUI then
  displays `已保存`. MCP topology, channels, server, logging, Agent feature
  toggles/name/memory path need restart but no response field/message says so;
  pool Agents do not receive the purported live Agent changes.
- Log/artifact: none

## Deviations

The frontend additionally calls legacy `update_config` for max_iterations,
but that endpoint changes only `web_config` and system prompt; it never calls
the framework's available `set_max_iterations`.

## Conclusion

This supports `A-CFG-01-P1-05` (false durability success) and
`A-CFG-01-P2-02` (unreported/partial live-reload semantics).

## Follow-Up

A-SRF-01 for GUI response contract and Q-E2E-01 for restart/live matrix tests.
