# A-CFG-01 / V03 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Provider selection, credential precedence, runtime application, and secret
projection can be traced from real GUI/TUI/startup callers to framework client
construction.

## Method

```text
1. Trace resolve_runtime_model through infra::create_agent_with_diagnostics,
   TUI /model, and Tauri provider commands.
2. Compare credential precedence in resolve_runtime_model and test_connection.
3. Search response types and tracing calls for auth_token/api_key/secret values.
4. Search both repositories for EKO_AUTH_TOKEN/EKO_BASE_URL/EKO_MODEL consumers.
```

## Expected Result

Configured provider values should precede standard provider environment keys;
runtime updates should reach primary and pool; secrets should be represented
to UI/logs only by presence/source metadata.

## Result

- Exit code: 0
- Duration: not applicable
- Summary: normal resolution is provider config -> legacy model config (same
  provider) -> provider-specific env; model selection reaches primary and pool
  when `apply_runtime_model` is called. GUI responses expose only
  `has_auth_token`/`auth_source`, and logs record source, not token. The three
  imported `EKO_*` aliases have no consumer after shell import.
- Log/artifact: none

## Deviations

The documented compatibility aliases are unreachable, and deletion skips the
otherwise-correct runtime application helper (V04).

## Conclusion

The ordinary provider and secret/log boundary is sound for EKO's trusted local
UI. This supports the narrower alias/discovery issue in `A-CFG-01-P2-01` and
does not support adding a new permission gate or keychain requirement.

## Follow-Up

V04 and V12.
