# A-CFG-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Global and project app-config precedence is coherent: a project override can
coexist with global provider credentials and the same paths are advertised by
discovery.

## Method

```text
1. Inspect echo-agent/src/config.rs:666-756.
2. Inspect echo-agent-cli/echo-agent-app-core/src/config_discovery.rs:219-265.
3. Compare lookup order and whether values merge or one file replaces another.
4. Compare app and MCP files listed by discover_config with runtime loaders.
```

## Expected Result

The precedence contract should name the same global/project paths and preserve
lower-scope values not overridden by a higher-scope partial file.

## Result

- Exit code: not applicable
- Duration: not applicable
- Summary: startup chooses the first parseable whole file in env -> cwd
  `echo-agent.yaml` -> user-data `config.yaml`; it performs no merge.
  Discovery advertises user-data `echo-agent.yaml` instead of `config.yaml`,
  and advertises project `.mcp.json` although the EKO MCP loader does not use
  that default.
- Log/artifact: none

## Deviations

The claim is false on both value composition and advertised paths.

## Conclusion

This supports `A-CFG-01-P2-01`. Dynamic loss of a global secret is separately
replayed in V10.

## Follow-Up

V10 and V16.
