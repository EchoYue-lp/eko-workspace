# A-CFG-01 / V20 / Attempt 09

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The workspace split-state finding accurately describes non-blocking primary
store writes and stale future-pool resource injection.

## Method

Inspect facade `src/agent/handle.rs:255-280` and application
`agent_pool.rs` shared-resource extraction, runtime-model propagation and future
Agent creation/injection.

## Expected Result

`try_write` can return `None` and callers ignore it; conversation/state stores
for future pool Agents remain the bootstrap shared handles.

## Result

- Exit code: 0
- Duration: under one second
- Summary: `try_write` returns `None` on lock contention; workspace transition
  ignores both results. Pool has explicit overrides for working dir/memory and
  model state, but future creation still injects `shared.state_store` and
  `shared.conversation_store` captured at bootstrap.
- Log/artifact: none

## Deviations

No full AppState fault injection was run because this source branch has no
isolated constructor/test seam.

## Conclusion

This independently supports A-CFG-01-P1-02.

## Follow-Up

Q-E2E-01 should add lock-contention and future-pool identity assertions.
