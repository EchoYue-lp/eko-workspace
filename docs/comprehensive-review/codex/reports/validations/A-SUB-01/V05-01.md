# A-SUB-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Plugin reload produces one effective catalog generation across primary, existing pool Agents, and future pool Agents.

## Method

```text
Trace PluginRuntimeService reload -> unload/register plugin Subagents; trace
primary system prompt/TaskCapabilityCatalog; trace AgentPool creation, existing
Agents, future create_agent, registry and ToolManager replacement. Search catalog refresh APIs/callers.
```

## Expected Result

One successful reload atomically replaces every model-facing catalog, authoring validator, and executable identity for all Agent identities.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: reload mutates primary registry and its local dispatch catalog through framework registration. It does not replace primary system prompt or Task capability snapshots and has no AgentPool reference/refresh. Existing pool prompts stay old. Future pool Agents rediscover disk definitions but do not incorporate plugin roles into their prompt; then their ToolManager is replaced with primary's shared manager, splitting explanatory catalog from executable registry.
- Log/artifact: `plugin_runtime.rs:201-206`, `:804-875`, `:1157-1164`; `agent_pool.rs:188-212`, `:824-883`; `infra.rs:233-253`; `register.rs:26-75`.

## Deviations

Framework per-Agent catalog projection remains canonical F-SUB-01-P1-02; this finding is EKO's missing generation propagation and pool integration.

## Conclusion

Supports A-SUB-01-P1-01.

## Follow-Up

Make PluginRuntime commit one EKO catalog generation and apply it to all consumers/pool identities.
