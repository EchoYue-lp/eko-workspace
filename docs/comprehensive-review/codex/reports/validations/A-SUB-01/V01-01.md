# A-SUB-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Definition precedence produces one deterministic effective role within each scope.

## Method

```text
Trace discover_subagents -> merge_scope -> scan_directory -> HashMap insertion.
Inspect scope precedence tests and search duplicate/collision handling for project,
user, builtin, and plugin definitions.
```

## Expected Result

Project > user > builtin is explicit; same-scope duplicate names are rejected or deterministically ordered with source diagnostics.

## Result

- Exit code: 0 for static inspections; claim failed.
- Duration: static inspection.
- Summary: cross-scope precedence is explicit and tested. Inside one project/user scope, recursive `read_dir` results are unsorted and every same-name definition silently overwrites the prior HashMap entry. Plugin definitions separately reject duplicates.
- Log/artifact: `subagent_loader.rs:262-337`, `:350-383`, tests `:804-866`; `plugin_components.rs:96-138`.

## Deviations

No filesystem-order fixture was run by direction.

## Conclusion

Supports A-SUB-01-P2-04.

## Follow-Up

Reject same-scope duplicates with both source paths, or define stable path precedence.
