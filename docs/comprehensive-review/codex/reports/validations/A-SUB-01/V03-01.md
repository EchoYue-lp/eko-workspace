# A-SUB-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

Plugin Subagents use the same EKO prompt compiler/cardinality/language/result contract as builtin, project, and user roles.

## Method

```text
Compare register_default_subagents compilation with plugin read/build/register.
Trace system prompt, compiler injection, delegation compiler, and existing prompt-contract tests.
```

## Expected Result

Every EKO role passes one role body through EkoSubagentPromptCompiler exactly once.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: default/project/user roles call `EkoSubagentPromptCompiler::compile_system`. Plugin roles put raw markdown directly in `system_prompt`; delegation-capable plugin roles install framework `DefaultSubagentPromptCompiler`. They omit EKO language, protocol, capability, result-quality, optional suggestions and result-contract system sections.
- Log/artifact: `infra.rs:626-638`; `subagent_prompt.rs:177-234`; `plugin_components.rs:511-550`.

## Deviations

Framework default compiler may still produce a generic invocation, but it is not EKO's declared product contract.

## Conclusion

Supports A-SUB-01-P1-02.

## Follow-Up

Compile plugin roles with the shared EKO compiler and add field/cardinality tests.
