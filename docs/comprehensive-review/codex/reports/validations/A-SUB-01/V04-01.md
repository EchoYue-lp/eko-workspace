# A-SUB-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: see V00

## Claim

The catalog injected into prompt and Task authoring contains only successfully constructed/registered executable roles.

## Method

```text
Trace discovered_subagents -> SubagentCatalogSnapshot/prompt/default-route gate ->
register_default_subagents per-role build_result -> registry list -> task tool registration.
Compare primary post-bootstrap and pool construction paths.
```

## Expected Result

Catalog snapshot is committed only after all advertised roles are ready, or startup fails atomically.

## Result

- Exit code: 0 for inspection; claim failed.
- Duration: static inspection.
- Summary: prompt and one pool Task capability snapshot are built from discovered definitions before Agent construction. Individual build failures only warn/skip. The primary post-hoc Task tools use registered definitions, but its immutable system prompt still advertises the pre-build set; pool `create_agent` registers task CRUD from the pre-build snapshot before sharing primary ToolManager.
- Log/artifact: `infra.rs:233-253`, `:489-515`, `:612-843`; `tasks/task_runtime/register.rs:26-75`; `agent_pool.rs:824-883`.

## Deviations

The framework readiness problem is canonical F-SUB-01-P1-01/P1-02. This validation isolates EKO's pre-construction snapshot timing and failure policy.

## Conclusion

Supports A-SUB-01-P1-03.

## Follow-Up

Prepare all roles, fail required/default roles, then commit one executable catalog generation to prompt/tools.
