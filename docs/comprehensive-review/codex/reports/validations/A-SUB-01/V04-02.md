# A-SUB-01 / V04 / Attempt 02

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: CLI clean; unrelated framework dirty paths excluded

## Claim

Prompt and Task authoring catalogs are published only from successfully
constructed and registered executable Subagent definitions.

## Method

Independently trace `discover_subagents` to `subagent_catalog_snapshot`, prompt
assembly, `register_default_subagents`, per-definition build failure handling,
and primary/pool `TaskCapabilityCatalog` construction in current CLI source.
Compare Arc provenance rather than comments or the delegated report.

## Expected Result

Both prompt and Task capability use a post-registration executable snapshot, or
startup fails before either is published.

## Result

- Exit code: 1 (invariant failed)
- Duration: static source inspection
- Summary: `infra.rs:242-253` creates and publishes one snapshot from every
  discovered definition before construction. Per-role builds later warn and
  skip failures. `infra.rs:505-515` passes that same pre-construction Arc to the
  primary Task capability catalog; it does not rebuild from registered roles.
  Pool Agent construction repeats the same sequence.

## Deviations

Delegated V04-01 correctly found the overall failure but incorrectly stated that
primary post-bootstrap Task tools use registered definitions. That attempt is
retained and not used for this narrower provenance claim.

## Conclusion

A-SUB-01-P1-03 is valid and affects primary prompt, primary Task authoring, and
pool prompt/Task authoring. The task report must remove the contrary conclusion.
