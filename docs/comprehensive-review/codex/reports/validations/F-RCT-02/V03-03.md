# F-RCT-02 / V03 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: `echo-agent` clean; concurrent CLI generated files not inspected

## Claim

The formal `AgentTurn` phase and iteration fields reflect the live non-streaming loop.

## Method

Search every construction, mutation, and consumer of `AgentTurn`, `TurnPhase`, `current_turn`, `record_iteration`, and `RunEvent::PhaseTransition`.

```text
rg -n 'current_turn|AgentTurn|TurnPhase::|record_iteration|PhaseTransition' src echo-* --glob '*.rs'
nl -ba src/agent/turn.rs | sed -n '1,120p'
nl -ba src/agent/react/run/react_loop.rs | sed -n '500,560p'
```

## Expected Result

Think, Act, and Finalize transitions and loop iteration counts are updated by the canonical driver or the duplicate state model does not exist.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: `prepare_react_context` creates a turn and advances it only to Recall. No production caller advances Think/Act/Finalize or calls `AgentTurn::record_iteration`; only the Recall phase transition is emitted. Replay code consumes phase transitions, so the stale projection is externally meaningful.
- Log/artifact: `src/agent/turn.rs:1-80`, `src/agent/react/run/react_loop.rs:511-555`, `src/eval/replay.rs:103-109`

## Deviations

The declared lifecycle is not driven by the canonical loop.

## Conclusion

Supports F-RCT-02-P2-05. It does not establish whether external consumers currently display `current_turn` directly.

## Follow-Up

Choose one state authority: derive phase events centrally from the driver or delete the disconnected turn projection.
