# F-RCT-02 / V03 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: `echo-agent` clean; concurrent CLI generated files not inspected

## Claim

All terminal outcomes use one authority that completes trace, task node, recovery state, transcript, lifecycle hooks, and the terminal event consistently.

## Method

Build a static terminal matrix for text success, final-answer tool success, no response, max iterations, prompt-hook block, think cancellation/block, tool cancellation, and direct-answer shortcut.

```text
nl -ba src/agent/react/run/phases/finalize.rs | sed -n '20,270p'
nl -ba src/agent/react/run/phases/prepare.rs | sed -n '55,93p'
nl -ba src/agent/react/run/phases/tools.rs | sed -n '170,430p'
nl -ba src/agent/react/run/stream_channel.rs | sed -n '494,755p'
```

## Expected Result

Every terminal outcome closes all applicable lifecycle projections exactly once before publishing its typed terminal.

## Result

- Exit code: 0
- Duration: under 1 second per bounded inspection
- Summary: text success finalizes trace; successful `final_answer` tool does not. Max-iteration failure has full failure fan-out, while no-response omits checkpoint, transcript, and failure/session hooks. UserPromptSubmit block sends a final answer and SessionEnd but does not close the already-started trace. Think/tool abandonment paths similarly return without a common terminal commit. The direct-answer shortcut bypasses `prepare_turn` and common finalizers.
- Log/artifact: `src/agent/react/run/phases/finalize.rs:23-111`, `:128-270`; `src/agent/react/run/phases/prepare.rs:57-92`; `src/agent/react/run/stream_channel.rs:509-516`, `:704-755`; `src/agent/react/run/react_loop.rs:598-750`

## Deviations

Terminal authority is fragmented across branches with materially different durable state.

## Conclusion

Supports F-RCT-02-P1-03. It does not require all side effects on every outcome, but trace closure and typed terminal state cannot be omitted.

## Follow-Up

Define one terminal outcome/commit API and validate its branch matrix.
