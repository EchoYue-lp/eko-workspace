# F-RCT-02 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: `echo-agent` clean; `echo-agent-cli` had concurrent untracked `web-frontend/src/generated/*.ts` files, which were not read or modified

## Claim

The framework has one reachable ReAct loop, and any competing loop implementation is identifiable.

## Method

From the repository root, read the task/protocol and F-RCT-01/F-LLM-01 dependencies. In `echo-agent`, search definitions and calls for `run_react_loop`, `run_core_loop`, `process_steps`, phase functions, loop detector types, and turn state. Trace `run_direct` and `run_chat_direct` to the loop.

```text
rg -n 'run_react_loop|run_core_loop|process_steps|LoopDetector|AgentTurn|TurnPhase' echo-agent/src --glob '*.rs'
rg -n 'process_steps\(' echo-agent/src --glob '*.rs'
rg -n 'run_core_loop\(' echo-agent/src --glob '*.rs'
```

## Expected Result

Exactly one production loop owns iteration and terminal dispatch; any alternative is either a thin adapter or deleted.

## Result

- Exit code: 0
- Duration: under 1 second per bounded search
- Summary: `run_direct`/`run_chat_direct` reach `run_react_loop`, which spawns `AgentRunSnapshot::run_core_loop`. The latter is the sole live iterative driver. `process_steps` has no caller and is retained behind `#[allow(dead_code)]`. `LoopDetector` has configuration and tests but no production construction or call.
- Log/artifact: source anchors in `src/agent/react/run/react_loop.rs:174`, `:598`, `:711`; `src/agent/react/run/stream_channel.rs:494`; `src/agent/config.rs:1025`; `src/agent/react/loop_detector.rs:41`

## Deviations

The expected single authority is undermined by a compiled, divergent dead implementation and a disconnected loop detector.

## Conclusion

Supports F-RCT-02-P2-04 and the runtime consequence of dependency finding F-RCT-01-P2-05. It does not prove dynamic termination.

## Follow-Up

Delete `process_steps` when the canonical phase loop is retained; connect or remove the detector option under F-RCT-01.
