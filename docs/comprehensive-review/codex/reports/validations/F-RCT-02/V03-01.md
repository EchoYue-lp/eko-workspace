# F-RCT-02 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: `echo-agent` clean; concurrent CLI generated files not inspected

## Claim

`FinalAnswer` is emitted only after Stop-hook continuation is resolved and is the last event of a completed non-streaming turn.

## Method

Inspect text and tool finalization ordering and the non-stream collector's terminal break.

```text
nl -ba src/agent/react/run/phases/finalize.rs | sed -n '20,208p'
nl -ba src/agent/react/run/react_loop.rs | sed -n '708,750p'
```

## Expected Result

Stop continuation precedes success persistence and terminal emission. If continuation is requested, no terminal success is visible and the execution lease remains held until the continued loop ends.

## Result

- Exit code: 0
- Duration: under 1 second
- Summary: text finalization checkpoints, marks the node successful, finalizes the trace, and sends `FinalAnswer` before invoking Stop. A continuation then returns `ControlFlow::Continue`. The non-stream collector breaks on the already-sent `FinalAnswer`, returns to its caller, and drops its turn lease while the spawned core may continue. Tool finalization also runs Stop after `FinalAnswer`, but ignores `continue_reason` rather than continuing.
- Log/artifact: `src/agent/react/run/phases/finalize.rs:141-207`, `:87-110`; `src/agent/react/run/react_loop.rs:729-750`

## Deviations

Both branch ordering and branch equivalence fail.

## Conclusion

Supports F-RCT-02-P1-02. It does not determine whether configured hooks currently request continuation in production; the public hook result makes the branch representable.

## Follow-Up

Add a hook-continuation regression that attempts a second turn immediately after the first observed terminal.
