# F-NBK-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex report files only outside the source repositories

## Claim

`NotebookCell` is a stable, lossless-enough projection of canonical tool execution identity, ordering, terminal outcome, and complete artifacts.

## Method

```text
1. Compare NotebookCell fields at src/notebook/mod.rs:13-26 with RunEvent
   ToolCall/ToolResult/ToolError at src/trace/mod.rs:302-359.
2. Trace call_id creation and terminal emission at
   src/agent/react/run/pipeline.rs:475-487 and 826-867.
3. Compare ToolOutputArtifactIdentity/Ref fields at
   echo-core/src/tools/artifact.rs:69-143.
4. Inspect record_cell index/timestamp assignment and its accepted inputs.
```

## Expected Result

A cell correlates one start with one success/failure terminal using stable run/call identity, preserves canonical order semantics, and references complete spilled output when summaries truncate it.

## Result

- Exit code: not-applicable; static field/call-chain comparison.
- Duration: under one second per bounded inspection.
- Summary: `NotebookCell` stores only an append index, tool name, caller-supplied 200-character summaries, duration, and record time. It has no conversation/run/turn/message/call identity, start identity, success/error/failure, truncation fact, byte counts, output handling, artifact reference/hash, or schema version. Its index is lock-acquisition append order and cannot be correlated with canonical tool-call order or repeated/concurrent calls. The live Tool trace already owns these missing facts.
- Log/artifact: this report.

## Deviations

The append index is unique and contiguous within one tracker. The defect is not duplicate numeric indexes; it is the absence of semantic correlation and terminal/artifact mapping.

## Conclusion

Supports F-NBK-01-P2-01. The current export cannot substantiate the module's reproducibility claim, and wiring it as a parallel recorder would create a weaker second authority beside canonical Tool events.

## Follow-Up

Define Notebook as a versioned projection of canonical events/artifacts and validate repeated IDs, concurrent completions, failures, truncation, and replay/reload.
