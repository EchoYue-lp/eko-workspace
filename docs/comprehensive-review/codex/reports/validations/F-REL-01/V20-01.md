# F-REL-01 / V20 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Independent source search can map generic retry, circuit, budget, JSON, hash
and time definitions to their real framework and EKO callers without reading
the delegated F-REL report.

## Method

Search both source repositories from current commits for `RetryPolicy`, provider
retry wrappers, ReAct retry, `CircuitBreaker` construction and transitions,
`TokenBudget`, `clean_json`, FNV and time helpers. Exclude targets, docs and
examples when classifying production callers.

## Expected Result

Each primitive has a definition/caller map and duplicate policies are explicit.

## Result

- Exit code: 0
- Duration: under one second per parallel search
- Summary: provider HTTP adapters use core `with_retry_if`; ReAct independently
  wraps provider/trait calls in `retry_llm_call`; only tests and the ReAct
  result-accounting path call circuit methods, with no production
  `try_advance`; budget and JSON helpers have reachable compression/critic
  callers; root time re-exports core while orchestration retains a duplicate
  `now_secs` helper.
- Log/artifact: none

## Deviations

This is the primary's independent evidence pass; no delegated F-REL task or
validation report was read before it completed.

## Conclusion

The map supports independent review of retry layering, missing circuit
admission, budget/JSON edges and low-impact time duplication.

## Follow-Up

V20-02 through V20-06 inspect and execute individual invariants.
