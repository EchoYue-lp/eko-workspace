# F-REL-01 / V03 / Attempt 04

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex review artifacts outside the source repositories are dirty

## Claim

FNV hashing is explicitly wrapping and deterministic, and local-time conversion
uses the offset applicable to the supplied instant rather than a process-cached
fixed offset.

## Method

```text
sed -n '1,80p' echo-agent/echo-core/src/utils/hash.rs
sed -n '1,260p' echo-agent/echo-core/src/utils/time.rs
```

Check the FNV-1a known vector for `hello` and inspect `to_local` for direct
conversion through `chrono::Local` on each supplied timestamp.

## Expected Result

Hash multiplication is deliberately wrapping and matches a published known
vector; winter and summer instants can select different DST offsets.

## Result

- Exit code: 0
- Duration: not separately measured
- Summary: `fnv1a_64` uses `wrapping_mul`; `to_local` calls
  `dt.with_timezone(&chrono::Local).fixed_offset()` for the supplied instant.
- Log/artifact: none

## Deviations

`now_millis` still narrows `u128` to `u64`, but overflow requires a clock roughly
584 million years beyond the epoch and is not reported as a material defect.

## Conclusion

Static inspection supports the claim. V06-05 provides the dynamic known-vector
and DST check. It does not prove collision resistance; FNV is documented only
for deterministic content deduplication.

## Follow-Up

None.
