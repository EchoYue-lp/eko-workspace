# F-REL-01 / V20 / Attempt 04

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; fixture under `/private/tmp`

## Claim

Public token-budget allocation remains within the total window and never
panics for public numeric inputs.

## Method

Fresh fixture: build a 1,000-token budget with a negative system allocation and
inspect component budgets; use `catch_unwind` around `allocate(usize::MAX,
usize::MAX, usize::MAX)`.

## Expected Result

Invalid percentages are rejected/clamped and extreme counts return a bounded
allocation without panic.

## Result

- Exit code: 0
- Duration: approximately 55 seconds including first compile
- Summary: negative system allocation produced `negative_system=0
  conversation=2000`; the max allocation panicked at `budget.rs:106` while
  adding total usage, and the fixture printed `max_allocate_panicked=true`.
- Log/artifact: `/private/tmp/f-rel-primary-probe`

## Deviations

The panic hook prints even though `catch_unwind` catches it; process exit 0
means the fixture completed, not that the budget invariant passed.

## Conclusion

Invalid percentages can allocate more than the window, and extreme public
counts panic. This is an independent P2 finding.

## Follow-Up

Use a fallible validated constructor and saturating/checked aggregation.
