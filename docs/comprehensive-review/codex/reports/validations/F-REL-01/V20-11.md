# F-REL-01 / V20 / Attempt 11

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before execution

## Claim

The public `config()`/`config_mut()` pair lets an external consumer apply the
documented consuming `AgentConfig` retry builders after builder construction.

## Method

Compile the external fixture after enabling the facade's `testing` feature. The
fixture attempted `agent.config().clone().llm_max_retries(...)` before assigning
the result through `config_mut()`.

## Expected Result

The public configuration can be copied, adjusted, and replaced, allowing the
runtime scenario to execute.

## Result

- Exit code: 101
- Duration: 1.6 seconds
- Summary: rustc E0507 showed that `AgentConfig` does not implement `Clone`;
  cloning `&AgentConfig` therefore clones only the reference, and the consuming
  `llm_max_retries(self, ...)` method cannot move from it.
- Log/artifact: compiler references the fixture `src/main.rs:21-24` and
  `agent/config.rs:628`.

## Deviations

This is a fixture-construction failure, not yet a runtime result. It also reveals
a secondary public-API ergonomics gap, but that gap is outside this validation's
reliability finding set.

## Conclusion

Inconclusive for cancellation. Construct `AgentConfig` before `ReactAgent`, then
inject the mock with the public `with_llm_client` method.

## Follow-Up

Use only the direct public constructor path and retain this failed attempt.
