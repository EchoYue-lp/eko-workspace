# F-REL-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; Codex review artifacts outside the source repositories are dirty

## Claim

Each generic reliability behavior has one framework authority, and the root
facade adds no second implementation.

## Method

Search both repositories by type, function, configuration field, arithmetic
shape, and behavioral term; then read each definition and its immediate calls.

```text
rg -n -g '*.rs' 'RetryPolicy|with_retry_if|with_retry\(|retry_llm_call|fn retry_delay_ms|backoff|CircuitBreaker|try_advance|record_success|record_failure|TokenBudget|TokenBudgetConfig|fnv1a_64|now_secs|now_millis|extract_json_from_markdown|clean_json' echo-agent echo-agent-cli
sed -n '1,260p' echo-agent/echo-core/src/retry.rs
sed -n '1,300p' echo-agent/src/agent/react/run/retry.rs
sed -n '1,105p;625,715p;760,850p' echo-agent/echo-execution/src/tools.rs
sed -n '1,80p' echo-agent/src/retry.rs
```

Working directory: repository root. No feature flags or network access.

## Expected Result

The public `echo_core::retry` primitive is the sole generic backoff authority;
domain wrappers supply error classification and policy only. Root facade code is
a lossless re-export. Shared utility helpers have no same-semantics copies in
crates that can already depend on `echo_core`.

## Result

- Exit code: 0
- Duration: not separately measured
- Summary: `src/retry.rs` is a pure facade, but live retry/backoff arithmetic is
  independently implemented by `echo_core::retry`, ReAct LLM retry, tool
  execution, and task orchestration. ReAct and provider retries can compose on
  one request. Six `now_secs` and two `now_millis` definitions also remain.
- Log/artifact: none

## Deviations

Some retry loops are legitimately domain-specific because they use different
terminal facts. The deviation is the duplicated generic delay/attempt authority,
not the mere presence of more than one retrying domain.

## Conclusion

The root retry adapter is correctly thin, but the single-authority claim fails.
This supports F-REL-01-P2-03 and the cleanup portion of F-REL-01-P3-08. It does
not by itself prove nested request counts or cancellation behavior.

## Follow-Up

Primary review must rebuild this inventory from source because F-REL-01 remains
`needs_evidence` after an isolation incident in V07-01.
