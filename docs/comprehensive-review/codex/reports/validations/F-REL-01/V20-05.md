# F-REL-01 / V20 / Attempt 05

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; fixture under `/private/tmp`

## Claim

`clean_json` repairs delimiters only outside JSON string values.

## Method

Pass valid JSON containing literal `,}` and `,]` inside a string plus one real
trailing object comma through the public helper and compare text.

## Expected Result

Only the structural trailing comma is removed; string content is unchanged.

## Result

- Exit code: 0
- Duration: 3.66 seconds
- Summary: input string value `literal ,} and ,] stay` became
  `literal } and ] stay`; `changed=true`.
- Log/artifact: `/private/tmp/f-rel-primary-probe`

## Deviations

The helper is reachable from `LlmCritic` auto-fix, so this is not dead utility
behavior.

## Conclusion

The repair helper silently rewrites valid user/model string content. This is an
independent P2 finding.

## Follow-Up

Parse with a tolerant structured parser or scan with string/escape state.
