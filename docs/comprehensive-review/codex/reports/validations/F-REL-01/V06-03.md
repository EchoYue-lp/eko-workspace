# F-REL-01 / V06 / Attempt 03

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean; fixture under this validation directory is review-only

## Claim

A half-open circuit probe cannot permanently consume the only quota when its
request is cancelled/dropped without a completion callback.

## Method

```text
cd docs/comprehensive-review/codex/reports/validations/F-REL-01/artifacts/edge-probe
cargo run --offline -- circuit
```

Open at threshold one with zero timeout; call `try_advance` once and deliberately
omit `record_success`/`record_failure`, then call it again.

## Expected Result

The abandoned probe quota is reclaimed, represented by an RAII permit, or the
second call can proceed under a documented recovery rule.

## Result

- Exit code: 0
- Duration: 0.02 seconds
- Summary: first probe was allowed; state remained `half_open`; second probe was
  rejected because `probes_in_flight` remained one.
- Log/artifact: [edge probe](artifacts/edge-probe/src/main.rs)

## Deviations

The command simulates cancellation/drop by omitting the callback exactly as the
public API contract permits callers to do accidentally. Current production
ReAct never gates, so this defect is latent until P1-01 is corrected.

## Conclusion

The cancellation invariant fails and supports F-REL-01-P2-06.

## Follow-Up

Use a permit whose `Drop` releases the half-open slot.
