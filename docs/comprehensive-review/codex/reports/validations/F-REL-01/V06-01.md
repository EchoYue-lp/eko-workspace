# F-REL-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source repositories clean; fixture under this validation directory and its target are review-only artifacts

## Claim

`TokenBudget` handles maximum counts and invalid allocation percentages without
panic or expanding a category beyond `total_window`.

## Method

```text
cd docs/comprehensive-review/codex/reports/validations/F-REL-01/artifacts/edge-probe
cargo run --offline -- budget
```

Fixture source: `artifacts/edge-probe/src/main.rs`. Debug profile, Rust 1.97.1,
local path dependency on reviewed `echo_core`; no network.

## Expected Result

No panic is observed and all derived category budgets remain within a 100-token
window, or invalid input returns a typed error.

## Result

- Exit code: 0
- Duration: 14.40 seconds including first fixture build
- Summary: `catch_unwind` observed an addition panic at `budget.rs:106` for
  `(usize::MAX, 1, 1)`. Allocation `(-0.5, 0, 0, 0)` produced system budget 0
  and conversation budget 150 for a total window of 100.
- Log/artifact: [edge probe](artifacts/edge-probe/src/main.rs)

## Deviations

The command exits zero because the fixture deliberately catches and asserts the
panic. The panic hook still prints the source location.

## Conclusion

The invariant fails and supports F-REL-01-P2-04. It proves debug behavior, not
the exact optimized wrap result.

## Follow-Up

Primary must recreate the boundary with an independent fixture.
