# F-REL-01 / V20 / Attempt 10

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before execution

## Claim

The fresh external Agent fixture compiles with the facade's default-free public
API and runs the cancellation scenario.

## Method

Run `cargo run --offline -- cancel` with
`CARGO_TARGET_DIR=/private/tmp/f-rel-primary-agent-target` after Cargo creates
the report-owned fixture lock file.

## Expected Result

Compilation succeeds and the executable prints a cancellation boundary result.

## Result

- Exit code: 101
- Duration: approximately 13 seconds after dependency compilation
- Summary: compilation reached the fixture and failed with E0432 because
  `echo_agent::testing` is gated by `cfg(any(test, feature = "testing"))`, while
  the fixture dependency had not enabled the public `testing` feature.
- Log/artifact: rustc pointed at `echo-agent/src/lib.rs:59-60` and the fixture
  import in `src/main.rs:4`.

## Deviations

This is a fixture manifest omission. It is not evidence that the reviewed
runtime behavior passes or fails. The build stayed in the dedicated temp target.

## Conclusion

Inconclusive. Enable the explicitly documented testing feature and rerun.

## Follow-Up

Amend only the report-owned fixture dependency, update its lock offline, and
retain this failed attempt.
