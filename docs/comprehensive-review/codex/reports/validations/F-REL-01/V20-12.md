# F-REL-01 / V20 / Attempt 12

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before and after execution

## Claim

Cancelling a public streaming Agent while its first retryable LLM failure is in
the outer backoff prevents a second provider attempt and completes promptly.

## Method

Use a fresh external consumer with `MockLlmClient` responses `[429,
"late response"]`, `llm_max_retries=1`, and `llm_retry_delay_ms=250`. Start
`chat_stream_with_cancel`, cancel after 20 ms, drain the stream, and print call
count, event/error count, and elapsed time. Run offline with the report-owned
lock and a dedicated temp target.

## Expected Result

`calls=1`, a typed cancellation terminal, and completion close to the 20-ms
cancellation point rather than after the full backoff.

## Result

- Exit code: 0
- Duration: 1.57 seconds compile plus execution
- Summary: `scenario=cancel calls=2 events=3 errors=[] elapsed_ms=272`.
  Cancellation did not interrupt the 250-ms outer sleep, a second LLM call
  started after cancellation, and the stream consumed a normal late response
  without an error.
- Log/artifact: report-owned `artifacts/primary-agent-probe`; target
  `/private/tmp/f-rel-primary-agent-target`.

## Deviations

None. The fixture is an independent primary reconstruction using only public
framework APIs.

## Conclusion

Finding F-REL-01-P1-02 is independently reproduced. Cancellation is not an
attempt-admission invariant of the live retry operation.

## Follow-Up

Make the chosen logical retry owner race cancellation against every backoff and
attempt setup, then regress this exact public fixture to `calls=1`.
