# F-REL-01 / V20 / Attempt 14

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean before and after execution

## Claim

All values representable by the public LLM retry-delay builder remain panic-free
and produce a typed stream failure.

## Method

Configure a public streaming Agent with one queued 429, one success,
`llm_max_retries=1`, and `llm_retry_delay_ms=u64::MAX`. Drain its public event
stream and record process status, runtime panic output, call count, events, and
typed stream errors.

## Expected Result

No panic; checked or saturating arithmetic yields either a bounded retry policy
or one typed configuration/runtime error visible to the caller.

## Result

- Exit code: 0
- Duration: 0.36 seconds
- Summary: a Tokio runtime thread panicked at
  `agent/react/run/retry.rs:33:28` with `attempt to add with overflow`, while the
  outer executable still exited successfully and printed
  `scenario=overflow calls=1 events=0 errors=[]`.
- Log/artifact: report-owned `artifacts/primary-agent-probe`; dedicated temp
  target `/private/tmp/f-rel-primary-agent-target`.

## Deviations

None. The apparent command success is itself part of the failed invariant: the
spawned execution panic was not surfaced as a typed stream error.

## Conclusion

Finding F-REL-01-P2-05 is independently reproduced. Public representable retry
configuration can panic a runtime task and collapse into a clean-looking empty
stream.

## Follow-Up

Validate or saturate retry inputs at construction, use checked delay arithmetic,
and convert spawned task failure into one typed terminal event/result.
