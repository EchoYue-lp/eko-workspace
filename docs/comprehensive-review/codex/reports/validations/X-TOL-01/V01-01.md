# X-TOL-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: dirty framework excluded through committed `git show HEAD:<path>`; CLI clean

## Claim

One invocation's name and arguments remain identical after intervention, hook,
and approval rewriting and in the GUI's durable Tool detail.

## Method

```text
1. In committed echo-agent HEAD, trace run_tools ToolCall emission through ToolExecutionContext, InterventionStage, PreToolUseHookStage, PermissionStage, ExecuteStage, trace, and returned AgentEvent.
2. In clean echo-agent-cli, trace AgentEvent::ToolCall through TauriChatSink to ToolExecutionRepository::start and ToolExecutionDetailManifest.args_full.
3. Compare requested name/input, ctx.tool_name/input/params, executed name/params, and persisted GUI fields.
No executable validation.
```

## Expected Result

The persisted execution record either contains the effective post-policy
invocation or explicitly stores both requested and effective values with rewrite
provenance.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not recorded
- Summary: `run_tools` emits `AgentEvent::ToolCall` with the model's original name/args before the pipeline (`src/agent/react/run/phases/tools.rs:63-81`). Intervention and PreToolUse can redirect/replace them (`run/pipeline.rs:90-120`, `:237-263`), and approval replaces only `ctx.params` (`:328-344`). Execute uses `ctx.tool_name` plus `ctx.params` (`:509-569`), while trace/audit/post-hook can still observe `ctx.input` (`:478-487`, `:438-449`, `:658-679`). Tauri persists the early ToolCall (`echo-agent-cli/src/tauri/commands/chat.rs:1193-1229`) and the repository exposes those original args (`echo-agent-app-core/src/tool_execution.rs:191-257`, `:394-410`). No later event carries the effective invocation.
- Log/artifact: this report

## Deviations

The generic schema-validation disconnect itself is already owned by
`F-EXT-01-P1-01`; this validation checks the separate cross-layer field-mapping
loss after a rewrite is allowed.

## Conclusion

The claim fails and supports `X-TOL-01-P1-01`. It does not establish how often
EKO hooks or approval modification are configured in practice.

## Follow-Up

Add a static/dynamic regression that rewrites both tool name and parameters and
asserts one requested/effective invocation identity across trace, persistence,
frontend detail, and actual Tool input.
