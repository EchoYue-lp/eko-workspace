# X-TOL-01 / V02 / Attempt 02

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: dirty framework excluded through committed blobs; CLI clean

## Claim

The live ReAct-to-EKO path forwards the rich terminal ToolResult (including
artifact metadata) before the application persists and pages complete output.

## Method

```text
1. Re-inspect committed ToolManager::execute_tool_stream_with_context handling of ToolStreamEvent::Complete.
2. Follow the returned ToolResult through ExecuteStage, TruncationStage, execute_tool_with_policy and run_tools AgentEvent::ToolResult.
3. Follow Tauri PendingToolCompletion and ToolExecutionRepository finish/read_output branches.
4. Compare with the canonical framework read_artifact cursor/hash implementation.
No executable validation.
```

## Expected Result

The exact rich terminal result processed by the framework reaches EKO, including
`truncated`, digest, byte counts, artifact identity, and final content source.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not recorded
- Summary: ToolManager consumes `ToolStreamEvent::Complete(result)` by returning it and forwards only Output/Progress (`echo-agent/echo-execution/src/tools.rs:823-845`). ExecuteStage receives that ToolResult; TruncationStage adds central spill metadata afterward (`src/agent/react/run/pipeline.rs:729-753`). `execute_tool_with_policy` then returns only a String (`src/agent/snapshot.rs:1229-1255`), and `run_tools` emits string-only `AgentEvent::ToolResult` (`src/agent/react/run/phases/tools.rs:273-314`, `:391-430`). Consequently, Tauri's `ToolStreamEvent::Complete` metadata capture (`echo-agent-cli/src/tauri/commands/chat.rs:1249-1261`) is unreachable through this live ReAct manager path and `finish` receives defaults (`:1264-1278`). A non-streaming centrally spilled result is persisted as a normal Result chunk containing only the preview/instruction, with `truncated=false`, no artifact descriptor, and preview-sized `output_bytes` (`echo-agent-app-core/src/tool_execution.rs:292-344`). If any Output chunk exists, it takes precedence and suppresses even that terminal String (`:260-330`, `:413-444`). The application's raw artifact fallback, when metadata is supplied by another adapter, still uses an unbound numeric cursor and no digest check (`:668-748`), unlike canonical `read_artifact` (`echo-agent/echo-tools/src/files/artifact.rs:280-405`).
- Log/artifact: this report

## Deviations

This attempt refines V02-01: it proves that, on the inspected live ReAct path,
artifact metadata is not merely shadowed after persistence; it never reaches
the application's Complete handler. V02-01 remains immutable and is superseded
for final conclusions by this attempt.

## Conclusion

The claim fails and supports `X-TOL-01-P1-02` and
`X-TOL-01-P2-03`. It does not duplicate the generic string-only projection
owner in `F-EXT-01`; it establishes the reachable EKO persistence/UI effect and
the dead adapter expectation.

## Follow-Up

After defining one typed terminal envelope, exercise non-streaming spill,
stream-plus-distinct-final-result, checksum mismatch, and artifact mutation
through the real ReAct and Tauri path.
