# X-TOL-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: dirty framework excluded through committed blobs; CLI clean

## Claim

Invalid arguments, Tool failure, timeout, user cancellation, parent failure,
and process interruption retain distinct terminal classifications and recovery
facts in EKO's durable Tool projection.

## Method

```text
1. Compare framework AgentEvent ToolResult/ToolError/Cancelled/Error and ToolFailure fields.
2. Trace direct-chat terminal handling in TauriChatSink.
3. Trace TaskRuntime Subagent ToolCompleted plus parent Completed/Failed/Cancelled/TimedOut handling.
4. Inspect ToolExecutionStatus, finish/cancel, boot recovery, durable TaskRuntime ToolFailed events, and frontend fallback classification.
No executable validation.
```

## Expected Result

Every announced invocation ends once with a reason-specific terminal and typed
recovery facts; missing per-call terminal facts remain `unknown/interrupted`,
not guessed as cancellation.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not recorded
- Summary: ToolFailure distinguishes category/recovery/side effect in the framework, but EKO's projection has only running/succeeded/failed/cancelled (`tool_execution.rs:54-61`). Direct-chat `AgentEvent::Error` and every non-running turn status cancel all active calls (`chat.rs:1325-1327`, `:1365-1368`). TaskRuntime maps parent Completed, Failed, Cancelled, and TimedOut alike to `cancel_active_tools` (`:1106-1132`). Repository restart recovery converts every lingering Running record to Cancelled (`tool_execution.rs:508-545`). By contrast, the durable TaskRuntime store retains ToolCompleted versus ToolFailed and failure facts (`tasks/task_runtime/store.rs:1999-2030`), showing that richer terminal classification already exists at one boundary but is not preserved for orphaned projections.
- Log/artifact: this report

## Deviations

The missing per-call batch timeout outcome is canonical
`F-RCT-04-P1-05`; this validation does not duplicate it. It identifies the EKO
adapter's additional inference that parent failure, timeout, completion, and
process interruption all mean per-call cancellation.

## Conclusion

The claim fails and supports `X-TOL-01-P1-04`. It does not prove the exact side
effect state of an interrupted Tool; that uncertainty is why the classification
must remain explicit.

## Follow-Up

Add fault scenarios for parent failed/timed_out/cancelled/completed with one
active possible-side-effect Tool and for restart with an unfinished record.
