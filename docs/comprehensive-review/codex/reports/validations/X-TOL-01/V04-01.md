# X-TOL-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: committed framework test blobs only; CLI clean

## Claim

Existing tests cover the cross-layer effective-argument, final-output,
artifact-integrity, and terminal-reason invariants reviewed by X-TOL-01.

## Method

```text
1. Inventory committed framework tests through committed blob/search evidence only.
2. Inventory CLI repository ToolExecutionRepository, Tauri projector, and frontend store tests.
3. Compare covered cases with X-TOL-01's field/error/output/cursor matrix.
No tests were executed.
```

## Expected Result

Tests distinguish requested/effective args, streamed/final output, verified
artifact identity, and cancelled/timed_out/failed/interrupted terminals.

## Result

- Exit code: not-applicable (static inventory)
- Duration: not recorded
- Summary: positive tests cover UTF-8 previews, JSONL paging, malformed final journal repair, conversation deletion, UTF-8 artifact page boundaries, persisted TaskRuntime detail, and terminal hydration. The repository test intentionally streams and finishes with the same `hello world`, so it cannot detect final-result omission. The Tauri projector test likewise uses `main output` for both chunk and result and expects every dangling call at parent completion to be cancelled (`chat.rs:1610-1721`). Frontend fixtures cover completed/failed merge order but not `ToolCompleted { success:false }`, timeout, unknown interruption, effective args, artifact mutation, checksum mismatch, or stream/artifact precedence.
- Log/artifact: this report

## Deviations

The accidental dirty-worktree `rg` is excluded under V00; this inventory uses
only clean CLI source and separately inspected committed framework anchors.

## Conclusion

The claim fails. The positive tests do not falsify any X-TOL-01 finding because
they use values that make the lossy branches observationally equivalent.

## Follow-Up

Implement V06 as focused regressions after the canonical contract is repaired.
