# X-TOL-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: dirty framework excluded through committed blobs; CLI clean

## Claim

The GUI's paged detail retains the terminal Tool result and complete artifact,
and verifies the artifact checksum/snapshot across cursor pages.

## Method

```text
1. Inspect committed ToolResult/ToolStreamEvent and central spill metadata production.
2. Inspect the committed canonical read_artifact SHA-256 and cursor identity checks.
3. Trace Tauri ToolStream Output/Complete and ToolResult into ToolExecutionRepository.
4. Inspect finish/read_output/read_artifact_page precedence and InlineToolCall pagination.
5. Inventory tests for stream chunk != final result, stream chunk + artifact, mutation between pages, and checksum mismatch.
No executable validation.
```

## Expected Result

Streaming chunks remain a live projection, the terminal result/artifact remains
the complete source, and every cursor is bound to immutable artifact identity
and expected SHA-256.

## Result

- Exit code: not-applicable (static inspection)
- Duration: not recorded
- Summary: the framework artifact carries path/bytes/SHA-256 (`echo-core/src/tools/artifact.rs:92-143`), and canonical `read_artifact` binds cursor to size/mtime/SHA and verifies first-page expected hash (`echo-tools/src/files/artifact.rs:16-55`, `:280-340`, `:375-405`). In EKO, any non-empty stream chunk sets `has_output`; `finish` then omits the terminal result even if distinct or backed by an artifact (`tool_execution.rs:260-330`). `read_output` always prefers that JSONL over `artifact_path` (`:413-444`), so one progress/output chunk can permanently shadow the complete terminal source. Its fallback artifact reader accepts a raw numeric offset, opens the raw path, and performs no SHA/file-identity check (`:668-748`). `InlineToolCall` trusts `complete` and concatenates returned pages (`InlineToolCall.tsx:61-83`, `:133-146`). Existing tests use identical streamed/final text and only verify UTF-8 page boundaries (`tool_execution.rs:851-887`, `:956-977`).
- Log/artifact: this report

## Deviations

Framework output/result metadata propagation has additional defects already
owned by `F-EXT-01`; this validation confines its new conclusion to the EKO
durable-detail adapter and duplicate reader.

## Conclusion

The claim fails and supports `X-TOL-01-P1-02` and
`X-TOL-01-P2-03`. Static inspection proves the branch behavior but not a
specific production artifact mutation frequency.

## Follow-Up

Run the V06 scenarios after remediation, including a partial stream plus a
different final result and an artifact changed between pages.
