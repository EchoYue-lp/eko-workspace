# X-TOL-01 / V00 / Attempt 01

> Status: inconclusive
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: `echo-agent` had external tracked/untracked changes across framework files; `echo-agent-cli` was clean. This validation wrote only Codex reports.

## Claim

All framework evidence used by X-TOL-01 can be isolated to committed `HEAD`
blobs while the CLI can be read from its clean worktree.

## Method

```text
1. Read TASKS.md, REPORTING.md, codex/README.md and the authorized Codex dependency reports.
2. Record both repository HEADs and status.
3. Inspect framework source only with `git show HEAD:<path>` or `git grep ... HEAD`.
4. Inspect the clean CLI worktree with `nl`/`sed`/`rg`.
5. Disclosure: one test-inventory command accidentally searched dirty framework bodies:
   rg -n -C 4 'updated_input|modified_args|PreToolUse|approval.*modified|artifact_sha256|stream.*artifact|distinct.*result' echo-agent/src echo-agent/echo-* echo-agent-cli/echo-agent-app-core/src/tool_execution.rs echo-agent-cli/src/tauri/commands/chat.rs --glob '*test*' --glob '*.rs'
   Treat its complete output as excluded and do not use it for conclusions.
Working directory: /Users/ls/MyWork/code/ylp_agent_learn/lp-agent
No build, test, fixture, or network execution.
```

## Expected Result

The committed framework and clean application source are independently
inspectable without adopting any concurrent dirty-body observation.

## Result

- Exit code: `0` for HEAD/status and committed-source inspections; `0` for the accidental `rg` whose result is excluded
- Duration: not recorded
- Summary: commit identities were stable; CLI was clean. The accidental command crossed the intended framework boundary, so this attempt is conservatively inconclusive despite the adopted evidence being reconstructable from committed blobs.
- Log/artifact: this report

## Deviations

The exact accidental command above read current dirty framework bodies. Its
output was not used. All framework anchors cited by the task report were
separately read with committed-blob commands.

## Conclusion

This attempt documents the isolation breach and the exclusion boundary. It does
not support a finding. V01-V04 contain only adopted evidence from committed
framework blobs and clean CLI source.

## Follow-Up

Primary reviewer must independently reconstruct every framework anchor from
`3aa7929`; do not use the accidental `rg` output.
