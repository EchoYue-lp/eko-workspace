# F-API-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The bilingual EventBus guide maps field and method names to the current public
transport source.

## Method

Compare `docs/{en,zh}/28-config-reference.md` EventBus blocks with
`src/event_bus.rs` and `echo-core/src/agent/event_envelope.rs`, including
parameter and receiver item types.

## Expected Result

The guide uses `EventEnvelope`, constructs required identity, sends the accepted
type and reads existing envelope fields.

## Result

- Exit code: 0
- Duration: under one second
- Summary: current `EventBus::send` takes `EventEnvelope` and subscriptions
  yield `Arc<EventEnvelope>`. There is no `BusEvent` or `send_for_run`; the guide
  sends `AgentEvent` and reads the removed `event` wrapper field.
- Log/artifact: none

## Deviations

The source comparison does not prove that the global bus receives live events;
that separate reachability defect belongs to F-CORE-01.

## Conclusion

The guide-to-source invariant fails and supports F-API-01-P2-01.

## Follow-Up

V04 compiles the exact public consumer.
