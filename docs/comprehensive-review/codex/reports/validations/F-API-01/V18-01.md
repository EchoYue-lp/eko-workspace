# F-API-01 / V18 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean; fixture under `/private/tmp`

## Claim

The facade's public `CoreDefaultAgentFactory` can perform the capability implied
by its concrete default-factory name.

## Method

Create `/private/tmp/f-api-factory.B5ibpg`, invoke `create_agent` with public
default config, and run it using the existing F-API target cache.

## Expected Result

The concrete implementation either constructs an agent or is absent from the
public surface.

## Result

- Exit code: 0
- Duration: approximately 20 seconds with shared target cache
- Summary: the program printed a deterministic error saying the method must be
  called from the facade crate and callers must use the other
  `DefaultAgentFactory`. No agent was constructed.
- Log/artifact: fixture `/private/tmp/f-api-factory.B5ibpg`

## Deviations

Process success means the fixture handled and printed the expected error; the
checked API invariant failed.

## Conclusion

The public core factory is a guaranteed-failure implementation and supports
F-API-01-P2-06.

## Follow-Up

V18-02 repeats the behavior from the unchanged fixture.
