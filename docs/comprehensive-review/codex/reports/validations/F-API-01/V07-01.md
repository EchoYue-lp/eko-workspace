# F-API-01 / V07 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Bilingual facade documentation avoids undeclared imports from independently
published split crates.

## Method

Count direct split-crate imports and the unique guide files containing them.

```text
rg -o 'use echo_(core|tools|state|integration|orchestration|execution)::' docs/en docs/zh \
  | sed 's/.*use //' | sort | uniq -c
rg -l 'use echo_(core|tools|state|integration|orchestration|execution)::' docs/en docs/zh | wc -l
```

## Expected Result

Zero undeclared direct imports, or explicit per-page dependency instructions.

## Result

- Exit code: 0
- Duration: under one second
- Summary: 62 imports occur in 22 files: `echo_core` 24, `echo_tools` 16,
  `echo_state` 12, `echo_integration` 4, `echo_orchestration` 4, and
  `echo_execution` 2. The getting-started install declares only `echo-agent`.
- Log/artifact: none

## Deviations

Some advanced pages intentionally discuss split APIs; the defect is the absent
dependency declaration, not use of split crates itself.

## Conclusion

The dependency contract is inconsistent and supports F-API-01-P2-03.

## Follow-Up

V08 compiles one exact facade-only example.
