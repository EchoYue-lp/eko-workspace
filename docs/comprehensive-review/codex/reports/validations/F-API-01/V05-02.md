# F-API-01 / V05 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

The bilingual context chapter's public fields and methods match the current
facade context types.

## Method

Compare both `40-context-system.md` files against `src/context/mod.rs`,
`src/context/selector.rs`, `src/lib.rs`, `src/llm.rs`, and
`echo-core/src/llm/types.rs` using field/method searches and source inspection.

## Expected Result

The documented message import, budget/source fields, `assemble` receiver and
selector method all exist as written.

## Result

- Exit code: 0
- Duration: under one second
- Summary: `Message` is public through `llm::types`/prelude but private at
  `echo_agent::llm::Message`; two budget and three source fields are absent;
  `assemble` consumes `ContextSources`; selector exposes `score_files` and
  `select_relevant`, not `select_files`. Both languages repeat the stale model.
- Log/artifact: none

## Deviations

The command succeeds as an inspection, but the checked documentation invariant
fails.

## Conclusion

This supports F-API-01-P2-02.

## Follow-Up

V06 compiles a representative public example.
