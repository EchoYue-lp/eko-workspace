# F-API-01 / V02 / Attempt 02

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

README and bilingual framework guides consistently describe symbols reachable
through the documented `echo_agent` dependency and its curated prelude.

## Method

Search only existing README, bilingual docs and inline facade/prelude source
for the advertised prelude, EventBus, context and direct split-crate imports;
then inspect the matching export blocks.

```text
rg -n 'All 67 built-in tools|prelude::\*|BusEvent|send_for_run|ContextBudget|ContextSources|select_files|echo_(core|tools|state|integration|orchestration|execution)::' \
  README.md docs/en docs/zh src/lib.rs
sed -n '130,220p' src/lib.rs
```

## Expected Result

Named facade/prelude APIs exist, and any split-crate import has an explicit
dependency instruction.

## Result

- Exit code: 0
- Duration: under one second
- Summary: README claims all 67 tools are available through prelude, while the
  prelude exports Think and four optional web/media concrete types. Both
  languages retain removed EventBus/context names. Numerous guides directly
  import independently published split crates despite the facade-only install
  instruction.
- Log/artifact: none

## Deviations

This is a static consistency inventory; V04, V06, V08 and V09 provide external
compiler behavior.

## Conclusion

The documented facade contract fails static consistency. This supports
F-API-01-P2-01 through P2-04.

## Follow-Up

Compile representative exact consumers in V04, V06, V08 and V09.
