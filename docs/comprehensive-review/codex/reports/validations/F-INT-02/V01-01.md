# F-INT-02 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories were clean at source capture

## Claim

LSP, channels, and A2A have explicit feature/export boundaries, while the IM channel framework has a real EKO entry and does not depend on EKO-only policy.

## Method

Static definition-to-export-to-registration search across both source repositories; compare root and integration Cargo features, root facade exports, examples, EKO feature-gated startup, and the channel handler construction path.

## Expected Result

Reusable protocol mechanisms remain in the framework; EKO chooses configured channels and supplies its product handler without duplicating transport logic.

## Result

- Exit code: 0
- `echo_integration` exposes independent `lsp` and `channels` features; the root crate exposes independent `a2a`, `lsp`, and `channels` features and facades.
- QQ and Feishu implement the common `ChannelPlugin` contract under `echo-integration`; the root facade re-exports them and adds only a React adapter.
- EKO `run_channels_mode` registers enabled QQ/Feishu plugins, constructs a per-channel `SessionHandler`, and injects `AppChannelMessageHandler`; it is therefore a real runtime consumer, not example-only code.
- No local-user interaction is gated by an agent automation permission mode in this path.

## Deviations

Reachability is real, but later validations show lifecycle, identity, and delivery contracts are not reliable.

## Conclusion

The feature and layering topology passes. Channel behavior does not.

## Future Validation

No build or feature matrix was run by explicit review-stage instruction. A future feature-isolation check should cover root `channels`, `lsp`, and `a2a` independently.
