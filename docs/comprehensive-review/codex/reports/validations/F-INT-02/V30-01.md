# F-INT-02 / V30 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex primary reviewer
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

F-INT-02's eight channel findings are source-conclusive, correctly separated
from F-INT-01's LSP/A2A ownership, and still present at the current framework
commit.

## Method

The primary reviewer independently sampled the current channel manager, unified
message types, SessionHandler dispatch, QQ and Feishu plugin lifecycles, Feishu
Webhook and long-poll acknowledgement paths, token arithmetic, and EKO's live
channel composition. The scoped `9b0e0faf..3aa79299` change set was checked for
overlap with these paths, followed by task-link, finding-ID, HEAD and clean-source
checks.

No Cargo, rustc, test, build, fixture, timing or network command was run.

## Expected Result

Each finding has a current source anchor and live or public reachability; no
finding duplicates F-INT-01; the external test-credibility commit does not alter
the reviewed channel behavior.

## Result

- Exit code: 0 for static source and integrity checks.
- `ChannelManager` returns positional start results without IDs, silently
  replaces duplicate registrations, discards stop errors, and its documented
  auto-stop Drop only logs.
- Canonical inbound messages contain `chat_id`, but SessionHandler and EKO
  conversation/cache keys use only `(channel_id, sender_id)`; reset and streaming
  replies likewise target `sender_id` for group messages.
- Session locks end before Agent handling or stream consumption, so reset/timeout
  may replace and notify while an old handler remains active; idle entries have
  no autonomous expiry owner.
- QQ discards the send-task handle and its `loop { if let Some(...) }` spins after
  queue closure. Feishu likewise detaches send/event work while stop owns only the
  top receive/server task.
- Webhook HMAC consumes reserialized JSON rather than raw request bytes. Webhook
  and long-poll acknowledge/deduplicate before detached processing has a
  recoverable terminal.
- Long-poll allocates `vec![None; sum as usize]` from peer-controlled fragment
  count without a count or aggregate-byte bound.
- QQ/Feishu `send` success means only queue admission; later token/HTTP failure is
  log-only. Token refresh and public session timeout conversions retain unchecked
  subtraction/addition/multiplication.
- The current framework commit changes only ReAct test-credibility paths, not the
  integration/channel paths above. Both source repositories were clean at final
  capture.
- Static summary: `findings=8 unique=8 current_anchors=1 live_or_public=1
  lsp_a2a_dedup=1 source_clean=1 links_ok=1`.

## Deviations

Dynamic protocol fixtures and platform calls remain intentionally not run. Their
absence does not weaken the source-conclusive lifecycle, identity, allocation and
arithmetic defects; they remain future regression evidence.

## Conclusion

Passed. F-INT-02-P1-01 through P1-07 and F-INT-02-P2-01 are accepted at the
current commit, and the task is promoted to `complete`.

## Follow-Up

Fix lifecycle ownership and conversation identity before adapter-specific retry
polish. Keep permission policy out of this work: the required changes are local
correctness, delivery truth, bounded input and cleanup, not cloud-style access
control.
