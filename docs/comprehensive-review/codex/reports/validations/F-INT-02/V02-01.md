# F-INT-02 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: source clean at capture; later unrelated external modifications appeared in two `echo-agent/src/testing` files and were not read or changed

## Claim

`ChannelManager` preserves plugin identity and owns startup, partial failure, shutdown, replacement, and drop cleanup truthfully.

## Method

Static control-flow inspection of `echo-agent/echo-integration/src/channels/manager.rs` and the live EKO caller in `echo-agent-cli/src/cli/modes.rs`.

## Expected Result

Every lifecycle outcome remains keyed by channel ID; partial startup has an explicit state/rollback policy; shutdown errors reach the caller; replacement cannot orphan a running plugin; documentation matches Drop behavior.

## Result

- Exit code: 0
- `register` uses `HashMap::insert` and discards any replaced plugin without stopping it (`manager.rs:33-38`).
- `start_all` iterates a `HashMap`, returns an unkeyed `Vec<Result<()>>`, starts sequentially despite promising concurrent startup, and leaves partial successes running (`manager.rs:47-74`).
- EKO can only count failures, cannot name them, then logs that all channels started (`cli/modes.rs:217-229`).
- `stop_all` logs every plugin error but always returns `Ok(())` (`manager.rs:95-108`).
- Type documentation promises auto-stop on Drop, but Drop only logs advice (`manager.rs:11-13,133-143`).

## Conclusion

Failed. The manager has no authoritative per-plugin lifecycle state and can report successful cleanup after failure.

## Future Validation

Use fake plugins to exercise duplicate registration, deterministic ID-correlated start results, partial start failure, stop failure, double start, and Drop after start. Not run by instruction.
