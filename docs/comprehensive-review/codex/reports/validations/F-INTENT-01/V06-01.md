# F-INTENT-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex primary reviewer
> `echo-agent` commit: 9b0e0faf74d35c9a432370b923acabfbb5f32d63
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: source read-only

## Claim

The live intent decision catalog follows skill install/unload/reload changes.

## Method

Compare EKO startup construction, router ownership, primary/pool descriptor
refresh, skill registry updates, and all `set_intent_router` callers.

## Expected Result

One catalog revision drives descriptor/tool availability and classification for
primary and pooled agents after live skill changes.

## Result

- Exit code: not-applicable (static inspection).
- Startup clones descriptors into KeywordClassifier and LlmIntentClassifier,
  then installs an immutable router.
- Live refresh updates pool descriptor registries only; no production caller
  rebuilds the primary or pooled intent router/classifier.
- Pooled agent construction registers descriptors but does not install the EKO
  TriggerSupervisor router at all.

## Deviations

This validation scopes the framework contract using its real EKO consumer; the
specific product refresh policy remains application-owned.

## Conclusion

Failed. Supports F-INTENT-01-P1-05.

## Follow-Up

Bind decisions to the same source-scoped catalog snapshot/revision used by skill
activation, and apply the same routing capability to all mode-equivalent agents.
