# A-CHAT-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

`drive_chat` returns an application outcome that agrees with the one canonical
Agent terminal observed by its sink.

## Method

```text
Build a static state table for stream setup error, FinalAnswer, Cancelled,
stream Error, raw EOF, sink close, and adapter error. Trace envelope terminal
normalization, drive_chat_inner return, Task-mode finalization, and GUI status.
```

## Expected Result

Success, failure, and cancellation remain typed through the return value; no
caller infers completion from successful event-loop plumbing.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: envelope Error/EOF normalization yields an Agent Error then ends the
  stream. `drive_chat_inner` nevertheless returns `Ok(())`; GUI maps that to
  `TurnStatus(completed)` and Done. In Task mode the same driver postlude may
  mark the formal run failed, so sink, status, and TaskRun disagree.
- Log/artifact: this report

## Deviations

Framework causes of malformed/missing terminals remain owned by F-RCT-02 and
F-RCT-03. This validation covers only EKO's outcome collapse.

## Conclusion

Application terminal truth is not preserved. Supports A-CHAT-01-P1-01.

## Follow-Up

Introduce one typed application outcome and table-test all terminal rows after
the framework terminal contract is corrected.
