# A-CHAT-01 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Each ChatSink only transports/renders canonical facts and does not condition
delivery of a framework event on unrelated application persistence.

## Method

```text
Compare ChannelChatSink, TuiChatSink, CLI/channel consumers, and TauriChatSink
event by event. Trace tool Call/Stream/Result/Error through GUI persistence.
```

## Expected Result

Persistence failure is an additional typed fact; it cannot erase or rewrite the
underlying tool event.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: TauriChatSink performs synchronous tool repository start/append/
  finish/cancel. Start/finish failure returns `Some(true)` before canonical GUI
  emission, suppressing the tool event. Other sinks project the event without
  this persistence prerequisite.
- Log/artifact: this report

## Deviations

The TaskRuntime execution projector has similar persistence responsibilities,
but this validation classifies only the primary chat tool path.

## Conclusion

The sink responsibility invariant fails and supports A-CHAT-01-P1-05.

## Follow-Up

Regression must fault each persistence operation while asserting the exact
tool trajectory remains visible plus an explicit projection-error fact.
