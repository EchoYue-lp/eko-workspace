# A-CHAT-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

All surfaces preserve the complete final-answer payload independently of Token
chunk shape.

## Method

```text
Compare AgentEvent::FinalAnswer mapping in GUI, TUI, CLI, and channel sinks.
Trace the live input-guard terminal, which emits FinalAnswer without Token.
```

## Expected Result

FinalAnswer(data) is the authoritative committed answer, while Token is an
optional/provisional rendering optimization.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: GUI forwards `data` to the frontend finalizer. TUI names it
  `_answer`, CLI ignores it, and channel only flushes buffered Tokens. A blocked
  input guard emits a non-empty FinalAnswer with no Token, so those three
  surfaces produce no assistant response.
- Log/artifact: this report

## Deviations

F-RCT-03 owns provisional/draft Token semantics; this is the separate EKO loss
of the committed terminal payload.

## Conclusion

Final answer projection is not lossless or surface-equivalent. Supports
A-CHAT-01-P1-03.

## Follow-Up

Replay non-empty FinalAnswer with zero, partial, duplicate, and full Token
streams through every sink; assert one exact committed answer.
