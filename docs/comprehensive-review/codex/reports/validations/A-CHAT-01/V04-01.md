# A-CHAT-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Every failure before or during stream setup emits one terminal product event so
all surfaces leave their running state and show an error.

## Method

```text
Trace errors from ensure_task_mode_run, cancellation registration,
PreparedUserTurn::to_message, stream setup, and stream iteration through each
caller and sink.
```

## Expected Result

No caller needs to infer or synthesize terminal state; an error is visible and
releases its surface state everywhere.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: stream setup failure emits Agent Error. Earlier driver failures do
  not. GUI caller synthesizes failed TurnStatus, but TUI only logs and remains
  processing, CLI only logs the joined driver error, and channel closes with no
  error message. `ExecutionPath` may still be emitted after the error result.
- Log/artifact: this report

## Deviations

A-INP-01-P1-02 supplies a concrete reachable `to_message` read failure; this
report owns the chat lifecycle impact, not attachment identity.

## Conclusion

Pre-stream failure semantics violate surface parity and support
A-CHAT-01-P1-02.

## Follow-Up

Fault every pre-stream phase across all four callers and assert one visible
failed terminal plus released/queued surface state.
