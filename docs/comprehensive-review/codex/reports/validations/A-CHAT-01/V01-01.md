# A-CHAT-01 / V01 / Attempt 01

> Status: passed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

One EKO application driver is reachable from GUI, TUI, CLI REPL, and IM
channels; other helpers are projections rather than duplicate ReAct drivers.

## Method

```text
Search both repositories for drive_chat, execute_stream_message, ChatSink,
ChatDriverEvent, and direct Agent stream consumption. Trace every production
caller through PreparedUserTurn and ChatResources.
```

## Expected Result

Interactive entries converge on one application lifecycle owner.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: GUI regular, TUI regular, CLI REPL, and IM channels call the same
  `echo-agent-app-core::chat_driver::drive_chat`. GUI/TUI steer correctly uses
  framework steer directly and is not a second chat run (A-INP-01 owns the live
  TUI steer preparation bypass). No parallel EKO ReAct driver was found.
- Log/artifact: this report

## Deviations

Lifecycle terminal ownership is still split between driver, caller, and sink;
that behavioral defect is covered by later validations.

## Conclusion

Definition and reachability converge structurally. This does not establish
correct terminal semantics.

## Follow-Up

Primary reviewer should independently sample the four production call paths.
