# A-CHAT-01 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: `3aa7929928442aab91e4dce9c426d909a5f0a1ab`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

TUI releases all active-turn state and advances queued input consistently for
FinalAnswer, Cancelled, and Error only after the shared driver is complete.

## Method

```text
Compare TUI handlers for FinalAnswer, Cancelled, Error, TurnStatus, and
ExecutionPath with `drive_chat` postlude ordering and dispatch_next_queued.
```

## Expected Result

One application completion signal clears cancel/turn identity and advances the
queue once after TaskRun finalization and execution-path publication.

## Result

- Exit code: 0
- Duration: static inspection; not timed
- Summary: FinalAnswer calls `finalize_stream` and immediately dispatches the
  next queued turn even though `drive_chat` still finalizes Task mode and emits
  ExecutionPath after the Agent terminal. Error clears state and advances the
  queue. Cancelled clears `active_cancel` but leaves `active_turn_id` and queued
  turns untouched. TUI production callers never emit TurnStatus themselves.
- Log/artifact: this report

## Deviations

Framework post-terminal continuation is owned by F-RCT-02; this result covers
TUI's application-state handling even for a correct single terminal.

## Conclusion

TUI has three inconsistent lifecycle authorities. Supports A-CHAT-01-P1-04.

## Follow-Up

Use one driver-complete outcome to advance queues and clear state; regression
all terminal variants with queued work and Task mode postlude.
