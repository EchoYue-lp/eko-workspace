# F-MEM-02 / V02 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

SqliteStore schema initialization is fail-closed and establishes every column
later queries depend on before returning a usable store.

## Method

Trace `open -> init_tables`, each DDL/migration error branch, and every later SQL
reference to importance/last_accessed/expires_at. Inspect schema-versioning and
migration tests.

```text
Inspect echo-state/src/memory/sqlite_store.rs:81-184 and all later references to
the three ALTER-added columns; search for user_version/schema_version.
```

Environment: static control/data-flow inspection; no database fixture executed.

## Expected Result

Unexpected DDL/migration errors abort opening; a versioned migration proves the
postcondition expected by subsequent writes/maintenance.

## Result

- Exit code: 0 for inspection; claim failed
- Duration: not recorded
- Summary: `CREATE TABLE`/FTS/vector failures propagate, but every non-duplicate
  `ALTER TABLE` error only logs a warning and initialization continues. There is
  no schema/user version table. Later puts write `importance` and `expires_at`,
  so a store can be returned successfully with a schema that immediately fails
  live mutations. No malformed/partial/old-schema test exists.
- Log/artifact: this report

## Deviations

The default fresh schema path likely adds all columns normally; the defect is
recovery/upgrade error handling, not fresh-create SQL syntax.

## Conclusion

This supports F-MEM-02-P1-01. It does not select a compatibility policy; the
project may replace old schemas instead of migrating them.

## Follow-Up

Future regression: inject each DDL failure and partial schema; open must fail
closed or atomically recreate according to one explicit policy.
