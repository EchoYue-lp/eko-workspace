# F-MEM-02 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

SqliteStore search preserves relevance order, surfaces corrupt rows, honors
limits, and produces finite meaningful numeric scores.

## Method

Trace keyword/LIKE/semantic/hybrid result order from SQL/scoring through batch
fetch, and inspect row-error/JSON-error/numeric edge branches.

```text
Inspect sqlite_store.rs:221-251, :254-457, :608-707, :785-907; model ordered
key lists entering SELECT ... IN and NaN/Inf/zero/dimension-mismatch vectors.
```

Environment: static query/numeric inspection; no executable vector fixture.

## Expected Result

Results remain relevance-sorted, limit zero returns empty, corrupt rows return a
typed error, and non-finite/malformed vectors cannot enter or poison ranking.

## Result

- Exit code: 0 for inspection; claim failed
- Duration: not recorded
- Summary: FTS/semantic code computes ordered keys, then `fetch_items*` retrieves
  them with SQL `IN` and no ORDER BY; row order is unspecified and not restored.
  FTS scores are attached by key, but returned order can violate Store's
  relevance contract. LIKE fallback uses `max(1)`, so limit zero can still query
  and return one item. Batch fetch uses `rows.flatten()` and silently skips
  row-conversion errors; JSON parse errors are silently dropped. `list` converts
  corrupt JSON to `Null` and drops row errors. Vector decode ignores trailing
  bytes, non-finite f32 values are accepted, cosine can produce NaN/Inf, and
  partial_cmp then treats them as equal rather than rejecting corruption.
- Log/artifact: this report

## Deviations

The happy-path tests establish simple FTS/vector hits but do not assert multi-hit
order, corrupt data, limit zero, malformed blobs, NaN/Inf, or dimension drift.

## Conclusion

This supports F-MEM-02-P1-04 and F-MEM-02-P2-02.

## Follow-Up

Future regressions: exact multi-hit order, limit 0/MAX, corrupt JSON/row types,
blob length modulo four, NaN/Inf/zero vectors, and embedding dimension changes.
