# F-MEM-02 / V08 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

Existing source tests cover SQLite Store and ConversationStore concurrency,
schema failure, search/numeric, namespace parity, error, and identity cases.

## Method

Inventory every existing test function in both SQLite memory modules and map
test bodies to the task's invariant matrix. Do not execute them.

```text
rg -n '^\s*#\[(tokio::)?test\]|^\s*(async )?fn test_' \
  echo-state/src/memory/sqlite_{store,conversation}.rs
```

Environment: static test-source inventory; no test binary executed.

## Expected Result

Both implementations have regression coverage for their public contracts and
all material failure/concurrency/numeric cases.

## Result

- Exit code: 0 for inventory; claim failed
- Duration: not recorded
- Summary: SqliteStore has 15 happy-path tests for CRUD, FTS/CJK, reopen,
  simple namespaces, missing embedder, embeddings, vector serialization/cosine,
  and text extraction. It lacks concurrent writers, partial schema/migration,
  injected errors, stale secondary indexes, pruning, order, limit zero,
  corrupt rows/JSON/blobs, non-finite vectors, dimension changes, and lossless
  namespaces. SqliteConversationStore has no test module at all.
- Log/artifact: this report

## Deviations

This inventory does not claim existing tests pass; review policy forbids test
execution.

## Conclusion

SQLite persistence has a major regression-coverage gap, especially the entire
conversation backend. This supports every finding's future validation but is not
itself a runtime defect.

## Follow-Up

Implement each named regression separately and execute under Q-FW-01/Q-FW-02.
