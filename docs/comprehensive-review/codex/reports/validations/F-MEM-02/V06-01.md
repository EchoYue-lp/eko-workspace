# F-MEM-02 / V06 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

SqliteConversationStore implements ConversationStore pagination, ensure,
message identity, ordering, and cascade semantics coherently.

## Method

Compare every ConversationStore method with the SQLite SQL and F-MEM-01's
canonical default-contract findings. Trace list query construction for all four
limit/offset combinations and message replacement with supplied IDs.

```text
Inspect sqlite_conversation.rs:62-465 against
echo-core/src/memory/conversation.rs:93-205; enumerate filter and message-ID
state transitions.
```

Environment: static contract/SQL inspection; no database fixture.

## Expected Result

All valid filter combinations produce valid SQL; ensure is atomic for this
concurrent backend; supplied message identity has explicit semantics; deletes
cascade and reads are ID-ordered.

## Result

- Exit code: 0 for inspection; claim failed
- Duration: not recorded
- Summary: foreign keys are enabled and conversation deletion cascades;
  get_messages orders by database ID. But offset without limit emits `... ORDER
  BY ... OFFSET ?`, invalid SQLite syntax. The backend inherits the trait's
  non-atomic get-then-create ensure instead of using `INSERT ... ON CONFLICT`.
  `save_messages` ignores every caller-supplied StoredMessage ID and generates
  new auto-increment IDs after deleting the old transcript, so
  `compressed_before_id` and external message references can become stale after
  each replacement. These default/identity concerns are not covered by tests.
- Log/artifact: this report

## Deviations

The current framework projection sets message IDs to None, so supplied-ID loss
mainly affects independent import/restore consumers. F-MEM-01 already owns the
generic default ensure defect; this report records SQLite's failure to override
it without creating a duplicate canonical finding.

## Conclusion

This supports F-MEM-02-P1-05 and links the inherited ensure problem back to
F-MEM-01-P2-01.

## Follow-Up

Future regression: offset-only/all filter combinations, concurrent same-ID
ensure, and replace messages while preserving/validating compression boundary.
