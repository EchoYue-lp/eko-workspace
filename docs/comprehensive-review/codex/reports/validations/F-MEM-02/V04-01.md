# F-MEM-02 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-12
> Executor: Codex review subagent
> `echo-agent` commit: `9b0e0faf74d35c9a432370b923acabfbb5f32d63`
> `echo-agent-cli` commit: `b3b2e81f2b2d9fdb319ec604a561beec5f66fea5`
> Worktree state: both source repositories clean

## Claim

SqliteStore keeps the main row, FTS row, and vector row transactionally aligned
across put, overwrite, delete, and prune.

## Method

Construct a state-transition table for main/FTS/vector tables from the SQL in
`put`, `delete`, and `prune_expired`, including embedder success/failure on an
existing vectorized key.

```text
Inspect sqlite_store.rs:464-560, :710-745, and :909-927; enumerate table state
after each branch and all ignored SQL errors.
```

Environment: static transaction/secondary-index inspection; no fixture.

## Expected Result

After every successful mutation, secondary indexes describe exactly the current
main row; maintenance deletes every dependent index row; any cleanup failure
rolls back the operation.

## Result

- Exit code: 0 for inspection; claim failed
- Duration: not recorded
- Summary: successful embedding updates all three tables transactionally.
  However, overwriting an already-vectorized key when embedding fails updates
  main+FTS but leaves the previous vector row, so semantic search returns the
  new value ranked by the old content. `prune_expired` deletes only `store_items`,
  leaving FTS/vector rows. `delete` ignores vector-table delete errors while
  committing main+FTS deletion. The store has no foreign-key cascades tying
  secondary rows to the main row.
- Log/artifact: this report

## Deviations

Semantic fetch filters orphan vector keys through the main table, so a pruned
orphan may be invisible rather than returned; it still consumes candidate
budget/storage and FTS prune orphans remain searchable candidates before fetch.

## Conclusion

This supports F-MEM-02-P1-03. It does not require embeddings to be mandatory;
failure may degrade to keyword-only if stale vectors are removed.

## Follow-Up

Future regression: overwrite with embed failure, expiration prune, and injected
vector-delete failure; assert all table states or whole transaction rollback.
