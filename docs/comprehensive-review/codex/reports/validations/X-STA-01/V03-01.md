# X-STA-01 / V03 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Missing, corrupt, and truncated persistent records are distinguishable and do
not destroy or conceal the last recovery evidence.

## Method

```text
1. Compare FileStore, FileConversationStore, RuntimeStateStore restore, and TaskRuntime file reads.
2. Inspect atomic-write and append durability boundaries.
3. Build a static admission matrix for missing, malformed full file, and partial JSONL tail.
```

## Expected Result

Missing may initialize empty; corruption must fail closed/quarantine; a partial
append tail must preserve the valid prefix and never make the full run opaque.

## Result

- Exit code: 1
- Duration: static inspection
- Summary: FileConversationStore correctly distinguishes missing from corrupt
  and atomically renames full records. In contrast, FileStore maps JSON decode
  failure to an empty map that a later flush overwrites, and ReAct checkpoint
  restore resets messages on load error. TaskRuntime full snapshots fail closed,
  but `read_events` parses every nonempty line, so a nonempty crash-truncated tail
  returns an error for the entire history despite a comment claiming the tail is
  tolerated.
- Log/artifact: this report

## Deviations

No malformed files were created because dynamic fixtures were prohibited.

## Conclusion

Supports `X-STA-01-P0-01` and `X-STA-01-P1-02`; the sound
FileConversationStore behavior is the local pattern to reuse.

## Follow-Up

Add corrupt-file quarantine and valid-prefix/partial-tail recovery regressions.
