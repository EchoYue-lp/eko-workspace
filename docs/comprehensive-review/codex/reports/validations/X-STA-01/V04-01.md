# X-STA-01 / V04 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Every crash point restores a whole identity aggregate or leaves the pre-recovery
state unchanged.

## Method

```text
1. Order framework checkpoint field application and required-safe-point writes.
2. Order TaskRuntime boot recovery: note, run transition, task reset, blocker write.
3. Order GUI transcript fetch, backend Agent restore, frontend replace, and autosave/finalization.
4. Mark each write boundary where process death/error can expose an intermediate generation.
```

## Expected Result

Recovery validates/stages all facts before publishing one committed generation,
and retries can identify whether recovery already completed.

## Result

- Exit code: 1
- Duration: static state-table inspection
- Summary: framework checkpoint restore is field-wise/additive and checkpoint
  saves are warning-only; GUI has distinct durable transcript, pooled Agent, and
  React projections without a shared revision; TaskRuntime persists `Paused`
  before resetting every Running task and writing recovery blockers. An error or
  second crash therefore exposes hybrid generations and can strand claims.
- Log/artifact: this report

## Deviations

Atomic defects already owned by dependencies were not assigned duplicate atomic
IDs here; this validation supports the cross-aggregate root finding.

## Conclusion

Supports `X-STA-01-P1-03` and backlinks to `F-RCT-05-P1-02/P1-05`,
`A-STATE-01-P0-01/P1-03/P1-04`, and `A-TSK-04-P1-02`.

## Follow-Up

Introduce staged recovery receipts/generations and kill-point regressions.
