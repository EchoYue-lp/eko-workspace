# X-STA-01 / V05 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Deleting a conversation cascades through checkpoint, TaskRun, Subagent, tool,
artifact, input, and frontend projections with one durable tombstone.

## Method

```text
1. Trace GUI delete_conversation and TUI /delete-session side effects.
2. Search TaskRuntimeStore for delete-by-run/delete-by-conversation APIs.
3. Trace RuntimeArtifact persistence and retention metadata.
4. Trace frontend active conversation and TaskRuntime store cleanup.
```

## Expected Result

Deletion first fences late writers, records a generation/tombstone, then either
atomically removes or durably schedules every owned record and body.

## Result

- Exit code: 1
- Duration: static inspection
- Summary: GUI deletes the conversation record, tool execution details, tool
  artifacts, and staged user input; TUI omits tool execution detail removal.
  Neither path deletes TaskRuntime run directories/events/artifact facts or the
  framework runtime checkpoint. TaskRuntime exposes list/add but no delete
  operation; artifact retention is free-form metadata. Frontend clears chat/tool
  state but not its TaskRuntime/Subagent stores in this delete path. No tombstone
  or conversation generation fences late writers.
- Log/artifact: this report

## Deviations

Active-turn resurrection itself remains owned by `A-STATE-01-P1-05`.

## Conclusion

Supports `X-STA-01-P1-05`, a broader cascade/retention defect rather than a
duplicate of the active-writer race.

## Follow-Up

Specify canonical ownership and an idempotent cascade with tombstone tests.
