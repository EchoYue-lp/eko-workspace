# X-STA-01 / V01 / Attempt 01

> Status: failed
> Validation date: 2026-08-13
> Executor: Codex review subagent
> `echo-agent` commit: 3aa7929928442aab91e4dce9c426d909a5f0a1ab
> `echo-agent-cli` commit: b3b2e81f2b2d9fdb319ec604a561beec5f66fea5
> Worktree state: framework externally dirty, committed blobs only; CLI `Cargo.lock` excluded

## Claim

Conversation, checkpoint, Task, Subagent, artifact, and frontend identities form
one lossless, generation-aware join tuple across persistence and restart.

## Method

```text
1. Enumerate each persisted identity definition and its storage key.
2. Trace joins from conversation_id -> run_id -> task_id -> execution_id -> artifact_id.
3. Compare typed fields retained by backend stores and frontend selectors.
4. Search both repositories for parallel authorities and opaque-ID reparsing.
```

## Expected Result

Every mutable identity includes a generation/revision, attempts are distinct,
and projection code never infers typed identity by parsing opaque strings.

## Result

- Exit code: 1
- Duration: static inspection
- Summary: TaskRuntime retains `run_id`, `task_id`, plan revision, attempt, and
  full execution ID, but conversation writes have no generation/CAS token and
  `latestSubagentRunsByTask` groups only `(run, task)` then parses/compares the
  attempt suffix. Artifact identity contains run/task but no conversation or
  producer-execution field.
- Log/artifact: this report

## Deviations

Framework identity definitions were read only from committed `HEAD`.

## Conclusion

Supports `X-STA-01-P1-04`; it does not claim backend Task claim identity is
itself absent.

## Follow-Up

Define one typed end-to-end identity tuple and cross-revision projection tests.
